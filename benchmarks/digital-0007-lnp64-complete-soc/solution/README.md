# Build the solution

`mini_candidate/` is an intentionally incomplete negative-control fixture, not
a solution. It exists to pilot the verifier and contains no processor. Implement
the complete SoC and meet the verifier contract; reference assets define behavior,
not construction.
