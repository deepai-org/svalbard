# Four-channel UART controller benchmark

An easy, complete RTL-to-layout GF180MCU benchmark. The frozen contract is in
`instruction.md`; `make selftest` checks the package and both calibration
candidates.

`solution/golden_output` is a working implementation. Its checked physical
result is recorded in `solution/evidence/physical.json`.
