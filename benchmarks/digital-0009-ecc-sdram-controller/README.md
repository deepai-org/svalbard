# SECDED SDR SDRAM controller benchmark

An easy-to-medium complete controller benchmark with a frozen ECC mapping, a
real x32 SDR SDRAM command interface, and a GF180MCU RTL-to-layout gate.

`solution/golden_output` is a working implementation. Its checked physical
result is recorded in `solution/evidence/physical.json`.
