# Build the solution

This benchmark has no golden netlist or layout. Implement the complete port and
meet the verifier contract.

Code under `tests/reference/` defines externally observable results. It does not
define the circuit, microarchitecture, floorplan, or layout.
