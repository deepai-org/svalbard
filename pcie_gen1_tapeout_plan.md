Create a tapeout-ready GF180 PCIe Gen1 x1 endpoint for a deliberately constrained common-clock, short, low-loss channel by first freezing the legally sourced PCIe revision, 2.5 GT/s electrical limits, one diagnostic BAR, package/board/channel envelope, clocks, supplies, temperature range, independent software peer models, and measurable success criteria, then implement the protocol RTL and firmware independently of a reusable `wireline_serdes` PHY split into immutable `serdes_tx`, `serdes_rx`, `pll`, `cdr`, `phase_interpolator`, `termination`, `serializer`, and `deserializer` macros; prove the digital endpoint without FPGA or external lab hardware by combining formal properties, at least two independently implemented PCIe root-complex and transaction-layer software BFMs, compiled RTL simulation, protocol assertions, coverage-guided constrained-random tests, mutation testing, and error-injection regressions for enumeration, configuration, BAR accesses, memory transactions, resets, malformed packets, replay, retry, timeout, credit exhaustion, link loss, and recovery, while developing the PHY through simulated externally clocked PRBS/loopback at 1.25 GBd, extracted 1000BASE-X operation, externally clocked 2.5 GT/s operation, reference-assisted sampling, autonomous CDR, and finally PCIe electrical-idle, receiver-detect, training, enumeration, and transfer; because public GF180 data does not establish every required RF/high-speed model envelope, production distribution and correlation, pad/ESD bandwidth, substrate coupling, package/bond parasitic, aging, thermal, fill-neighborhood, or full-chip variation behavior, record each dependency as `public_qualified`, `provider_supplied`, `bounded`, or `unavailable`, obtain provider review where possible, use conservative bounded/adversarial sweeps rather than invented probability distributions, and make no unsupported compliance, interoperability, yield, ESD, or lifetime claim; carry the remaining uncertainty in silicon with topology-diverse CDR and reference-assisted paths, external reference and clock injection, independently selectable TX/RX, programmable bias/current/swing/common-mode/emphasis/termination/threshold/bandwidth/phase/VCO bands/loop settings, safe reset codes and hardware clamps, divided-clock and lock outputs, raw-symbol access, near/far and package loopbacks, PRBS generators/checkers, eye/phase scans, error and reset-cause counters, temperature/supply monitors, direct high-speed test structures, pad/interconnect open-short-thru-load de-embedding structures, and exact local replicas on both the product and risk-macro dies; verify schematic and extracted circuits over every defensible PVT, load, trim, package, channel, jitter, startup, sequencing, noise, crosstalk, simultaneous-switching, supply-injection, Monte Carlo, bounded missing-model, and fault-injection condition, cross-check critical results with independent simulators or models, require calibration range and margin away from trim rails, and propagate transistor envelopes through checked behavioral twins into full-link simulations; use the same generated vectors and scoreboards across RTL, generated RTL, synthesis, real pad-cell models, final gate netlist, and post-layout netlist, with lint, formal properties, CDC/RDC, equivalence, multi-corner STA, scan/DFT, SDF only if qualified, IR/EM, antenna, density/fill, ERC, hierarchical and flattened DRC/LVS/PEX, post-fill extraction, and provider-accepted precheck; select the qualified pad/ESD cells, package, bond plan, connector, reference source, termination, and PCB before analog layout freezes, co-simulate extracted die through pad, ESD, bond/package, board and worst channel using versioned electromagnetic and circuit models, provide alternate bond-outs and termination footprints, and ensure the slow SPI/JTAG control island, scan, external-clock mode, raw PHY diagnostics, and conservative configuration remain usable even if the CDR, PLL, SRAM, protocol engine, or automatic calibration fails; complete reviewable evaluation-board and fixture schematics/layouts without requiring their fabrication, software root-complex models and BFMs without requiring commercial peers or protocol analyzers, firmware, drivers, deterministic simulation automation, safe power-up procedures, calibration searches, test vectors, and raw-data schemas before submission; finally run an independent schematic/layout/pad/PDN/package/FMEA review, require every unresolved risk to have an observable, control, fallback, owner and bounded claim, explicitly label all results as pre-silicon evidence rather than measured compliance, generate the release layout only with `make candidate SUBMISSION=<id>`, run all checks on read-only copies, prove byte identity and layout XOR against the signed manifest after the exact pinned provider precheck, and upload only that immutable content-addressed candidate with its tool/PDK/deck/model/checksum/waiver evidence and laddered L1–L7 bring-up plan.
