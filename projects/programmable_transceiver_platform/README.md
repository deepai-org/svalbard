# Programmable analog/PHY companion

**Current operating decision:** RF and wired payload operation are mutually exclusive on the same chip. Both capabilities remain required; additional physical resource sharing is encouraged. See the [exclusive-engine policy](spec/exclusive-engine-policy.md), which supersedes simultaneous-operation requirements below. Model/RTL enforcement and resource rebudgeting remain implementation work.

Circuit-level overview: [full-chip block diagram (SVG)](docs/diagrams/transceiver-block-diagram.svg) · [PNG preview](docs/diagrams/transceiver-block-diagram.png).

**Working method:** [analog schematic design and simulation workflow](spec/analog-design-workflow.md), including an experiment template and evidence-linked lessons. Apply it to subsequent passes and improve it when experiments reveal a better method.

**Current priorities:** [three whole-platform feasibility gates](spec/feasibility-gates.md) addresses autonomous timing, RF/converter signal quality, and die/package coexistence, explicitly separating model-supported evidence from undisclosed or unbounded process uncertainties. The [risk log](spec/risk-priorities.md) preserves the individual experiments.

**Whole-chip view:** [block diagram and implementation map](spec/block-diagram.md) shows the proposed RF, wired, clock, host and shared-resource architecture, including missing implementations.

**Architecture review:** [CRC necessity review](spec/crc-architecture-review.md) recommends streaming payload without mandatory whole-frame CRC/quarantine and separate metadata/control protection. V2 is now the default integrated RTL; v1 remains a compile-time comparison. Both pass finite functional tests, while physical timing still fails. Historical pass entries below describe their measured states.

The full first-silicon target is the [programmable companion architecture](../../docs/roadmap/programmable-transceiver-pin-plan.md): one RF chain, one full-duplex wired PHY, shared generic resources, and a bidirectional regular-GPIO FPGA interface on a single wafer.space slot with 50 total terminals. External FPGA logic supplies protocols. This research project is not yet assigned a committed v1 shuttle slot.

**Accepted operating conditions:** the user permits narrow temperature and supply-voltage ranges, including a controlled operating point for peak performance. Numerical junction-temperature and per-rail limits are not yet fixed. See the [operating-envelope requirement](../../docs/roadmap/programmable-transceiver-pin-plan.md#accepted-operating-envelope); broad environmental qualification is not required, while process/mismatch, self-heating, supply noise and reliability still need closure.

Run `make transceiver-contract` for the current planning checks. The canonical machine-readable pin, area and transport budget is [spec/contract.json](spec/contract.json). The checker uses exact rational arithmetic, independent capacity per direction, conservative frequency offsets and whole-word static reservations. Its tests reject hidden/duplicate terminals, wrong bus direction, over-allocation, excessive simultaneous traffic, and insufficient host speed.

**Fast mathematical baseline:** run `make transceiver-math-fast` for the integrated twelve-executable acceptance suite. It exercises RF/wired traffic, external RF and nonlinear loopback quality, calibration/recovery, diagnostics, local MCU-style operation, warm retuning, and LO sideband propagation. [Model documentation](system_model/architecture_fast/README.md) defines the tested scope and assumptions; [the generated report](evidence/fast-common-acceptance.json) records current results and source hashes. This functional baseline precedes transistor-level and physical qualification.

## Broad implementation baseline

The user requested an end-to-end implementation pass to create a concrete refinement base. `make transceiver-baseline` now exercises the new synthesizable digital datapath, both bandwidth profiles, SPI, capture/playback, PRBS and calibration. Independent Python decoding checks RTL-generated frames. See [the implementation and macro contract](integration/macro-contract.md) for actual storage, interfaces and remaining obligations. [The 50-terminal top](integration/pt_chip.sv) connects the digital core to explicit physical macro black boxes; those black boxes and the ideal AFE models are not finished analog circuitry. No fabrication readiness is claimed.

## Pass 1: executable resource budget (superseded transport sizing)

Verified: 36 signal terminals plus 14 supply/ground; 12.92 mm² total planned core allocation. With provisional independent ±100 ppm source/host budgeting, each 32-word frame reserves two control words and:

| Mode | Host profile | Wired payload slots | I/Q payload slots |
|---|---|---:|---:|
| 1.25 Gb/s raw wired + 40 MS/s × I/Q × 12 bits | 125 MHz DDR | 17 | 13 |
| 2.5 Gb/s raw wired + 20 MS/s × I/Q × 8 bits | 156.25 MHz DDR | 26 | 4 |

Both allocations use all 30 payload slots in each direction. The Ethernet reservation rises from 16 to 17 slots because a nominally exact 16-slot allocation has no independent clock-offset margin. There is unused bit capacity inside some reserved slots, but no unallocated whole slot; new sample headers cannot be assumed free. These are sufficient average-rate reservations, not a proof of a bounded FIFO or working transport. Bit packing across words is required; 12-bit I/Q is not padded to 16-bit containers.

## Pass 2: scheduling and finite FIFO model (superseded frame geometry)

The [scheduling specification](spec/transport-scheduling.md) defines deterministic slots, frame staging, per-source valid counts, and continuous bit packing. One control word now carries two 5-bit valid counts so unused reserved slots cannot inject padding into live data. The other control word still needs its fast-control and protection design.

The executable model passes 48 phase/frequency cases over 512 frames each, plus negative tests for missing prefill, undersized FIFOs and insufficient service. Observed peaks are 260 ingress bits and 280 egress bits; the provisional allocations are 1,024 and 2,048 bits per source. These finite simulations assume source-matched playout and exclude CDC, errors, stalls and SSC; they do not prove final FIFO sizes. ±100 ppm remains a planning stress bound, not a PCIe spread-spectrum model.

## Pass 3: frame-integrity budget correction

The two-word control allowance could not cover counts, integrity, sequencing and fast controls together. The current contract therefore uses 64-word frames with 59 payload words. A reference codec defines the three-word header and CRC16/trailer and releases nothing until integrity, count and sequence checks pass. This supersedes the earlier frame geometry and observed FIFO peaks.

The Ethernet/RF mode reserves 33 wired and 25 RF slots at 125 MHz DDR (one idle slot). The PCIe/RF mode reserves 52 wired and 7 RF slots at 156.25 MHz DDR (no idle slot). Both retain the full declared data rates under the provisional clock margins. Top-profile useful capacity is 2.880859375 Gb/s per direction.

CRC-gated delivery is included in the updated 48-case finite queue matrix. Maximum observed ingress/egress occupancies are now 520/1,040 bits, within the provisional 1,024/2,048-bit allocations. The reference codec checks all 640 individual frame-bit flips, all legal PCIe-mode count combinations, sequence wrap/loss and malformed counts. These tests cover a word/frame-aligned error-free-timing codec, not frame acquisition or hardware.

Next small pass: explicit training/acquisition and safe fast-command semantics, followed by a hard check of physical GPIO capability and the PCIe control/round-trip latency budget. CRC buffering adds latency; a passing bandwidth sum alone is not enough to establish an external PCIe PHY. Independent consumer drift, SSC and CDC remain open.

## Pass 4: installed GPIO pad evidence

The [physical screen](spec/gpio-physical-screen.md) and retained [table report](evidence/gpio-liberty-screen.json) replace the ideal-GPIO assumption with 120 output-arc observations from four installed PDK corners. `make transceiver-gpio-screen` reproduces the digest-pinned, read-only extraction. Slow-corner 24 mA fast-slew rise time is about 1.72 ns at a 10 pF table-load coordinate, compared with the 3.2 ns host UI. This is not an eye/timing qualification.

A load-charging sanity check reaches 62.39 mA for eleven alternating outputs at 3.63 V and assumed effective 10 pF loading, before internal losses; the documented supply-cell DC capability is 60 mA. The single host supply/ground pair is therefore an explicit unresolved architecture risk. Next prioritize load-convention resolution, a clock/data pad transient harness and real FPGA-bank timing, alongside outstanding framing acquisition and controller latency work.

## Pass 5: native pad-pair transistor simulation

The [transient screen](spec/gpio-transient-screen.md) uses unmodified pad circuitry and a documented simulator compatibility mode. Four short TT/SS alternating/PRBS7 cases at 5 pF external load show no sampled threshold failures in a 0.4 ns assumed aperture; the worst measured voltage margin is 0.836 V. Four extractor regressions cover a corrected exact-threshold-crossing bug. The retained result is an explicit replay of the saved waveforms, not a claim of physical/FPGA qualification.

The important architecture result is current: the nominal alternating pair draws 13.27 mA from the I/O rail. Linear scaling to eleven identical outputs gives about 73 mA, above the documented 60 mA single supply-cell limit. The initial single host supply/ground pair is not an acceptable closed allocation for that case. Next prioritize host-bank power/return allocation, actual FPGA timing and a package/SSO model, preserving the full 50-terminal companion objective.

## Pass 6: host-bank power allocation

The [power-partition candidate](spec/power-partition.md) preserves 50 terminals and all data interfaces, but reassigns one CORE supply/return pair to HOST. Two physically distinct host segments own five and six fast outputs. A limited measured-current extrapolation with explicit provisional allowances reaches 51.77 mA against a 55 mA per-segment target; it does not assume equal sharing between parallel pads.

The tradeoff is explicit: CORE has one supply/return pair and a 48 mA ceiling that no implemented-core power estimate yet proves. Native supply-cell formal pins are audited, but physical rail isolation, ESD/clamps, current peaks and package behavior remain unverified. Six new tests exercise allocation mistakes, higher current and the rejected original host allocation. Next replace the ideal-supply extrapolation with high-voltage/current and package/rail evidence and obtain a real core-power estimate.

## Pass 7: real FPGA host frequency screen

The [ECP5 host screen](spec/fpga-host-screen.md) finds that the top transport profile exceeds this candidate's documented LVCMOS output-clock limit. A hypothetical 150 MHz DDR replacement also fails the PCIe/RF bandwidth budget (61 required payload slots versus 59). The full-rate design target is retained; ECP5 is only a lower-profile candidate, and no host is yet qualified. Next identify a full-rate ordinary-GPIO host and close its actual electrical and DDR timing constraints.

## Pass 8: actual host capacitance and reopened power budget

The [Artix-7 load screen](spec/artix-host-load-screen.md) replaces the earlier 5 pF assumption with an 8 pF nominal native-pad experiment, still excluding package/PCB. Both short cases retain positive threshold margin under an assumed aperture. However, the existing current extrapolation and allowances put HOST_B at 63.54 mA against its 55 mA planning ceiling. This reopens host power closure; the old 5 pF check cannot establish suitability for this host. Full-rate Artix GPIO timing is not yet qualified. Next investigate power/driver/load choices without reducing the full companion target.

## Pass 9: lower-current native drivers at unchanged bandwidth

The [driver selection experiment](spec/host-driver-selection.md) tests four nominal cases at the same 8 pF load and top data rate. Both 12/16 mA native driver modes retain positive sampled margins; the 12 mA setting reduces HOST_B's estimated current with unchanged allowances to 55.75 mA. This still exceeds 55 mA, so neither replacement is accepted yet. Six extractor/pin-mapping tests passed. Next investigate weaker drive and realistic package/rail loading, retaining the full terminal and throughput targets.

## Pass 10: 8 mA candidate passes nominal load/current screen

The [weaker-driver screen](spec/weak-drive-screen.md) adds four nominal cases at 8/10 pF and the unchanged top rate. At 10 pF, the HOST_B current estimate with unchanged allowances is 53.80 mA versus 55 mA, and the worst measured host-threshold margin is 0.661 V. Six existing measurement/pin tests and two new synthetic host-analysis tests pass. Carry 8 mA drive forward as a candidate; package/rail effects, full-bank switching, process/operating-window bounds and actual host timing remain unqualified.

## Pass 11: signal-path R/L sensitivity and extra-clock detection

The [signal-path screen](spec/signal-path-screen.md) adds two nominal 10 pF cases with selected series R/L and unequal data/clock inductance. Minimum host-threshold margin is 0.665 V, with no diagnostic clock-interval failures. Nine measurement/pin tests pass; saved pass-10 waveforms also pass the new extra-clock-edge check without changed margins. These are assumed lumped paths, not package qualification. Next add shared supply/return effects and full-bank switching.

## Pass 12: supply/return sensitivity exposed a numerical issue

The [shared-rail experiment](spec/supply-path-screen.md) adds moving output supply/return nodes and explicit R/L without extra decoupling. Four default-integration cases and two Gear timestep-comparison cases end without waveforms before the stimulus, under bounded timeouts. No electrical result is inferred. Ten measurement/pin tests pass; exact failed decks/logs are retained. Next capture startup behavior and isolate the rail topology before full-bank simulation.

## Pass 13: quiet startup isolation and two-sided uncertainty policy

Four [one-nanosecond startup diagnostics](spec/rail-startup-diagnostic.md) complete with ideal, supply-only, return-only and shared impedance. DC bias is sensible and no large rail excursion appears in that short interval; the later full-run slowdown remains unexplained. The user's [uncertainty requirement](spec/uncertainty-envelope.md) now explicitly requires adverse extremes and combinations while keeping unsupported bounds open. Next capture intermediate traces approaching the numerical slowdown.

## Pass 14: checkpointed rail isolation

The [checkpoint diagnostic](spec/rail-checkpoint-diagnostic.md) preserves intermediate traces through timeouts. Ideal rails complete 12 ns; supply-only, return-only and shared impedance save 1–2 ns before bounded timeout. Captured rail variation remains below a nanovolt while adaptive steps reach femtosecond scales. This narrows the issue toward numerical sensitivity without proving its cause. Next compare linear solvers on an unchanged circuit before extending the bank.

## Pass 15: solver comparison and stale-snapshot rejection

The [solver diagnostic](spec/rail-solver-diagnostic.md) verifies identical physical decks under SPARSE and KLU. Both complete the ideal control; shared rails time out under SPARSE and abort initially under KLU. An analyzer defect exposed by stale one-row operating-point files is fixed, with four regression tests. Saved artifacts are reanalyzed without rerunning simulations. Next isolate R versus L and inspect model contributions; numerical rail closure remains open.

## Pass 16: R/L and capacitor isolation; switch to full implementation baseline

The [element-isolation report](evidence/rail-element-screen.json) retains four diagnostics. Full pads time out with R-only and L-only rails; isolated native pad MOS capacitors complete 12 ns with R/L and R-only rails. Capacitors alone do not reproduce the problem. These diagnostic reductions cannot qualify the full pad.

The user requested an end-to-end implementation speedrun as the next baseline for refinement. Proceed across digital control, data transport, clock crossings, analog connections and system simulation rather than waiting for every isolated analog issue to close. Preserve all earlier physical risks and distinguish implemented RTL, executable analog models and candidate transistor circuits from qualified macros.

## Tapeout evidence still required

The project is not tapeout-ready. [risks.yaml](risks.yaml) records open constraints; [claims.yaml](claims.yaml) keeps the tapeout claim explicitly missing. Existing RF and wired block dependencies are candidate reuse sources, not qualified macros for this chip.

| Gate | Evidence needed | Current state |
|---|---|---|
| G0 | Provider/process/deck locks, custom pad/assembly eligibility, model validity, standards access | Not established for this chip |
| G1 | Complete electrical/resource/clock contracts and actual area/power budgets | Planning arithmetic only |
| G2 | Transport/helpers RTL, CDC/reset checks, FPGA adapters, protocol peer verification | Digital baseline passes finite functional tests; CDC, host adapters and protocol peers remain unqualified |
| G3 | Complete analog designs, extracted PVT/mismatch/noise and calibration evidence | No integrated companion evidence |
| G4 | Full padframe/layout, DRC/LVS/PEX, timing, package/board, power/reliability and coexistence closure | Missing |
| G5 | Independent review, provider prechecks, immutable submission, fixture and bring-up readiness | Missing |

Keep progressing through small verified passes toward the full companion. A green contract check is not O2 qualification, physical fit, interoperability, or signoff. Final completion requires each applicable gate's authoritative artifacts, not merely an empty list of checker errors.

## Pass 17: mapped digital area screen

`make transceiver-synthesis` now maps the baseline into installed GF180 7-track 3.3 V TT cells using the pinned digital image. The retained [area report](evidence/digital-area-screen.json) records source/library/netlist hashes and explicit exclusions. Structural checking after loading the actual cell port definitions reports no problems; all non-metadata cells are mapped standard cells.

Measured cell area is 1,054,435.7824 µm² (1.054 mm²): 27,662 physical cells, including 9,423 flip-flops. Storage maps to flops and multiplexers; no SRAM is assumed. Sequential cell area is 0.624 mm². Comparing against the combined 1.5 mm² digital plus 0.8 mm² memory planning allocation avoids counting these memories twice.

Illustrative 35%, 50% and 65% utilization assumptions require approximately 3.013, 2.109 and 1.622 mm² respectively. These are sensitivity points, not proven congestion bounds. At 50% utilization, only 0.191 mm² of region headroom remains before omitted physical implementation overhead. Consequently the result does not close area. Clock trees, routing/hold repair, physical interface logic and support cells remain additional obligations. The 3.3 V mapping point is not a frozen operating envelope.

No timing target was given to ABC; this is an area screen, not evidence for a 312.5 MHz host word clock. The mapped netlist still needs equivalence checking, actual clock constraints, placement/timing and switching-power analysis. Next address the timing and CORE-current implications of this concrete implementation before assuming the flop-based memories are practical. The full-chip analog and DDR macro obligations remain unchanged.

## Pass 18: first mapped timing diagnostics

`make transceiver-timing-screen` regenerates the actual mapped netlist, then runs OpenSTA with the two intended sets of host/wired/RF periods. SPI and reference periods are provisional 100 ns and 25 ns. No mode case analysis prunes logic; each run covers the generic implementation with that profile's clock periods. Reports explicitly separate same-domain register paths from asynchronous recovery checks. External I/O delays, interconnect, clock-tree skew/jitter and CDC qualification remain absent.

The [retained diagnostic](evidence/digital-timing-screen.json) **fails** timing and electrical checks. In the faster clock profile, reported same-domain setup slacks are −158.1555 ns (host TX), −23.6104 ns (host RX), −5.3831 ns (wired RX) and −3.5548 ns (wired TX). These are raw unplaced-netlist diagnostics, **not physical delay predictions or an fmax bound**. The host-TX worst path traverses `core.trst`, which drives 1,414 input pins without a distribution tree. The driver delay and downstream recovery/setup values are dominated by unrealistic loading/slew extrapolation. Both runs report 5,586 electrical violation rows, including propagated slew violations; that is not a count of distinct failing nets. Clock networks are ideal and also lack physical trees.

The run exposes 52 input bits without input-delay constraints, 128 output bits without output-delay constraints and 128 unconstrained endpoints. These are retained explicitly; no blanket false-path constraint hides them. Positive slow-domain slacks do not close these interfaces. The current mapping's failure does not yet justify shrinking bandwidth or declaring the architecture impossible. Next repair high-fanout control/reset distribution with actual cell sizing/buffering and explicit physical constraints, then reassess the data paths. Area and power must include those additions. G2/G4 remain open.

## Pass 19: bounded-fanout buffering experiment

`make transceiver-buffer-screen` regenerates the mapped RTL and inserts trees of actual GF180 `buf_4` cells on nonclock nets with more than 16 input-pin loads. This is an **unplaced diagnostic netlist**, not a physical reset tree or a tapeout implementation. Groups follow netlist order, without wire parasitics or geometry. The Liberty buffer output function is `I`; an exhaustive structural connection check collapses every inserted buffer and verifies every original cell-port connection is restored. Original top ports remain unchanged, the maximum nonclock fanout is checked, and Yosys checks the transformed netlist. This does not prove reset skew, analog timing or CDC correctness.

The [retained report](evidence/digital-buffer-screen.json) includes transformation provenance, added area, both timing profiles and mapped critical-register names. 1,426 buffers add 43,824.9728 µm², bringing cell area to 1.098 mm². At illustrative 50% utilization that is 2.197 mm² of the combined 2.3 mm² digital/memory allocation, before the still-missing physical interfaces and clock tree. No power estimate is established.

Electrical violation rows drop from 5,586 to 18. The remaining rows are one over-capacitance driver and its propagated slew failures, so a fanout-count limit alone is insufficient. Full-rate same-domain setup slacks remain −12.3455 ns host TX, −10.8635 ns host RX, −5.2929 ns wired RX and −3.5548 ns wired TX. Ideal-clock recovery failures remain too. These unplaced delays remain diagnostic, not physical speed bounds.

The host-TX critical path starts at `frame_tx.pos[3]` and ends at `frame_tx.crc[13]`: word selection and CRC evaluation share a cycle. Host RX runs from `frame_rx.rpos[2]` into `leftw[2]`, implicating slot decoding/count updates. Wired RX runs from the write Gray pointer into a storage bit; wired TX runs from the synchronized write pointer into the read Gray pointer. Next shorten these paths through predecode/pipelining and assess a wider internal datapath if necessary, preserving frame semantics and both full-rate profiles. Do not infer that a two-word-wide datapath alone would resolve a roughly 15 ns path. Timing-driven cell sizing and physical buffering still need to be evaluated.

## Pass 20: receive slot predecode

The receive-frame release scheduler now loads two 59-bit slot masks when a frame passes validation, then shifts them alongside payload release. This removes combinational schedule decoding from the same cycle as valid/count updates. It preserves the 64-word format, CRC quarantine, slot order, release latency, rate profiles and external interfaces. Faulted release stops advancing the masks; the next accepted frame reloads them. Mode remains frozen while armed.

`make transceiver-baseline` passes both full-rate loopbacks, corruption/sticky-fault/reset checks, SPI, helpers and finite-memory tests. Independent Python decoding still verifies 234/293 complete frames, 7,414/14,878 wired words and 2,371/1,190 I/Q samples. These finite tests do not establish all clock/phase/fault combinations.

The same regenerated mapping and fanout-buffering flow produces the [predecode timing report](evidence/digital-predecode-screen.json). Faster-profile host-RX same-domain setup slack improves from −10.8635 ns to −8.9803 ns. The critical path moves from `rpos` through slot decode/count update to `rpos[5]` through frame-bank selection into `iq_receive.mem[11][3]`. Thus a remaining memory-read path, not the old slot decoder, now limits this diagnostic. The change adds 118 flops; buffered cell area rises from 1.098261 to 1.114371 mm² (+0.016111 mm²). At illustrative 50% utilization, this occupies 2.229 mm², leaving approximately 0.071 mm² of the existing combined digital/memory allocation before omitted circuitry. Eighteen electrical violation rows remain.

This is a measured incremental improvement, not timing closure. Next pipeline or restructure frame-bank readout and TX selection/CRC while preserving throughput and checking added latency against the external protocol budget. A placement-aware buffering/sizing run, CDC analysis and CORE power estimate are still required; the full analog and host-DDR blocks remain unfinished.

## Pass 21: pipelined receive-bank readout

Receive readout now registers eight 16-word subreads and selects the required group in the following stage. The pipeline carries wired/IQ validity with the data, decrements source counts when issuing the read, and checks output readiness even after the final read was issued. CRC/sequence/count validation still precedes release. No pin, framing, data-rate or sample-width reduction is introduced. Receive latency increases by exactly one host-word cycle: 4 ns in the lower profile and 3.2 ns in the higher profile. This increment must be included in eventual external protocol latency closure.

`make transceiver-baseline` passes both streaming profiles and the independent frame codec, as well as existing control/helpers/memory checks. A new release-tail test refuses the last pending output after memory release stops, verifies the prior 51 wired and seven IQ words, and requires a sticky fault with no subsequent valid output. The faster core loopback observes one fewer wired word within the fixed simulation window because of the extra latency; ordering checks pass. This finite test is not a general FIFO/CDC proof.

The [retained pipeline screen](evidence/digital-read-pipeline-screen.json) reports host-RX setup slack improving from −8.9803 to −8.4266 ns under the same full-rate nominal unplaced diagnostic. Its critical path now runs from `frame_rx.pos[2]` to `frame_rx.crc[13]`, replacing the old memory-read path. The mapped implementation adds 83 flops and 0.007457 mm² of buffered cell area, totaling 1.121828 mm². At illustrative 50% utilization this consumes 2.243657 mm² of the 2.3 mm² digital/memory allocation, with physical clock trees/interfaces still omitted. Eighteen electrical violation rows remain.

CRC-related critical paths have appeared in both host directions across these experiments. Correction from the retained pass-21 report: that pass's current worst host-TX path was `frame_tx.pos[0]` to `iq_in.count[5]`, while host RX was CRC-limited. Next separate or restructure CRC computation and word selection while preserving the exact polynomial, bit order and trailer timing. The narrow operating-envelope allowance does not by itself resolve these logic-depth failures. Area, power, CDC, physical timing and the analog/DDR macro implementations remain open.

## Pass 22: parallel CRC equations and frame-boundary initialization

`verification/generate_crc.py` derives explicit balanced XOR equations over GF(2) for the existing ten-bit MSB-first CRC16 update (polynomial 0x1021). Both frame engines include the same generated function. CRC state is reinitialized at the frame's final word rather than multiplexing FFFF into the next frame's first update. Trailer sampling still uses the completed old CRC at that edge. No frame format, bit order, cycle count, external interface or throughput changes.

`make transceiver-baseline` now rejects stale generated equations and runs a SAT proof against the original bit-by-bit recurrence. All 26 input bits are unconstrained; the combinational equivalence proof passes. Both full-rate loopbacks, independent Python decoding, release-tail fault test and existing helpers/control/memory tests pass. The SAT proof covers the CRC function; sequential initialization and trailer timing are covered by finite frame tests, not a whole-design formal proof.

The [retained CRC screen](evidence/digital-crc-screen.json) measures full-rate host-TX setup slack of −10.3125 ns (previous −11.7219 ns) and host-RX slack of −7.9343 ns (previous −8.4266 ns). Buffered cell area increases by 0.004131 mm² to 1.125960 mm²; 18 electrical violation rows remain. TX's reported critical path ends at `frame_tx.crc[6]`; RX now runs from `iq_receive.rp[1]` to `unpacker.bits[24]`. Whole-design mapping changes also affect other paths, so these are netlist-level comparisons, not isolated CRC cell-delay measurements.

Next shorten TX word selection/CRC and the RX I/Q FIFO/packing path, while checking whether broader pipelining or a wider internal datapath is needed. At illustrative 50% utilization only approximately 0.048 mm² of the existing 2.3 mm² digital/memory region remains before missing clock trees, host interfaces and physical overhead. CORE power is still unmeasured. The area and power pressure must be resolved alongside timing, not hidden by adding pipeline registers indefinitely.

## Pass 23: clock-load screen and CRC architecture review

`make transceiver-clock-power-screen` regenerates the mapped netlist and sums characterized clock-input capacitance per actual clock domain. At nominal 3.3 V, CVf charging current is 19.34 mA in the lower profile and 24.50 mA in the higher profile. The [retained report](evidence/clock-power-screen.json) excludes internal flip-flop power, data switching, the clock tree/wires, leakage and GPIO pre-drivers. Thus this is a partial nominal accounting screen, not a total-power estimate or a corner bound. The higher profile already consumes approximately half the 48 mA CORE planning allowance in this one term. Its clock-source rail assignment remains an explicit physical implementation assumption.

The user's CRC question triggered a more consequential architectural review: mandatory full-payload transport CRC/quarantine was a design choice, not a requirement of the companion partition. The [review](spec/crc-architecture-review.md) recommends deterministic streaming with separately protected metadata/control and a measured host-link BER budget. Existing CRC RTL remains the tested comparison implementation; it has not been silently bypassed. The next architecture pass should specify that simpler transport before investing further in CRC-path optimization. External protocol checks are not substitutes for protecting every control or raw SDR sample, and no buffer/power saving is yet measured.

## Pass 24: executable streaming transport candidate

The [v2 candidate specification](spec/streaming-transport-v2.md) replaces full-payload CRC/quarantine with a five-word protected header followed by immediate payload release. It preserves the same 64 words, 59 payload slots, quotas, sample widths and data rates. A shortened extended-Hamming detection-only code protects counts, sequence and commands; invalid metadata cannot emit a command or payload in the aligned reference receiver. No correction is attempted. Raw payload errors are intentionally undetected, and the host BER requirement remains open.

`make transceiver-stream-codec` passes seven tests, including 20,875 one-/two-/three-bit header corruption patterns, all 1,308 legal count pairs, sequence wrap/loss and explicit undetected payload/four-bit-header error examples. [Evidence](evidence/streaming-codec-v2.json) records exact source hashes and scope. Existing v1 CRC RTL is retained unchanged; the next pass is a v2 streaming receiver implementation checked against this reference. No physical savings or protocol interoperability are inferred from a passing codec.

## Pass 25: standalone v2 streaming receiver

Implemented [pt_stream_rx.sv](rtl/pt_stream_rx.sv) with protected-header validation and same-edge streaming payload transfers, without payload quarantine. The [interface contract](spec/streaming-transport-v2.md#standalone-receiver-rtl-pass-25) explicitly defines readiness and fault behavior. No CRC v1 core behavior is changed yet.

`make transceiver-stream-rx` passes 251,151 cycles from independent Python vectors across 20,888 reset cases; all 20,875 one-/two-/three-bit header corruption patterns and 1,308 legal count pairs are included. Refused wired and IQ transfers fault without delivering the rejected word. [Evidence](evidence/streaming-rx-v2.json) records source/vector hashes. Structural elaboration passes; physical timing, acquisition/CDC and whole-chip savings remain unproven. Next implement v2 TX/acquisition and integrate both ends, then compare actual area, timing, current and latency against v1.

## Pass 26: standalone v2 transmitter

Added [pt_stream_tx.sv](rtl/pt_stream_tx.sv), retaining transmit snapshot staging while replacing the payload CRC/trailer with the v2 protected header. The first frame is empty; counts and controls snapshot for the following frame, preserving their association. Mode and FIFO ownership assumptions are explicit in the [TX contract](spec/streaming-transport-v2.md#standalone-transmitter-rtl-pass-26).

`make transceiver-stream-tx` passes 84,096 checked word cycles / 1,314 frames across both profiles. Independent Python vectors cover all 1,308 legal count pairs plus over-quota clamping/drain cases, sequence wrap, padding and mid-frame command-input changes. [Evidence](evidence/streaming-tx-v2.json) records source/vector hashes. Structural elaboration passes. V1 remains the integrated core; next add acquisition and integrate the verified v2 blocks before measuring full-core timing, area, current and latency. No total storage saving is claimed from the standalone TX.

## Pass 27: coordinated v2 startup wrappers

Added [pt_stream_link.sv](rtl/pt_stream_link.sv): a finite eight-word TX preamble and RX startup matcher, sticky acquisition timeout, and explicit-reset-only recovery. The [startup contract](spec/streaming-transport-v2.md#coordinated-word-aligned-startup-pass-27) distinguishes word-frame acquisition from unfinished DDR alignment, CDC, clock-presence detection and host enable sequencing.

`make transceiver-stream-link` passes both profiles through startup, streaming, metadata fault, timeout and restart; both hierarchies pass structural elaboration. V1 is still the integrated core, so these results do not establish whole-chip savings or physical speed. Next integrate these v2 wrappers into the existing streaming core and retest source ordering, faults, storage, area, timing and current at unchanged rates.

## Pass 28: v2 core integration and measured comparison

V2 startup/TX/RX now replace frame transport in `pt_core` and `pt_digital` by default. Compile-time `STREAM_V2=0` retains v1; no runtime dual-protocol logic is required. `make transceiver-baseline` checks all four variant/rate combinations, protected-transport fault/reset, existing helper/control/memory tests and independent Python decoding for both formats. V2 decodes 234/293 clean complete frames with 7,416/14,881 wired words and 2,371/1,190 I/Q samples. Contract version 4 describes v2; the old finite phase/frequency queue model is explicitly tied to `legacy_v1_scheduling`, not presented as a v2 bound. `make transceiver-contract` passes.

The [integrated report](evidence/integrated-v2-screen.json) retains the same regenerated nominal mapping/buffering/timing screen and clock-pin-load calculation. Compared with the last integrated CRC build, buffered cell area drops from 1.125960 to 0.964299 mm² and flop count from 9,629 to 8,119. Declared payload/storage arrays drop by the 1,280-bit receive frame bank to 7,296 bits; other removed registers include its pipeline and control. The nominal clock-pin CVf estimate drops from 19.34/24.50 mA to 15.36/19.51 mA across the two profiles. That excludes cell-internal/data/tree/wire/leakage and pad pre-driver consumption; it is not total CORE power.

Timing remains failed. High-profile host TX setup improves from −10.3125 to −9.2393 ns, while host RX worsens from −7.9343 to −11.1264 ns. The new RX critical path is `frame_rx.payload.pos[2]` to `iq_receive.mem[11][0]`: direct slot decoding now feeds FIFO writes. The limited electrical report has zero violation rows, but ideal-clock recovery failures, unconstrained external interfaces, absent placement/interconnect and unqualified clocks prevent a timing claim. At illustrative 50% utilization, 0.964299 mm² cell area needs 1.928597 mm² of the 2.3 mm² region; actual placement remains unproved.

Next predecode the v2 receive schedule or otherwise shorten that path, then address FIFO write/decode and TX selection paths without sacrificing the measured area/current reduction. Startup enable ordering and a clock-presence watchdog remain obligations. Full analog/DDR macro implementation and physical signoff are still missing; the full companion objective is unchanged.

## Pass 29: v2 receive schedule predecode

The v2 receiver loads wired/IQ slot masks on successful header acceptance and shifts them through its 59 payload slots. Transfer qualification uses their low bits directly, removing position-counter schedule decoding from FIFO writes. Masks clear naturally after the last slot and reset to zero; a failed header cannot load them. No payload buffering, extra latency, frame-format change or lower data rate is introduced.

`make transceiver-stream-rx` again passes 251,151 reference-checked cycles, including all corruption/count cases. `make transceiver-baseline` passes both v1/v2 profiles, independent decoding and existing fault/helper tests. The [retained integrated screen](evidence/integrated-v2-predecode-screen.json) records unchanged payload ordering and the physical comparison.

High-profile host-RX setup slack improves from −11.1264 to −10.0815 ns, but asynchronous recovery slack worsens from −1.4435 to −2.4561 ns. The worst RX setup path now runs from `payload.header[14]` to `payload.wire_slots[15]`, implicating header validation and schedule loading rather than payload-slot decode. Buffered area increases from 0.964299 to 0.974386 mm²; mapped flop count increases by 117 to 8,236. High-profile nominal clock-pin charging current rises from 19.51 to 19.92 mA. This remains below v1's 24.50 mA partial estimate, but is not total power or a worst-case bound. The limited electrical report still has zero violation rows; physical timing is not closed.

Next separate header validation from wide schedule loading or precompute validation over the header-word interval. Preserve detection of all tested metadata errors and same-edge payload readiness. Before extrapolating achievable speed from these area-oriented mappings, also evaluate timing-driven mapping/sizing and physical buffering. A register-heavy optimization must continue to account for core power and available area; no amount of passing functional simulation substitutes for these missing checks.

## Pass 30: schedule-preload experiment rejected

Tested loading the fixed receive schedule at header word zero, with a separate acceptance bit preventing transfers until the complete header passed. The candidate passed all 251,151 receiver vectors and the integrated v1/v2 baseline, without added payload latency. However, the comparable nominal mapping/buffering screen did not demonstrate a timing benefit: full-rate RX setup slack worsened from −10.0815 to −10.4152 ns and TX from −10.4320 to −11.0739 ns. RX's new critical path began at `core.er` and ended at `wire_out.mem[112][0]`, rather than header validation. RX recovery improved slightly from −2.4561 to −2.4311 ns but still failed. Buffered area increased by 39.5136 µm² and one mapped flop; clock-pin charging current was essentially unchanged.

The [rejected experiment report](evidence/rejected-v2-preload-screen.json) retains the candidate, source hashes and measurements. Production RTL was restored **byte-for-byte** to the pass-29 receiver, verified against its retained SHA256. Therefore pass 29 remains the current implementation/evidence baseline; scratch synthesis outputs from pass 30 describe the rejected candidate and must not be mistaken for current RTL. Reproducible mapping targets regenerate before use.

This does not prove that schedule preloading is intrinsically slower. Whole-design area-oriented mapping and netlist-order buffering changed other paths, including untouched TX logic. It shows that this experiment has no demonstrated aggregate timing benefit under the current flow. Next evaluate timing-driven mapping/sizing and physically meaningful reset/enable distribution before accumulating more RTL timing tweaks. Keep functional tests and full-rate requirements unchanged. No timing, power or tapeout gate is closed by this experiment.

## Pass 31: explicit mapping target and sizing experiment

No RTL change. Added `make transceiver-delay-mapping` with a 3,200 ps ABC target, and `PT_ABC_SIZING=1 make transceiver-delay-mapping` to additionally invoke ABC's built-in buffering/upsize/dnsize sequence. Separate output directories keep these experiments apart from default synthesis. Mapping options are recorded in area/timing reports. The sizing case uses an illustrative `buf_1` driving cell and 5 fF load on ABC boundaries; these are not actual board I/O constraints, do not express all sequential timing budgets, and do not replace multi-clock STA.

The [target-only report](evidence/delay-target-only-screen.json) matches pass-29 area, cell count and reported timing exactly. The [sized report](evidence/delay-sized-screen.json) improves high-profile TX/RX setup slack from −10.4320/−10.0815 ns to −8.0069/−8.2062 ns. Buffered cell area decreases from 0.974386 to 0.956517 mm² with unchanged 8,236 flop count. Wired RX setup improves from −7.7570 to −4.9870 ns, and wired TX remains failed at −4.0737 ns. RX's limiting path still runs from header validation into a schedule-mask register. Recovery remains failed (host TX/RX −3.8757/−2.5349 ns). The same limited electrical report has no violation rows; this is not physical closure.

**Terminology correction:** earlier entries called the baseline mapping “area-oriented.” The pinned Yosys `help abc` shows the default `&nf` mapping flow already performs mapping optimization; the established difference is absence of an explicit delay target and driver/load constraints, not proof that it optimized area alone. `-constr` enables the additional buffer/upsize/dnsize stages, verified in this run's ABC log. Supplying `-D` alone did not improve this design.

The sizing variant is a diagnostic, not an adopted signoff netlist. No full mapped-netlist equivalence, placement, extracted wires, reset/clock distribution, realistic port loading or total power evidence exists yet. Default RTL and default synthesis remain unchanged. Next qualify the promising mapping variant and introduce physical distribution/placement constraints, while addressing the still-deep header and FIFO logic paths. Do not interpret nominal unplaced timing as either a guaranteed speed ceiling or a proof that the full-rate target works.

## Pass 32: mapped sizing-equivalence proof

`make transceiver-mapping-equivalence` compares the explicit-delay mapped design with the sized-and-buffered variant from pass 31. It requires the two existing experiment artifacts, records their hashes and does not silently substitute current RTL. Regenerate them with `make transceiver-delay-mapping` and `PT_ABC_SIZING=1 make transceiver-delay-mapping` when needed. Register identities, types, parameters and port directions must match. The harness cuts all 8,236 corresponding register Q outputs into shared arbitrary inputs, then compares all 18,692 register-input bits (including D, CLK and reset/set pins) plus 128 top-output bits using the pinned Liberty Boolean cell models.

The [retained proof](evidence/mapping-equivalence.json) passes the SAT check and a negative control that deliberately inverts status bit zero. The negative control initially caught a harness error: port and net-name aliases were inconsistent after the deliberate mutation, yielding an invalid comparison. Both are now updated together and checked for consistency; positive and negative checks were rerun successfully. No earlier misleading pass is treated as evidence.

This establishes two-state combinational next-state/output equivalence of these mapped artifacts under matching arbitrary register state, with matching sequential cell types. It supports using the sizing experiment's functional logic for further work. It does **not** establish behavioral RTL-to-gates equivalence, X/metastability/glitch behavior, physical clock/reset correctness or timing closure. The designs have no net initialization attributes in these artifacts; corresponding initial state remains the proof relation. Both high-speed setup and recovery checks still fail. Next pursue physical distribution/placement and timing constraints, retaining mapped equivalence as a regression when netlists change.

## Pass 33: first legal digital-region placement

`make transceiver-placement-screen` checks the sizing candidate against both its characterized Verilog hash and the mapped-equivalence JSON hash, then places it with the pinned GF180 7-track nominal technology/cell LEFs. The experiment requests a local 1500 × 1530 µm core region (2.295 mm²), within the 2.3 mm² digital-plus-memory allocation. Its enclosing local die boundary and abstract macro-interface pins are experiment scaffolding, not a whole-chip padframe or final macro position. Global placement uses a 0.55 target density; realized utilization is reported separately.

The [retained placement screen](evidence/digital-placement-screen.json) records all 26,269 cells placed and 0.956517 mm² physical cell area, matching the characterized netlist. Realized row utilization is 41.8272%. OpenROAD detailed placement/checking completes, and an independent geometry checker verifies every cell's row, site grid, bounds, orientation and non-overlap. Deliberately off-grid, out-of-bounds and overlapping layouts are rejected. DEF/ODB and geometry dumps are retained under `scratch/transceiver-placement`, with hashes in the report.

This establishes a legal standard-cell packing for the selected digital region. It does not establish routability or final region sufficiency: no analog macros/pads, PDN/taps/decaps/fillers, clock/reset distribution, routing/parasitics or timing repair are included. All prior high-speed setup/recovery failures remain open; no post-placement speed claim is made. Next introduce explicit clock/reset distribution and routing/RC constraints, then reassess placed timing and current before treating this footprint as a qualified macro.

## Pass 34: placed signal-interconnect timing screen

`make transceiver-placed-timing` verifies the retained ODB hash and analyzes the same placed sizing candidate with and without placement-estimated signal parasitics. Both runs use nominal 3.3 V cells, the high-rate clock profile, and ideal clocks. The signal RC estimate uses the installed nominal Metal3 technology-LEF model; this is an illustrative uniform-layer placement estimate, not routed/coupled extraction or a proven uncertainty bound.

The [retained comparison](evidence/placed-timing-screen.json) reproduces the earlier zero-wire host TX/RX setup slacks exactly (−8.0069/−8.2062 ns). Estimated signal wires worsen these to −16.2450/−12.2338 ns. Host TX/RX recovery worsens to −6.1974/−4.6833 ns. Electrical violation rows rise from zero to four. Those violations and unqualified wire geometry make the delays diagnostic, not exact achievable-frequency predictions.

This establishes that legal cell packing does not address the interconnect problem. It does not close CTS, reset distribution, routing, external interfaces or any physical timing gate. Next repair distribution and evaluate layer/RC sensitivity with actual routing assumptions before drawing architectural speed conclusions. Clock-tree area, current and skew remain additional costs; no change to target rates or chip scope is made.

## Pass 35: placement-aware electrical repair

`bash projects/programmable_transceiver_platform/verification/run_placement_repair.sh` verifies the original placement hash, reads that database through a read-only mount, and runs OpenROAD `repair_design` using the same nominal Metal3 signal-RC estimate and ideal high-rate clocks. It then legalizes, checks placement, re-estimates parasitics and retains separate repaired ODB/DEF/Verilog artifacts. The original placed candidate remains unchanged.

The [repair screen](evidence/placement-repair-screen.json) records six inserted buffers in four nets, clearing all four reported capacitance violations. Cell count becomes 26,275 and area 0.956635 mm² (an increase of 118.5408 µm²). All cells are placed and OpenROAD's placement check completes. Host TX/RX setup slacks remain −16.2450/−12.2359 ns; recovery remains −6.1974/−4.6833 ns. Electrical repair therefore does not resolve the high-speed paths. Zero reported electrical violations does not establish physical timing closure.

This transformed netlist is experimental: independent geometry checking and mapped equivalence have not yet been rerun. Uniform-layer estimated RC, ideal clocks, unconstrained external IO, missing CTS/PDN/routing and incomplete analog/DDR macros remain limitations. Next inspect the critical paths for logic-depth versus wire-delay contributions and evaluate timing-driven restructuring or optimization; retain the full rates and functionality while testing architectural changes. Before adopting any physical optimization, prove its functional correspondence and recheck geometry. This pass does not establish bounds on unknown process/interconnect behavior.

## Pass 36: critical-path cell/net decomposition

`bash projects/programmable_transceiver_platform/verification/run_path_delay.sh` analyzes the retained experimental repaired placement with zero signal RC and with nominal Metal3 placement-estimated RC. Reports include every input pin, permitting separation of intra-cell timing arcs from inter-instance net arcs. The parser checks that their sums reproduce arrival times within 0.0001 ns, resolves endpoint register names from the repaired Verilog, checks all eight domains, and binds inputs/logs/scripts by hash. The [retained report](evidence/path-delay-screen.json) includes individual arcs for the worst reported path in each domain/group.

The estimated-RC host-TX path runs from `core.wire_in.rb[0]` into `core.stream_v2.frame_tx.payload.banks[8][8]`. It contains 17 cell arcs including launch clock-to-Q, totaling 18.9089 ns, plus 0.0265 ns explicit net delay. Host RX instead runs from `core.iq_receive.rp[2]` to `core.unpacker.bits[25]`: 15 cell arcs including launch, totaling 14.9211 ns plus 0.0103 ns explicit net delay. Zero-RC TX has the same source but a different bank endpoint, with 10.7057 ns cell delay; zero-RC RX selects the header-to-schedule path. These are worst-path comparisons, not subtraction of matched physical paths.

Small explicit net delay does **not** exonerate interconnect: library cell delay includes output-load capacitance and incoming-slew effects. The previous electrical repair did not target setup, and both intrinsic logic depth and load-dependent cell delay need attention. The existing FIFO uses combinational indexed readout, connecting pointer decoding/muxing directly to downstream datapath work. Next evaluate a registered/prefetched FIFO output or staged bank readout with explicit valid/count/ownership semantics, full-throughput back-to-back transfers, empty/refill/reset tests and latency accounting. Do not simply register `dout`: that would change which word is consumed on a pop. Nor does a single extra stage establish closure of a roughly 19 ns path against 3.2 ns.

This pass is diagnostic evidence, not a functional RTL change or physical qualification. The repaired netlist still requires equivalence and independent geometry verification before adoption; clocks remain ideal, RC is illustrative, external IO is unconstrained, and analog/DDR macros remain incomplete. No rate, pin, RF/wired capability or fabrication-scope reduction is made.

## Pass 37: stage transmit-bank writes

The default v2 TX now registers selected FIFO data and its destination address before writing the inactive transmit bank on the following edge. This separates FIFO readout/source selection from bank write distribution. Pop edges, payload order, frame output and command snapshots remain unchanged. The delayed last-slot write carries its original bank address across the frame boundary; reset invalidates any pending write. No transport cycle is added externally, and no target rate or payload quota is reduced.

`make transceiver-stream-tx transceiver-baseline` passes the 84,096-cycle independent TX codec comparison, all four integrated transport/rate variants and existing control/helper/memory/release checks. `make transceiver-tx-stage-compare` additionally compares the staged TX with retained pre-stage RTL for 69,824 cycles, resetting at all 64 frame positions in both modes with deterministic varying inputs. The comparison initially could not launch because the container lacks `sed`; the wrapper now generates the reference module on the host and the final run passes. These finite comparisons are not whole-design formal equivalence or CDC qualification.

`PT_ABC_SIZING=1 PT_MAPPING_OUT="$PWD/scratch/transceiver-tx-stage-mapping" bash projects/programmable_transceiver_platform/verification/run_delay_mapping.sh` retains separate artifacts without overwriting the prior placed candidate's mapping. The [staging screen](evidence/tx-stage-screen.json) uses the same nominal zero-wire/ideal-clock sizing screen as pass 31. Host TX setup improves from −8.0069 to −7.2009 ns; host RX changes from −8.2062 to −7.2193 ns through whole-design remapping, not an RX RTL change. Area rises from 0.956517 to 0.963680 mm², and flops from 8,236 to 8,254. The new worst TX path is synchronized write-Gray pointer to snapshot remaining count (`wire_in.wg2[5]` to `frame_tx.payload.leftw[1]`). Other paths are not uniformly better: wired TX worsens from −4.0737 to −4.4749 ns. Timing remains failed.

Retain the staged RTL as an incremental candidate, not a physically qualified implementation. Historical placement, mapped equivalence and power evidence remain tied to their old hashes and do not qualify this change. Next assess count/snapshot decoding and FIFO readout, then remap/place and re-estimate area/current/timing with all changed paths included. Full analog/DDR implementations, clock/reset distribution, interconnect uncertainty bounds and external protocol closure remain required for the original full-chip objective.

## Pass 38: count-snapshot staging experiment rejected

Tested an additional TX stage that captures raw FIFO counts and mode at word zero, then clamps quotas and initializes remaining counts at word one. Commands still snapshot at word zero and payload consumption starts at word five. The candidate therefore preserves the intended frame membership while separating Gray-to-binary/subtraction from quota comparison. Candidate and pre-experiment RTL are retained in `evidence/experiments/tx-count-stage.sv` and `tx-before-count-stage.sv`.

Independent TX vectors now change live counts immediately after the snapshot edge, in addition to changing commands. The count-stage candidate passes 84,096 vector cycles, all four integrated baseline variants and 69,824 cycle-exact comparisons against the original pre-bank-stage transmitter, including resets at every frame phase in both modes. This is finite functional evidence, not formal equivalence. The strengthened count-input vectors remain in the regression.

The [retained experiment](evidence/rejected-count-stage-screen.json) uses the same nominal, zero-wire, ideal-clock, 3200 ps target/sizing screen as pass 37, with separate artifacts under `scratch/transceiver-count-stage-mapping`. Host TX setup changes from −7.2009 to −7.2583 ns; host RX from −7.2193 to −8.2650 ns. Cell area falls slightly from 963,679.6288 to 963,170.3424 µm², while flops increase from 8,254 to 8,268. The worst TX path moves from count calculation to `frame_tx.payload.pos[4]` through scheduling/pop control into `wire_in.rg[4]`. Recovery and some other domains improve, so this is not a uniform regression; nevertheless it provides no measured improvement to the principal host setup bottleneck and adds state. No placed or power benefit has been established.

Rejected the candidate as the default and restored pass-37 TX byte-for-byte against its retained source hash. Both strengthened independent vectors and the reset-phase comparison pass again on restored RTL. Existing physical evidence remains tied to the earlier placed implementation; neither this experiment nor pass 37 is physically qualified. Reproducing the candidate requires substituting its retained RTL in an isolated source copy, then running the delay-mapping flow with `PT_ABC_SIZING=1` and a separate `PT_MAPPING_OUT` directory.

Next investigate predecoded transmit scheduling/pop control and FIFO pointer-update depth. A higher-level caution remains: migrating the worst path from one roughly 10 ns cone to another does not establish a viable 3.2 ns architecture. Changes must eventually demonstrate simultaneous closure across all domains, with actual wire loading, clock overhead and protocol latency. Wider/lower-frequency internal processing may need evaluation if local staging cannot do that; it must preserve external throughput and all analog/PHY capability targets rather than lower the goal.

## Pass 39: combine predecoded ownership with count staging

Tested a two-bit register that decodes next-word TX ownership one cycle ahead, removing current-position schedule decoding from the same cycle as FIFO pop/pointer updates. Idle legacy schedule positions naturally cover the v2 header and frame wrap. Mode remains frozen while armed. The standalone variant passes independent codec vectors, integrated baseline tests and reset-phase comparison, but host TX remains count-limited and slack worsens to −7.7026 ns. Its lower 0.959008 mm² area alone does not solve the timing objective.

Then combined this predecode with the previously rejected raw-count snapshot stage. Counts/mode and commands snapshot at word zero; saved counts are clamped at word one, before any payload pop. The combined variant also passes 84,096 independent TX vector cycles (live counts and commands change after the snapshot), all four integrated variants, and 69,824 comparison cycles against original TX including resets at every frame phase in both modes. These are finite functional checks, not whole-design formal equivalence.

The [two-experiment report](evidence/owner-stage-comparison.json) retains source/log hashes, full nominal zero-wire ideal-clock timing screens, and clock-pin charging estimates. Compared with pass 37, the combined variant improves host-TX setup from −7.2009 to −7.0654 ns, but host RX worsens from −7.2193 to −7.5014 ns and TX recovery from −3.5981 to −3.8887 ns. Cell area changes from 963,679.6288 to 963,539.1360 µm²; flops increase from 8,254 to 8,270. The worst host-TX path now runs from `rf_in.rb[0]` to `packer.bits[18]`. This supports investigating RF FIFO readout/packing next; it does not establish closure of any high-speed domain or a uniform timing improvement.

Retain the combined RTL as the next candidate: it separates two identified control cones without changing the external schedule or payload rates. Both standalone and combined experiment RTL are preserved under `evidence/experiments/tx-owner-stage.sv` and `tx-owner-count-stage.sv`. The combined mapped artifacts live separately under `scratch/transceiver-owner-count-stage-mapping`. Nominal clock-pin charging current increases from 15.7278/19.9797 mA to 15.7719/20.0349 mA across the two profiles. These estimates omit internal/data/tree/wire/leakage and pad pre-driver power; remaining arithmetic rail headroom is not a power guarantee.

All prior placement, mapped-equivalence and physical timing artifacts remain bound to older netlists. The new candidate still needs those checks, alongside a redesign of deeper readout/packing paths if local staging cannot meet the target. No extrapolation from small zero-wire gains to routed operation is justified. Analog/DDR macro completion, process and RC uncertainty bounds, clock/reset/PDN design and external protocol qualification remain open obligations for the full chip.

## Pass 40: RF packer input holding register

Added one ready/valid sample holding register before reservoir packing. FIFO readout now terminates at that register, while shifting/inserting the previous sample is a separate path. The stage can accept a replacement on the insertion edge, and stores at most one extra sample when output stalls. Reset discards pending data. Mode is frozen while armed. The local initially empty path adds one host cycle; source acceptance and possible frame-boundary assignment change, so this is not claimed cycle-equivalent to the prior packer.

`make transceiver-pack-stream` uses an independent queue of accepted bits to check 40,062 cycles, 10,068 accepted samples and 19,487 output words across both widths. It includes continuously offered traffic, random source gaps, long/random output stalls, reset while data is pending, output stability while stalled and final draining. The first 10,000 continuous cycles must accept at least 2,800 24-bit or 3,700 16-bit samples, checking sustained rate rather than just eventual ordering. Injected output-bit corruption and ignored backpressure both fail the checker as intended. The negative-control generator initially matched both packer/unpacker conditions; scoping it to the packer fixed the harness, and both final controls pass. All four integrated variants and existing baseline checks also pass.

The [retained nominal screen](evidence/pack-stage-screen.json) reports host-TX setup changing from −7.0654 to −6.9688 ns. The worst TX path now spans I/Q FIFO count bits (aliased as `frame_tx.payload.iq_count[3]` to `[5]`), rather than RF FIFO readout into packing. RX worsens from −7.5014 to −7.7232 ns through whole-design remapping. Cell area rises from 963,539.1360 to 964,926.5024 µm², with 25 additional flops (8,295 total). Nominal clock-pin charging current rises from 15.7719/20.0349 to 15.8409/20.1212 mA; this still excludes internal/data/tree/wire/leakage and pad pre-driver loads and does not close CORE power.

Retain the packer stage as a candidate; its small zero-wire gain is not evidence of a qualified physical improvement. The recent sequence of critical-path changes demonstrates multiple long cones, not a nearly closed design. Next investigate FIFO count/flag/pointer structure and consider wider internal processing if staged word-rate logic cannot meet full throughput. Updated placement, mapped equivalence, reset/clock distribution, complete interface timing, end-to-end latency and queue bounds are required. All analog/DDR completion and full-chip signoff obligations remain open; no target-rate or capability reduction is made.

## Pass 41: register synchronous FIFO full/empty flags

The I/Q input and receive FIFOs now maintain registered full/empty state from accepted transfers. This removes current-count equality decoding from the same path that qualifies transfers and updates the next count. Count, depth, memory readout and sticky errors retain their previous behavior. Flags are updated to match post-edge occupancy, not delayed an extra cycle. Async FIFO logic is unchanged.

`make transceiver-sync-fifo-flags` passes 26,864 cycles against an independent queue/count/error model, including all 33 occupancies × four push/pop combinations, draining beyond empty, random pointer wrap and reset. Simultaneous push/pop at full and empty preserves the old reject/accept behavior and sticky error flags. A mutation that asserts full one slot early is rejected. Tests exercise the actual installed W=10/A=5 configuration; they are not a proof for arbitrary parameters or physical CDC. All four integrated variants and other baseline checks pass.

The [retained screen](evidence/fifo-flags-screen.json) uses the same nominal zero-wire/ideal-clock mapping conditions as pass 40. Host TX setup improves from −6.9688 to −6.0369 ns, and host RX from −7.7232 to −6.9512 ns. The worst TX path is now `wire_in.wg2[5]` through Gray-to-binary/count arithmetic into `frame_tx.payload.snapshot_wire_count[6]`. Wired RX/TX setup worsens from −4.8994/−3.7544 to −5.0503/−3.8959 ns through whole-design remapping, so this is not a uniform improvement. High-speed setup and host recovery still fail.

Cell area increases from 964,926.5024 to 967,053.6512 µm², with four additional flops (8,299 total). Clock-pin charging current increases from 15.8409/20.1212 to 15.8520/20.1350 mA. This excludes internal/data/clock-tree/wire/leakage and pad pre-driver consumption; no total-power qualification follows. Separate artifacts are under `scratch/transceiver-fifo-flags-mapping`; pre-change RTL is retained in `evidence/experiments/fifo-before-registered-flags.sv`.

Retain registered flags as the next candidate. Next investigate asynchronous FIFO count visibility without breaking Gray-pointer CDC conservatism or frame snapshots, and recheck actual placed timing before treating these nominal improvements as physical progress. Additional synchronizer latency can change available counts and buffer bounds; it must not be hidden as an equivalent combinational optimization. All mapped equivalence, clock/reset/PDN/routing, interface latency, analog/DDR implementation and full-chip signoff obligations remain open.

## Pass 42: balanced Gray-count conversion rejected

Tested a parallel-prefix Gray-to-binary function in the asynchronous FIFO count outputs. Successive XOR shifts of 1, 2 and 4 replace the serial recurrence at the installed pointer widths. This changes no register, reset, synchronizer, count latency or FIFO acceptance rule. The experiment addresses combinational structure only; it neither adds CDC protection nor permits relaxed Gray-bus routing constraints.

`make transceiver-gray-prefix-proof` extracts the actual old/candidate functions and proves Boolean equivalence with Yosys SAT for every input at A=4,6,7, the installed asynchronous FIFO depths. Each width has a deliberately inverted-output negative control that must fail proof. The first harness incorrectly built a miter before lowering function processes; its negative control unexpectedly passed, invalidating that attempt. Lowering processes before miter construction fixes the harness: all three positive proofs pass and all three negative controls fail as expected. This is a two-state combinational proof, not whole-FIFO or metastability qualification. The proof now uses retained experiment sources so it remains reproducible after rejection. All integrated baseline variants also pass on the candidate.

The [retained experiment](evidence/rejected-gray-prefix-screen.json) shows cell area falling from 967,053.6512 to 964,535.7568 µm² with the same 8,299 flops, but host TX/RX setup worsening from −6.0369/−6.9512 to −6.7420/−9.3032 ns. Wired TX worsens from −3.8959 to −5.8125 ns, and host-TX recovery also worsens. The worst TX path still runs from `wire_in.wg2[5]` to `frame_tx.payload.snapshot_wire_count[6]`. This is a whole-design remapping result; writing a shallower Boolean expression alone did not produce a measured high-speed benefit.

Rejected the candidate, retained it under `evidence/experiments/fifo-prefix-candidate.sv`, and restored pass-41 FIFO RTL byte-for-byte against its evidence hash. The independent FIFO scoreboard and its early-full negative control pass after restoration. Candidate mapping remains separate in `scratch/transceiver-gray-prefix-mapping`. No reduced clock target, extra synchronizer cycle or altered snapshot semantics was accepted.

Next prioritize a physical recheck of the accumulated accepted changes, rather than continue inferring physical benefit from small unplaced improvements. The newest placed database still predates passes 37–41. That evidence gap must close before choosing further timing restructuring; count latency, reset distribution, coupled/routed RC, clock overhead and power must remain explicit. The full analog/DDR design and full-chip signoff are still incomplete, so this pass does not establish tapeout readiness.

## Pass 43: physical recheck of accepted RTL

`make transceiver-current-placement` verifies every characterized source hash and the mapped Verilog hash against pass 41, then places that candidate in a separate `scratch/transceiver-current-placement` directory. It preserves the older placed design. The same pinned PDK, local 1500 × 1530 µm requested core, density setting and placement/pin method are used. The geometry checker now accepts an explicit characterization report, verifies the netlist hash, and retains its old default; count/area invariants were not removed to accommodate a different candidate. This new placement is diagnostic: the older mapped-equivalence proof does not apply to its changed logic.

The [current placement screen](evidence/current-placement-screen.json) records all 26,429 cells legally placed, with independent row/boundary/site/orientation/overlap checks and three malformed-layout negative controls passing. Cell area is 0.967054 mm² and utilization of the same realized row area is 42.2879%, versus 0.956517 mm² and 41.8272% previously. The unchanged digital region still accommodates the cells; omitted clock trees, physical interfaces, PDN/taps/decaps and routing overhead prevent a final-fit claim.

The zero-RC placed run reproduces every domain/group of the pass-41 unplaced timing screen within 0.0002 ns. With the same nominal uniform-Metal3 placement estimate and ideal clocks, host TX/RX setup is −10.6511/−11.1665 ns, compared with −16.2450/−12.2338 ns in pass 34. Host recovery is −5.3528/−4.5825 ns. Thus the accumulated changes improve the worst reported host setup paths under this physical estimate, but still miss the target by large margins. Electrical violation rows rise from four to 25, so these delay figures remain diagnostic and may involve library extrapolation. Changes to both netlist and placement contribute; this is not an isolated measurement of any single RTL edit.

Next repair the current placement's electrical violations and verify the resulting transformation before further architecture decisions. Complete current-netlist functional correspondence, clock/reset distribution, routing and qualified interconnect assumptions remain necessary; comparing two unqualified estimates does not establish a worst-case bound. External IO, end-to-end protocol latency, total power and all analog/DDR/full-chip signoff obligations remain open. No target rate or capability has been reduced.

## Pass 44: repair current placement and fix missing native ties

`make transceiver-current-repair` verifies the current placement hash, reads it without modifying the original, repairs electrical violations, legalizes and exports a separate netlist/database. The first mapped-state equivalence attempt failed. Inspecting every original connection showed six reset-release register D pins that were originally constant one had become an undriven `one_` net in the export. This is a real missing physical tie-cell implementation, not a permissible proof exception. The [failure record](evidence/missing-tie-failure.json) identifies the six instances and hashes the retained failing netlist/proof. Prior placement screens must not be treated as having implemented physical constant drivers or established export equivalence.

The corrected flow inserts native GF180 `tieh/Z` and `tiel/ZN` cells, distributes high ties with at most one load each and handles unused low ties before legalization. An initial low-tie pin-name mistake was rejected by OpenROAD; the installed Liberty confirms `ZN`. Applying fanout repair to both polarities also removes the unused low-tie source that otherwise failed placement checking. Six tie-high cells remain, along with three inserted buffers and two resized NOR4 cells. Pre-export JSON conversion now includes an explicit `check -assert` to reject undriven logic before proof.

The [retained current repair screen](evidence/current-repair-screen.json) records 26,438 placed cells and 967,233.6576 µm² cell area, an increase of 180.0064 µm² over pass 43. OpenROAD legalization/checking passes and all 25 prior electrical violation rows disappear under the same nominal Metal3 placement estimate. Host TX/RX setup remains −10.6511/−11.1665 ns, and recovery −5.3528/−4.5825 ns. Electrical repair and constant-driver implementation do not resolve the speed failure.

Mapped-state correspondence matches all 8,299 register identities/types/parameters. SAT proves equality of all 18,881 register input bits (data, clocks, resets/sets) and 128 top-level output bits under shared arbitrary register state. A deliberately inverted status bit fails the same proof. Actual Liberty logic models are used, including tie functions and resized gates. This proves the repaired mapped netlist preserves the selected original mapped logic under matched state; it does not prove behavioral RTL-to-gates equivalence, analog behavior, metastability, glitches or physical reset/clock timing.

Artifacts are separate under `scratch/transceiver-current-repair`. Native tie cells still need explicit supply/well connectivity and PDN/physical signoff; a Boolean constant model does not provide that. Independent post-repair geometry checking remains to be rerun. Next close that check and inspect the current failing paths before choosing further architecture changes. Routed/coupled RC, CTS, external IO, total power, protocol latency, analog/DDR implementation and full-chip signoff remain incomplete. No high-speed or tapeout-readiness claim follows from this repair.

## Pass 45: independent repaired geometry and logical CORE supply assignment

`make transceiver-repaired-geometry` verifies the repaired database/netlist hashes, opens the database read-only, and independently checks all 26,438 cells against DEF row boundaries, site grid, orientation, height and non-overlap. The existing three malformed-layout controls remain effective. Cell count and 967,233.6576 µm² area match the equivalence-qualified repaired candidate, including six tie-high cells and three new buffers. Realized row utilization is 42.2958%. This closes the outstanding independent geometry check for pass 44, not full DRC or routability.

The database audit found all 105,752 standard-cell power/well pins unconnected. A separate output database now assigns VDD/VNW to the existing `VDD_CORE` rail and VSS/VPW to `VSS_CORE`. The [retained screen](evidence/repaired-geometry-supply-screen.json) verifies exactly those four pins on every instance, including all 24 tie-cell supply/well pins, with no remaining unassigned pins. A deliberately wrong-rail assignment is rejected. All non-supply instance-pin net connections and the entire DEF component placement section are unchanged, so this operation does not alter the already-compared signal logic or geometry.

The resulting `scratch/transceiver-repaired-geometry/supply-connected.odb` contains logical rail assignments only. There are no physical straps, rails, vias, well taps or connections to chip supply pads yet. Net names do not establish conductive paths, well/substrate continuity, isolation, powered functionality, IR drop or electromigration margins. The original repaired database remains intact. The digital region is assigned to CORE in accordance with the existing integration skeleton; physical power delivery still must respect the single CORE supply/ground pair and its unqualified total-current budget.

Next turn this explicit rail assignment into a candidate physical power network and audit connectivity and routing resources, while preserving space for clock distribution and the analog/DDR interfaces. The large setup/recovery failures remain unchanged. Clock/power uncertainty bounds, current-netlist RTL-to-gates equivalence, routed timing, external protocol latency, full analog/DDR implementations and full-chip signoff remain required for the original chip objective.

## Pass 46: first local digital metal power network

`make transceiver-pdn-screen` verifies the supply-connected database hash and generates a provisional local CORE grid in a separate `scratch/transceiver-pdn` directory. Native cell VDD/VSS pins use 0.6 µm Metal1 rails, confirmed from the installed LEF. The first candidate used Metal2 vertical and Metal3 horizontal straps. Both rail networks passed connectivity, but post-PDN placement checking reported 7,572 blocked-layer conflicts with cells. That candidate was rejected; its Tcl is retained in `evidence/experiments/pdn-low-layer.tcl`. Electrical connectivity alone was insufficient.

The retained candidate instead uses Metal4 vertical straps of 2 µm width and Metal5 horizontal straps of 4 µm width, each with 80 µm pitch and 10 µm offset, plus Metal1 follow-pin rails. Generated via stacks connect Metal1–Metal4 and Metal4–Metal5; generated local supply access pins use Metal5. These dimensions are provisional design choices, not qualified IR/EM sizing or uncertainty bounds.

The [retained PDN screen](evidence/digital-pdn-screen.json) records successful `check_power_grid` results for both VDD_CORE and VSS_CORE and a passing post-PDN placement check. Each net has 195 Metal1 rails, 19 Metal4 straps, 19 Metal5 straps and 11,476 special-wire via objects (not individual cut counts). Entire DEF component and signal-NETS sections are unchanged. This preserves the checked placement and signal connections; it does not establish powered operation or rerun timing with the new routing obstructions/parasitics.

Generated macro-region access pins are not chip supply pads. The single CORE supply/return pair, pad-to-grid feed geometry, straps outside this local region, well taps/substrate continuity and analog-domain isolation remain unfinished. Independent DRC/LVS, via-rule signoff, IR drop, electromigration, dynamic supply noise and complete current estimates are still required. Metal4/Metal5 resources now consumed by the grid must be included in signal/clock congestion and timing analysis. Next audit the actual via/metal geometry and establish explicit current/feed assumptions for a power-integrity screen before treating this network as adequate. Prior high-speed setup/recovery failures and full analog/DDR completion remain open.

## Pass 47: opposite-rail geometric contact audit

`make transceiver-pdn-geometry` reads the hash-bound generated PDN database and expands every special-wire via into its native local metal-enclosure and cut rectangles translated to the placement coordinates. This includes the intermediate Metal2/Metal3 shapes in the Metal1–Metal4 stacks, even though the main straps use Metal4/Metal5. It exports all rectangles and runs an independent spatial-bin intersection checker by layer. Both positive-area overlaps and exact edge contacts between opposite rails are considered failures; contacts within one rail are permitted.

The [retained audit](evidence/pdn-geometry-screen.json) checks 148,286 rectangles across Metal1–Metal5 and Via1–Via4 and finds zero VDD_CORE/VSS_CORE contacts. Deliberately injected overlap and edge-touch cases are rejected by the same checker. This adds a geometric check absent from the previous per-net connectivity results: two connected rail networks must also remain separate. The PDN database itself is unchanged.

This scope is intentionally explicit: generated PDN rectangles only. Cell internals, unrelated signal geometry, pads, wells and substrate shapes are not included. Minimum spacing, via enclosure/cut-array rules, density, complete DRC/LVS and physical domain isolation remain unverified. Nor does a short-free grid establish current capacity, IR drop, electromigration or supply-noise tolerance. Next establish explicit source/feed and load assumptions for a scoped resistive screen and then expand geometry/rule checking; no bound should be inferred for unknown total current or package impedance. All high-speed timing and full analog/DDR completion obligations remain open.

## Pass 48: explicit-feed resistive screen and load-audit correction

`make transceiver-pdn-ir` reads the hash-bound PDN and exports nominal resistance networks using explicit source CSVs rather than an implicit ideal supply at every top-layer access point. The requested local feed centers are (20.08, 61.76) µm for VDD and (20.08, 21.76) µm for VSS, with a 2 µm source region. Each export contains exactly one voltage source; actual nodes snap to (23.58, 61.76) and (20.08, 21.76) µm respectively. These are ideal local feeds, not implemented package pads or pad-to-grid conductors. The [OpenROAD source-model documentation](https://openroad.readthedocs.io/en/latest/main/src/psm/README.html) and [CSV format](https://openroad.readthedocs.io/en/latest/main/src/psm/doc/Vsrc_description.html) define the interface; actual exported source count/location is checked.

The first PDNSim run assigned nominal per-instance power intended to total 48 mA at 3.3 V. Although its report printed the expected total power, its exported current sources summed to 82.110272 mA. The requested 24 mA case summed to 58.106541 mA. Both contain approximately 34.1 mA of extra modeled load, so the direct voltage results are **not accepted as 24/48 mA results**. The origin of that discrepancy remains unresolved; no automatic activity estimate is silently substituted for the requested load. The exported current vectors and logs are retained for investigation.

The independent SciPy sparse solver instead uses the exported resistor topology with explicitly assigned uniform fixed current at all 26,438 instance pins. It checks an analytic series-resistor circuit, finite solutions, nonnegative drops, KCL residual below 1e-8 A, source-current balance within 1e-8 A and exact 2:1 scaling between 24 and 48 mA. Every exported current node is bound to the matching instance-pin coordinate. VDD drop and VSS rise are solved separately and combined at each instance, not by blindly adding unrelated extrema.

The [retained screen](evidence/pdn-resistance-screen.json) gives local VDD–VSS ranges of 3.130055–3.255564 V at 24 mA and 2.960110–3.211127 V at 48 mA, from ideal 3.3/0 V feeds. The worst combined instance is `_21561_`. These are conditional uniform-load DC results, not measured activity, qualified resistance corners or worst-case spatial bounds. They already show that a single supply adjustment cannot erase the grid's spatial voltage spread; nominal 3.3 V cell timing cannot be applied uncritically throughout the region.

No package/feed resistance or inductance, transient switching/decoupling, process resistance uncertainty, electromigration or well/substrate power path is qualified. Concentrated loads can differ materially from uniform loads, and 48 mA remains a planning allocation rather than a proven bound on actual CORE consumption. Next test load/feed sensitivity and derive conservative resistance bounds where possible, then resize/redistribute feeds and straps accordingly. The unexplained PDNSim load discrepancy must also be resolved before relying on its automatic power/current analysis. Existing high-speed timing failures and full analog/DDR/full-chip signoff gaps remain open.

## Pass 49: conservative spatial-load bound exposes missing constraints

`make transceiver-pdn-path-bound` checks the retained resistor-network and instance-map hashes, then computes shortest resistance paths from each explicit source to every cell supply pin. Parallel resistor edges are treated as alternative paths using the minimum edge weight, not accidentally summed as series resistances. An analytic series/parallel-path example checks the method; negative resistances and disconnected networks are rejected. The retained uniform-load solutions lie inside the resulting bound.

For a passive linear network with one ideal source and nonnegative loads totaling I, no branch carries more than I: voltage-directed nonzero flows cannot form a directed cycle, and each is part of the total source-to-load flow. Along any source-to-pin path, the voltage difference is therefore at most I times the sum of its positive resistances. Shortest resistance paths tighten this conservative bound. VDD and VSS path bounds add at each matching cell; the maximum across cells bounds the local supply loss for any allowed spatial distribution. This is a network-model argument, not a claim that 48 mA bounds actual chip consumption or that nominal resistor values bound process variation.

The [retained result](evidence/pdn-path-bound.json) finds a maximum combined path resistance of 174.239986 Ω. At assumed totals of 24 and 48 mA, the combined-loss bounds are 4.181760 and 8.363519 V. Both exceed the ideal 3.3 V supply and therefore provide **no useful positive-supply guarantee**. They are not predictions of negative physical operating voltage and do not prove every realistic activity pattern fails. The much smaller uniform-load drop cannot substitute for arbitrary spatial-load qualification. Example 0.05/0.1/0.2 V loss budgets yield sufficient total-current limits of only 0.287/0.574/1.148 mA under this loose bound; these are not estimated maximum usable currents or accepted design targets.

This audit identifies a higher-level missing requirement: load concentration needs justified per-cell/regional bounds, and the local feed/grid needs improvement before a narrow operating-voltage promise can be made. Simply setting one external supply voltage does not ensure the same internal voltage everywhere. Next combine per-cell/activity limits with concentrated-load scenarios or tighter network bounds, and revise feed/strap geometry while preserving the one CORE supply/ground pair. Package resistance/inductance, nonlinear current behavior, resistance corners, electromigration and power-model discrepancies remain unresolved. Timing, analog/DDR completion and full-chip signoff remain open; this pass neither narrows the chip objective nor accepts the present grid as adequate.

## Pass 50: identify and guard the PDNSim load-model mismatch

Inspected the exact OpenROAD source revision reported by the pinned binary and reproduced a zero-user-load control on the same PDN. The [source and usage restriction](spec/pdnsim-load-model.md) explain the defect: `buildNodeCurrentMap` adds automatic STA current and then adds user current, while overwriting the separate power-summary map. Consequently the printed total looks like an override but the network load is additive. No local tool binary was patched.

The [retained audit](evidence/pdnsim-load-audit.json) confirms 34.105033 mA remains at zero user load on each rail. Requested 24/48 mA cases export 58.106541/82.110272 mA. Every one of the 26,438 current sources per rail matches zero-case current plus requested uniform current within 1.1e-8 A, accounting for SPICE rounding; resistor networks and voltage sources are identical across all three cases. This establishes the cause of the pass-48 mismatch, not actual silicon power. The automatically estimated residue must not be treated as measured or bounded consumption.

`run_pdn_ir.sh` now includes the zero-load run and mandatory source-by-source audit before using the independent explicit-current solver. The checker fetches the pinned source if absent and verifies its recorded SHA-256 before checking the implicated code. Direct PDNSim voltages remain excluded as requested-load results for this build. The independent 24/48 mA calculations and conditional path bounds remain unchanged and retain all their physical limitations.

This removes one tool-model uncertainty without solving the grid's voltage drop. Next improve feed/grid geometry and test regional loading using explicit currents, then recheck routing resources and timing at justified local voltages. Current limits, package impedance, resistance corners, IR/EM, full analog/DDR implementation and full-chip signoff remain open. No external issue or message was published.

## Pass 51: wider upper-metal grid candidate

`make transceiver-wide-pdn` builds a separate grid from the same supply-connected placement, widening Metal4/Metal5 straps from 2/4 µm to 4/8 µm while retaining 80 µm pitch, 10 µm offset, native Metal1 row rails and one requested ideal feed region per rail. No extra supply terminal or independent voltage source is introduced. The original grid remains intact; this is a candidate pending routing-resource and physical-rule qualification.

Both rail connectivity checks and post-PDN placement checking pass. Independent opposite-rail geometry checking expands 268,404 rectangles, including regenerated via arrays, and finds no rail contacts; injected overlap/edge-contact controls still fail as expected. The entire DEF component and signal-net sections remain unchanged. Main-strap rectangle area doubles: Metal4 from 115,638.56 to 231,277.12 µm², and Metal5 from 227,951.36 to 455,902.72 µm². Metal1 area is unchanged. These are metal footprint comparisons, not routed congestion or proven usable track capacity.

The [retained comparison](evidence/wide-pdn-comparison.json) uses the independent explicit-current solver, not PDNSim's misleading requested-power summary. All current-node mapping, source-count, KCL, current-balance and linear-scaling checks pass. At 48 mA uniformly distributed, local supply improves from 2.960110–3.211127 V to 3.122345–3.258148 V. At 24 mA the candidate range is 3.211173–3.279074 V. The requested feed CSVs match exactly; actual snapped source nodes are retained in the report, since geometry-dependent snapping is part of this comparison. The worst candidate instance is `_21544_`.

The approximately 0.178 V worst combined loss at uniform 48 mA is an improvement, not an accepted supply budget or arbitrary-load bound. Doubling top-layer strap area may worsen signal/clock congestion, and pad-to-grid feeds, package impedance, resistance corners, concentrated activity, EM, well taps and full DRC/LVS remain unqualified. The widened candidate is retained separately under `scratch/transceiver-wide-pdn`, with its independent load screen and geometry reports, for further evaluation. Next assess spatial loading and routing cost before choosing a grid; all timing and full analog/DDR obligations remain open.

## Pass 52: fixed-total-current regional loading

`make transceiver-pdn-regional-loads` audits the wider-grid resistor/instance-map hashes and solves five distributions with exactly 48 mA total: uniform across all cells, then uniform within each of four quadrants with zero assumed current outside that quadrant. Quadrants use VDD instance-pin coordinates relative to (760, 775) µm, and the same selected cell set/current is applied to both VDD and VSS networks. This tests spatial sensitivity without introducing additional ideal sources or changing total current. It does not assert that zero background leakage or equal per-cell regional current describes real operation.

The [retained matrix](evidence/pdn-regional-loads.json) gives minimum local VDD–VSS of 3.122345 V for uniform loading, 3.162703 V for southwest, 3.075833 V for northwest, 3.146906 V for southeast and 3.086409 V for northeast concentration. Northwest is the worst of these finite cases, with about 0.224 V combined loss from the ideal 3.3 V feed. Direction/distance alone does not rank the cases; the selected populations and actual network geometry matter.

The positive-resistor solver was extracted unchanged into `verification/pdn_linear.py` for reuse. The analytic series-resistor control passes, every solve checks finite/nonnegative drops, per-node KCL residual below 1e-8 A and total feed current within 1e-8 A, and the uniform case reproduces both endpoints of the retained pass-51 voltage range within 1e-7 V. Quadrant membership is checked to be a disjoint complete partition of all 26,438 mapped cells.

These five cases are not an arbitrary-load bound and do not establish transistor timing at the predicted local supplies. Smaller hot spots, per-cell activity/leakage limits, package/feed impedance, process resistance corners, dynamic noise and EM remain unqualified. The wider grid is still a candidate, not an accepted power-delivery solution. Next evaluate its routing-resource cost and candidate feed improvements while maintaining the single CORE supply/ground pair; full timing, analog/DDR and full-chip signoff obligations remain open.

## Pass 53: compare global-routing cost of the wider grid

`make transceiver-pdn-routing` routes the same placed signal design against the original and wider PDN databases, using Metal2–Metal5 for global routing and retaining coarse Metal1 pin-access guides. Both inputs are checked against their retained database hashes. The runs permit congestion so a failure to fit remains measurable rather than preventing report generation. No global capacity derating is applied; this is a controlled comparison, not a margin-qualified routing result.

The [retained comparison](evidence/pdn-routing-comparison.json) records the same 26,498 net names in both guide sets, including all eight clock input nets. Both cases finish with zero global overflow. The wider grid reduces modeled total resources from 1,696,388 to 1,617,914 (−4.626%) and increases wirelength from 3,151,881 to 3,156,627 µm (+0.151%). Total demand rises from 283,870 to 283,961, with reported utilization changing from 16.73% to 17.55%. Metal2 resources also decrease because the wider via stacks occupy lower-layer geometry; top-layer stripe footprint alone would miss that cost.

Clock nets have no STA clock definitions in this geometric screen and are explicitly routed as ordinary, unbuffered high-fanout nets. Presence in the guide files is checked rather than assuming the router included them. This does not represent a synthesized clock tree, its buffers, skew, latency, current or hold-fix demand. No timing-driven priority or post-route timing claim is made.

These results support continuing with the wider candidate: its improved explicit-load DC result does not incur global overflow in this particular comparison. They do not establish detailed pin access, DRC, antenna closure, signal/coupling parasitics or final routability after CTS, pads and analog macros. Next use the guides to refine signal RC/timing and reserve realistic clock/power overhead before adopting a final digital floorplan. Full power integrity, high-speed timing and analog/DDR/full-chip signoff obligations remain open.

## Pass 54: global-route signal-RC timing

`make transceiver-global-route-timing` regenerates the wider-grid global routing, then defines the eight nominal high-rate clocks and estimates parasitics from global-route geometry. The route-guide SHA-256 exactly matches pass 53, avoiding an accidental comparison against a different routing solution. Clocks remain ideal during timing analysis; their routed high-fanout wires are not a qualified clock tree. External IO delays remain unconstrained.

The [retained timing screen](evidence/global-route-timing-screen.json) records all eight domain groups and the estimated SPEF: 26,498 nets (matching the routed-net count), 353,217 resistors and 280,180 capacitor entries. The parser verifies units and finite nonnegative component values. These checks verify the estimator output's basic consistency, not detailed routing, extracted coupling or qualified process corners.

Host TX/RX setup slack is −11.7277/−11.9146 ns, versus the repaired placement-estimate values of −10.6511/−11.1665 ns. Host recovery worsens to −5.3964/−4.8442 ns. Wired RX/TX setup is −7.7964/−6.1818 ns. No electrical violation rows are reported. The worst host-TX path runs from `wire_in.rb[5]` through FIFO readout into `frame_tx.payload.stage_data[4]`, showing that the staged destination still receives a long physical readout path.

The physically informed estimate therefore does not validate the earlier small unplaced gains as sufficient. At the 3.2 ns host period, a setup miss exceeding 11 ns demands substantial datapath restructuring or lower-rate parallel internal processing with unchanged external throughput; legal placement and zero congestion do not solve this. Next examine the critical readout structure against such architectural options before investing in finer physical optimization of a failing word-rate datapath. No reduction in wired/RF throughput, pin count or full-chip scope is authorized by this result.

Nominal TT/3.3 V timing still ignores the local voltage spread found in the power-grid screens, and global-route RC is not foundry extracted interconnect. CTS, supply-sensitive timing, hold/CDC/reset constraints, protocol latency, detailed-route DRC/LVS and full analog/DDR implementation remain open. The chip is not tapeout-ready.

## Pass 55: executable parallel-datapath feasibility model

The [parallel-datapath candidate](spec/parallel-datapath-candidate.md) responds to the persistent physical word-rate timing failures without lowering external rates. It compares 20/40/80-bit internal beats and makes collection/control latency, multi-enqueue storage, TX snapshot epochs and the still-fast physical gearbox explicit. Eight-word host processing would run at 31.25/39.0625 MHz with 32/25.6 ns periods, but this rate arithmetic is not proof of implementation timing.

`make transceiver-block-receiver` passes the [retained model comparison](evidence/block-receiver-model.json): an independent vector-rank RX algorithm matches scalar command/payload ordering for all 1,308 legal count pairs at each of three widths, including sequence wraps; all 50 single header-bit corruptions per mode/width suppress effects and latch faults. The comparison concerns ordered events, not identical word-cycle timing. The eight-word candidate adds up to 28/22.4 ns of collection delay, plus unmodeled handoff/processing/application delay. No area/power/physical speed benefit is claimed.

A crucial architectural restriction is recorded: variable multiword pointer increments cannot simply reuse ordinary one-step Gray-pointer CDC. Fixed-block transactions, local residual handling and explicit word/block count semantics are required. Widening the host path alone also leaves the failing wired FIFO domains untouched; their analog-macro parallel boundary needs corresponding treatment. These constraints prevent a nominally wider bus from being mistaken for a complete solution.

Existing RTL remains unchanged. Next implement a small verified block-storage/CDC foundation and measure it before committing the complete transport rewrite. The original full wired/RF capability, 50 terminals, one slot and FPGA-companion partition remain the goal; analog/DDR macro implementation and all physical signoff are still incomplete.

## Pass 56: standalone block FIFO candidate

Added eight-entry storage for 80-bit blocks plus valid-word counts, using one-step
Gray pointers across clock domains. Illegal counts are discarded with a sticky
write-domain fault. `make transceiver-block-fifo` passes an independent asynchronous
transaction scoreboard: 7,413 accepted blocks, 7,412 received (one flushed by reset),
7,439 rejected invalid requests, all legal counts exercised, backpressure, wraps
and complete final drain. Both metadata-corruption and invalid-admission negative
controls fail as required. See the [candidate contract](spec/parallel-datapath-candidate.md)
and [retained evidence](evidence/block-fifo.json).

The integrated transport remains unchanged. This establishes finite functional
block-storage evidence only; physical CDC/reset qualification, synthesis/placement
cost, gearbox implementation and full wide-path integration remain open. The chip
is not tapeout-ready.

## Pass 57: remove duplicate reset-release decisions

The standalone block wrapper had its own reset-release synchronizers alongside
those in storage. Equal RTL depth does not ensure equal physical release cycles;
the wrapper could acknowledge a transaction while storage remains reset. It now
uses the actual storage release signals for ready/valid and write-fault reset.
The shared FIFO exposes these as optional output ports; existing integrated
instances retain their behavior and no transport architecture was replaced.

`make transceiver-block-fifo` now deliberately holds each storage release low for
different intervals after external reset. It passes 7,489 accepted blocks, 7,487
received (two flushed by reset), 7,419 rejected invalid requests and a complete
final drain. All three negative controls fail: corrupted metadata, invalid-count
admission and bypassed write reset gating. `make transceiver-baseline` also passes
both transport versions/profiles, control/helpers/memory/release checks, independent
codec checks and structural synthesis. [Evidence](evidence/block-fifo-reset.json)
records the source digests. Pass 56 evidence remains historical.

This closes a logical reset acceptance hazard, not physical reset or metastability
signoff. Shared reset-tree recovery/removal, Gray-bus skew, dual-clock storage,
clock-ratio coverage and timing/area remain unqualified. Full analog/DDR and wide
transport integration still remain; the chip is not tapeout-ready.

## Pass 58: map the block FIFO into GF180 cells

`make transceiver-block-fifo-mapping` maps the standalone candidate with the pinned
EDA image and nominal GF180 7-track TT/25°C/3.3 V library. The [retained report](evidence/block-fifo-mapping.json)
records 2,011 standard cells, 707 flip-flops and 76,568.576 µm² cell area. The 672
storage bits map to flip-flops, not an SRAM macro. This is cell area, not a placed
macro footprint; clock/reset trees, routing whitespace and supply overhead remain.

At a 25.6 ns period in both domains, same-domain setup slack is +16.6562 ns for
writes and +20.1240 ns for reads. Nominal reset recovery paths are also positive,
and the electrical report contains no violation rows. These are ideal-clock,
unplaced, zero-wire-RC checks. The asynchronous data output and other boundary
paths have no interface delay budgets: 87 output endpoints remain unconstrained.
Consequently this result does not establish full-interface timing, usable maximum
frequency, CDC safety or physical reset timing. Mapping equivalence is also not
established by structural synthesis alone.

The result supports continuing the wide-path investigation but is not a whole-chip
area or speed claim. Next qualify the output mux with an explicit receiving
register and interface budget before integrating transport logic. The full analog,
DDR/serial gearboxes, wide transport, clocking and physical signoff remain open;
the chip is not tapeout-ready.

## Pass 59: receiving-register timing fixture

`make transceiver-block-capture-mapping` maps a standalone timing fixture that
captures each accepted FIFO read into an 84-bit register. It is not integrated
chip RTL. The fixture plus FIFO occupies 85,437.184 µm² cell area, with 2,251
standard cells and 791 flip-flops. All 84 receiving D pins are identified from
mapped connectivity, with explicit count checks; synthesis naming is normalized
before writing both JSON and Verilog so timing endpoints cannot silently disappear.

The [retained screen](evidence/block-capture-mapping.json) uses the nominal
TT/25°C/3.3 V library, ideal 25.6 ns clocks, no wire RC, illustrative 4 ns boundary
delays, 0.5 ns input transitions and 5 fF output loads. The worst read-domain
capture path is from `rd_ready`, arriving at 7.8524 ns with +17.3183 ns slack.
The worst read-pointer-to-capture path arrives at 6.6885 ns with +18.5067 ns slack;
this includes empty/enable logic and is not exclusively an address mux path.
The storage-clock-to-capture path arrives at 3.2438 ns. Its +21.9306 ns slack is
against a provisional explicit 25.6 ns max-delay budget, not a meaningful
phase-related setup check between asynchronous clocks.

No electrical violation rows or unconstrained output endpoints are reported;
external reset remains unconstrained. This does not prove the boundary budgets,
all remaining timing paths, hold, reset recovery/removal distribution, mapped
functional equivalence or extracted physical performance. In particular, the
bundled-data stability window and reuse protection must justify an actual CDC
budget; imposing a max-delay constraint cannot establish that justification.
The nominal result supports further investigation, not tapeout readiness.

## Pass 60: derive the block FIFO's stability windows

The [stability derivation](spec/block-stability-windows.md) separates publication,
storage reuse and output hold. Under coherent causal pointer synchronization,
a newly written block cannot be captured before two read periods have elapsed;
a consumed slot cannot be reused before two write periods have elapsed. These
are conditional architectural bounds, not metastability or physical timing proofs.

`make transceiver-block-windows` passes 188 finite event-model cases, 294,021 reads,
292,439 reuses and 376 coordinated flushes. All tested phases, stalls and extra
pointer visibility delays preserve the bounds; a one-stage negative control
fails. The [report](evidence/block-stability-windows.json) retains source hashes.
This is independent model evidence, not exhaustive RTL equivalence.

A separate hold obligation is explicit: advancing the read pointer changes the
output mux on the capture edge, long before storage can be reused. Therefore the
reuse bound cannot justify receiving-register hold. Physical pointer skew,
synchronizer behavior, reset and min-delay checks remain open. The chip is not
tapeout-ready; this pass establishes which bounds the implementation must meet.

## Pass 61: nominal capture hold screen

`make transceiver-block-capture-hold` reads the immutable pass-59 mapped fixture
and checks minimum-delay paths to all 84 receiving registers. The report verifies
84 unique endpoints in each path class and checks the input artifact hashes.
Unlike the previous maximum-delay fixture, `rd_ready` has an explicit zero minimum
input delay: a 4 ns maximum arrival budget cannot be reused as a hold assumption.

The [retained report](evidence/block-capture-hold.json) records minimum hold slack
of +3.0055 ns from the read pointer, +1.2647 ns from capture-register feedback,
and +1.1485 ns from `rd_ready` (also the read-domain minimum). An illustrative
0.5 ns hold uncertainty reduces the latter to +0.6485 ns. A deliberately excessive
5 ns uncertainty produces −3.8515 ns; the verifier checks both the failure and
exact slack sensitivity. These are nominal TT/25°C/3.3 V, ideal-clock, zero-wire-RC
results, not an approved clock-skew budget or physical hold closure.

Fast-corner/mismatch behavior remains necessary even with narrow ambient and
supply operation. CTS skew, extracted min delays, reset distribution and the
asynchronous storage-data stability assumptions remain unqualified. No FIFO or
integrated transport RTL changed. The chip is not tapeout-ready.

## Pass 62: available PVT capture-path stress screens

`make transceiver-block-capture-corners` reuses the pass-59 mapped fixture without
remapping, verifies its input digests, and screens the five installed 3.3 V-class
library corners. The [retained evidence](evidence/block-capture-corners.json)
includes library hashes and separate capture setup/hold results. TT reproduces
prior results. Read-domain capture setup minimum is +11.1982 ns at SS/125°C/3.00 V.
Hold minimum is +0.6776 ns at FF/−40°C/3.60 V, versus +1.1485 ns nominal; the
illustrative 0.5 ns uncertainty leaves only +0.1776 ns at that fast corner. Every
corner covers all 84 hold endpoints and fails the excessive 5 ns uncertainty
control with the expected one-for-one slack change.

These libraries change process, voltage and temperature together. No installed
FF/SS 25°C/3.30 V library was found in this library set. This is an available-corner
stress screen, not isolated process characterization, a newly imposed operating
range, or proof that these corners bound every path at the intended narrow
operating point. The user acceptance of narrow temperature/supply remains intact.

A narrow ambient/supply target does not remove process spread or clock-tree hold
risk. The fast-corner result makes routed min-delay and skew control material;
the nominal margin cannot be spent freely. No physical timing closure or complete
FIFO timing qualification is claimed: capture paths only, ideal clocks, no wire
RC/local variation. Analog macros, full wide transport and chip signoff remain
open; the chip is not tapeout-ready.

## Pass 63: place the block capture fixture

`make transceiver-block-capture-placement` places the unchanged nominally mapped
fixture into a requested 500 × 400 µm local region. All 2,251 cells are placed;
85,437.184 µm² cell area uses about 43% of that region. OpenROAD placement checks
pass. An independent rectangle checker verifies region containment and no cell
overlap; an injected duplicate rectangle fails as required. This is not full
DRC, site/orientation auditing, pin-access or physical integration proof.

The [retained screen](evidence/block-capture-placement.json) uses placement-based
Metal3 wire RC and ideal clocks. Capture setup minimum falls to +5.7741 ns at
SS/125°C/3.00 V (+15.6368 ns nominal); FF/−40°C/3.60 V read-domain hold minimum is
+0.7544 ns, or +0.2544 ns with the illustrative 0.5 ns uncertainty. All 84 hold
endpoints are checked in each of the three screened corners, and no electrical
violation rows are reported. Available mixed PVT corners remain stress cases,
not proof of bounding the intended narrow operating envelope.

No CTS, routed signal parasitics, PDN, reset-tree or Gray-pointer skew qualification
is included. Clock skew can consume the remaining hold margin. The result keeps
the wider FIFO candidate plausible at this scale but does not establish the full
transport's area, power or speed. Analog/gearbox implementation and full-chip
physical signoff remain open; the chip is not tapeout-ready.

## Pass 64: propagated clock-tree screen exposes a boundary hold deficit

`make transceiver-block-capture-cts` builds two clock trees for the placed fixture,
covering 791 sinks with 54 tree buffers plus tool-created balancing dummy loads.
Detailed placement/checks pass. The [retained timing report](evidence/block-capture-cts.json)
checks all 84 capture endpoints per hold-path class with propagated clock delays
and placement-based signal/clock RC. Capture setup minimum is +5.8279 ns in the
hot-slow screen, and no electrical violation rows are reported.

Cold-fast hold is now +0.4112 ns on the externally modeled `rd_ready` path, versus
+0.7544 ns before CTS. With the existing illustrative 0.5 ns hold uncertainty it
**fails at −0.0888 ns**. Internal pointer and feedback minima at that corner are
+2.3687 ns and +0.7562 ns, respectively. Nominal overall hold is +0.6229 ns before
uncertainty. The verifier intentionally records and requires the observed failure;
a successful report-generation command is not a passing timing signoff.

The ready input currently has zero minimum arrival relative to the root clock,
while the capture clock arrives through CTS. Do not conceal the failure by raising
that minimum delay without implementation evidence. Integrate a representative
upstream launching register and its clock tree, or derive a justified boundary
minimum-delay contract; then evaluate hold repair if needed. CTS equivalence,
independent post-CTS geometry checks, routed parasitics, reset and Gray skew remain
open. This is a fixture experiment, not accepted chip clocking. Full analog/PHY
and wide transport implementation remain incomplete; the chip is not tapeout-ready.

## Pass 65: implement a receiving elastic buffer

Added standalone `pt_block_elastic`: the block CDC FIFO plus two read-domain
elastic entries. Registered local capacity drives the CDC read handshake, removing
the direct downstream-ready-to-CDC-capture path that limited the timing fixture.
This changes actual architecture rather than assuming a more favorable input delay.
It adds 168 data bits plus control, a receiving capture stage and two blocks of
capacity; full-boundary behavior and latency are documented in the
[candidate contract](spec/parallel-datapath-candidate.md).

`make transceiver-block-elastic` passes the independent transaction scoreboard:
8,577 accepted blocks, 8,574 received (three flushed by reset), 7,440 invalid
requests rejected and complete final drain. Saturated operation achieves 1,071
consecutive read transfers. A stored-data corruption negative control fails.
[Evidence](evidence/block-elastic.json) records source hashes. The test includes
random stalls and delayed per-domain storage release; physical reset remains open.

No integrated transport RTL was replaced and no timing improvement is yet claimed.
The new receiving buffer must be mapped and physically checked, including its
registered-full path and downstream mux, before superseding the previous fixture.
The full analog/PHY and wide transport implementation and signoff remain incomplete.

## Pass 66: map the elastic receiving stage and verify its control boundary

`make transceiver-block-elastic-mapping` maps the standalone candidate into the
nominal GF180 library: 95,963.168 µm² cell area and 881 flip-flops. This is larger
than the earlier one-register timing fixture and provides two receiving entries;
it is not a like-for-like area saving. The [retained report](evidence/block-elastic-mapping.json)
records mapping/library hashes and the exact scope of timing coverage.

Mapped-connectivity traversal stops at sequential cells and confirms no
combinational path from external `rd_ready` to any of the 168 receiving-storage D
pins. The full/capacity flag is a single mapped register. Its paths to all 168
receiving bits pass with minimum nominal setup slack +20.5516 ns at 25.6 ns and
hold slack +3.0739 ns. This confirms the intended control partition in mapped
logic; it is not full RTL-to-gates equivalence. Structural synthesis passes and
the nominal electrical report contains no violation rows.

Only the registered-capacity paths are screened here. Output mux timing, local
buffer control, other FIFO paths, actual receiving consumers, reset, CDC, placement,
CTS and routed timing remain open. In particular, this unplaced result does not
erase the pass-64 fixture failure or establish a physically closed replacement.
The chip remains far from tapeout-ready, with full analog and wide-path integration
still required.

## Pass 67: placed elastic-buffer capacity path with clock trees

`make transceiver-block-elastic-physical` places the nominally mapped candidate
in the same requested 500 × 400 µm region, then builds two clock trees covering
881 sinks with 59 tree buffers plus balancing dummy loads. OpenROAD detailed
placement checks pass before and after CTS. Pre-CTS counts/area are retained
separately; they must not be mistaken for post-CTS total footprint or power.

The [retained report](evidence/block-elastic-physical.json) checks the registered
capacity flag's setup and hold paths to all 168 receiving-storage bits. With
propagated clocks and placement-estimated Metal3 signal/Metal4 clock RC, minimum
setup slack is +12.6768 ns at SS/125°C/3.00 V. Minimum hold slack is +1.9451 ns at
FF/−40°C/3.60 V **including** the illustrative 0.5 ns uncertainty. Nominal setup/
hold results are +19.0739/+3.7039 ns. No electrical violation rows are reported.

This supports the intended capture-control partition after CTS. It is not a
like-for-like whole-interface comparison with the old one-register fixture: the
new buffer adds capacity, control and a downstream mux. Other buffer state paths,
external ready to local control, downstream capture, full upstream timing, routed
parasitics, independent post-CTS geometry/functional equivalence, Gray skew and
reset qualification remain open. The narrower path result does not establish
that the whole FIFO or chip meets timing. Full analog/PHY and wide transport
implementation are still incomplete; the chip is not tapeout-ready.

## Pass 68: broaden timing scope and retain interface hold failures

`make transceiver-block-elastic-boundary` checks same-domain paths across the
post-CTS elastic block, including control and interface paths, with explicit
provisional input max/min delays of 4/0 ns, output max/min delays of 4/0 ns,
0.5 ns input transitions, 5 fF output loads and 0.5 ns hold uncertainty. The
[retained report](evidence/block-elastic-boundary.json) separates ordinary timing
from reset recovery/removal groups and records worst-path endpoints. External
reset remains unconstrained; cross-domain timing is not qualified by these reports.

All screened same-domain maximum-delay minima remain positive, worst +3.9193 ns
in the hot-slow write domain. However ordinary boundary hold fails in every corner:
write/read minima are −1.7075/−0.6816 ns nominal, −1.1916/−0.6462 ns cold-fast,
and −2.9218/−0.7263 ns hot-slow. The worst hot-slow starts are `wr_valid` and
`rd_ready`, respectively. Report ordering lists asynchronous removal paths first;
the verifier parses groups explicitly rather than mistaking that order for severity.

These failures do not contradict the passing registered-capacity path: the new
structure isolates data capture but still has input-driven control registers.
Inputs modeled at zero minimum delay relative to root clocks can change before
delayed capture clocks. Do not raise minimum delays without a real source timing
contract or integrated upstream registers/clock trees. Retain this failed screen
as an integration requirement. A successful reproduction/report command records
the failure; it is not a passing timing gate. No RTL or boundary assumptions were
changed to conceal it. Routed timing, CDC/reset and full analog/wide transport
implementation remain open; the chip is not tapeout-ready.

## Pass 69: registered ingress with verified replacement/stall behavior

Added standalone `pt_block_staged`, placing a real data/count/valid holding stage
ahead of the elastic block FIFO. It accepts into empty storage, holds under
backpressure, or replaces a transferred block on the same edge. Illegal counts
are rejected at ingress with a sticky fault. The
[candidate contract](spec/parallel-datapath-candidate.md) records reset behavior,
eleven-block total capacity and the extra write-domain launch latency.

`make transceiver-block-staged` passes: 8,587 legal accepted blocks, 8,584 received
(three flushed by reset), 7,447 illegal requests rejected, complete final drain,
and 1,071 consecutive saturated reads. The inherited independent queue scoreboard
covers asynchronous clocks, random stalls and delayed storage reset release.
A mutation that overwrites the holding stage while blocked fails with a block
mismatch. [Evidence](evidence/block-staged.json) retains source hashes.

No physical improvement is yet claimed. This supplies actual launch registers
for the FIFO boundary but does not qualify the new external ingress boundary or
downstream ready. The full block assembler, source/consumer clock relationships,
latency and physical timing still need integration. Full analog/PHY implementation
and chip signoff remain incomplete; the chip is not tapeout-ready.

## Pass 70: ordered compactor for the wider receiver

Added the standalone `pt_lane_compact` primitive to turn a selected subset of an
eight-word beat into an ordered block with an explicit valid-word count. This
connects the earlier vector receiver algorithm toward actual RTL instead of
returning to single-word queue writes. The
[candidate contract](spec/parallel-datapath-candidate.md) specifies mask ownership,
zero-count suppression and concurrent wired/RF output requirements.

`make transceiver-lane-compact` passes 8,192 independently generated vectors over
all 256 masks, checking order, count and zeroed unused lanes. A reversed-source
mutation fails, and structural synthesis passes. The
[evidence](evidence/lane-compact.json) records source hashes. No physical timing,
area saving or fully integrated receiver claim is made.

Next connect validated scheduling/count masks to the two source compactors and
block queues, preserving the five-word header acceptance boundary. Existing
integrated transport RTL is unchanged; analog/PHY, gearboxes, wide transport and
physical signoff remain incomplete. The chip is not tapeout-ready.

## Pass 71: wire/RF block routing from schedule and remaining counts

Added combinational `pt_block_route`, connecting the transport schedule to two
concurrent lane compactors. It excludes header words, clips each source to its
remaining count, emits ordered blocks/counts and computes next counts. Disabled
routing has no payload and preserves counts. This advances the wide RX data path;
header validation and queue-commit control remain explicit integration work.

`make transceiver-block-route` passes 20,928 vectors from independent scalar slot
consumption over all 1,308 legal count pairs, every frame beat and both allow
states. The schedule-offset negative control fails and structural synthesis
passes. [Evidence](evidence/block-route.json) retains source hashes. These tests
cover routing, not header acceptance, clock-domain behavior or queue capacity.

Next integrate header/sequence validation and define how simultaneous source
outputs commit when a destination lacks capacity. The first beat must validate
its five header words before any same-beat payload effect. No default transport
RTL changed; full wide RX/TX, analog/PHY and physical signoff remain incomplete.

## Pass 72: parallel first-beat header validation

Added `pt_block_header`, validating all five header words combinationally and
returning counts/command fields only for an accepted header. It checks parity,
syndrome, tag, guard, sequence and semantics without correction. This is the
prerequisite for safely routing the first beat's three payload words.

`make transceiver-block-header` passes 51,510 vectors, including all legal count
pairs, every weight-1/2/3 header corruption around the tested mode codewords,
sequence tests, all opcode/argument combinations and quota violations. The
missing-syndrome negative control fails and structural synthesis passes.
[Evidence](evidence/block-header.json) records source hashes. Four-bit undetected
errors remain possible; no payload protection was added.

Header validation must next join the routing stage with frame state and an explicit
queue-commit policy. The combined combinational path is not yet timed, and command
latency, fault latching, acquisition and clock crossings remain integration work.
No default transport changed. Full analog/PHY, wide transport and physical signoff
remain incomplete; the chip is not tapeout-ready.

## Pass 73: integrated aligned eight-word RX candidate

Added `pt_block_rx`: parallel header validation, concurrent wired/RF compaction,
remaining counts, beat/sequence state and sticky fault behavior. Each beat commits
both required outputs together; a bad header or insufficient destination capacity
suppresses all same-beat effects and faults. This explicit beat-level policy does
not retract earlier beats or claim scalar fault-cycle equivalence. See the
[contract](spec/parallel-datapath-candidate.md).

`make transceiver-block-rx` passes 17,842 cycles against scalar payload/command
decoding plus the atomic beat policy. Coverage includes both modes, all legal
count pairs, sequence wrap, invalid cycles, header corruption, every beat's three
destination refusal combinations, sticky faults and reset/restart. The
capacity-bypass negative control fails, and structural synthesis passes.
[Evidence](evidence/block-rx.json) records the source hashes.

This is a functioning aligned wide RX candidate, not default transport replacement.
Acquisition, actual block queues, command acceptance/application latency, combined
header-to-queue timing and wide TX remain to be integrated and verified. Analog/PHY,
gearboxes and physical signoff remain incomplete; the chip is not tapeout-ready.

## Pass 74: include command acceptance in first-beat commit

Closed an interface gap in `pt_block_rx`: it now requires `command_ready` on every
first beat, alongside capacity for nonempty wired/RF outputs. No-op records also
participate. Refusal suppresses all same-beat effects and faults; later payload
beats ignore command readiness. This prevents payload commitment while silently
losing the associated command. Acceptance remains distinct from physical command
application, whose CDC and latency are still unspecified.

`make transceiver-block-rx` passes 21,202 cycles, including every readiness
combination across all supported commands with/without payload in both modes,
plus the earlier ordering/corruption/reset coverage. Whole-capacity bypass and
command-only bypass mutations both fail. Structural synthesis passes.
[Evidence](evidence/block-rx-command-ready.json) records the current source hashes;
pass-73 evidence remains historical. The expanded test generator initially encoded
multi-bit readiness masks into single-bit ports; converting each to a Boolean
corrected that testbench error before recording passing evidence.

Next provide actual destination queues and command retention/application behavior.
No default transport changed. Physical timing, acquisition, analog/PHY and full
wide-path integration remain incomplete; the chip is not tapeout-ready.

## Pass 75: assembled wide RX fails its first mapped timing screen

`make transceiver-block-rx-mapping` maps the current aligned receiver into the
nominal GF180 library and screens both fixed modes with 25.6 ns ideal clocks,
4 ns maximum input and output budgets, zero minimum budgets, 0.5 ns input
transitions and 5 fF output loads. It uses 29,687.8848 µm² cell area and 22
flip-flops. Small cell area does not establish speed.

The [retained report](evidence/block-rx-mapping.json) records **setup failures**:
−11.4461 ns in mode 0, from `words[20]` to `iq_words[30]`, and −11.2924 ns in
mode 1, from `words[20]` to `wire_words[12]`. These paths combine header decoding,
count selection/clipping, compaction and commit gating. Even removing the entire
8 ns provisional interface allocation would leave negative slack under this
unplaced model; widening alone has not solved the receiver logic depth. Mode 0
is also screened at the stricter high-profile block period rather than its 32 ns
period. Minimum timing slack is +0.4093 ns with ideal clocks and no uncertainty,
which is not hold signoff. No electrical violation rows are reported; external
reset is unconstrained.

This failed result changes the next step: pipeline the validated header/beat and
routing path before committing queue integration. Preserve frame order, first-beat
atomicity, command acceptance and fault suppression across in-flight stages; do
not simply add latency without defining their commit epoch. The report-generation
target succeeds only as reproduction of the retained failure, not timing closure.
Physical parasitics, CTS and mapped equivalence are still absent. Full analog/PHY,
wide TX and chip signoff remain incomplete; the chip is not tapeout-ready.

## Pass 76: pipeline header capture ahead of wide RX routing

Added separate `pt_block_rx_pipe` rather than overwriting the characterized failed
candidate. It captures the beat and validated header fields, then routes/commits
one cycle later using current destination readiness. Input frame tracking is
separate from committed residual counts. A failing commit discards younger pending
work and faults; reset flushes the stage. The
[contract](spec/parallel-datapath-candidate.md) records the additional one-beat
latency and preserved throughput/atomicity.

`make transceiver-block-rx-pipe` passes 21,219 cycles against a delayed scalar
reference with explicit pending work, including readiness changes between arrival
and commit. All three negative controls fail: capacity bypass, command-readiness
bypass and substituting live input-valid for stored-valid. Structural synthesis
passes. [Evidence](evidence/block-rx-pipe.json) retains source hashes.

No timing improvement is claimed until this partition is mapped. It may still
need further routing/compaction pipelining, with count dependencies and commit
latency preserved. Actual queues, acquisition, analog/PHY, wide TX and chip signoff
remain incomplete; the chip is not tapeout-ready.

## Pass 77: header pipeline improves but does not close wide RX timing

`make transceiver-block-rx-pipe-mapping` uses the same nominal library, 25.6 ns
period, ideal clocks, zero-wire RC and provisional 4 ns input/output allocations
as pass 75. The [retained report](evidence/block-rx-pipe-mapping.json) records
38,088.9152 µm² cell area and 131 flip-flops, versus 29,687.8848 µm² and 22
flip-flops before the pipeline. The added stage has a measurable area cost.

Setup still **fails** at −4.9203 ns in mode 0 and −4.9197 ns in mode 1, improving
by 6.5258/6.3727 ns from the unpipelined result. The worst path starts at mapped
register `_3046_`, whose Q connectivity belongs to the stored `beat` index, and
ends at `wire_words[30]`. Schedule selection, count clipping and compaction remain
in one stage. Minimum slack is +0.3481 ns on an input-valid path, without clock
uncertainty; this is not hold closure. No electrical violation rows are reported.

The new evidence changes the next optimization: separate schedule/rank selection
from compaction or predecode it into registered lane masks, preserving count
feedback, first-beat validation and atomic commit. More header-only staging would
not address the current worst path. Even reclaiming the 4 ns output allocation
would leave this path negative in this model. Both candidate implementations and
failed results remain available; no default transport changed. Actual queues,
physical timing, analog/PHY and wide TX remain incomplete; the chip is not
 tapeout-ready.

## Pass 78: predecode schedule into stored lane masks

Added the separate `pt_block_rx_mask` candidate and `pt_block_route_mask`. The
input stage now captures two schedule masks alongside its beat, moving owner
selection out of the count/compaction stage without another latency cycle. Header
exclusion, mode, count feedback and current-readiness commit behavior are retained.

`make transceiver-block-rx-mask` passes the same 21,219-cycle delayed scalar
reference and structural synthesis. All four negative controls fail, including
an intentional next-beat schedule-mask error. The
[evidence](evidence/block-rx-mask.json) retains source hashes. This is functional
evidence, not a claim of timing closure or formally proven cycle equivalence.

Next map the candidate to determine whether mask predecode addresses the critical
path or whether count clipping/compaction still needs restructuring. The earlier
failed candidates remain unchanged. Actual queues, acquisition, analog/PHY, wide
TX and physical signoff remain incomplete; the chip is not tapeout-ready.

## Pass 79: mask predecode has poor measured area/timing tradeoff

`make transceiver-block-rx-mask-mapping` repeats the nominal 25.6 ns screen with
unchanged 4 ns input/output allocations. The
[retained report](evidence/block-rx-mask-mapping.json) records 55,692.224 µm² cell
area and 147 flip-flops, up from 38,088.9152 µm² and 131 flip-flops for the prior
pipeline. Setup still fails at −4.5981 ns in both modes: only about 0.32 ns better
for roughly 46% more mapped cell area. This candidate is not selected as a
preferred implementation on those results.

The worst startpoint `_5038_` maps to `beat`; the endpoint is `iq_words[71]`.
Schedule owner lookup has moved out of the stage, but beat-zero selection of
header versus residual counts, clipping and compaction still form a long path.
This makes count/rank selection and compaction the next restructuring target,
not another round of header-only staging. Static analysis may include paths not
sensitizable in valid protocol operation; no functional timing exceptions were
introduced, and such exclusions would require proof before use.

Minimum slack is +0.4426 ns on an input-data capture path, with ideal clocks and
no uncertainty. No electrical violation rows are reported. This remains an
unplaced nominal screen, not physical speed, hold or mapped-equivalence evidence.
Earlier candidates and failed results are preserved. Full queues, acquisition,
wide TX, analog/PHY and physical signoff remain incomplete; the chip is not
 tapeout-ready.

## Pass 80: remove serial remaining-count dependency from routing

Added `pt_block_route_rank`, selecting lanes by schedule-prefix rank and computing
residual counts with one subtraction per source. The separate `pt_block_rx_rank`
candidate uses it without changing pipeline latency or commit behavior.

`make transceiver-route-rank` proves all-input binary combinational equivalence to
the prior mask router for data, counts and residuals, including arbitrary masks
and all six-bit counts. An off-by-one rank mutation fails the SAT proof.
`make transceiver-block-rx-rank` also passes 21,219 integrated reference cycles,
four negative controls and structural synthesis. The
[evidence](evidence/block-route-rank.json) retains source and proof-log hashes.

This is a verified structural rewrite, not an assumed speed improvement. Mapping
must determine whether rank computation and the unchanged compactor meet the
period at acceptable area. Queues, command application, acquisition, wide TX,
analog/PHY and physical signoff remain incomplete; the chip is not tapeout-ready.

## Pass 81: rank rewrite improves measured timing and area, still fails

`make transceiver-block-rx-rank-mapping` repeats the same nominal 25.6 ns timing
screen. The [report](evidence/block-rx-rank-mapping.json) records 52,004.288 µm²
cell area and 147 flip-flops. Relative to the mask/decrement candidate, cell area
falls by 3,687.936 µm² (about 6.6%) and setup margin improves by 3.3817 ns.
The behavioral equivalence proof from pass 80 gives this measured rewrite a
stronger basis than a timing-only trial.

Setup nevertheless **fails at −1.2164 ns in both modes**. The worst startpoint
`_4674_` maps to `beat`, and the endpoint is `wire_words[26]`. Beat-zero count
selection, rank-qualified selection and the generic compactor still form the
limiting cone. Minimum slack is +0.3987 ns with ideal clocks and no uncertainty.
No electrical violation rows are reported. These are unplaced TT/25°C/3.3 V
results with 4 ns input/output allocations, not physical closure. Removing an
interface budget merely to obtain positive slack is not justified.

The rank candidate is a useful functional optimization, but full receiver timing
still requires restructuring or staging count selection/compaction and then
physical verification. No default transport was replaced or throughput reduced.
Queues, command application, acquisition, wide TX, analog/PHY and tapeout signoff
remain incomplete.

## Pass 82: remove remaining counts from word-placement control

Added `pt_block_route_prefix` and the separate `pt_block_rx_prefix` candidate.
The router compacts all scheduled words, then retains only the permitted output
prefix. Counts/allow no longer control the compactor's placement network. The
rewrite preserves output zeroing, count updates, latency and commit behavior.

`make transceiver-route-prefix` proves all-input binary equivalence to the original
mask router, including full payloads/counts/residuals. An extra-lane leakage mutation
fails SAT. `make transceiver-block-rx-prefix` passes 21,219 reference cycles, four
negative controls and structural synthesis. The
[evidence](evidence/block-route-prefix.json) retains source and proof-log hashes.

This addresses the measured count-to-compaction dependency without changing rates
or assuming a looser interface budget. Timing/area must still be mapped; no speed
claim is made yet. Queues, command application, acquisition, wide TX, analog/PHY
and physical signoff remain incomplete; the chip is not tapeout-ready.

## Pass 83: compact-first RX passes nominal unplaced setup screen

`make transceiver-block-rx-prefix-mapping` uses the unchanged 25.6 ns period,
4 ns input/output allocations, TT/25°C/3.3 V library, ideal clocks and zero-wire RC.
The [retained report](evidence/block-rx-prefix-mapping.json) records +1.4126 ns
setup slack in both fixed modes, improving by 2.6290 ns versus the rank candidate.
Cell area is 50,968.1536 µm² with 147 flip-flops, about 2% smaller than the rank
implementation. This is the first assembled wide RX candidate to pass this
particular nominal setup screen without changing latency, rates or boundary budgets.

Minimum slack is +0.3481 ns without hold uncertainty or CTS. The worst setup
endpoint is `iq_words[10]`; no electrical violation rows are reported. The result
is a narrow success, not a physical timing guarantee: modest margin can disappear
with cell corner changes, routed loads and clock uncertainty. No external reset
timing, mapped functional equivalence or actual queue loads are qualified here.

The next step is physical/corner characterization of this candidate before treating
it as a replacement for the integrated receiver. The verified compact-first
identity supports retaining this implementation for that work, but whole-chip
speed, area and power remain unproven. Queues, command application, acquisition,
wide TX, analog/PHY and full tapeout signoff are incomplete.

## Pass 84: compact-first receiver fails available slow PVT screens

`make transceiver-block-rx-prefix-corners` characterizes the same nominally mapped
netlist with all five installed 3.3 V-class library corners. The
[retained report](evidence/block-rx-prefix-corners.json) verifies the mapped input
hash, reproduces nominal +1.4126 ns setup margin, and records library digests.
With the unchanged 25.6 ns period and 4 ns input/output allocations, setup slack
is +10.1635 ns at FF/−40°C/3.60 V, +4.9510 ns at FF/125°C/3.60 V,
−6.4298 ns at SS/−40°C/3.00 V, and −19.3801 ns at SS/125°C/3.00 V (both modes).
Minimum reported hold slack is +0.2347 ns at cold-fast, without uncertainty or CTS.
No electrical violation rows are reported. This remains an unplaced timing screen.

The narrow operating-temperature/supply acceptance is unchanged. These mixed PVT
corners are available stress cases, not an imposed −40°C-to-125°C or 3.0-to-3.6 V
specification, and they do not isolate process spread at the desired operating
point. Passing TT does not establish slow-process behavior there; failing these
extreme cases does not prove failure at that point either. The narrow envelope and
its supporting timing models still need qualification. Do not discard process
variation merely because ambient/supply will be controlled.

This evidence argues for more robust stage partitioning while the envelope remains
unqualified, or obtaining characterized bounds for that envelope before accepting
the path. Retain the failed stress results rather than marking the receiver closed
on nominal evidence. Queues, command application, acquisition, wide TX, analog/PHY
and physical signoff remain incomplete; the chip is not tapeout-ready.

## Pass 85: separate routed results from destination commit

Added `pt_block_rx_commit`, a two-stage candidate storing routed payload/counts
before destination-capacity checks and final output gating. It sustains one input
beat per cycle with two-cycle effect latency. Counts advance as internal planning
state; a failed older commit flushes younger work and faults until reset. The
[contract](spec/parallel-datapath-candidate.md) makes this speculative state and
fail-stop requirement explicit.

`make transceiver-block-rx-commit` passes 21,254 reference cycles, including oldest
command refusal with younger beats in flight and reset with both stages occupied.
All four negative controls fail and structural synthesis passes.
[Evidence](evidence/block-rx-commit.json) records source hashes. Existing candidates
remain unchanged. This adds pipeline storage/latency and is not an assumed timing
win; mapping must check routing-to-register and commit-to-output paths separately.

Actual queues, command application, acquisition, wide TX, analog/PHY and physical
signoff remain incomplete; the chip is not tapeout-ready.

## Pass 86: second RX stage materially improves corner timing

`make transceiver-block-rx-commit-mapping` and `make transceiver-block-rx-commit-corners`
map the two-stage candidate and screen the same five available library corners.
The [mapping report](evidence/block-rx-commit-mapping.json) records 69,234.4128 µm²
cell area and 330 flip-flops, versus 50,968.1536 µm² and 147 for the one-stage
prefix candidate. The extra stage therefore costs 183 mapped flip-flops and about
36% cell area, as well as the documented extra block-cycle latency.

At the unchanged 25.6 ns period and 4 ns input/output budgets, nominal setup margin
rises from +1.4126 to +9.7177 ns. The
[corner report](evidence/block-rx-commit-corners.json) records +15.4011 ns cold-fast,
+12.6477 ns hot-fast, +3.3003 ns cold-slow and **−6.8820 ns hot-slow**, in both modes.
The previously failing cold-slow stress case now passes this unplaced screen;
the hot-slow failure remains. These mixed PVT cases do not define or prove bounds
for the accepted narrow operating-temperature/supply envelope. No clock uncertainty,
wire RC or CTS is included, and physical hold/queue loading remain unqualified.

The measured improvement supports carrying the two-stage candidate into physical
characterization, not claiming tapeout readiness. Preserve the failed hot-slow
result and establish appropriate process-aware timing bounds for the actual
operating window. Further stage partitioning remains possible if physical data
requires it. Queues, command application, acquisition, wide TX, analog/PHY and
full-chip signoff remain incomplete.

## Pass 87: two-stage RX placement/CTS exposes setup and boundary hold limits

`make transceiver-block-rx-commit-physical` places the mapped two-stage receiver
in a requested 400 × 400 µm region, then builds one clock tree with 330 sinks and
31 tree buffers plus balancing loads. Tool placement checks pass before/after CTS.
The [report](evidence/block-rx-commit-physical.json) distinguishes pre-CTS area/
counts from the clocked artifact and retains artifact hashes. This is not an
independent physical-geometry audit or full-chip floorplan commitment.

With propagated clocks and placement-estimated Metal3 signal/Metal4 clock RC,
setup slack is +6.3217 ns nominal, +14.7345 ns cold-fast, −1.6075 ns cold-slow and
−13.5283 ns hot-slow in both modes. The cold-slow unplaced pass does not survive
this experiment. With the explicit 0.5 ns hold uncertainty and provisional zero
minimum input delays, hold also fails: −1.6869 ns nominal, −1.2199 ns cold-fast,
−2.1423 ns cold-slow and −2.7837 ns hot-slow. Worst hold starts are input payload
bits, not evidence of a failed internal storage protocol. Their launch timing is
relative to root clocks while capture clocks include insertion delay.

Do not hide the interface failures by assuming later input arrival: connect and
characterize the real upstream beat-launch registers/gearbox, including their
clocks and min/max delays. Setup still needs stage optimization or supported bounds
for the actual narrow operating envelope; the mixed PVT stress cases remain
informative, not a newly imposed environmental specification. No electrical
violation rows are reported, but routing, CTS equivalence, reset, source timing,
queues and full analog/PHY integration remain unqualified. The chip is not
tapeout-ready.

## Pass 88: prove explicit rank compactor; rebalance work toward analog

Added standalone `pt_lane_compact_rank`, replacing variable-position writes with
prefix-rank qualification and parallel OR selection for each output bit.
`make transceiver-compact-rank` proves all-input binary equivalence to the original
compactor for both packed data and count. Removing source-selection qualification
fails SAT. [Evidence](evidence/compact-rank.json) retains source/proof hashes.
No integrated receiver substitution or mapped timing improvement is claimed.

The user challenged the prolonged digital focus. The recent passes have refined
digital transport while actual RF and high-speed wired analog macros remain
unimplemented. This imbalance must change: the next pass should inspect existing
analog artifacts and implement/simulate a concrete transistor-level critical block.
Passing digital tests is not evidence for the central analog feasibility risks,
and the chip remains far from tapeout-ready. Retain the full companion scope and
narrow-temperature/supply acceptance; do not redefine success around the digital
portion that happens to be implemented.

## Pass 89: reuse real RF transistor core and test load sensitivity

Repository inspection corrected an overly broad prior status statement: substantial
analog source and physical experiments already exist under `ip/blocks/analog`,
including RF LNA/mixer and wireline CDR blocks. They are not an integrated analog
implementation for this programmable transceiver. Earlier claims that analog was
entirely unimplemented must be read as applying to this project's integration,
not the whole repository. Reuse and qualification should precede duplication.

`make transceiver-lna-load` now runs a concrete project-specific transistor-level
AC/DC sweep of the existing 16-finger GF180 `wifi_lna_cs_core`, using the pinned
analog-tools image. Fifteen cases hold supply at 3.3 V, temperature at 27°C and
external gate bias at 1.5 V while varying MOS process (typical/ff/ss) and added
output capacitance (0/50/200/500/1000 fF). The
[retained results](evidence/lna-load-screen.json) record circuit/runner hashes.
This pass adds the integration load experiment, not a newly invented LNA circuit.

Nominal 2.4 GHz source-to-output voltage gain falls from 1.347455 V/V with no added
capacitance to 0.9819596 V/V at 200 fF and 0.3170388 V/V at 1 pF. At slow process,
the corresponding gains are 1.258909, 0.9120001 and 0.2932053 V/V. Supply current
ranges from 3.025 to 4.491 mA across the three processes. This exposes output-load
sensitivity before claiming a useful integrated RF front end. Zero added capacitance
still includes device model capacitances; it is not a physically unloaded transistor.

The bench retains ideal external bias, 50-ohm source, 300-ohm drain resistor,
82-ohm source degeneration, 20 pF coupling and 5 kohm output load. These are explicit
bench components, not demonstrated on-chip matching/bias/passives. The run uses
schematic compact models, not existing PEX, and does not establish RF model validity,
noise figure, matching, linearity, blocker tolerance or a complete Wi-Fi radio.
Next connect and simulate the actual mixer input/load and assess bias/headroom,
rather than assuming the LNA's isolated gain survives integration. Narrow operating
conditions remain accepted; mismatch, parasitics and model limits remain open.

## Pass 90: directly connect the existing LNA and switching mixer

`make transceiver-lna-mixer` reuses the existing transistor-level
`wifi_rx_external_lo_parent`: the LNA drain directly drives the mixer's RF port.
This is a new project integration screen of existing circuits, not a newly
implemented mixer or a claim that the repository previously lacked composition.
Nine transient runs cover typical/fast/slow MOS process at fixed 3.3 V and 27°C,
with 0, 1 mV and 10 mV peak RF source amplitudes. The external LO is 2.3 GHz,
the RF is 2.4 GHz and the measured differential IF is 100 MHz. Existing ideal
bias, resistors, capacitors and external complementary LO sources remain bench
assumptions. This reference experiment does not adopt the older project's
100 MHz real-IF ADC architecture for our programmable I/Q companion.

The measurement integrates complex tone projections over three consecutive
20 ns windows after 40 ns settling. Exact window endpoints are interpolated
from adaptive transient samples; the explicit maximum time step is 1 ps.
Zero-input complex baselines are subtracted before computing conversion gain.
A separate analytic reference verifies amplitude and phase extraction with
nonuniform sampling, DC offset and a much larger 2.3 GHz component. Simulator
completion and finite waveforms are checked; positive gain alone is not a
receiver acceptance criterion. Decks, logs and waveforms remain under
`scratch/transceiver-lna-mixer`; retained numerical evidence follows below.

[Results](evidence/lna-mixer-screen.json): baseline-subtracted source-to-differential-IF
voltage gain at 1 mV input is 0.807637 nominal, 0.917709 fast and 0.625575 slow.
Changing input amplitude to 10 mV changes this gain by less than 0.0006 dB in
these cases. Zero-input 100 MHz residuals are below 1.75 nV, and the largest
relative complex-phasor change between the last two windows is 0.0000172.
These controls support the measured conversion; they do not establish noise
floor, large-signal linearity, input matching or power conversion gain.

The nominal zero-input shared drain swings from 1.408 to 1.680 V, substantially
below the isolated LNA's 2.179 V DC point. Across process, that range moves to
1.220–1.472 V fast and 1.573–1.904 V slow. The mixer therefore changes DC bias
and imposes periodic loading; the prior static-capacitance experiment cannot
represent it fully. Useful conversion exists in these ideal-boundary simulations,
but all three voltage gains are below unity. No matching/bias optimization,
RF model qualification, PEX, noise, mismatch, LO-driver implementation, I/Q
balance, actual sample/filter load, package or full-chip power closure is proven.
The next RF increment should confront the intended low-frequency I/Q interface
and its loading rather than treating this 100 MHz reference as the final receiver.

## Pass 91: expose the whole architecture and compose a quadrature RX candidate

The user's architecture question prompted a consolidated
[whole-chip diagram and implementation map](spec/block-diagram.md), linked at
the top of this document. It distinguishes the proposed full chip from the
implemented RTL, physical black boxes, and standalone analog research. It also
records that the current analog macro interface lacks the full intended
resource/control/diagnostic contract; diagram coverage is not implementation.

`analog/rx_iq_core.spice` now composes the existing transistor LNA with two
existing switching-mixer instances on the same drain. This is an experimental
single-ended internal topology, not the final differential RF pad interface.
`make transceiver-rx-iq` tests nominal 3.3 V/27°C/typical process with an ideal
2.4 GHz quadrature LO and RF tones at 2.39/2.41 GHz. Both branches have external
1 kohm/1 pF loads, with the prior external bias and LNA passives. A zero-input
control, opposite-sideband test and halved maximum timestep check accompany
complex 10 MHz measurements in successive 100 ns windows. Analytic extraction
checks include DC and large LO-frequency leakage. The circuit has no physical
LO driver, real converter/filter, transistor mismatch or extracted routing.

[Retained results](evidence/rx-iq-screen.json) show about 0.3164 V/V per I/Q
branch for the upper sideband and 0.3147 V/V for the lower. Q relative to I
changes from approximately +90° to −90°, with the dominant complex combination
changing accordingly. At zero RF input the shared drain swings 1.052–1.204 V
and average VDD current is 7.254 mA, excluding external LO/bias-source power.
A 1 ps versus 2 ps maximum timestep changes the upper-sideband complex phasors
by 0.183%; this supports approximate gain, not precision I/Q balance.

The ideal symmetric bench reports 112–139 dB sideband ratios, varying markedly
with timestep. These are numerical cancellation diagnostics, emphatically not
physical image rejection: mismatch, LO phase/duty errors, parasitics and DC
offsets are absent. Do not turn near-perfect simulated symmetry into a spec.
The previous one-mixer experiment used a different LO and IF, so the gain
change cannot be attributed solely to adding a mixer. A same-frequency
one-versus-two-branch comparison and bounded LO/load asymmetry are the next
useful controls before choosing the RX topology. Full differential integration,
real converter loading and autonomous LO generation remain unimplemented.

## Pass 92: isolate the cost of two mixers sharing one LNA drain

`make transceiver-rx-branch-load` runs a matched four-case comparison: one versus
two mixer instances, each with zero and 1 mV RF input. Both use 2.41 GHz RF,
2.4 GHz LO, the same 1 kohm/1 pF output loads, 1.5 V gate bias, 3.3 V supply,
27°C typical process and 2 ps maximum transient timestep. The only topology
change removes the Q mixer; its external output loads and LO sources remain.
This removes the frequency/IF confound in comparing passes 90 and 91.

The standalone template and generated single-branch core make this reproducible
without scratch artifacts from previous passes. `rf_measure.py` holds the
complex-window integration used for this experiment, preserving historical
runner sources. The measurement is checked against an analytic tone with DC
and strong LO-frequency content; a separate nonuniform-sample check also
rejects an incomplete measurement window. Per-topology zero-input subtraction
and consecutive 100 ns windows check the reported signal component.

[Retained results](evidence/rx-branch-load-screen.json): I-branch conversion gain
falls from 0.828787 V/V with one mixer to 0.316374 V/V with two, a ratio of
0.381731 (−8.36485 dB). Average VDD current rises from 6.03271 to 7.25418 mA.
Zero-input IF residuals stay below 0.955 nV; final-window relative phasor changes
are at most 0.0000203. Both two-branch cases reproduce the prior nominal screen.
This is direct evidence of a costly shared-drain topology under these bench
conditions, including its changed operating point and periodic load; it is not
an extracted input-capacitance value or a measured receiver noise penalty.

Do not freeze this topology merely because it converts a tone. The full-chip
map now explicitly leaves RF gain-to-mixer fanout open. Next compare a locally
isolated or separately driven pair of mixer branches, accounting for its input
loading, area and total bias/LO power. RF gain sharing is a possible optimization,
not a requirement that overrides noise, gain or headroom. The final differential
front end, physical LO drivers, converter/filter load, mismatch and PEX remain
open; no new RF performance envelope or tapeout readiness is claimed.

## Pass 93: separate LNA drain loads without hiding RF input fanout

`analog/rx_iq_split_core.spice` introduces a candidate with one existing LNA
core per mixer. Both gates connect to the same RF input; drain and source
nodes are independent, each with the same external 300-ohm drain load and
82-ohm degeneration used before. This adds a full LNA core rather than an
ideal buffer or lossless splitter. The finite 50-ohm RF source drives both
input capacitances through the same coupling capacitor and gate-bias resistor.

`make transceiver-rx-split` reruns shared and split topologies with zero and
1 mV input at the same nominal environment, 2.41 GHz RF and 2.4 GHz LO. It
measures both I/Q tone phasors, the actual RF gate tone, both drain swings and
total VDD current. The gate-tone metric exposes loading but is not an input
match or noise measurement. The shared case uses a zero-load observation
source only to duplicate its common drain voltage into the second diagnostic
column; this is not a functional RF buffer. Both cases keep ideal external LO,
bias, coupling and 1 kohm/1 pF IF loads. No final RF topology is selected yet.

[Results](evidence/rx-split-screen.json): split branches recover I/Q conversion
gain to approximately 0.821905 V/V each, versus 0.316374 V/V shared. Source-to-gate
RF amplitude changes from 0.994619 to 0.984873 V/V: this bench includes the
extra input loading rather than granting each branch an independent ideal
source. Average VDD current rises from 7.25418 to 12.06333 mA, excluding external
LO/bias power. Device count rises from 48 to 64 NFET fingers; placed area and
passive overhead cannot be inferred from that count alone.

Per-topology zero-input residuals are below 8.8 nV, and final-window relative
phasor changes are below 0.000026. Nominal quadrature remains near 90 degrees,
but ideal symmetry does not prove image rejection or mismatch tolerance. This
candidate recovers most of the prior single-mixer gain while providing both
branches, at substantial current and area cost. It is a useful comparison
candidate, not a selected final LNA or proof of acceptable receiver noise.

Next expose asymmetry in both directions (LO phase/duty, branch load and bias)
and include realistic LO-drive effort before optimizing away duplication.
Preserve the shared candidate for comparison. The RF domain's existing 48 mA
planning target is not a qualified supply limit or available residual budget:
converters, filters, TX, references, physical LO drivers and concurrent activity
remain unaccounted for. Differential pad integration, PEX, RF model validity,
noise and power closure still prevent a receiver or tapeout claim.

## Pass 94: deliberately break quadrature symmetry in both directions

`make transceiver-rx-phase` shifts both Q LO rails together by −5 ps or +5 ps
relative to the ideal I rails, retaining their pulse widths and mutual phase.
Each case has its own zero-input control and 1 mV, 2.41 GHz RF stimulus; the
split-LNA circuit, 2.4 GHz LO, loads and nominal operating point are unchanged.
The experiment measures actual I/Q amplitude and phase and the ratio of the
`I-jQ` and `I+jQ` tone combinations. This exposes a selected static error;
it does not simulate a characterized LO driver, jitter spectrum or mismatch
population, and ±5 ps is not a proven worst-case bound.

The [quadrature sensitivity note](spec/rf-quadrature-budget.md) derives the
single-tone phase/gain relationship and lists illustrative error allocations.
For equal gains, ±5 ps at 2.4 GHz means ±4.32 degrees and approximately 28.47 dB
image ratio. A phase-only 40 dB example permits only about 1.33 ps; these are
sensitivity examples, not newly imposed application requirements. They explain
why ideal symmetric schematic cancellation cannot establish physical rejection.

[Results](evidence/rx-phase-screen.json): −5 ps produces 85.6718° Q-to-I phase
and a 28.4525 dB complex sideband ratio; +5 ps produces 94.3278° and 28.4535 dB.
Both are close to the 28.4693 dB equal-gain prediction. Per-branch gain remains
approximately 0.822 V/V, while amplitude imbalance changes sign at approximately
±0.00445 dB. Gain alone therefore cannot qualify I/Q accuracy. Final-window
phasor changes remain below 0.000026 and zero-input IF residuals below 8.8 nV.
Generated-deck inspection confirms both delay signs and unchanged complementary
pulse shapes. This is still a 2 ps maximum-step schematic screen, not a precise
physical error bound, calibrated receiver or broadband image-rejection result.

The next priority is transistor-level LO-drive loading, waveform quality and
power, followed by phase/duty/load uncertainty with that actual driver. Static
phase correction remains an architectural obligation rather than demonstrated
calibration circuitry. Keep real converter headroom and error budgets visible;
do not rely on later FPGA correction to recover clipped or noisy samples.

## Pass 95: drive actual mixer gates with transistor CMOS buffers

`analog/lo_buffer.spice` implements a two-stage non-inverting buffer using
native GF180 3.3 V NFET/PFET models. Base inverter sizes are 4/8 micrometres
N/P, consistent with the elementary inverter in existing IF research; the
second stage is four times larger. This does not reuse that IF driver's large
feedback topology. A sizing multiplier of 0.5, 1 or 2 is screened. Multipliers
are schematic sizing candidates, not finished finger placement/layout.

`make transceiver-lo-buffer` drives both LO terminals of an actual existing
LNA/mixer parent at 2.4 GHz, with separate measured buffer and LNA supplies.
The ideal clock sources now drive only first-stage inputs; mixer gates see
transistor outputs. The LNA bias and external IF loads remain, with zero RF
input. Three 41 ns nominal runs use a 1 ps maximum step, measuring 20–40 ns.
Analytic tests cover nonuniform edge interpolation, plateaus, absent crossings
and time-weighted current averaging.

[Results](evidence/lo-buffer-screen.json):

| Buffer scale | Worst LO rise/fall, 10–90% | LO high fraction | Buffer-pair mean current | Rising delay |
|---|---|---|---|---|
| 0.5 | 73.17 / 55.85 ps | 53.82% | 1.585 mA | 106.37 ps |
| 1 | 57.67 / 47.40 ps | 52.50% | 2.792 mA | 100.45 ps |
| 2 | 49.69 / 43.48 ps | 51.73% | 5.252 mA | 97.13 ps |

Both LO outputs show 48 rising and falling mid-supply crossings in the 20 ns
measurement window, with full-swing transition measurements. These waveforms
are materially slower than the previous 10 ps ideal-source edges. Outputs
extend approximately −0.079 to +3.414 V across the sizing cases; device-terminal
stress and extracted ringing must be qualified, not inferred safe from a
successful simulator run. Equal ideal input spacing and identical circuits do
not establish real phase matching or non-overlap. High fractions above 50%
make the prior ideal complementary waveforms an insufficient mixing model.

Only two buffers are measured; I/Q needs four, and their actual integrated
loading/power must be simulated. Upstream phase-generator/oscillator drive
power is excluded, as are PEX, mismatch, supply noise and process variation.
The observed current/slew tradeoff does not select a final size. Next connect
these actual buffers to the split I/Q receiver and measure conversion and
quadrature, then stress the selected candidate across process and bounded
loading. A working buffer alone does not implement an autonomous RF LO or
qualify the receiver, clock network or chip for fabrication.

## Pass 96: compose four transistor LO buffers with the split I/Q receiver

`analog/rx_iq_buffered_core.spice` is a hierarchical transistor-level candidate:
two LNA/mixer branches and four scale-1 two-stage LO buffers. Four external
quadrature clock sources now drive buffer inputs; mixer gates are internal
buffer outputs. RF gates still share one finite-impedance source, and each LNA
retains its own externally supplied drain/source loads. This removes the ideal
LO source at the mixer boundary without claiming an implemented oscillator or
quadrature generator.

`make transceiver-rx-buffered` compares ideal and buffered LO configurations,
each with zero and 1 mV RF input. The same 2.41 GHz RF/2.4 GHz LO, typical
3.3 V/27°C environment, 1 kohm/1 pF IF loads and 2 ps maximum timestep apply.
Separate ideal supply sources measure LNA/mixer and four-buffer currents;
reported totals exclude upstream clock generation, converters, bias generation
and other chip functions. Both final 100 ns windows are measured after 100 ns
settling, with a per-configuration zero-input subtraction.

[Results](evidence/rx-buffered-screen.json): transistor-buffered conversion gain
is 0.788775 V/V for I and 0.788773 V/V for Q, compared with 0.821905 V/V under
ideal LO drive. LNA/mixer current is 11.90218 mA; the four buffers draw
5.58557 mA, giving 17.48774 mA through the two measured supplies. These are
actual integrated schematic currents, not twice an isolated buffer extrapolation.
The JSON `current_ratio` field compares LNA/mixer current only; the explicitly
named per-case `measured_total_current_a` includes buffers and is the relevant
subtotal for this composition.

The nominal Q/I phase is 90.00034°, with negligible ideal-symmetry amplitude
imbalance. This does not establish manufactured quadrature accuracy. Zero-input
IF residuals stay below 13.4 nV, and final-window relative phasor changes below
0.000042. The real buffer waveform therefore preserves tone conversion in this
nominal experiment with a modest gain reduction, while materially increasing
measured current. RF noise, blockers, mismatch, PEX, safe device stress and
process spread remain unqualified. Neither gain nor current is a worst-case
bound, and the ideal-drive comparator excludes the power of its ideal LO sources.

Next test this composed circuit across MOS process at the accepted fixed supply
and temperature, then loading/phase asymmetry. Preserve separate clock-generation,
converter/filter, TX and digital budgets; 17.49 mA is a receiver subcircuit subtotal,
not chip current or RF-domain power closure. No completed on-chip PLL, quadrature
generator, differential pad interface or tapeout-ready analog macro is claimed.

## Pass 97: process variation with the buffered receiver at fixed supply/temperature

`make transceiver-rx-process` runs the composed split-LNA/four-buffer receiver
with GF180 `ff` and `ss` global MOS model sections, holding both supplies at
3.3 V, temperature at 27°C and external gate bias at 1.5 V. Each corner has a
zero-input and 1 mV RF transient with the same 2.41 GHz RF/2.4 GHz LO and IF
loads as pass 96. This tests process variation without imposing a broad
supply/temperature requirement on the accepted narrow operating envelope.

Per-corner baseline subtraction, two settled measurement windows and separately
measured LNA/buffer supply currents are retained. The result's historical
`topology` labels identify process corners in this runner, not different circuit
topologies. Neither global FF/SS nor the nominal reference establishes local
mismatch, opposing N/P corner behavior, passive variation or production yield.
The source remains single-ended internally; autonomous LO/bias generation,
differential RF pad integration, converter/filter load and full-chip isolation
are still absent.

[Retained process results](evidence/rx-process-screen.json), with the nominal
reference from [pass 96](evidence/rx-buffered-screen.json):

| Global MOS corner | I-branch conversion gain | LNA/mixer current | Four LO buffers | Measured subtotal |
|---|---|---|---|---|
| Fast | 0.905900 V/V | 13.07418 mA | 5.95747 mA | 19.03165 mA |
| Typical (pass 96) | 0.788775 V/V | 11.90218 mA | 5.58557 mA | 17.48774 mA |
| Slow | 0.589986 V/V | 10.63765 mA | 5.20808 mA | 15.84572 mA |

Tone conversion survives both tested global process extremes at the same supply,
temperature and gate bias. Slow-process gain is about 25% below nominal; the
observed range must inform downstream gain/headroom design rather than being
hidden behind the typical case. No required gain, noise or EVM threshold has
been met merely because conversion remains nonzero. Source-to-gate amplitudes
remain 0.98086–0.98650 V/V; this is still not an input match measurement.

Zero-input IF residuals remain below 11.5 nV; the largest final-window relative
phasor change is 0.000097. Nominally matched branches remain close to quadrature
in these deterministic global-corner models, which do not contain random
branch mismatch. Generated-deck inspection confirms fixed temperature/supplies,
the intended model sections and the buffered DUT in all four cases. Next
include opposing NMOS/PMOS corners and explicit loading/phase asymmetry before
using this candidate to set calibration ranges. Oscillator, converter, TX,
physical rail/package, noise and extracted reliability obligations stay open.

## Pass 98: use the PDK's mixed MOS corners

The installed model library explicitly provides `fs` and `sf` sections in
addition to typical/ff/ss. `make transceiver-rx-mixed-process` uses those native
sections for the buffered split-I/Q receiver with zero and 1 mV RF input at
fixed 3.3 V/27°C and 1.5 V external bias. It does not synthesize mixed corners
by manually combining unrelated model cards. These additional global cases
address variation omitted by same-direction FF/SS; they still do not establish
local mismatch, yield or a complete worst-case envelope.

The shared process runner now accepts `--corners`, validates names and rejects
duplicates; its default remains ff/ss for the pass-97 target. New reports hash
the design/model entry files as well as the circuit, template and runner. The
pinned container remains the full model-environment reference; these entry-file
hashes alone are not a recursive dependency manifest. Historical reports keep
the runner hashes from their original executions.

[Mixed-corner results](evidence/rx-mixed-process-screen.json): FS gives
0.841241 V/V I gain and 18.30873 mA measured receiver/buffer subtotal; SF gives
0.676077 V/V and 16.60201 mA. These lie inside the observed FF/SS range.
Zero-input IF residuals remain below 16.9 nV; final-window relative phasor
changes are at most 0.000102. Near-90° global-corner quadrature still relies
on identically modeled branches and is not a mismatch result.

`verification/summarize_rx_corners.py` checks identical circuit/template hashes,
frequency, voltage, temperature, timestep and tool image before combining the
retained results. The [five-corner summary](evidence/rx-five-corners.json) records
observed I gain 0.589986–0.905900 V/V and measured current 15.84572–19.03165 mA.
It preserves input-report hashes and explicitly labels this an observation,
not signoff. Those numerical extrema are not proven bounds over all process,
passive, mismatch, supply or loading combinations. No model is accepted simply
because every corner still converts a tone.

Next expose unequal I/Q loading and replace the ideal IF termination with a
concrete baseband/filter/converter interface. Keep the branch gain and headroom
spread visible when sizing that interface; noise, linearity, clock-generation,
differential pad integration, PEX and complete chip power remain unresolved.

## Pass 99: expose the receiver to a real switched sampling load

Inspection of the existing IF source-follower probe found a different design
boundary: its 100 MHz low-output-impedance setting draws up to approximately
0.991 A per output under ideal bias. It is not a suitable drop-in implementation
for this RF domain, and its older converter assumptions are not inherited here.

`analog/iq_sampler.spice` instead composes two existing transistor transmission
gates with four ideal 5 pF hold capacitors. `make transceiver-rx-sampler` connects
this diagnostic sampler directly to the buffered receiver at 40 MS/s, retaining
the previous 1 kohm/1 pF mixer loads. Sampling clocks are ideal complementary
200 ps edges with approximately half-period tracking. This is added switched
loading, not a completed baseband filter, sample-clock driver or ADC.

Zero and 1 mV RF runs retain raw held samples and tone phasors. Eight samples
at 124, 149, …, 299 ns cover two 10 MHz periods near the end of each hold phase.
A separate analytic check validates extraction of a known tone with a much
larger DC offset. Per-sample zero-input subtraction measures the small signal;
it does not implement offset calibration or establish available ADC headroom.
This short coherent-clock test does not bound arbitrary RF/LO/sample phase,
frequency response, settling, aperture jitter or alias rejection.

For 5 pF per leg, the ideal independent-capacitor differential expression
`sqrt(2*k*T/C)` gives approximately 40.7 µV RMS at 27°C. This is a calculation,
not transistor noise simulation or receiver noise qualification. It excludes
amplifier, switch, reference and folded noise. Capacitance is a diagnostic
choice, not frozen ADC sizing; baseband gain and noise must be budgeted before
assigning effective resolution. The 8/12-bit transport format still does not
promise that ENOB.

[Results](evidence/rx-sampler-screen.json): baseline-subtracted held-tone gain
is 0.767300 V/V on I and 0.766958 V/V on Q. Tone transfer survives this nominal
switched load, but the zero-input held values are approximately −4.3 mV I and
+24.3 mV Q at the selected coherent clock phase—far larger than the roughly
0.77 mV peak wanted output from a 1 mV RF input. Retained raw samples make that
failure of the ideal zero-offset assumption visible. These offsets combine
periodic mixer behavior and sampling effects; this experiment does not uniquely
attribute them to one device mechanism.

Direct sampling is therefore not accepted as the final interface merely because
baseline-subtracted gain remains useful. A real anti-alias/baseband filter,
gain/common-mode plan and offset/headroom management are required. The coherent
LO/sample ratio can fold periodic feedthrough into static offsets in this test;
other frequencies/phases may produce in-band components that constant offset
subtraction cannot remove. Next design and test a bounded filter interface
against this actual switched load, while preserving the full 20 MHz-channel
objective and accounting for its noise and settling. A complete converter,
physical sampling clock, matching, noise and extracted implementation remain
open, so this is not ADC or radio qualification.

## Pass 100: add a finite RC interface before the sampling switches

`analog/iq_rc_filter.spice` adds one 330-ohm series resistor and 15 pF shunt
capacitor per mixer-output leg before the existing 5 pF transmission-gate
sampler. The original mixer loads remain. `make transceiver-rx-filtered-sampler`
repeats zero/1 mV input runs with the same buffered RF receiver, clocks, nominal
environment and sample-phase extraction as pass 99. This replaces the direct
mixer-to-switch connection with a concrete passive network, while retaining
ideal resistor/capacitor models pending PDK passive selection and layout.

The isolated ideal-input pole is 32.15 MHz, not the actual loaded response.
Mixer source impedance, charge redistribution and tracking duty alter it.
Furthermore, one such ideal pole attenuates 30 MHz only modestly relative to
10 MHz; at 40 MS/s those frequencies can alias onto each other. It cannot be
accepted as a complete anti-alias filter based on rejecting 2.4 GHz feedthrough
or preserving one desired tone. Filtering ahead of the switch also does not
remove clock injection created downstream by that switch.

The experiment retains raw zero-input held offsets and sample-domain tone
gain. It does not hide headroom problems by treating per-run subtraction as
implemented calibration. The prior ideal hold-capacitor kT/C calculation is
still reported with an explicit limitation: it omits resistor/filter noise and
is not a noise result for this new network. No baseband gain, ADC, active common-
mode control, offset cancellation or physical clock-generation circuit is added.

[Results](evidence/rx-filtered-sampler-screen.json): held-tone gain becomes
0.557162 V/V I and 0.557130 V/V Q, versus 0.767300/0.766958 for direct sampling.
The zero-input held offsets drop to approximately +0.54 mV I and −0.05 mV Q
at the selected coherent clock phase. The filter therefore substantially
reduces the observed feedthrough/offset in this case, at approximately 2.8 dB
additional desired-tone loss. The remaining I offset is still comparable to
the 0.557 mV peak wanted output for this 1 mV RF input; offset/headroom remains
an interface requirement rather than a solved problem.

Circuit/template hashes for the receiver, LO buffers and sampler match pass 99,
so the new passive network is the intentional circuit change. These are short
nominal, coherent-clock observations with eight extracted samples, not bounds
on arbitrary sample phase or a measured channel response. Keep this candidate
for comparison; do not freeze it as the final filter. Next measure an actual
out-of-band tone that aliases into the sampled passband, then choose the required
filter order and gain/buffer interface from the resulting attenuation and
loading. Preserve the full approximately 20 MHz-channel target rather than
silently narrowing bandwidth to make attenuation easier.

A post-run settling check limits the interpretation further: the zero-input
sample sequence contains approximately 3.8–4.0 µV of apparent 10 MHz component,
and phasors from its first versus second four-sample windows differ by
7.5–8.0 µV. The baseline-subtracted signal-window difference is much smaller
(0.15–0.19 µV), but raw offset settling is not established. The inherited report
wording “settled samples” is therefore too strong for this filter candidate.
Extend the settling interval and test window convergence before interpreting
small residual offsets or comparing alias rejection. Baseline subtraction must
not conceal startup drift in the hardware interface.

## Pass 101: verify late-window convergence before judging the RC interface

`make transceiver-rx-filtered-settling` extends each zero/1 mV filtered-receiver
run from 301 to 601 ns without changing circuits, stimuli or maximum timestep.
The shared runner accepts `--extended`; its short mode remains available and
its inherited claim of automatically “settled samples” has been removed.
Twenty samples span 124–599 ns. Only the last eight samples determine final
gain and mean offsets; five separate four-sample windows retain the startup
history. Continuous IF projections also move to the late 400–600 ns interval.

The convergence report compares both raw zero-input and baseline-subtracted
sample streams, checking changes in window means and complex 10 MHz phasors.
A 100 nV last-window change is an explicitly chosen numerical diagnostic,
not a converter accuracy, noise, yield or analog performance requirement.
A stable difference waveform alone cannot demonstrate raw-offset settling;
both streams are retained and evaluated independently. This does not test
arbitrary power-up conditions, reset sequences, sample phase or input frequency.

[Extended-run evidence](evidence/rx-filtered-settling.json): both tested streams
meet the selected 100 nV last-window diagnostic. Raw zero-input mean change is
33.06 nV and phasor change 0.059 nV; baseline-subtracted signal mean change is
16.74 nV and phasor change 38.79 nV. These refer only to the final two 100 ns
windows under this startup and coherent-clock condition, not guaranteed
long-term equilibrium or startup time across corners.

Late held-tone gains are 0.557219 V/V I and 0.557271 V/V Q. Mean zero-input
held offsets remain +0.538396 mV and −0.050113 mV. The late apparent zero-input
10 MHz components fall to 5.38/11.69 nV, versus approximately 3.8/4.0 µV in
the short record. The earlier apparent component was therefore strongly
contaminated by startup drift; the persistent mean offset is not removed by
waiting longer. A slow residual mean trend remains visible in the retained
window history, so do not relabel this limited diagnostic as complete settling
or drift qualification.

All five phasor-window positions and the late two-window extraction were checked
against an analytic tone with DC offset. The next filter comparison should use
this extended measurement timing and an RF tone producing 30 MHz IF, which can
alias into the 40 MS/s sampled passband. This test has not improved alias
rejection or added the missing gain/offset-control circuit; it makes the
measurement trustworthy enough to assess that next change without relying on
startup artifacts.

## Pass 102: measure an unwanted tone that aliases into the wanted band

`make transceiver-rx-alias` uses the same filtered, buffered receiver and sampler
with a 2.43 GHz RF tone and 2.4 GHz LO, producing 30 MHz analog IF. Sampling
at 40 MS/s aliases the complex tone to −10 MHz, an edge of the intended roughly
20 MHz complex channel. A digital notch there would also discard wanted-band
content; the unwanted component must be controlled before sampling.

The shared runner now accepts `--if-mhz 10|30` with 10 as default. Analog IF
projections follow the actual IF, while sample-domain extraction remains at
10 MHz magnitude and records quadrature sign. An analytic two-channel test
verifies equal sample amplitudes and opposite quadrature signs for 10/30 MHz
inputs, including DC offsets and the exact late sample instants. The new run
uses the same 601 ns duration and late-window convergence diagnostics as pass
101, with a fresh zero-input control.

The comparison measures total receiver-plus-filter-plus-sampler response, not
an isolated RC transfer function. It is a small-signal, separate-tone experiment,
not a simultaneous blocker, compression, intermodulation or compliance test.
Keep the filter unqualified until measured attenuation and full band response
meet an explicit application-derived budget; positive wanted-tone gain is not
a substitute for that requirement.

[Alias results](evidence/rx-alias-screen.json) and the
[checked comparison](evidence/rx-alias-comparison.json) show sampled gains of
0.279094/0.279120 V/V for the 30 MHz analog IF, versus 0.557219/0.557271 for
the wanted 10 MHz reference. Relative chain rejection is only 6.0055 dB on
each channel. Sampled Q relative to I is −90.0085°, consistent with folding
to the opposite complex-band edge. Both raw-zero and subtracted-signal late
windows meet the same 100 nV convergence diagnostic; the largest signal
phasor change is 22.2 nV. Actual decks confirm the intended 2.43 GHz stimulus
and 601 ns duration.

This rejects the single-pole RC network as the complete anti-alias solution
for the intended general-purpose receiver. It reduced clock-related offsets,
but equal-amplitude out-of-band input still produces approximately half the
wanted output amplitude at an indistinguishable sampled band location. In a
linear extrapolation, only twice the unwanted input amplitude would make its
sampled contribution comparable to the desired signal; no strong-blocker
experiment has been performed here.

The next implementation increment must provide steeper programmable baseband
filtering together with an explicit gain/drive interface, rather than trying
to remove this alias after conversion or silently narrowing the intended
channel. Filter order, noise, passband shape, tuning and current must be designed
and verified together. The exact application-derived rejection requirement
remains open; the observed 6 dB is evidence against the current topology, not
a newly invented universal Wi-Fi or SDR compliance threshold.

## Pass 103: implement a real baseband gain primitive for filter development

`analog/bb_diff_gain.spice` adds a transistor differential pair with a transistor
tail sink and externally supplied tail-gate bias. Each input device is 16 × 4 µm,
and the tail is 32 × 4 µm, all native 0.28 µm-channel GF180 NFET models. Crossed
output labels give positive differential voltage gain with the bench's resistor
loads. Multipliers describe schematic sizing, not finished layout or matching.

`make transceiver-bb-gain` runs nine nominal DC/AC cases: gate bias 0.85/0.95/1.05 V
and 1/5/20 pF per output, with 1 kohm loads to 3.3 V. Input common mode is
1.177 V, chosen from the observed filtered receiver output; it is not a qualified
input range. AC excitation is normalized to one differential volt for transfer
measurement, not a demonstrated one-volt input swing. The MOS tail sink replaces
an ideal current source, but its compliance and bias regulation are not proven.

[Results](evidence/bb-gain-screen.json), for 5 pF per leg:

| External tail bias | Output common mode | Supply current | Gain at 10 MHz | Gain at 30 MHz |
|---|---|---|---|---|
| 0.85 V | 2.530 V | 1.540 mA | 4.728 V/V | 3.851 V/V |
| 0.95 V | 2.006 V | 2.589 mA | 5.656 V/V | 4.677 V/V |
| 1.05 V | 1.624 V | 3.352 mA | 6.027 V/V | 5.028 V/V |

At 1.05 V bias, increasing load to 20 pF reduces 10 MHz gain to 4.480 V/V and
adds approximately 43.7° phase lag. The tail node is only 0.202 V above ground
in that setting; device operating-region and compliance checks are required
before treating the tail as a well-regulated current source. Gain alone cannot
select the operating point. Output common-mode movement is substantial, and
the next stage would not automatically see the 1.177 V input condition tested
here. One copy per I/Q channel at that setting would cost roughly 6.70 mA at
this nominal bias before any additional filter stages or bias circuitry.

This is a concrete gain/transconductance primitive, not an implemented
higher-order filter or sampler driver. Loads and bias generation remain ideal
bench elements; there is no common-mode feedback, degeneration/linearity
qualification, mismatch/noise analysis, PEX, or switched-load settling proof.
Next characterize input common-mode sensitivity and large-signal behavior, then
close the common-mode/bias interface before using this primitive in a tunable
filter section. Preserve the 20 MHz channel objective and account for every
additional gain/drive stage in the RF current and area budgets.

## Pass 104: expose baseband common-mode sensitivity and finite signal swing

`make transceiver-bb-swing` runs twelve 10 MHz transient cases on the actual
differential gain cell at fixed 1.05 V external tail bias, 3.3 V/27°C typical
process, 1 kohm loads and 5 pF per output. Input common mode is 0.9, 1.177,
1.624 or 2.0 V; differential input peak is 1, 20 or 100 mV. The 1.624 V case
represents the previous stage's nominal output level, making this a relevant
cascade-interface check rather than a one-operating-point gain demonstration.

[Results](evidence/bb-swing-screen.json):

| Input common mode | Gain at 1 mV peak | Output CM at 1 mV | Current at 1 mV | Gain change at 100 mV | H2–H5 ratio at 100 mV |
|---|---|---|---|---|---|
| 0.900 V | 5.070 V/V | 2.406 V | 1.788 mA | −0.047 dB | 0.145% |
| 1.177 V | 6.026 V/V | 1.624 V | 3.352 mA | −0.027 dB | 0.0833% |
| 1.624 V | 4.938 V/V | 1.012 V | 4.577 mA | −1.048 dB | 3.665% |
| 2.000 V | 0.534 V/V | 0.893 V | 4.813 mA | +0.124 dB | 0.458% |

H2–H5 means the root-sum-square of harmonics 2 through 5 divided by the
fundamental, not an exhaustive noise/distortion or EVM measurement. Analytic
validation recovers a known 2% third harmonic in the presence of DC. Successive
200 ns fundamental windows agree closely in these deterministic simulations;
no numerical/mismatch/noise precision beyond the tested setup is inferred.

The candidate is not accepted as a freely cascadable filter/gain cell. Changing
only common mode alters gain, quiescent current, output level and distortion;
the input level delivered by one stage does not preserve the behavior measured
for the next. Fixed tail gate bias is not regulated current, and the output
has no common-mode feedback. A finite load capacitance in this test also does
not establish driving a real sampled load or another transistor stage.

Next rework tail compliance/common-mode regulation (or choose a better input-
pair topology) and verify an actual cascade before synthesizing the filter from
these cells. Maintain explicit input/output common-mode ranges, gain/swing and
current allocations. The small-signal 6× result remains valid at its tested
point but cannot justify a higher-order filter, universal analog tile or
receiver linearity claim.

## Pass 105: PMOS input alternative and an actual two-cell cascade

`analog/bb_pmos_gain.spice` replaces the NMOS-input topology with PMOS inputs
and a PMOS tail source. Inputs are 32 × 4 µm each and the tail 64 × 4 µm;
loads connect to ground. This doubles the nominal transistor widths relative
to the NMOS candidate, not necessarily its physical area. External bias and
ideal load passives remain; there is still no common-mode feedback.

`make transceiver-bb-pmos-gain` screens three bias settings and three output
loads. At 2.25 V tail-gate bias, 1.177 V input common mode and 5 pF per output,
[DC/AC evidence](evidence/bb-pmos-gain-screen.json) gives 4.653 V/V gain at
10 MHz, 1.069 V output common mode and 2.137 mA. The tail node is 2.438 V,
leaving approximately 0.862 V to the 3.3 V supply. That is more voltage headroom
than the preceding NMOS case, not a substitute for operating-region verification.

`make transceiver-bb-pmos-swing` repeats the twelve common-mode/amplitude cases
at that bias. [Transient results](evidence/bb-pmos-swing-screen.json) show
1 mV-input gains of 4.557, 4.653, 4.740 and 4.621 V/V at 0.9, 1.177, 1.624
and 2.0 V input common mode. At 100 mV differential peak, H2–H5 ratios remain
approximately 0.124–0.166%, with less than 0.06 dB gain reduction. These are
limited deterministic harmonic measurements, not receiver linearity or noise
qualification. Output common mode still moves from approximately 1.105 to
0.859 V across the tested inputs; bias regulation is not solved.

`make transceiver-bb-pmos-cascade` directly connects two actual transistor
cells with 1 kohm/5 pF loads on each stage, including the second cell's gate
loading. [Cascade DC/AC results](evidence/bb-pmos-cascade-screen.json) give
21.401 V/V at 10 MHz, 13.676 V/V at 30 MHz and 4.304 mA total. The first and
second output common modes are 1.069 and 1.083 V for the 1.177 V input. This
is a measured nominal small-signal cascade, not a multiplication of two ideal
independent gains. It does not establish cascaded large-signal swing or stability
under feedback, and its modest 10-to-30 MHz selectivity is not the required
higher-order anti-alias response.

Carry the PMOS topology forward as the better common-mode candidate while
retaining the NMOS results. Next develop a controlled filter section using the
actual finite-gain/loading behavior and verify its cascade, tuning and signal
swing. Process/mismatch, common-mode/bias control, noise, physical passives,
sampler drive, area and full current budget remain open; this is not a complete
baseband filter, ADC driver or tapeout-ready analog macro.

## Pass 106: close feedback around a transistor filter section

`analog/bb_filter_section.spice` connects two PMOS gain cells with crossed
resistive feedback to the first input pair. Input resistors are 1 kohm;
feedback resistors and four equal per-node capacitors are design parameters.
The output-to-opposite-input connection provides negative differential feedback
around two positive-gain stages. Both stages retain real finite gain, transistor
input loading and 1 kohm ground-referenced output loads. This is not an ideal
op-amp or behavioral filter. Parameter sweeps do not implement physical tuning
switches, capacitor banks or a calibration algorithm.

`make transceiver-bb-filter` checks nine nominal AC/DC configurations with
10/20/50 kohm feedback and 15/20/25 pF per node. The
[retained evidence](evidence/bb-filter-screen.json) reports gain, peak response,
10 MHz edge change and 10-to-30 MHz relative attenuation. Increasing selectivity
can cost wanted-band gain or introduce peaking; no single scalar gain is used
as an acceptance criterion.

At 20 kohm and 20 pF, gain is 10.249 V/V at 1 MHz and 9.148 at 10 MHz:
−0.987 dB edge change, with 12.839 dB relative attenuation at 30 MHz. The
corresponding real-circuit 1 mV differential pulse reaches 10.248 mV with 4.77%
overshoot, then settles and returns near zero after the input pulse. This is
one nominal transient trajectory, not loop-gain/phase-margin, common-mode-loop,
startup, large-signal or all-corner stability qualification.

Other examples: 10 kohm/25 pF gives +0.213 dB edge change and 13.930 dB
relative attenuation; 50 kohm/15 pF gives −1.854 dB and 11.141 dB respectively.
These provide candidate sections for a steeper cascade, but multiplying their
isolated responses is not sufficient verification. Actual cascading changes
common mode, gate loading and feedback behavior. The present bench includes
only section capacitances, not the receiver source impedance or switched ADC
load, so its rejection is not directly interchangeable with pass 102's sampled
end-to-end result.

Next compose a higher-order candidate using actual sections, verify its band
shape and transient response, then connect the real receiver/sampler interface.
Bias, physical passives/banks, noise, mismatch, process, drive/headroom, feedback
stability and the full current/area allocation remain open. This is progress
toward a programmable baseband filter, not a qualified complete filter or chip.

## Pass 107: refocus on autonomous RF timing and falsify a clock interface

The user's high-level review redirects work from isolated baseband refinement
to foundational risks. [The priority map](spec/risk-priorities.md) preserves full
RF/wired/converter/host scope and distinguishes real progress from nominal
sub-block optimization. The previous goal turn was a priority-setting response;
this pass adds actual circuit evidence.

Existing `wireline_serdes/pll` contains substantial oscillator, bias-bank,
restorer, divider and physical work. Its selected autonomous bank targets
1.25 GHz for a half-rate wired receiver, not an already qualified 2.4 GHz RF
quadrature source. Older 2.5 GHz ring experiments remain useful candidates.
The existing fast CML-to-CMOS sampler is clocked and cannot simply generate the
clocks needed for its own operation.

`make transceiver-rf-vco-load` reuses the existing three-stage CML ring and its
output buffer, with real p-poly model loads and MOS capacitors (load length
5.25 µm, capacitor W/L 4/3 µm). Six nominal 3.3 V/27°C transient cases cover
VCTRL 0.88, 1.08 and 1.30 V, either with 25 fF per output alone or with the
actual CMOS LO buffers and biased LNA/mixer load added. A deliberate 4 mV
initial-condition differential seed is retained from the research methodology;
this is free-running after initialization, not proof of unassisted startup.

[Results](evidence/rf-vco-load-screen.json):

| VCTRL | Reference oscillation | Loaded oscillation | Loaded VCO current | CMOS LO result |
|---|---|---|---|---|
| 0.88 V | 2.2900 GHz | 2.2907 GHz | 3.951 mA | Both outputs remain near 3.3 V |
| 1.08 V | 2.4999 GHz | 2.5007 GHz | 8.409 mA | Both outputs remain near 3.3 V |
| 1.30 V | 2.4774 GHz | 2.4783 GHz | 11.838 mA | Outputs remain above approximately 2.918 V |

All loaded cases have sustained CML differential oscillation but no LO-output
mid-supply crossings. The direct CML-to-CMOS-buffer connection therefore fails
as a mixer clock source. This is a real voltage/slew interface failure, not a
failure to generate a GHz oscillation in the isolated ring. Low measured RF
current in these failed cases is not a power benefit: the mixers are not being
properly commutated. The sampled tuning curve is also nonmonotonic; no target
frequency coverage, PLL polarity or lock range is qualified by these three
controls.

The next RF clock work needs a static level-conversion interface appropriate
to these actual CML waveforms, then a demonstrated quadrature-generation
architecture and phase-noise/tuning/startup assessment. A complementary pair
is not I/Q, and an ideal divider or phase shifter must not silently bridge that
gap. This experiment is schematic-only, not reuse of the prior PEX results as
proof for the new load. Keep this blocker visible while next inspecting the
complete high-speed wired lane, rather than returning to easy filter tuning
or spending indefinitely on only one island. No autonomous RF LO, functioning
radio, integrated 2.5 Gb/s companion lane or tapeout readiness is established.

### Pass 108 — audit the real wired capture-clock boundary

Ran the existing routed 2.5 GT/s RX-through-capture integrity checker against
the current files: PASS, five environments, maximum composed current 52.203 mA.
Inspected its stimulus and timing generation: each case scores only 16 serial
bits and retains independently ideal sampler and conversion/capture clocks.
The existing real pulse/bridge experiment instead uses static RX data and an
ideal upstream recovered clock; its recorded sustained result is 3/5. Neither
experiment proves changing data through the actual capture timing circuitry,
and their separate successes cannot be combined into that claim.

Added `verification/audit_wired_boundary.py` and
[evidence inventory](evidence/wired-boundary-audit.json), preserving exact
hashes and clearly distinguishing a rerun integrity check from a new electrical
simulation. The [risk priorities](spec/risk-priorities.md) now specify the next
bounded test: changing data through the real pulse/bridge consumer at nominal
supply/temperature, with separately scored even/odd events and the remaining
ideal upstream clock explicitly declared. No new lane, CDR, BER, or tapeout
qualification is claimed.

### Pass 109 — real pulse/capture chain loses two changing-data bits

`make transceiver-wired-pulse-data` regenerates the pulse layout, checks DRC
and unique LVS, extracts full RC, and composes it with the checked bridge and
regenerative RX/capture PEX. Three TT 3.3 V/27 C 17-ns simulations exercise both
static signs and a fixed 40-bit pattern at 2.5 Gb/s. The upstream 1.25 GHz rail
clock, low-impedance differential RX pin drive, supplies and biases remain
ideal; no ideal sources drive the downstream SENSE/BOOST/WRITE/capture controls.
Inter-macro wires remain unrouted. This is not a channel, CDR or BER test.

[Recorded evidence](evidence/wired-pulse-data-screen.json) includes source,
deck and waveform hashes and the physical records. Run
`python3 projects/programmable_transceiver_platform/verification/analyze_wired_pulse_data.py`
after the simulation to reproduce the inventory and exploratory common-latency
analysis. Static signed output minima exceed 3.119 V. The changing case has
strong outputs (minimum absolute 2.681 V) but at least two wrong-polarity samples
in the shared 0--2400 ps delay scan: even input bits 20 and 30, both zero, appear
positive. The best mapping has 11/13 even and 12/12 odd samples correct. A
retrospective delay fit does not prove hardware timing calibration or aperture.
Changing-data VDD current is 83.956 mA, versus approximately 75.98 mA static,
without TX or autonomous recovery in this composition.

Two harness issues were corrected before accepting evidence. The first physical
invocation omitted PDK selection and extracted with the container's IHP default;
that scratch directory is marked invalid, and the committed runner explicitly
selects GF180. Adding waveform export also exposed ngspice name shadowing:
`meas e_q_diff` replaced the vector of the same name. The adapted bench now
renames scalar results, an independent pulse-source export check confirms the
vector survives, and all three circuit cases were rerun. No circuit conclusion
uses either invalid run. The original scalar-only bench is not claimed to have
been affected by our later waveform-export mistake.

The next experiment is a data/clock phase sweep with converter/capture event
inspection around the missed zeros. Preserve this failed point and the power
measurement. No complete wired lane or tapeout readiness is established.

### Pass 110 — 100 ps earlier data does not recover the missed zeros

`make transceiver-wired-phase` reuses the exact pass-109 pulse, bridge and
regenerative RX/capture PEX, requiring their passing physical records and
matching hashes. Only the differential pin-data transition times advance by
100 ps; the actual pulse/capture circuitry, upstream clock, biases, loading,
400 ps bit period and 17 ns duration stay identical. The changed include path
points to the same read-only pulse PEX. The original failing waveform is
reanalyzed alongside the new run, not overwritten.

[Evidence](evidence/wired-phase-screen.json), generated by
`verification/analyze_wired_phase.py`, preserves all latency candidates and
stage observations. Both tested phases retain two wrong even samples and zero
wrong odd samples under their best common-latency mappings (25 samples total).
The same input zeros, bits 20 and 30, are lost. The retrospective matching delay
moves from 1025 ps to 1125 ps as expected for earlier data; this is not a
hardware delay calibration. Dynamic VDD current changes from 83.956 to
84.469 mA. No useful phase window is established by these two failing points.

In the baseline, even-converter differential output remains positive throughout
bit-start through 1.3 ns later for both missed zeros, despite a clear negative
input during each bit. Earlier data produces short negative converter excursions
near the bit starts, but at the first subsequent even capture-clock rising
crossing the converter is already positive: +2.897/+2.939 V. These crossings
are raw timing observations, not a claim that the first following edge must
capture that numbered bit; end-to-end bit association remains retrospective.
A negative excursion by itself therefore cannot count as successfully converted
and retained data. Output drive strength is not the established failure owner.

Next: cover the remaining coarse relative phases and expose raw RX amplifier
outputs in the waveform, so input-to-amplifier settling, converter evaluation,
and capture timing can be distinguished. Keep the failed phases, fixed pattern,
and signed checks. Acquisition, noise/mismatch, current allocation, routed
clock distribution and host transfer remain open; no lane qualification is
claimed.

### Pass 111 — four coarse phases fail; preserve consecutive-bit association

Extended `run_wired_phase_screen.sh` / `wired_phase_screen.py` with explicit
-100/-200/-300 ps choices and separate output directories. Two new 17 ns
simulations at -200 and -300 ps reuse the exact hash-bound pass-109 extraction
stack and append raw RXOP/RXON waveform columns. The historical 0/-100 ps data
and decks remain intact. No transistor, bias, source-clock or load was changed.

Run the new points with
`verification/run_wired_phase_screen.sh -200` and `-300`, then
`python3 projects/programmable_transceiver_platform/verification/summarize_wired_phases.py`.
The [four-point evidence](evidence/wired-coarse-phase-screen.json) checks hashes,
environment, identical circuit decks after removing the intentional source-time
changes and extra observations, and one shared consecutive-bit mapping.

| Data advance | Wrong signed samples / 25 | VDD current, 6--17 ns |
|---|---:|---:|
| 0 ps | 2 | 83.956 mA |
| 100 ps | 2 | 84.469 mA |
| 200 ps | 6 | 84.764 mA |
| 300 ps | 7 | 82.216 mA |

A measurement audit found another important limitation of retrospective delay
fitting: near input transitions, slight capture-period settling can make a
fixed-delay lookup skip or duplicate an input bit. Such a mapping cannot prove
consecutive stream transfer. The summary now rejects those candidates and
preserves both the unconstrained and valid best fits. In the -300 ps case this
changes the minimum error count from six to seven; the other selected counts
are unchanged. The per-run raw latency scans remain exploratory records, not
acceptance gates. None of these four points passes either interpretation.

Raw amplifier observations begin to separate the failure stages. At the -200 ps
point, 300 ps after bit 20 starts, RX pin differential is -197 mV and raw RX
is -213 mV, while the even converter is +2.715 V. At 400 ps the raw RX is
-238 mV while that converter remains +2.935 V. At -300 ps, raw RX reaches
-191/-298 mV at the corresponding offsets, with converter +2.093/+2.793 V.
The amplifier therefore does respond to the isolated zero; this observation
does not by itself prove that it settles before the converter's actual decision
instant. The full stage samples, SENSE/capture levels and outputs are retained.

Next focus is the regenerative decision/reset timing under the real pulse
waveform and loaded RX output, rather than assuming that static resolution,
output swing or one input phase establishes dynamic conversion. Four points
100 ps apart can miss a narrow working aperture, so this is not proof that no
phase can work. It is evidence against treating phase calibration alone as an
already solved integration step. Noise/mismatch, CDR, selected I/O/channel,
complete current allocation and sustained host transfer remain open.

### Pass 112 — distinguish reset opportunity from valid evaluation data

Inspected `cml_to_cmos_fast.spice`: SENSE low drives the dynamic-node and input-
node precharge devices; SENSE and the separate BOOST signal control parallel
NMOS tail paths. The held output latch need not return to zero during precharge.
Consequently, a persistent converter output alone cannot diagnose failed reset.

`verification/analyze_wired_reset_windows.py` checks the saved waveform hashes
and measures the SENSE falling/rising crossings around original missed input
zeros 20 and 30 in both new coarse-phase cases. This is analysis of the existing
simulations, not a new electrical run. The [record](evidence/wired-reset-window-analysis.json)
retains source hashes and all timing observations.

For -200 ps data phase, SENSE spends 106.05--106.79 ps below 0.5 V. As it rises
through 0.5, 0.8 and 1.65 V, raw RX differential is respectively approximately
-214 to -218 mV, -174 to -179 mV, and -53 to -59 mV. Thus the input advantage
is disappearing during the rising SENSE transition. At -300 ps the corresponding
raw differential progresses from approximately -25 mV through +7/+8 mV to
+94/+95 mV. These observation levels are not characterized transistor decision
thresholds, and the measured low duration is not a reset pass/fail limit.

Two mechanisms remain unresolved: late evaluation after RX differential decays,
and incomplete precharge/reset, potentially involving the independently driven
BOOST tail. The current exports omit BOOST and the dynamic/internal tail nodes,
so neither mechanism is established. Before changing transistor strength or
claiming the phase aperture is impossible, repeat the same -200 ps circuit with
BOOST, local SENSE, XP/XN, NIP/NIN and NTAIL observations. Preserve consecutive-
bit scoring and the failed operating point. No new circuit qualification is
claimed by this timing analysis.

### Pass 113 — BOOST remains enabled through the actual precharge interval

`make transceiver-wired-internal` reruns the exact failing -200 ps case with
additional non-loading observations: even XP/XN, NIP/NIN and NTAIL extracted
`t0` nodes, external BOOST and the actual local SENSE/BOOST tail-transistor gate
nodes. The script identifies those tail gates from the extracted device
connectivity and binds all three macro hashes. All 16 original waveform columns
are bitwise identical to the prior run; the existing six wrong samples remain.
No transistor or clock connection changed.

`verification/analyze_wired_internal.py` produces the
[internal-node evidence](evidence/wired-internal-screen.json). Around missed
zeros 20 and 30, while the local SENSE gate is below 0.8 V, the local BOOST gate
stays at 2.916--3.150 V and 2.932--3.158 V respectively. The nominal circuit
uses that BOOST gate on the large parallel NMOS tail bank, so reset is opposed
by an enabled evaluation path; this is not just a slow final CMOS output.

At minimum SENSE (approximately 0.262 V), the observed XP/XN nodes are
2.665/1.881 V and 2.674/1.867 V. The corresponding input nodes are approximately
0.90/1.34 V. Even at the best lower-dynamic-node voltage during the below-0.8 V
interval, XP/XN are only approximately 2.72/2.56 V and retain about 160 mV
imbalance. At the later 1.65 V SENSE rising crossing, the raw RX differential
has weakened to -23/-29 mV while the dynamic pair retains roughly 75 mV
imbalance. These are local extracted-node observations, not universal reset
thresholds or a proof that this is the sole error mechanism.

This experiment supports precharge/evaluation contention as a concrete circuit
problem, alongside the previously observed shrinking input-valid interval.
Next perform a bounded BOOST-disabled or reset-qualified-BOOST experiment,
preserving real SENSE/capture generation and signed consecutive-bit scoring.
Removing BOOST can reduce contention but also weaken/slow regeneration; both
outcomes must be measured. If the generator's BOOST output loses its original
load, explicitly account for that changed load rather than describing the
experiment as timing-identical. A successful diagnostic still needs an actual
programmable enable/qualification circuit and physical reclosure. No working
lane, current-budget closure or tapeout qualification is claimed.

### Pass 114 — disabling BOOST removes wrong signs but leaves one weak sample

`make transceiver-wired-boost-off` changes only the two receiver BOOST ports
from their generated pulses to ground on the exact -200 ps internal-observation
case. The generator still runs, but its BOOST outputs lose their receiver gate
loads; this is explicitly not a load-preserved experiment. The unchanged
extracted receiver, bridge and pulse macros remain hash-bound. No ideal
high-speed source replaces SENSE or capture. Hardwiring BOOST off is a diagnostic,
not a finished programmable qualification/enable circuit.

`verification/analyze_wired_boost_off.py` compares the fresh 17 ns transient
against the baseline using actual capture falling edges, sampling 100 ps later,
and only shared delay associations that yield consecutive bits. The
[evidence](evidence/wired-boost-off-screen.json) preserves all valid candidates.

| Metric | Baseline | BOOST disabled |
|---|---:|---:|
| Wrong-polarity samples / 25 | 6 | 0 |
| Samples below signed 500 mV / 25 | 6 | 1 |
| Minimum signed output | -3.140 V | +0.254 V |
| VDD current, 6--17 ns | 84.764 mA | 80.442 mA |

The weak sample is the first scored even output, at 6.3676 ns, associated with
input bit 11 (zero) in the retrospective best mapping. Do not discard it as
startup without a characterized acquisition/settling rule. The fitted delay
changes from 1225 to 825 ps (one UI), preserving a consecutive stream but not
proving hardware latency calibration or alignment. Actual sampled capture
fall times move by -1.19 to +1.67 ps even and -3.65 to +3.13 ps odd; the load
change therefore has measurable effects even though the topology of the real
clock path was retained.

Reset improves: the lower of the observed XP/XN nodes reaches 3.022/3.015 V
during the two measured below-0.8 V SENSE intervals, versus approximately
2.560/2.557 V before. Residual dynamic-node differences at those instants fall
from approximately 160/165 mV to 72/74 mV. Local BOOST is about 27 mV at minimum
SENSE. These observations and the data improvement support the contention
mechanism, but do not prove fully reset, mismatch-tolerant operation.

This remains a failed 500 mV screen. Next measure the weak sample's output
settling and retained-valid interval, then verify a longer changing-data run
with a frozen consecutive-bit mapping and explicit startup rule. If reduced
BOOST remains useful, it needs a real enable/qualification implementation,
load-aware clock reclosure, and both faster/slower regeneration checks. No
working full lane, BER qualification, complete power budget or tapeout readiness
is claimed.

### Pass 115 — measure a retained output window without changing bit identities

`verification/analyze_wired_hold_window.py` analyzes the saved BOOST-disabled
waveform with exactly the prior 25 bit identities. It does not refit delay when
moving the observation time. The script finds threshold crossings by linear
interpolation between actual adaptive samples and intersects the connected
above-500 mV intervals containing a declared 400 ps anchor. An analytic check
covers both entering and leaving a valid interval, plus a rejected invalid
anchor. All waveform/source hashes are retained in the
[hold-window analysis](evidence/wired-hold-window-analysis.json).

The shared observed interval is approximately **110.09--753.75 ps after each
branch's actual capture falling edge**. Across every saved sample and endpoint
in the conservative 200--650 ps interval, minimum signed output is 2.561 V.
At 400 ps it is 2.993 V. The previously weak output therefore settles after the
original 100 ps observation, rather than retaining the wrong bit in this short
run. The original 100 ps/500 mV failure remains valid and preserved.

Freeze the next independent test before running it: sample at +400 ps, take the
first even capture fall after 6 ns as input bit 11 and the first odd as bit 12,
then advance each branch by two bit indices per event. Do not search latency
again or drop weak samples after seeing the new data. The 6 ns start remains a
fixture boundary, not a demonstrated acquisition specification. Exercise a
longer, different changing-data pattern on the same BOOST-disabled circuit.

This pass adds waveform analysis, not a new transient or a physical retiming
stage. The 110--754 ps interval is pattern- and environment-specific; it is not
jitter tolerance, a process/mismatch bound, a clock implementation, or a host
setup/hold contract. If the frozen observation succeeds on independent data,
implement and load-test the actual retiming/word-assembly boundary needed to
present these bits to the FPGA companion interface.

### Pass 116 — independent longer pattern fails the frozen receiver check

The previously launched run completed without being restarted after the user's
interruption. `make transceiver-wired-frozen-pattern` changes only stimulus and
simulation duration on the BOOST-disabled circuit: an 80-bit prefix of an
explicitly verified 127-state LFSR sequence, seed 0x5b, at the same 2.5 Gb/s pin
rate, and a 33 ns transient. The prior +400 ps observation and ordinal bit
association are loaded from the committed pass-115 decision before simulation;
there is no new alignment/delay search.

[Results](evidence/wired-frozen-pattern-screen.json): all 65 expected samples
are present and consecutively associated, but two even outputs have wrong
polarity. Odd samples pass with minimum signed output 2.917 V. The previous
200--650 ps retained-window check also fails for the same two even outputs.
Same-interleave periods remain approximately 796.3--807.6 ps even and
797.5--803.2 ps odd. VDD current averages 78.357 mA over 6--33 ns. This invalidates
any extrapolation from the prior short pattern to general working reception.
`verification/check_wired_frozen_pattern.py` independently verifies hashes,
frozen-point margins, cadence and hold intervals. Failure remains authoritative.

### Pass 117 — rebase work around the three whole-chip feasibility questions

Added [feasibility gates](spec/feasibility-gates.md), linked as the active work
program. It preserves the full first-chip ambition and narrow operating envelope,
but separates applicable specifications, model-supported predictions, assumed
stress scenarios and unbounded/undisclosed information. It specifies concrete
clock, converter/RF and physical coexistence demonstrations plus an observability
and calibration strategy within the existing pin/area/power limits. It does not
assume the fab will disclose missing information or that calibration fixes noise.

`verification/feasibility_bounds.py` produces
[conditional analytic envelopes](evidence/feasibility-bounds.json). Examples:
a -30 dB total-EVM scenario allocating one quarter of its error power to residual
LO phase error allows about 1.049 ps equivalent RMS time error at 2.4 GHz;
this is not a full PLL phase-noise specification. Ideal differential kT/C bounds
at 1 Vpp, with thermal RMS capped at half quantization RMS, are about
0.026/0.417/6.674 pF per leg for 8/10/12 bits. Other converter impairments remain
unmodeled. The current planning ceilings sum to 350 mA or 1.155 W at an assumed
common 3.3 V; no package or thermal capacity is established by that sum.

An [installed-model inventory](evidence/model-inventory.json) hashes the three
entry files and records library/include declarations and noise/mismatch-related
terms. It deliberately does not equate parameter presence with activation or
RF validity. Reviewed public PDK correlation/statistical documentation is linked
in the work program, including its limitations. The next work packets address
RF loaded autonomous timing, converter signal quality, and a physical package/
PDN/thermal candidate; repeated local receiver fitting is no longer the sole
priority. None of the three whole-chip feasibility questions is yet closed.

### Pass 118 — verify model switches and screen schematic LNA noise

The installed design file comments describe statistical switches as default-on,
but its actual assignments are zero. Explicit activation matters.
`make transceiver-model-behavior` runs 16 fresh ngspice cases and checks two
identical NFET circuits. Global-only variation shifts both devices together;
mismatch separates their currents; a repeated seed reproduces the result.
Three seeds demonstrate operation, not a distribution, yield, or silicon bound.
The declared flicker corner increases input-referred noise by 10.24 dB at 1 kHz,
10.02 dB at 1 MHz and 0.416 dB at 2.4 GHz in this circuit. These are noise-spectrum
ratios, not oscillator phase noise. Low-frequency noise can upconvert in an
oscillator or mixer. [Evidence](evidence/model-behavior-screen.json).

`make transceiver-lna-noise-feasibility` runs six standalone schematic cases:
TT/FF/SS times two flicker settings, at 3.3 V, 27 C and fixed 1.5 V bias.
At 2.4 GHz the bench-relative noise figure spans 8.87--9.80 dB, voltage gain
2.00--3.02 dB and current 3.02--4.49 mA. Typical/default is 9.17 dB,
2.59 dB and 3.74 mA. Output noise divided by AC gain agrees with ngspice's
input-referred noise within 0.5%. [Evidence](evidence/lna-noise-feasibility.json)
records deck and raw-artifact hashes; reproductions write scratch/result.json.

This noise figure includes the bench's resistors and uses its 50-ohm source
reference. Voltage gain into 5 kohm is not available power gain; it must not be
inserted into a Friis cascade as power gain. The low voltage gain means useful
full-chain signal quality remains an open question, especially with actual
mixer loading. This bench is schematic, with ideal bias/coupling and without
RF pads, package, matching loss, substrate coupling, mismatch or switched-mixer
noise. It does not demonstrate receiver sensitivity, EVM or Wi-Fi performance.
No corner is a demonstrated bound on undisclosed RF model errors.

These results remove uncertainty about basic model activation and establish a
numerical LNA starting point. They do not close any of the three whole-chip
gates. Next decisive RF evidence remains an autonomous oscillator with working
level conversion/quadrature and actual mixer loading, followed by a noise method
appropriate to the time-varying chain and a physical coexistence candidate.

### Pass 119 — autonomous LO boundary switches, but rejects supply ripple poorly

`make transceiver-rf-ac-clock` replaces the failed direct CML-to-CMOS connection
with 200 fF AC coupling per leg and 100 kohm feedback from each first inverter
output to its input. This is a schematic candidate, not a physical capacitor/
resistor implementation. The seeded ring, actual buffer gates and actual single
mixer load run together at nominal 3.3 V/27 C. LO outputs now swing about
0.125--3.099 V at 2.5004 GHz. VCO current is 8.409 mA; the LNA/mixer/buffer branch
uses 6.630 mA. Varying only the VCO supply to 3.29/3.31 V gives local frequency
sensitivity 64.76 MHz/V. [Evidence](evidence/rf-ac-clock-screen.json).
The short beat fits cover less than half a cycle and are not accepted as gain
evidence; pass 120 uses longer paired runs.

`make transceiver-rf-supply-ripple` adds 10 mV peak, 100 MHz sinusoidal ripple
only to the VCO rail. Differential CML edge timing modulates by 0.419 ps peak,
but single-ended CMOS LO timing modulates by 11.07 ps peak. Reducing maximum
timestep from 2 ps to 0.5 ps changes those amplitudes by less than 0.03%.
[Evidence](evidence/rf-supply-ripple-screen.json). Thus the newly switching
boundary is not a clean-clock solution: most timing modulation appears during
level conversion. Common-mode/amplitude-to-time conversion is a hypothesis to
isolate, not yet a proven sole mechanism. A differential restoring interface
and supply isolation deserve priority over assuming a faster inverter fixes it.

This is deterministic supply-induced timing, not intrinsic phase noise or
random jitter. Only one ripple frequency/amplitude and roughly four late cycles
were examined; no PLL rejection, quadrature, noise/mismatch, unassisted startup,
PEX or package is established. Fine ripple is compared with the same coarse
reference, whose fitted 100 MHz timing component is only about 0.0013 ps.
Ngspice documents explicit transient-noise sources and describes its periodic
steady-state functionality as under development:
[official simulator notes](https://ngspice.sourceforge.io/extras.html).
These ordinary deterministic transients must not be relabeled device-noise
simulations. Intrinsic phase-noise work still needs a validated time-varying
noise method and relevant device-noise inputs.

### Pass 120 — autonomous LO through active filter and sampling switch

`make transceiver-rf-autonomous-chain` uses pass 119's measured clock to select
an RF input approximately 10 MHz above it. It connects the single LNA/mixer
branch to the existing transistor active filter and transmission-gate sampler
with 5 pF per held leg. Two 301 ns runs use zero and 1 mV peak RF input, with
identical initial conditions. Sampling remains ideal 40 MS/s; the RF LO is the
actual seeded oscillator and buffers. There is no ADC or quadrature branch.
[Evidence](evidence/rf-autonomous-chain-screen.json).

Eight late held samples, after paired zero-input subtraction, give voltage gain
0.02641 V/V, or about 26.4 uV peak from 1 mV peak at the source. Separate four-
sample blocks give 0.02650 and 0.02632. The observed IF is 9.999834 MHz. The
mixer-output tone gain is only 0.00321 V/V, rising to 0.02662 after the active
filter. These measurements locate severe loss before the filter; they do not
separate LNA loading from switching-mixer conversion loss. VCO current remains
8.409 mA and RF/filter branch current is 10.921 mA; ideal sampling-clock and
bias-source power are not fully budgeted by those rail currents.

The fitted held residual (~0.103 uV RMS) is not receiver noise, SNR, EVM or
converter resolution. The result establishes signal propagation but fails to
establish useful conversion gain. Next isolate actual LO waveform effects and
RF/filter loading with otherwise identical paired controls, then improve that
boundary before relying on complete-chain noise predictions. Preserve the full
chip ambition while recognizing that both clock cleanliness and receive gain
now have concrete, connected-circuit weaknesses.

### Pass 121 — ideal LO does not cure connected receive loss

Added `verification/run_rf_ideal_lo_control.sh` and an independent comparison
analyzer. Four fresh 301 ns transients compare paired zero/1 mV RF stimulus
with ideal complementary 20 ps-edge LO at either 0--3.3 V or 0.125--3.099 V.
Frequency, RF source, receive topology, filter, sampling and initial conditions
match pass 120. The analyzer verifies artifact hashes and exact deck equality
after removing only the intended LO substitution. Oscillator/buffers remain but
lose mixer-gate loading; these are diagnostic controls, not equal-power clock
implementations or autonomous operation.

[Results](evidence/rf-lo-control-comparison.json): autonomous / ideal-full /
ideal-limited held tone gains are 0.02641 / 0.03993 / 0.02177 V/V. Mixer-output
gains are 0.003208 / 0.004848 / 0.002641. Full ideal drive helps by only about
1.51x, leaving severe loss. Matching voltage extrema alone does not match actual
slew, duty cycle, overlap or source impedance. Four-sample block gains are
retained to expose settling; eight samples and paired subtraction remain a
small deterministic diagnostic, not noise/EVM/dynamic-range qualification.

The risk reassessment moves the next receive experiment to mixer/LNA bias and
loading, including the active filter's input common mode. Clock restoration's
supply sensitivity remains independently unresolved. Intrinsic phase noise,
actual ADC/DAC performance and physical coexistence retain whole-chip priority;
this result is not permission to spend an unlimited series tuning receiver gain.

### Pass 122 — frozen mixer states identify substantial RF loading

`make transceiver-rf-loading-op` runs six fresh DC/AC cases with mixer gates
frozen both off, one on, or both on, and the active filter attached/disconnected.
The disconnected filter remains biased from ideal 1.177 V dummy inputs; original
1 kohm/1 pF IF loads remain. RF source AC gain is measured at 2.5 GHz.
[Evidence](evidence/rf-loading-op-screen.json) preserves netlist/log/data hashes.

With filter attached, LNA drain voltage gain is 1.375 with both switches off,
0.499 with one on, and 0.240 with both on. Drain DC levels are 2.179, 1.724,
and 1.432 V respectively. Disconnecting the active filter gives gains 1.375,
0.500 and 0.240: almost unchanged. This identifies substantial loading from
the mixer/IF network independently of the active filter. It does not prove
that static loading alone explains the observed ~0.00485 conversion gain.

Frozen IF levels are highly state-dependent (one-on: approximately 1.545 V
and 0.096 V with filter attached). These are not the periodically settled
common mode at GHz switching; do not diagnose filter saturation from them.
The next bounded test should examine the periodically operating LNA drain and
IF waveforms while varying the existing IF capacitance/load, preserving ideal
LO and paired stimuli. Intrinsic clock noise, ADC/DAC implementation and physical
coexistence remain unresolved whole-chip risks.

The initial analysis script assumed ngspice would emit two AC points; it emitted
one at the requested 2.5 GHz starting frequency. The parser was corrected to
accept the actual matrix while explicitly checking frequency, shape and finite
values; all six cases were then rerun. No failed-parser result is accepted.

### Pass 123 — reducing IF capacitance worsens periodic conversion

Extended the ideal-LO runner with `--if-cap-pf`; default remains 1 pF. Two fresh
301 ns runs use 0.1 pF per IF leg with paired zero/1 mV RF input. The analyzer
verifies raw hashes and exact deck equality with pass 121 after restoring only
the two capacitor values. [Evidence](evidence/rf-if-cap-comparison.json).

Mixer-output gain falls from 0.004848 to 0.002323 V/V; active-filter output gain
falls from 0.04027 to 0.01504; held gain falls from 0.03993 to 0.01493. Smaller
capacitors do not recover conversion in this topology. Late zero-input sample
variation also increases; it is deterministic feedthrough/settling, not a noise
measurement. The ideal capacitor value is a diagnostic, not a physical selection.

This rejects the simple inference from pass 122 that reducing capacitive loading
will improve useful conversion. Frozen-switch AC loading is not sufficient to
predict periodically switched behavior. Next receive work should instrument RF
gate, LNA drain/source, IF common mode and LO gate waveforms in the same periodic
fixture before another component sweep. Preserve the original 1 pF candidate;
this pass does not promote the worse alternative. Intrinsic clock noise,
converter realization and physical coexistence remain whole-chip priorities.

### Pass 124 — internal observation exposes an initialization error

The instrumented pass-121 signal run reproduces all original waveform columns
bit-for-bit. Independent late-window FFT confirms mixer gain about 0.00488 V/V,
so fitting was not hiding a strong signal. However, LNA gate voltage averages
only 19.74 mV over 200--301 ns, with source average near zero. Intended gate bias
is 1.5 V. UIC skipped the operating point and the 1 Mohm/20 pF gate-bias path has
an approximately 20 us charging timescale. The receiver was not biased for the
claimed steady-state test. [Evidence](evidence/rf-internal-screen.json).

Withdraw the architectural inference that these recent conversion measurements
establish severe loss in a correctly biased receiver. Preserve all raw failures
and annotate their evidence files. Pass 122 DC/AC solved an operating point and
remains a distinct valid frozen-state diagnostic. Clock experiments still show
switching/supply response for their actual underbiased mixer load; they require
rechecking after bias correction. A prebiased paired test is the next comparison,
explicitly not cold-start qualification.

The user's proposed whole-chip SPICE workflow is adopted in
[executable-chip-model.md](spec/executable-chip-model.md): mixed behavioral,
transistor and extracted blocks with explicit confidence levels and mandatory
bias/startup checks. It complements targeted simulations and must not convert
ideal ADC/PLL behavior into physical feasibility evidence.

### Pass 125 — corrected prebias recovers gain; assemble transistor RF hierarchy

The paired ideal-LO run changes only `.ic v(GATE)=1.5`. The analyzer verifies
exact deck equality after removing that initial condition and checks raw hashes.
Mixer gain recovers from 0.00485 to 0.78657 V/V; filter gain from 0.04027 to
6.52787; held gain from 0.03993 to 6.47108.
[Comparison](evidence/rf-prebias-comparison.json). Withdraw the earlier claim of
severe conversion loss in the intended biased receiver. These are prebiased
nominal results, not a demonstrated startup solution or receiver qualification.

Following the user's clarification, the primary flow is actual PDK transistor/
passive schematic first, then layout, extraction and the same verification.
The exploratory behavioral `system_model` is explicitly not the implementation.
`analog/rf_rx_candidate.spice` now assembles the existing oscillator, buffer,
LNA, mixer, active-filter and sampling-switch circuits as one reusable hierarchy.
Its biases, control voltage and sampling clocks remain external ports. A real
PLL, quadrature branch and ADC remain missing; passives are still ideal lumped
values pending physical realization.

Hierarchical DC elaboration gives gate/source/drain 1.500/0.301/1.873 V. An 81 ns
seeded/prebiased transient with actual oscillator and mixer load passes explicit
boundary checks over 40--81 ns: gate 1.491--1.512 V, mean source 0.296 V, LO
0.130--3.091 V, clock 2.500414 GHz. RF-branch current is 14.254 mA and oscillator
rail current 8.409 mA. Ideal bias/sampling-source power is not fully represented
by those rail currents. [Evidence](evidence/rf-candidate-bias-screen.json).

`check_rf_candidate_bias.py` rejects invalid gate/source bias or absent clock
activity before any useful-performance claim. The short run does not establish
conversion gain, startup, intrinsic phase noise, mismatch or extracted behavior.
Next verify cold-bias startup/enable sequencing and corrected-bias autonomous
conversion; retain ADC/PLL completion and physical coexistence as higher-level
implementation gaps rather than resuming the invalid loss-driven tuning series.

### Pass 126 — cold-bias settling in the assembled transistor hierarchy

`make transceiver-rf-bias-startup` uses the actual RF hierarchy with no gate
precharge. Supply and external bias voltages ramp from zero over 1 us. Oscillator
supply/control remain zero and sampling is disabled. Three 200 us transient
runs vary the gate-bias resistance to 0.5/1/2 Mohm while retaining the 20 pF
coupling capacitor and all other circuitry.

Gate voltage reaches and remains within 15 mV of 1.5 V at 46.71/92.89/185.27 us.
The independent checker verifies initial uncharged gate, raw hashes and continued
in-band behavior through the end of each run.
[Evidence](evidence/rf-bias-startup-screen.json). This confirms a slow but finite
bias-startup path in these schematic scenarios and explains why 301 ns UIC
measurements without precharge were invalid.

The 0.5x/2x resistance scenarios are not validated process/mismatch bounds;
15 mV is a diagnostic threshold, not an established gain/noise accuracy limit.
Ideal ramped bias sources are not bias-generator implementations. No oscillator
release, acquisition, simultaneous startup, cold-RF reception or package behavior
has been demonstrated. Next test staged oscillator release after bias settling,
with startup/asymmetry assumptions explicit; only then score autonomous signal
quality. A fast-charge circuit is a possible design change, not required by an
as-yet-unspecified startup-time target.

### Pass 127 — enforce whole-schematic-before-layout and audit missing circuits

The user explicitly requires a working integrated transistor-level schematic of
the whole analog chip before new layout/recreation. Updated the main plan and
schematic workflow; existing layouts remain reference material. Added the explicit
[implementation inventory](spec/schematic-implementation.json) and reproducible
[source audit](evidence/schematic-implementation-audit.json). It records source
hashes/subcircuits while deliberately withholding readiness claims.

The existing PLL-named clock path contains VCO/restoration/division, not a closed
PLL. Timing-control DACs do not implement radio ADC/DAC functionality. Radio
conversion, TX integration, real bias generation, closed timing loops and complete
programmable interconnection remain substantial gaps. Next new circuit priority
is a transistor PLL feedback path, tested independently then connected to actual
VCO/divider circuits. This rebalances work away from prolonged receiver tuning.

### Pass 128 — first transistor PLL phase/frequency detector candidate

Added actual GF180 CMOS inverter/NAND gates, resettable master/slave flip-flops
and a shared asynchronous-reset PFD in `analog/pll/pfd.spice`. No behavioral
logic or ideal internal clock detector implements the candidate. Test sources
provide reference, feedback, supply and startup reset; each output sees 50 fF.

Three fresh 401 ns TT/3.3 V/27 C runs at 25 MHz test feedback phase offsets
-5/0/+5 ns. Over seven late periods, normalized UP-minus-DOWN pulse areas are
-35.003 ns, +0.00790 ns and +35.029 ns, respectively. Reset holds outputs below
0.1 V. [Evidence](evidence/pll-pfd-screen.json); `make transceiver-pll-pfd`
reproduces the run and artifact/direction checks.

Aligned clocks still produce about 0.82 ns equivalent UP and DOWN high-time
per cycle. Their near-balanced difference is not a low-jitter/dead-zone claim;
a real charge pump must tolerate their overlap, mismatch and switching effects.
This pass establishes only nominal reset and phase direction, not frequency
acquisition, charge-pump drive, loop stability, process/mismatch, supply rejection
or closed PLL operation. Next test frequency-error/reset-reentry behavior and
connect a transistor charge pump with explicit compliance/current measurements.
The whole-schematic-before-layout gate remains closed.

### Pass 129 — PFD frequency error and reset re-entry

Two fresh transistor simulations use a 40 ns reference period with feedback
period 36 or 44 ns, and reassert reset during running clocks from 600.1 to
640 ns. In the 700--1200 ns observation window, net normalized UP-minus-DOWN
areas are -247.977 ns and +24.004 ns. An independent ideal edge-event PFD model
predicts -248 ns and +24 ns for the same clock/reset schedule. Finite-window
phase history explains the unequal magnitudes; frequency-error sign alone is
not the only check. Reset output excursions stay below 1.26 mV in the checked
605--639 ns interval. [Evidence](evidence/pll-pfd-frequency-screen.json).

`make transceiver-pll-pfd-frequency` checks hashes, reset re-entry, correction
direction and agreement within 1 ns of the event-reference net area. This is
nominal 3.3 V/27 C, 50 fF output loading and two selected frequency offsets;
not capture-range, minimum-pulse, metastability, mismatch or loop qualification.
Next connect the detector to an actual transistor charge pump and measure
charge/current versus output voltage before closing the oscillator loop.

### Pass 130 — transistor charge-pump DC current/compliance screen

Added a switched PMOS/NMOS mirror candidate in `analog/pll/charge_pump.spice`,
including transistor UP inversion and separate diode-connected reference legs.
Two external ideal 20 uA sources remain explicit fixtures, not implemented bias
generation. Twenty-eight DC simulations cover off/up/down/both states at seven
output voltages from 0.3 to 3.0 V, TT/3.3 V/27 C.
[Evidence](evidence/pll-charge-pump-dc.json).

Source current spans 19.385--20.321 uA and sink magnitude 19.532--20.864 uA.
Both-on residual changes from +0.789 uA at 0.3 V to -1.479 uA at 3.0 V,
with +0.009 uA at 1.3 V. Off current is below 1 nA in all cases. The independent
checker verifies deck hashes, source/sink polarity and consistency of both-on
current with the two individual branches. No current-matching specification is
claimed passed; a small residual at one voltage is not broad matching evidence.

This is an unregulated candidate with ideal static controls and clamped output.
It has not been driven by the actual PFD; switching charge, overlap, leakage
under transients, process/mismatch, noise and loop stability remain unproved.
Next connect PFD to pump at a clamped VCO-control voltage, measure delivered
charge per cycle, then add the loop filter and actual feedback chain. Do not
infer PLL jitter or reference-spur performance from standalone DC matching.

### Pass 131 — connected PFD/pump reveals switching charge offset

The actual transistor PFD now drives the transistor charge pump, retaining 50 fF
per output in addition to the pump gate loading. Three 401 ns simulations use
-5/0/+5 ns feedback phase at 25 MHz with output clamped at 1.08 V and ideal
external 20 uA bias references. Charge per cycle is approximately -78.94,
+22.42 and +130.25 fC. [Evidence](evidence/pll-pfd-pump-screen.json).

An independent checker integrates seven exact 40 ns windows, verifies hashes,
reset and correction direction, and retains cycle-to-cycle spread. Aligned
clocks do not produce zero net charge: ~22.42 fC corresponds to ~0.560 uA average
or a 1.12 ns equivalent pulse at 20 uA. This is a real nominal switching-offset
observation, not an inferred random jitter value. It would require compensating
phase error in a closed loop and may contribute to reference ripple/spurs.
Standalone DC matching did not establish this dynamic property.

Next connect a passive loop-filter load and measure charge-to-voltage behavior,
including this offset, before assuming loop equilibrium. Charge-injection,
control-path asymmetry and current-source recovery have not been separated.
The clamped-output test does not establish filter ripple, lock, phase noise,
process/mismatch, reference generation or complete synthesizer behavior.

### Pass 132 — passive loop filter exposes drift and switching ripple

Connected the transistor PFD/pump to an ideal lumped passive candidate: 2 pF
shunt plus a 10 kohm/50 pF series branch, precharged to 1.08 V. Three nominal
801 ns runs retain -5/0/+5 ns phase offsets and no oscillator feedback.
Over 120--800 ns the control changes -25.35/+7.28/+42.34 mV. Last-cycle
linearly detrended peak-to-peak excursions are 38.94/24.16/66.69 mV.
[Evidence](evidence/pll-filter-screen.json).

The independent checker integrates pump current and compares it with total
capacitor charge change; errors are below 0.067 fC in all cases. This verifies
charge accounting, not filter adequacy. Aligned clocks still cause drift and
substantial switching ripple. Values are diagnostic candidates, not a selected
closed-loop bandwidth or physical capacitor implementation. No VCO loading,
feedback, noise, process/mismatch or startup is qualified. Next assess the actual
VCO control sensitivity/loading and loop dynamics before treating this as a PLL.

### Pass 133 — loaded local VCO tuning fixes feedback polarity

Three fresh seeded/prebiased simulations use the assembled transistor RF receiver
and actual mixer/buffer load, varying only control voltage to 1.06/1.08/1.10 V.
Measured 20--40 ns frequencies are 2.491617/2.500414/2.507753 GHz. LNA gate
remains within the diagnostic 1.4--1.6 V range in all cases.
[Evidence](evidence/vco-local-tuning.json).

Local secant slopes are +439.84 and +366.97 MHz/V. Thus near this operating
point, sourcing pump charge increases frequency; reference-leading UP has the
intended negative-feedback direction once a correctly phased divider is connected.
The slope variation precludes treating one constant as a global VCO model. Earlier
coarse tuning evidence remains non-monotonic over a wider range; these three
points establish neither full tuning coverage nor process-robust loop polarity.

The divider feedback load is absent, and no acquisition, loop stability, jitter
or phase-noise claim follows. Next connect and verify the actual high-speed
divider/restorer boundary before closing feedback. Do not turn the 25 MHz PFD
fixture into an implicit ideal divide-by-100 implementation. The user deferred
transmission-line, twisting and shielding structures to the later layout phase;
this pass changes schematic evidence only.

### Pass 134 — actual first divider works with the loaded RF oscillator

Connected the existing transistor CML divider to the assembled RF hierarchy,
comparing direct VCO output connection against the existing two-stage CML
restorer. External divider bias is 0.95 V, restorer bias 1.0 V; reset releases
at 5.1 ns. Each divider output sees 25 fF. VCO seed and LNA prebias remain.
Two 41 ns transients are scored over 20--40 ns.
[Evidence](evidence/loaded-divider-screen.json).

Direct: 2.500719 GHz oscillator, 1.250359 GHz divider, 2.158 mA divider branch,
about +/-1.256 V differential output. Restored: 2.500280/1.250140 GHz, 5.404 mA
including restorer, about +/-1.390 V differential. Both have 25 observed rising
output crossings, division ratio within 0.001 of two, and valid LNA gate bias.
The tiny deterministic period spread is not a random-jitter or phase-noise result.

Direct coupling is the next integration candidate because this nominal test
does not require the extra restorer power. Preserve the restored alternative;
neither topology is qualified across process, mismatch, supply or downstream
loading. Next extend actual division/loading toward a usable reference-frequency
feedback path and verify its final logic interface. One stage is not divide-by-100,
PLL acquisition, quadrature generation or full clock closure.

### Pass 135 — transistor divide-by-128 chain reaches the actual PFD

Seven existing CML divider stages now connect the loaded RF oscillator to an
AC-coupled CMOS interface driving the actual transistor PFD. Divider bias is
0.95 V; interface coupling is 1 pF with 100 kohm first-inverter feedback. This
is a fixed test divisor, not the final programmable reference architecture.

A 321 ns nominal run gives oscillator 2.500756 GHz and successive division down
to 19.537137 MHz. The final CMOS interface measures 19.535268 MHz over its four
late crossings, with -0.027--3.336 V excursion. Its small frequency difference
from the differential stage over this short window needs longer settling/timing
analysis; it is not a separate locked frequency. Divider/interface/PFD current
is 15.629 mA. LNA gate remains 1.492--1.511 V.
[Evidence](evidence/divider-chain-screen.json).

The checker verifies each observed stage period within 2% of the expected
VCO-derived period, feedback swing and LNA bias. Only four rising edges exist at
the final stage in the 100--320 ns window, so this is short nominal cadence
coverage, not long-run stability, jitter, acquisition, variation or PFD correction
qualification in the combined circuit. Oscillator seed, LNA prebias and external
bias sources remain. Next connect actual pump/filter to VCO control and inspect
closed-loop behavior; do not replace this feedback chain with an ideal divider.

### Pass 136 — first closed transistor loop leaves the verified tuning region

Connected the actual pump and passive filter to VCO control in the loaded RF
hierarchy with the actual divide128/interface/PFD feedback path. Removed the
ideal control voltage source. Filter and LNA are prebiased and VCO seeded;
external bias references, sample clocks and reference clock remain fixtures.
The 321 ns transient completes with valid LNA gate bias.
[Evidence](evidence/closed-loop-screen.json).

Control spans 1.081--1.300 V after detector release, exceeding the verified
1.06--1.10 V local tuning interval. From 100--200 ns, mean control is 1.208 V and
VCO frequency 2.51195 GHz; from 220--320 ns, mean control is 1.255 V but frequency
falls to 2.49803 GHz. These are transient averages, not DC tuning measurements
or proof of a specific local slope. They are consistent with earlier wide-range
non-monotonic tuning and invalidate applying the local positive-gain model to
this trajectory. Near-target late frequency is not lock evidence.

A separate [conditional linear envelope](evidence/pll-linear-envelope.json),
using ideal averaged 20 uA pump, divide128, measured local slopes and ideal filter,
has left-half-plane poles with ~12--15 us four-time-constant envelopes. It ignores
switching, offset, VCO loading/delay and nonlinear tuning; its apparent stability
cannot qualify this actual excursion. The short transistor run is an integration
and early-behavior observation only.

Next establish a usable monotonic tuning branch/range or separate coarse and
fine control before long acquisition testing. Do not merely extend the run and
label an accidental near-target frequency locked. Reassess the clock architecture
alongside missing converters and bias generation; no new layout work is authorized
before the whole schematic gate.

### Pass 137 — controlled sweep confirms loaded tuning reversal

Four fresh 41 ns fixed-control tests retain actual RF hierarchy and seven-stage
feedback load. At 1.08/1.16/1.24/1.30 V, frequencies are
2.500751/2.521135/2.504545/2.479089 GHz. Gate bias remains valid. Secant slopes
change +254.80 to -207.38 and -424.26 MHz/V.
[Evidence](evidence/vco-capture-tuning.json).

This independently confirms reversal across the region entered by pass 136,
rather than inferring a static tuning curve from transient averages. It does
not locate the precise turning point or prove monotonicity between samples.
The clock loop's fixed UP/source-charge polarity cannot be assumed corrective
over the entire excursion. Near-target frequency on the opposite branch is
not successful acquisition.

Next establish a coarse/fine or branch-selection approach with enough capture
margin and constrain actual acquisition excursions accordingly. Existing
split-control transistor VCO variants are reference candidates, not validated
substitutes for this RF oscillator. Retain the whole-chip risk review: successful
PLL correction would still leave converter implementation, intrinsic clock noise,
reference generation and simultaneous analog operation unproved.

### Pass 138 — separate regenerative bias removes sampled reversal

Created an alternate transistor VCO and RF hierarchy with a separate regenerative-
tail bias port. Fixed that external bias at 1.08 V while sweeping main control
1.08/1.16/1.24/1.30 V. Device sizes, loads and stage topology remain unchanged;
the checker reverses the naming/port/control edits and verifies identity with
the original source. Actual receiver and divider loads remain connected.

Frequencies are 2.500751/2.636553/2.715986/2.746032 GHz. Secant slopes are all
positive, approximately 1.698/0.993/0.501 GHz/V. At equal main/regenerative bias,
the baseline differs from the original by only 74 Hz, a numerical comparison
rather than silicon reproducibility. LNA bias remains valid.
[Evidence](evidence/vco-split-tuning.json).

This isolates tied regenerative tuning as a contributor to the sampled reversal
and provides a concrete alternative. Four points do not establish global or
process-robust monotonicity. The much larger, variable tuning gain requires fresh
loop analysis and transient tests; old local-gain calculations do not transfer.
The separate bias remains ideal and externally supplied. Its implementation,
noise sensitivity, current budget, below-target tuning, startup and lock remain
open. Preserve both candidates; no layout work follows from this nominal screen.

### Pass 139 — separate-bias candidate brackets both RF target frequencies

Three further actual-divider-loaded fixed-control tests with regenerative bias
1.08 V give 2.261458/2.366934/2.459291 GHz at main control 0.98/1.02/1.06 V.
Together with pass 138, all seven sampled points increase from 2.261 to 2.746 GHz
across 0.98--1.30 V, bracketing nominal 2.4 and 2.5 GHz targets. LNA bias remains
valid. [Evidence](evidence/vco-split-lower.json).

This demonstrates sampled tuning coverage on both sides of the intended target,
not continuous/process-robust monotonicity, acquisition or channel accuracy.
Lower-region secants are 2.637 and 2.309 GHz/V, substantially larger than the
original tied-control gain. Next retest the actual closed loop with this alternate
VCO and account for the changed gain; retain separate bias generation/noise,
startup and converter implementation as unresolved high-risk obligations.

### Pass 140 — actual closed loop with independent regenerative bias

Substituted only the split-bias VCO/RF hierarchy into the pass-136 loop, with
regenerative bias fixed externally at 1.08 V. The checker verifies exact deck
identity after reversing that substitution and checks waveform hashes, frequency,
control range and LNA bias. Actual divider/PFD/pump/filter remain connected.
[Evidence](evidence/closed-loop-split-screen.json).

Over 100--200 and 220--320 ns, oscillator frequencies are 2.677036 and 2.688458 GHz;
control after 100 ns remains 1.080405--1.262989 V, inside the seven-point sampled
0.98--1.30 V envelope. This is not a proven continuous capture region. LNA bias
remains valid. The oscillator is above the 2.50 GHz target; lock is not established.
The filter, VCO and receiver remain preinitialized and biases externally ideal.

### Pass 141 — distinguish initial phase catchup from wrong pump polarity

Measured feedback lags the first four reference edges by
31.840/28.288/24.648/21.052 ns. Integrated pump charge in corresponding complete
reference cycles decreases 672.226/600.423/526.692/453.724 fC. The continued UP
response is consistent with this initial phase lag despite above-target oscillator
frequency. Do not misdiagnose positive charge alone as reversed frequency feedback.
[Evidence](evidence/pll-split-response.json).

Recomputed conditional averaged-loop poles for all six sampled split-bias secants,
including the gap between sweeps. All are left-half-plane under the stated ideal
model; four-time-constant estimates span 1.94--10.91 us (about 2.51 us for the
1.06--1.08 V secant). These are scenario estimates, not transistor stability or
settling guarantees. A 321 ns trace cannot establish acquisition. Next extend the
unchanged transistor loop to observe phase catchup and pump reversal, then test
longer settling, startup offsets and bias/load variation. No layout work yet.

Whole-chip reassessment: this loop is still a deterministic nominal experiment,
not low-jitter evidence. Radio ADC/DAC and complete TX, real reference/bias,
quadrature, wired CDR/BER, and host timing remain independent blockers. Unknown
process/package/noise parameters must be bracketed in both directions as explicit
scenarios; neither a narrow temperature/supply target nor these passes closes them.

### Pass 142 — first actual radio-ADC comparator, with a common-mode failure

Added an 11-FET clocked regenerative comparator using GF180 3.3 V devices.
Twelve nominal transient cases exercise alternating input polarity, both initial
signs, 1/10 mV differential magnitude and 0.8/1.2/1.65 V common mode. Fixture:
200 MHz ideal clock, 1 kohm source resistance and 1 pF per input, 50 fF per output,
TT 3.3 V 27 C. Require correct output rails above 90%/below 10% of supply over
1.9--2.3 ns after the start of each clock edge. Eight measured cycles per case
include polarity reversals, with explicit reset checks.
[Evidence](evidence/adc-comparator-screen.json).

All eight cases at 1.2/1.65 V common mode pass the decision window. All four at
0.8 V fail it; resets pass. Retain failures as a real interface/timing constraint,
not a reason to assert a universal input range. A finite source impedance is more
realistic than ideal clamped inputs but is not yet the actual sampler/CDAC.
Matched deterministic 1 mV resolution says nothing about offset, noise, ENOB or
yield. No conversion controller, CDAC, reference or output latch is implemented.
The 200 MHz comparator clock does not establish 20--40 MS/s ADC throughput.

In parallel, the unchanged split-bias PLL has a 1.201 us transistor run in progress
(`run_closed_loop_extended.sh`). Only simulation duration and retained output
vectors change. Its checker compares early waveforms to pass 140, verifies deck
identity, records feedback edges and per-reference-cycle charge, and never treats
pump reversal alone as lock. Do not treat this pending experiment as evidence.

### Pass 143 — actual sampler/comparator connection exposes held-node disturbance

Connected the existing GF180 differential transmission gate, separate hold
capacitors and new clocked comparator. Twenty-four fresh transient cases pair an
active comparator with an otherwise identical comparator held in reset. Sweep
1/5/10 pF per held leg, 1.2/1.65 V common mode and both signs of a 1 mV input.
A DC operating point starts in track; the sampler opens at 10--10.1 ns and the
comparator evaluates at 12 ns. Source resistance is 1 kohm per leg and output
loads are 50 fF. These capacitances are scenarios, not process bounds.
[Evidence](evidence/adc-sampled-comparator-screen.json).

All 12 active cases meet the same output-level window as pass 142. Nevertheless,
active-minus-reset-held peak differential disturbance is 2.952--3.357 mV at 1 pF,
0.622--0.703 mV at 5 pF and 0.313--0.353 mV at 10 pF. Common-mode disturbance
spans 2.15--25.84 mV. Differential residual after comparator reset is much smaller,
about 0.302--3.225 uV at 19--19.5 ns. Sampler turnoff alone introduces about
6.16--96.32 uV differential error in these small-input tests.

The checker verifies exact paired-deck identity except comparator clock/output
filename, source and waveform hashes, equal pre-evaluation held inputs and sampler
clock levels during evaluation. It separates comparator disturbance from sampler
turnoff rather than labeling all finite-source error as kickback.

This is a real connected input stage but still not an ADC. A correct decision
under deterministic symmetry cannot establish charge accuracy, metastability,
noise or mismatch tolerance. Returning near the initial sample after one reset
does not prove harmlessness during a sequence of CDAC decisions. Next connect
actual CDAC switching and test repeated decisions, acquisition from previous
samples, offset/noise and uncertain capacitance/loading before selecting buffering
or larger sampling capacitance. Larger capacitors have settling, drive and area
costs; do not assume they are a free precision fix.

### Pass 144 — first switched CDAC fails major-carry settling

Added an eight-bit differential capacitor array (20 fF unit, 5.12 pF per side)
with actual GF180 transmission-gate reference switches. Connected it to the
existing sampler/comparator. References are ideal sources behind 10 ohm/10 pF;
capacitors remain ideal and matched. External code controls step from 128 to
64/127/129/192 at 12 ns and return at 22 ns; comparator decisions follow at
16/26 ns. Each code has an identical reset-held comparator control run.
[Evidence](evidence/adc-cdac-step-screen.json).

This initial equal-switch-size implementation fails badly. At 15--15.8 ns,
128-to-127 produces +232.5 mV differential change instead of ideal -7.8125 mV;
128-to-64 produces -319.7 mV instead of -500 mV. At 25--25.8 ns, code-return
residuals span about -0.97 to -236 mV. Four of eight active decision windows have
the wrong expected rails. These are finite-time errors, not proof of permanent
charge loss. The major-carry waveform is still visibly settling; unequal switch
RC across a 128:1 capacitor range is a concrete suspect. Do not optimize only the
single-bit 128-to-129 transition, which hides the major-carry failure.

Next test switch conductance scaled with capacitance while preserving all other
circuit and stimulus settings. Reference/clock driver loading must subsequently
be included: larger switch banks do not provide free speed. This eight-bit
candidate is an implementation increment, not a reduction of full-chip converter
goals or a claim of eight effective bits. No complete SAR conversion exists yet.

### Pass 145 — proportional switch banks recover settling, stress references

Created a separate CDAC candidate with transistor multiplicity proportional to
capacitor weight, preserving unit device widths (2/4 um N/P) and all other test
settings. An initial attempt to enlarge single widths hit a PDK model-bin limit;
the simulated candidate uses parallel-device multiplicity, not out-of-range
single devices. This nominal multiplicity model does not establish independent
finger mismatch or a physical implementation.
[Evidence](evidence/adc-cdac-scaled-screen.json).

All eight decision windows now pass, including 128-to-127 and return-to-128.
Measured step gains are 0.988674--0.988700 of ideal across the four transitions.
Before the second decision, active code-return error is -0.55 to +0.16 uV in
these matched runs; this is not a noise/precision specification. The checker
verifies multiplicities, source hashes and unchanged benchmark decks except the
CDAC substitution. Comparator disturbance remains roughly 0.89--2.51 mV.

The improvement has a cost: the major-carry transition pulls the nominal 2.15 V
reference as low as 1.8697 V and pushes nominal 1.15 V as high as 1.4255 V through
the fixture's 10-ohm/10-pF reference network. Ideal code drivers hide switch-gate
drive current. Reference transients, actual clock/control loading and switching
overlap must be resolved before selecting this candidate. The near-constant gain
error is not proven calibratable across codes, process, temperature or signal.
Next test reference impedance/timing scenarios and actual conversion sequencing;
do not count successful signed decisions as eight-bit ADC accuracy.

### Pass 146 — longer actual PLL shows pump reversal, still no lock proof

The 1.201 us transistor run completed. Deck comparison confirms unchanged circuit,
initialization and timestep; all 17 measured signals match the earlier run exactly
through the checked overlap before 320 ns. Frequency falls from 2.688458 GHz at
220--320 ns through 2.645081 and 2.556528 GHz to 2.488502 GHz at 1100--1200 ns,
crossing the 2.50 GHz target. Pump charge changes from positive to negative in the
612.05--663.25 ns reference interval. The last complete interval still removes
226.48 fC, so this is an evolving transient, not stationary lock.
[Evidence](evidence/closed-loop-extended-screen.json).

Control stays within 1.028571--1.262989 V after 100 ns, inside the sampled tuning
envelope. LNA bias remains valid. Two feedback edges in one reference interval
around phase catchup demonstrate why naive one-to-one per-period edge pairing
would be wrong; the checker retains all edge times/counts. Sampled-envelope
containment does not prove continuous/process-safe capture. Startup remains
seeded/prebiased, with ideal references and bias currents and no intrinsic noise.
An unchanged 3.201 us run is now pending to examine settling; it is not evidence
yet. Even successful nominal settling would leave ripple/jitter and variation open.

### Pass 147 — reference scenarios pass signs while degrading analog accuracy

Nine fresh major-carry/return runs sweep 1/10/100 ohm reference source resistance
and 1/10/100 pF local reference capacitance. Exact deck comparisons permit only
these passive-network changes; the 10-ohm/10-pF baseline reproduces the earlier
waveform. All 18 signed decision windows pass, but analog step ratios range from
0.912725 to 0.988700 of ideal and return errors reach -594.37 uV. Correct bit signs
with generous margin are not conversion-accuracy evidence.
[Evidence](evidence/adc-cdac-reference-screen.json).

At 100 ohm, significant reference-span errors persist before comparison. More
capacitance reduces peak excursions but can prolong recovery: even 10 ohm/100 pF
has a 10.31 mV worst reference-span error over 15--15.8 ns. Strong 1-ohm scenarios
demand brief source-current peaks up to 121.9 mA; these are fixture currents, not
measured chip current or an implemented driver's capability. Peak supply-reference
errors span about 40--654 mV across scenarios. No one network setting closes drive,
settling, area and coexistence budgets simultaneously by this evidence alone.

These intentionally broad scenarios are not manufacturing bounds. Next model
actual code-driver timing/overlap and reference-drive behavior, then complete SAR
sequencing and measure a transfer curve before discussing effective resolution.
Retain whole-chip priorities: autonomous clock quality, complete RF conversion/TX,
real bias/reference and wired/host timing remain incomplete. No new layout work.

### Pass 148 — actual CDAC code drivers reveal large transient current

Replaced directly forced CDAC true/complement gate voltages with three GF180 CMOS
inverters per bit, weighted to the switched capacitor bank. First-stage logic
commands remain ideal; the final gate loads and complementary-path delay are
actual transistor behavior. Driver supply is separately metered but still ideal.
All four code-step/return cases retain correct signed decisions and approximately
0.98867--0.98870 step gain. Exact deck reversal and source hashes verify the
substitution. [Evidence](evidence/adc-cdac-driver-screen.json).

The major-carry case now exposes a brief 652.15 mA driver-supply peak and 423.48 pJ
net driver-rail energy over 10.2--30.5 ns. Other transition-pair energies span
1.67--318.80 pJ. These are fixture transition energies and peaks, not sustained
current, complete ADC power or package-qualified delivery. First command-source
drive cost is still omitted. True-output delay is ~136--141 ps and complementary
output ~161--171 ps for switching bits; complement lags by ~26--30 ps in this
nominal record. No nonoverlap guarantee follows.

This reveals a coexistence/supply-loading obligation hidden by ideal control
sources. Next test smaller driver widths against the same actual load, preserving
the array and reference network. Do not infer impossible operation from a brief
ideal-supply peak, or affordable operation from average energy alone. Actual
supply impedance, decoupling and driver timing/variation remain unresolved.

### Pass 149 — smaller physical driver widths reduce current without losing tested settling

A separate driver candidate changes only unit N/P widths from 2/4 um to 0.5/1 um;
array switches, capacitors, multiplicities, references and commands are unchanged.
The checker reverses the source change exactly and verifies the original deck
substitution. All eight nominal signed decision windows pass, with step gain
still ~0.98867--0.98870. [Evidence](evidence/adc-cdac-small-driver-screen.json).

For major carry, peak ideal-driver-supply current falls 652.15 to 153.89 mA and
transition-pair energy falls 423.48 to 146.70 pJ over the same measurement window.
True/complement delays rise to roughly 147--155/190--206 ps; their skew increases
to ~42--51 ps. This is useful nominal timing/energy evidence, not a process-robust
optimum. Reference disturbance remains ~269 mV, and the actual supply network
could alter both timing and charge injection. Neither nominal width choice is
package/PDN-qualified. Input command drive and autonomous SAR logic remain missing.

Next examine finite driver-supply impedance and reference drive/control timing,
then close the actual conversion loop. Avoid repeated nominal sign tests as a
substitute for threshold/transfer accuracy, mismatch, noise and previous-sample
settling. The 3.201 us PLL run remains active; no new lock result is available.

### Pass 150 — finite driver supply preserves this nominal residue test

Six fresh major-carry/return runs add 0.1/1/10 ohm source resistance and 10/100 pF
local capacitance to the smaller code-driver rail. The checker verifies exact
network-only deck changes and source/waveform hashes. All twelve signed decision
windows pass; predecision step gain remains 0.988691--0.988699 and code-return
error stays within about 0.24 uV in these matched deterministic cases.
[Evidence](evidence/adc-cdac-supply-screen.json).

Driver-rail minima range 3.2849 down to 2.6247 V. Upstream source-current peaks
range 18.15--152.37 mA. These include local-capacitor charging and must not be
confused with instantaneous current inside the driver. Inferred driver energy,
after subtracting resistor loss and capacitor stored-energy change, spans roughly
119.36--145.90 pJ across the same two-transition measurement window.

This supports isolated nominal driver-path tolerance in the chosen RC scenarios,
not accuracy/noise/PDN qualification. Comparator/sampler supplies and reference
fixtures remain separate; there is no shared ground, inductance or RF/PLL coupling.
A low current at the upstream source does not mean switching charge disappeared.
Do not extrapolate this deterministic signed test to ADC ENOB or chip coexistence.

Periodic risk reassessment: the ADC now has sampler/CDAC/comparator/code drivers,
but still lacks an autonomous SAR decision loop, output capture, real references,
transfer/noise/mismatch evidence and acquisition from changing samples. Prioritize
connecting that decision loop over further easy fixture sweeps. Autonomous PLL
settling/noise and complete RF TX remain separate unresolved high-risk blocks.
The existing 3.201 us PLL simulation remains active and must not be restarted.

### Pass 151 — actual SAR feedback exposes a readout-load failure

Connected 17 transistor D flip-flops and NAND selection/trial gates to the actual
sampler, CDAC, code drivers and comparator. A token advances MSB-to-LSB and each
comparator decision retains or clears its trial bit. START/reset/update/sample/
evaluation clocks remain external fixtures. Four constant inputs (-0.4, -0.123,
+0.123, +0.4 V differential) exercise all eight decisions. Bit interval is a
conservative 25 ns, not the required converter throughput.
[Evidence](evidence/adc-sar8-closed-screen.json).

Per-step checks confirm sampled decision retention, next-trial formation, one-hot
token movement and DONE timing. However, comparator decisions sometimes disagree
with pre-evaluation residue: output codes are 225/188/156/120 versus ideal-capacitor
quantizer codes 179/143/112/76. Final residues remain about +339--344 mV. Thus the
first actual conversion loop fails despite valid sequencing and earlier isolated
comparator tests. This failed result is retained, not labeled working ADC evidence.

The comparator's QN node directly drives all eight selection gates while QP lacks
that extra load. Dynamic readout asymmetry is a leading suspect for the large
systematic error. A controlled alternative inserts matched two-inverter readout
paths and is running (`run_sar8_buffered.sh`); no completed four-case result yet.
The checker will reverse only that insertion, then recheck every analog decision
and digital state transition. Independent sampling/CDAC gain errors, offset/noise,
real reference/clock generation, changing-sample acquisition and throughput remain
open even if buffering fixes this particular failure. No layout work follows.

### Pass 152 — matched comparator readout fixes tested SAR decision failure

Four completed transistor-loop runs insert matched two-inverter paths between
both regenerative comparator outputs and subsequent logic. All other circuit
and stimulus settings match pass 151; the checker reverses the insertion exactly.
All 32 comparator decisions agree with the pre-evaluation analog residue, versus
14 disagreements without the buffers. Decision retention, each next trial, token
advance and DONE pass independently. [Evidence](evidence/adc-sar8-buffered-screen.json).

Output codes for -0.4/-0.123/+0.123/+0.4 V are 180/144/111/75, one count away
from the ideal-capacitor quantizer in each case. Final residue spans about
-6.70 to -0.84 mV, inside approximately one observed DAC step for these samples.
This confirms a nominal functional conversion loop at conservative timing, not
an eight-effective-bit ADC, calibrated transfer curve, or process/noise tolerance.
The controlled substitution supports readout loading as the cause of the large
prior failure; it does not prove arbitrary downstream-load isolation.

A fresh timing experiment reduces bit interval from 25 ns to 5 ns, retaining long
initialization/acquisition and unchanged transistor circuitry. It is running as
`run_sar8_fast.sh`; no result yet. Comparator evaluation and update phases remain
external and deliberately separated. Even eight correct decisions at 5 ns would
not prove repeated 20--40 MS/s operation: acquisition, reset/start, clock generation,
output capture and changing-sample behavior still need closure. Layout remains
blocked on the whole-schematic gate; the separate PLL settling run remains active.

### Pass 153 — 5 ns SAR bit interval works in four nominal conversions

Four fresh buffered-loop runs shorten the bit interval from 25 to 5 ns, with
comparison starting 2 ns before each update and the next comparison 3 ns after it.
All 32 comparator decisions agree with pre-evaluation residue; captured trial
states, token advances and DONE pass. Final codes remain 180/144/111/75 for the
same four inputs. [Evidence](evidence/adc-sar8-fast-screen.json).

An initial checker window 1.4--1.8 ns after update was too early to require all
trial outputs at final 90%/10% rails. Waveforms explicitly show ongoing transitions.
The revised check measures settling through 2.8 ns and checks stable trial code at
2.5--2.8 ns, before the actual next comparison at 3 ns. Worst observed trial-logic
settling is 2.0975 ns after update. This correction aligns the checker with the
circuit's real deadline; it does not erase the slow-edge observation. Logic-rail
margin is not the same as analog DAC settling margin or process robustness.

The source and deck checks preserve the transistor circuit and allow only fixture
clock/timing changes from the buffered slow case. Timestep is 5 ps. Long startup
and acquisition remain, so this does not establish a 20 MS/s repeated converter,
and certainly not the 40 MS/s profile. A separate changing-input acquisition
screen is running to expose the analog drive/time requirement. Four comfortably
spaced input values also cannot establish near-threshold decision accuracy,
INL/DNL, noise or mismatch. The one-code ideal-model differences remain unresolved.

### Pass 154 — changing-sample acquisition exposes the upstream-drive bottleneck

Twelve paired transistor tests compare constant final input with a 0.8 V
differential reversal 10 ns before sampler opening, in both directions. Source
resistance is 100/300/1000 ohm per leg. All connected sampler/CDAC/comparator/
controller circuitry remains; evaluation has not started during measurement.
Exact deck comparisons and waveform/source hashes verify the controlled changes.
[Evidence](evidence/adc-sar-acquisition-screen.json).

Paired held-sample errors are approximately 21 uV / 5.74 mV / 144.14 mV at
100/300/1000 ohm, respectively (about 0.0027 / 0.735 / 18.45 ideal 2 V eight-bit
LSBs). Both directions show the same practical bottleneck. Subtracting the paired
constant-input case isolates acquisition history from nominal sampler turnoff;
it does not remove that separate gain/error mechanism from the real converter.
The constant-input held values themselves differ from the applied 0.4 V.

Thus the earlier 1 kohm input fixture cannot support this particular 10 ns large-
step acquisition requirement. Passing 5 ns bit timing is insufficient for a
50 ns repeated conversion frame. Next implement/test an actual baseband/sampling
driver against this load or revise acquisition topology; merely replacing a
fixture resistor with 100 ohm does not implement the required drive. Values are
scenarios, not process bounds, and RF waveform slew can differ from this deliberate
large-step stress. Previous conversion charge history, actual reset/start timing,
noise/mismatch, reference drivers and output capture still need verification.

### Pass 155 — actual sampling buffer fixes speed but exposes static error

Inserted two actual GF180 two-stage unity-feedback amplifiers between the existing
1 kohm source fixture and connected sampler/CDAC load. Bias uses diode-connected
PDK devices supplied by ideal 20 uA references; compensation is 1 pF with 100 ohm
series resistance. Four paired constant/stepped-input tests retain both directions
of the 0.8 V reversal and the 10 ns acquisition window. Exact source/deck checks
verify the buffer-only insertion. [Evidence](evidence/adc-sample-driver-screen.json).

Paired held-sample acquisition errors shrink from ~144 mV to approximately
-178/+166 uV. Quiescent current for both buffer legs and their fixture references
is ~3.294 mA, with step-window peaks ~3.374 mA. But constant differential output
error is ~33.8 mV for a 0.4 V input (roughly 8.5% attenuation). Thus this is a
useful drive implementation experiment, not an accurate sampling driver. The
constant held sample is only ~0.37085 V. Small paired history error does not cancel
that static transfer error in the real converter.

A controlled longer-channel first-stage PMOS mirror variant is running, preserving
nominal W/L by increasing multiplicity. It targets finite mirror gain/output
resistance as a hypothesis, not a proven sole cause. Large-signal startup/load
transients, loop stability margins, common-mode range, noise/mismatch, reference
implementation and combined supply loading remain unqualified. The separate PLL
settling run continues; no tapeout/layout gate is closed by this pass.

### Pass 156 — longer mirror is a failed fix, retained as evidence

Changing only the two first-stage PMOS mirrors from L=0.5 um/m=4 to
L=2 um/m=16 preserves nominal W/L. Exact source reversal verifies that geometry
change. Static differential error stays ~33.85 mV, while 10 ns paired acquisition
error worsens to ~31.88--31.90 mV. The nominal quiescent current remains ~3.294 mA.
[Evidence](evidence/adc-sample-driver-longmirror-screen.json).

Reject this candidate as a solution to the observed error. The unchanged static
error contradicts treating short PMOS mirrors as the sufficient explanation; the
larger mirror adds dynamic cost. Investigate actual internal headroom before more
blind sizing or compensation changes.

### Pass 157 — operating points identify the sampling-buffer headroom limit

Four DC tests of the original buffer sweep input common mode 1.2/1.4/1.65/1.9 V
with 0.4 V differential input. Model-reported VDS, VDSAT, VGS, VTH, gm and gds
are recorded for both input pairs and tails. Source/waveform hashes and numerical
values are checked. [Evidence](evidence/adc-sample-driver-op.json).

At 1.65 V common mode, the positive-side signal-input transistor has
VDS=0.12674 V versus VDSAT=0.21870 V, a -91.96 mV nominal headroom difference.
At 1.9 V that difference worsens to -286.26 mV. Differential gains are
0.99692/0.99340/0.91542/0.52546 across the four common-mode points. Lowering common
mode is not a universal fix either: at 1.2 V the negative-side tail falls about
19.03 mV below model VDSAT. Only the 1.4 V sample has positive headroom in all six
probed devices, and four points do not define a qualified input range.

This provides a concrete mechanism behind the severe nominal gain compression.
The second-stage gate voltage holds the first-stage output too low for the upper
input common mode. Next raise that internal node through second-stage sizing or
an appropriate topology change, then rerun the same static and loaded acquisition
tests. Positive model headroom would still not establish stability, precision,
noise, mismatch or process margin. The whole-chip schematic remains incomplete.

### Pass 158 — second-stage sizing repairs the identified nominal headroom failure

A separate candidate changes only output NMOS multiplicity from 32 to 8. The
same load current therefore requires a higher gate/internal-node voltage. Exact
source reversal checks that single device-bank change. Four fresh DC common-mode
tests retain the original inputs, biases and other device sizes.
[Evidence](evidence/adc-sample-driver-headroom-op.json).

At 1.65 V common mode and 0.4 V differential input, the deficient input device's
VDS-minus-model-VDSAT improves from -91.96 to +118.09 mV. Differential gain improves
from 0.91542 to 0.993857, leaving ~2.46 mV differential error. All six probed NFETs
have positive headroom at this point. This does not cover all devices, process
variation or every input level. The 1.9 V common-mode case still fails headroom,
and the 1.2 V case still exposes a tail limitation.

This controlled result supports the internal-node-headroom diagnosis rather than
blindly changing mirror dimensions. A loaded acquisition rerun is pending; reduced
output-device transconductance may change settling/stability. Positive nominal
headroom is not loop stability, conversion accuracy or a completed input buffer.

### Pass 159 — headroom repair survives loaded acquisition, with a settling tradeoff

Four paired loaded-sampler runs use the m=8 output-device candidate, preserving
all other benchmark settings. Static differential error is now ~2.458 mV instead
of ~33.8 mV. Paired held acquisition error is ~0.767--0.769 mV, worse than the
original buffer's ~0.17 mV but far below the unbuffered 1 kohm result. Quiescent
current remains ~3.293 mA. [Evidence](evidence/adc-sample-driver-headroom-screen.json).

The constant held value is ~0.402628 V for 0.4 V input: buffer attenuation and
sampler turnoff error partly oppose one another. Do not treat this accidental
combination as calibration or demonstrated accuracy. The controlled device change
repairs the diagnosed nominal headroom problem but still needs stability/load,
transfer, noise/mismatch and full changing-input conversion tests. Next integrate
this driver into repeated SAR frames; avoid endlessly optimizing an isolated
fixture without testing the requested converter behavior.

### Pass 160 — PLL settling run aborts; preserve partial evidence and cross-check numerics

The 3.201 us run terminated with ngspice timestep-too-small at 2.7625 us, naming
`vregen#branch`. It is no longer running. The runner failed before writing a
completed result. Preserved deck/log/partial waveform hashes and analyzed only
windows ending by 2.6 us, away from the failure neighborhood. All 17 observed
signals reproduce the successful 1.201 us run exactly over the checked prefix.
[Partial evidence](evidence/closed-loop-settling-aborted.json).

Frequency at 2.5--2.6 us is ~2.498493 GHz, with control 1.072867--1.091293 V and
net pump charge +35.74 fC over that window. This is incomplete settling evidence,
not lock, jitter qualification or physical instability. No conclusions are drawn
from the solver's vanishing timesteps near the abort.

A fresh unchanged-circuit run uses `.options method=gear maxord=2` as an explicit
numerical cross-check (`run_closed_loop_gear.sh`). Original failed artifacts are
retained in their separate directory. Gear numerical damping can alter oscillatory
behavior; successful completion alone will not prove physical stability. Compare
windowed behavior and timestep sensitivity before any stronger conclusion. Do not
restart the terminated original or mistake this new run for continuation of its
internal state. Real bias impedance, clock noise and process variation remain open.

### Pass 161 — actual driver and fast SAR complete changing-input conversions

Connected the headroom-repaired input driver directly to the full 5 ns/bit
transistor SAR loop. Four runs pair constant +/-0.4 V inputs with 0.8 V reversals
10 ns before hold, retaining the 1 kohm upstream source. Exact deck comparison
permits only driver insertion and the declared input step. All 32 comparator
choices agree with pre-evaluation residue, and each stored decision, next trial,
token advance and DONE is verified.
[Evidence](evidence/adc-sar8-driven-screen.json).

Constant and changing cases produce the same codes: 180 for -0.4 V and 75 for
+0.4 V. They remain one count from ideal-capacitor arithmetic. Acquisition history
still shifts analog residue by ~0.77 mV; it simply does not cross a code boundary
in these selected cases. One changing-input final residue is only about -0.189 mV,
so noise/mismatch and threshold-position tests matter. Equal final codes must not
be generalized into zero acquisition error or eight-bit accuracy.

The entire driver/sampler/CDAC/comparator/readout/decision-register path is now
connected for this nominal test, but phase clocks, bias references and supplies
remain ideal. Each run still contains only one conversion after long preparation.
A new two-stream test (`run_sar8_frames.sh`) exercises three 50 ns-spaced frames,
including controller reset, midcode preload and alternating input steps. It is
running, not evidence yet. Its external timing fixtures and absent host capture
still prevent calling it a qualified 20 MS/s converter even if those frames pass.

### Pass 162 — repeated frames reveal acquisition history despite correct decisions

Two completed streams exercise three conversions each at 50 ns spacing, with
alternating +/-0.4 V input and actual reset/preload between frames. Declared
sampling/input/reset timing is checked alongside source/deck identity. All 48
comparator decisions agree with residue; decision retention, token sequencing and
DONE also pass. [Evidence](evidence/adc-sar8-frames-screen.json).

Nevertheless output codes depend strongly on history: the negative-first stream
returns 180/83/178; the positive-first stream returns 75/178/83. The same positive
input changes from 75 initially to 83 after a prior conversion; the same negative
input changes from 180 to 178. This invalidates treating the single-conversion
success as a working full-rate ADC. The logic accurately converts a corrupted
held sample, so sign/sequence checks alone would miss the failure.

Waveforms show reset clearing the DAC code to zero and later token preload
restoring midscale during the acquisition interval. The sampling driver undergoes
large disturbances and ringing while also tracking the reversed input. A new
actual transistor-gate mask forces DAC code128 throughout acquisition, independently
of controller reset/preload; trial codes pass during conversion. The controlled
variant is running (`run_sar8_masked_frames.sh`), not yet verified. Its checker
will verify the mask at acquisition and actual DAC trial code before each decision.

The test still uses external phase fixtures and has no persistent host output
register. Noise, mismatch, thresholds/transfer, real references, physical coupling
and the 40 MS/s profile remain open. The unchanged-circuit Gear2 PLL cross-check
also remains active; no settled low-jitter result exists.

### Pass 163 — track-midcode mask improves history error but does not remove it

A transistor NAND/inverter mask forces the DAC to code128 during track while
passing SAR trial codes during hold. Both three-frame streams completed. Exact
source/deck comparisons isolate the mask insertion; sampled DAC control signals
verify midcode during acquisition and the intended trial before every decision.
All 48 analog decision signs and digital captures still pass.
[Evidence](evidence/adc-sar8-masked-frames-screen.json).

Negative-first codes are now 180/76/178, and positive-first codes 75/178/76.
The prior eight-code positive-input history shift shrinks to one code, but the
negative-input shift remains two codes. Thus this is a partial repair, not an
accurate repeated-frame converter. Mask assertion currently coincides with sampler
reconnection, so the midcode transition still disturbs the acquisition start.

A timing-only variant is running with midcode selected 0.3 ns after the final
update begins, before sampler reconnection 2.2 ns later. It retains the actual
mask gates and circuit. The checker will require DAC midcode settled before track,
verify all decisions, and distinguish last-trial analog residue from final stored
code because the DAC has already returned to midscale by output observation.
The new mask-phase source remains ideal; a real phase generator and robust timing
margin remain necessary. No full-rate ADC qualification follows from this test.

### Pass 164 — earlier mask timing: selected codes recover, rail deadline fails

The early-mask streams return 180/75/180 and 75/180/75, removing the selected
code-history shifts. However, the weak mask fails its pretrack 90%-rail criterion
on the MSB after positive-input frames. This criterion measures SD outputs
**upstream of regenerative code buffers**, not actual switch gates or capacitor
plates. It does not prove downstream analog settling failure. Failed evidence is
retained in [early-mask results](evidence/adc-sar8-early-mask-frames-screen.json).

### Pass 165 — installed MIM model invalidates the assumed unit-capacitor sizing

Twelve AC cases audit the installed M4-M5 1.5 fF/um2 model at its PCell's 5um by
5um minimum. Typical capacitance is 44.3336 fF at 27C, giving 11.3494 pF per
256-unit leg versus the assumed 5.12 pF. Model ff/ss values are 37.4619/51.2053 fF.
The tested par=2 parameter does not double capacitance. The model's voltage
coefficient expression is commented out; equal results at 0 and 1 V cannot
establish real voltage independence. [Audit](evidence/adc-mim-model-audit.json).

These are installed model/PCell observations, not certified process bounds or
proof that other capacitor structures cannot realize 20 fF. Availability of this
specific stack/density remains conditional. No layout was generated.

### Pass 166 — stronger mask verified; PDK-capacitor substitution launched

Increasing only the final MSB mask NAND transistor widths fourfold passes the
pretrack SD rail criterion in both streams. All 48 comparator signs agree with
residue; capture/token/DONE checks pass. Selected codes remain 180/75/180 and
75/180/75. [Results](evidence/adc-sar8-strong-mask-frames-screen.json). Small decision
margins and selected input coverage still prevent accuracy/noise qualification.

A new CDAC explicitly instantiates 512 PDK MIM unit capacitors, retaining switch
sizes and all other circuitry. Both three-frame streams are being tested at the
three MIM model corners. This is a controlled loading substitution, not a new
layout. The fixture also saves actual MSB switch gates, both MSB bottom plates,
and reference rails to distinguish control timing from analog settling. Results
remain unverified until completion and independent waveform checks.

### Pass 167 — PDK-capacitor partial results and second PLL numerical failure

The first completed PDK-MIM stream (typical, negative-first) gives 179/76/179.
All 24 comparator signs, trial/capture, token and DONE checks pass. Held sample
error measured 0.2--0.35 ns after hold is +1.016/-1.397/+1.403 mV. Actual MSB
switch gates reach the required polarity before later acquisition intervals;
bottom-plate error relative to its instantaneous reference reaches 1.801 mV
in one pretrack window. Reference-high voltage spans approximately 2.009--2.185 V
in the positive-input frame. Matching selected output codes is not an analog
settling, transfer or noise qualification. Five corner/sequence cases remain
pending at this pass. [Partial audit](evidence/adc-sar8-mim-frames-screen.json).

The new checker verifies explicit unit replication, complementary DAC wiring,
unchanged fixtures/circuit except the declared capacitor substitution, completed
waveform horizon and actual decision/capture behavior. It reports analog errors
without inventing a passing threshold. Its partial status cannot qualify corners
that have not completed.

The Gear2 PLL run also aborted at 2762.5 ns, reporting vsense#branch instead of
vregen#branch. Deck comparison confirms only the numerical integration method
changed. The failure time coincides with the end of reference rising edge 52
(100 ns delay, 51.2 ns period, 0.1 ns rise); this is a diagnostic clue, not proven
causality. The 2500--2600 ns VCO average is about 2.49843 GHz, similar to the
original run, but neither completed its requested horizon. Pointwise oscillator
waveforms differ substantially between methods; average-frequency similarity
cannot establish jitter or lock. [Failure audit](evidence/closed-loop-gear-aborted.json).
Both PLL runs are now terminal, not active. Next numerical diagnosis should
isolate reference-edge breakpoint sensitivity before interpreting the abort as
physical instability or launching another unchanged long run.

### Pass 168 — nominal MIM streams checked; controlled PLL breakpoint diagnostic

Both typical-capacitance streams now complete: 179/76/179 and 76/179/76.
All 48 selected decisions and associated trial/capture/token/DONE checks pass.
Held error remains approximately 1.0--1.4 mV; four ss/ff streams are pending.
The checker now explicitly distinguishes partial auditing from required complete
coverage and records aggregate logic/decision failures before rejecting a full
verification request. Make targets expose the MIM model, strong mask and complete
MIM-frame checks. No transfer/noise/sample-rate qualification is implied.

A PLL diagnostic replaces only the ideal reference PULSE representation with
explicit PWL points at the same edge times, levels and slopes. The full deck is
otherwise identical to the aborted Gear2 circuit. An independent fixture check
compares 100734 analytical probe points and the original simulator reference
record: maximum differences are 9.64e-12 and 2.37e-11 V respectively.
[Fixture evidence](evidence/closed-loop-pwl-reference-fixture.json).
The new long simulation is running; it does not yet explain or fix the abort.
Changing breakpoint representation is a numerical diagnostic, not implementation
of a real reference source or proof of PLL quality.

Pass 168 follow-up: the first ss MIM-capacitance stream completed during audit.
Codes become 181/74/181 and held error grows to approximately 15.5--15.7 mV,
although the decision/capture checks still pass. Only capacitor model corner
changed; FETs remain typical. This exposes material acquisition/held-sample
sensitivity to the higher capacitance scenario. Diagnose driver settling and
sampler turnoff from the stored waveform before attributing a physical cause.
Three streams remain pending; nominal matching codes did not predict robustness.

### Pass 169 — separate acquisition error from turnoff; screen buffer response

Both typical and high-capacitance polarity streams are now analyzed around the
sampling edge. Typical pre-turnoff error is approximately 4.1--4.5 mV and a
3.1 mV opposite turnoff-interval shift partially cancels it. High-capacitance
pre-turnoff error is 12.3--12.5 mV and a 3.2 mV shift adds to it. Held voltage
moves about 24--25 mV in the last nanosecond of track in both cases. The sampler
is catching a moving trajectory; selected nominal accuracy is partly cancellation.
[Waveform diagnostic](evidence/adc-sar8-acquisition-diagnostic.json).

The net turnoff shift includes feedthrough, channel charge, changing driver
behavior and ongoing settling. It is not an isolated charge-injection measurement.
Observed overshoot also does not by itself establish loop stability or instability.

Nine isolated track-mode AC cases connect actual FET buffers and continuously-on
sampling switches to 256 explicit PDK MIM units per leg. Three MIM corners and
0.5/1/2 pF compensation settings show low-frequency differential gain ~0.995 and
0.94--1.74 dB peaking, with peaks around 55--126 MHz. For the current 1 pF setting,
typical/high/low-capacitance peaks are 1.58/1.43/1.74 dB respectively.
[AC evidence](evidence/adc-driver-mim-ac.json).

This isolated test grounds capacitor bottoms and excludes finite references,
CDAC switching, comparator activity and large-signal input steps. Compensation
remains ideal. No phase-margin or ADC accuracy claim follows; no compensation
change has been adopted based on these curves. Next evaluate large-signal settling
and return ratio over the relevant loading states before selecting a repair.
The low-capacitance frame streams and PWL-reference PLL diagnostic remain active.

### Pass 170 — full capacitor scenario coverage and window-based settling candidate

All six connected ADC streams completed. The 144 comparator decisions and
trial/capture/token/DONE checks pass. Typical codes are 179/76, high-capacitance
codes 181/74, and low-capacitance codes 177/78 in the selected alternating streams.
Held errors reach about 15.7 mV high-capacitance and 15.4 mV low-capacitance.
This is a demonstrated analog acquisition sensitivity in both directions despite
correct logic. These PDK MIM corners are scenarios, not proven fab bounds.
[Complete stream audit](evidence/adc-sar8-mim-frames-screen.json).

Nine new isolated large-signal tests exercise both directions of a 0.8 V
input reversal with the sampler continuously on and PDK capacitor loads.
Errors are measured over windows rather than a selected crossing. Maximum
absolute error over 10--12 ns after the input edge is 2.83--6.82 mV for 0.5 pF
compensation, 14.86--21.39 mV for 1 pF, and 62.66--91.16 mV for 2 pF across
loads. Late static error is approximately 2.4--2.5 mV.
[Step-response audit](evidence/adc-driver-mim-step.json).

The isolated load has grounded bottoms and excludes reference disturbance,
CDAC trials and sampler turnoff. Smaller compensation is therefore only a
candidate. A connected six-stream run changes exactly the two buffer CC
parameters from 1 pF to 0.5 pF, retaining every other circuit and fixture.
The candidate checker verifies this controlled difference before applying the
existing logic and analog measurements. The candidate is running, not adopted.
Return ratio, noise, mismatch and physical compensation remain unqualified.
The waveform-equivalent-reference PLL diagnostic remains active.

### Pass 171 — reference drive demand and narrower PLL failure diagnosis

Across all six original PDK-capacitor streams, the high reference fixture sources
up to 14.97 mA and sinks up to 3.57 mA; the low reference sources up to 3.12 mA
and sinks up to 14.26 mA. Maximum rail deviations are 149.7/142.6 mV. Current is
computed from the actual 10ohm resistor voltage; integrated charge also accounts
for the 10pF reservoir's endpoint charge change.
[Reference-load audit](evidence/adc-reference-load-audit.json).

These are observed demands with the present impedance, not universal ratings:
a different driver impedance changes the current waveform. A real reference
buffer must handle both polarities; an unloaded voltage reference is insufficient.
Actual reference generation, buffering, stability, startup and noise remain absent.

The waveform-equivalent PWL PLL test is terminal: it aborts at 637.7 ns at
vsense#branch, coinciding with the end of a reference falling edge. The original
pulse representation failed later at 2762.5 ns. This demonstrates numerical
sensitivity, not a proven physical cause or lock.
[Failure record](evidence/closed-loop-pwl-reference-aborted.json).

Both exact reference waveforms then complete 3.201us in a minimal 100ohm/1pF
passive-load circuit with the same Gear2 method and 2ps maximum timestep.
[Minimal diagnostic](evidence/reference-breakpoint-screen.json). The source fixture
alone does not reproduce the abort. Next retain active PFD/pump/filter circuitry
in the reduced test before attributing the issue to a generic simulator bug.
No full PLL long run remains active at this point.

The half-compensation connected ADC candidate remains running. Its first nominal
stream gives 178/77/178 and ~4.57mV held error: worse nominal absolute accuracy
than the original setting, despite better isolated settling. Await the other
loads and inspect timing sensitivity before selecting or rejecting the candidate.

### Pass 172 — connected settling improvement; reduced active PLL diagnostic

The 0.5pF compensation candidate has completed both typical streams and the
negative-first high-capacitance stream. Codes are 178/77 in the selected frames.
High-capacitance held error improves from ~15.7mV to ~4.3mV; nominal error remains
~4.57mV, worse than the original cancellation-assisted nominal value.
The last acquisition nanosecond now spans ~2.1--2.5mV instead of ~24--25mV.
[Candidate acquisition diagnostic](evidence/adc-sar8-acquisition-fast-comp-diagnostic.json).

This supports improved settling across the completed loads, not precision closure.
Pre-turnoff error remains ~6.3--6.7mV with a ~2.0--2.15mV opposing turnoff shift.
Low-capacitance cases and the other high-capacitance polarity remain pending.
No default compensation change has been adopted; static error, input range,
return-ratio stability, noise/mismatch and actual compensation realization remain.

A reduced PLL test now retains the actual PFD, charge pump, current-sense source,
filter and bias currents, replacing oscillator/divider feedback with a declared
ideal stimulus. Both reference representations are exercised with feedback
leading, coincident and lagging by 10ns, at the same 3.201us horizon, Gear2 and
2ps maximum timestep. This six-case diagnostic is running. Its checker preserves
aborts as evidence and checks completion without claiming lock or autonomous
timing. The ideal feedback removes divider loading/supply interactions, so even
completion would narrow the numerical problem rather than qualify the full loop.

### Pass 173 — reject a scaled sampling amplifier as the reference driver

The full-chip review keeps RF TX, bias/reference generation, wired recovery,
host timing and whole-chip integration visibly incomplete. To advance reference
implementation, a new actual-FET candidate uniformly scales the existing
sampling amplifier (devices and compensation) by 1/4/16. Twelve DC sweeps test
both directions of +/-20mA load at the intended 1.15/2.15V reference targets.
[Driver evidence](evidence/reference-buffer-dc-screen.json).

The candidate fails as a direct precision reference driver. At 2.15V, unloaded
error is ~64--65mV. Even S=16, drawing ~25.9mA unloaded, has ~238mV error while
sourcing 15mA. At 1.15V unloaded error remains ~10.48mV; S=16 supports the selected
+/-15mA operating points with ~9--12.5mV absolute error, but costs ~26.1mA.
These results reject simple uniform sizing as sufficient repair. A driver with
appropriate input/internal-node range and source/sink output capability is needed.
The exact operating-region cause has not yet been isolated with device OP data.

Small candidates driven beyond sourcing capacity fall below ground under the
ideal current load. Such points are compliance failures, not safe model-qualified
operating states. They are preserved without extrapolating silicon performance.
The screen implements a driver only: target voltages and bias currents remain
ideal, compensation/reservoir capacitors are ideal, and transient stability,
startup, noise/mismatch and coupled reference regulation are unverified.

The reduced active PLL tests and remaining half-compensation ADC streams remain
running. No layout work has begun and the full-chip schematic gate remains closed.

### Pass 174 — identify reference headroom loss; complementary candidate improves range

Six device-OP checks diagnose the original S16 reference amplifier. At a 2.15V
target unloaded, XIN VDS is ~0.110V versus model VDSAT ~0.237V, with gm/gds~0.85.
At +15mA load it falls to VDS~0.051V versus VDSAT~0.364V, gm/gds~0.14. At the
1.15V target XIN retains substantial headroom. This directly supports loss of
input-stage gain at the high reference, rather than insufficient width alone.
[Operating-point audit](evidence/reference-buffer-op-screen.json).

A complementary-polarity version swaps N/P devices, supply connections and bias
ports while retaining geometry and scaling. Twelve DC sweeps complete. At the
2.15V target S4 consumes ~6.34mA unloaded with +13.15mV offset; error is +17.46mV
while sinking3mA and -2.95mV sourcing15mA. This substantially improves intended
high-rail drive compared with the original topology. The original S4 low-rail
candidate has -5.84mV error sinking15mA and -12.00mV sourcing3mA. Together they
suggest asymmetric high/low driver choices with ~12.9mA unloaded total, but the
static errors, finite gain and dynamic behavior remain unqualified.
[Complementary DC audit](evidence/reference-buffer-complement-dc-screen.json).

Neither candidate is a voltage/current reference generator. Ideal target/bias
sources remain, and target trimming would not by itself fix load regulation,
noise or stability. Overload points outside supply rails remain rejected
compliance states. No reference candidate has yet been connected to the ADC.

All six half-compensation ADC streams also complete: 144 selected decisions and
logic checks pass. Worst held error over these streams falls from ~15.7mV to
~4.58mV; low-capacitance error is ~0.90mV. Nominal/high-capacitance codes are178/77,
low-capacitance179/76. This supports retaining 0.5pF as the next test candidate,
not declaring transfer accuracy or replacing the architecture's verification gate.
[Complete candidate audit](evidence/adc-sar8-mim-fast-comp-screen.json).
The active reduced PFD/pump/filter diagnostic remains running.

### Pass 175 — connect actual reference output drivers to the ADC

A new reference-pair subcircuit connects the complementary S4 driver to VH and
the original-polarity S4 driver to VL. The connected ADC replaces both ideal
source/10ohm reference-feed paths with this pair, retaining the 10pF reservoirs,
0.5pF sampling-buffer compensation and all existing sampling/SAR fixtures.
The 2.15/1.15V targets now drive amplifier inputs only. Actual diode-connected
bias devices are supplied by ideal20uA sources on a separate ideal3.3V rail.

Two nominal-MIM alternating streams are running. The checker verifies exact
reversal of the declared substitution, original driver-source hashes, actual
reference-pair wiring, all SAR decisions and reference rail waveforms. New supply
current observations measure the reference subsystem's average and peak demand.
[Pair schematic](analog/reference/adc_reference_pair.spice).

This advances connected transistor implementation without claiming a working
reference subsystem. Target/bias generation, realistic compensation/reservoirs,
startup, noise, mismatch, loop stability and shared-supply behavior remain open.
DC offsets and dynamic reference excursions may invalidate selected codes; the
test preserves those failures. The earlier half-compensation checker still passes
its complete six-case logic/decision audit after the new test mode was added.
The reduced active PLL diagnostic remains live; no final outcome is claimed yet.

### Pass 176 — reference integration exposes decision-time span error

The first actual-reference-driver stream completes with codes175/78/175 and
held errors ~5.06/-5.87/+7.08mV. All 24 selected comparator/logic decisions pass,
but the high reference falls as low as1.9264V in one frame and the low rises as
high as1.3370V. Reference supply averages13.10--13.23mA and peaks17.22mA in these
frames. This is failed analog regulation evidence, not a qualified reference.
[Connected partial results](evidence/adc-sar8-reference-driver-screen.json).

A separate audit examines the actual pre-comparator windows rather than only
whole-frame extrema. Reference span means range0.99928--1.06835V versus the
intended1V; high/low individual errors reach69.1/66.2mV in those windows.
[Decision-window audit](evidence/adc-reference-decision-windows.json).
Thus errors persist where decisions are made, not merely during irrelevant
switching spikes. Total code error cannot be attributed solely to references;
sampling, static offsets and comparator behavior coexist.

Next reference work must assess dynamic drive/compensation and reservoir sizing
with actual capacitor models. Merely trimming the static target will not remove
time-varying span. The second nominal stream remains active.

The first reduced active PLL case (pulse reference, feedback10ns early) completes
3.201us with the actual PFD/pump/filter and ends atCTRL2.23438V. It passes both
previous abort times without reproducing failure. This is a numerical diagnostic
with forced feedback and a ramping filter, not a locked-loop result. The other
five cases remain pending. [Partial diagnostic](evidence/pfd-pump-breakpoint-screen.json).

### Pass 177 — actual reference reservoir model and controlled connected substitution

Both original FET-reference-driver streams now complete. All48 selected decisions
and capture checks pass, but codes remain175/78 and worst held error~7.09mV.
Decision-window span still reaches~1.06835V in either polarity sequence, with
individual rail errors~69.1/66.5mV. This confirms a repeatable regulation problem.

A reservoir subcircuit now uses hierarchical explicit replication of2048 PDK MIM
units per rail, without relying on the model's inactive par parameter. Independent
AC checks and hierarchy-count verification measure90.7952pF typical,
104.8684pF ss and76.7219pF ff at27C. These are model scenarios, not fab bounds.
[Reservoir model audit](evidence/reference-reservoir-model.json).

The connected candidate replaces only the two ideal10pF reservoirs with these
PDK subcircuits, leaving reference drivers, compensation, clocks, sampling and
SAR unchanged. Both nominal streams are running. The checker reverses the exact
substitution before comparing the baseline and reuses decision-time reference,
logic, held-sample and supply-current measurements. More capacitance may reduce
droop or impair settling; no outcome is presumed.

Actual targets/bias generation and compensation remain incomplete. The MIM
process option is conditional, and no layout/parasitic/noise/mismatch or stability
qualification follows from capacitor-count/AC verification. The reduced active
PLL diagnostic still has only its first completed case audited; remaining cases
continue without restarting the live process.

### Pass 178 — larger reservoir shifts impedance peaks, does not fix reference regulation

The first larger-reservoir stream completes with codes178/78/178 and held
errors~4.51/-4.01/+2.99mV. Nevertheless reference span at decision windows ranges
0.95272--1.05169V; individual rail errors still reach54.2/28.0mV. Some selected
sample errors improve, but time-varying reference accuracy remains inadequate.
[Decision-window evidence](evidence/adc-reference-reservoir-decision-windows.json).
The other nominal polarity stream remains active.

Twelve small-signal output-impedance cases exercise both actual reference drivers
at their selected sourcing/zero/sinking DC loads with either ideal10pF or the
explicit90.8pF PDK reservoir. High-rail peak impedance remains approximately
43--86ohm, moving from92--115MHz to33--42MHz. Low-rail peaks remain~34--78ohm
with the larger reservoir, moving into~75--112MHz. Low-frequency impedances are
much smaller (~0.26--1.74ohm). [Impedance audit](evidence/reference-driver-impedance.json).

Thus DC drive strength concealed large frequency-dependent output impedance.
The larger reservoir shifts the peaks rather than removing them. Compensation
and damping need direct assessment before further capacitor-size optimization.
These are closed-loop impedance measurements at fixed DC operating points, not
return-ratio/phase-margin or stability proof. Large CDAC switching, cross-rail
coupling, noise and periodic operating points are absent from this isolated test.
No reference or ADC candidate is qualified by these partial improvements.

### Pass 179 — reservoir damping tradeoff and reduced-fixture reset caveat

Both larger-reservoir streams complete, with decision-window span as low as
0.94409V and as high as1.05187V. The first-stream-only minimum0.95272V therefore
understated the observed worst error. The checker retains both sequences.

Twenty-four controlled AC scenarios add0.5/2/10/50ohm in series with the actual
reservoir, covering both rails and three selected DC loads. Worst peak output
impedance becomes84.7/81.0/66.5/43.5ohm respectively. At50ohm, worst impedance at
200MHz and1GHz rises to32.1/28.8ohm; at0.5ohm those values are11.3/1.78ohm.
[Series-damping audit](evidence/reference-reservoir-damping.json).

This damping branch reduces resonance at the cost of high-frequency bypass.
It is not adopted as a sufficient repair; driver compensation/topology needs
further work. Resistors are ideal exploratory values, not physical PDK instances.
No transient, noise, mismatch or phase-margin qualification follows from AC peaks.

A separate fixture audit notices that the reduced PLL feedback=-10ns case starts
its first feedback rising edge at90ns, over90--90.1ns: exactly the external reset
release interval. Consequently its eventual positive filter ramp cannot be used
as a clean phase-polarity test for an established feedback lead. Which edge the
PFD captures and the resulting modulo-cycle branch require waveform verification
with unambiguous reset timing. The completion result remains valid numerical
scope evidence, but no phase-sign conclusion is attached to it. Ongoing cases
are not modified or restarted. This caveat is recorded in their checker/evidence.

### Pass 180 — reset timing selects the PFD phase branch

Two short actual-PFD/pump/filter tests differ only in external reset-release time
(90--90.1ns versus80--80.1ns); all reference/feedback waveforms, device circuits,
filter and initial state remain equal. Both complete321ns. Exact deck/source
checks and reference/feedback edge measurements verify the controlled comparison.
[Waveform audit](evidence/pfd-reset-phase-screen.json).

Over203--305.4ns (two reference periods), original reset timing yields83.93ns UP,
1.552ns DN and+1721.5fC pump charge. Earlier reset yields1.522ns UP,21.554ns DN
and-356.9fC. Control changes are+33.35mV versus-6.87mV over that window.
Thus the apparent unexpected pump polarity in the forced-feedback -10ns case
comes from the selected startup phase branch, not evidence of reversed pump
connections. Two deterministic timings do not establish metastability behavior.

The autonomous loop must still demonstrate acquisition from both branches and
from both sides of target frequency. A correct PLL need not always start on one
particular branch, but cannot rely on a seeded filter and favorable clock phase.
[Startup obligations](spec/clock-startup-verification.md) make those distinctions
explicit, including real reset/bias generation and downstream clock qualification.
The long reduced numerical diagnostic remains active and unchanged; its existing
completion observations retain their limited numerical scope.

### Pass 181 — controlled internal compensation comparison

Seventy-two AC cases vary the reference amplifiers' internal Miller capacitance
and series compensation resistance across both rails and three selected DC loads.
New parameterized variants reproduce the original source when normalized, and
the original parameter setting reproduces the baseline waveforms numerically.
Every candidate deck is checked against its declared change.
[Compensation audit](evidence/reference-compensation-screen.json).

The lowest worst-case output-impedance peak in this grid is50.41ohm, versus
86.08ohm for the original compensation with the same PDK reservoir. That setting
uses actual scaled values8pF and500ohm; worst200MHz impedance is10.22ohm.
This is partial impedance improvement, not a satisfactory reference or stability
result. No setting is adopted; compensation elements remain ideal and connected
transient, return-ratio, noise/mismatch and startup qualification remain absent.

### Pass 182 — codify and incrementally improve the analog design method

At the user's request, the [analog design workflow](spec/analog-design-workflow.md)
now records experiment selection, cheap-to-expensive diagnostics, model audits,
controlled baseline comparisons, provenance, failure handling and integration
checks. An [experiment template](verification/experiment-template.md) makes the
question, alternative explanation and decision criteria explicit before a run.
An evidence-linked lessons table captures reusable findings rather than relying
on the chronological pass log alone.

Apply the workflow to subsequent work, update it when evidence reveals a better
method, and reassess whole-chip risk after integration failures and at least every
five substantive passes. Existing runners are not retroactively declared compliant:
after-run-only hashing, fixed column indices and duplicated fixtures are explicit
maintenance debt to improve when touching the relevant tests. Source-model noise
coverage and phase-noise methods remain unresolved, not solved by documentation.
The schematic plan now points to current inventory/risk evidence instead of an
obsolete instruction to build already-existing PFD/pump/filter candidates.

### Pass 183 — whole-chip reassessment starts the missing radio DAC

The workflow review prevents continued reference tuning from obscuring a missing
TX converter. Autonomous clock/noise evidence, ADC/reference accuracy, complete
RF conversion, wired recovery and host/coexistence remain unresolved. Reference
compensation results remain diagnostic; no candidate was adopted as a fix.

A first8-bit binary-weighted differential current-steering DAC now uses actual
GF180 tail-current and steering FETs. All255 weighted unit currents share an
external bias node. An actual diode-connected FET establishes that node from an
ideal20uA reference. Behavioral sources provide only testbench digital bit inputs;
ideal clamps expose static behavior at1.2/1.65/2.1V outputs.
[DAC schematic](analog/dac/current_steering8.spice).

The new runner hashes the candidate and installed top-level SPICE/model files
before and after execution. The checker validates all16 true/complement controls,
waveform headers,256 codes per clamp and binary weighting. All three static
transfers are monotonic. Differential endpoints are approximately+/-5.157,
+/-5.221 and+/-5.281mA; step size40.45--41.42uA. Total output current changes
~2.4% across clamp voltages, so output-voltage sensitivity is already visible.
[Static transfer audit](evidence/current-dac-dc-screen.json).

Very small endpoint-normalized DNL in matched weighted instances does not prove
physical8-bit precision. Next discriminate compliance/gain error under a finite
differential load, then major-carry glitch and settling with actual code drivers.
Mismatch/noise, reference generation, reconstruction/upconversion/output drive
and connection to host data remain missing. This is a DAC core candidate, not a
single-chip radio or a completed TX chain.

Three pulse-reference reduced PLL cases now complete the requested3.201us
horizon. The -10ns startup-branch caveat remains; PWL cases are still running.
Completion with forced feedback does not settle the full autonomous-loop failure.

### Pass 184 — finite DAC loads expose swing-dependent static error

Eight controlled256-code sweeps replace equal ideal output clamps with50/100/200/
400ohm resistive loads to2.15 or3.3V. Actual DAC devices and code fixtures are
unchanged. Before/after source hashes, exact baseline reversal, all bit controls,
waveform headers and full code coverage are verified.
[Finite-load audit](evidence/current-dac-loaded-dc-screen.json).

All tested transfers remain monotonic, but the2.15V/400ohm case reaches1.863LSB
endpoint-normalized INL and0.196LSB DNL; minimum output is~0.199V. This is
swing-dependent curvature hidden by equal clamped-output tests. Do not infer a
specific transistor operating-region cause without device OP measurements.
The3.3V/400ohm case has~0.417LSB INL despite remaining above1.235V output.

The2.15V/100ohm scenario produces~+/-0.5218V differential endpoints with minimum
output1.628V and matched-model INL~0.00526LSB. Retain this as a useful dynamic
fixture, not an accuracy-qualified operating envelope. Its ideal termination
supply/resistors and matched current weights omit noise, mismatch and real load
variation. Small normalized static errors are not evidence of physical8-bit
precision, SFDR or dynamic ENOB.

Next TX experiment should expose127-to128 major-carry switching in both directions,
actual true/complement driver delay, output capacitance and skew. This builds on
a finite-load baseline without pretending the missing reconstruction/mixer/output
chain or actual reference generation is already implemented.

### Pass 185 — actual DAC code drivers reveal major-carry glitches

Six connected transistor tests exercise127->128->127 with the existing actual
true/complement code-driver cells,100ohm loads to2.15V,32/128 explicit MIM units
per output (~1.42/5.67pF), and MSB command skew-200/0/+200ps. Command edges are
external fixtures; driver asymmetry and DAC switching are actual FET behavior.
Before/after source hashes, headers, all command/settled control bits, baseline
circuit preservation and complete horizons are checked.
[Dynamic audit](evidence/current-dac-dynamic-screen.json).

The intended step is~4.09mV, but zero-command-skew peak errors are~35.7--102.1mV;
with declared skew, the worst is~360.0mV. Larger capacitive load reduces peak
amplitude while slowing the return to the selected half-LSB diagnostic band.
All12 transitions meet that band in the late15--20ns window; last observed band
violations extend to~3.81ns after the command edge. This finite settling result
is not evidence of acceptable RF glitch energy, SFDR or modem EVM.

Actual code-driver supply peak reaches~163.1mA briefly on a separate ideal rail.
That is not average consumption or a qualified supply requirement, but it exposes
an integration cost omitted by static current measurements. Local supply/ground
response, physical clock skew, mismatch/noise and complete TX filtering/mixing
remain untested. The binary core is a failed dynamic baseline, not a qualified
radio DAC. Next compare controlled carry timing or segmentation with actual logic.

### Pass 186 — worst DAC glitch persists at half timestep

The worst observed small-load/+200ps case repeats with2.5ps rather than5ps
transient step limits and an otherwise identical deck. Peak errors become345.72
and360.71mV, changing~0.12/0.19%; absolute integrated error changes below0.10%.
[Step-sensitivity audit](evidence/current-dac-dynamic-fine-screen.json).

This supports the robustness of the large-glitch observation to this numerical
change. It is one-case timestep sensitivity, not complete convergence, extracted
performance or a silicon bound. Error area uses a declared ideal-step midpoint
and observation window; it is not a measured RF spectrum.

### Pass 187 — full-loop numerical failure reproduced without the VCO/RF chain

The reduced actual PFD/pump/filter diagnostic now reproduces a timestep-too-small
failure at637.7ns for PWL reference with equal-phase PULSE feedback. All three
PULSE-reference cases and the PWL-reference/-10ns case completed3.201us; the
last PWL/+10ns case remains active. The failure time matches the earlier full-loop
PWL abort and coincides with a nominal falling-edge endpoint.
[Reduced diagnostic](evidence/pfd-pump-breakpoint-screen.json).

This evidence narrows the problem: neither oscillator/divider nor RF loading is
necessary to produce this numerical failure. It does not establish that the
original pulse-reference full-loop abort has the same cause, nor prove a simulator
bug or physical instability. Startup phase-branch caveats still apply separately.

A shorter800ns reproducer is now running on the failed reduced circuit. It
compares the original mixed PWL/PULSE inputs against two bitwise-identical PWL
source descriptions. Only feedback representation changes; PFD, pump, filter,
reset and initial conditions stay fixed. It records actual clock differences,
UP/DN, pump current and control voltage, preserves nonzero exits and partial
waveforms, and hashes sources before/after. No outcome is claimed while pending.

This newly discriminating autonomous-clock evidence takes priority over the
planned DAC driver timing modification. The DAC glitch remains unresolved and
no purported balanced driver has been implemented or qualified in this pass.

### Pass 188 — equivalent clock representation changes reduced-test completion

The800ns controlled reproducer completes its comparison. Mixed PWL reference /
PULSE feedback aborts again at637.7ns; bitwise-identical PWL descriptions on both
inputs complete800ns. Exact source/deck checks confirm identical transistor
circuits, reset, filter precharge, numerical method and step limits; only feedback
source representation changes. [Comparison](evidence/pfd-matched-waveform-screen.json).

Maximum saved reference/feedback voltage difference in the failed case is only
2.51e-12V; matched inputs are exactly equal in the saved data. The failed case's
control range is1.07547--1.10496V, with last control1.08782V, UP/DN near-0.975/
-0.964mV and pump current~47.3nA. The saved prefix does not show control-rail
runaway. Do not interpret a terminal sample as proof of the uncomputed trajectory.

This establishes source-representation sensitivity of the reduced numerical
fixture, not a transistor-performance failure or a diagnosed simulator defect.
It does not prove the cause of every earlier abort. Identical forced clocks
cannot replace autonomous VCO/divider feedback in a qualification test.
Next numerical/implementation work should examine a realistic reference-input
buffer or other justified source-interface model, retaining actual feedback and
checking physical behavior as well as solver completion. Intrinsic jitter/noise,
startup/acquisition and long-loop completion remain unresolved.

## Pass 189 — actual reference buffer does not resolve clock-source sensitivity

Added a four-FET, two-inverter nominal 3.3 V reference interface and independently
checked the completed [buffered-reference experiment](evidence/pfd-buffered-reference-screen.json).
The checker verifies source/artifact hashes, reverses the exact circuit addition
against the baseline, checks waveform headers and actual completion, and preserves
failed-case observations. The buffer is not a pad/ESD or level-conversion design.

The PULSE case completes 800 ns; the PWL case still aborts at 637.7 ns. Rising-edge
propagation delay is 127.746–127.755 ps in the observed cases. Integrated pump
charge over 203–305.4 ns is 38.165 fC for both. Thus a realistic transistor load
and propagation delay at the reference input are insufficient to remove this
particular numerical failure. No autonomous acquisition or jitter claim follows.
The previously running six-case forced-clock matrix also finished: five complete
3.201 us, while equal-phase PWL reference/PULSE feedback aborts at 637.7 ns.
See [the completed matrix](evidence/pfd-pump-breakpoint-screen.json).

Next discriminate exact coincident source-breakpoint sensitivity with explicitly
small positive and negative timing offsets, keeping the actual buffer and failed
baseline. Such perturbations change physical phase and must remain diagnostics,
not a way to waive an autonomous-loop failure. Restore real divider feedback for
subsequent integration. Whole-chip priorities remain autonomous timing/noise,
reference regulation, DAC carry distortion and complete RF paths; this experiment
does not reduce those qualification obligations. No layout was generated.

## Pass 190 — signed clock-offset diagnostic launched

Implemented `analog/pll/pfd_offset_screen.py`, its container runner, and an
independent `verification/check_pfd_offset.py`. The experiment preserves the
Pass189 buffered PWL reference circuit, changing only ideal feedback-source delay
by -10 ps or +10 ps. Each requests 800 ns at the same 2 ps maximum step. Exact
baseline reversal, pre/post source hashes, generated-deck hashes, timeouts, return
codes and failed artifacts are retained. The checker distinguishes partial case
coverage from completed horizons; no completed cases were available at launch.

This tests sensitivity to near-coincident source events in both directions.
It changes physical phase, so even successful completion cannot establish a
simulator defect or qualify autonomous operation. The failed zero-offset baseline
remains preserved. Results belong in `evidence/pfd-offset-screen.json`; while
partial, no numerical-remedy conclusion is justified. The first run remains live
under exec session62105; poll that handle before considering any rerun.

Pass190 interim observation: the -10 ps feedback-offset case completed the full
800 ns and passed the independent deck/artifact/header/horizon checks. Control
range is 1.076317–1.103808 V. The +10 ps case remains running under session62105;
post-run source consistency for the whole matrix remains pending. This supports
sensitivity to the changed source timing but does not distinguish phase effects
from numerical breakpoint effects. The zero-offset failure remains authoritative.
The implementation inventory now includes the actual reference buffer, and the
risk document leads with current whole-chip priorities instead of historical
recommendations. Inventory audit still reports all-chip schematic incomplete.

## Pass 191 — reference buffer integrated into actual-feedback loop

Started `verification/run_closed_loop_buffered.sh` under exec session46440,
output `scratch/transceiver-closed-loop-buffered`. It adds the four-FET reference
buffer to the preserved Gear2 full-loop fixture, retaining actual VCO, seven
CML dividers, feedback restorer, PFD, charge pump, filter and RF load. Reference
source timing, reset, initial conditions, loads and 3.201 us requested horizon
are unchanged. Independent whole-deck reversal confirms the declared change.
The runner records 18 recursively discovered source/include dependencies before
execution, generated-deck hash, timeout/return status, artifact hashes and source
consistency afterward. Completion and waveform analysis remain pending.

This is an integration experiment, not a demonstrated numerical remedy. The
reduced buffered mixed-source fixture still fails; full-loop acquisition, cold
startup, intrinsic noise and physical loading remain unqualified regardless of
whether this transient completes. No clock-offset workaround is applied to this
full-loop experiment. The separate signed-offset diagnostic remains live under
session62105, with only its negative-offset case verified complete so far.

## Pass 192 — signed-offset evidence complete; full-loop phase audit prepared

Both -10 ps and +10 ps feedback-source offsets completed 800 ns. Independent
checker verifies exact baseline reversal, hashes, headers, monotonic finite time
samples, actual horizons and pre/post source consistency for the complete matrix.
Control ranges are 1.076317–1.103808 V and 1.076262–1.104143 V respectively.
[Completed evidence](evidence/pfd-offset-screen.json). Session62105 is terminal.
Zero offset still fails at 637.7 ns: this establishes sensitivity in both tested
directions, not a causal distinction between physical phase and source-breakpoint
handling. Offsets are diagnostic scenarios, not a proposed PLL requirement.

Added `verification/check_closed_loop_buffered.py` for the actual-feedback run.
It checks provenance and exact circuit change, preserves terminal failures, and
will measure VCO frequency/control/pump charge in completed windows plus ordered
feedback-minus-reference edge timing without modulo wrapping. Edge counts and
startup-dependent alignment remain explicit; none of these metrics alone proves
lock or phase-noise performance. Its provenance/pending path has executed; its
terminal waveform-analysis path awaits the run and is not yet verified on that
result. Session46440 remains live; do not restart it merely because this turn ends.

## Pass 193 — actual DAC switch-gate timing audit

While the actual-feedback PLL run remains active, independently audited all six
existing DAC dynamic cases with `verification/analyze_dac_gate_timing.py`.
Artifact hashes, waveform headers and finite data are checked before extracting
both directions of physical true/complement gate crossings and input command
crossings. [Evidence](evidence/current-dac-gate-timing.json).

At the MSB, rising true-path delay is ~131.2–131.4 ps and complementary-path delay
~158.8–159.1 ps; reverse delays are ~142.5 ps and ~171.2–171.4 ps. Thus the serial
true/complement driver introduces ~28 ps crossing asymmetry across both load
scenarios and all tested command skews. Weighted both-high/both-low gate states
also occur. These 1.65 V threshold classifications are timing diagnostics, not
transistor conduction/current measurements: source-node voltage and finite edge
slopes remain relevant. No causal decomposition of glitch energy is claimed.

This narrows the next circuit experiment: correcting complement-path asymmetry
alone cannot eliminate the independently imposed +/-200 ps inter-bit command
skew. Compare a segmented upper DAC with actual decoder/driver circuitry while
retaining the binary baseline and the same loads/skews, and observe source-node
and output currents as well as gate timings. Ideal thermometer controls alone
would not qualify that replacement. No new DAC topology is implemented in this
pass. Full-loop session46440 remains active; retain its exact handle.

## Pass 194 — actual transistor thermometer decoder

Implemented `analog/dac/thermometer4.spice`: a four-bit to fifteen-threshold
static-CMOS decoder built from explicit GF180 FET AND/OR/inverter circuits.
`build_thermometer4.py` emits the concrete netlist using shared threshold terms.
Every threshold output has a two-inverter output stage; internal logic depth
still varies. There is no behavioral logic inside the decoder and no retiming.

Ran the pinned ngspice nominal DC sweep with ideal binary input stimuli. The
independent checker verifies all sixteen command codes and all240 threshold
outputs, pre/post model/source hashes, generated deck and waveform artifacts.
Maximum rail error is36.6uV. [Evidence](evidence/dac-thermometer4-dc.json).
The10mV rail tolerance is a static logic diagnostic, not a converter accuracy
requirement. Ideal20fF output capacitors are only a fixture; DC says nothing about
fanout timing or switching current. Tiny static supply current does not predict
active power.

This supplies the actual decoder for the proposed four-upper-bit thermometer /
four-lower-bit binary DAC comparison. Current cells and drivers are not connected
to this decoder yet. Skew hazards, shared-node loading, major-carry glitch energy,
retiming need and dynamic power remain to be measured before replacing the binary
candidate. No modulation-quality improvement is claimed. Full PLL session46440
remains active and unrelated to this local DC verification.

## Pass 195 — connected segmented converter static verification

Implemented `analog/dac/segmented8.spice`: four binary-weighted low cells plus
fifteen weight16 high cells, actual thermometer decoder and nineteen actual
true/complement drivers. Total current weighting remains255 units. This candidate
is combinational and has no retiming or dynamic hazard suppression.

Ran256-code nominal DC transfer with2.15V/100ohm output terminations and the same
20uA ideal bias-current fixture as the binary baseline. Independent checker
verifies source/deck/artifact provenance, all input codes,3840 loaded decoder
threshold states and transfer monotonicity. Endpoints are+/-0.5218168V, nominal
LSB4.092681mV; endpoint-fit INL0.005276LSB. Maximum output difference from the
binary baseline is1.166uV. [Evidence](evidence/dac-segmented8-dc.json).
These small errors reflect nominal matched-device simulations, not physical
precision, yield or converter qualification.

Next compare actual major-carry transitions with the binary baseline under both
PDK output loads and signed command skews. Preserve decoder hazards, observe
current-cell source nodes and output current, and account for decoder supply
current. The present static result does not establish glitch improvement.
Full-loop PLL session46440 remains active; no layout was generated.

## Pass 196 — segmented DAC dynamic comparison underway

Started six-case connected segmented-DAC matrix under exec session49628, scratch
`transceiver-dac-segmented-dynamic`: same127/128 transitions, +/-200ps and zero
MSB command skew,32/128-unit actual PDK output loads,100ohm/2.15V terminations and
5ps maximum step as the binary experiment. Captures actual decoder outputs, every
switch gate, all19 current-cell source nodes and combined decoder/driver supply
current. Static core is preserved; commands and load additions are audited.

First independently checked cases:32-unit load, -200ps skew has250.26/224.42mV
forward/reverse peak errors; zero skew has62.56/52.03mV. Both settle into the late
half-LSB diagnostic band. Binary counterparts were352.48/337.58mV and92.90/102.11mV.
This is a limited reduction, not a usable-modulation claim: error remains large
relative to4.09mV step. Corresponding ideal-supply peaks are71.64mA and21.72mA.
[Partial evidence](evidence/dac-segmented8-dynamic.json). Matrix post-run source
consistency and remaining cases are pending. Do not promote these two selected
cases to complete uncertainty coverage or inferred spectral quality.

The reduction does not remove combinational decoder hazards or input skew.
Finish the comparison, inspect transient thermometer states and decide whether
actual output retiming is needed before more sizing. Full PLL session46440 is
still active independently; preserve both handles and all failed baselines.

## Pass 197 — segmented matrix complete; decoder timing failure isolated

All six segmented dynamic cases complete79.9ns with source/deck/artifact hashes
and commands independently checked. Peak errors (forward/reverse,mV) are250.26/
224.42,62.56/52.03,60.38/56.68 for32-unit load at-200/0/+200ps;134.89/127.09,
32.84/28.26,32.55/32.81 for128-unit load. All pass only the late half-LSB settling
diagnostic. Larger-load negative-skew reverse peak is slightly worse than the
binary124.73mV result; segmentation is not a uniform improvement. Session49628
is terminal. [Complete matrix](evidence/dac-segmented8-dynamic.json).

Independent waveform analysis distinguishes input intermediate codes from decoder
hazards. With zero command skew, the upper input code stays7/8 at the1.65V logic
threshold, but the decoded high-cell count reaches11 forward and4 reverse.
Counts outside7/8 persist approximately220/195ps. Non-monotonic thermometer
patterns also occur. With-200ps skew, actual input codes transiently reach15/0,
and decoder excursions extend~455/445ps. These are sampled threshold/duration
diagnostics, not measured current shares. They explain why static decoder truth
was insufficient without claiming a complete glitch-energy decomposition.
[Hazard audit](evidence/dac-segmented-decoder-hazards.json).

Next candidate: actual registers after the decoder and on the four low bits,
clocked after the command/decoder settling interval, then actual output drivers.
Preserve combinational baseline, account for added latency/clock current, and test
clock-to-output mismatch and available setup margin in both directions. An ideal
instantaneous thermometer code would hide the failure rather than implement a
solution. Full PLL session46440 remains active. Whole-chip noise/reference/host/
coexistence obligations remain unchanged and no layout is authorized by this pass.

## Pass 198 — actual post-decoder register candidate launched

Implemented `analog/dac/segmented8_registered.spice`: nineteen actual static-CMOS
resettable DFFs reuse the existing transistor cell, one per decoded high control
and low bit. Actual two-inverter clock buffering (scales16/64) drives the register
bank. Original decoder, nineteen output drivers and current cells remain. There
is no ideal register or instantaneous thermometer substitution in the core.

Started two initial32-unit-load cases (-200ps and zero input skew), exec
session91751, scratch`transceiver-dac-segmented-registered`. Ideal external clock
rises at6/31/56ns with100ps edges, versus command onset30/55ns. Reset releases
at2ns; the first clock captures the initial127 code. This intentionally allocated
capture interval is a fixture, not a verified setup/hold budget or host interface.
Observe the actual buffered clock, all register outputs, every downstream gate,
source nodes and total decoder/register/driver current, plus clock-source current.
Independent reversal verifies the first generated deck's declared additions
against the prior combinational case. Simulation results are pending.

Account for the added update latency when comparing waveforms: evaluate waveform
shape/glitch relative to the intended registered update as well as latency from
input command. Do not label the deliberately held old sample as glitch energy.
Do not hide actual clock-to-output skew by independently aligning every cell.
Next verify capture, command-time isolation, real output synchronization, power
and residual glitch; then vary setup/clock timing and loads. Post-run source
consistency and checker waveform analysis remain outstanding. PLL session46440
also remains active. No layout or full-chip readiness claim follows.

## Pass 199 — register isolation works in two nominal cases; residual glitch remains

Both registered cases complete79.9ns; source consistency, exact declared deck
changes, artifacts, headers and horizons are audited by
`verification/check_segmented8_registered.py`. All19 outputs retain the old
threshold state until the actual buffered clock edge, despite decoder transients.
Selected late windows correctly hold127/128/127, rail errors below20uV, output
errors below0.164mV. Clock edges occur near6.198/31.198/56.198ns.

With actual buffered-clock edge as the ideal update time, peak errors are
11.069/37.500mV for-200ps input skew and11.069/37.490mV at zero skew. The two
command-skew cases now closely agree, supporting nominal input-skew isolation
at this allocated capture interval. Pre-capture output deviation is below0.128mV.
This is not complete glitch suppression: reverse-direction error remains~9LSB.
Absolute error areas use a different intended update instant than combinational
results, explicitly including clock-to-output delay and excluding intentional
one-sample holding latency. No individual cell alignment is performed.
[Evidence](evidence/dac-segmented8-registered.json).

Whole-record logic-supply peak is69.16mA; external clock source peak1.27mA.
These include actual clock/register/decoder/driver behavior but ideal rails;
startup versus steady-update peaks need separation before budgeting. Next inspect
actual register and downstream gate crossing spread at the residual reverse
transition, then sweep capture margin and positive command skew/load before
promoting the candidate. Noise/mismatch, arbitrary codes, repetition, RF spectrum
and host integration remain unqualified. Session91751 is terminal; PLL46440
remains active. No layout generated.

## Pass 200 — residual registered-DAC timing and current localized

Independent analysis of both completed registered waveforms verifies data hashes
and headers, then measures the five intentionally changing cells at1.65V crossing.
Forward register clock-to-Q spread is38.6ps; true-gate spread56.3ps. Reverse
register spread is202.0ps, growing to247.6ps at true gates: L0 rises~120.5ps after
clock while H8 falls~322.6ps after clock. Complement gates add~28–29ps. The fixed
DFF drives unequal driver input sizes, and opposite-polarity transitions have
substantial delay differences. These are observations, not a controlled causal
separation of load versus polarity. [Timing evidence](evidence/dac-registered-timing.json).

The previously reported69.16mA maximum occurs during startup0–10ns. In the
negative-skew case,29–40ns and54–65ns update windows peak at16.95/14.36mA and draw
42.51/40.74pJ from the ideal3.3V logic rail. These windows include decoder and clock
activity without idle subtraction. A small negative signed idle-window energy
is retained as simulated charge return, not interpreted as negative dissipation.
Actual shared supply impedance and repeated steady-state power are unqualified.

Next discriminate unequal register loading with identical small input buffers
before the weighted drivers, preserving the current candidate and its polarity
asymmetry. Such buffering may improve load dependence but cannot be assumed to
repair opposite-direction clock-to-Q delay; measure both. Also retain positive
command skew, larger load, setup interval and clock uncertainty as required
coverage. Do not treat one carry event or low DC error as radio-quality evidence.

Whole-chip reassessment: this bounded DAC series has established a real dynamic
failure and an improved but incomplete registered candidate. Autonomous timing
noise/acquisition, reference regulation, quadrature/complete RF paths, wired CDR,
host timing and shared supply coexistence remain independent high risks. The
buffered actual-feedback PLL is still running under46440; no new layout. Avoid
further broad DAC tuning grids before the identified load/polarity discriminator
and missing timing coverage are resolved.

## Pass 201 — equal register input loading experiment launched

Added `analog/dac/segmented8_isolated.spice`: each of19 register outputs now
sees an identical S1 inverter followed by S4 inverter before the existing weighted
driver. Independent circuit reversal verifies only those buffer insertions and
the subcircuit name change. This equalizes nominal immediate input capacitance,
not downstream delay; Miller effects, polarity, weighted fanout at the added
buffer outputs and PVT/mismatch remain relevant.

Launched the same two32-unit-load capture fixtures under session96174, scratch
`transceiver-dac-segmented-isolated`. Input timing, clock/reset, output loads and
current cells are unchanged. The existing registered checker now supports
`--isolated`, including exact deck reversal, augmented header/provenance checks
and the same capture/isolation/glitch metrics. Its baseline path was rerun;
the isolated path currently has no completed cases, so no benefit is claimed.
Each extra buffered node is saved to separate register delay from buffer/driver
delay. Post-run hashes, residual glitch and power comparison are pending.

A reduction at register Q that merely reappears downstream would reject this
as a sufficient remedy. Added latency/current and reverse-polarity asymmetry
must be reported even if nominal gate spread improves. PLL session46440 remains
active independently. No new layout and no whole-chip gate change.

## Pass 202 — equal-loading comparison rejects buffering alone as a remedy

First isolated-buffer case (-200ps input skew) completes and passes capture/
isolation checks. Forward/reverse peaks are16.824/36.476mV versus11.069/37.500mV
without the added buffers. Forward absolute error area grows3.38->8.04mV ns;
reverse area is essentially unchanged at18.37mV ns. Thus this candidate is not
promoted as a remedy. Second zero-skew case and matrix final provenance remain
pending under96174. [Partial output evidence](evidence/dac-segmented8-isolated.json).

Shared timing analyzer now supports both candidates, checking the augmented
headers. Identical register loads reduce low-bit same-polarity Q crossings to
nearly identical values, but opposite-polarity spread remains~125ps. Forward
spread actually increases38.6->125.0ps, showing that unequal loading previously
partly canceled polarity delay. Reverse Q spread reduces202.0->125.1ps, while
true-gate spread only reduces247.6->219.1ps; forward true-gate spread worsens
56.3->89.6ps. [Timing evidence](evidence/dac-isolated-timing.json). These are nominal
threshold timings, not mismatch bounds or current-waveform decomposition.

This exposes accidental delay cancellation in the earlier candidate. Next focus
on a register/output interface with controlled opposite-polarity timing and
loading rather than assuming equal capacitance guarantees simultaneous current
steering. Evaluate both directions and increased load/skew coverage before
selecting a topology. Avoid fitting a fixed delay to this single nominal carry.
The unbuffered registered baseline remains preserved. PLL46440 remains active;
other whole-chip risks and the no-layout gate are unchanged.

Pass202 completion: second case now completed; the checker verifies both cases
and final pre/post source consistency. Zero-skew peaks are16.826/36.491mV, closely
matching the negative-skew case. Session96174 is terminal. The candidate remains
rejected as a sufficient remedy; earlier pending statements record launch/interim
state rather than current coverage.

## Pass 203 — complementary register-output interface launched

Implemented `analog/dac/segmented8_dual.spice`: each DFF Q and QB drives an
identically sized first inverter, followed by equally sized weighted output
inverters for true/complement current-steering controls. Both branches have two
inversions. This replaces the serial complement-generation path, loads both
register outputs, and removes nominal branch-depth asymmetry; it does not imply
equal switching times or cure the internal DFF polarity delay. Unequal stage
fanout across cell weights remains explicit.

Independent netlist reversal verifies the new driver substitutions against the
preserved registered core. Started the same negative/zero input-skew fixtures
under session94549, scratch`transceiver-dac-segmented-dual`, capturing all QB
outputs in addition to existing clocks/Q/gates/current-cell source nodes. No
clock, reset, command, load or initial-state change is introduced. Shared checker
now supports`--dual`; its pending path and existing isolated path were executed.
No completed dual-driver waveform was available at this checkpoint.

Compare both gate polarities, output error and supply current, and retain any
added latency. Capturing correct values alone will not select this candidate;
large residual glitches or improvement in just one direction remain inadequate.
Further clock/setup/load variation is still required. PLL46440 remains active;
no layout or readiness claim follows.

## Pass 204 — complementary interface exposes Q/QB timing separation

First dual-driver case (-200ps input skew) completes and holds/captures the
selected codes correctly. Q and QB rail checks are now both included in the
shared checker. Output error peaks worsen to50.245/55.893mV versus11.069/37.500mV
for the original registered candidate. Error areas grow to39.37/48.50mV ns.
This candidate is not selected. Second case and final matrix hashes remain
pending under94549. [Partial evidence](evidence/dac-segmented8-dual.json).

Actual Q/QB crossings differ by~52ps, with sign reversing with transition
polarity. The nominally equal-depth output branches grow that separation to
~60ps at weight1 and~133ps at weight16. Thus nominal schematic symmetry does not
ensure simultaneous switch gates: cross-coupled register sequencing and opposite
edge propagation remain important. [Timing evidence](evidence/dac-dual-timing.json).
No channel-current decomposition or PVT bound follows from threshold crossings.

End this small interface-topology series rather than repeatedly sizing around
one carry event. Retain the original registered candidate as the best measured
of these incomplete candidates; next extend positive input skew, larger load
and capture-margin coverage before any promotion. Its residual glitch still
requires spectral/error-budget evaluation and cannot be waved away by static
accuracy. Reference regulation and complete RF/wired integration remain independent
high risks. The actual-feedback PLL46440 is still active. No layout generated.

Pass204 completion: both cases and final source consistency are now verified. Zero-skew peaks are50.243/55.892mV, confirming the regression in this second fixture. Session94549 is terminal; the dual-driver candidate remains unselected.

## Pass 205 — broaden retained registered-DAC coverage

Started the four previously untested cases of the original registered candidate:
32-unit load/+200ps input skew and128-unit load at-200/0/+200ps. The actual circuit,
clock/reset timing,79.9ns horizon and5ps maximum step are preserved. The same
registered runner construction and shared checker (`--coverage`) apply; planned
cases are confirmed in the live manifest. Session15502, scratch
`transceiver-dac-segmented-coverage`; no completed case at this checkpoint.
Results will be recorded separately from the existing two-case evidence, keeping
both baselines and pending coverage explicit.

This extends declared loading/timing scenarios, not guaranteed fab bounds or
capture-margin qualification. A later setup/clock sweep is still needed, as are
noise/mismatch, arbitrary-code spectra and full TX integration. No new nominal
interface topology is being selected based on structural symmetry. The living
workflow now records lessons from decoder hazards, registered latency, accidental
delay cancellation, failed symmetry assumptions and startup/update power
separation. PLL46440 remains active; no new layout.

## Pass 206 — positive skew verified; observed capture interval quantified

The32-unit/+200ps registered case completes with correct selected captures and
pre-capture holding. Peaks11.067/37.481mV closely match the prior negative/zero
skew cases. Three128-unit cases remain in the live coverage job15502; final
coverage-source consistency is pending. No complete load-matrix claim is made.

Added independent waveform audit `analyze_dac_capture_interval.py`. Across all
three completed small-load skew cases, all nineteen actual register inputs stay
within10% of the intended rail for0.626–0.710ns before the buffered capture edge.
The last violating sample is retained and the next sample starts the conservative
reported clean interval. Actual register-loaded decoder outputs and low-bit
inputs are used, not ideal command timing. [Evidence](evidence/dac-capture-interval.json).

This is an observed settling interval for one code transition, not DFF setup/hold
qualification or a proven worst-case logic delay. It motivates advancing capture
by0.5ns and0.8ns as discriminating tests spanning comfortable/near-boundary nominal
conditions; preserve delayed capture as baseline and inspect both directions.
A later capture test is also needed before claiming broad timing tolerance.
No arbitrary delay is added to individual output cells. Clock/load variation,
repeated operation, noise/mismatch and full TX quality remain unresolved.
PLL46440 remains active; no layout changes.

## Pass 207 — signed capture-phase test finds a wrong-code boundary scenario

Started fixed-circuit clock shifts of-0.8,-0.5,+0.5ns relative to the original
registered fixture. Only ideal external capture-clock phase and output filename
change; exact deck reversal and source/artifact checks preserve that contract.
Session4421, scratch`transceiver-dac-segmented-capture`. Independent checker records
wrong captures instead of treating simulation completion as functional success.

First-0.8ns case completes but captures incorrect data; the reverse intended127
holds weighted code95 in the late window. This rejects that early capture scenario
and demonstrates why prior~0.63ns observed rail settling could not be called a
verified setup margin. The other signed phases remain pending. This is a finite
code/load scenario, not a metastability or universal setup/hold boundary.
[Evidence](evidence/dac-capture-phase.json).

Larger-load registered coverage has meanwhile completed negative/zero skew cases
with peaks~6.87/20.21mV and~6.88/20.20mV; positive-skew large load remains pending
under15502. The DAC still has residual output errors despite correct late codes.
PLL46440 remains active. Preserve all three handles and do not restart on an
observation timeout. No layout generated.

## Pass 208 — six registered load/skew scenarios verified

All four added coverage cases and final source hashes are verified; session15502
is terminal. Combining them with the two original registered cases yields six
disjoint nominal scenarios with identical source/model hashes and selected
correct captures/pre-capture holding. Independent summary checks exact case
coverage and records constituent evidence hashes.
[Coverage summary](evidence/dac-registered-coverage-summary.json).

32-unit load peaks remain~11.07/37.5mV across-200/0/+200ps input skew.128-unit
load peaks are~6.87–6.88/20.20–20.21mV. These are finite scenarios, not uncertainty
bounds, arbitrary-code accuracy, SFDR or radio qualification. Larger capacitance
reducing peak does not establish improved distortion or adequate bandwidth.

Capture phase-0.5ns completes and gets127/128/127 correct in selected windows;
-0.8ns gets127/208/95. Do not interpolate a safe interval or infer monotonic failure
behavior from those two points. The+0.5ns case remains pending under4421. Future
clock/interface allocation must include measured setup/hold, mismatch, supply,
command skew and actual distribution, not just nominal command-to-clock delay.

Whole-chip checkpoint: stop treating this finite carry improvement as completion
of RF TX. Remaining architecture-level issues include full reconstruction/mixer/
output integration, intrinsic converter/LO noise, failed dynamic reference
regulation, wired CDR and host timing. PLL46440 remains active. Next independent
work should revisit reference regulation or a missing full-path interface rather
than multiply nominal DAC driver candidates. No layout generated.

## Pass 209 — reference compensation candidate connected to ADC

Capture-phase diagnostic is complete: -0.8ns gives incorrect208/95 codes,
-0.5ns and+0.5ns give the intended selected127/128/127 values. Final source hashes
are verified; session4421 is terminal. These sampled outcomes still do not prove
a continuous safe setup/hold interval or arbitrary-code timing.

Returned to the independent reference-regulation failure. Added
`analog/reference/adc_reference_pair_tuned.spice`, selecting the previously
measured AC candidate with actual8pF compensation and500ohm series resistance
per S4 amplifier (CC parameter2pF and RZ parameter2000ohm). FET geometry, bias,
reservoirs and ADC remain unchanged. Components are still ideal compensation
passives; this does not qualify physical implementation or stability.

Started both opposite-history ADC streams under1550, scratch
`transceiver-adc-sar8-reference-compensated`. Runner preserves the reservoir
baseline, exact interface substitution, pre/post source hashes, deck hashes,
timeouts and failed artifacts. Assess reference span at the same comparator
windows, held input error, codes and supply current; do not select by AC peak
reduction alone or by a correct code. Results and independent checker integration
are pending. The autonomous-feedback PLL46440 remains active. No new layout.

## Pass 210 — reference comparison audit prepared and baseline rechecked

Extended the existing connected ADC checker with`--reference-compensated`, using
exact reversal to the reservoir baseline before the existing circuit/decision
checks. Added pre/post provenance, return/timeout status and artifact/deck checks
for the new runner, and explicit reference compensation values. The decision-window
analyzer accepts`--compensated` and uses the same pre-comparator windows as before.
The new pending path executes; no compensated waveform has completed yet.

Reran the existing reservoir baseline through its complete checker. Baseline
reference span across decision windows is0.95272–1.05169V in first-negative
history and0.94409–1.05187V in first-positive history, for an intended1V span.
Worst individual high/low rail errors reach54.30/29.50mV. FET instance lines of
both tuned amplifier variants match their originals exactly; only compensation
parameterization/selection is changed. This preserves the interpretation of the
upcoming comparison rather than combining a new amplifier topology with new
compensation. Initial runner source hashes and planned histories are recorded.

Session1550 remains live, with terminal waveform/provenance paths still pending;
PLL46440 also remains live. Assess connected decision errors, sample acquisition
and current together before adopting the candidate. Reduced AC impedance peak
alone is not stability, ADC precision or radio-quality evidence. No layout.

## Pass 211 — connected compensation gives partial improvement, still inaccurate

First compensated ADC history completes with codes177/78/177 and held errors
~4.49/-4.45/4.34mV. The shared ADC checker audits the exact circuit substitution,
source/artifact/deck hashes and decision behavior; the paired comparison verifies
identical hold/decision windows against the reservoir baseline.
[Comparison](evidence/reference-compensation-connected-comparison.json).

Decision reference span improves from0.95272–1.05169V to0.97273–1.04358V in this
history. Worst high/low reference error shrinks54.22/27.95mV to30.25/15.74mV.
Average reference current remains~13.08–13.20mA; peak reaches14.90mA, slightly above
the baseline14.65mA. The changed code177 instead of178 is not proof of improved
conversion accuracy, and remaining2.7–4.4% span excursions still defeat a precision
reference claim. Candidate is retained for diagnosis, not adopted as qualified.

Opposite-history stream and final source consistency remain pending under1550.
PLL46440 is still live, observed at~2.704us toward its requested3.201us horizon;
progress past a time value would not itself establish lock. Next reference
analysis should inspect whether worst errors are sustained offsets or residual
ringing at the actual evaluation window before another compensation change.
No new layout or whole-chip readiness claim.

## Pass 212 — buffered actual-feedback PLL still aborts

Full-loop session46440 is now terminal. Ngspice exits1 at2.7625us with timestep
collapse and reported node`vregen#branch`, before the3.201us requested horizon.
This matches the prior unbuffered full-loop failure time. Independent checker
verifies exact circuit change, full source consistency, deck/artifact hashes and
waveform header/horizon; preserves the partial trace and ordered phase history.
[Evidence](evidence/closed-loop-buffered-screen.json). Checker session75036 also
completed; do not treat either handle as still running.

Partial frequency windows are2.676GHz at100–200ns,2.644GHz at500–600ns,
2.489GHz at1100–1200ns and2.482GHz at2000–2100ns. These are finite seeded
trajectories, not lock, jitter or stability qualification. The buffer is rejected
as a sufficient remedy for the full-loop simulation failure, consistent with the
reduced mixed-source result. The reported node is diagnostic context, not proof
of the physical failing device or a simulator defect.

Clock priority: preserve the full failing fixture and reduce the interaction
around the source breakpoint/ideal regenerative-bias branch before another long
integration run. Changes must distinguish source-event handling from actual bias
loading; don't turn reference representation or ideal-clock matching into a lock
claim. Autonomous phase-noise/startup/variation evidence remains independently
missing even if numerical completion is repaired. Reference compensation stream
1550 remains live; its first history is only partially improved. No layout.

## Pass 213 — complete reference comparison exposes dynamic residual

Both compensated streams complete with source consistency and all existing
selected logic/decision checks retained. Codes are177/78/177 and78/177/78;
held errors remain about4.3–4.5mV. Session1550 is terminal. Across both histories,
decision mean reference span improves0.94409–1.05187V to0.96840–1.04360V; worst
high/low rail errors are31.43/15.74mV. No tested loop or reference simulation is
currently left running from these passes.

Added an independent reference-motion audit with waveform hashes and VH/VL header
checks. Maximum measured span motion within a150ps pre-comparator window grows
from5.15mV to5.79mV despite the reduced overall excursion. A mathematical optimal
constant additive correction of the recorded decision spans still leaves at
least37.60mV worst mean error (baseline53.89mV). This is a bound on that fixed
postprocessing operation, not on changing a physical bias or implementing dynamic
calibration. [Evidence](evidence/reference-motion-diagnostic.json).

Thus the candidate is a partial dynamic improvement with a competing local
settling metric, not a reference solution. Do not tune static targets to make one
code correct. Next inspect load-current response and amplifier internal-node
headroom during the worst windows, or test a controlled stronger output-stage
candidate while retaining the existing compensation baseline. Short-window
secants do not prove oscillation or identify the transistor responsible. Noise,
startup and mismatch remain unqualified. No layout generated.

## Pass 214 — connected reference transistor observations added

Started an observation-only rerun of the compensated first-negative ADC stream,
with internal amplifier A/T/X/Z nodes and device VDS/VDSAT/VGS/ID for both input
pairs and output/load FETs. The electrical deck is unchanged; independent removal
of added save/wrdata statements exactly recovers the preserved baseline.
Original48-column waveform remains separate from40 added probe columns.
Session17825, scratch`transceiver-reference-connected-headroom`.

A3ns elaboration preflight completes; all32 device-parameter vectors are finite
and vary with time, confirming they are saved transient observations rather than
scalar final OP values. This is probe validation, not performance evidence. Full
209.9ns rerun is active. Source/deck/artifact hashes, timeout and exit status are
retained. Before interpreting device headroom, compare original waveform results
against baseline and audit PMOS sign conventions and multiplicity/current scaling.

Next correlate source/drain headroom, output current and internal-node motion with
the worst pre-comparator reference windows. Avoid attributing a reference error
to the node named by a solver or to one isolated voltage. This adds diagnosis,
not a new regulator or performance claim. No other simulation remains active
from prior passes, and no layout has been generated.

## Pass 215 — transistor probe conventions checked against branch currents

Ran a small independent GF180 DC experiment with NFET/PFET multiplicity1 and4,
explicit drain voltage-source currents, and the same device ID/VDS/VGS/VDSAT
probes used by the connected reference diagnostic. Source/deck/artifact hashes
and pre/post model consistency are retained and checked.
[Evidence](evidence/device-probe-contract.json).

At the selected forward-conduction points, reported ID matches the magnitude of
external drain-source current and scales4x with multiplicity4. Thus reported ID
already includes multiplicity; multiplying by the schematic m again would create
a false current estimate. PFET VDS/VGS are reported as positive magnitudes here,
although signed external drain-source/gate-source voltages are negative. External
voltage-source current sign reverses between NFET and PFET, as expected from
current direction. These checks do not characterize reverse-conduction reporting
or every transient operating region; test node relationships before extrapolating.

This avoids two plausible headroom/current-analysis mistakes before selecting a
new reference output stage. Full observation-only stream17825 remains live;
no new performance result or layout is claimed. Next compare its original vectors
against baseline and examine device margins at actual worst decision windows.

## Pass 216 — connected headroom evidence rules out one local explanation

Observation-only reference rerun completes; session17825 is terminal. Independent
checker validates exact electrical-deck preservation, source/deck/artifact hashes,
headers and209.9ns horizon. Every original saved waveform vector reproduces the
baseline with zero measured difference. Added device-vector time grids match.
Output-device VDS reporting agrees with physical node differences under the
previously verified PMOS normalization. [Evidence](evidence/reference-connected-headroom.json).

Across60–209.8ns, probed input/output/load devices all retain positive VDS-VDSAT.
Minimum high/low output-FET margins are0.392/0.711V; input devices have at least
0.665V margin. At actual decision windows, minimum high/low output margins are
0.399/0.737V. This does not support loss of saturation in those probed devices as
the cause of the observed reference error. Tail/mirror devices were not included
in this probe set; no blanket amplifier headroom or stability qualification follows.

High output/load channel-current ranges are5.54–7.45mA /5.34–5.73mA; low ranges
4.97–11.01mA /5.23–5.39mA. These are aggregate channel-model currents, not complete
terminal currents including displacement. Do not subtract extrema taken at
different instants to claim an available peak output current.

Next discriminate closed-loop dynamic drive/recovery from actual ADC load demand,
using time-aligned branch current and compensation-current accounting or a bounded
load-step test at the connected operating point. Increasing size to repair a
presumed saturation failure is not justified by this record. All prior jobs are
terminal; no layout generated and the whole-chip gate remains closed.

## Pass 217 — time-aligned drive estimate; terminal-current instrumentation launched

Added `analyze_reference_dynamic_current.py` using the completed observation-only
trace. Subtracts channel currents at matching times and includes compensation
branch current (X-Z)/500ohm with explicit direction. High-rail estimated delivered
current ranges-0.158..1.930mA; low rail-5.451..0.351mA over60–209.8ns. Compensation
alone ranges-0.146..0.062mA high and-0.076..0.284mA low.
[Evidence](evidence/reference-dynamic-current.json).

These are observed responses, not drive capacity limits. Output-FET displacement/
junction and input-feedback gate currents are omitted, so this estimate does not
establish total amplifier current or ADC demand. Started an explicit0V sensor at
each amplifier output, with the same ADC/reservoirs and zero voltage drop to the
internal feedback point. Full trace and device probes are retained plus signed
sensor currents and both sides of each sensor. Exact substitution reversal is
checked in the runner; original-vector comparison remains mandatory after run.

Session27197, scratch`transceiver-reference-connected-current`,209.9ns requested.
Provenance, timeouts and failed artifacts are preserved. No completion/result yet.
Next compare measured current against the estimate before attributing residual
regulation to drive capacity or choosing a stronger stage. No layout generated.

## Pass 218 — terminal-current checker prepared with invariance safeguards

Added `verification/check_reference_connected_current.py`. Its pending path has
executed and independently verifies the sensor-only deck substitution and current
source hashes. On terminal completion it will check run status, pre/post hashes,
all waveform headers/time grids, zero voltage across both sensors, and every
original vector against the preserved unsensed baseline. It will then compare
measured delivered current with time-aligned channel-plus-compensation estimates,
retaining peak residual and signed residual charge rather than silently treating
the estimate as exact terminal current.

The sensors measure total amplifier delivery into the combined reservoir/ADC
network. They cannot alone distinguish reservoir charge from ADC switch demand,
and their extrema do not establish available drive capacity. The checker records
these limits. Its terminal-data path is not yet exercised; session27197 remains
live. [Pending evidence](evidence/reference-connected-current.json).

After this bounded diagnostic, use a controlled circuit or load-step change that
could reject the chosen driver approach, rather than indefinitely adding probes.
Reference noise/generation/startup and full-chip coexistence remain independent
requirements, as do unresolved autonomous PLL simulation/noise and RF/wired path
integration. No layout generated.

## Pass 219 — signed load pulses expose slow reference recovery

Ran four bounded load-pulse cases on the tuned drivers and existing2048-unit PDK
reservoirs: high/low reference, +/-2mA10ns pulse,100ps ramps,200ns horizon and20ps
maximum step. Unloaded OP initialization; no ADC substitution or cold-start claim.
Independent checker reverses the exact load-source change to the prior AC fixture,
checks artifacts/horizon, and separates static target offset from dynamic error.
[Evidence](evidence/reference-load-step.json). Session88788 is terminal.

High-reference excursion is56.18mV for injected2mA and50.38mV for drawn2mA.
Last violation of a1mV band around its pre-pulse value occurs41.47/41.91ns after
release. Low-reference excursion20.55/22.72mV and recovery13.87/14.77ns. Both
return near pre-pulse values by180–200ns. Pre-pulse offsets remain+13.15mV high
and-10.48mV low; dynamic recovery must not hide those absolute errors.

These recovery times are long relative to5ns ADC bit spacing, supporting a
closed-loop dynamic-regulation problem in this nominal driver/reservoir design.
The pulse is not a bound on actual ADC demand, and1mV is an exploratory recovery
metric rather than an allocated precision requirement. No pole/phase-margin,
noise or PVT qualification is inferred. Next compare a controlled driver change
against these same signed pulses before another full ADC integration.

The sensor-instrumented connected ADC27197 remains active. Its terminal checker
will still determine how well the channel-plus-compensation estimate represents
actual delivered current. No layout generated.

## Pass 220 — measured terminal currents and bounded stronger-output comparison

Connected sensor run27197 completes. Exact sensor/deck/provenance checks pass;
maximum difference among original vectors is79.6u in native vector units, reported
per vector in evidence rather than claiming exact numerical identity. Measured
high/low delivery ranges-0.240..1.910mA and-5.386..0.348mA. Channel-plus-compensation
estimate omits instantaneous contributions up to0.724/0.469mA respectively; it is
not exact terminal-current accounting. [Evidence](evidence/reference-connected-current.json).

Implemented output2 variants doubling only XOUT/XLOAD multiplicities of each
reference amplifier, keeping input stage, compensation and PDK reservoir fixed.
Independent cell reversal confirms those are the only electrical changes. Four
signed2mA pulse cases complete, session13262 terminal, with matched checker and
final source consistency. [Evidence](evidence/reference-output2-step.json).

High-rail excursions improve56.18/50.38mV to32.26/30.81mV; recovery to pre-pulse
1mV band improves41.47/41.91ns to27.77/27.45ns. Low excursions improve20.55/22.72mV
to15.01/15.55mV, recovery13.87/14.77ns to11.63/12.07ns. Static offsets are unchanged.
Peak supply rises to12.02/14.12mA high and~11.94mA low, versus~6.6–8.7mA baseline.
This is a measured speed/power tradeoff, still slow relative to5ns bit spacing;
no full-ADC adoption or regulator qualification is justified.

Next compare energy/recovery and connected decision errors before selecting a
larger output stage; do not continue uniform current scaling without a chip-level
budget. Signal/noise, bias generation, startup, PVT and full coexistence remain
open. All currently launched simulations are terminal. No layout generated.

## Pass 221 — stronger reference stage integrated; static power cost quantified

Added `adc_reference_pair_output2.spice` and started both opposite-history ADC
streams using the doubled-output/load devices, with the same compensation,
reservoirs, input stages, timing and supplies. Independently reversed the first
emitted deck to the compensated baseline. Runner records source/deck/artifact
hashes, failures and final consistency. Session27023, scratch
`transceiver-adc-sar8-reference-output2`; results pending. Shared waveform checker
still needs this explicit variant before interpreting completed outputs.

Audited signed pulse waveforms for unloaded and excess event supply energy.
Unloaded high/low currents grow6.343/6.561mA to11.989/11.897mA. Thus the pair's
unloaded increase is10.982mA, about36.24mW at3.3V. This is a material standing cost,
not merely a higher short peak. [Power evidence](evidence/reference-output2-power-comparison.json).
Event energy is signed ideal-source energy above idle, not dissipated heat or
actual repeated ADC average power.

Do not adopt current scaling solely because the isolated pulse settles faster.
Compare connected reference span, within-window motion, held error and current
in both histories. Whole-chip power/noise/coexistence and missing reference/bias
startup remain unqualified. No layout generated.

## Pass 222 — shared connected-ADC verification extended for output2

Added`--reference-output2` to the existing ADC checker and`--output2` to the
same decision-window analyzer. The new path verifies the immediate compensated
baseline hash, reverses the output-stage pair substitution, then retains the
existing reservoir/driver/capacitor/decision checks. It reports actual pair-source
hash, output-stage multiplier, compensation values and run provenance. Pending
path executes with both histories still incomplete; the previously completed
compensated path was rerun successfully after this extension. Terminal output2
waveform analysis remains unexercised until data arrives.

Promoted the prior pulse power calculation into reproducible
`compare_reference_output_power.py`, adding header/horizon and source-consistency
checks. All eight records reproduce the documented standing-power tradeoff.
This avoids a prose-only power decision or an unreproducible one-off calculation.
Session27023 remains active. No result, candidate adoption or layout is claimed.

## Pass 223 — stronger output stage has a mixed connected result

First output2 history completes. Shared checker exercises the new terminal-case
path, verifies exact substitution/provenance and retains selected ADC logic and
held-sample checks. Codes remain177/78/177; held errors are~4.53/-4.62/4.75mV.
Opposite-history stream and final matrix source consistency remain pending27023.

Extended paired comparison with`--output2`, keeping identical decision windows
and adding maximum in-window span motion to both comparisons. Mean decision span
narrows0.97273–1.04358V to1.00182–1.03507V in this history, but150ps-window motion
worsens5.74->7.16mV. Worst high error decreases30.25->21.33mV while worst low error
increases15.74->20.75mV. Average current grows~13.08–13.20mA to24.06–24.19mA;
peak14.90->26.91mA. [Paired evidence](evidence/reference-output2-connected-comparison.json).

The pulse-test improvement therefore does not translate into uniformly improved
connected accuracy. Codes staying equal do not qualify the analog reference,
and the new waveform still exceeds precision goals. Retain candidate as a
tradeoff experiment, not an adopted reference implementation. Finish the second
history before choosing the next change; avoid simply scaling current again.
No layout generated.

## Pass 224 — architecture-to-evidence consistency audit

Corrected stale block-diagram implementation rows to reflect actual SAR and
registered DAC circuitry, without converting finite nominal tests into qualified
RF functionality. Updated the leading risk priorities to the completed actual-
feedback PLL failure rather than its obsolete pending source-offset experiment.

Explicitly recorded the replication gap: separate opposite-history ADC runs are
not simultaneous I/Q conversion. Shared reference loading needs both converters
with aligned/offset updates and independent data; separate pairs require actual
power/area allocation. The stronger pair's~23.9mA unloaded fixture current would
be~47.8mA/~158mW if duplicated, before conversion load. This is planning arithmetic,
not measured full-chip current or a decision to duplicate. TX I/Q and clock loading
have analogous obligations. Retain the full wired/RF companion scope.

Second output2 history remains active under27023; no further data available at
this checkpoint. Finish that controlled comparison before selecting a reference
candidate or multiplying its topology across the chip. No layout generated.

## Pass 225 — complete stronger-stage tradeoff; candidate remains unqualified

Both output2 histories and final source consistency verified. Session27023 is
terminal; all current runs are finished. Selected codes remain177/78/177 and
78/177/78, held error reaches4.75mV. Mean decision span is1.00036–1.03508V across
both histories. Average reference current~24.06–24.19mA, peak26.91mA, versus the
compensated baseline~13.1–13.2mA. [Complete paired comparison](evidence/reference-output2-connected-comparison.json).

Extended the same reference-motion analysis to the output2 candidate. Its optimal
constant additive correction on this finite record would still leave17.36mV
worst decision-mean span error; maximum150ps-window motion is7.19mV, worse than
the compensated baseline5.79mV. This is neither a simulated trim implementation
nor a full ADC error/noise model. It establishes why narrower average excursion
and unchanged codes alone cannot select the design.
[Three-candidate motion audit](evidence/reference-motion-diagnostic.json).

Disposition: retain output2 for comparisons, not as a qualified reference or a
mandated I/Q implementation. Do not proceed to another uniform current increase
on this evidence. The next circuit choice must improve regulation at the ADC
aperture with an explicit current/area cost and preserve two-history comparisons.
Simultaneous I/Q loading, arbitrary codes, intrinsic noise, startup and shared
supply interaction remain missing. The whole-chip architecture still also lacks
qualified autonomous timing and complete RF/wired paths. No layout generated.

## Pass 226 — combined passive sources complete but expose tiny breakpoint steps

Returned to autonomous timing after the bounded reference series. Tested exact
REF/FB sources together with independent100ohm/1pF loads, no active devices,
Gear2/UIC and800ns horizon. Both pulse/pulse and PWL/pulse cases complete. This
extends the earlier separate-source passive diagnostic and does not reproduce
the active reduced-loop abort. Session56912 is terminal.
[Evidence](evidence/mixed-passive-clock-screen.json).

The mixed case nevertheless has a minimum positive printed time step~1.06e-22s,
eight equal printed-time intervals, including near637.7ns; pulse/pulse has no
duplicate printed times and minimum step2e-14s. Input mismatch remains~2.73pV,
passive output mismatch~1.64pV. The initial checker correctly flagged violation
of its strictly-increasing-time assumption; it now retains nondecreasing raw
records and explicitly reports duplicates rather than treating export rounding
as time reversal or dropping data. No integration is performed over duplicate
samples. Source/deck/artifact hashes and passive topology are independently checked.

This narrows the failure: simultaneous equivalent source representations create
extreme breakpoint steps even in a completing passive fixture, while active
circuit interaction remains necessary to reproduce the tested abort. It is not
proof of a simulator defect, physical instability, or common cause for every
full-loop failure. Next diagnose source breakpoint numerical spacing with a
controlled representational test in the retained reduced active fixture before
another multi-hour full-loop run. No layout generated.

## Pass 227 — exact timestamp representation comparison launched

Retained the active buffered PWL/PULSE failure fixture and rewrote only the
reference PWL timestamps from integer picosecond suffixes to exact decimal
seconds. Generator verifies equality with Decimal; independent checker verifies
every timestamp/value using rational Fractions and reverses the entire deck to
the failed baseline. No intentional phase, slope, amplitude, circuit or simulator
option change is introduced. This differs from prior signed10ps tests, which
changed physical phase.

Session58746, scratch`transceiver-pwl-seconds`,800ns requested. Pre/post source and
deck hashes, timeout/return status and artifacts retained. Checker pending path
passes exact waveform/deck audit; terminal waveform path awaits completion. The
hypothesis is different floating-point breakpoint construction despite identical
rational stimuli, motivated by the completing passive mixed-source run's~1e-22s
steps. Outcome will not establish full-loop lock or imply a proven simulator bug.
No layout generated.

## Pass 228 — exported breakpoint neighborhood audited without extra rounding

Added decimal-export analysis of the completed passive clock fixtures around
637.7ns. Within+/-1fs of the nominal falling endpoint, pulse/pulse has one saved
sample while mixed PWL/PULSE has26, including two equal printed-time intervals
and a smallest positive printed step1e-22s. Exact original text values are retained;
Decimal arithmetic avoids further analysis rounding. Linear falling-edge endpoint
extrapolations are included only for positive source values, with explicit limits
on recovering simulator-internal precision.
[Evidence](evidence/clock-breakpoint-spacing.json).

This characterizes the numerical neighborhood, not a new physical timing failure
or internal event-queue diagnosis. It supports inspecting timestamp construction,
while passive completion continues to limit the inference about the active abort.
Active exact-seconds test58746 remains live, observed beyond365ns at this checkpoint;
its original800ns horizon and failed baseline are preserved. No restart or new
full-loop run, and no layout.

## Pass 229 — notation test fails identically; active circuit reduced further

Exact-decimal-seconds reference test58746 is terminal with timestep collapse at
637.7ns. Independent checker verifies rational waveform equivalence, final hashes
and partial data. Exported arrays are exactly identical to the original buffered
picosecond-notation failing case, not merely similar control extrema. Reject
this notation rewrite as a remedy. [Evidence](evidence/pwl-seconds-screen.json).

Started a further reduction retaining actual reference buffer, PFD, reset,
original mixed clock sources and50fF UP/DN loads, removing pump/filter, associated
bias sources/control initialization and their loading. The exact six removed
lines are recorded and independently checked against the failing fixture. This
changes active loading deliberately and cannot qualify a real loop.
Session85334, scratch`transceiver-pfd-only-breakpoint`,800ns requested.

If it still fails, PFD/buffer interaction is sufficient in this fixture; if it
completes, investigate pump/loading/filter contributions rather than declaring
the PLL repaired. Source/deck/artifact and final consistency checks are preserved.
No long-loop rerun or layout generated.

## Pass 230 — reduced active-fixture audit prepared

Added `verification/check_pfd_only_breakpoint.py`. Independently enumerates the
six permitted removed lines instead of trusting an arbitrary removal manifest,
checks exact retained circuit/options/horizon and source/deck hashes. On terminal
completion it will verify final hashes and raw data, report the actual horizon,
failure time, repeated printed timestamps, and UP/DN observations in a fixed
window. Those observations are not labeled a PFD functional pass.

Pending path executes; session85334 remains live. Terminal waveform analysis is
not yet exercised. Removing the pump also removes gate loading, so a completing
result cannot distinguish gate loading from pump/filter dynamics without another
controlled comparison. Preserve the original failure and all prior source tests.

Updated the reusable workflow to distinguish time reversal from equal printed
timestamps caused by closely spaced exported samples. No silent retiming or
sample deletion is permitted as an analysis shortcut; interpolation/derivative
rules must remain explicit. No circuit qualification or new layout in this pass.

## Pass 231 — PFD alone reproduces failure; buffer-only reduction launched

PFD-only fixture85334 is terminal: it aborts637.7ns despite removal of the pump,
filter, control initialization and their gate loading. Exact reduction and final
provenance are independently verified. Thus those removed elements are not
necessary for this reduced failure. This does not establish cause of the separate
full-loop2.7625us abort. [Evidence](evidence/pfd-only-breakpoint-screen.json).

Started another declared reduction: remove PFD and its UP/DN capacitors, retain
four-FET reference buffer and exact mixed clock/reset sources, add50fF loads to
buffer output and forced feedback. No state machine remains. Session96559,
scratch`transceiver-buffer-only-breakpoint`,800ns requested. Independent checker
verifies the exact three removed lines and two added loads; pending path executes.
Terminal outcome/provenance await the run. No long-loop rerun or layout generated.


## Pass 232 — buffer-only failure verified; isolated source-event test

The buffer-only run is terminal, exit1, with a timestep abort at791.2ns of
800ns requested. Independent checking verifies the declared reduction, hashes,
actual horizon and raw waveform ordering. It has four equal printed-time
intervals. The changed failure time prevents attributing all earlier aborts to
one cause. No PFD or pump is necessary for this particular numerical failure.
[Evidence](evidence/buffer-only-breakpoint-screen.json).

Started a controlled comparison replacing only the electrically separate
feedback PULSE source by zero DC. Buffer, reference PWL, reset, capacitor loads,
models, options, output vectors and horizon remain identical. This tests whether
an unused source's events contribute to the numerical difficulty; it is not an
allowable replacement for feedback in the real PLL.
Reproduce with `bash verification/run_buffer_single_source.sh` from this project;
check with `python3 verification/check_buffer_single_source.py`.
Scratch: `scratch/transceiver-buffer-single-source`; live session61988 at launch.
The independent checker verifies exact electrical change and unchanged controls.
[Evidence record](evidence/buffer-single-source-screen.json).

Reviewed whole-chip priorities: clock quality, conversion errors, missing TX and
wired recovery paths, actual bias and shared-I/Q/coexistence remain open. Further
numerical reductions must earn their time by changing the next design decision.
No new layout or qualification claim.


### Pass 232 terminal result

Session61988 completed, exit0, actual horizon800ns. The independent checker
passes terminal artifact/source consistency and exact-change checks. Holding the
separate feedback source at DC permits completion with the buffer and its own
PWL stimulus unchanged. This supports source-event interaction as a contributor
to the reduced failure; internal simulator cause and relevance to the autonomous
full-loop abort remain unresolved. The fixture contains no active feedback or
physical coupling between that source and the buffer except ideal ground.
Both clock diagnostic sessions are now terminal; do not restart them as pending.


## Pass 233 — first connected DAC/TX commutator interface

Reassessed missing full paths after the source-event diagnostic. Added a
32-FET differential passive TX commutator by connecting two existing PDK receive
switch banks with opposite LO polarity. This is a candidate, not an adopted
radio architecture. Connected it to the actual static4+4 segmented DAC and its
100ohm/2.15V terminations; added RF loads of50/200ohm per leg to ideal1.89V.
All256 codes in both static LO states complete (four cases, about1.1–1.2s each).
Independent checker verifies exact added interface, unchanged baseline,
pre/post source hashes, artifact hashes, complete code sweep and finite vectors.

Output polarity reverses correctly and all four transfers remain monotonic.
Endpoint RF differential magnitude is84.56mV at50ohm per leg and226.11mV
at200ohm, versus DAC endpoint magnitude353.97/409.62mV under those loads.
Static bridge voltage transfer is therefore0.239/0.552, already a substantial
loss before RF parasitics. Endpoint-fit deviations reach1.185/2.968mV.
These are DC interface measurements, not conversion gain, RF output power,
linearity qualification or ENOB. Bias, LO, load common mode and resistors remain
ideal; no reconstruction, quadrature, RF output driver or noise is included.
Load scenarios are not verified package bounds.

Next discriminating test: observe conducting switch headroom/conductance across
code and common mode, then compare a justified switch variant before spending
on full RF transients. Preserve the baseline and measure LO capacitance/current
cost when changing switch geometry. Update the whole-chip inventory without
opening its layout gate.

Reproduce: `bash verification/run_tx_dac_commutator_dc.sh` then
`python3 verification/check_tx_dac_commutator_dc.py` from this project.
[Evidence](evidence/tx-dac-commutator-dc.json).


## Pass 234 — TX common-mode sensitivity before geometry changes

Previous pass was progress: connected TX interface evidence exposed static loss.
Tested unchanged DAC/commutator geometry with both ideal termination biases
shifted together by-0.3/0/+0.3V.50ohm per RF leg, static LO high, all256 codes.
All three sweeps complete; independently checked exact substitutions, full sweep,
source/artifact hashes and bit-identical baseline reproduction.

Lower/baseline/upper RF endpoint magnitude:102.40/84.56/46.69mV.
RF-to-loaded-DAC endpoint voltage ratio:0.3258/0.2389/0.1077.
Differential equivalent series resistance, defined as50*(Vbb/Vrf-1), spans
97–103/145–159/380–414ohm respectively across codes. This describes the whole
static bridge and is not an individual FET channel resistance measurement.
Transfers remain monotonic; endpoint-fit error is0.743/1.185/1.045mV.
Gate voltage minus the higher conducting-path terminal falls markedly in the
upper scenario; this is not threshold-corrected overdrive.

The result establishes strong common-mode sensitivity, not isolated body-effect
causation. DAC compliance changes too. Lower bias is not selected merely because
this diagnostic improves: current-source compliance, noise and dynamic behavior
must survive. Next test should observe actual FET operating points or compare a
complementary switch at fixed biases, carrying LO-drive cost into the decision.
RF transient conversion, reconstruction, autonomous quadrature and output drive
remain missing; no layout started.

Reproduce from project: `bash verification/run_tx_common_mode_dc.sh` then
`python3 verification/check_tx_common_mode_dc.py`.
[Evidence](evidence/tx-common-mode-dc.json).


## Pass 235 — observation-only TX device operating points

Added probes for all19 DAC current-source tails and four representative conducting
mixer fingers (both terminal orientations). Replayed the lower/baseline/upper
common-mode sweeps without electrical changes. All256 codes complete in each;
original output vectors reproduce bit-for-bit. Independent checker verifies the
exact save/wrdata-only edits, source/deck/artifact hashes and full code coverage.

Minimum tail VDS-VDSAT is1.260V in the lower scenario and1.798V in the upper;
all19 tails retain positive margins in all three cases. Summed tail current is
5.192–5.213mA lower and5.267–5.289mA upper. These observations argue against
DAC tail saturation collapse as the main reason for the large transfer loss.
They do not qualify current accuracy or dynamic compliance.

Mixer fingers reverse conduction over the sweep. Raw model VGS-VTH can become
negative for one orientation while its reversed companion reports a positive
value. Do not diagnose cutoff by applying a forward-only interpretation to that
quantity. Preserve signed VDS and raw device outputs. Probe coverage is not a
complete off-bank leakage or switch-region audit.

Next useful candidate comparison is a complementary transmission-gate bridge
at the existing fixed biases, with static transfer checked first and LO drive
charge/capacitance checked before adoption. No switch sizing or bias candidate
is selected by these diagnostic results alone. RF dynamic conversion, quadrature,
reconstruction and output drive remain unqualified. No new layout.

Reproduce from project: `bash verification/run_tx_device_op_dc.sh` then
`python3 verification/check_tx_device_op_dc.py`.
[Evidence](evidence/tx-device-op-dc.json).


## Pass 236 — complementary TX bridge candidate, fixed-bias comparison

Added32 actual PDK PMOS fingers (4um/.28um each, bodies at3.3V) in parallel
with the unchanged32 NMOS fingers. Gates use opposite LO rails. No boosting,
ideal signal switch or bias change. All256 codes complete for two LO states
and50/200ohm per-leg loads. Independent checker verifies transistor connectivity,
exact fixture reversal, hashes, complete sweeps and numerical observations.

50ohm: endpoint output84.56→102.22mV; static bridge transfer0.2389→0.3206;
maximum endpoint-fit error1.185→0.564mV.
200ohm: endpoint output226.11→257.24mV; transfer0.5520→0.6527;
endpoint-fit error2.968→1.148mV. Both LO polarities retain monotonicity.
These are matched DC measurements, not RF gain, modulation quality or ENOB.

Candidate retained for further tests, not adopted: adds128um PMOS gate width,
whose LO charge/current and RF parasitics have not been measured. Next compare
NMOS/TG with identical timed LO stimuli and observe gate-source current and
output behavior; ideal drive tests must be followed by an actual driver load
check. Static improvement cannot close the autonomous timing or TX quality gate.
Whole-chip scope remains unchanged; no new layout.

Reproduce from project: `bash verification/run_tx_tg_dc.sh` then
`python3 verification/check_tx_tg_dc.py`.
[Evidence](evidence/tx-tg-dc.json).


## Pass 237 — connected TX bridge switching and ideal LO burden

Compared NMOS and complementary bridges with the actual static segmented DAC,
fixed codes128/255,50ohm RF loads and1pF per RF output. Ideal complementary LO
is2.5GHz with20ps edges. All four OP-initialized10ns transients complete at2ps
maximum timestep. Checker verifies declared electrical substitutions, hashes,
full horizon, named vectors and strictly increasing time. Metrics integrate an
explicit6–10ns window (ten cycles), interpolating only window boundaries.

At code255, RF differential fundamental peak rises89.41→107.44mV for NMOS→TG;
peak-to-peak rises164.17→196.30mV. Peak absolute current in either ideal LO source
rises15.69→24.39mA. Sum of absolute LO charge per cycle is recorded separately
from signed source energy; signed energy return is not driver dissipation.
Code128 is near midscale, not exact zero differential input for this255-unit DAC.
Neither its residual carrier nor fullscale output is a modulation/EVM measurement.

The static benefit survives this single nominal dynamic fixture, but the added
LO drive burden requires an actual driver comparison before selecting TG. No
autonomous LO, RF reconstruction, quadrature, modulated spectrum, mismatch,
noise or package coverage is claimed.1pF is a scenario, not a validated bound.
Timestep sensitivity remains to be checked before precise RF quality claims.

Reproduce from project: `bash verification/run_tx_lo_switching.sh` then
`python3 verification/check_tx_lo_switching.py`.
[Evidence](evidence/tx-lo-switching.json).

Whole-chip review: completing ideal-LO TX switching does not repair autonomous
PLL completion/noise, reference regulation, simultaneous I/Q load, wired CDR or
host timing. Next bounded TX step is actual LO driver loading; then reconsider
those independent top risks rather than indefinitely optimizing this bridge.


## Pass 238 — transistor LO buffers driving connected TX bridges

Inserted two existing S1 two-stage PDK CMOS LO buffers between ideal clocks and
bridge gates. Added separate ideal3.3V buffer supply and current observation.
Original DAC, code128/255, bridge variants,50ohm/1pF RF loads, stimulus and10ns
horizon remain unchanged. All four runs complete. Independent checker verifies
exact insertion/reversal, source/deck/artifact hashes, columns, finite waveforms,
strict time ordering and actual horizon; metrics use6–10ns, ten LO cycles.

At code255, NMOS/TG RF fundamental peak is82.85/95.85mV (ideal-drive baseline
89.41/107.44mV). Buffer pair supply power averages10.27/13.02mW and peak supply
current6.11/6.76mA. Upstream ideal source peak current is about2.82mA; it must
still be supplied by a real clock source. Driver supply power is a separate
physical-circuit supply measurement, not the earlier ideal-LO signed energy.

LO extrema are approximately-0.074/3.378V for NMOS and-0.048/3.335V for TG.
Peak absolute deviation of LO+LOB from3.3V is0.718/0.145V. This is an observed
waveform asymmetry measure, not jitter or phase error. Small overshoots and
complementarity need timestep/load and eventual terminal-stress checks; nominal
supply rails alone do not bound internal voltages.

TG's benefit persists but it remains unselected pending broader dynamic evidence.
No autonomous oscillator, quadrature, reconstruction, modulation/noise or shared
supply/package qualification. Review whole-chip priorities now: further bridge
sizing is lower value than validating the clock source/driver boundary and
simultaneous converter/reference behavior. Layout remains deferred.

Reproduce from project: `bash verification/run_tx_buffered_lo.sh` then
`python3 verification/check_tx_buffered_lo.py`.
[Evidence](evidence/tx-buffered-lo.json).


## Pass 239 — finite source-resistance scenarios at the TX LO input

Previous pass was progress: actual LO buffer integration exposed power and
waveform costs. Added50/500ohm per complementary source ahead of those buffers,
keeping full-rail ideal waveforms and all downstream circuits unchanged.
Both bridge candidates at code255 complete10ns. Checker verifies exact series
resistor insertion, baseline reversal, hashes, horizon and vectors; analysis
retains the6–10ns window. Initial checker invocation found no terminal result;
that was a running job, not a failed simulation. Polled the same session22639
until terminal; all four simulations return0. Checker now reports pending
explicitly when a terminal result is absent.

NMOS RF fundamental is82.842/82.938mV for50/500ohm, versus82.847mV at zero
source resistance. TG is95.845/95.837mV, versus95.855mV. Buffer power remains
about10.25–10.27mW NMOS and12.99–13.02mW TG. Input extrema at500ohm remain
near0.02/3.29V. LO complement-sum error increases in the500ohm cases even though
fundamental amplitude barely changes; amplitude alone is not a timing metric.

Conclusion is limited: selected series resistance alone does not collapse this
full-swing ideal-source interface.50/500ohm are scenarios, not measured oscillator
impedance or worst-case bounds. Reduced source swing, common mode, source loading,
intrinsic noise and real clock generation still require actual-circuit evidence.
Do not extend this into a larger resistance sweep simply to accumulate passing
cases. Next clock-interface work should use actual oscillator/restorer waveforms
and circuits; revisit simultaneous I/Q reference loading independently.
No new layout or full-chip readiness claim.

Reproduce from project: `bash verification/run_tx_source_resistance.sh` then
`python3 verification/check_tx_source_resistance.py`.
[Evidence](evidence/tx-source-resistance.json).


## Pass 240 — actual ring/TX integration completes but signal collapses

Removed both ideal LO sources and connected the actual split-control ring through
the existing RX-style200fF AC coupling and100kohm feedback-biased CMOS buffers.
Fixed ideal CTRL/REGEN1.08V, ring3.3V,25fF output loads; seeded ring initial nodes.
Extended to40ns and used UIC. Both bridge cases complete, with provenance and
fixture checks. This changes initialization as well as clock source and is not
a controlled attribution of a failure to oscillator loading alone.

Late30–40ns ring/output crossings show~2.5004GHz. LO swing is0.135–3.067V NMOS
and0.225–2.969V TG. However RF differential peak-to-peak is only0.187mV NMOS
and2.085mV TG, versus substantial prior ideal-clock output. Exploratory waveform
inspection finds both DAC outputs near equal mean levels (~2.132V NMOS,
~2.112V TG), not the expected code255 differential state. Frequency continuity
therefore does not qualify a working TX conversion chain. Preserve this failure.

Next discriminating test: replay the ideal-clock baseline with the same UIC and
40ns horizon, and probe actual DAC code/bias/current/decoder state. This isolates
initialization from actual-clock integration before modifying ring or mixer.
Avoid concluding that ring timing caused the signal collapse from this fixture.
Ring remains seeded/free-running: no cold startup, PLL lock, intrinsic noise or
quadrature claim. No new layout.

Reproduce from project: `bash verification/run_tx_ring_driven.sh` then
`python3 verification/check_tx_ring_driven.py`.
[Evidence](evidence/tx-ring-driven.json).


## Pass 241 — UIC replay reproduces TX collapse; bias still charging

Ran the prior ideal-clock/NMOS/code255 circuit with40ns UIC, plus an
observation-only replay of the ring-driven fixture. Added commanded code, BN,
all eight input bits, representative physical low/thermometer controls and tail
nodes. Both runs complete. Independent checker validates exact declared edits,
source/deck/artifact hashes and full horizon. Ring replay reproduces every
original vector bit-for-bit.

Ideal-clock UIC gives only2.559mV RF differential peak-to-peak; ring replay gives
0.187mV. Thus a ring oscillator is unnecessary to reproduce signal collapse.
Command CODE remains255 and probed input/control logic is consistent with high
code, while BN is still rising through0.316–0.365V ideal-clock and0.319–0.368V
ring during30–40ns. DAC differential means remain near zero. This supports an
unsettled bias-state explanation; do not claim a faulty decoder or intrinsic
oscillator failure from the earlier transient.

Next isolate the actual diode/current-source bias driving all255 units' gate
load, establish settling time/current, and extend connected startup accordingly.
Do not merely precharge BN and call cold startup solved. An OP/prebiased run may
separately test steady operation with its assumption clearly labeled.
No physical cold-start, noise, RF modulation or full-chip readiness claim.

Reproduce from project: `bash verification/run_tx_startup_state.sh` then
`python3 verification/check_tx_startup_state.py`.
[Evidence](evidence/tx-startup-state.json).


## Pass 242 — establish loaded DAC bias startup timescale

Kept the actual fullscale DAC, buffers and NMOS bridge; held LO inputs static
high/low to remove GHz switching cost. Compared operating-point initialization
against UIC for1us,20ps maximum timestep. No device resizing, bias precharge or
ideal gate-load replacement. Ideal20uA reference and instantaneous supplies remain.
Checker verifies exact declared changes, provenance, named vectors, actual horizon
and settling against the matched OP reference.1mV is explicitly a diagnostic
band, not an allocated acceptance requirement.

OP run completes with BN0.823483V and DAC differential-0.353971V, far from the
~0.35V BN in40ns startup runs. UIC replay launched as session44009; its result
must be checked before setting a connected startup duration. A static-LO settling
result does not establish RF-switching startup or an actual reference generator.

Reproduce from project: `bash verification/run_tx_bias_settling.sh` then
`python3 verification/check_tx_bias_settling.py`.


### Pass 242 terminal result

Both runs complete1us; session44009 is terminal. UIC bias reaches and remains
within1mV of OP after324.875ns; DAC differential after375.555ns, through the
observed1us horizon. Final differences are below1nV. Thus40ns was far too short
for this static loaded bias startup. Next connected RF run should allow at least
~0.5us and verify bias explicitly; that duration is a test choice, not guaranteed
settling under periodic switching or variation. No precharge workaround selected.
[Evidence](evidence/tx-bias-settling.json).


## Pass 243 — longer connected ring/TX startup, exact horizon-only change

Launched600ns replay of the observed NMOS ring-driven TX circuit. Only transient
horizon40→600ns and output filename change;2ps maximum timestep, all devices,
loads, UIC/ring seeds and observations unchanged. No BN precharge introduced.
Session67210 is live at launch; scratch`transceiver-tx-ring-settling`.
Timeout is1800s to accommodate300,000 requested maximum-size steps plus adaptive
steps. Poll this exact handle; do not restart based on an observation timeout.

Added independent checker with exact baseline reversal, hashes, full-horizon
and partial-failure reporting. It will measure30–40,390–400,490–500 and590–600ns
windows only when available, retaining bias range/mean, DAC differential state,
RF excursion, ring frequency and actual ring/buffer power. Pending checker path
passes; terminal path awaits simulation. No recovered TX signal is claimed yet.

Prior result remains static-LO settling, not a guarantee during periodic RF
switching. Inventory records that distinction. Full-chip schematic readiness
remains false, with no new layout.

Reproduce from project: `bash verification/run_tx_ring_settling.sh` then
`python3 verification/check_tx_ring_settling.py`.
[Evidence record](evidence/tx-ring-settling.json).


## Pass 244 — carrier measurement prepared while connected startup runs

Verified session67210 remains live; no restart. The pending600ns result is not
promoted to a TX success. Updated leading risk summary to reflect actual TX
integration and the known bias-settling confound instead of stale pass232 status.

Peak-to-peak output alone could count switching spikes as useful signal. Added
`verification/tx_cycle_metrics.py`: projects RF differential output onto sine/cosine
over each complete measured ring cycle, subtracting that cycle's time-weighted
DC and interpolating exact boundaries. Strict time ordering; no retiming or
sample deletion. Reports complete-cycle count and fundamental magnitude range
in the existing observation windows. It does not measure phase noise, modulation
quality or total distortion. Analytic known-amplitude/phase sinusoid on nonuniform
samples and a DC-only case pass explicit checks. Actual terminal analysis awaits
the simulation; pending checker executes successfully.


## Pass 245 — close a TX power-accounting omission while startup runs

Verified session67210 live, log progressing beyond248ns; no restart or terminal
claim. Reviewed measurement scope: ring/buffer power alone omits termination and
RF load-bias delivery, important to the whole-chip budget. Extended late-window
analysis to report signed power from saved PLL/buffer/logic/reference supplies
plus currents reconstructed from verified100ohm/2.15V and50ohm/1.89V load
connections. Separately report the four resistors' dissipation without adding
it twice to source power. CTRL/REGEN source currents are unsaved and explicitly
excluded; this is not a complete chip power estimate.

Validated reconstruction sign/scale against independently saved VTERM/VCM branch
currents for both static LO states across256 codes. Maximum discrepancy below
2.4e-17A, exact artifact hashes checked. Shared formula is used by the connected
run checker. No new simulation needed for this measurement contract.
`python3 verification/check_tx_load_power.py` reproduces the check.
[Evidence](evidence/tx-load-power-contract.json).

The connected transient remains pending. Static contract checks do not establish
settled RF operation, adequate signal quality or physical power delivery.


## Pass 246 — TX ideal-bias load requirements made explicit

Session67210 remains live, progressing past454ns; no restart or terminal claim.
Used completed DC artifacts to extract signed source current across all256 codes,
both LO states,50/200ohm loads and both bridge variants. Hashes and complete sweep
coverage checked.2.15V termination sources about5.039–5.241mA.1.89V shared RF
bias supplies positive net current in these cases (~12–192uA), but individual
branches can return current: NMOS50ohm branches span about-0.750 to+0.942mA.
Current exchange through one shared node is not equivalent to two independently
lightly loaded references. No claim of required net DC sinking in the selected
cases; transient source/sink demand remains unmeasured.

These measurements specify a missing circuit obligation: actual TX termination
and common-mode generation with dynamic impedance/current tolerance. Do not
silently reuse the already stressed ADC reference or omit its power. Ideal-bias
TX success would still leave this implementation gap. Static selected cases
are not worst-case process/package bounds.
`python3 verification/analyze_tx_bias_loads.py` reproduces the analysis.
[Evidence](evidence/tx-bias-load-requirements.json).


## Pass 247 — settled connected ring/TX output recovered

Session67210 is terminal, return0; complete600ns waveform passes exact-change,
provenance, named-vector, finite/time-order and horizon checks. Late590–600ns BN
mean0.823481V with0.404mV peak-to-peak ripple; DAC differential mean-0.456028V.
Measured ring frequency2.500405GHz. Complete-cycle RF fundamental is45.299mV
peak and RF differential excursion98.656mV peak-to-peak.390–400ns amplitude
was45.095–45.134mV;490–500ns45.281–45.285mV. Thus the prior near-zero signal
was an unsettled-bias result in this fixture, not evidence that ring-driven TX
is impossible. Residual settling remains visible across windows.

Accounted source power52.824mW includes ring27.749mW, buffers13.858mW,
termination10.554mW, RF bias0.597mW and reference feed0.066mW. Signed logic
source contribution is negligible/slightly negative over the selected window.
CTRL/REGEN current, real reference generation and broader chip functions remain
excluded. Four load resistors dissipate2.259mW, already included in energy supplied;
do not add it again. This is one branch and a seeded open-loop oscillator, not
qualified whole-chip power or simultaneous I/Q operation.

Independent matched measurement method shows ideal-upstream buffered TX carrier
82.83–82.87mV versus actual-ring45.299mV. Actual buffer input only spans1.019–
2.062V; restored LO0.135–3.060V versus ideal-upstream output-0.074–3.378V.
Clock delivery remains a meaningful signal-quality penalty. Initialization and
source differ, so this comparison alone does not isolate amplitude/duty/edge
shape as the cause. No PLL lock, intrinsic noise, modulation/EVM or PVT claim.

Whole-chip reassessment: next bounded clock-interface experiment should reproduce
the actual source swing/common mode before changing buffer geometry; no more
blind full-rail source-resistance sweeps. Independent missing references,
simultaneous I/Q conversion, RF reconstruction and closed wired recovery remain
high risks. Static-code carrier recovery does not make a Wi-Fi transmitter.
No new layout; full schematic gate remains closed.

[Startup evidence](evidence/tx-ring-settling.json),
[clock delivery comparison](evidence/tx-clock-delivery-comparison.json).
Reproduce comparison: `python3 verification/compare_tx_clock_delivery.py`.


## Pass 248 — measured input-waveform replay localizes clock-delivery loss

Extracted exact590–600ns LOIN/LOBIN traces from the completed actual-ring run,
replayed as PWL sources into unchanged OP-initialized NMOS TX/buffers. No loop,
bias, gate geometry or load optimization. Independent checker validates measured
waveform hash, every PWL timestamp/value, full deck reversal, source/artifact
hashes and completed10ns horizon. Late6–10ns complete-cycle RF fundamental is
44.631–44.682mV versus45.299mV in the connected ring and82.83–82.87mV with
full-swing ideal inputs. The replay reproduces most of the signal reduction.

This supports delivered input waveform as the main loss mechanism in the chosen
comparison. It does not isolate amplitude, common mode, duty or edge shape.
~1.5percent amplitude discrepancy remains, and stiff replay removes bidirectional
loading while OP initialization changes downstream initial state. Do not claim
exact circuit equivalence or autonomous qualification. Source remains seeded,
noise/modulation/reconstruction and actual reference drivers remain missing.

Next bounded experiment can independently move the measured input common mode
or scale differential swing while preserving its shape, before resizing a real
restorer. Any chosen fix must then return to the connected oscillator and retain
its input load/power cost. No new layout or whole-chip readiness claim.

Reproduce: `bash verification/run_tx_measured_lo_replay.sh` then
`python3 verification/check_tx_measured_lo_replay.py` from project.
[Evidence](evidence/tx-measured-lo-replay.json).


## Pass 249 — restorer common-mode sensitivity is a hard failure mode

Four independent measured-input transformations complete: common-mode-150/+150mV
with differential waveform unchanged; differential swing0.75/1.25 with instantaneous
common-mode waveform unchanged. Independent checker validates every transformed
PWL sample/time, exact unchanged remainder, hashes and10ns horizon. No physical
circuit modification selected; ideal sources still isolate feedback/loading.

Common-mode-150mV leaves LO between-0.021 and0.161V; +150mV leaves LO between
3.081 and3.328V. Neither has1.65V rising crossings in6–10ns. Carrier amplitude
using delivered-LO cycle reference is undefined, reported null, not zero RF.
Swing0.75 gives33.834–33.864mV carrier peak;1.25 gives53.924–53.992mV compared
baseline44.631–44.682mV. Scaling swing also changes slew, so this is not an
amplitude-only experiment.

The fixed-input restorer is sharply sensitive to common mode. Its actual100kohm
feedback bias may regulate that mode, but this replay removes that feedback and
does not establish a real process-offset bound. Next investigate restoring-stage
DC threshold/self-bias and its closed bias loop, including mismatch and loading,
before a gain-oriented resize. A successful nominal connected run cannot justify
assuming robust clock restoration. Other whole-chip risks remain unchanged.

Reproduce: `bash verification/run_tx_replay_sensitivity.sh` then
`python3 verification/check_tx_replay_sensitivity.py` from project.
[Evidence](evidence/tx-replay-sensitivity.json).


## Pass 250 — actual restorer feedback regulates static input bias

Tested the existing S1 two-stage PDK buffer with its200fF AC coupling and100kohm
IN-to-first-stage-output feedback. Injected-3 to+3uA into IN across61 DC points;
no ideal voltage source clamps IN. Fixed3.3V/27C/typical, diagnostic50fF output
load. Checker verifies exact electrical topology, source/deck/artifact hashes,
complete finite sweep and feedback KCL (residual<1e-20A).

Zero-injection bias1.530318V; input shift-17.299/+16.873mV at-3/+3uA. The
first-stage output moves1.247–1.813V to provide feedback current. Whole two-stage
static supply current1.854–2.539mA. These DC points demonstrate negative-feedback
bias regulation, not RF gain, dynamic stability or successful startup.

This qualifies the interpretation of pass249: forced-input+/-150mV tests bypass
self-bias and therefore cannot be treated as predicted process susceptibility.
The real network suppresses a selected injected DC disturbance, while dynamic
charge pumping, mismatch, threshold tracking and source loading remain untested.
DC source common mode is blocked by the coupling capacitor; frequency-dependent
loading and startup must be evaluated with the real loop before selecting changes.
Next useful test is a connected bias-current disturbance under RF switching,
rather than further forced-input offsets. No layout or whole-chip readiness claim.

Reproduce from project: `bash verification/run_restorer_bias_dc.sh` then
`python3 verification/check_restorer_bias_dc.py`.
[Evidence](evidence/restorer-bias-dc.json).


## Pass 251 — dynamic self-bias regulates DC shift but RF transfer remains fragile

Replayed measured ring CP/CN outputs upstream of actual200fF coupling capacitors
and100kohm input-to-first-stage-output feedback; retained actual DAC/buffers/NMOS
bridge. Equal input-current injections0/-3/+3uA ramp at2–2.1ns. All three10ns
OP-initialized runs complete. Checker verifies every recorded-source PWL point,
exact electrical substitutions, current directions, hashes and complete horizon.

6–10ns input common-mode means:1.53443V zero,1.51116V minus,1.55886V plus.
Complete-cycle RF fundamental ranges:46.25–47.95mV zero,0.128–7.529mV minus,
76.36–82.01mV plus. Each has nine complete delivered-LO cycles; surviving midrail
crossings do not mean adequate RF transfer. Strong within-window amplitude motion
means no settled gain or tolerance boundary can be assigned. The zero case itself
has not reproduced the600ns connected value exactly; short bias transients matter.

The DC feedback result remains valid, but~24mV average shifts can accompany large
RF-transfer changes under these current disturbances. Do not infer robustness
from small bias error alone. Next extend this diagnostic to test recovery/settling
with a contiguous longer recorded waveform (not discontinuously tiled clips),
and inspect first-stage output/duty and physical mixer controls. No favorable
+3uA operating point selected. Injection is a scenario, not a fabrication bound.
Independent clock noise, references/converters, quadrature and wired recovery
remain open; do not let this local series stand in for whole-chip qualification.

Reproduce from project: `bash verification/run_tx_selfbias_dynamic.sh` then
`python3 verification/check_tx_selfbias_dynamic.py`.
[Evidence](evidence/tx-selfbias-dynamic.json).


## Pass 252 — self-bias disturbance/recovery experiment launched

Extended the shared replay runner/checker with explicit`--recovery` mode rather
than a separate copied implementation. Uses contiguous540–600ns recorded CP/CN
waveforms, no tiled seams. Three cases: zero and equal+/-3uA injections ramping
at2–2.1ns, held to20ns, released by20.1ns. Retains actual200fF/100kohm coupling
and feedback and downstream transistor circuits. Adds first-stage MID node
observations.60ns requested at2ps maxstep, OP initialization, ideal supplies.

Session87665 is live; scratch`transceiver-tx-selfbias-recovery`. Zero deck
preflight confirms exact60ns horizon, release timing and first-stage observations.
Shared checker preserves old10ns results and now reports6–10,16–20,26–30 and
56–60ns windows, complete carrier cycles, input mean, first-stage extrema and
buffer power. Existing completed short-run checker still passes with identical
numeric results. Recovery terminal path is pending; no settled/recovered claim.

This test separates persistent RF degradation from temporary bias response;
recorded source still removes bidirectional oscillator loading. Asymmetric
injection, mismatch, real references and noise remain independent open gaps.
Reproduce from project: `bash verification/run_tx_selfbias_recovery.sh` then
`python3 verification/check_tx_selfbias_dynamic.py --recovery`.


## Pass 253 — verified ongoing recovery run; preserve negative outcomes

Polled session87665, which remained live and advanced through the zero case
(return0); disturbed cases remain in progress. No restart. Previous turn was
progress: launched recovery experiment and added its measurement path.

Improved the shared checker to record per-case return code, timeout, actual
horizon and solver failure time. Missing waveforms no longer cause a false
completion assumption. Available complete windows can be retained from a partial
case while the overall diagnostic is explicitly incomplete. Source/deck/artifact
integrity assertions remain strict. This prevents an abort from yielding only
an assertion without a usable evidence report.

Completed short-run artifacts still pass. An isolated temporary-fixture test,
using real short-run artifacts with a synthetic timeout flag, correctly marks
the matrix incomplete while preserving measurements; production artifacts were
unchanged. This is a checker fault-injection test, not a new simulation failure.
Recovery matrix remains pending; no settled disturbance/recovery result claimed.


## Pass 254 — whole-chip scope and evidence boundaries refreshed

Verified session87665 live; negative-disturbance case progressing through~38ns.
No restart or recovery conclusion. Reviewed all ten schematic obligations and
corrected the RF TX row: successful ring-driven carrier uses a fixed-code
combinational DAC, whereas registered updates/glitches are separate experiments.
Replaced accumulated TX inventory prose with a current statement and direct
links to the evidence supporting these distinct claims.

After this bounded recovery check, prioritize simultaneous I/Q/reference loading
rather than indefinite one-tone restorer tuning. Existing regulation failures,
missing independent clock/noise qualification, RF reconstruction/quadrature,
closed wired recovery and host timing remain full-chip risks. No gate narrowed,
no new layout, no streaming RF or protocol success implied by the carrier result.


## Pass 255 — actual simultaneous ADC fixture connected and elaborated

Added a second complete channel to the existing compensated-reference ADC fixture:
29 top-level elements replicate sampler, comparator, SAR state, CDAC, code drivers,
track mask and input amplifier/bias. Internal nodes are distinct; opposite input
history comes from the verified companion baseline. The actual reference pair
and reservoirs appear only once. Supplies and phase clocks remain shared ideal
sources, not a physical distribution model. Baseline first channel unchanged.

First elaboration failed because a naive instance prefix changed SPICE element
types. Preserved that terminal failure in
`scratch/transceiver-adc-shared-iq-preflight-invalid-prefix`; corrected naming to
retain each element's leading type letter. Corrected1ns run completes, code0.
Independent checker reverses every cloned node/element, checks shared-node list,
exact deck changes, baseline/opposite-history hashes, source/artifact integrity,
headers, finite waveforms and actual horizon. No conversion occurs in1ns; this
proves elaboration/connectivity only, not reference regulation or conversion.

Next extend unchanged connected fixture through complete three-frame timing and
measure both channels' decision-window reference state, captured codes and power.
Then test same-direction and other relative switching histories; opposite-input
case alone cannot bound shared reference demand. Recovery session87665 remains
independently live as last polled; no duplicate run launched.

Reproduce from project: `bash verification/run_adc_shared_iq_preflight.sh` then
`python3 verification/check_adc_shared_iq_preflight.py`.
[Evidence](evidence/adc-shared-iq-preflight.json).


## Pass 256 — recovery verified; complete simultaneous ADC conversion launched

Recovery session87665 terminal, all three cases return0. Exported endpoint is
59.99999999999996ns; checker initially marked it short. Added explicit1e-21s
endpoint tolerance solely for export rounding, many orders smaller than2ps step;
raw times are unchanged. All cases now correctly complete. During16–20ns,
negative injection leaves only0.532–0.534mV carrier, positive80.08–80.25mV,
zero46.25–46.33mV. After release20.1ns,56–60ns zero/minus/plus carriers are
45.667–45.696/45.883–45.929/45.488–45.503mV. Selected-case recovery is observed,
not exact settling or tolerance to sustained disturbance. No physical offset
bounds or autonomous-loop stability inferred.
[Recovery evidence](evidence/tx-selfbias-recovery.json).

Launched full shared-I/Q ADC fixture by changing only1ns horizon to209.9ns.
Actual two channels and one compensated reference pair/reservoir unchanged,
opposite input histories and simultaneous ideal phase clocks. Session22510,
scratch`transceiver-adc-shared-iq-frames`;3600s timeout. Poll existing handle.
Added independent checker for exact horizon-only reversal, provenance, actual
completion/partial failure, both-channel captured codes and shared-reference
predecision windows over all three frames. Pending path passes; no full conversion
result claimed yet. Decision sample means intentionally match prior diagnostics.

Reproduce from project: `bash verification/run_adc_shared_iq_frames.sh` then
`python3 verification/check_adc_shared_iq_frames.py`.
[ADC evidence record](evidence/adc-shared-iq-frames.json).


## Pass 257 — measure shared-reference conversion at the relevant analog times

Verified session22510 still live, progressing past126ns; no restart. Reviewed
measurement semantics before terminal scoring. Renamed final-capture top-plate
measurement: track-mask reset occurs earlier, so this value must not be called
held-sample accuracy. Added explicit first predecision window(hold+0.2 to+0.35ns)
plate differential ranges for both channels, separate from digital code/rail
checks and shared reference decision-window measurements. No ideal-code check
is substituted for analog precision.

Added time-weighted power from the saved VLOG/VDRV/VBUF/VREFSUP rails over each
frame. These are aggregate shared-rail measurements, not per-channel power; VDD
comparator/sampler current is unsaved and explicitly excluded. No total ADC/chip
power claim. Checker compiles and pending path executes; terminal analysis still
awaits the connected simulation. No new layout or readiness change.


## Pass 258 — simultaneous I/Q completes, reference sharing worsens accuracy

Session22510 terminal, code0, full209.9ns. Independent checker verifies exact
horizon extension/provenance and all three completed frames. I captures177/78/177;
Q captures78/177/78 with rail-valid stable outputs. This is selected digital
behavior, not conversion accuracy. Both predecision plate signals also retain
input-buffer acquisition errors; no ENOB claim.

Shared decision-window reference span0.95315–1.04948V, versus0.96840–1.04360V
across the two separate single-channel histories. Worst high-rail error rises
31.43→41.50mV; low error15.74→16.98mV;150ps span motion5.789→6.451mV.
Reference supply consumes44.36–44.57mW in the scored dual-channel frame windows,
versus43.17–43.70mW in separate single-channel runs. Additional reference power
is modest but regulation is worse; no qualified power-saving architecture follows.
Matched-window comparison checks raw waveform hashes and completion separately.

This closes a missing connection, not the converter/reference feasibility gate.
Next test same-direction simultaneous histories and phase relationships before
choosing reference changes; opposite inputs need not maximize net reference load.
Preserve actual clocks/supplies/targets and noise/mismatch as open requirements.
No new layout or whole-chip readiness claim.
[Connected evidence](evidence/adc-shared-iq-frames.json),
[baseline comparison](evidence/adc-shared-reference-comparison.json).
Reproduce comparison: `python3 verification/compare_adc_shared_reference.py`.


## Pass 259 — aligned I/Q switching test launched as a two-source change

Extended the shared frames runner/checker with`--same-history`. Baseline is the
completed opposite-history209.9ns fixture. Only Q's two ideal input PWL sources
now match I's inputs; every transistor, reservoir, reference driver, clock,
initialization, observation and timestep remains unchanged. Independent checker
reconstructs exactly these two substitutions and verifies the new deck hash.
Completed opposite-history checker still passes all three frames with the shared
analysis. No sharing architecture is qualified by those stable codes.

Session14434 is live; scratch`transceiver-adc-shared-iq-same`. Pending checker
passes. This comparison tests aligned versus opposite selected histories; neither
is assumed to bound arbitrary signal/phase relationships, mismatch or loading.
Reference span/motion and both digital/analog channel states will be compared,
not just simulator completion. No new layout or readiness change.

Reproduce from project: `bash verification/run_adc_shared_iq_same.sh` then
`python3 verification/check_adc_shared_iq_frames.py --same-history`.
[Evidence record](evidence/adc-shared-iq-same.json).


## Pass 260 — shared-reference error is not a fixed span offset

Verified aligned-history session14434 remains live; no restart and no result
inferred. Extended the existing raw-waveform comparison to compute the minimax
constant additive correction of recorded decision-window span means. For the
completed shared-I/Q opposite-history case, optimal correction is-1.31849mV,
but minimum worst residual remains48.1641mV. Separate single-channel histories
leave35.4286/37.6021mV under their own optimal corrections.

Thus a constant offset of these recorded spans cannot remove the observed dynamic
spread. This is mathematical postprocessing, not a simulated trim circuit or a
bound on changed reference topology, gain calibration or per-code correction.
Physical target/bias changes could alter the dynamics and must be simulated.
The result discourages mistaking dynamic regulation error for a trim-only issue.
Exact prior measurement windows and artifact hashes are retained.
[Updated comparison](evidence/adc-shared-reference-comparison.json).


## Pass 261 — aligned I/Q switching exposes larger shared-reference error

Session14434 terminal, code0, all209.9ns and three frames verified. Only Q input
history changed. Both channels capture177/78/177 with stable rail levels, but
reference decision span widens to0.928369–1.059641V (opposite-history0.953154–
1.049483V). Worst150ps span motion increases6.451→9.691mV. Digital behavior
again hides substantial analog-reference failure. Best constant span correction
still leaves65.636mV worst residual, mathematical diagnostic only.

Extended the shared comparison with explicit`--same-history`, requiring verified
terminal coverage before generating combined results and reporting both channels'
codes. It refused to produce a completed comparison while the audit was pending,
then completed after verification. All raw waveform hashes checked; exactly
matched decision windows used across single/opposite/aligned cases.

Next diagnose actual reference current/headroom and reservoir response around the
worst aligned event. Prior single-channel output-stage scaling traded accuracy,
motion and power; do not assume another sizing increase solves this. Time-skew,
mismatch, real references/targets and distribution remain open. No new layout.
[Combined comparison](evidence/adc-shared-history-comparison.json).
Reproduce: `python3 verification/compare_adc_shared_reference.py --same-history`.


## Pass 262 — aligned I/Q reference device probes launched

Reused the existing observation-only headroom runner with`--shared-iq`. Captures
VDS/VDSAT/VGS/channel current for all seven FETs in each reference amplifier,
including previously unprobed tail and mirror devices, plus eight internal node
voltages. External bias diodes are not included in that device count. Original
aligned-I/Q circuit, clock/source timing, reservoir, compensation and load remain
unchanged; only save/wrdata probes are added.

3ns preflight completes with65 finite output columns and real time-varying data.
Full209.9ns replay is live as session52462, scratch`transceiver-iq-reference-headroom`.
Independent checker validates exact probe set, observation-only deck transform,
provenance, completion and bit-identical original-vector reproduction when data
arrives. Reports signed VDS diagnostics separately to avoid misinterpreting reverse
conduction. Device channel currents are not total delivered reference current;
displacement and compensation currents still matter.

No headroom diagnosis yet. Pending path passes; terminal analysis awaits the run.
No transistor resizing or new layout. Reproduce from project:
`bash verification/run_iq_reference_headroom.sh` then
`python3 verification/check_iq_reference_headroom.py`.
[Evidence record](evidence/iq-reference-headroom.json).


## Pass 263 — correlate reference failure with simultaneous device state

Verified session52462 live, progressing past150ns; no restart. Completed aligned
baseline identifies125.20–125.35ns (middle frame, bit6) as both the largest high
reference error and largest decision-span-motion window. Worst low-reference
error occurs185.20–185.35ns (third frame, bit4). These selected windows are from
verified completed evidence, not a guessed switching edge.

Extended probe checker to report VDS/VDSAT/VGS/channel current for every amplifier
FET at the exact same timestamp as each selected peak reference error. It checks
measurement time-axis equality and baseline waveform hash before correlation.
This avoids subtracting unrelated current extrema or assigning headroom loss at
one instant to reference error at another. Global extrema remain supplementary.
Checker compiles and pending path passes; terminal correlation remains unexecuted
until the run finishes. No causal headroom/current diagnosis claimed yet.

## Pass 264 — completed I/Q probes expose high-reference tail compliance

The209.9ns observation-only replay is terminal and complete, with unchanged
source hashes and bit-identical original waveforms. All14 amplifier devices
were checked. Only the high-reference tail has negative reported VDS−VDSAT:
minimum−158.6mV over60–209.8ns. Extended the checker to inspect every predecision
window: all24 have negative high-tail margins throughout, ranging−158.0 to
−137.9mV. The low-tail margins in those windows remain+80.3 to+104.1mV.

At the worst high-reference error (−60.794mV), high-tail VSD is97.2mV against
235.1mV reported VDSAT; channel current is746.8uA. No negative reported VDS
occurs in the active trace. This reveals a previously unprobed compliance issue,
not proof that it causes the complete dynamic error. Earlier input/output-only
headroom measurements could not exclude it.

Next discriminating candidate is increased high-rail input-pair width, screened
first for DC headroom/current at signed loads, then dynamics if useful. Preserve
the existing candidate as baseline; no resizing adopted in this pass. Full-chip
schematic and autonomous timing remain incomplete, layout gate closed.
Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_iq_reference_headroom.py`.
[Evidence](evidence/iq-reference-headroom.json).


## Pass 265 — input width improves but does not repair reference headroom

Completed27 isolated high-reference DC cases: input-pair width factors1/2/4,
targets2.00/2.15/2.30V, signed output loads−2/0/+2mA, typical27C3.3V.
Only input-pair width changes between candidates; existing tail, output stage,
compensation, reservoir and ideal bias sources remain. The factor1 cell is
text-identical to the existing cell. Source hashes are checked before/after;
checker independently reads waveforms and verifies complete case coverage.

At2.15V and zero load, tail margins are−153.1/−115.3/−76.65mV;
tail current increases656.4/863.3/1015.4uA. DC output errors improve
13.15/10.15/8.02mV, but the worst tail margins across the matrix remain
−199.5/−179.5/−155.9mV. Widening helps the hypothesized mechanism but is
insufficient in these candidates. No candidate adopted and no dynamic benefit
claimed. These loads/target offsets are diagnostic scenarios, not process bounds.

Next discriminate tail-bias overdrive from input common-mode limitations: test
independent tail bias with actual diode-derived bias circuitry and matched tail
current, instead of blindly increasing input capacitance further. If a useful
DC operating point exists, require signed load-step and loop-response checks
before repeating the connected I/Q case. Periodic full-chip risk review remains
due; reference optimization must not obscure unresolved autonomous clock noise
and complete RX/TX conversion. No new layout.

Reproduce from repository root:
`bash projects/programmable_transceiver_platform/verification/run_reference_input_headroom.sh`
then `python3 projects/programmable_transceiver_platform/verification/check_reference_input_headroom.py`.
[Evidence](evidence/reference-input-headroom.json).


## Pass 266 — separate tail bias restores nominal compliance, not DC accuracy

Completed27 DC cases with input width held at4x. Tail and dedicated PMOS diode
multiplicity factors2/4 preserve the nominal64:1 mirror ratio; the factor1
control retains the original bias connection. Dedicated bias still uses an ideal
20uA current source. Targets2.00/2.15/2.30V and loads−2/0/+2mA are diagnostic
scenarios, not fabrication bounds. The checker verifies exact declared deck
transformations against pass265 and bit-identical factor1 baseline waveforms.

At nominal2.15V/no load, tail margins change−76.65/−19.83/+20.96mV for
factors1/2/4; currents are1.015/1.113/1.162mA. Preserving mirror ratio did not
preserve actual current. Output error stays near+8.02mV, so improved tail
compliance does not appreciably repair this DC error. Worst matrix margins
remain−155.9/−100.8/−63.62mV. Factor4 is a diagnostic candidate only: no
stability, dynamic regulation, noise or startup qualification and no adoption.

Next useful local test is a matched signed-load transient/impedance comparison
of this nominally compliant candidate against input-width4 baseline, with actual
tail-bias circuitry retained. Do not claim the small nominal margin tolerates
uncertain loading or process. Before extending the reference work further,
reassess the autonomous-clock and complete RF chain risks. No new layout.

Reproduce from repository root:
`bash projects/programmable_transceiver_platform/verification/run_reference_tail_headroom.sh`
and `python3 projects/programmable_transceiver_platform/verification/check_reference_input_headroom.py --tail`.
[Evidence](evidence/reference-tail-headroom.json).


## Pass 267 — whole-chip review returns attention to autonomous timing

Reviewed the ten-block schematic inventory and corrected stale reference-sharing
and TX self-bias recovery entries against completed evidence. The inventory audit
still reports the whole connected chip missing/incomplete. Autonomous lock/noise,
complete quadrature RF conversion, accurate shared references, wired recovery,
host physical timing, actual bias generation and coexistence remain open. Several
isolated reference improvements cannot establish overall feasibility.

Started a controlled reduced-clock experiment: add50ohm in series with the ideal
reference source feeding the actual four-FET input buffer. Preserve both mixed
PWL/PULSE timed sources, reset, loads, integration method and800ns horizon.
This tests ideal source stiffness as a contributor without removing clock events.
It is not a physical clock implementation or a change to the autonomous loop.
Existing checker extended with exact transformation and provenance checks.

Job82875 was verified live; latest observed progress near663.2ns. Do not restart
while it runs. Scratch: `scratch/transceiver-buffer-finite-source`.
Reproduce from root: `bash projects/programmable_transceiver_platform/verification/run_buffer_finite_source.sh`;
check with `python3 projects/programmable_transceiver_platform/verification/check_buffer_single_source.py --finite-source`.
[Evidence](evidence/buffer-finite-source-screen.json). No outcome claimed while
pending; terminal failure would still be useful diagnostic evidence. No new layout.


## Pass 268 — verified live clock diagnostic, progress reporting strengthened

Polled job82875 and inspected its actual ngspice process: live, not terminal.
The finite50ohm source experiment repeatedly reports rounded time663.2ns;
no completed waveform/result exists yet. Do not interpret repeated rounded
progress as a proven solver deadlock, physical instability or completion.
No restart; retain the original300s runner timeout.

Extended the shared checker to retain last printed simulation time and count
consecutive identical progress strings, explicitly separate from completion and
live-process evidence. Pending path exercised on the real run. Terminal checker
handling remains unchanged; next action is poll82875 and analyze result.json
when terminal. This pass is a verified wait plus a measurement-reporting change,
not a transistor-performance result. Full-chip scope and layout gate unchanged.


## Pass 269 — loaded oscillator supply sensitivity launched during verified wait

Finite-source buffer job82875 subsequently terminated with exit1 and a
reported timestep failure at663.2ns, before800ns; not a timeout. Added50ohm
source resistance does not resolve this reduced numerical failure and will not
be promoted as a full-loop remedy. No restart. Noise-method
review confirms deterministic transient results cannot qualify intrinsic phase
noise. Added an independent three-point supply experiment on the existing
split-bias ring with actual RF and seven-stage divider/PFD loading. Change only
PLLVDD to3.29/3.30/3.31V, holding ideal control/regeneration and other supplies
fixed. Seeded/prebiased41ns runs; frequency measured from interpolated positive
CML crossings over20–40ns. These small signed supply offsets are diagnostic,
not process limits or a completed supply-noise budget.

New runner recursively hashes included sources before/after and preserves
terminal failures. Checker validates exact deck changes, waveform/time/bias,
completion and nominal baseline reproduction; local Hz/V slopes are emitted
only when all cases complete. First3.29V simulation exited0; whole matrix is
still running as job75144. No supply sensitivity result claimed yet.

Reproduce: `bash projects/programmable_transceiver_platform/verification/run_vco_supply.sh`;
check: `python3 projects/programmable_transceiver_platform/verification/check_vco_supply.py`.
Scratch `scratch/transceiver-vco-supply`; [evidence](evidence/vco-supply-screen.json).
Next poll existing supply job75144 and analyze terminal artifacts. Still no random
phase-noise, closed-loop rejection or cold-start qualification; no new layout.


## Pass 270 — loaded ring supply sensitivity measured in both directions

Job75144 completed all three41ns simulations without timeout/abort. Nominal3.3V
waveforms reproduce the prior split-ring baseline bit-for-bit. RF input bias
remains valid. Frequencies measured over20–40ns are2.500103/2.500751/2.501396GHz
at3.29/3.30/3.31V: local sensitivity64.776 and64.535MHz/V. Separate20–30 and
30–40ns windows give slopes64.767/64.536 and64.782/64.534MHz/V respectively.
Thus this result is not merely a frequency average over visibly different early
and late sensitivity, within these selected windows and numerical resolution.

This is deterministic open-loop DC supply sensitivity with loaded RF/divider/PFD
circuitry, seeded/prebiased operation and fixed ideal control/regeneration biases.
It is neither random jitter nor dynamic supply-to-phase transfer, and provides
no closed-loop rejection, process, mismatch or package bound. Approximately
0.65MHz shift per10mV makes supply disturbance a measurable clock-quality risk;
a phase-noise budget still requires a validated noise method and frequency-
dependent response. No intrinsic noise claim follows from clean waveforms.

Next discriminating clock-quality experiment: matched nominal versus sinusoidal
PLLVDD disturbance with explicit ripple amplitude/frequency, enough steady cycles,
and phase measured against the unperturbed run. Preserve clock loading and fixed
biases, and separate startup drift from periodic phase modulation. This should
precede converting the DC sensitivity into a claimed dynamic supply requirement.
Full-loop completion and noise-method work remain independent obligations.

Checker now reports subwindow frequencies/slopes and all completion/provenance
checks passed. [Evidence](evidence/vco-supply-screen.json).
Reproduce analysis: `python3 projects/programmable_transceiver_platform/verification/check_vco_supply.py`.
No live jobs remain from passes267–270; finite-source buffer failed, supply matrix
completed. Whole connected schematic remains incomplete; no new layout.


## Pass 271 — supply ripple-to-edge-displacement test launched

Extended the verified supply runner with explicit ripple mode, preserving its DC
mode and dependency hashing. Matched quiet/ripple cases extend the existing
loaded split-ring fixture to201ns. Ripple is10mV peak at50MHz on PLLVDD only,
starting20ns; fixed control/regeneration, other supplies, seeds, RF/divider/PFD
loading and timestep remain unchanged. This is a deterministic diagnostic,
not random phase noise, a supply-noise bound or closed-loop qualification.

New checker verifies exact deck transformations, source/artifact hashes, terminal
horizon and RF bias. Positive CML crossings are paired by index after verifying
pre-ripple alignment and absence of missing/extra cycle intervals. It reports
raw delay, fitted offset/drift and sinusoidal displacement separately over40–200,
40–120 and120–200ns. Synthetic known3ps sine+4ps cosine with independent7ps
offset and0.03ps/ns drift validates the fit; recovered peak5ps and negligible
residual. No circuit performance result is claimed from that measurement test.

Job22285 verified live, quiet case progressing beyond96ns. Preserve the existing
job; scratch `scratch/transceiver-vco-supply-ripple`. Reproduce from root:
`bash projects/programmable_transceiver_platform/verification/run_vco_supply_ripple.sh`;
check `python3 projects/programmable_transceiver_platform/verification/check_vco_supply_ripple.py`.
[Pending evidence](evidence/vco-supply-ripple.json). Next poll existing job, then
inspect subwindow consistency and drift before interpreting modulation. No new
layout; whole-chip schematic, autonomous PLL and intrinsic noise remain open.


## Pass 272 — edge interpolation contract checked while ripple run continues

Verified job22285 live; quiet201ns case exited0 and ripple case started. No
restart and no physical modulation result yet. Extended the shared crossing
extractor's measurement contract: a synthetic2.50075GHz carrier with known
0.8ps,50MHz sinusoidal time displacement sampled at2ps recovers amplitude
within0.001ps. Offset/drift fitting remains separately tested. A test starting
exactly at zero initially exposed a boundary-crossing count ambiguity; use a
common nonzero initial carrier phase in this synthetic fixture. Actual paired
runs still require matching pre-disturbance crossings and cycle-count checks.

Synthetic interpolation validates the analysis on that waveform, not transistor
numerical accuracy. Actual timestep convergence remains necessary before trusting
small timing modulation. Checker pending path passes; quiet/ripple final analysis
awaits terminal matrix artifacts. Poll existing22285 next. No new layout.


## Pass 273 — completed quiet baseline analyzed during verified ripple wait

Job22285 remains live; ripple progress verified beyond73ns. Extended checker to
consume the runner's hashed completed-case progress record while clearly retaining
matrix completed=false and after-matrix source-hash verification=false. Exact
deck transform, artifact hashes, valid vectors/time and201ns quiet horizon pass.
No partial matrix can emit a ripple displacement result.

Quiet average frequencies over40–120 and120–200ns are2.500754626 and
2.500754667GHz (about41Hz difference). Both windows contain200 positive
crossings; RF gate remains1.49190–1.51107V. These deterministic averages support
using this quiet baseline but do not establish intrinsic jitter or noise.
Full source-hash-after check remains pending until the matrix terminates.

Next poll existing22285; no restart. Analyze paired windows only when both cases
complete. [Partial evidence](evidence/vco-supply-ripple.json). No new layout.


## Pass 274 — supply ripple produces measured periodic edge displacement

Both201ns runs completed; source hashes unchanged, exact deck comparisons and
pre-ripple edge pairing passed.10mV peak50MHz PLLVDD ripple produces fitted
0.836113ps peak displacement over40–200ns. Early/late80ns windows give
0.836127/0.836101ps. Mean displacement−0.7997ps and drift0.0002583ps/ns
are reported separately; residual0.002303ps RMS is deterministic fit residual,
not intrinsic noise. Raw delay spans−1.65384 to+0.05391ps.

Integrating the measured DC frequency sensitivity as an instantaneous model
predicts0.8214–0.8245ps peak via K_supply*A/(2*pi*f_ripple*f_carrier).
Its agreement is a sanity comparison, not proof of a frequency-independent model
or PLL rejection. Supply amplitude/frequency, corner and fixed bias assumptions
remain narrow. No random phase-noise or complete timing-quality claim.

Launched matched1ps timestep repeat of both quiet/ripple cases, same201ns horizon
and circuitry, to test numerical sensitivity before adopting the small timing
measurement. Runner timeout600s per fine case; prior2ps artifacts preserved.
Job39893 active, scratch `scratch/transceiver-vco-supply-ripple-fine`.
Reproduce `bash projects/programmable_transceiver_platform/verification/run_vco_supply_ripple_fine.sh`;
check `python3 projects/programmable_transceiver_platform/verification/check_vco_supply_ripple.py --fine`.
[2ps evidence](evidence/vco-supply-ripple.json), [pending1ps evidence](evidence/vco-supply-ripple-fine.json).
No new layout; intrinsic clock noise and autonomous PLL remain unqualified.


## Pass 275 — timestep comparison prepared during verified fine-run wait

Polled39893: live fine quiet run, progress beyond29ns; no restart. Added an
independent comparison script requiring completed coarse/fine audits, identical
source hashes and decks differing only by2ps versus1ps transient steps. It
verifies raw artifact hashes before comparing peak amplitude, sinusoidal vector,
phase, offset, drift and residual across the three analysis windows. Pending
path exercised; completed comparison remains unexecuted until evidence arrives.

No automatic convergence threshold or physical pass is invented. Two step sizes
will show numerical sensitivity, not prove physical accuracy. Updated current
risk priorities with completed ripple evidence and the live verification task;
full PLL acquisition/noise and missing quadrature remain higher-level obligations
than a broad ripple sweep. Reproduce comparison:
`python3 projects/programmable_transceiver_platform/verification/compare_vco_ripple_timestep.py`.
[Pending comparison](evidence/vco-ripple-timestep-comparison.json). No new layout.


## Pass 276 — first explicit PDK passive quadrature circuit

Fine ripple job39893 verified live; continue existing run. Independently added
an exploratory differential RC quadrature subcircuit: low-pass I and high-pass Q,
four PDK poly resistors (2um width/length) and four explicitly replicated4-unit
MIM banks. Ideal1.5V common mode and differential AC source with25ohm per leg.
Completed41-point2–3GHz AC sweeps with equal0/25/100fF loads per output.

At2.5GHz I/Q gains are0.6063/0.7101 unloaded,0.5601/0.6561 at25fF and
0.4492/0.5261 at100fF. Q/I=1.1713. Phase is90degrees to printed precision in
this perfectly symmetric linear fixture; that exactness is not physical robustness.
Ratio varies0.9370–1.4055 over2–3GHz. No oscillator, buffer threshold/limiting,
actual mixer load, dynamic edges, mismatch or noise is qualified.

Next discriminate symmetry assumptions with unequal I/Q loading in both directions,
then actual self-biased input buffers if useful. Do not infer mixer-ready quadrature
from phase alone. Candidate is exploratory, not an adopted clock architecture.
Source hashes/artifacts, finite data and complete frequency grids checked.
Reproduce `bash projects/programmable_transceiver_platform/verification/run_quadrature_rc.sh`
then `python3 projects/programmable_transceiver_platform/verification/check_quadrature_rc.py`.
[Evidence](evidence/quadrature-rc.json). No new layout.


## Pass 277 — unequal loading breaks nominal RC quadrature symmetry

Completed matched25/25fF control and25/100,100/25fF I/Q per-output load cases.
At2.5GHz Q-leading-I phase is90.000,81.833 and98.167degrees; amplitude ratios
are1.1713,0.9368 and1.4644 respectively. Symmetric P/N loading remains within
each branch. These are deliberate scenarios, not predicted mismatch bounds.
Control waveform is bit-identical to pass276; checker verifies exact changes
against that hashed baseline deck and all41 frequency points in each case.

Phase errors±8.17degrees demonstrate sensitivity concealed by equal loads.
Do not adopt this unbuffered topology from its nominal90degree phase. Next useful
step is actual self-biased buffer input loading and amplitude restoration, with
branch mismatch and source impedance retained as risks; no broad ideal-RC tuning
sweep yet. Source/common-mode remain ideal and no RF oscillator integration or
large-signal edge/noise evidence exists for this candidate.

Fine ripple job39893 remains active: quiet case completed, ripple pending in
partial-case evidence. Continue that handle without restarting. Reproduce RC test:
`bash projects/programmable_transceiver_platform/verification/run_quadrature_rc_asymmetric.sh`
then `python3 projects/programmable_transceiver_platform/verification/check_quadrature_rc.py --asymmetric`.
[Evidence](evidence/quadrature-rc-asymmetric.json). No new layout.


## Pass 278 — quadrature network connected to actual self-biased buffers

Added four existing two-stage CMOS LO buffers (16PDK FETs) through four4-unit
MIM coupling banks and100k feedback resistors to first-stage outputs. Retained
25fF per RC node, added50fF per buffer output. OP and41-point AC runs complete;
checker verifies the exact circuit addition against the prior hashed25fF baseline.
All buffer inputs self-bias to1.530318V. DC buffer supply current10.1544mA
is a static operating-point value, not switching power.

At2.5GHz RC-node I/Q gains0.53295/0.62422 become small-signal output gains
2.65348/3.10792. Matched branches preserve90degree phase and1.17126 amplitude
ratio; actual transistor input loading does not by itself correct imbalance.
AC gain around self-bias does not prove full-swing output or large-signal timing.
Next transient test needs finite differential input swing, actual buffers and
edge/duty/amplitude checks before oscillator/mixer integration or architecture
adoption. Supply, source, common-mode, feedback R and output C remain explicit
ideal fixtures. No process/mismatch/noise or startup qualification.

Fine ripple job39893 remains active; quiet completed, ripple not yet terminal.
Reproduce buffered screen: `bash projects/programmable_transceiver_platform/verification/run_quadrature_rc_buffered.sh`
then `python3 projects/programmable_transceiver_platform/verification/check_quadrature_rc_buffered.py`.
[Evidence](evidence/quadrature-rc-buffered.json). No new layout.


## Pass 279 — finite-swing quadrature and fine-timestep ripple results

Completed two81ns OP-initialized quadrature transients: ideal2.5GHz differential
sinusoid with0.2/0.4V peak per leg (0.4/0.8V differential peak),25ohm source,
actual RC/MIM and four self-biased CMOS buffers. Late60–80ns differential
zero-crossing phase is90.0666/91.3089degrees. I output swings0.995–2.056V
and0.545–2.537V; Q swings0.905–2.144V and0.454–2.651V. At1.65V threshold,
I/Q high fractions are42.53/43.62% and46.56/47.26%. Buffer power33.45/32.93mW
excludes source/common-mode drive and other chip blocks. Earlier40–60ns window
agrees closely, but OP initialization is not cold startup.

Finite signal amplitude changes phase and duty; small-signal90degree symmetry
was not proof of adequate drive. No interface pass claimed. Next useful boundary
is actual oscillator loading and mixer input gates, not ideal-source amplitude
optimization alone. [Transient evidence](evidence/quadrature-buffered-transient.json).
Reproduce `bash projects/programmable_transceiver_platform/verification/run_quadrature_buffered_transient.sh`
and `python3 projects/programmable_transceiver_platform/verification/check_quadrature_buffered_transient.py`.

Fine ripple job39893 also completed both201ns cases. Exact coarse/fine circuit
and model comparison passes.1ps-step modulation amplitude0.836341ps versus
0.836113ps at2ps differs0.02722%; fitted phase differs−0.00642degrees.
This supports timestep insensitivity for this selected deterministic response;
it does not qualify random jitter, PLL rejection or unknown process behavior.
[Comparison](evidence/vco-ripple-timestep-comparison.json). No remaining live jobs
from these experiments. Whole-chip schematic incomplete; no new layout.


## Pass 280 — actual ring connected to RC quadrature and buffers

Replaced ideal sinusoidal sources and25ohm source resistors with the split-bias
PDK ring, fixed1.08V control/regeneration and25fF per ring output. Retained RC
network, self-biased buffers and lumped output loads. Seeded ring nodes require
UIC for this fixture; it is not cold startup.81ns run completes. Checker verifies
exact circuit transformation, source hashes and actual measured period rather
than assuming2.5GHz when converting edge delay to phase. Manifest scope text was
corrected after execution; raw circuit/waveforms were not changed.

Late ring frequency2.495423GHz, differential swing±0.40192V. Buffered I/Q
phase89.67983degrees; early window89.67984degrees. I outputs0.961–2.090V,
Q0.872–2.193V. Ring and buffer source powers27.744 and33.445mW exclude
control/regeneration/common-mode source delivery and all missing chip functions.
Window high fractions are not exact per-cycle duty measures when windows contain
fractional cycles. Bias and selected-window timing observations remain nominal.

This connects actual ring/RC/buffer circuits, but incomplete output swing leaves
mixer operation uncertain. Next attach actual mixer gates with explicit RF/input
and output terminations, then score conversion as well as LO timing. No actual
PLL, noise, process/mismatch or package qualification. Full-chip risk review:
quadrature now has connected nominal evidence, while complete RF conversion,
autonomous timing quality, converter references, wired recovery and host timing
remain unresolved. Avoid treating this one chain as a complete transceiver.

Reproduce `bash projects/programmable_transceiver_platform/verification/run_quadrature_ring.sh`
then `python3 projects/programmable_transceiver_platform/verification/check_quadrature_buffered_transient.py --ring`.
[Evidence](evidence/quadrature-ring.json). No live jobs; no new layout.


## Pass 281 — ring/RC/buffers now drive actual I/Q mixer gates

Added two16-FET PDK switching mixers to the completed ring/RC/buffer fixture.
Both see one finite300ohm RF source at1.5V nominal bias; each mixer output has
1k/1p termination to ground. Existing50fF buffer-output lumped loads remain, so
this adds real gate loading rather than replacing all uncertain load by nominal
FET capacitance. RF bias/source/terminations remain ideal and no LNA/ADC added.

Matched no-tone and10mV tone cases run201ns with fixed RF frequency2.51542263GHz
(about20MHz above the prior unloaded LO). Actual loaded LO frequency must be
measured; do not impose that nominal IF in analysis. Ring seeds/UIC unchanged.
Checker verifies exact declared addition, saved nodes, provenance and horizons,
and will report late LO/output ranges. Separate conversion/no-tone analysis is
still required before claiming useful mixer operation.

Job21704 verified live. Scratch `scratch/transceiver-quadrature-mixers`.
Reproduce `bash projects/programmable_transceiver_platform/verification/run_quadrature_mixers.sh`
then `python3 projects/programmable_transceiver_platform/verification/check_quadrature_mixers.py`.
[Pending evidence](evidence/quadrature-mixers.json). Next poll existing job and
inspect bias/swing/loading before fitting conversion; no new layout.


## Pass 282 — actual oscillator/quadrature/mixers show nominal conversion

Both201ns no-tone/tone cases completed; exact circuit-change, source/artifact,
waveform/horizon checks pass. Measured loaded LO is2.49542246GHz, producing
about20.00017MHz IF from the fixed2.51542263GHz source. Weighted least-squares
fits include DC/drift, IF,4or8 LO harmonics,RF and RF+LO; known noncoherent
synthetic multitone validates the fit. Three window choices expose leakage and
finite-record sensitivity (only3.2IF cycles in40–200ns,1.6in120–200ns).

Tone IF amplitudes are2.20735mV I and2.59931–2.59936mV Q, with Q/I1.1776
and Q-relative-phase about−89.596degrees. Gains from the10mV ideal source are
0.2207/0.2599; actual RF-node tone is4.17227mV. No-tone fitted IF in40–200ns
with4harmonics is0.109/0.135uV. Fit order/window changes leave tone amplitude
consistent, while residual strongly changes as more LO harmonics are fitted;
residual is not intrinsic noise. No broadband image rejection, linearity,
receiver sensitivity or EVM claim.

Mixer loading reduces buffer swings to roughly1.08–1.96V I and1.01–2.04V Q.
RF-node mean0.8128V despite1.5V source DC through300ohm: real LNA loading/bias
must be checked next. Limited LO swing still permits nominal conversion here,
but this does not qualify it against variation or actual RF signal chain demands.
Next attach actual LNA/bias network, preserving a no-tone control and verifying
LNA operating state before scoring conversion; no ideal-input optimization as
substitute for the complete receiver. No live jobs; no new layout.
[Conversion evidence](evidence/quadrature-conversion.json).
Reproduce `python3 projects/programmable_transceiver_platform/verification/check_quadrature_mixers.py`
then `python3 projects/programmable_transceiver_platform/verification/analyze_quadrature_conversion.py`.


## Pass 283 — actual LNA attached; no-tone loaded bias verified

Replaced ideal mixer RF drive with existing16-finger PDK LNA,300ohm drain load,
82ohm source degeneration,1meg/20p gate-bias/input coupling and50ohm input
source. LNA gate explicitly prebiased1.5V with seeded-ring UIC; no cold-start
claim. Preserved two real mixers and their loads. Matched zero/1mV input tone
cases run201ns. Only one forward-oriented LNA finger is device-probed; no claim
that this substitutes for full mismatch/noise or every-finger device auditing.

No-tone case completed. Late gate1.50149–1.50363V, source0.29303–0.29478V,
drain/mixer-input1.40952–1.48555V. Representative finger VDS−VDSAT minimum
+0.71938V, no negative reported VDS samples. LNA supply power20.465mW includes
drain-load delivery to the mixers; it is not transistor dissipation alone.
These selected dynamic bias observations show no gross LNA headroom collapse,
but tone gain, input loading, linearity and quality remain unmeasured.

Tone case remains live as job18543. Reused checker validates exact LNA circuit
addition and provenance; shared conversion analyzer has explicit LNA mode and
waits for completed matrix. Scratch `scratch/transceiver-quadrature-lna`.
Reproduce `bash projects/programmable_transceiver_platform/verification/run_quadrature_lna.sh`;
check `python3 projects/programmable_transceiver_platform/verification/check_quadrature_mixers.py --lna`;
analyze `python3 projects/programmable_transceiver_platform/verification/analyze_quadrature_conversion.py --lna`.
[Partial evidence](evidence/quadrature-lna.json). No new layout.


## Pass 284 — loaded LNA integration reveals poor conversion gain

Tone201ns run completed; both matched cases and source/artifact/deck checks pass.
At1mV source peak, late-window gate RF tone0.98964mV, drain tone1.06963mV:
loaded drain/gate voltage gain1.0808. I/Q IF amplitudes82.890/94.465uV yield
source-to-IF gains0.08289/0.09446 and drain-to-IF ratios0.07749/0.08832.
Q/I1.1396, relative phase−90.414degrees. Earlier window and4/8harmonic fits
agree closely on amplitudes; finite-record residual is not noise. No-tone IF
components remain tens of nV; ratios/phases of no-tone fit leakage are now omitted
rather than treated as transfer measurements.

Tone gate/source/drain ranges retain representative positive headroom (minimum
VDS−VDSAT0.7177V), but bias compliance does not guarantee RF gain. The actual
LNA-to-mixer operating point, nominal mixer drive and source impedance differ
from the earlier ideal RF fixture. These comparisons identify weak loaded gain
and conversion without uniquely attributing cause. Do not declare a useful
receiver from tone detection. Next discriminator: increase actual CMOS LO drive
with a controlled buffer-stage candidate, preserving the LNA/RF fixture; compare
conversion, loading, phase/duty and power against this baseline. Actual gain/noise,
filter/ADC integration and complete clock quality remain open.

[Evidence](evidence/quadrature-lna-conversion.json). Conversion analyzer now
reports gate/drain/IF breakdown and excludes no-tone ratio interpretations.
No live jobs; no new layout.


## Pass 285 — controlled final LO inverter candidate launched

Added one actual CMOS inverter after each of four LO buffers: PMOS8um/NMOS4um,
L=.28um,m=4 each. Existing buffer output now drives that stage; final node still
carries the same mixer gates and50fF load. All four polarities invert together,
so this introduces a common LO inversion rather than deliberately reversing only
one I/Q branch. Added8FETs; LNA, ring/RC, RF stimulus, mixer termination, biases,
seeds and201ns horizon preserved. This is a candidate, not an adopted repair.

Shared checker verifies exact stage addition/removal and adds actual buffer/ring
power and differential LO phase ranges. Baseline checker rerun passes. Conversion
analysis has explicit final-stage mode and remains pending until both zero/tone
cases finish. Full-chip review: low gain at the loaded LNA/mixer boundary is a
measured bottleneck; more LO swing is only one hypothesis. Autonomous PLL/noise,
baseband/ADC integration, shared reference regulation, wired recovery and host
physical timing remain independent unresolved gates. No broad sizing sweep.

Job30794 verified live, zero case progressing beyond195ns. Scratch
`scratch/transceiver-quadrature-lna-final`. Reproduce
`bash projects/programmable_transceiver_platform/verification/run_quadrature_lna_final.sh`;
check and analyze with existing `check_quadrature_mixers.py` and
`analyze_quadrature_conversion.py`, both with `--lna --final-stage`.
[Pending evidence](evidence/quadrature-lna-final.json). No new layout.


Pass285 partial result: zero case completed after the live check. Final I swing
0.304–2.860V,Q0.227–2.951V: drive increases substantially. Buffer supply
power57.98mW; LO phase92.790–92.795degrees. LNA drain now1.129–1.237V
and representative headroom minimum0.445V, showing changed mixer loading.
Tone case remains live; conversion benefit is not yet established.


## Pass 286 — stronger actual LO drive improves conversion with clear costs

Both final-stage histories completed. Independent comparison verifies identical
shared source hashes and exact eight-FET stage addition against both baseline
decks; analyzes matched tone windows/orders and accounted supplies. Late8harmonic
fit: I/Q420.355/470.127uV from1mV input, improvements5.071/4.977x
(14.102/13.939dB). Q/I improves1.1396→1.1184, but relative phase worsens
−90.414→−93.398degrees. No-tone IF fits remain far below the tone response;
residual contains unmodeled deterministic harmonics, not a noise measurement.

Buffer power33.508→57.978mW and LNA supply20.465→23.413mW; ring~27.744mW
unchanged. Accounted increase27.419mW excludes ideal bias/control/common-mode
sources. Stronger switching changes LNA load: drain/gate RF tone gain falls
1.0808→0.4942 and drain mean1.4395→1.1715V. Drain-to-IF tone ratios rise to
0.856/0.958, but these periodically loaded-node ratios are not standalone mixer
power conversion gain. Overall source-to-IF voltage gain remains below unity.

Retain as a promising diagnostic candidate, not adopted/qualified circuitry.
The matched change demonstrates drive was a significant limitation, while power,
phase imbalance and loaded RF gain remain open. Next investigate LNA/mixer
interface impedance and useful baseband gain/filter integration rather than
blindly adding more LO stages. Keep complete RX noise/dynamic range and autonomous
timing as system gates. No live jobs; no new layout.
[Comparison](evidence/quadrature-lo-driver-comparison.json).
Reproduce `python3 projects/programmable_transceiver_platform/verification/compare_quadrature_lo_driver.py`.


## Pass 287 — isolate LNA/mixer DC loading with actual MIM coupling

Started a matched zero/tone candidate adding a256-unit explicit PDK MIM bank
between the LNA drain and shared I/Q mixer input. Nominal capacitance~11.35pF
from the previously audited unit model. Retain final LO stages, all LNA/bias
components, mixers, baseband terminations, clock seeds and201ns horizon. No new
ideal mixer-input bias source: its DC operating point must be established by
the actual switching paths and existing terminations. This tests DC isolation;
RF impedance/loading, transient charge and common-mode shifts can still harm it.

Checker has explicit AC-coupled mode, reverses exactly the added bank and mixer
node reconnection to verify the prior circuit, and reports separate drain and
mixer-input excursions. Conversion analyzer separately fits the mixer-input RF
tone and mean; neither more drain headroom nor a larger drain signal alone will
select the candidate. Compare source-to-IF conversion, phase/power and bias state
before adoption. No outcome yet; pending paths execute successfully.

Job48375 verified active, zero run progressing beyond64ns. Scratch
`scratch/transceiver-quadrature-lna-ac`. Reproduce
`bash projects/programmable_transceiver_platform/verification/run_quadrature_lna_ac.sh`;
check/analyze with `check_quadrature_mixers.py` / `analyze_quadrature_conversion.py`,
both using `--lna --final-stage --ac-coupled`.
[Pending evidence](evidence/quadrature-lna-ac.json). No new layout.


Pass287 partial result: zero case completed. LNA drain2.173–2.179V, mixer
input10.0–16.4mV, representative LNA headroom minimum1.468V. LNA supply
power12.374mW, buffer58.678mW; mixer-output excursions reach about−25mV.
DC operating points separate as intended, but tone conversion and switching
quality remain unmeasured. Tone case now live; do not infer success from bias.


## Pass 288 — DC isolation improves bias but worsens RF transfer

Both AC-coupled histories completed; exact single-bank/node-reconnection and
shared-source comparison verified against the stronger-driver direct baseline.
At1mV input, late I/Q IF153.509/159.348uV versus420.355/470.127uV before:
−8.750/−9.397dB. Q/I improves1.1184→1.0380 but relative phase worsens
−93.398→−95.018degrees. Loaded LNA drain/gate gain falls0.4942→0.1872;
drain RF tone186.31uV and mixer input172.29uV. High drain DC headroom did
not repair RF gain. Small no-tone IF components remain distinct from tone;
finite-record harmonic residual is not noise.

Accounted ring/buffer/LNA power decreases10.339mW. LNA drain mean2.1751V,
mixer input mean12.31mV; RF loading still strongly damps the LNA. Reject this
unbiased AC-coupled candidate as the next integration baseline; preserve it as
evidence, not a general rejection of AC coupling. Continue from the direct-
coupled stronger-driver candidate, itself still unqualified.

Next discriminating measurement: add an observation-only zero-volt current sensor
at that candidate's LNA/mixer interface, verify waveform reproduction, and fit
RF voltage/current together. This tests actual periodic loading before another
bias, capacitor or switch-size change. Do not conflate DC isolation, voltage
gain, power gain and impedance in a switching network. Whole receiver noise,
linearity, filter/ADC and autonomous timing remain open. No live jobs or layout.
[Comparison](evidence/quadrature-ac-comparison.json).
Reproduce `python3 projects/programmable_transceiver_platform/verification/compare_quadrature_lo_driver.py --ac-coupled`.


## Pass 289 — interface current sensor replay validates no-tone observation

Inserted VSENSE RF MIXRF0 into the direct-coupled final-driver baseline; only
the two mixer RF terminals move to MIXRF. Positive sensor current enters both
mixers. No load/circuit sizing change. Checker reverses exact topology/probe edits,
checks provenance, and reports sensor voltage and original-vector reproduction
without requiring artificial bit identity or silently realigning clocks.

No-tone201ns replay completes. Sensor voltage exactly0 in saved data; original
vectors are not bit-identical, but120–200ns maximum interpolated differences are
14.1nV atRF,86.7/101.0nV atI/Q LO and≤6.63nV atIF nodes. Average mixer-port
current3.58322mA, range3.24615–3.77193mA. These are dynamic DC/total-current
observations, not RF input resistance or transistor conduction-only current.

Tone replay still live as job77594. Next use paired RF voltage/current phasors
with no-tone control, preserving phase/sign and distinguishing periodic-port
response from a simple resistor. No gain/current inference from unrelated extrema.
Scratch `scratch/transceiver-quadrature-interface-current`.
Reproduce `bash projects/programmable_transceiver_platform/verification/run_quadrature_interface_current.sh`
then `python3 projects/programmable_transceiver_platform/verification/check_quadrature_interface_current.py`.
[Partial evidence](evidence/quadrature-interface-current.json). No new layout.


## Pass 290 — measured fundamental mixer loading explains an interface burden

Both sensor replays complete. Tone sensor drop exactly0; selected original-node
maximum differences≤69.3nV late in the trace (no-tone≤101.1nV). Weighted
multitone fitting reuses a shared helper; independent known parallel-RC fixture
validates current direction and complex phasor sign. Fits include source RF,
measured IF,4/8 LO harmonics and RF+LO over three windows.

Late8harmonic tone fit: RF voltage490.911uV peak, input current3.17442uA peak.
Conditional fundamental admittance5.8117+j2.8352mS, ratio Z=138.989−j67.804ohm,
|Z|154.646ohm; current leads26.005degrees. Full-window4harmonic fit gives
138.836−j67.430ohm. No-tone RF leakage is separately reported, not divided
into impedance. Large time-domain residual is unmodeled switching content, not
noise; stability across fit choices supports the selected tone estimate only.

Two mixer ports therefore present substantial RF loading alongside the300ohm
LNA drain resistor. This does not uniquely explain all weak LNA gain: intrinsic
output capacitance, degeneration, frequency conversion and sideband terminations
also matter. Do not turn this conditional driven-port ratio into a broadband
LTI resistor or power gain. Next discriminate loading from LNA transconductance
with a controlled mixer-size or interface-isolation experiment, carrying LO
phase/power/conversion tradeoffs. Avoid an arbitrary bias change based on DC alone.

Full-chip review: RF chain is increasingly connected but useful gain/noise,
baseband/ADC integration, autonomous PLL/noise, reference regulation, wired
recovery and host timing remain unqualified. No live jobs or new layout.
[Loading evidence](evidence/quadrature-interface-loading.json).
Reproduce `python3 projects/programmable_transceiver_platform/verification/analyze_quadrature_interface.py`.


## Pass 291 — half-width mixer candidate with current measurement launched

Changed each of16 fingers per mixer from4um to2um, preserving L=.28um, explicit
finger orientation, actual final LO stages, LNA, RC network, terminations and
zero/tone sources. Retained observation sensor at the combined mixer input.
Generated decks inline an exact width-only model variant; original IP remains
unchanged. This tests loading versus switch resistance, not an assumed benefit
from smaller geometry. Models/circuit otherwise identical to the sensor baseline.

Checker verifies the original model hash,16 substitutions and exact circuit
reversal, and reports supply powers alongside current/reproduction differences.
Waveform differences are intentional for this candidate. Loading and conversion
analyzers share validated weighted phasor fitting and explicit half-width mode;
pending paths pass, completed analyses await both201ns histories.

Job15109 verified live. Scratch `scratch/transceiver-quadrature-interface-half`.
Reproduce `bash projects/programmable_transceiver_platform/verification/run_quadrature_interface_half.sh`;
check `python3 projects/programmable_transceiver_platform/verification/check_quadrature_interface_current.py --half`;
loading `python3 projects/programmable_transceiver_platform/verification/analyze_quadrature_interface.py --half`;
conversion `python3 projects/programmable_transceiver_platform/verification/analyze_quadrature_conversion.py --lna --final-stage --half-mixer`.
[Pending evidence](evidence/quadrature-interface-half.json). No new layout or
candidate adoption; complete RF quality and whole-chip gates remain open.


## Pass 292 — smaller switches relieve loading but do not improve chain gain

Half-width zero/tone201ns runs and exact geometry/source checks complete. Late
RF-port ratio changes from139−j68ohm (|Z|154.6) to319−j140ohm (|Z|348.5).
Loaded LNA drain/gate tone gain rises0.4942→0.7714, but drain-to-IF ratios fall
0.856/0.958→0.533/0.598. Net I/Q IF407.73/457.62uV versus420.36/470.13uV
at1mV input. Q/I1.1224 and relative phase−93.201degrees remain imbalanced.
Accounted supply power falls only~1.93mW. Retain wider direct-coupled final-stage
baseline for next integration; half-width remains a useful rejected gain-improvement
experiment, not proof of worse noise or all-condition performance.

No-tone controls and4/8harmonic/window comparisons separate fitted tone from
switching leakage. Smaller geometry changes both loading and conversion; the
measured tradeoff does not support an endless size sweep. Next boundary is actual
baseband filtering/gain: existing filter was characterized around1.177V input
common mode, which cannot be assumed compatible with these mixer outputs. First
check its loaded bias/input current and gain at the observed mixer common modes,
then connect it and remeasure end-to-end conversion. No ADC/noise/linearity claim.

[Comparison](evidence/quadrature-half-comparison.json),
[loading](evidence/quadrature-interface-half-loading.json),
[conversion](evidence/quadrature-interface-half-conversion.json).
No live jobs; no new layout.


## Pass 293 — baseband filter common-mode/loading preflight

Completed six DC/AC cases of the existing actual6-FET two-stage PMOS filter,
RFB20k/C20p,2.25V external bias,3.3V/27C typical. Differential-source common
modes0.6/0.9/1.177V and0/1kohm per-leg source resistance test interface scenarios;
these are not foundry uncertainty bounds. All6 device reported VDS−VDSAT margins
remain positive; worst across cases0.4946V. Output common mode1.052–1.087V.

At20MHz port-to-output gain4.295–4.373. With1kohm source legs, source-to-output
gain4.485–4.580; the source/port distinction matters for an active feedback input.
DC current flows OUT of the filter into low-common-mode sources: up to21.53uA
per leg at0.6V,8.11uA at0.9V. At1.177V it draws4.26uA per leg. Finite source
resistance shifts input bias by up to20.61mV. Positive device headroom and an AC
gain curve do not prove loop stability, startup, noise or mixer compatibility.

This clears an isolated nominal bias preflight only. Next connect two actual
filter instances to the retained wider-mixer/final-driver chain, preserve mixer
terminations initially, and compare loaded DC, conversion and power with the
unfiltered baseline. ADC/common-mode interface still needs its own circuit;
filter output near1.07V is not automatically the ADC's required bias. No live jobs
or new layout. [Evidence](evidence/bb-interface.json).
Reproduce `bash projects/programmable_transceiver_platform/verification/run_bb_interface.sh`
then `python3 projects/programmable_transceiver_platform/verification/check_bb_interface.py`.


## Pass 294 — two actual filters connected to complete nominal RF front end

Added original two-stage transistor filter sections on I and Q mixer outputs,
RFB20k/C20p, ideal3.3V supply/2.25V bias. All prior mixer1k/1p terminations,
LNA, actual ring/RC/LO buffers and final drivers remain unchanged. No ADC load.
Matched zero/1mV tone runs201ns with original seeded/prebiased RF and UIC;
filter initial charging must be evaluated rather than assumed settled.

No-tone run completed. Late mixer common modes~0.888/0.911V; filter outputs
approach1.0696/1.0711V; combined filter power28.818mW over120–200ns.
40–120ns output means~1.050/1.053V and much larger excursions show startup
settling. Even late common-mode range retains up to~0.43mV movement; no steady-
state or stability claim. Checker verifies exact added circuitry/probes and all
source/artifact/horizon checks; conversion analyzer has explicit filter mode.

Tone remains active as33056. Next compare late versus earlier fits and no-tone
residual, extending horizon if startup contaminates conversion. Adding nominal
filter gain cannot be multiplied blindly into earlier isolated RF results.
Scratch `scratch/transceiver-bb-connected`.
Reproduce `bash projects/programmable_transceiver_platform/verification/run_bb_connected.sh`;
check `python3 projects/programmable_transceiver_platform/verification/check_bb_connected.py`;
analyze `python3 projects/programmable_transceiver_platform/verification/analyze_quadrature_conversion.py --lna --final-stage --filter`.
[Partial evidence](evidence/bb-connected.json). No new layout or receiver-quality claim.

## Pass 295 — connected filter horizon extension and risk reassessment

Both201ns zero/tone runs from pass294 completed. Their120–200ns filter tone
amplitudes were1.8456/2.0726mV, but the no-tone fitted components remained
2.13/6.78uV. Extended the unchanged circuit to401ns in separate scratch;
both cases completed with verified source/deck/artifact hashes and horizons.
Exact deck reversal proves simulation duration was the only circuit-fixture change.
The overlapping120–200ns fitted metrics reproduce exactly.

Over240–400ns the1mV RF input produces1.84865/2.07331mV differential I/Q,
Q/I1.12152 and relative phase−93.487degrees. Comparing240–320 against320–400ns,
amplitudes change0.0262%/0.00756% and phase0.01854degrees. No-tone last-window
fitted components are0.367/0.186uV. These are deterministic fit diagnostics, not
noise, ENOB, EVM or a requirement-derived settling pass. Seeded oscillator,
prebiased LNA, external ideal biases/supplies, ideal filter passives and no ADC
load remain. Retain the wider-mixer/final-driver/two-filter circuit as the nominal
connected diagnostic baseline; it is not a qualified receiver.

Updated the implementation inventory to include this circuit and evidence;
the inventory audit still reports whole-chip schematic incomplete/layout gate
closed. Whole-chip review keeps autonomous lock/noise and RF/converter quality
at the front. Next audit actual model noise coverage and a tractable LNA/filter
noise fixture before more gain tuning. A DC small-signal noise result must not
be represented as periodically switched mixer noise or oscillator phase noise.
ADC reference regulation, filter-to-ADC bias interface, wired recovery, host timing
and simultaneous operation remain open. No new layout or live jobs.

Reproduce from repository root:
- `bash projects/programmable_transceiver_platform/verification/run_bb_connected_settling.sh`
- `python3 projects/programmable_transceiver_platform/verification/check_bb_connected.py --settling`
- `python3 projects/programmable_transceiver_platform/verification/analyze_quadrature_conversion.py --lna --final-stage --filter --settling`
- `python3 projects/programmable_transceiver_platform/verification/compare_bb_settling.py`

The comparison also requires the retained201ns evidence/raw artifacts.
Scratch: `scratch/transceiver-bb-connected-settling`.
[Completion audit](evidence/bb-connected-settling.json),
[conversion](evidence/bb-connected-settling-conversion.json),
[controlled comparison](evidence/bb-settling-comparison.json).

## Pass 296 — calibrated stationary filter noise and installed-model inspection

Added a reproducible noise fixture for the actual transistor filter atTT27C,
3.3V supply,2.25V external bias,0.9V source common mode and1kohm per-leg source
resistance. Includes a1kohm resistor calibration and matched PDK fnoicor0/1 cases;
all three completed. Checker verifies named columns,201 frequency points through
100MHz, finite spectra, exact declared corner-only change, artifact/source hashes,
thermal calibration within0.1%, and numerical PSD integration against simulator
output within0.5%. The calibration is4.071nV/sqrt(Hz), consistent with4kTR.

Output noise integrated1kHz–20MHz is269.26/343.62uVrms for fnoicor0/1;
input-referred41.22/52.27uVrms. At1kHz output densities are0.851/2.919uV/sqrt(Hz),
while at20MHz they are41.05/41.86nV/sqrt(Hz). The installed PDK explicitly
provides the flicker selector and noise coefficients; response to the selector
is measured here. This establishes usable stationary model behavior, not silicon
validation, a bound on hidden fab uncertainty, or a phase-noise method.

Both noisy source resistors are included. Actual switched mixer loading/noise
folding, LNA, real bias sources, oscillator and ADC are absent. This is a diagnostic
band, not a Wi-Fi SNR/EVM result; do not divide the prior connected tone by this
noise to claim receiver quality. Next attribute source/device/resistor noise and
establish an allocation, then extend to the actual LNA. Full autonomous timing,
ADC references, wired recovery and host timing remain open. No circuit candidate
replaced and no new layout or live jobs.

Reproduce from repository root:
- `bash projects/programmable_transceiver_platform/verification/run_bb_noise.sh`
- `python3 projects/programmable_transceiver_platform/verification/check_bb_noise.py`

Scratch `scratch/transceiver-bb-noise`; runner takes less than a second here.
[Noise evidence](evidence/bb-noise.json),
[selected installed-model lines and hashes](evidence/model-noise-coverage.json).

## Pass 297 — stationary noise contributor attribution

Replayed the resistor calibration and both filter noise settings with per-device
noise reporting enabled. Source hashes and exact reversal of reporting-only deck
changes match pass296. Total spectra and named integrated noise values reproduce
exactly. The extra vectors change the implicit scalar wrdata scale; checker uses
named total columns and retains this distinction rather than claiming raw-file
identity. Sum of16 MOS/resistor aggregate noise powers matches the total at every
frequency within1e-10 relative; MOS subcomponent sums independently match their
aggregates. No aggregate/subcomponent double counting.

Over1kHz–20MHz, default fnoicor0 output noise power is38.97% filter input
resistors,38.97% external source resistors,11.45% first-stage MOS,8.11% feedback
resistors,1.62% load resistors and0.89% second-stage MOS. For fnoicor1 these become
23.93%,23.93%,43.74%,4.98%,0.99%,2.43%. MOS flicker contributes5.81%/42.16%
of total (already included in MOS groups). Source and input-resistor groups each
contribute168.08uVrms in quadrature. Near-zero tail contribution is a symmetric
DC-linearized result, not mismatch or common-mode-noise immunity.

This narrows the next experiment: lower the filter input resistance and examine
DC/AC gain/loading before selecting a noise improvement; don't jump directly to
larger transistors. Actual mixer noise/loading and a system receiver noise allocation
remain missing. Baseline circuitry unchanged, no new layout, no live jobs.

Reproduce from repository root:
- `bash projects/programmable_transceiver_platform/verification/run_bb_noise_contributors.sh`
- `python3 projects/programmable_transceiver_platform/verification/check_bb_noise_contributors.py`

Requires retained pass296 scratch artifacts for the comparison.
Scratch `scratch/transceiver-bb-noise-contributors`.
[Evidence](evidence/bb-noise-contributors.json).

## Pass 298 — lower filter input resistance has mixed noise/gain consequences

Tested only RIP/RIN1kohm→500ohm, leaving the retained source files unchanged by
inlining the candidate in generated decks. Six common-mode/source-resistance
DC/AC cases and two flicker-noise cases for each variant completed:16 cases.
Baseline data reproduce exactly; checker reverses the declared cell substitution,
verifies source/deck/artifact hashes, finite outputs and requested frequency ranges.

Candidate1kHz–20MHz output noise is267.01/366.34uVrms (fnoicor0/1), versus
269.26/343.62 baseline:−0.83%/+6.61%. Input-referred noise is36.02/47.78uVrms,
a12.61%/8.58% reduction, but the transfer function changes. At0.9V CM/1kohm
source legs,1MHz gain rises6.644→8.035; sampled gain peak shifts10→7MHz
and7.084→8.229;20MHz gain falls4.552→4.464. Across six scenarios minimum
reported VDS−VDSAT remains positive, worst0.4844V; input DC current magnitude
increases about2.2%, while power changes less than0.05%. No stability claim.

Disposition: retain original1kohm input filter baseline. Reduced integrated
input-referred noise does not establish usable receiver SNR improvement when
frequency response and loading differ. Next examine actual LNA gain/noise and
system allocation before more filter tuning. Actual switched mixer noise, ADC
references, autonomous clock quality, wired recovery and host timing remain open.
No new layout or live jobs.

Reproduce from repository root:
- `bash projects/programmable_transceiver_platform/verification/run_bb_input_resistance.sh`
- `python3 projects/programmable_transceiver_platform/verification/check_bb_input_resistance.py`

Requires retained `transceiver-bb-noise` and `transceiver-bb-interface` scratch
baselines. New scratch `scratch/transceiver-bb-input-resistance`.
[Evidence](evidence/bb-input-resistance.json).

## Pass 299 — actual LNA stationary noise and output-load scenarios

Six cases complete: original16-finger LNA with unloaded output, an approximate
mixer load, and a heavier load, each at fnoicor0/1. Original50ohm RF source,
20pF coupling,1.5V ideal gate bias,300ohm drain and82ohm source resistance remain.
The approximation uses172.07ohm AC-coupled shunt resistance plus179.39fF and
3.583mA ideal DC sink, derived from earlier conditional mixer-port measurements.
Heavy case uses100ohm/250fF/4mA. These are scenarios, not uncertainty bounds;
load resistance is noisy and the ideal DC sink noiseless, unlike a real mixer.

At2.5GHz source-to-drain gains are1.420 unloaded,0.491 approximation,0.340 heavy;
drain DC voltages2.179/1.175/1.060V. Selected forward-finger saturation margins
remain positive1.476/0.491/0.378V. Unloaded stationary source-referred noise factor
is9.09/9.33dB; approximation11.24/11.37dB; heavy12.31/12.41dB. These are not
receiver noise figures. Default unloaded noise power splits31.44% drain resistor,
29.90% source resistor,26.32% MOS and12.34% RF source. Input noise densities
are2.592/2.666nV/sqrt(Hz) unloaded and3.321/3.372 with the approximation.

Checker verifies completion, hashes, named contributor sums closing to total,
source-resistor thermal calibration, AC gain against output/input noise ratio,
and exact flicker-setting-only changes. Approximate bias/gain agreement with the
connected circuit supports this limited loading diagnostic, not mixer-noise fidelity.
No matching network, pads/package, actual bias noise or model accuracy established.

Next test RF bypass of the82ohm source resistor while retaining DC degeneration;
check gain/noise/headroom before rejoining the actual mixers. Original circuit
remains baseline. Autonomous clock, ADC regulation, wired/host and coexistence
risks remain unresolved. No new layout or live jobs.

Reproduce from repository root:
- `bash projects/programmable_transceiver_platform/verification/run_lna_noise.sh`
- `python3 projects/programmable_transceiver_platform/verification/check_lna_noise.py`

Scratch `scratch/transceiver-lna-noise`.
[Evidence](evidence/lna-noise.json).

## Pass 300 — ideal RF source bypass improves LNA gain and stationary noise

Added only20pF across the existing82ohm source resistor. All six retained
stationary load/flicker cases complete. Exact deck reversal and source/artifact
hashes verify the controlled change; every DC OP vector reproduces exactly.

At2.5GHz, unloaded gain increases1.420→2.716 and stationary noise factor
9.09/9.33→5.71/6.22dB. Under approximate mixer loading gain0.491→0.958,
noise factor11.24/11.37→7.13/7.46dB. Heavy-load gain0.340→0.665 with
noise factor12.31/12.41→7.88/8.15dB. Across cases gain improves1.91–1.96x
and noise factor3.11–4.43dB. Source resistor noise contribution is strongly
suppressed; existing DC bias/headroom/power is unchanged. These are stationary
ideal-capacitor results, not switched receiver sensitivity or silicon qualification.

Disposition: candidate for PDK MIM substitution and actual mixer integration;
retained connected circuit is unchanged. Next use an explicit MIM bank and test
uncertain series impedance before treating20pF as realizable at RF. RF stability,
compression/linearity, startup and full converter noise remain unverified.

Whole-chip review keeps autonomous PLL completion/lock/noise, ADC/reference
regulation, wired recovery, host timing and complete bias/pad/supply integration
as independent blockers. A local gain improvement cannot retire them. No new
layout or live jobs.

Reproduce from repository root:
- `bash projects/programmable_transceiver_platform/verification/run_lna_bypass.sh`
- `python3 projects/programmable_transceiver_platform/verification/check_lna_noise.py --bypass`
- `python3 projects/programmable_transceiver_platform/verification/compare_lna_bypass.py`

Comparison requires retained original LNA noise artifacts.
Scratch `scratch/transceiver-lna-bypass`.
[Comparison](evidence/lna-bypass-comparison.json), [case evidence](evidence/lna-bypass.json).

## Pass 301 — explicit MIM bypass and series-impedance scenarios

Replaced ideal20pF with448 explicit5x5um MIM units using existing256+128+64
hierarchical banks, approximately19.86pF at the previously audited nominal unit
value. Plate area alone is11200um²; contacts, spacing and routing are unallocated.
No reliance on the ignored `par` parameter. Tested clean connection,5ohm/300pH
and5ohm/1nH series paths across three load scenarios and two flicker settings:
18 cases completed. These are diagnostic scenarios, not layout estimates or bounds.

Clean approximate-load gain0.9576, stationary noise factor7.126/7.460dB;
5ohm/300pH gives0.9122 and7.386/7.698dB;5ohm/1nH gives0.8958 and
7.447/7.746dB. Unbypassed gain0.4914 and11.240/11.374dB. Benefit survives
all selected cases. Clean DC vectors reproduce exactly; series-path cases differ
by at most2e-15 in exported OP quantities, below explicit1e-12 numerical tolerance.
Checker verifies source hashes and exact reversal of declared bank/series additions.
No extracted geometry, RF stability, compression, startup or receiver-noise claim.

Next connect the actual MIM bank to the retained mixer/filter chain and perform
matched zero/tone transients before promoting it. Retained connected baseline
unchanged. Clock/ADC/wired/host and whole-chip integration obligations remain open.
No new layout or live jobs.

Reproduce from repository root for each MODE in clean, lossy, inductive:
- `bash projects/programmable_transceiver_platform/verification/run_lna_mim.sh MODE`
- `python3 projects/programmable_transceiver_platform/verification/check_lna_noise.py --mim=MODE`
- Then `python3 projects/programmable_transceiver_platform/verification/compare_lna_mim.py`

Scratch `scratch/transceiver-lna-mim-MODE`.
[Comparison](evidence/lna-mim-comparison.json).

## Pass 302 — explicit bypass bank connected; matched transients running

Added448-unit MIM bank directly from LNA source to ground in the retained actual
ring/RC/LO-driver/LNA/two-mixer/two-filter fixture. All earlier terminations,
biases, initial conditions,1mV RF frequency and401ns/2ps transient settings stay
unchanged. No series parasitics in this first connected test. Original baseline
remains retained. This is actual switching integration, not the stationary load
approximation from passes299–301.

Shared runner/checker/analyzer now have explicit `--settling --bypass` mode,
separate scratch/evidence, exact reversal of the three bank instances, and
late-window LNA finger headroom/source-power checks. Prepared a comparison that
requires both completed zero/tone analyses, matching source/artifact hashes and
an exact bank-only deck change; it reports gain, I/Q phase and late-window
sensitivity without inventing a passing threshold. Pending paths and Python
compilation checked; comparison results are not yet available.

Live job68614: process confirmed running zero.spice. Do not restart on observation
timeout. Scratch `scratch/transceiver-bb-connected-bypass`. Next observe terminal
status and run the following from repository root:
- `python3 projects/programmable_transceiver_platform/verification/check_bb_connected.py --settling --bypass`
- `python3 projects/programmable_transceiver_platform/verification/analyze_quadrature_conversion.py --lna --final-stage --filter --settling --bypass`
- `python3 projects/programmable_transceiver_platform/verification/compare_bb_bypass.py`

Reproduction runner (only for a deliberate new run):
`bash projects/programmable_transceiver_platform/verification/run_bb_connected_bypass.sh`.
No connected gain/noise/startup claim, no new layout.

## Pass 303 — bypass zero-input case completes; tone remains live

Continued existing job68614 without restart. Zero-input case completed401ns,
200516 saved rows, returncode0 and no timeout. Partial checker verifies its
artifact hashes, exact declared bank-only circuit changes, finite named vectors
and full horizon. Matrix-level after-run source hashes await the tone case;
partial evidence must not be represented as completed provenance for the pair.

Over320–400ns, LNA source mean0.28808V, drain mean1.17671V, selected-finger
minimum VDS−VDSAT0.45035V. Filter output common modes~1.069656/1.071051V.
Measured main-source power: LNA23.356mW, LO buffers57.974mW, ring27.744mW,
filters28.816mW, sum137.891mW. This excludes real bias generation and missing
chip functions. No-tone headroom is not tone linearity or receiver qualification.

Strengthened pending comparison to include both circuits' late-window DC bias,
selected-finger headroom, output common mode and main-source power alongside
conversion gain/phase. Python compilation passes; final comparison has not run.

Live tone case remains in68614; retain scratch and process. Next finish case audit,
then conversion analysis and comparison using pass302 commands. No new layout.
[Partial case evidence](evidence/bb-connected-bypass.json).

## Pass 304 — connected bypass gain improvement verified

Existing job68614 completed both401ns histories; audit, conversion analysis and
comparison48783 are terminal. Source hashes match before/after; exact reversal
confirms the448-cell MIM bank is the only circuit change from the retained
unbypassed401ns baseline. Both full raw artifacts are checked.

Over240–400ns,1mV RF produces3.60445/4.03776mV filtered I/Q, versus
1.84865/2.07331mV:1.94977/1.94750x gains. Loaded LNA drain/gate gain0.9774.
Q/I1.12022,relative phase−93.4287degrees; phase changes only0.0586degree.
Last two80ns amplitude windows differ0.0168%/0.000794%. No-tone fitted
components0.226/0.067uV are deterministic fit diagnostics, not intrinsic noise.

Selected-finger late minimum margin0.44899V versus0.44896V; accounted four-source
power137.8909mW versus137.8904mW. Filter common modes1.069656/1.071052V.
Positive nominal headroom and finite-window consistency do not prove startup,
stability, compression, receiver noise, EVM or all-condition function.

Retain bypass chain as next experimental baseline, preserving original comparison.
Next signal-level dependence test should determine whether conversion remains
proportional before noise/gain improvements are promoted. Implementation inventory
now includes separate noise evidence and explicitly flags the contract's differential
RF_RX_P/N interface as absent from this single-ended fixture. Inventory audit still
reports incomplete schematic/layout gate closed. No live jobs or new layout.

Reproduce audit/analysis/comparison with pass302 commands.
[Comparison](evidence/bb-bypass-comparison.json).

## Pass 305 — connected10mV signal-level experiment running

Retained bypass chain now has a separate amplitude-only test: RF peak1mV→10mV,
frequency2.51542263GHz,401ns/2ps, original seeded/prebiased history. Goal is an
initial proportionality/compression diagnostic, not a1dB compression point or
intermodulation qualification. All circuit elements and terminations unchanged.

Avoided rerunning zero input: runner verifies the retained zero deck is identical,
all prior artifacts match their hashes, and model/source hashes agree before
copying its data/log. New case explicitly records reuse; checker independently
verifies the original control and copied artifacts. Shared checker passes this
partial case and correctly leaves matrix completion false. Tone is running as
5771, confirmed live ngspice process; no tone results yet.

Prepared an amplitude-only comparison requiring both completed analyses and exact
reversal of the RF amplitude change. It reports normalized gain ratios/dB, phase,
late-window consistency, DC/headroom and four-source power. Python compilation
passes; numerical candidate comparison remains pending. Original baseline retained.

Scratch `scratch/transceiver-bb-connected-level10`. Continue existing5771;
when terminal, run from repository root:
- `python3 projects/programmable_transceiver_platform/verification/check_bb_connected.py --settling --bypass --level10`
- `python3 projects/programmable_transceiver_platform/verification/analyze_quadrature_conversion.py --lna --final-stage --filter --settling --bypass --level10`
- `python3 projects/programmable_transceiver_platform/verification/compare_bb_level10.py`

Reproduction only for deliberate new run:
`bash projects/programmable_transceiver_platform/verification/run_bb_connected_level10.sh`.
No new layout or linearity qualification.

## Pass 306 — connected two-level proportionality and selected harmonic fits

Existing10mV job5771 completes401ns; full audit and analysis/comparison82008
complete. Reused zero control matches retained artifacts; source before/after
hashes agree. Exact amplitude-only deck change verified. No rerun on observation
timeout. Over240–400ns outputs36.03022/40.36233mV, normalized gain ratios
0.999603/0.999621 relative to1mV:−0.00345/−0.00329dB. Phase changes0.00525degree.
Selected-finger minimum headroom0.43686V; accounted power137.89093mW, change
0.036uW. Last-window amplitude changes0.000753%/−0.000521%.

Added targeted second/third IF harmonic analysis, fitted jointly with fundamental,
LO harmonics and RF nuisance tones; known synthetic noncoherent signal validates
harmonic separation. At10mV third components0.630/1.035uV over240–400ns and
0.647/1.042uV over320–400ns. Second components and1mV harmonics are near
the no-tone/window-dependent fitting floor. Do not claim precise low-level THD
from them. Filtering suppresses higher IF harmonics; this is not broadband RF
linearity, intermodulation, receiver noise or EVM evidence. Only3.2/1.6IFcycles
are present in these windows.

Retain bypass baseline. Two-level proportionality is supported, but compression
point, stronger-signal behavior and modulated quality remain open. Next whole-chip
risk review should revisit autonomous timing and ADC/reference regulation before
an indefinite RF amplitude sweep. No live jobs or new layout.

Reproduce pass305 audit/analysis/comparison, then:
`python3 projects/programmable_transceiver_platform/verification/analyze_bb_harmonics.py`.
[Comparison](evidence/bb-level10-comparison.json),
[selected harmonics](evidence/bb-level10-harmonics.json).

## Pass 307 — signed separate-source offsets remove reduced buffer failure

Returned to autonomous timing after RF proportionality work. Retained finite-source
buffer fixture aborts663.2ns despite an electrically separate FB source. Changed
only that source delay100ns→99.999ns or100.001ns; retained reference PWL,
50ohm source resistance, transistor buffer, loads, reset, Gear2/UIC and800ns/2ps.
Both cases complete800ns, returncode0, no timeout or timestep error, no backward
or equal exported timestamps. Exact deck reversal and before/after model/source
and artifact hashes checked. Jobs23464 and case checker are terminal.

This is evidence of event-timing sensitivity in the reduced circuit. It narrows
the numerical hypothesis beyond removing FB entirely, but does not expose the
internal event queue or prove full-loop failure has the same cause. It does not
validate an arbitrary phase offset as a chip fix: a real PLL must tolerate the
required relative phases. Next revisit the active PFD boundary with this controlled
mechanism and actual clock-edge comparisons before any long full-loop rerun.
Autonomous lock, noise and cold startup remain unqualified. No new layout/live jobs.

Reproduce from repository root:
- `bash projects/programmable_transceiver_platform/verification/run_buffer_event_offset.sh`
- `python3 projects/programmable_transceiver_platform/verification/check_buffer_event_offset.py`

Scratch `scratch/transceiver-buffer-event-offset`; retained baseline
`scratch/transceiver-buffer-finite-source`.
[Evidence](evidence/buffer-event-offset.json).

## Pass 308 — signed event-offset test reaches active PFD boundary

Added a matched−1ps/+1ps FB-delay test to the retained actual PFD/pump/filter
mixed PWL/PULSE fixture. It previously aborts637.7ns; same800ns/2ps Gear2/UIC
history now changes only FB delay. Unlike pass307, FB now affects the circuit.
Full oscillator/divider remains absent, so successful completion cannot establish
acquisition or autonomous timing. Both pulse waveforms remain present.

Runner checks exact baseline/source hashes, records artifacts and before/after
source provenance. Checker verifies reversal of the declared delay, full horizon,
time ordering and actual reference-minus-feedback threshold crossings. Pump
current integration uses complete reference cycles, interpolating endpoints only
when exported times are strictly increasing; repeated timestamps remain reported.
No assumption that a requested1ps offset equals measured transistor response.
Compilation and pending-state handling pass; no completed candidate result yet.

Job61371 is live in early case; do not restart on timeout observing it. Scratch
`scratch/transceiver-pfd-event-offset`. Next observe case completion then run
`python3 projects/programmable_transceiver_platform/verification/check_pfd_event_offset.py`
from repository root. Reproduction only for deliberate new run:
`bash projects/programmable_transceiver_platform/verification/run_pfd_event_offset.sh`.
Retained PLL unchanged. No new layout or autonomous qualification.

## Pass 309 — actual PFD pin phase differs from requested source skew

Continued61371; early−1ps active PFD/pump/filter case completes800ns with
400469 rows and verified1ps REF-minus-FB threshold crossings. No backward or
repeated printed timestamps. Over11 complete reference cycles202.45–765.65ns,
net pump charge into filter+244.37fC; late control range1.07710–1.10620V.
Positive charge is an observation, not proof of correct autonomous correction.
Matrix source-after checks await late case; partial case audit remains incomplete.

While waiting, analyzed retained completed buffered±10ps cases. Actual reference
buffer delay127.754ps makes PFD REF-minus-FB phases+137.754/+117.755ps,
despite source phases+10/−10ps. Mean pump currents over complete cycles are
+0.37284/+0.38134uA. Do not interpret these as symmetric near-zero PFD tests or
static lock offset. Source/artifact checks, named vectors and finite monotonic
waveforms verified. Updated startup contract to require actual pin-phase accounting.

New analysis reproduction from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_pfd_buffered_phase.py`.
[Evidence](evidence/pfd-buffered-phase.json).

Job61371 continues late+1ps case. Next observe it and rerun
`python3 projects/programmable_transceiver_platform/verification/check_pfd_event_offset.py`.
No restart, PLL circuit change or new layout. Autonomous lock/noise still unqualified.

## Pass 310 — both active signed-offset cases complete; persistent pump charge

Continued61371 to terminal completion. Both early/late cases reach800ns;
source-before/after hashes and exact declared1ps source-delay changes verify.
Measured REF-minus-FB is+1ps/−1ps, no backward or repeated exported times.
This extends pass307's event sensitivity to active PFD/pump/filter, but still
excludes autonomous VCO/divider feedback and does not identify simulator internals.

Added per-reference-cycle charge accounting. Trapezoidal integrations include exact
interpolated edge endpoints; positive plus negative charge equals net, and sum of
11 complete cycles agrees with the full measurement window. Early net charge
22.239→22.192fC/cycle, late22.287→22.241fC/cycle. Full-window charges
244.369/244.902fC. Early example first cycle has+53.799/−31.560fC components.
Both deliver positive current~0.434uA and increase CTRL, with only a small change
from2ps phase difference. This is a persistent dynamic bias in the tested history,
not merely one partial pulse at the integration boundary.

Next isolate dynamic pump charge at fixed control voltage and observe real switch
controls; do not resize devices based only on static up/down DC current matching.
Neither near-zero charge imbalance nor buffer delay alone proves a PLL cannot
lock: a nonzero equilibrium phase may compensate it. Acquisition/stability/noise
remain unqualified; arbitrary timing offsets are not adopted as a chip remedy.
Whole-chip ADC regulation, RF noise and differential interface, wired recovery,
host timing and coexistence obligations remain open. No live jobs or new layout.

Reproduce checker:
`python3 projects/programmable_transceiver_platform/verification/check_pfd_event_offset.py`.
[Completed case and cycle evidence](evidence/pfd-event-offset.json).

## Pass 311 — fixed-control dynamic pump diagnostic running

Launched a controlled extension of completed active±1ps cases: add ideal1.08V
clamp at CTRL, retaining PFD, pump, filter, precharge, biases, clocks and800ns/2ps
settings. The clamp is a characterization fixture, not a proposed PLL component
or loop repair. It separates dynamic pump charge from changing output compliance.
Added voltage probes for XCP.UPB/PS/NS and BPCP/BNCP plus clamp current.

Shared case checker has `--clamped` mode, distinct artifacts, exact reversal of
clamp/probe additions, fixed-voltage check and internal-node ranges. Existing
complete-cycle positive/negative/net charge accounting applies; requested phase
is still checked at actual REF/FB nodes. Compilation/pending-state handling pass.
No completed case or charge comparison yet. Retained PLL circuitry unchanged.

Live job42534 in early case. Scratch `scratch/transceiver-pump-clamped`; continue
existing job rather than restart. When cases finish, run from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_pfd_event_offset.py --clamped`.
Reproduction only for a deliberate new run:
`bash projects/programmable_transceiver_platform/verification/run_pump_clamped.sh`.
Next compare cycle charge and actual switch timing against pass310; do not infer
mirror sizing corrections from DC current or upstream UP/DN alone. No new layout.

## Pass 312 — first fixed-control case retains charge imbalance

Continued42534; early case completes800ns. Partial audit verifies declared
clamp/probe additions, artifact hashes, finite ordered data and1.08V clamp.
Late case remains live; matrix source-after checks and paired conclusion pending.

Added actual gate-command timing and charge partition using interpolated1.65V
crossings. Integrate total current with intervals split at every command crossing;
all four bin charges close to full-cycle current integral. Categories are logic
labels, not channel conduction states or a device-level cause attribution.

Early case net charge22.3948fC/cycle over11cycles, compared with~22.2fC in the
unclamped case. Thus filter-voltage drift does not account for the imbalance in
this test. Mean UP high810.711ps, DN high823.274ps, actual UPB low824.572ps;
PMOS/NMOS command duration difference only1.298ps. Per-cycle charge bins:
both commands+29.1975fC, PMOS-only+0.5941fC, NMOS-only−0.7762fC,
neither−6.6206fC. Most positive charge coincides with both commands, with later
negative contribution. Actual PMOS VSG spans−1.100–1.778V, NMOS VGS
−0.0414–2.2495V. These dynamic quantities require device-current/headroom
inspection; do not choose pulse-width correction or mirror sizing from bins alone.

Reproduce from repository root after each completed case:
- `python3 projects/programmable_transceiver_platform/verification/check_pfd_event_offset.py --clamped`
- `python3 projects/programmable_transceiver_platform/verification/analyze_pump_switch_timing.py`

[Partial timing/charge evidence](evidence/pump-switch-timing.json).
Live late case42534; no restart, no new layout or autonomous PLL claim.

## Pass 313 — paired clamp result verified; device observation replay active

Job42534 completes both cases; full clamped audit and timing analysis now pass
before/after source hashes, artifacts and declared changes. Net charge22.3948/
22.4435fC per reference cycle persists at fixed1.08V. Early/late PMOS-minus-NMOS
command-width differences1.298/3.337ps, with both-command charge29.198/29.328fC
and neither-command charge−6.621/−6.666fC. Categories locate current in time,
not physical conduction or causality. No dynamic bias correction selected.

Launched observation-only early replay adding id,VDS,VDSAT,VGS,VTH for the
PMOS mirror/switch and NMOS switch/mirror. Circuit, source waveforms,800ns/2ps
settings and clamp unchanged. Checker requires original13 exported columns to
reproduce exactly before reporting signed device ranges. Model id is not treated
as complete terminal/displacement current. This is intended to discriminate
mirror headroom and switch/internal-node charging before any transistor change.

Live job5103; scratch `scratch/transceiver-pump-device`. Pending checker and
Python compilation pass; no device results yet. When terminal run:
`python3 projects/programmable_transceiver_platform/verification/check_pump_device.py`.
Reproduction only for deliberate new run:
`bash projects/programmable_transceiver_platform/verification/run_pump_device.sh`.
Retained PLL unchanged. No new layout or autonomous qualification.

## Pass 314 — pump device probes reproduce baseline and expose asymmetric transients

Job5103 completes800ns; checker38794 and follow-up analysis10390 terminal.
All original13 waveform columns reproduce exactly after adding20 MOS probes.
Source/deck/artifact checks pass. Verified model VDS/VGS against physical terminals:
PMOS probes are sign-inverted, NMOS probes same sign, errors~1e-15V. This prevents
incorrect headroom interpretation based on named-source polarity alone.

PMOS output-switch reported id peaks362.7uA versus NMOS83.69uA. Mirror peaks
34.39/27.48uA; nominal external references20uA. Mirror below-VDSAT time is largely
idle and must not be presented as a whole-circuit failure; output switching drives
internal nodes through large excursions. Conditional forward/inversion diagnostics
and signed probe ranges retained for inspection, not silicon qualification.

Over11 complete cycles, reported model id integrals per cycle are54.865fC(PMOS
mirror),54.841fC(PMOS switch),36.808fC(NMOS switch),32.554fC(NMOS mirror).
The18.033fC PMOS-minus-NMOS switch id integral differs from measured pump charge
22.395fC by4.362fC. Therefore model id alone is inadequate for full terminal-charge
attribution; do not call that remainder displacement, diode current or a solver error
without further evidence. Next zero-volt terminal branch sensors should close KCL
and distinguish where a real circuit change could help. No sizing/steering change
adopted. No live jobs or new layout; autonomous timing remains open.

Reproduce from repository root:
- `python3 projects/programmable_transceiver_platform/verification/check_pump_device.py`
- `python3 projects/programmable_transceiver_platform/verification/analyze_pump_device.py`

[Audit](evidence/pump-device.json), [analysis](evidence/pump-device-analysis.json).

## Pass 315 — terminal branch sensors launched; whole-chip scope retained

Added two zero-volt sensors to the instrumented clamped pump: VP from PMOS drain
node to OUT, VN from OUT to NMOS drain node. Positive VP delivers charge to OUT;
positive VN removes it. Existing MOS probes remain. Generated deck inlines an
exactly checked pump cell with only these drain-node splits/sensors; production
cell unchanged. Clock/input/filter/clamp800ns history unchanged.

New checker reverses the declared cell/probe changes, checks source/artifact hashes,
zero drops and instantaneous/integrated KCL VP−VN=VSENSE. It records original-vector
identity or interpolated waveform differences rather than assuming sensor insertion
is numerically invisible. Cycle-integrated terminal charges will be compared with
model id using the same reference-cycle bounds. Compilation and pending-state
checks pass. No terminal-current result yet.

Job97139 live; scratch `scratch/transceiver-pump-branch`. Continue existing process.
When terminal, run from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_pump_branch.py`.
Reproduction only for deliberate new run:
`bash projects/programmable_transceiver_platform/verification/run_pump_branch.sh`.

Whole-chip review: charge attribution must lead to a bounded pump/loop decision,
not replace autonomous acquisition/noise verification. RF differential input,
complete RX/TX/converter quality, ADC reference regulation, wired recovery, host
physical timing and whole-chip bias/pad/power integration remain major obligations.
No layout work or readiness claim; retained PLL unchanged.

## Pass 316 — terminal KCL closes; reverse model-current orientation resolved

Job97139 completes800ns; terminal audit passes source/deck/artifact checks and
zero sensor drops. VP−VN matches VSENSE with max residual1.63e-19A. Over11
complete cycles, PMOS delivers54.84102fC, NMOS withdraws32.44624fC, net
22.39478fC/cycle. Baseline vectors are not bit-identical: largest interpolated
voltage difference23.47uV at UPB, while net charge changes only~0.0000014fC.
This numerical replay difference is recorded rather than concealed.

Corrected interpretation of pass314 discrepancy: NMOS output switch briefly has
negative model VDS while reported id retains the other current convention.
Integrating sign(VDS)*id gives32.44609fC withdrawal, only0.0001495fC from
terminal sensor. PMOS counterpart differs0.0001838fC. Thus nearly all previous
4.36fC discrepancy comes from orientation, not evidence of missing displacement
charge. Full-cycle agreement does not equate model id with instantaneous terminal
current. The real terminal charge imbalance remains~22.39fC/cycle.

Next bounded experiment is complementary dummy current steering to keep mirror
internal nodes from returning to idle rails. Start with separate ideal dummy-node
clamp as a diagnostic, preserve actual FET switches/drivers, and compare charge,
loading and power before implementing a physical dummy bias. Do not claim such
an ideal clamp belongs in the final PLL. Full autonomous timing remains open.
No live jobs or new layout.

Reproduce:
`python3 projects/programmable_transceiver_platform/verification/check_pump_branch.py`.
[Evidence](evidence/pump-branch.json).

## Pass 317 — complementary dummy-steering diagnostic launched

Built generated candidate from the verified branch-sensor fixture. Adds PMOS
mirror-node→DUMMY switch gated by UP, NMOS DUMMY→mirror-node switch gated by
new inverter output DNB; main output switches and branch sensors remain. Two dummy
FETs plus two-FET inverter, all PDK devices. Separate ideal DUMMY1.08V source
is explicitly a diagnostic placeholder; retained output clamp remains1.08V.
No production pump file changed or ideal dummy reference adopted as implementation.

Matched control adds only unloaded dummy source and supply/dummy-power probes.
This rerun is needed because retained artifacts did not save total VDIV current.
Candidate also saves DNB. Both retain800ns/2ps and early−1ps clock history.
Checker reverses declared edits, checks model/artifact provenance, zero clamp error,
output KCL, complete-cycle charge, internal ranges and supply/dummy power. Control
records baseline numerical agreement. Candidate will be assessed for added load
and power as well as charge reduction; no presumed improvement or threshold.

Job87020 live in control case. Scratch `scratch/transceiver-pump-steering`.
Compilation/pending checks pass; no result yet. Continue existing job. When cases
finish, run from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_pump_steering.py`.
Reproduction only for deliberate new run:
`bash projects/programmable_transceiver_platform/verification/run_pump_steering.sh`.
No new layout; PLL acquisition/noise and physical dummy-bias implementation remain open.

## Pass 318 — steering control verified; dummy-driver demand checks added

Continued87020. Control case completes800ns and verifies exact declared additions,
artifact hashes, clamp voltages, monotonic waveforms and KCL. Original vectors
are not bit-identical, but maximum interpolated voltage difference is1.84e-13V.
Complete-cycle net charge22.39478fC reproduces baseline. Main VDIV-source power
266.581uW; unloaded dummy source current/power exactly zero; output clamp absorbs
0.47239uW. These are named-source measurements for the reduced PFD/pump fixture,
not total PLL or chip power.

Extended checker to report dummy-source peak supply and sink currents separately,
supplied/absorbed charge per cycle, and signed dummy power. This prevents small
net average dummy current from hiding transient driver demand. Final paired report
will include main-supply power difference and net pump-charge ratio. No assumption
that an ideal dummy-voltage source is physically implemented.

Steering case now live under87020; matrix after-run provenance and candidate
performance pending. Scratch `scratch/transceiver-pump-steering`.
Next finish existing run, then:
`python3 projects/programmable_transceiver_platform/verification/check_pump_steering.py`.
[Partial control evidence](evidence/pump-steering.json).
No candidate adoption, new layout or autonomous PLL qualification.

## Pass 319 — steering reduces nominal imbalance with explicit bias/power cost

Job87020 terminal; both control/candidate800ns cases and full checker24732 pass.
Hashes, exact declared topology changes, clamp voltages, output KCL and cycle
integration verified. Added per-cycle reporting: charges repeat consistently over
all11 cycles, rather than a favorable average masking drift.

Net output charge changes+22.3948→−3.69615fC/cycle:83.5% lower magnitude,
sign reversed. Candidate PMOS delivery20.163fC, NMOS withdrawal23.859fC.
Main-source power266.581→337.597uW, additional71.015uW. PS range contracts
1.329–3.301V→1.268–1.750V; NS−0.0053–1.093V→0.865–1.104V.
Mirror bias-node swings also decrease. This controlled comparison supports the
steering direction but does not isolate loading versus internal-node stabilization.

Dummy source supplies123.39uA peak and sinks134.45uA peak; per-cycle supplied/
absorbed charges22.575/34.231fC, average source power−0.2459uW (absorbing).
Near-zero average would conceal a real transient bias-driver requirement. Both
output and dummy clamps remain ideal; no physical dummy bias is implemented.

Disposition: retain original pump; keep steering as candidate for signed-phase
response and finite/actual dummy-bias tests. A near-zero charge improvement alone
does not prove useful phase-detector slope, acquisition, compliance or noise.
Next use phase offsets on both sides of the candidate operating point before
further tuning. No live jobs or new layout.

Reproduce:
`python3 projects/programmable_transceiver_platform/verification/check_pump_steering.py`.
[Completed evidence](evidence/pump-steering.json).

## Pass 320 — signed steering-pump phase response launched

Generated two cases from completed steering candidate, changing only ideal FB
source delay99.999ns→99.7ns/100.3ns. All actual PFD/pump/dummy FETs, ideal
output/dummy1.08V clamps, loads and800ns/2ps history retained. This tests opposite
phase-error directions before spending effort on a physical dummy driver.

Checker verifies exact delay-only reversal, model/artifact hashes, actual REF-minus-
FB threshold offsets+300/−300ps, clamp values, branch KCL and per-cycle charge.
It reports whether charge sign is consistent with positive VCO tuning; it does not
assert correct polarity by construction or call either case autonomous acquisition.
Compilation and pending-state checks pass; no completed phase case yet.

Live job60897, early-feedback case. Scratch `scratch/transceiver-pump-steering-phase`.
Continue existing run, then from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_pump_steering_phase.py`.
Reproduction only for a deliberate new run:
`bash projects/programmable_transceiver_platform/verification/run_pump_steering_phase.sh`.

Scope review: candidate charge reduction still depends on ideal dummy bias and
output clamps; actual loop stability/acquisition and intrinsic noise remain open.
The RF/converter, wired recovery, host timing and full-chip integration obligations
remain independent. No pump adoption or new layout.

## Pass 321 — early-feedback steering correction verified

Continued60897 without restart. Early case completes800ns; partial checker98582
verifies exact delay-only change, artifact hashes, named finite ordered vectors,
clamp voltages and branch KCL. Measured REF-minus-FB+300ps across12 edges.
All11 full cycles remove~9.80619fC from output, mean pump current−0.19153uA.
That direction is consistent with slowing a positive-gain VCO when feedback leads.
This one-side diagnostic is not acquisition or a full phase-response qualification.

Implementation inventory now links completed pump/terminal evidence and marks
steering as an unadopted candidate requiring ideal dummy bias. It explicitly notes
that retained full-loop failure used an older RF-load fixture; latest bypassed RX
and candidate pump have not been connected in a qualified autonomous loop.
Inventory audit still reports whole-chip schematic incomplete/layout gate closed.

Late-feedback case remains active under60897 in
`scratch/transceiver-pump-steering-phase`. Next observe completion and rerun
`python3 projects/programmable_transceiver_platform/verification/check_pump_steering_phase.py`.
Terminal matrix source-after checks remain pending. No new layout.
[Partial evidence](evidence/pump-steering-phase.json).

## Pass 322 — phase response and finite dummy-source sensitivity

Both steering-pump phase cases complete800ns with verified source/artifact hashes,
exact delay-only changes, measured +/-300ps REF-minus-FB offsets and branch KCL.
Early feedback removes9.806fC/cycle; late feedback adds2.577fC/cycle. Both signs
match correction for a positive-gain VCO. Ideal clocks and clamps remain; this
is neither acquisition nor proof of a static equilibrium.

Continued existing finite-source job without restart (runner PID3479531,
Docker client3479487). The resistive case completes800ns and verifies exact
1kohm-only fixture change, phase, clamp and terminal-current consistency.
Pump charge is -7.498fC/cycle versus ideal-dummy -3.696fC/cycle; dummy node
ranges0.984..1.172V. This is material bias-impedance sensitivity, not evidence
that1kohm bounds the eventual driver. Optional precharged32-unit MIM reservoir
case is still running; source-after checks await terminal matrix completion.

From repository root, next inspect existing process/artifacts in
`scratch/transceiver-pump-dummy-impedance`, then run
`python3 projects/programmable_transceiver_platform/verification/check_pump_dummy_impedance.py`.
Updated risk decision and implementation inventory. Production pump remains
unchanged, full autonomous loop unqualified, whole-chip schematic incomplete.
No layout work. [Phase evidence](evidence/pump-steering-phase.json),
[finite-source evidence](evidence/pump-dummy-impedance.json).

Pass322 terminal update: both finite-source cases now complete800ns; source
before/after hashes and all case artifacts verify. Added an independent resistor
Ohm-law/source-current check (max residual5.36e-19A). The explicit32-unit MIM
reservoir reduces dummy swing to1.068768..1.086374V and charge to-3.945896fC
per cycle, close to ideal-dummy -3.696155fC. Bias-source current peaks fall to
11.232uA supply/6.374uA sink. This supports testing an actual transistor tracking
driver plus reservoir; it does not qualify cold startup (reservoir precharged),
control-voltage range, mismatch or autonomous loop performance. All jobs in this
experiment are terminal; no pending process to restart. Next use the verified
reservoir fixture as the control for a physical-driver experiment, preserving the
original production pump until integration evidence justifies adoption.

## Pass 323 — transistor dummy follower DC sizing screen

Reused existing actual two-stage reference followers rather than inventing a
new amplifier: NMOS-input and complementary versions, scale0.0625/0.25,
target0.9/1.08/1.3V, load -150..+150uA in5uA steps. All12 DC sweeps complete;
checker verifies source/model hashes, artifact hashes, exact coverage, finite
vectors and61-point load grids. These are sizing scenarios, not physical bounds.

At1.08V, small NMOS-input follower has -10.388mV no-load tracking error,
0.467mW total VDD-source power including ideal-current bias branches, and
maximum28.42ohm incremental resistance over +/-15uA. Larger scaling lowers
resistance to6.71ohm but leaves essentially the same offset and costs1.473mW.
Small complementary candidate is worse here: +18.574mV offset and147.47ohm.
Small versions lose regulation at an extreme load, with forced-current solutions
outside supply rails; those are failure observations, not usable operating points.

Retain small NMOS-input topology for the next connected reservoir/pump transient,
not production adoption. Verify compensation, bias startup, charge, both load
histories and supply power; do not infer stability from low DC impedance. Ideal
20uA bias generators remain. Uniform scaling uses fractional multiplicities;
normalize into realizable widths/fingers and verify equivalence before physical
implementation. No noise, mismatch, process or layout qualification here.

Reproduction from repository root:
`bash projects/programmable_transceiver_platform/verification/run_dummy_driver_dc.sh`
then `python3 projects/programmable_transceiver_platform/verification/check_dummy_driver_dc.py`.
Scratch `scratch/transceiver-dummy-driver-dc`; all jobs terminal.
[Evidence](evidence/dummy-driver-dc.json). Whole-chip schematic remains incomplete.

## Pass 324 — connected transistor dummy follower launched

Replaced ideal dummy1.08V source and1kohm resistor in verified reservoir fixture
with the existing NMOS-input two-stage FET follower atS=0.0625. Its input senses
CTRL; output supplies the actual complementary pump switches and32-unit MIM
reservoir through a zero-volt current probe. Separate3.3V supply exposes driver
power. Two20uA ideal bias sources feed actual diode-connected bias FETs; bias
reference generation remains missing. Output clamp, ideal PFD clocks, original
800ns/2ps horizon and reservoir precharge are unchanged. Compensation remains
the existing scaled RC/CC candidate, not a qualified stability design.

Runner preserves baseline/model hashes and records terminal artifacts. Checker
verifies exact declared replacement, completion, phase, output clamp, pump KCL,
and measures dummy range, charge per complete cycle, bias state and driver power
in early/late windows. No performance threshold invented from this run; differing
windows or excursions will determine whether to diagnose settling/compensation.
Pending-state checker passes; simulation confirmed live under session39499,
runner PID3492311 (Docker client3492251). No completed transient result yet.

Next observe the existing job in `scratch/transceiver-pump-follower`, then run
`python3 projects/programmable_transceiver_platform/verification/check_pump_follower.py`
from repository root. Reproduction for a deliberate new run:
`bash projects/programmable_transceiver_platform/verification/run_pump_follower.sh`.
No production pump adoption, cold-start/PLL/noise claim or layout work.

## Pass 325 — connected follower response verified; whole-chip risk review

Continued existing session39499 without restart; run completes800ns, exit0.
Checker verifies before/after source/model hashes, artifact hashes, exact fixture
replacement, finite monotonic vectors, input phase, clamp and terminal KCL.
Early200..450ns and late550..790ns windows show consistent cycle charge:
-4.50851fC, versus -3.94590fC with1kohm ideal source/reservoir. Dummy range
1.057985..1.076578V; driver supply power0.46716mW; source/sink peaks about
5.87/4.76uA. No secular change is apparent between these selected windows;
this is not a general stability proof or a cold-start test.

Retain follower as a connected diagnostic candidate. Next verify signed phase
correction and control-voltage range with this real follower, then return to
autonomous-loop integration. Avoid indefinite local offset optimization: intrinsic
clock noise, RX/converter quality, modulated TX, wired recovery, real bias,
interfaces and coupled loading remain independent high risks. Inventory/risk
review updated; layout gate remains closed. All jobs in this pass terminal.
[Evidence](evidence/pump-follower.json).

## Pass 326 — signed-phase screen reuses verified transistor follower

Extended existing steering-phase runner/checker with explicit `--follower` mode,
retaining default ideal-dummy behavior. New cases change only FB delay to99.7ns
and100.3ns in completed follower fixture: actual follower, bias diodes, steering
switches, reservoir, precharge and output clamp remain. Each requests800ns/2ps.
This tests whether previous +/-300ps correction signs survive physical dummy
loading; no autonomous clock or startup claim follows from these forced clocks.

Checker keeps exact delay-only reversal, hashes, measured phase, output clamp,
terminal KCL and full-cycle charge. Follower mode measures dummy excursions and
separate driver power rather than asserting an ideal dummy voltage. Compilation,
pending follower check and default completed-evidence replay pass. Reused shared
infrastructure instead of duplicating another phase checker.

New simulation confirmed live: session28181, runner PID3496082, Docker3496019;
early case progressing in `scratch/transceiver-pump-follower-phase`. No completed
case yet. Next observe existing job and run from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_pump_steering_phase.py --follower`.
Reproduction only for a deliberate new run:
`bash projects/programmable_transceiver_platform/verification/run_pump_follower_phase.sh`.
If correction signs survive, next control-range screen then autonomous integration;
otherwise inspect bias/charge response before adoption. Layout gate remains closed.

## Pass 327 — control-range experiment grounded in loaded VCO evidence

Verified existing phase job remains live (session28181, runner3496082); early
case progressing past399ns, requested800ns. Pending checker reports no completed
case; no restart. Previous pass was implementation progress, not qualification.

Inspected retained split-ring tuning evidence: at fixed regeneration1.08V,
CTRL0.98V gives2.26146GHz,1.08V gives2.50075GHz,1.16V gives2.63655GHz.
These older loaded fixtures motivate diagnostic clamp endpoints0.98/1.16V
around nominal operation. They do not establish current full-RX-load tuning,
continuous monotonicity, autonomous capture or process bounds. The unsplit-ring
local slope is different and must not be substituted into split-ring loop design.

Prepared `analog/pll/pump_control_range_screen.py` with both phase signs at both
endpoints. Only clamp, associated filter/reservoir precharges and feedback phase
change from verified follower baseline; actual circuits remain. Hash/progress/
terminal provenance retained. Script compiles; deliberately not launched before
nominal signed-phase evidence is available. Matching control-range checker and
launch wrapper still required. Next finish observing28181 and run shared checker
with `--follower`; launch endpoint tests only after interpreting nominal results.
No new circuit-performance claim or layout work.

## Pass 328 — early-phase follower correction verified; endpoint checker ready

Continued session28181. Early case completes800ns with measured REF-minus-FB
+300ps, exact delay-only deck change, artifact hashes, finite ordered vectors,
output clamp and branch KCL verified. Mean charge -10.53249fC/cycle has the
expected correction direction for positive VCO gain; mean pump -0.20571uA,
dummy1.060218..1.079171V, driver0.467162mW. Late case is running under the
same session/runner3496082; full matrix source-after checks remain pending.
[Partial evidence](evidence/pump-follower-phase.json).

Shared phase checker now supports `--control-range`, verifying explicit0.98/1.16V
clamp and associated precharge changes independently of manifest replacement
text, exact phase changes, unique four-case coverage and measured clamp values.
Prepared `verification/run_pump_control_range.sh`. Compilation, existing default
terminal-evidence replay and follower partial-evidence check pass. Endpoint branch
has not yet been exercised against simulated endpoint artifacts; no endpoint
performance claim. It remains unlaunched pending late nominal phase result.

Next observe existing28181 and rerun shared checker `--follower`. If both signs
are useful, run endpoint wrapper then checker `--control-range`. No autonomous
acquisition, startup, intrinsic-noise or layout qualification claimed.

## Pass 329 — conditional loop-filter sanity audit during phase simulation

Verified ongoing session28181/runner3496082; late-feedback case progressing,
not restarted. Inspected autonomous-loop construction and retained filter:
CP2pF in parallel with10kohm/50pF series branch, divide128. Existing fixture
still uses the original pump and older RF load; follower has not been integrated.

Added `verification/analyze_pll_local_model.py`, an explicitly conditional averaged
model using ideal-dummy signed-phase charge slope20.639uA equivalent pump current
and measured split-ring frequency secants. Computes L(s)=Ieff*KvHz*Z(s)/(128*s),
Z(s)=(1+s*R*CZ)/(s*(CP+CZ)+s*s*R*CP*CZ), unity gain and closed-loop cubic poles.
Inputs are hashed; filter parameter identity checked. Across intervals0.98..1.16V,
unity gain0.497..0.710MHz and phase margin53.9..61.0deg; averaged poles are
left-half-plane. Upper1.24..1.3V interval falls to32.9deg as tuning gain declines.

This narrows one bird's-eye concern: no obvious feedback-sign/filter-value error
in this simplified model, so do not resize the filter merely because the full
transistor simulation aborted. It does NOT establish actual loop stability:
source slopes are sparse/older fixtures, follower input load and sampled delay
are omitted, and acquisition/offset/noise/process/parasitics remain absent.
Next replace ideal-dummy slope with completed follower data, then use this only
as context for actual autonomous integration. No filter adoption or lock claim.
[Conditional evidence](evidence/pll-local-model.json).

## Pass 330 — both follower correction signs verified; endpoint tests launched

Phase session28181 is terminal. Both800ns cases verify full source-before/after
provenance, artifacts, exact phase-only changes, measured +/-300ps, clamp and KCL.
Early charge -10.53249fC/cycle, late +1.61780fC/cycle; both expected directions.
Late dummy1.054548..1.076214V; driver power0.467162mW. The smaller positive
correction and nonzero nominal charge remain; this is not a measured equilibrium
or autonomous acquisition. [Evidence](evidence/pump-follower-phase.json).

Extended averaged audit with explicit `--follower` input and measured phase
separation: equivalent current20.25049uA, conditional phase margins53.6..60.7deg
across0.98..1.16V intervals. Original-mode evidence replay remains consistent.
These averaged results retain all previously stated omissions and do not prove
stability. [Model](evidence/pll-local-model-follower.json).

Launched four endpoint cases via `run_pump_control_range.sh`: session92006,
scratch `scratch/transceiver-pump-control-range`. First0.98V early case running;
pending shared checker `--control-range` passes. Next observe this existing job,
then evaluate completed endpoint charge signs, dummy tracking and driver power.
Production pump unchanged; whole-chip schematic still incomplete/layout closed.

## Pass 331 — autonomous follower-loop deck prepared with exact reversal

Endpoint runner3503271/session92006 remains live; first0.98V early case active,
no completed case yet. Continued observation rather than restart.

Added `verification/prepare_follower_loop.py`; executed successfully. Reads hashed
retained failing buffered autonomous loop and completed follower fixture, extracts
actual steering-pump cell and follower/reservoir circuitry, and substitutes them
into the loop. Exact reversal reproduces baseline byte-for-byte. Structural checks
retain actual divider/buffer feedback into PFD, follower sensingCTRL and3201ns/2ps
UIC history; no ideal VFB or output VCLAMP enters this generated deck. Added
observations expose dummy voltage, driver power/bias and pump terminal currents.

Prepared `scratch/transceiver-closed-loop-follower-prepared/closed.spice` and
manifest; NOT simulated or adopted. Older RF load deliberately retained to isolate
this substitution; it is not the final receiver integration. Execution still needs
a runner with actual include/model provenance, completion/lock checks and endpoint
result review. Initial seeds and ideal bias/supply/reference sources remain.
[Preparation evidence](evidence/follower-loop-preparation.json).

Next continue92006, check `--control-range`, then decide whether this controlled
loop candidate is ready to execute. No new layout or autonomous-performance claim.

## Pass 332 — first control endpoint verified; autonomous execution preflight

Existing endpoint session92006 continues without restart.0.98V early case
completes800ns and passes the new control-range checker: exact clamp/precharge/
phase-only reversal, measured +300ps, output0.98V, artifact hashes, ordered finite
vectors and branch KCL. Charge -12.19006fC/cycle is the expected correction sign;
dummy0.960516..0.980107V, driver0.46072mW. Remaining three cases and terminal
source-after check pending. [Partial evidence](evidence/pump-control-range.json).

Extended existing buffered-loop runner with `--prepared` and `--preflight`,
keeping original generation path. Added `run_closed_loop_follower.sh`; ran with
`--preflight` inside pinned container. Prepared deck hash/exact-reversal manifest
verified;19 actual include/dependency files resolve and are hashed. No ngspice
elaboration/transient occurred. Output `scratch/transceiver-closed-loop-follower`
has preparation manifest and deck only, no result or active loop job. Execution
will re-enumerate dependencies and retain before/after/artifact checks.

Next observe current endpoint92006/runner3503271 and check `--control-range`.
Autonomous execution remains contingent on interpreting full endpoint evidence;
then invoke loop wrapper without `--preflight`. Completion and acquisition checks
still needed before any loop claim. No new layout.

## Pass 333 — autonomous checker prepared; startup range oversight identified

Endpoint session92006/runner3503271 remains live in0.98V late case; no restart.
Extended existing autonomous checker with explicit `--follower` mode: prepared
deck identity, absence of diagnostic VFB/VCLAMP,27 named vectors, completion,
ordered phase history, pump KCL, dummy range and driver power. Pending-state check
passes. Original-mode replay verifies retained full-loop failure and partial
waveform diagnostics; no regression into claiming completion or lock.

That replay reveals a scope mistake in the planned range screen: old-loop CTRL
reaches1.259887V during100..200ns, above proposed1.16V endpoint. Frequency
bracketing is not acquisition excursion coverage. Keep current four cases as
useful diagnostics, but add a1.30V upper diagnostic before claiming startup-range
coverage. Even1.30V is not a bound on a new loop or process. Lower excursions
and full-history extrema also require checking before interpreting coverage.
[Range-gap record](evidence/pump-control-range-gap.json).

Next finish current endpoint evidence, then extend upper coverage or explicitly
carry that unqualified excursion into controlled autonomous diagnosis. Do not
silently interpret0.98..1.16V success as startup compliance. No new simulation
of prepared autonomous deck; completed-waveform follower checker branch remains
untested until that actual run. Layout gate remains closed.

## Pass 334 — low-control phase offset limitation found

Full retained autonomous waveform audit (0..2762.5ns, and separately after100ns)
measures CTRL minimum1.024213V at1380.563ns and maximum1.262741V at227.391ns.
Selected0.98/1.30V diagnostics cover this historical record only; they do not bound
new-loop excursions. Added extrema/time reporting to shared autonomous checker.

Continued endpoint92006. Both0.98V cases complete800ns; exact changes, artifacts,
phase/clamp and KCL verify. Early charge -12.19006fC/cycle; late charge
-0.181953fC/cycle, wrong sign for the selected300ps lag. Do not round this to
zero or claim bipolar correction at0.98V. Nominal1.08V late result was positive,
so control dependence matters. A shifted balance phase could still permit lock;
these two phases do not establish impossibility or an equilibrium. Next finish
1.16V pair, then inspect larger-phase recovery/compliance before changing topology.

Risk decision updated. Current run advances to1.16V cases under92006/3503271;
full matrix terminal provenance pending. Autonomous-loop execution remains held
for interpretation, not abandoned. No new layout.

## Pass 335 — low-control response slope retained; balance remains unmeasured

Verified endpoint92006/3503271 remains active in1.16V early case. Rechecked
completed0.98V pair without restarting simulation. Added and ran
`verification/analyze_pump_phase_balance.py`, consuming only completed cases and
hashing input evidence. Two-point equivalent current20.0135uA at0.98V versus
20.2505uA at1.08V: measured response slope has not collapsed at low control.
Linear zero estimates REF-minus-FB -309.09ps and -220.11ps respectively; the
former is an extrapolation outside +/-300ps, the latter an interpolation, neither
a simulated equilibrium or proof of lock. [Evidence](evidence/pump-phase-balance.json).

This changes the next diagnostic choice: test a clearly larger late-feedback
phase (e.g.600ps) at0.98V before treating the small negative300ps result as
lost compliance or redesigning the pump. It does not erase the measured wrong
sign at300ps, nor excuse process/noise/initialization tests. Finish current1.16V
pair, then combine that discriminating lower-control test with required1.30V
coverage. Autonomous integration remains the objective; do not optimize away an
arbitrary phase-offset target without a system requirement.

## Pass 336 — upper nominal early case verified; follow-up decks prepared

Continued endpoint92006/3503271 without restart.1.16V early case completes800ns;
checker verifies exact substitution, phase, clamp, KCL and artifacts. Charge
-9.370055fC/cycle is expected correction direction; dummy1.140195..1.158384V,
driver0.468327mW. Three cases complete; final1.16V late case now running.
Terminal source-after checks remain pending.

Added and executed `verification/prepare_pump_range_followup.py`: three decks
from hashed completed nominal follower fixture, each with exact reversal. Cases
are0.98V/600ps late feedback and1.30V/both300ps signs. Prepared only in
`scratch/transceiver-pump-range-followup-prepared`; no performance claim or new
simulation launched. Avoided modifying the active runner because it hashes its
own source before/after. Follow-up execution/checking still needs wiring into
shared infrastructure after current run completes.

Next observe final endpoint case, then run the bounded follow-ups to distinguish
shifted balance from lost correction and cover historical startup excursion.
Autonomous integration remains next; no production adoption or layout work.

## Pass 337 — discriminating range follow-ups launched

Existing endpoint92006 remains live in final1.16V late case. Replayed its three
completed cases through extended checker successfully; no source edits to its
active hashed runner. Added prepared-deck execution runner with exact reversal,
baseline/source/prepared hashes, timeout and per-case/terminal artifacts.
Shared checker `--range-followup` independently verifies three-case coverage,
600ps versus300ps phase, control/precharge changes, clamp, KCL and charge.
Compilation/shell syntax and pending-state checks pass; completed follow-up branch
will be exercised as data arrives.

Launched session33479, scratch `scratch/transceiver-pump-range-followup`: first
0.98V late600ps case, then1.30V early/late300ps. These depend on completed nominal
follower fixture and existing low-control/old-loop evidence, not unfinished1.16V
late result, so running alongside it is justified. No duplicated simulations.
Reproduction `bash projects/programmable_transceiver_platform/verification/run_pump_range_followup.sh`;
checker `python3 projects/programmable_transceiver_platform/verification/check_pump_steering_phase.py --range-followup`.
Next observe both existing sessions; no new performance claim or layout work.

## Pass 338 — endpoint matrix complete; control-dependent offset retained

Endpoint session92006 terminal, all four800ns cases verified including full
source-before/after hashes and unique coverage. Final1.16V late charge+2.857199fC
per cycle, dummy1.134507..1.155860V, driver0.468327mW. Together with early
-9.370055fC, both tested signs correct at1.16V;0.98V late limitation remains.

Reran phase-balance analysis after terminal checker finished: equivalent currents
20.0135/20.2505/20.3788uA at0.98/1.08/1.16V; linear balance estimates
-309.09/-220.11/-159.80ps REF-minus-FB. These remain unverified interpolated/
extrapolated zeros, not lock points. Updated schematic inventory evidence links.

Follow-up session33479/runner3513593 still running0.98V late600ps, followed by
1.30V pair. Continue that existing job; endpoint92006 needs no further polling.
Whole-chip readiness remains false. No new layout or autonomous lock claim.

## Pass 339 — larger-lag low-control correction verified

Continued33479/3513593.0.98V600ps late case completes800ns; exact changes,
measured -600ps REF-minus-FB, clamp, finite ordered vectors, artifacts and KCL
verify. Pump adds5.712285fC/cycle (mean0.11157uA), dummy0.951513..0.977564V,
driver0.460720mW. This contrasts with -0.181953fC at300ps lag: opposite
measured signs support a shifted balance, not a lost sourcing capability.
No simulated zero, continuity/stability or acquisition proof follows.

Extended phase-balance analysis with explicit measured300/600ps comparison and
input hashes. Updated risk decision: finish1.30V pair, then use autonomous
integration to test actual behavior rather than optimize an arbitrary phase zero.
Session33479 continues1.30V early then late; terminal source-after checks pending.
No new layout. [Evidence](evidence/pump-range-followup.json).

## Pass 340 — prepared autonomous circuit elaborates in ngspice

Verified follow-up33479/3513593 active in1.30V early case. No restart or new
performance conclusion while that run progresses.

Added `analog/pll/loop_elaboration_screen.py` and execution wrapper; ran pinned
container against hashed prepared autonomous deck. Replaced only control commands
with `listing expand`/quit. Ngspice exits0 with no detected resolution/fatal errors;
expanded output contains nested follower, steering switch and reservoir instances.
Host verifies resulting deck/log hashes. This is expanded-listing evidence only:
no OP, transient, bias, acquisition or stability claim. Original prepared transient
deck remains unchanged. [Evidence](evidence/loop-elaboration.json).

Reviewed connection: actual divider/buffer drives FB, follower input sensesCTRL,
no ideal VCLAMP/feedback stimulus, dummy zero-volt sensor joins driver to reservoir
and switches. Ideal supply, bias-current and regeneration sources remain explicit.
Next finish1.30V pair, then execute prepared full loop with existing runner/checker.
Layout stays deferred; full-chip obligations remain incomplete.

## Pass 341 — upper-startup diagnostic early correction verified

Continued existing33479/3513593, no restart.1.30V early case completes800ns;
shared follow-up checker verifies artifact hashes, exact clamp/precharge/phase
changes, +300ps REF-minus-FB, control clamp, ordered finite vectors and KCL.
Mean charge -7.986644fC/cycle, mean current -0.155989uA, dummy
1.280872..1.296938V, driver0.468638mW. Expected early correction remains at
this selected upper-control scenario; it is not a bound on new-loop excursions.
[Partial evidence](evidence/pump-range-followup.json).

Final1.30V late case is live under same session/runner, observed beyond154ns.
Next complete it and terminal source checks, then execute prepared autonomous
loop if no new failure calls for diagnosis. No further pump optimization is
justified solely by these offsets. Clock acquisition/noise, full RX/ADC/TX quality,
wired recovery and complete bias/host/pad integration remain outstanding.

## Pass 342 — phase-drift diagnostic prevents frequency-only lock inference

Follow-up33479/3513593 confirmed live in final1.30V late case; no restart.
Extended shared autonomous checker with last21 ordinal REF/FB-pair diagnostics:
window, both frequencies, unwrapped phase drift, fitted slope and detrended
peak-to-peak residual. Replayed historical failed loop successfully: over
1687.38..2711.38ns, REF19.53125MHz vs FB19.42162MHz, drift5.78026ns,
detrended residual2073.75ps p-p. This is not settled lock or intrinsic jitter.
Partial-run history still ends before3201ns and near known numerical failure.

Updated startup contract so average-frequency proximity cannot substitute for
phase convergence. No lock threshold invented and no lock flag added. New
follower completed-data branch awaits real autonomous simulation; current work
adds useful diagnostics rather than qualification. Next finish current range
case/source hashes, then execute prepared autonomous loop.

## Pass 343 — local follow-ups complete; autonomous follower loop launched

Session33479 terminal, all three800ns cases and full source/artifact hashes
verified. Final1.30V late charge+4.302294fC/cycle, dummy1.275120..1.294841V,
driver0.468638mW; early charge-7.986644fC. Alongside low-control600ps sourcing,
this supports moving from clamped diagnostics to actual autonomous feedback.
No equilibrium/noise/startup claim follows from forced-clock tests.

Launched prepared autonomous loop via `run_closed_loop_follower.sh`: session16465,
runner3522829, Docker3522765, scratch `scratch/transceiver-closed-loop-follower`.
Confirmed actual transient progressing beyond8ns, requested3201ns. Actual VCO,
divide128, buffered reference/feedback, PFD, steering pump, tracking follower,
reservoir and loop filter connected; no output clamp or ideal feedback. Original
seeded/prebiased history and older RF load preserved as controlled comparison.
Ideal supplies/bias/reference and physical uncertainty remain unqualified.

Pending checker `check_closed_loop_buffered.py --follower` passes structural/
provenance checks. Next observe existing16465, then inspect completion, phase,
frequency, control excursions, pump charge and driver loading. All local pump
runs are terminal; do not restart them. Inventory/risk decision updated.

## Pass 344 — actual RF/loop boundary audited while autonomous run progresses

Verified autonomous16465/3522829 live and progressing; do not restart. Inspected
prepared loop and retained latest bypassed receiver decks. Added/executed
`audit_rf_loop_boundary.py`, verifying fixture hashes and explicit connections.
Latest receiver changes oscillator loading to RC quadrature/four LO paths, has
two mixers/filters and448-unit bypass; its P/N nodes have no divider load. Old
loop has divide128 plus two direct LO paths and sample switches/5pF held loads;
latest filter outputs have no ADC/sample loading. These are material differences,
not an interchangeable RF instance name.

Recorded concrete migration boundary: retain latest receiver loads, attach real
divider to P/N, replace only ideal VCTRL with real loop connection, keep separate
regeneration bias explicit, and rerun loaded tuning/phase/conversion. Missing
ADC/interface loading stays an obligation. This is a structural audit, not new
circuit performance or a reason to discard controlled old-load loop experiment.
[Audit](evidence/rf-loop-boundary.json). Next observe16465 while preparing the
latest-load integration after its result; no layout work.

## Pass 345 — latest receiver plus actual loop structurally composed

Verified16465/3522829 still live, progressing beyond175ns in controlled old-load
loop. No restart; full result remains pending.

Added/executed `prepare_latest_rf_loop.py`. Uses hashed completed bypassed zero-RF
receiver and prepared actual loop. Connects first divider input to existing P/N,
removes VCTRL, inserts actual divider/PFD/pump/follower/filter/reference buffer,
preserves independent regeneration and all existing RF/quadrature/LO/filter loads.
Reuses reservoir definitions through rc_split instead of duplicating them. Adds
loop observation vectors and3201ns horizon. Exact reversal reproduces original
receiver deck byte-for-byte, including initial conditions and all original loads.

Prepared `scratch/transceiver-latest-rf-loop-prepared/latest.spice`, not simulated
or adopted. Added divider changes oscillator load and may change tuning/quality;
old-loop evidence cannot qualify this circuit. ADC output loads, physical biases,
startup and RF pad interface remain missing. Next require elaboration/provenance
before execution, while current old-load result isolates pump substitution.
[Preparation](evidence/latest-rf-loop-preparation.json). No layout work.

## Pass 346 — latest receiver/autonomous loop elaboration verified

Current old-load loop16465/3522829 confirmed live past263ns; no restart.
Extended shared elaboration tool with `--latest` and separate wrapper/output.
Pinned ngspice expands latest receiver plus actual loop, exit0, no detected
subcircuit-resolution/fatal errors. Checks confirm quadrature, both filter paths,
divider, follower and reservoir plus actual mixer terminal connections.
Initial checker guessed nonexistent mixer x1 instance; inspected expanded listing
and source (XP0 fingers), corrected assertion to explicit RF/OIP/MIP and RF/OQP/MQP
connections and reran successfully. This was a checker error, not circuit failure.

Verified output hashes and recorded [evidence](evidence/latest-loop-elaboration.json).
Expanded listing alone proves neither OP nor timing/noise; no new transient run.
Latest source deck unchanged; missing ADC/interface loads and ideal bias/seed
assumptions remain. Next interpret current autonomous result and provide latest
fixture with its own execution provenance and waveform checker before simulation.

## Pass 347 — latest receiver-loop execution and checker prepared

Confirmed controlled-loop16465/3522829 live beyond350ns; no restart. Extended
shared runner with explicit `--latest` deck/manifest selection, preserving its
already-running compiled old-load execution. Latest wrapper preflight resolves
and hashes19 actual dependencies; no transient launched in this pass.

Added latest-fixture checker using named exported columns: exact prepared deck,
source/artifact provenance, terminal completion, finite ordered time, ordinal
REF/FB phase, control/dummy/RF/filter node ranges and six accounted supply powers.
Pending-state check passes; completed-data branch remains untested until actual
waveforms arrive. No lock flag or invented passing phase threshold.

Commands: `run_latest_rf_loop.sh` (execution), `check_latest_rf_loop.py` (audit),
under project verification directory from repository root. Output scratch
`transceiver-latest-rf-loop`. Next decide when to launch latest-load integration
while preserving current controlled old-load diagnostic. All missing bias/startup,
ADC/interface and noise obligations remain; no layout work.

## Pass 348 — latest receiver autonomous transient launched

Confirmed older-load loop16465/3522829 live beyond456ns. Launched latest-load
loop with `run_latest_rf_loop.sh`: session48413, runner3531746, ngspice3531771,
Docker3531704; output `scratch/transceiver-latest-rf-loop`. Actual transient
progress confirmed beyond14ns; requested3201ns. Pending checker passes.

Latest path includes actual split VCO, RC quadrature, four LO driver branches,
bypassed LNA, I/Q mixers/filters plus divide128/PFD/steering pump/follower/filter.
Zero RF input isolates timing/bias first. Initial seeds, ideal bias/supply/reference,
no ADC output load and absent physical parasitics remain explicit. This is a
materially more connected schematic, not a performance or lock claim.

Both runs intentional: older RF load isolates pump substitution; latest load
addresses actual current receiver integration. Inventory updated with running
status and preparation/elaboration/evidence links. Observe existing sessions;
never restart because a wait expires. Once terminal, run respective checkers and
compare completion, phase history, control range, bias and accounted supply power.
No layout work; whole-chip readiness remains false.

## Pass 349 — latest-loop diagnostics separate startup from late behavior

Both simulations confirmed live: controlled16465/3522829 beyond528ns; latest
48413/3531746 beyond68ns. Neither restarted; no terminal waveform available.

Extended latest checker with fixed100ns windows through3100ns: differential
VCO edge frequency, control/dummy ranges, pump charge, and I/Q filter common-mode
and differential ranges. Also adds last21 ordinal-edge phase drift/slope and
detrended residual, explicitly not intrinsic jitter. Whole-run extrema remain
for excursion reporting; windows allow checking whether bias/phase continues
moving instead of mixing startup with late behavior. Compilation and pending
state pass; completed-data calculations await actual waveform verification.

Next observe both existing jobs and evaluate actual results before asserting
settling or lock. Missing ADC loading means even settled unloaded filter outputs
would not qualify the converter interface. No circuit change or new layout.

## Pass 350 — receiver/ADC common-mode and drive gap quantified

Both autonomous jobs confirmed live:16465/3522829 and48413/3531746. Continue
without restart; no completed timing result yet.

Inspected actual shared-I/Q ADC fixture and completed bypassed RX zero-tone
measurements. Late320..400ns filter common modes I1.069656V/Q1.071051V;
ADC ideal test sources alternate1.45/1.85V around1.65V through1kohm into actual
sample drivers. Approximately0.58V common-mode mismatch between demonstrated
fixtures, and ideal source drive masks actual filter impedance/kickback interaction.
This is untested integration, not proof of impossible ADC operation at1.07V.

Recorded hashed [boundary evidence](evidence/rx-adc-boundary.json). Next ADC-boundary
increment should screen actual sample-driver bias/headroom at receiver CM, then
connect actual filter/driver/switch/CDAC and measure settling/kickback. Do not
insert an ideal level shifter and call the interface implemented. Sample-rate,
swing and anti-alias allocation and existing shared-reference accuracy failure
remain separate obligations. No layout work.

## Pass 351 — actual ADC sample driver at receiver common mode

Both autonomous simulations confirmed live and retained. Added/executed short
DC screen of actual pt_sample_driver_headroom, same diode-biased20uA sources,
3.3V/27C/TT, compensation0.5pF and1kohm input source. Sweep0.85..1.85V at
constant loads-100/0/+100uA. All three101-point sweeps and source/artifact hashes
verify; loads are diagnostic choices, not switched-CDAC bounds.

At1.07V/no load, output1.059624V (-10.376mV), driver supply5.490mW. Tail model
VDS-VDSAT only28.29mV versus465.64mV at1.65V. +/-100uA retains nominal DC
tracking but does not test sampling pulses, filter-source dynamics or stability.
Thus direct common-mode operation is not disproven, but limited tail headroom
must be tested under swing/variation before inferring compatibility. Absolute
follower offset is not automatically differential ADC error; mismatch and actual
paired-channel behavior matter. No topology change or ideal level shifter adopted.

Runner/checker `run_adc_receiver_cm.sh` / `check_adc_receiver_cm.py` from project
verification directory, scratch `transceiver-adc-receiver-cm`; all these DC jobs
terminal. [Evidence](evidence/adc-receiver-cm.json). Next examine lower-input
headroom and connect finite filter/driver/sampling load. Timing runs remain active;
no layout work.

## Pass 352 — headroom crossing is not a demonstrated tracking failure

Both autonomous jobs verified live (16465/3522829,48413/3531746); no restart.
Extended completed ADC-driver DC checker to locate tail modelVDS-VDSAT crossings
and local output/input slope, reusing hashed waveforms without another simulation.
All three constant loads cross between1.02..1.03V input. Across0.95..1.15V,
measured DC secant gains0.99856..0.99972 remain near unity, including below the
model saturation boundary. No-load tracking error varies about0.20mV across this
interval atop~10.4mV common offset.

Therefore do not label1.03V a hard input limit or reject the topology solely from
negative model headroom. The real unresolved question is dynamic gain/settling,
noise and process sensitivity with finite filter drive and switched CDAC load.
[Updated evidence](evidence/adc-receiver-cm.json). Next connected load experiment
should include both sides of the crossing; selected inputs are diagnostic cases,
not guaranteed uncertainty bounds. No bias/topology change or layout work.

## Pass 353 — dynamic dual-ADC common-mode substitution launched

Prepared and launched actual shared-reference same-history I/Q ADC fixture with
only four input-source levels shifted1.85/1.45V→1.27/0.87V: common mode1.65→1.07V,
unchanged+/-0.4V differential swing, clocks, source1kohm, drivers, switches,
PDK CDAC, comparator/control and actual shared references. This probes low-input
dynamics and comparator behavior, not just buffer DC. Ideal input sources remain;
actual RF filter impedance and bias/reference shortcomings are not repaired.

Runner `run_adc_shared_iq_receiver_cm.sh`, session33800, scratch
`transceiver-adc-shared-iq-receiver-cm`, requested209.9ns/5ps. Source/model/deck
hashes and terminal artifacts retained. Extended existing frame checker with
`--receiver-cm`, independently reconstructing exact four-line change and preserving
reference-window/code/rail diagnostics. Pending check passes; no completed result.
Next observe33800 alongside autonomous16465/48413; do not restart existing runs.
No new layout or end-to-end accuracy claim.

## Pass 354 — comparator common-mode observations added to shared ADC audit

All three jobs confirmed live: controlled loop16465/3522829 beyond1.013us,
latest loop48413/3531746 beyond518ns, low-CM ADC33800/3538590 beyond45ns.
No restart and no terminal-result inference from progress logs.

Extended shared ADC frame checker to report first predecision top-plate common
mode and I/Q comparator-input common-mode ranges in every bit decision window.
Replayed completed same-history1.65V-source baseline: three frames verify, captured
I codes177/78/177 unchanged. First predecision common modes~1.6394/1.6400/1.6413V;
last decision of third frame~1.6320V. The internal operating point varies with
sampling/CDAC history and should not be inferred from ideal input-source CM alone.

These added observations will distinguish comparator operating-point changes
from mere output-code agreement in pending1.07V fixture. They do not allocate
common-mode limits or establish accuracy/noise. Next observe33800 and run shared
checker `--receiver-cm`; retain both loop runs. No new layout.

## Pass 355 — low-common-mode dynamic ADC degradation verified

ADC33800 terminal,209.9ns complete with source/artifact hashes, exact four-source
substitution and three-frame checks. Added/executed controlled comparison against
verified same-history baseline. Input differential unchanged, but I codes
177/78/177→173/82/173 at logic rails. First predecision differential magnitudes
~0.3954..0.3957V→0.3676/0.3698/0.3717V, i.e.24..28mV degradation. Held CM
~1.061V. Largest decision-span mean changes relative baseline8.55..15.92mV;
reference dynamics remain a confounder and independent existing weakness.

DC tracking alone therefore did not establish dynamic compatibility. Do not
assign all error to tail headroom, sampler or reference without isolated probes.
Next inspect buffer output versus held plate immediately around track-to-hold,
then separate sampling disturbance/settling from reference response before choosing
bias/common-mode/topology remedy. Actual filter loading still missing.
[Comparison](evidence/adc-receiver-cm-comparison.json). Inventory updated.
Both autonomous loops16465/48413 continue; no layout work.

## Pass 356 — low-CM error already present before sampling turnoff

Added/executed saved-waveform `analyze_adc_cm_sampling.py`, verifies both completed
fixture artifacts and compares time-weighted I driver/plate means just before
hold(-0.30..-0.15ns) and after hold(+0.20..+0.35ns), before first comparator edge.
Original driver differential magnitudes0.39434..0.39474V; low-CM0.36907..0.37298V.
Tracking plate magnitudes original0.39262..0.39304V, low-CM0.36357..0.36782V.
Turnoff-associated magnitude changes2.69..2.80mV original versus3.92..4.17mV
low-CM. Most added error therefore precedes switch opening; blaming only switch
charge injection would miss the larger loaded driver/tracking deficit.

This narrows diagnosis, not a unique transistor cause. Loaded driver settling,
reference interaction and sampling path all remain possible contributors. Next
isolate time available for driver settling or reproduce its actual sampling load;
do not resize the comparator based solely on captured codes. Q driver outputs
were not saved, so this mechanism observation is specifically I-path evidence.
[Sampling diagnostic](evidence/adc-cm-sampling.json).

Autonomous16465/3522829 and48413/3531746 confirmed live; no restart or terminal
claim. No new layout.

## Pass 357 — input-settling-time discriminator launched

Both autonomous runs confirmed live; unchanged. Extended completed low-CM ADC
runner with explicit `--early-input`: only first transition of four ideal source
waveforms moves60..60.1ns→50..50.1ns. First sampling turnoff70ns and all other
clocks/source transitions remain fixed. First frame therefore has10ns more input
settling time; later frames carry potentially changed history and are not separate
matched controls. No throughput or circuit change proposed by this diagnostic.

Preserved previous runner bytes as `shared_iq_receiver_cm_before_early.py` to
verify historical input provenance before using revised runner, rather than
ignoring its old recorded hash. Extended shared checker independently reconstructs
four timing-only changes; pending state passes. New session86096, scratch
`transceiver-adc-shared-iq-early-input`, requested209.9ns/5ps. Next observe existing
job then compare first-frame driver/plate errors and reference response with low-CM
baseline. Improved first-frame behavior would support settling sensitivity, not
uniquely identify driver or sampler mechanism. No new layout.

## Pass 358 — both driver legs contribute to low-CM tracking deficit

Verified loops16465/3522829 and48413/3531746 active; ADC early-input86096/
3547756 active beyond45ns. Reused completed low-CM waveform and actual no-load
DC sweep, with artifact hash checks, to compare individual driver legs before
first hold against their corresponding settled DC values.

At low-CM first-frame targets0.87/1.27V, falling driver output0.878439V vs
unloaded DC0.859694V (+18.745mV); rising output1.247507V vsDC1.259333V
(-11.827mV). Both dynamic/load-dependent deviations compress differential swing;
a common follower DC offset alone cannot explain it. DC comparison is not a
proof of pure transient settling because sampling load differs from unloaded DC.
[Updated diagnostic](evidence/adc-cm-sampling.json).

Next evaluate existing longer-input-settling run before changing transistor sizes,
then distinguish persistent load interaction from incomplete settling. No extra
simulation launched, no process/noise conclusion, no new layout.

## Pass 359 — first-frame low-CM deficit is strongly settling-time sensitive

Early-input86096 terminal209.9ns; source/artifact hashes, exact four timing-only
changes and all frame diagnostics verified. Extended sampling analyzer with
explicit early-input mode; baseline replay unchanged. First-frame input settles
20ns rather than10ns: driver differential -0.369068→-0.399793V; tracking plates
-0.363571→-0.399272V; held predecision -0.367561→-0.401489V. I/Q first codes
173→177. This is strong sensitivity to time available, not proof of a unique
transistor cause or a qualified fix. Held overshoot and reference error remain.

Later I/Q codes82/175 versus low-CM82/173 show history changes; only first input
transition moved, so later frames are not independent matched settling controls.
Do not adopt extra acquisition time without allocating throughput/sample timing.
Next characterize loaded response versus time and bias/headroom, then choose a
circuit or timing remedy against the actual rate requirement. Avoid substituting
slower operation for full intended radio capability.
[Sampling evidence](evidence/adc-cm-sampling-early.json),
[frame evidence](evidence/adc-shared-iq-early-input.json).
Both autonomous loops16465/48413 remain live; no new layout.

## Pass 360 — low-CM driver device observation replay launched

Both autonomous loops confirmed live. Launched observation-only low-CM ADC replay
session54440, scratch `transceiver-adc-receiver-driver-probe`,209.9ns/5ps. Added
id/VDS/VDSAT/VGS/VTH for tail, pair, output and load FETs of both I driver legs,
plus input/bias/internal nodes. No circuit, source timing or solver change.
Historical runner hash resolved through preserved exact snapshot; no provenance
check bypassed. Checker proves exact observation-only reversal and will compare
all original waveform vectors before accepting device interpretation.

Pending checker passes. Next observe54440, verify completion and original-vector
reproduction, then inspect60..70ns loaded transitions. Raw model current direction
and terminal roles must be checked before current attribution. No device sizing
change, performance claim or layout work.

## Pass 361 — steady acquisition budget checked against first-frame recovery

Verified three jobs live; probe54440/3552725 beyond58ns. Audited actual SC source
edges from low-CM ADC deck: holds70/120/170ns; steady track starts110/160ns.
Thus50ns cadence=20MS/s/channel has10ns track +40ns held conversion, while first
track lasts70ns. Moving first input edge60→50ns gains settling inside that long
first track; it does not demonstrate20ns steady acquisition at unchanged rate.
Saved hashed schedule evidence and updated whole-chip risk decision.

No lower throughput adopted. Next device evidence should support a faster loaded
driver, altered sampling approach or verified timing reallocation; cannot simply
extend all tracks without accounting for cadence/bandwidth. Both autonomous loops
remain active and intrinsic timing/RF quality still unqualified. No layout work.

## Pass 362 — device replay reproduces waveform; headroom alone insufficient

Probe54440 terminal209.9ns; checker verifies source/artifact hashes and all original
waveform vectors bit-identical. Device analysis validates raw VDS against actual
tail/output/load/input-drain terminal voltages before interpretation.

Falling I leg tail current ~303uA before step,~174uA just before hold with model
headroom~-79mV. Rising leg just before hold retains~171mV headroom and~303uA,
but still has -22.58mV output-minus-input error. Both driver errors change sign
between the65..69.85ns average and final69.7..69.85ns window, consistent with
unsettled dynamic response; tail saturation status alone cannot explain both legs.
No dominant pole/slew mechanism or causal repair proven by these observations.

Next quantify loaded ringing/settling and test compensation or bias changes one
at a time, preserving10ns acquisition and actual sampling load. Do not simply
increase bias because one transistor is below reportedVDSAT. Raw id is a model
probe, not terminal-current attribution. [Replay](evidence/adc-receiver-driver-probe.json),
[device analysis](evidence/adc-driver-devices.json). Both autonomous runs remain
active; no layout work.

## Pass 363 — compensation-only low-CM dynamic candidate launched

Both autonomous runs confirmed live: controlled16465 beyond2.167us; latest48413
beyond1.626us. No restart. Added explicit `--compensation` variant to shared ADC
runner/checker, preserving old source provenance via exact archived runner.
Only four sample-driver compensation values change0.5pF→1pF; same low-CM input,
10ns steady tracking, conversion clocks, reference circuit, sample loads and all
FET sizes. Exact four-line reconstruction and pending checker pass.

Launched session20283, scratch `transceiver-adc-shared-iq-compensation`,209.9ns.
Hypothesis: stronger compensation may reduce loaded overshoot/ringing enough to
improve sampled accuracy, despite lower bandwidth/slew. Could instead worsen
10ns settling; both outcomes informative. Not a physical capacitor realization
or production adoption. Evaluate predecision signal and reference motion, all
three frame histories and codes; do not select solely on first-frame code.
Next observe20283 alongside loops16465/48413. No new layout.

## Pass 364 — compensation candidate completes but does not repair acquisition

Previous workflow pass made authoritative documentation progress. Confirmed both
autonomous runners live (PIDs3522829/3531746), logs beyond2.597us/2.040us; no
restart. Compensation job20283 is terminal at209.9ns. Shared checker validates
exact declared changes, provenance, finite vectors, horizon and three frames.
Extended the existing sampling analyzer with mutually exclusive --compensation
mode, preserving prior modes and separate evidence outputs.

At holds70/120/170ns, I driver differential magnitudes improve from
369.068/372.007/372.981mV to386.566/388.349/389.780mV. However tracking-plate
magnitudes become429.339/434.318/437.523mV and held magnitudes
421.228/425.548/428.483mV against400mV target. Hold-associated changes reverse
sign and grow to8.11..9.04mV. Absolute held errors improve in the first two
frames but slightly worsen in the third; this is not a consistent acquisition
repair or an accuracy qualification. Shared references remain a confounder.
Retain0.5pF baseline;1pF candidate retained for diagnosis only.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_adc_shared_iq_frames.py --compensation`
then `python3 projects/programmable_transceiver_platform/verification/analyze_adc_cm_sampling.py --compensation`.
Artifacts: scratch/transceiver-adc-shared-iq-compensation; evidence
[checker](evidence/adc-shared-iq-compensation.json) and
[comparison](evidence/adc-cm-sampling-compensation.json).
Next inspect the continuous driver/plate transient and actual sampler/CDAC
connections to distinguish residual history from turnoff disturbance; do not
optimize on codes or driver swing alone. Continue observing both autonomous
loops. Full signal quality, startup, variation and integration remain open;
no new layout or tapeout-readiness claim.

## Pass 365 — tracking history exposes large pre-hold overshoot

Previous pass produced verified circuit evidence. Confirmed autonomous runners
3522829/3531746 live, beyond2.699us/2.138us respectively; no restart.
Inspected actual sampler wiring: IP/IN connect through complementary PDK FET
banks to HP/HN; ideal SC/SCB remain on during the first60..70ns input transition.
CDAC track mask holds midcode until70ns; physical reference/bottom voltages can
still move. Thus a pre70ns plate excursion cannot be caused by the later hold edge.

Added and ran `verification/analyze_adc_tracking_history.py` from repository root
(prefix projects/programmable_transceiver_platform/). Validates completed runs,
artifact hashes, finite vectors and horizon, then measures entire acquisition
windows plus named samples and saved control ranges for both cases/all3frames.
[Evidence](evidence/adc-tracking-history.json).

First baseline driver reaches-647.46mV at64.888ns and plate-592.45mV at65.543ns
against-400mV target.1pF candidate reaches-626.39mV at65.868ns and plate-592.68mV
at66.723ns. Across3frames baseline plate overshoot is184.8..192.4mV, candidate
192.7..204.6mV. Larger compensation delays the response without consistently
reducing plate overshoot. This explains why a better near-hold driver value alone
was misleading; it does not isolate a pole, prove instability or exonerate moving
references. No circuit candidate adopted.

Next use a reduced actual driver + continuously-on sampler + representative
CDAC load fixture to distinguish amplifier/load settling from switched-reference
interaction, preserving the observed common mode and both transition directions.
Compare that reduction to this retained full fixture before tuning compensation.
Autonomous feedback, intrinsic noise and complete RF/ADC integration remain
unqualified. No new layout.

## Pass 366 — autonomous follower loop terminal at the same reference event

Previous pass produced verified acquisition evidence. Rechecked processes:
old-load follower3522829 terminal, latest-RF3531746 live beyond2.240us.
Priority shifted from preparing ADC reduction to examining newly terminal clock
evidence. Ran check_closed_loop_buffered.py --follower: exact prepared circuit,
source/artifact hashes, finite time series and pump KCL validate; requested3201ns
not reached. Timestep failure at2762.5ns names vdummy#branch. A named trouble
node is not proof of the physical root cause.

Added and ran verification/audit_loop_failure_event.py (repository-root prefix
projects/programmable_transceiver_platform/). Both original buffered and follower
loop fail at2762.5ns, reference cycle52 rise end for PULSE delay100ns,
rise100ps,period51.2ns. Hash-verified event coincidence narrows the next numerical
discriminator; it does not prove source-breakpoint causation.

Follower last21 ordinal REF/FB pairs show5.511ns drift versus original5.780ns;
feedback19.4267MHz versus19.53125MHz reference. Detrended residual1.983ns p-p
is deterministic, not intrinsic jitter. CTRL1.02863..1.26006V. Candidate does
not demonstrate completion or lock; preserve as diagnostic, not production fix.
[Terminal checker](evidence/closed-loop-follower-screen.json),
[event comparison](evidence/loop-failure-event.json).

Next controlled actual-feedback experiment should change only reference source
representation while preserving intended edge schedule; assess equivalence and
completion before interpreting performance. Do not remove feedback or infer a
physical repair from numerical completion. Latest loop remains live; ADC reduced
loaded-settling test remains pending. No layout or readiness claim.

## Pass 367 — equivalent-reference actual-feedback comparison launched

Previous pass verified terminal failure/event evidence. Latest-RF runner3531746
confirmed live beyond2.359us. Prepared follower-loop PWL reference variant from
hash-verified terminal deck. Only ideal REFRAW representation changes: integer-ps
explicit points preserve100ns delay,100ps edges,25.5ns high width and51.2ns period.
Exact reversal recovers original deck;489 breakpoint/midpoint checks through
3201ns agree with independent pulse formula within2.23e-16V. No feedback, device,
load, solver, initialization or requested horizon changes.

Added prepare_follower_pwl.py and run_closed_loop_follower_pwl.sh; extended shared
checker with --pwl and runner to preserve preparation's scope description.
Preflight verifies19 dependencies; pending checker passes. Launched session68693,
scratch/transceiver-closed-loop-follower-pwl,requested3201ns,14400s wall timeout.
This is a source-representation discriminator, not an adopted circuit fix.

Reproduce from root: `python3 projects/programmable_transceiver_platform/verification/prepare_follower_pwl.py`,
then `bash projects/programmable_transceiver_platform/verification/run_closed_loop_follower_pwl.sh`.
Observe existing session rather than rerunning. Terminal checker:
`python3 projects/programmable_transceiver_platform/verification/check_closed_loop_buffered.py --pwl`.
[Preparation evidence](evidence/follower-pwl-preparation.json).
Next check both live loops; if PWL completes, compare trajectories before claiming
anything about acquisition. If it fails, retain exact event and state. ADC reduced
settling fixture remains pending. No new layout; goal remains incomplete.

## Pass 368 — ideal-reference diagnostic preserves full acquisition circuit

Previous pass prepared and launched controlled clock evidence. Both clock jobs
confirmed live: latest3531746 beyond2.479us, PWL3574849 beyond53ns. No restart.
Selected a narrower causal intervention than deleting most of the ADC fixture:
replace shared reference delivery with ideal2.15/1.15V clamps, retaining actual
sampling switches, drivers, CDACs, comparator/control, reservoirs and all timing.
Original reference amplifiers remain instantiated but outputs VHISO/VLISO are
isolated from ADC load; retained internal observations no longer describe loaded
reference operation. This is explicitly an idealized diagnostic, not a repair.

Extended existing shared runner/checker with --fixed-references; exact one-block
substitution verified against completed low-CM baseline. Historical source hash
uses the existing exact runner snapshot; current runner hash captured separately.
Launched session33764/PID3577418, scratch/transceiver-adc-shared-iq-fixed-references,
209.9ns/5ps,900s timeout. Confirmed live beyond9.78ns. Pending checker passes;
Python compilation passes. Existing sampling analyzer now supports separate
--fixed-references output; terminal measurements not yet available.

Question: does large pre-hold overshoot persist when reference motion is removed?
If yes, inspect driver/sampler/CDAC dynamic loading; if substantially reduced,
reference interaction deserves a matched finite-impedance follow-up. Neither
outcome alone isolates a device or proves stability. Keep0.5pF physical baseline.
Reproduction from root: bash projects/programmable_transceiver_platform/verification/run_adc_shared_iq_fixed_references.sh.
Observe current job rather than rerunning. On completion run shared checker and
analyze_adc_cm_sampling.py with --fixed-references. Compare all3frames and
continuous tracking extrema, not just codes. No layout work.

## Pass 369 — synchronize whole-chip risk inventory with measured failures

Previous pass launched a controlled experiment and verified it live. This pass
rechecked all3processes: latest clock3531746 beyond2.585us, PWL3574849 running,
ADC3577418 beyond53.94ns. Observation only; no restarts. Extended tracking-history
analyzer with --fixed-references, separate evidence destination and identical
whole-window metrics; compilation passes. Terminal comparison remains pending.

Updated authoritative schematic inventory: autonomous follower is failed/partial,
not still running; PWL replay and latest receiver loop remain active. Added actual
ADC pre-hold overshoot, rejected1pF candidate,10ns steady acquisition constraint
and ideal-reference diagnostic scope. Linked measured evidence. Inventory audit
passes its structural checks for10obligations and explicitly reports whole-chip
schematic incomplete/layout closed; this is not circuit qualification.

Periodic whole-chip reassessment: highest active uncertainties remain autonomous
clock convergence/phase evolution and loaded RX-to-ADC acquisition. Even fixing
both would leave intrinsic oscillator noise, complete switched-RX noise/linearity,
actual modulated TX, wired CDR/BER, host timing, real bias/startup and pad/package
coexistence unqualified. Unknown package/process cases must still be explored in
both adverse directions; no finite chosen scenario set is a foundry guarantee.
Do not let an ideal-reference diagnostic or numerically completed seeded loop
replace any of those obligations. Next evaluate terminal ADC run with checker,
sampling analyzer and tracking-history analyzer --fixed-references; then choose
one causal circuit experiment. Continue monitoring existing clock jobs.

## Pass 370 — reference clamp does not remove acquisition overshoot

Previous pass updated authoritative inventory and reusable analysis. Observed
existing jobs without restart. ADC33764/PID3577418 terminal209.9ns; shared checker
verifies exact substitution/provenance, all3frames and ideal rail error<1nV over
entire saved waveform. Sampling and tracking-history analyzers --fixed-references
complete. Existing compensation mode also reproduces previous evidence after
shared-checker changes.

Ideal-reference held magnitudes365.984/367.825/367.728mV compare with physical
reference baseline367.561/369.796/371.740mV, against400mV target. Tracking plate
overshoot remains195.35..195.96mV; driver overshoot250.17..250.28mV. Removing rail
motion does not remove the large transient and slightly worsens held accuracy.
Thus reference-rail motion is not necessary for this observed acquisition failure;
this does not absolve actual references of their separate regulation failure.
MSB bottom plates still move behind switches even with ideal rails. Do not infer
all load-side dynamics have been removed or a unique amplifier pole identified.
[Clamp checker](evidence/adc-shared-iq-fixed-references.json),
[sampling](evidence/adc-cm-sampling-fixed-references.json),
[history](evidence/adc-tracking-history-fixed-references.json).

Next prioritize actual driver/sampler/load dynamics with a reduced matched load
fixture or valid loaded-loop analysis; retain both transition directions and10ns
acquisition. Ideal references are diagnostic only, original baseline unchanged.
Latest-RF3531746 confirmed live beyond2.7819us, passing older-load2.7625us failure
point; no completion/lock claim. PWL clock replay remains an existing active job.
No layout; complete RF quality and whole-chip obligations remain open.

## Pass 371 — reduced actual-driver fixture reproduces acquisition failure

Previous pass verified ideal-reference result. Confirmed latest/PWL clock runners
3531746/3574849 live. Built loaded_driver_screen.py: actual two sample drivers,
same1k source/bias/0.5pF compensation, continuously-on actual sampler and full
static-midcode PDK MIM CDAC, ideal reference rails. Removes comparator, SAR logic,
Q channel and finite code-driver impedance explicitly; does not substitute a
nominal capacitor for the physical CDAC model. First input step60ns matches full
fixture; opposite step80ns permits diagnostic recovery, not conversion throughput.

Launched session49562 via run_adc_loaded_driver.sh; completed100ns. Checker
check_adc_loaded_driver.py verifies manifest/artifacts, source hashes unchanged,
actual static code and sampler connections, finite ordered vectors and horizon.
Both directions give driver overshoot~250.2mV and plate~196.0mV. First60..70ns
waveform differs from full ideal-reference ADC by at most0.339mV driver and0.403mV
plate. Thus omitted comparator/control/Q behavior is unnecessary to reproduce
this first acquisition failure to that measured tolerance. Not a general
interface-equivalence or stability proof.
[Evidence](evidence/adc-loaded-driver.json).

Reproduce from root with bash projects/programmable_transceiver_platform/verification/run_adc_loaded_driver.sh
and python3 projects/programmable_transceiver_platform/verification/check_adc_loaded_driver.py.
Scratch transceiver-adc-loaded-driver,terminal. Next use this cheaper matched
reproducer for loaded small-signal/step diagnostics and one controlled circuit
change; retain full ADC baseline as integration test. No physical design adoption
or layout; autonomous clocks and full-chip qualification still incomplete.

## Pass 372 — loaded AC response measured at three operating points

Previous pass produced a verified reduced transient reproducer. Both autonomous
jobs confirmed live; latest-RF beyond3.00875us. Added loaded_driver_ac.py using
hash-verified reduced deck. Only stimuli change to DC operating points
(.87,1.27),(1.07,1.07),(1.27,.87) with1V differential small-signal excitation;
control block changes to AC1kHz..10GHz. Exact reversal verified for each case.
Actual driver/sampler/CDAC and0.5pF compensation retained.

First run exited0 but exported no vectors: name pi collided with simulator vector
semantics (different-length export error). Preserved all failed artifacts in
scratch/transceiver-adc-loaded-driver-ac-export-failed, renamed plate_im and reran.
Checker validates all3cases, hashes, finite701-point ordered frequency grids and
endpoints. Demonstrates why process exit alone is not simulation evidence.

Driver peaking2.925..3.022dB at114.8..128.8MHz; plate peaking1.489..1.874dB at
104.7..112.2MHz. Low-frequency gain~0.999. This characterizes local closed-loop
response, not return ratio, phase margin or large-signal stability. Large-step
overshoot remains separately demonstrated; no causal pole attribution yet.
[Evidence](evidence/adc-loaded-driver-ac.json).
Reproduce from root with verification/run_adc_loaded_driver_ac.sh then
verification/check_adc_loaded_driver_ac.py under projects/programmable_transceiver_platform.
Next use this quick fixture to examine compensation zero/damping versus bandwidth,
then verify both-direction10ns settling in reduced transient and full ADC. No
candidate adopted or layout work.

## Pass 373 — compensation resistor reduces loaded AC peaking; PWL loop fails

Previous pass established verified loaded AC baseline. Confirmed latest clock live
beyond3.164us; PWL job terminal. Ran shared PLL checker --pwl: hashes and partial
vectors verified, timestep failure637.7ns,vdummy#branch. Early100..200/500..600ns
frequency/control metrics closely match pulse baseline, but completion is worse.
637.7ns coincides with cycle10 reference fall end (100+10*51.2+25.7ns).
This demonstrates source-representation sensitivity in actual feedback, not a
physical fix or proof of its internal numerical cause. No repeat launched.
[Evidence](evidence/closed-loop-follower-pwl-screen.json).

Extended existing loaded AC runner/checker with --damping: locally inline the
same driver cell with only RC100ohm->1kohm; CC0.5pF and all FETs unchanged.
Exact deck substitution reversal verified; actual source file stays unchanged.
All3operating-point sweeps complete with verified hashes/grids. Driver peaking
falls2.925..3.022dB->1.262..1.422dB; plate1.489..1.874dB->0.430..0.657dB.
Driver peak shifts to93.3..100MHz. This supports testing the candidate dynamically;
no phase-margin, bandwidth sufficiency or10ns settling claim follows automatically.
[Candidate AC](evidence/adc-loaded-driver-ac-damping.json).

Reproduce from root with bash projects/programmable_transceiver_platform/verification/run_adc_loaded_driver_ac_damping.sh
then python3 projects/programmable_transceiver_platform/verification/check_adc_loaded_driver_ac.py --damping.
Next run matched reduced transient with resistor-only substitution; score both
polarities, full acquisition windows and near10ns error before full ADC integration.
Latest autonomous receiver loop still pending; physical baseline unchanged.

## Pass 374 — latest autonomous receiver completes; resistor transient improves

Previous pass verified AC candidate and PWL failure. Latest3531746 reached3201ns
and is terminal; check_latest_rf_loop.py completed with source/artifact, finite
waveform and horizon validation. This is the first completed latest-load seeded
zero-RF autonomous diagnostic, not lock qualification. Late21 ordinal pairs drift
0.84928ns over1024ns, detrended residual1009.24ps p-p. Phase is still evolving;
no intrinsic jitter claim. PWL and older-load runs remain failed, not superseded
as successful. All previously active clock jobs now terminal.
[Latest loop](evidence/latest-rf-loop.json).

Built loaded_driver_damping.py from hash-verified transient baseline with exact
reversal of only driver RC100ohm->1kohm. Original cell unchanged. Session3146
completed100ns. Shared checker --damping verifies exact circuit substitution,
source/artifact hashes, finite vectors and both directions. Plate overshoot
196.0mV->132.9mV; near10ns plate magnitude361.7mV->386.64mV against400mV target,
so error~38.3mV->13.36mV. Driver overshoot250.3mV->179.3mV. Improvement warrants
full-ADC test but is not sufficient accuracy or window-wide settling evidence.
[Transient candidate](evidence/adc-loaded-driver-damping.json).

Reproduce from root using verification/run_adc_loaded_driver_damping.sh and
verification/check_adc_loaded_driver.py --damping under project directory.
Updated schematic inventory with completed latest loop and partial ADC improvement.
Next examine late clock trajectory to choose a justified longer unchanged run,
and test damping candidate in actual shared-reference ADC across all frame
histories. Preserve original baseline; no layout or tapeout claim.

## Pass 375 — full ADC resistor candidate launched; phase-tail windows disagree

Previous pass verified latest-loop completion and reduced resistor improvement.
Extended existing shared ADC runner/checker with --damping. Inline driver cell
changes only RC100ohm->1kohm for all4instances; CC0.5pF, all devices, source
histories,10ns steady acquisition and actual shared references remain. Exact
substitution checker and Python compilation pass. Launched session50531,
scratch/transceiver-adc-shared-iq-damping,209.9ns,900s timeout. Pending, not adopted.
Reproduce from root with verification/run_adc_shared_iq_damping.sh and
verification/check_adc_shared_iq_frames.py --damping under project directory.

Added analyze_latest_loop_phase_tail.py; validates completed latest-loop evidence
and waveform artifacts. Last21pairs drift+0.849ns, but last10 -0.427ns and last5
-0.238ns. Last5 average feedback19.55395MHz versus~19.53125MHz reference.
Phase trajectory has turned; full late-window average conceals ongoing motion.
This justifies a longer unchanged seeded run to distinguish damped acquisition
from persistent motion, not a lock claim or immediate pump resizing. No intrinsic
noise inference. [Tail evidence](evidence/latest-loop-phase-tail.json).
Next observe50531 and prepare a longer latest-receiver run preserving all circuit,
solver and initialization parameters; allocate late windows before inspecting it.
All prior clock jobs terminal; no restart of an active job. No layout.

## Pass 376 — longer unchanged autonomous receiver run launched

Previous pass launched full-ADC candidate and verified late phase reversal.
Confirmed ADC3593181 live; no restart. Hash-verified completed latest loop and
prepared stop-time-only3201ns->8001ns extension. Exact reversal reproduces completed
deck; all circuitry,2ps timestep,Gear options and seeded initialization preserved.
Comparison windows allocated before run:3000..3100,5000..5100,7000..7100 and
7900..8000ns, plus existing startup windows. Requested runtime expected~100min
from prior~40min run; existing14400s timeout retained.

Added prepare_latest_loop_extended.py/run_latest_rf_loop_extended.sh. Shared
runner now records actual deck horizon; shared latest checker --extended uses
8001ns completion threshold and extra windows. Preflight verifies19dependencies;
pending checker passes. Launched session69276, scratch
transceiver-latest-rf-loop-extended. This reruns a terminal baseline at longer
horizon; it does not restart a live job or change acquisition initial conditions.
[Preparation](evidence/latest-loop-extended-preparation.json).

Reproduce from root using verification/prepare_latest_loop_extended.py then
verification/run_latest_rf_loop_extended.sh under project directory. Observe
session69276 rather than launch again; terminal checker check_latest_rf_loop.py
--extended. Next observe fullADC50531, quantify all3frames if complete, and assess
late clock drift when available. No lock/noise qualification or layout claim.

## Pass 377 — damping improvement survives full shared-reference ADC

Previous pass launched longer loop. Confirmed ADC3593181 and extended clock
3595903 live, then observed existing ADC50531 to terminal209.9ns without restart.
Shared checker --damping passes exact substitution, provenance, horizon and all
3frames. Extended existing sampling/history analyzers with separate --damping
outputs; both complete. Original source circuit remains unchanged.

Held I magnitudes388.733/389.351/390.565mV versus baseline367.561/369.796/371.740mV
against400mV target: error reduced32.44/30.20/28.26mV to11.27/10.65/9.43mV.
Tracking plate overshoot reduces184.8..192.4mV to129.4..131.8mV. Hold-associated
magnitude increment shrinks~3.9..4.2mV to1.54..1.74mV. Improvement is consistent
across retained histories, not merely a better first code. Residual error and
large transient persist; no ENOB/linearity/uncertainty qualification.
[Sampling](evidence/adc-cm-sampling-damping.json),
[tracking](evidence/adc-tracking-history-damping.json),
[full checker](evidence/adc-shared-iq-damping.json).

Disposition: retain1kohm as integration candidate, not qualified production
baseline. Actual shared-reference regulation remains a separate failure; no
ideal-clamp result substitutes for it. Next inspect residual settling over windows
and decide one additional damping/bandwidth experiment using cheap reproducer,
then widen input/load/common-mode cases before adoption. Extended clock69276
remains running; do not restart. No layout.

## Pass 378 — second resistor candidate exposes sampling-instant tradeoff

Previous pass verified fullADC1kohm improvement. Extended clock3595903 confirmed
live. Parameterized reduced damping runner with explicit1k/2k choices; original
cell/baseline unchanged. Shared checker --two-k verifies exact resistor-only
reversal and adds8..9.9ns error range for both original1k and new2k cases.
Session24345 completed100ns, all hashes/finite vectors/horizon validated.

2kohm plate overshoot~84.7mV versus132.9mV at1kohm; near9.7..9.85ns mean
magnitude400.249/400.253mV versus386.639/386.638mV. However maximum absolute
plate error over8..9.9ns worsens~16.8mV->22.7mV. Near-target mean is partly a
transient crossing, not demonstrated settled accuracy. Driver near10ns magnitude
395.54mV versus384.31mV; load lag matters. No selection from one sampled value.
[2kohm evidence](evidence/adc-loaded-driver-damping2k.json),
[1kohm window comparison](evidence/adc-loaded-driver-damping.json).

Reproduce from root using verification/run_adc_loaded_driver_damping2k.sh and
verification/check_adc_loaded_driver.py --two-k under project directory.
Disposition:2kohm diagnostic candidate only, not promoted. Next quantify actual
aperture neighborhood/timing sensitivity and broader input histories before
fullADC2kohm test; do not endlessly tune the crossing. Physical ADC references,
real filter source drive and complete transfer/noise remain independent gaps.
Long autonomous run69276 remains active. No layout.

## Pass 379 — local timing neighborhoods qualify the resistor tradeoff

Previous pass verified2kohm reduced transient. Extended clock3595903 confirmed
live beyond277ns, no restart. Added analyze_adc_aperture_neighborhood.py; validates
source/artifact provenance, terminal100ns, finite strictly ordered data for100ohm,
1kohm and2kohm cases. Interpolates actual saved tracking waveform at10ns after
each step and includes all samples plus exact endpoints within+/-0.1/0.5/1ns.
These are declared diagnostic neighborhoods, not guaranteed timing bounds.

2kohm nominal error~0.87mV, worst within+/-100ps~1.28mV,+/-500ps~2.46mV,
+/-1ns~6.68mV in both directions.1kohm retains~13.6mV maximum error in all3
neighborhoods. Thus2kohm is locally better despite worse8..9.9ns maximum; the
previous broader window alone should not reject it. It still has nonzero slope
and needs actual sample turnoff/conversion, varied input amplitudes and loading.
No settled/noise claim from hypothetical observations of an always-on sampler.
[Evidence](evidence/adc-aperture-neighborhood.json).

Reproduce from root: python3 projects/programmable_transceiver_platform/verification/analyze_adc_aperture_neighborhood.py.
Next test2kohm in full actual-reference ADC with nominal hold then controlled
clock perturbations, retaining code-independent analog measurements. Keep1kohm
and original baselines. No layout or readiness claim.

## Pass 380 —2kohm full ADC candidate launched; whole-chip priorities refreshed

Previous pass verified timing-neighborhood evidence. Extended clock3595903 live
at4min28s elapsed; no restart. Extended shared ADC runner/checker with --two-k,
retaining --damping1kohm semantics. Only inlined compensation resistor differs
from original actual-reference low-CM dualADC; actual samplers/CDAC/control and
209.9ns schedule unchanged. Python compilation, exact-substitution pending checker
and existing1kohm terminal checker pass.

Launched session95106, scratch/transceiver-adc-shared-iq-damping2k,900s timeout.
Question: does near-aperture improvement survive actual turnoff and finite shared
references over all3frame histories? Compare held error, transient history and
reference regulation, not codes alone. No acceptance limit invented after result.
Reproduce via verification/run_adc_shared_iq_damping2k.sh; checker
verification/check_adc_shared_iq_frames.py --two-k under project directory.

Refreshed risk-priorities current entry: numerical completion is separate from
clock quality; improved input acquisition does not close references/RF source
loading/noise. Preserve TX/wired/host/bias/package obligations and avoid an endless
resistor sweep. Next observe95106 and69276; fullADC2k terminal analysis then
controlled actual edge perturbations in both directions if improvement persists.
No layout or tapeout readiness.

## Pass 381 — prepare matched analysis and audit actual edge-test semantics

Previous pass launched fullADC2k. Confirmed clock3595903 andADC3605046 live; ADC
beyond46.6ns. No restart or early waveform claim. Existing sampling/history
analyzers now support --two-k with separate outputs including original/1k/2k;
compilation passes, terminal measurement deferred until completed artifact exists.

Added audit_adc_edge_perturbation.py against hash-verified generated2k deck.
Proposed +/-100ps sampler-only release shifts move SC/SCB together and preserve
input, mask, conversion and reset schedules. All3frames retain at least300ps
ideal sampler-ramp-end to first comparator-rise gap. Positive shift deliberately
overlaps mask release: this is an actual circuit-interaction test, not equivalent
to observing the always-on reduced waveform later. Physical delays may consume
that gap; it is not a setup qualification. No perturbed run launched yet.
[Schedule audit](evidence/adc-edge-perturbation-plan.json).

Next verify terminal nominal2k across all3frames before launching paired offsets;
compare predecision analog state and reference motion, not only codes. Boundary
scenarios do not bound unspecified fab/clock behavior. Clock69276 remains live;
no new layout.

## Pass 382 —2kohm full ADC completes; acquisition improves, references do not

Previous pass prepared analysis and source-edge audit. Confirmed ADC3605046 and
clock3595903 live, then observed existing95106 to terminal209.9ns (no restart).
Shared checker --two-k validates exact circuit change/provenance/horizon/allframes;
sampling/history analyzers --two-k complete with baseline/1k/2k comparisons.

Held magnitudes401.414/401.878/403.164mV against400mV target give absolute errors
1.414/1.878/3.164mV versus1kohm11.267/10.649/9.435mV. Tracking overshoot now
84.6..85.5mV versus1kohm129.4..131.8mV. Both channels codes177/78/178, rails valid;
code improvement is not independent accuracy proof. Actual reference span across
selected decisions0.92756..1.05798V, maximum intra-window motion9.7206mV: reference
regulation remains unqualified and can dominate conversion error.
[Sampling](evidence/adc-cm-sampling-damping2k.json),
[tracking](evidence/adc-tracking-history-damping2k.json),
[checker](evidence/adc-shared-iq-damping2k.json).

Retain2kohm for timing/load validation, not qualified baseline. Next paired actual
sampler-edge offsets +/-100ps with mask/conversion unchanged as audited; then
prioritize reference regulation and actual RF-filter drive rather than another
resistor sweep. Need broader signal histories, transfer/noise and adverse loads.
Extended clock69276 remains active; no layout or tapeout-readiness claim.

## Pass 383 — paired actual sampler-release offsets launched

Previous pass verified nominal fullADC2k. Extended clock3595903 confirmed live.
Added sampler_edge_screen.py reading completed/hash-verified2k fixture. Only VS/VSB
release ramps70/120/170ns and their100ps endpoints shift together by-100ps or
+100ps; rearm, mask, conversion, reset, input and circuit unchanged. Exact reversal
verified; current and all baseline source hashes captured before run.

Launched early session86530 and late96814, separate scratch
transceiver-adc-shared-iq-edge-{early,late},each209.9ns/900s timeout. Shared checker
--edge-early/--edge-late independently checks changed source names, every changed
and unchanged time, unchanged non-time tokens and exactly2changed lines. Both
pending checks and Python compilation pass. Existing predecision windows remain
fixed relative to comparator schedule, so all begin after shifted hold ramps.

Question: does nominal1.4..3.2mV held error remain similar under actual turnoff
perturbation, including interaction with unchanged mask release? Do not equate
these cases with noise simulation or a guaranteed uncertainty envelope.
Reproduce via verification/run_adc_sampler_edge_{early,late}.sh and shared checker
flags under project directory. Observe existing sessions rather than restart.
Next verify terminal results for both signs and compare analog state/reference
motion to nominal; then address reference regulation/RF-filter source loading.
No candidate qualified or layout work.

## Pass 384 — prepare current attribution before further reference redesign

Previous pass launched timing pair. Confirmed clock3595903 and timing3610259/
3610361 live, no restart. Reviewed retained reference compensation/output2/headroom
and impedance evidence: stronger output devices traded lower mean error for more
window motion and much higher current in older single-channel fixtures. Do not
assume those results diagnose latest dualADC or repeat blind geometry scaling.

Added prepare_adc_reference_probes.py from completed/hash-verified2k dualADC.
Four0Vseries sensors separately expose high/low driver delivery and high/low
reservoir current. ADC demand can then be evaluated as delivered minus reservoir
current with declared signs. Exact reversal recovers baseline; actual reservoir,
FETs, timing and voltage values unchanged. Prepared only, not simulated.
[Preparation](evidence/adc-reference-current-preparation.json).
Scratch transceiver-adc-reference-current-prepared,frames.spice/manifest.json.

Next run observation replay and first compare all original waveform columns;
adding ideal sensors can affect numerics and must not silently change behavior.
Then measure per-decision delivery, reservoir support, ADC demand and recovery to
choose reference remedy. Current timing tests remain live; no additional circuit
candidate or layout adopted. This addresses shared-reference regulation directly
instead of continuing sample-driver resistor tuning.

## Pass 385 — reference-current replay launched with reproduction gate

Previous pass prepared reference probe circuit. Confirmed clock3595903 and timing
pair3610259/3610361 live. Added sensor-terminal voltages to prepared observations
so zero-voltage constraints can be checked; exact reversal still passes.
Launched reference replay session1988, scratch transceiver-adc-reference-current,
209.9ns/900s timeout, via run_adc_reference_current.sh. No circuit sizing change.

New check_adc_reference_current.py validates preparation/artifacts/completion,
all original named vectors, and sensor voltage constraints. Predeclared maximum
reproduction deviations10uV for voltages/10nA for currents; reports actual errors
and same-time-grid status. If grids differ, explicit interpolation policy applies;
failed reproduction prevents matched-current interpretation. Pending checker
passes; terminal branch awaits result. Baseline and prepared provenance retained.

Reproduce scripts under project verification directory from repository root.
Next observe timing pair and current replay; diagnose delivered/reservoir/ADC
current only after reproduction gate. Existing long clock69276 continues.
Reference regulation remains a highest active signal-quality risk; no layout.

## Pass 386 — paired sampler-release tests complete with sub-mV change

Previous pass launched reference replay. Both timing jobs now terminal209.9ns;
shared checkers --edge-early/--edge-late validate exact changes, provenance and
all3frames. Extended clock3595903 confirmed live; reference replay log beyond63ns.
Added compare_adc_sampler_edges.py with hash-verified completed evidence and
time-weighted held differential in unchanged comparator predecision windows.

Early100ps I held errors1.928/2.418/3.782mV; late100ps0.970/1.412/2.622mV in
magnitude versus nominal1.414/1.878/3.164mV. Changes from nominal at most~0.619mV;
both channels retain177/78/178 codes.2kohm improvement survives these actual
turnoff/mask-interaction cases, but independent complementary skew, arbitrary
input histories and RF-filter loading remain untested. No jitter/noise bound or
transfer-accuracy qualification. [Comparison](evidence/adc-sampler-edge-comparison.json).

Updated ADC interface status in authoritative inventory; structural inventory
audit passes and still reports whole-chip incomplete/layout closed. Next observe
reference replay1988, verify reproduction, then attribute currents and choose
reference-regulation experiment. Stop resistor tuning for now; remaining dynamic
references and actual receiver-source loading are more consequential gaps.
Reproduce comparison via project verification/compare_adc_sampler_edges.py.
Long clock69276 remains active; no layout.

## Pass 387 — current replay completes but strict reproduction gate fails

Previous pass verified paired sampler timing. Confirmed reference3614314 and
clock3595903 live; observed existing1988 to terminal209.9ns. Checker validates
hashes, completion and exactly zero sensor voltages with identical time grid.
Original-vector tolerance gate fails: maximum v(keepp) difference1.070mV;
comparator nodes~43..62uV, above predeclared10uV. Do not describe replay as
bit-identical or silently loosen tolerance.
[Replay evidence](evidence/adc-reference-current-replay.json).

Added analyze_adc_reference_currents.py for signed per-decision/cycle delivery,
reservoir current and KCL-derived ADC demand. Compiles; execution correctly stops
at failed reproduction gate before writing attribution. Derived demand would not
be an independent KCL closure test. No current-mechanism conclusion made.

Next localize differing times/nodes and compare actual decisions plus analog
reference/plate behavior to determine whether numerical perturbation is harmless
for a specifically scoped current measurement or requires another probe method.
Any revised tolerance must be justified from that analysis, preserving original
failed gate. Long clock69276 still active; no layout or design qualification.

## Pass 388 — scope replay validity without erasing failed global gate

Previous pass completed replay and blocked attribution correctly. Extended clock
3595903 confirmed live. Added audit_adc_reference_reproduction.py: validates both
artifact sets and identical time grid, locates failing vectors, checks named
analog/reference nodes, captured codes/rails and ordinal threshold crossings.
Largest logic difference1.070mV at201.4225ns occurs during transition; all captured
I/Q codes match177/78/178. Compared logic edge shift maximum0.009585ps. Named
analog-node maximum2.049uV, reference rails<0.174uV, reference supply4.237nA,
input-buffer supply3.158nA. These remain inside original10uV/10nA tolerances.

Digital supply currents differ up to1.331uA, so global reproduction remains false
and matched digital-power claims are excluded. Scoped audit explicitly adds a
1ps logic-edge diagnostic criterion after inspecting failure (100x below tested
100ps sampler offset); this is not retroactive global passing or qualification.
Scoped reference-current diagnostic validity is true; artifact records disclose
both results. [Audit](evidence/adc-reference-reproduction-scope.json).

Next allow explicit scoped mode in current analyzer, tied to audit hash and
limitations, then inspect reference delivery/reservoir/demand per decision.
Do not remove default strict gate or use this for precision power/noise claims.
Long clock69276 active; no circuit/layout change or readiness claim.

## Pass 389 — scoped current attribution identifies pulse/recovery burden

Previous pass established scoped replay audit without global passing. Confirmed
clock3595903 live. Added explicit --scoped analyzer mode requiring exact audit hash
and scoped-valid flag; default strict gate remains unchanged. Full artifact hashes
checked. Per-bit/cycle delivery, reservoir and KCL-derived ADC-demand metrics saved,
plus preceding-update windows for each rail's worst decision.

Worst high decision125.2..125.35ns: mean2.09024V,max error61.30mV; delivered2.750mA,
ADC demand0.826mA and reservoir recharge1.924mA mean. Preceding122.5..125.35ns
contains ADC-demand peak72.66mA and reservoir withdrawal71.06mA; driver maximum
2.804mA. Rail ranges2.06789..2.17342V. Net interval ADC charge11.31pC includes
3.899pC driver delivery and7.409pC reservoir withdrawal. This supports a pulse
loading/recovery problem, not simply insufficient DC current at decision time.
Worst low decision has20.17mV error; rail asymmetry remains relevant.
[Attribution](evidence/adc-reference-current-attribution.json).

Peak current estimates are timestep-sensitive diagnostics, not precision signoff;
KCL-derived demand is not independent KCL closure. No new current-device geometry
chosen from these observations. Next distinguish reservoir depletion/recovery and
reference loop dynamics using a measured-load replay or controlled pulse-load
fixture, retaining both current directions and actual PDK reservoir. Avoid blind
larger capacitance/current: prior experiments already exposed tradeoffs.
Reproduce analyzer with --scoped. Long clock69276 continues; no layout.

## Pass 390 — measured-load reference reduction prepared without decimation

Previous pass produced scoped current attribution. Confirmed clock3595903 live.
Added prepare_reference_load_replay.py: preserves exact actual reference-pair,
bias/reservoir and sensor blocks from verified replay, replaces dualADC demand
with signed PWL current sources. Keeps all42618saved points per rail, round-trip
numeric equality verified at17digit serialization; no decimation or pulse-shape
approximation beyond interpolation inherent in saved samples.209.9ns horizon.

Whole-record demand ranges high-23.2..94.1mA,low-67.0..18.0mA; signed charges
+91.75/-91.57pC. These include startup/all transitions, unlike the previously
reported selected worst-decision event. They are not constant load requirements.
Prepared scratch transceiver-reference-load-prepared/{load.spice,demand.spice,
manifest.json}; [evidence](evidence/reference-load-preparation.json).

Next run unchanged reduced reference circuit and compare VH/VL and delivery to
original waveform before using it for parameter experiments. The frozen current
history removes voltage-dependent ADC feedback; any candidate must return to full
ADC. Source manifest retains former /baseline/frames.spice provenance, which the
execution wrapper must map to original reference-current-prepared artifact rather
than confuse with new prepared load deck. No reduced simulation result yet.
Long clock69276 remains active; no layout or regulator qualification.

## Pass 391 — reduced measured-load replay fails at initial timestep

Previous pass prepared full-resolution current history. Confirmed clock3595903
live. Added measured_load_replay.py/run_reference_load_replay.sh with exact
prepared-deck hashes and historical /baseline/frames mapping to /origin/frames.
Captured all source hashes before/after, preserving recorded load and actual
reference circuit. Run terminal return1, timestep too small at5e-16s naming
vrefl_res#branch, only2rows. Not a rail-reproduction result.

New check_reference_load_replay.py validates terminal artifacts and records
incomplete horizon separately from performance. [Evidence](evidence/reference-load-replay.json).
Original saved current timestamps start0,50fs,100fs,200fs; literal PWL replay
creates source breakpoints absent as sources in the original coupled circuit.
This is a candidate numerical sensitivity, not a proven mechanism or physical
impossibility. Failed scratch transceiver-reference-load-replay preserved.

Next isolate load-source startup representation with a controlled equivalent or
quantified approximation and a tiny preflight before another full run. Preserve
reference FETs, reservoirs and failure evidence; do not resize devices in response
to this initial-step failure. Long clock69276 remains live; no layout.

## Pass 392 — quantified startup-source change resolves short replay failure

Previous pass verified reduced replay initial-step failure. Clock3595903 confirmed
live. Added load_startup_screen.py/run_reference_load_startup.sh:3cases of same
reference circuit shortened to1ns, with recorded source, DC initial-current control,
and recorded source whose first1ps is linearly interpolated between endpoints.
Later waveform retained. No FET/reservoir/solver parameter change.

Session46144 terminal: recorded fails5e-16s; DC and startup1ps complete1ns.
Validated before/after source hashes, artifact hashes, finite vectors and horizons.
Startup approximation changes high current at most12.04pA, low0.510pA over first
1ps (verified at union of piecewise-linear knots); later history unchanged.
This establishes sensitivity to source startup representation, not underlying
solver mechanism or physical regulator repair.
[Evidence](evidence/reference-load-startup.json).

Next run full209.9ns recorded-load replay with explicitly quantified first1ps
approximation and compare rail trajectories to connected baseline. Do not claim
reproduction from1ns completion or silently discard startup change. Both directions
of later current demand retained. Long clock69276 remains active; no layout.

## Pass 393 — full startup-adjusted measured-load replay running

Previous pass isolated source-startup sensitivity with completed1ns controls.
Clock3595903 confirmed live. Added prepare_reference_load_startup_full.py: exact
completed startup1ps source bytes, unchanged full209.9ns circuit. Retains quantified
12.04pA/0.510pA initial approximation explicitly. Added wrapper and --startup
shared checker, strengthening original waveform hashes and header validation.
Existing1mV maximum rail-reproduction diagnostic limit retained, not changed after
observing candidate data.

Launched session84439/PID3633853, scratch transceiver-reference-load-startup-full,
900s timeout. Confirmed live beyond80.5ns, so initial failure resolved for this
representation; requested horizon/reproduction still pending. No restart.
[Preparation](evidence/reference-load-startup-full-preparation.json).
Reproduce via verification/prepare_reference_load_startup_full.py then
verification/run_reference_load_startup_full.sh under project directory. Once
terminal run check_reference_load_replay.py --startup; it must compare rails to
connected recorded-current replay before reduced fixture guides circuit tuning.

Full measured-current replay currently not materially faster than ADC on this
run, likely due dense forced breakpoints; do not assume reduction saves runtime.
Any later compression must quantify waveform/charge error and verify reproduction.
Long autonomous clock69276 remains running. No reference qualification or layout.

## Pass 394 — full measured-load reduction reproduces reference rails

Previous pass launched startup-adjusted replay. Confirmed reference3633853 and
clock3595903 live, then observed existing84439 to terminal209.9ns without restart.
Shared checker --startup validates sources/artifacts, baseline hashes, expected
headers, finite ordered vectors and requested horizon. Maximum whole-waveform
rail difference versus connected current replay: VH123.163uV,VL63.843uV, within
predeclared1mV diagnostic limit. First1ps12.04pA/0.510pA approximation retained in
evidence. [Reproduction](evidence/reference-load-startup-full.json).

Reduced fixture is useful for reference screening at this fixed history, not
voltage-dependent ADC equivalence.212859rows versus42618in parent and comparable
runtime show dense PWL breakpoints carry cost; no assumed speedup. Any changed
reference driver must return to fullADC, since load may change with rail behavior.
Next use one controlled reference-dynamics experiment, guided by delivered versus
reservoir current, and score decision-time error/recovery plus power; avoid blind
geometry/capacitance growth already explored historically.

Updated living workflow with validated reductions, appropriate timing windows,
separate scoped audits and measured-source breakpoint lessons. Long clock69276
continues (last observed beyond2.120us); no layout or circuit qualification.

## Pass 395 — controlled high-reference damping candidate launched

Previous pass verified measured-load rail reproduction. Clock3595903 confirmed
live. Reviewed prior AC compensation results: largest earlierRZ parameter2000;
current high reference usesS4,CC2p so physical series resistor500ohm/CC8pF.
Prepared only highRZ2000->4000 (physical500->1000ohm); low reference, capacitors,
FETs, recorded signed load and solver unchanged. Exact reversal and identical
load hash verified; no original circuit source edit.

Launched session94914, scratch transceiver-reference-high-rz,209.9ns/900s timeout.
Hypothesis: reduce recovery ringing without geometry/power growth; potential cost
is slower recovery. New check_reference_high_rz.py validates exact substitution,
compares all24decision windows for both rails and active-window supply power.
Pending checker passes; no result/adoption claim. [Preparation](evidence/reference-high-rz-preparation.json).

Reproduce prepare_reference_high_rz.py/run_reference_high_rz.sh under verification;
observe existing94914, then checker. FullADC reintegration mandatory for any
retained candidate because measured demand is voltage independent. Long clock
69276 continues; no layout or reference qualification.

## Pass 396 — high-reference resistor tradeoff insufficient; not promoted

Previous pass launched single-parameter candidate. Confirmed clock3595903 and
candidate3637466 live, observed existing94914 to terminal209.9ns. Strengthened
checker validates actual identical load bytes, exact substitution, headers and
source/artifact hashes; evaluates all24decisions plus active rail range/power.
No repeated simulation or threshold change.

High worst decision error61.329->57.676mV improves only3.653mV, while worst
in-window motion3.146->4.703mV worsens.3/24high-rail decision windows regress.
Low worst error20.173->20.209mV essentially unchanged. Active reference supply
44.4228->44.4300mW (+7.19uW). High rail active minimum2.04199->2.04258V changes
little. Candidate does not solve droop and worsens a competing metric; retain
for diagnosis only, original reference baseline unchanged.
[Comparison](evidence/reference-high-rz.json).

Next stop reference series-resistor tuning. Use measured impulse/recovery evidence
to assess a materially different delivery/recovery approach (with explicit power,
headroom and reservoir costs) before another fullADC run. Do not repeat prior
blind output scaling. Actual RF-filter drive and full-chip obligations remain.
Long clock69276 remains active; no layout or reference qualification.

## Pass 397 — audit ADC-generated pulse before another regulator topology change

Previous pass rejected promotion of high-reference resistor candidate. Clock
3595903 confirmed live. Inspected actual CDAC rail selectors and code driver:
true B and complement BB use asymmetric inverter paths, shared complementary
controls select VH/VL on each bottom plate. Reference pulse may include capacitor
charging, overlap and other switching; amplifier-only assumptions are premature.

Added audit_cdac_reference_pulse.py with scoped-current and source/artifact hashes.
At selected123.7775ns72.66mA high-demand peak, saved B7=-0.0008V,BB7=3.2996V,
so MSB gate transition alone cannot explain that instant. Whole-record active
peak94.07mA at158.4325ns occurs with B7=2.9245V,BB7=0.5726V. Other selected
frame peaks~57.78mA have MSB gates already railed. This does not exonerate earlier
MSB activity or other bits. SD0..7 saved upstream commands are not physical gates.
[Audit](evidence/cdac-reference-pulse-audit.json).

Next observation replay should expose actual per-bit gates/bottom plates and
reference-switch branch currents with verified current conventions. Distinguish
avoidable crossover from necessary capacitor charge and control-reset transitions
before choosing break-before-make or another output stage. Preserve measured-load
reference fixture for amplifier comparisons; it cannot diagnose changing ADC
load topology. No circuit change/adoption or layout. Long clock69276 continues.

## Pass 398 — per-bit terminal-current probes preflighted; full replay launched

Previous pass audited pulse/control ambiguity. Clock3595903 confirmed live.
Added cdac_branch_replay.py: inlines exact CDAC selector cell with0Vreference-port
sensor feeding both FETs, all device sizes/controls unchanged.64terminal-current
vectors cover I/Q,8bits,positive/negative plates,high/low selectors. Adds physical
bit gates/bottoms (124newvectors total after deduplication). Exact observation
substitution reversal and source hashes retained.

1ns preflight19438 completes, exports all64current vectors, finite expected-width
waveform and requested horizon. [Evidence](evidence/cdac-branch-preflight.json).
This verifies names/connectivity only. Launched full209.9ns replay via
verification/run_cdac_branch_replay.sh, scratch transceiver-cdac-branch-replay,
900s timeout; observe returned execution session. Must compare original analog
vectors/codes/edge timing and independently sum reference-port currents against
prior rail-demand measurement before attributing crossover or charge transfer.
Dummy MIM reference connection also contributes low-rail demand and must be
accounted for in KCL; do not silently call its remainder missing switch current.

No switch or driver redesign adopted. Long clock69276 continues. No layout.

## Pass 399 — branch replay checks include independent high-rail current closure

Previous pass preflighted sensors and launched replay. Confirmed branch3644708
and clock3595903 live; branch beyond14.9ns at first observation, no restart.
Added check_cdac_branch_replay.py with source/artifact/header checks, completion,
original named-vector error report,10uV selected analog reproduction gate and
both-channel captured-code/rail comparison. Pending branch passes.

Independent high-rail KCL test sums32selector-port currents across both ADCs and
compares with driver-delivery minus reservoir current; predeclared1nA residual
limit for diagnostic validation. Low rail includes four unsensed dummy MIM
terminals (one per top plate) and is explicitly reported as an unclosed residual,
not unexplained switch current. Terminal branch currents include displacement;
opposed signs alone cannot prove conductive crossover.

Next observe37965, run checker and inspect reproduction/current closure before
attributing per-bit pulses. If these gates pass, rank branch contributions at
known peaks and relate to actual gates, then determine what additional evidence
separates charge transfer from overlap. Long clock69276 continues; no layout.

## Pass 400 — localize the failed CDAC probe reproduction before attribution

Previous turn made workflow documentation progress. Revalidated the current
artifacts: branch replay completed209.9ns; long clock process3595903 remains live,
observed beyond3.63us. No restart or circuit candidate promotion.

The branch replay has identical exported timestamps and captured I/Q codes.
Independent high-rail KCL residual is0.1273pA, but the original10uV analog
reproduction gate fails: HN/Q_HN reach10.7496uV difference at158.4225ns, with
three samples each above the limit,158.4175–158.4275ns. Other selected nodes
remain below10uV. The post-inspection decision-window audit finds at most0.398uV
plate difference and0.0672uV reference difference; this localization does not
replace the original gate or authorize branch attribution.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_cdac_probe_difference.py`.
[Evidence](evidence/cdac-probe-difference.json) verifies parent artifact hashes
and retains the failed gate. Next discriminating test: matched original/sensor
circuits with tighter solver tolerances, checking convergence of both traces
before interpreting current peaks. Do not loosen the threshold simply because
the failure is small. Low-rail closure still excludes four dummy capacitors.

Whole-chip priorities remain autonomous clock quality and complete RF/converter
signal quality. This measurement-validity issue must not become indefinite ADC
parameter tuning. Extended PLL is pending; RF noise/linearity, physical bias,
connected RF-to-ADC loading, wired recovery and host timing remain unqualified.
No layout or tapeout-readiness claim.

## Pass 401 — tighter-tolerance matched test exposes startup sensitivity

Pass400 made diagnostic progress, preserving a failed reproduction threshold.
Added cdac_probe_convergence.py and run_cdac_probe_convergence.sh: original and
sensor decks, unchanged full209.9ns history and5ps maximum step, only explicit
reltol1e-5/abstol1e-14/vntol1e-8 and output paths changed. Exact reversal,
parent/source hashes and fresh per-case directories preserve both originals.
Sequential cases use1800s timeout each in the pinned simulator.

Launched session83422, runner3659042. Original-circuit case failed initial
transient at vrefl_res#branch after4.72s; the sensor case was confirmed running
beyond49ns. Do not restart it. This result prevents the proposed matched
convergence comparison; smaller tolerances are not automatically a better or
usable simulation. No physical circuit change or threshold relaxation adopted.

Added check_cdac_probe_convergence.py; current evidence records
comparison_unavailable and the failed baseline, leaving sensor status pending.
Checker preserves failures and would compare both original/tightened trajectories
and captured codes only after both cases complete successfully. Reproduce from
repository root with `python3 projects/programmable_transceiver_platform/verification/check_cdac_probe_convergence.py`.
Artifacts: scratch/transceiver-cdac-probe-convergence;
[evidence](evidence/cdac-probe-convergence.json).

Next inspect the running sensor result, then isolate which tolerance changes
trigger startup failure in cheap matched short tests before another full replay.
Avoid replacing actual circuit startup with prebias just to obtain a passing
comparison. Long autonomous clock3595903 was independently confirmed live beyond
3.74us. Both circuit quality and numerical robustness remain unqualified.

## Pass 402 — isolate startup failure to absolute-current tolerance

Pass401 produced new failed-run evidence. Confirmed existing full sensor replay
3659042 and extended PLL3595903 live; neither restarted. Added a matched1ns
startup matrix using unchanged original/probed circuits, default options and
individual reltol1e-5, abstol1e-14, vntol1e-8, plus the combined settings.
Deck reversal checks restrict changes to horizon, declared options and output
path. Sources, parent decks and artifacts are hashed; failed cases retained.

All10cases terminal, verified by check_cdac_probe_startup.py. Baseline default,
reltol-only and vntol-only complete1ns. Baseline abstol-only and combined fail
at time0, reporting vrefl_res#branch. All five probed cases complete1ns.
This isolates a sufficient numerical trigger in this fixture: tightening only
absolute-current tolerance reproduces the failure. It does not establish a
physical defect or prove that the successful settings produce accurate dynamics.

Reproduce checks from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_cdac_probe_startup.py`.
Runner: verification/run_cdac_probe_startup.sh, terminal session68917.
Artifacts: scratch/transceiver-cdac-probe-startup;
[evidence](evidence/cdac-probe-startup.json). Next full matched comparison should
use reltol-only, whose startup passed both circuits, keeping the failed original
10uV reproduction gate unchanged. Do not claim convergence from startup success.
At final observation, existing combined-tolerance sensor run was live beyond147ns;
its baseline already failed, so it cannot provide a complete matched comparison.
No circuit redesign adopted; no layout work.

## Pass 403 — launch matched reltol-only history with current-closure checks

Pass402 completed the startup discriminator. Confirmed live clock and prior
sensor run before starting new work. Launched run_cdac_probe_reltol.sh, session
64348, runner3663714, scratch/transceiver-cdac-probe-reltol. Both original and
probed decks retain209.9ns history and5ps maximum step; only reltol1e-5 and output
paths change. The original combined-settings runner remains unchanged because
it was live and hashes its own source. The new runner preserves exact reversal,
parent/source hashes and1800s timeout per sequential case.

Extended the existing checker with --reltol, keeping separate evidence paths,
matching explicit options, original-versus-tightened trajectory comparisons,
captured codes, and independent high-rail KCL with the original1nA limit. The
low-rail dummy-capacitor omission remains explicit. No attribution runs while
results are pending; no tolerance threshold relaxed.

Current reltol baseline confirmed live beyond31ns. Previous combined-settings
sensor run completed209.9ns with45028rows, while its paired original circuit
failed startup; matched comparison remains unavailable. Extended PLL3595903
confirmed live beyond4.12us. Observe existing jobs; no restart.

Reproduce check from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_cdac_probe_convergence.py --reltol`.
[Pending evidence](evidence/cdac-probe-reltol.json). Next inspect matched
reproduction, changes from original trajectories, codes and current closure;
only then decide whether terminal-current attribution is supported. Full-chip
clock/RF/converter obligations remain open; no layout or circuit promotion.

## Pass 404 — reconcile whole-chip inventory and identify actual loading gate

Pass403 launched a controlled experiment and added checks. Confirmed current
PLL3595903 and reltol replay3663714 live; latter beyond67ns at observation.
No restart. Reviewed all10implementation obligations rather than extending ADC
solver work without reconsidering system risk.

Updated stale synthesizer gap to distinguish the completed3.201us latest-load
loop from older failed loops. Updated ADC/reference entries to retain the failed
reproduction gates, completed scoped current evidence, rejected compensation
candidate and pending reltol comparison. Historical easier capacitor cases are
not the current receiver-interface qualification. Added exact evidence links.

The next physical integration gate is now explicit: actual two receiver filters
must drive actual dual sample-driver/switched-ADC loads, with matched unloaded
and loaded waveforms. Current ideal1kohm sources at1.07V common mode do not
establish that compatibility. Prioritize this after the active discriminator,
while retaining clock-noise/startup and complete RF quality as leading risks.
Updated risk-priorities current entry; prior pass380 entry retained as historical.

Ran audit_schematic_inventory.py: all10declared block source sets present;
whole_chip_schematic_ready remains false. This is a structural audit, not circuit
qualification. Both live simulations remain pending; no new layout or promotion.

## Pass 405 — prepare actual RF/filter-to-dual-ADC schematic

Pass404 reconciled implementation evidence. Confirmed PLL3595903 and reltol
pair3663714 live before independent integration work. Added
prepare_rx_adc_connected.py, composing retained actual RF/LNA/mixers/two filters
with the actual dualADC/shared references/sample drivers. Both parent artifact
sets verified against successful unchanged-source results and hashes.

Prepared scratch/transceiver-rx-adc-connected-prepared/connected.spice and
manifest: separate hierarchical RF/ADC instances prevent node collisions. Filter
FIP/FIN/FQP/FQN connect directly to ADC GP/GN/Q_GP/Q_GN. Removed exactly eight
ideal-input/source-resistor fixture lines. Actual sample drivers, switching loads,
reference circuits and control remain. Seven ADC clock/reset/mask sources shift
nonzero events by400ns, preserving repeated timing; first hold is470ns.
RF seed conditions are explicitly qualified by the receiver instance name.

Important integration change: RF UIC now applies to ADC too. A400ns delay is an
experiment allowance, not evidence that biases settle. Ideal supplies, bias
sources, RF control and ADC clocks remain; this is not the autonomous-loop or
whole-chip schematic. Prepared610ns history has not been elaborated or run.
Do not infer connectivity success from file generation.

[Preparation evidence](evidence/rx-adc-connected-preparation.json). Next short
elaboration preflight, additional physical control/bias/code probes, matched
unloaded receiver history, and actual bias verification before scoring analog
acquisition or conversion. Current reltol/PLL runs continue; no restart/layout.

## Pass 406 — connected receiver/ADC elaboration completes, startup unqualified

Pass405 prepared the actual connected schematic. Confirmed PLL3595903 and reltol
replay3663714 live. Added rx_adc_preflight.py and run_rx_adc_preflight.sh: exact
prepared deck with only1ns horizon and28additional physical bias/control/code
probes. Recursive include/model hashes before/after, parent/deck hashes and
artifact hashes retained. Session88542 completed, return0,516rows,46columns.

check_rx_adc_preflight.py verifies exact exported header, finite values, strictly
increasing time, requested horizon and provenance. All checks pass for elaboration.
[Evidence](evidence/rx-adc-preflight.json). Twelve duplicate reservoir-subcircuit
warnings remain: RC splitter includes reservoir_mim.spice and the composed deck
also includes it directly. Inspect and remove the duplicate include before a
full run, preserving the failing/warning-bearing artifact.

At1ns ADC BN~0.149V, BP~2.940V, reference RBN~0.0647V and RBP~2.963V. These are
startup observations, not settled bias or headroom qualification. Retain the
explicit UIC caveat; a delayed first conversion must be supported by actual bias
and signal histories. Next deduplicate includes, prepare matched unloaded
receiver, and verify startup before conversion interpretation. No layout.

## Pass 407 — remove duplicate reservoir include with exact waveform replay

Pass406 completed connected elaboration. Revalidated clock3595903 and reltol
runner3663714 live; reltol sensor case beyond125ns at first observation.
Added --dedup variant to the terminated preflight runner/wrapper/checker; unique
scratch/transceiver-rx-adc-preflight-dedup preserves original warning-bearing run.
Only removed the direct reservoir_mim.spice include, verifying that rc_split.spice
already supplies it. No device, clock, initialization or load change.

Session79186 completes1ns,516rows,46columns, no warnings. All exported values
and timestamps are bit-identical to the original preflight; artifact.dat hashes
match. Checker verifies parent/source/artifact hashes, expected exports, exact
deck reversal and waveform identity. Include cleanup is accepted for subsequent
integration fixtures, not as startup/performance qualification.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_rx_adc_preflight.py --dedup`.
[Evidence](evidence/rx-adc-preflight-dedup.json). Next prepare matched longer
loaded/unloaded receiver histories from this clean deck, retain physical bias
and control probes, and examine ADC UIC startup before conversion scoring.
Current PLL and reltol runs continue; no circuit-performance claim or layout.

## Pass 408 — matched loading preflight and completed reltol comparison

Pass407 verified include cleanup. RelTol pair64348 now terminal: both209.9ns runs
complete; interpolated selected analog probe-versus-original difference4.0685uV,
below unchanged10uV criterion, codes match all four runs and high-rail KCL passes.
However tightening tolerances changes plate trajectories by~1.90mV versus default
runs, much larger than probe insertion alone. Numerical convergence remains
unproven; original failed gate remains failed. Do not treat matching codes as
analog accuracy. Evidence: cdac-probe-reltol.json, shared checker --reltol.

Added rx_adc_loading.py/run_rx_adc_loading.sh. Loaded circuit uses direct actual
filter-to-driver connections. Isolated control inserts four ideal unity VCVSs,
removing ADC back-loading while retaining ADC circuits and clock-source events.
This is only a comparison fixture, not a proposed buffer or narrower product.
Full schedule610ns, first hold470ns,2ps maximum step, seeded UIC unchanged.

Initial1ns pair88422 exposed invalid v(XADC.GP/GN/Q_GP/Q_GN) exports: subcircuit
ports alias top-level nodes. Both runs returned0 despite export errors; retained
scratch/transceiver-rx-adc-loading-preflight. Removed redundant invalid exports,
retaining top-level filter nodes, actual driver outputs/biases and controls.
Corrected pair75962 in ...-preflight-v2 completes both cases with516rows,
58expected finite columns, monotonic timestamps and no errors/warnings.
Source/artifact/header/horizon checks verified by an explicit Python audit;
[evidence](evidence/rx-adc-loading-preflight.json). No settled-bias claim.

Launched full sequential loaded/isolated610ns histories in
scratch/transceiver-rx-adc-loading via run_rx_adc_loading.sh; preserve returned
execution handle and observe, do not restart. Timeout10800s per case; extrapolated
preflight cost is roughly an hour per case but adaptive stepping may differ.
Clock3595903 was also confirmed live this pass. Next inspect bias histories and
loaded/unloaded filter response before conversion scoring; no new layout.

## Pass 409 — localize tolerance sensitivity to switching intervals

Pass408 completed numerical evidence and launched connected loading. Verified
PLL3595903 and loading3681439 live (session95508); loaded waveform beyond4.29ns
at observation. No restart. Added audit_cdac_tolerance_windows.py using completed
hash-checked default and reltol-only original/probed ADC runs.

Both pairs show global plate maxima near158.389–158.425ns. Across original and
probed comparisons, time-weighted held differential change is at most0.608uV;
checked predecision plate errors at most1.572uV and reference errors at most
2.578uV. This localizes the~1.9mV global discrepancy to other intervals rather
than establishing a millivolt shift in held accuracy. Explicit interpolation
without time alignment; held integration includes exact window endpoints.

[Evidence](evidence/cdac-tolerance-windows.json). Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_cdac_tolerance_windows.py`.
Post-inspection analysis introduces no new pass threshold and does not establish
numerical convergence. Original reproduction failure remains preserved. Peak
current interpretation is still sensitive to sharp events and needs comparison
of signed charge and peak amplitude across settings before causal attribution.
Existing acquisition findings are less sensitive in the measured decision windows,
but this does not establish noise/ENOB or physical variability tolerance.

Next prepare the running loading experiment's startup/window checker and evaluate
current pulse sensitivity from saved probed histories. No layout or promotion.

## Pass 410 — compare pulse charge and peak sensitivity on saved histories

Pass409 localized voltage sensitivity. Confirmed clock3595903 live beyond4.93us
and connected loading3681439/session95508 live beyond37.6ns. Added
verification/audit_cdac_current_sensitivity.py: hash-checked completed default,
reltol-only and combined-tolerance probed runs; independent high-rail branch sum
closure; native-grid signed peaks and endpoint-interpolated signed/absolute charge
in three frame windows and122.5–125.35ns preceding worst reference decision.

Across selected comparisons, maximum signed-charge change2.592fC, maximum peak
change2.051mA. Worst-decision high charge11.31075pC default versus11.31111pC
reltol; high peak72.6629mA versus72.5744mA. Larger frame peak changes show why
instantaneous maxima should not alone select driver sizing. These finite tests
support charge as a less sensitive diagnostic, not full numerical convergence.

[Evidence](evidence/cdac-current-sensitivity.json). Reproduce from repository
root with `python3 projects/programmable_transceiver_platform/verification/audit_cdac_current_sensitivity.py`.
The original failed analog gate and failed combined-tolerance unprobed startup
remain preserved. Low-rail sum still omits dummy capacitors. Terminal currents
include displacement, so neither signed charge nor opposing currents prove
conductive overlap. Next branch analysis should compare integrated contributions
as well as peaks, using the validated reltol matched pair and explicit limits.
Connected RF/ADC startup and autonomous clock results remain pending; no layout.

## Pass 411 — separate peak contributors from net reference charge

Pass410 completed sensitivity evidence. Confirmed clock3595903 and connected
loading3681439 still live. Extended analyze_cdac_branch_peaks.py with --reltol:
requires completed matched10uV reproduction, identical captured codes and independent
high-rail KCL; checks evidence/manifest/result/artifact hashes. Original default
path still rejects its failed gate. Added signed/absolute per-bit charge and
independent sum checks at peaks and over each integration window.

At123.7765ns preceding the worst reference decision, high demand72.5744mA is
primarily bit6 (48.6078mA); bit7 contributes16.8023mA. Yet over122.5–125.35ns,
bit7 supplies10.8303pC net high-rail demand and bit6 only2.29594pC, with other
bits returning charge. Total is11.3111pC. Thus the highest instantaneous branch
and dominant net charge are different. At158.4385ns, bit7 dominates90.3679mA
of94.2810mA total; bit6 returns13.8248mA at that instant. Physical gates/bottoms
and both port currents are retained in the report.

[Evidence](evidence/cdac-branch-peaks-reltol.json). Reproduce from repository
root: `python3 projects/programmable_transceiver_platform/verification/analyze_cdac_branch_peaks.py --reltol`.
Terminal currents include capacitor displacement and cannot prove switch overlap.
Next hypothesis: distinguish bit6 transition-driven redistribution from bit7 net
charge demand by examining complete B/BB/bottom trajectories and a controlled
switch-drive timing change; do not redesign solely the largest peak branch.
Full numerical convergence, ADC precision and RF integration remain unqualified.
No circuit candidate adopted or layout started.

## Pass 412 — physical gate history narrows the next controlled change

Pass411 produced matched branch attribution. Confirmed live PLL3595903 and
loading3681439. Added audit_cdac_gate_history.py with chained evidence/result/
artifact hashes, actual B/BB crossing interpolation and both reference-port
currents, top/bottom voltages at branch peaks for bits6/7 in both ADCs.

In122.5–125.35ns, bit6 B rises through1.65V at123.696462ns; BB falls through it
at123.739599ns (43.14ps later). Positive-plate high peak24.4996mA occurs at
123.776544ns, with low-port+0.1994mA. At the later bit7 high peak158.438521ns,
high45.3993mA accompanies low+0.3090mA. These same-signed, unequal port currents
at the sampled peaks are not a simple equal/opposite rail-through-current
signature. They do not exclude overlap elsewhere or resolve displacement current.

[Evidence](evidence/cdac-gate-history.json). Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_cdac_gate_history.py`.
1.65V is only a logic timing marker, not a transistor conduction threshold.
Next controlled physical hypothesis: change only bit6 complement-driver strength
in both channels, retaining actual FET gates and the same clock/history. Compare
rising/falling transitions, integrated reference charge, peak shape, all reference
decision windows and held/captured accuracy; reducing one peak alone is insufficient.
No candidate adopted. Receiver integration and clock results remain pending.

## Pass 413 — controlled bit6 complement-driver candidate launched

Pass412 established gate-history evidence. Confirmed PLL3595903/loading3681439
live. Added bit6_complement_screen.py and run_bit6_complement.sh. Candidate uses
completed reltol probed209.9ns baseline, actual shared references, all original
clocks/inputs and current sensors. A separate subcircuit changes only X2 inverter
multiplicity4*WEIGHT to8*WEIGHT, instantiated only at I/Q bit6. Existing circuit
source and baseline artifacts remain unchanged. Stronger X2 also increases its
input loading; this is a physical sizing change, not a pure imposed delay.

Verified exact deck reversal and independent subcircuit reversal, parent/source/
deck hashes; declared evaluation includes both gate transition directions, pulse
charge/peaks, reference decision errors, held voltages, codes and driver energy.
No passing threshold invented and no adoption based solely on smaller peaks.
[Preparation](evidence/bit6-complement-preparation.json).

Launched session84232, runner3688811, scratch/transceiver-bit6-complement,
1800s timeout; confirmed beyond17ns. Next completion and comparison checker
against scratch/transceiver-cdac-probe-reltol/probed, with all24reference windows
per rail and both channels. Current RF-loading/PLL runs continue; no restart or
layout. Candidate is unqualified until measured results support a decision.

## Pass 414 — prepare candidate tradeoff checker before results

Pass413 launched a controlled circuit experiment. Confirmed all three jobs live:
PLL3595903, connected-loading3681439 and bit6-candidate3688811, candidate beyond
47ns at observation. Added check_bit6_complement.py, pending evidence only.

Checker verifies source/artifact hashes and exact declared deck reversal before
comparing actual completed trajectories. Requires successful return, no warning/
error/abort, complete209.9ns horizon, exact named header, finite data and strictly
increasing timestamps. Measures48reference decision windows, six held values,
six captured-code windows, both rails' pulse peaks/charge, four bit6 gate midpoint
histories and VDRV energy over60–209.9ns. Native grids and explicit endpoints;
no per-case waveform alignment or acceptance threshold added after inspection.

Ran pending path successfully; completed-result analysis remains unexercised
until the candidate finishes. [Pending evidence](evidence/bit6-complement.json).
Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bit6_complement.py`.
Next inspect tradeoffs and reject or retain based on all relevant measurements,
not solely smaller peak current. No physical performance result claimed this
pass; active simulations were not restarted. Layout gate remains closed.

## Pass 415 — connected loading checker separates bias observations from codes

Pass414 added pending candidate checks. Confirmed three live processes: clock
3595903, loading3681439 beyond223ns, candidate3688811 beyond123ns. No restart.
Added check_rx_adc_loading.py: exact expected exports, parent/deck/source/artifact
hash checks, return/log/finite/time/horizon checks, explicit pending/failed states.
Native-window statistics include six bias nodes, both filter differential/common
modes and held differential windows at470/520/570ns. Full-history bias windows
350–400,420–450,450–469,500–519,550–569,590–609ns are declared before results.
No arbitrary bias stability pass threshold or early code qualification.

Exercised completed-data parsing/statistics on both corrected1ns preflights via
--preflight (both pass elaboration). Full run remains pending; late-window paths
await real completed data. Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_rx_adc_loading.py`.
[Pending full evidence](evidence/rx-adc-loading-check.json),
[verified preflight checker](evidence/rx-adc-loading-preflight-v2-check.json).

Whole-chip review: only one new driver sizing candidate is active; evaluate it
before any sweep. Loaded RF/ADC integration is now running and should outrank
further isolated peak tuning once complete. Autonomous intrinsic noise/cold
startup, complete RF quality, modulated TX, wired recovery, host timing and real
bias/pad/package coexistence remain unresolved. Stable bias windows alone cannot
establish device headroom or conversion quality. No layout or readiness claim.

## Pass 416 — reject stronger bit6 complement; audit integration schedule

Pass415 prepared useful checks. Clock3595903 and loading3681439 confirmed live.
Candidate84232/3688811 now terminal209.9ns,45035rows,return0. Completed
check_bit6_complement.py passes provenance/header/horizon checks and exercises
its full measurement path. Codes unchanged; maximum held change4.27uV.

Candidate narrows bit6 B/BB midpoint separation43.14→33.53ps but delays both
crossings due to added input load. Target high peak72.574→64.152mA, high signed
charge11.3111→11.2431pC. Worst high-reference error61.3485→61.2934mV improves
only0.0551mV. Low worst error20.1733→20.1319mV, but low decision motion
6.8769→6.9695mV and low peak magnitude65.595→68.151mA worsen. VDRV energy
683.84→758.20pJ over60–209.9ns (+10.87%). Reject as a regulation fix; retain
original4*WEIGHT complement driver. Smaller peak does not repair sustained
reference accuracy. No additional driver-strength sweep justified by this result.
[Comparison](evidence/bit6-complement.json).

Added audit_rx_adc_schedule.py: independently parses all7actual PWL sources,
verifies voltage values and400ns shift against parent, sampler complements,
8comparisons/frame at5ns spacing and reset release. Holds470/520/570ns;
repeated fully-on command tracking9.9ns and release-end to comparator-start0.4ns.
First470ns tracking is startup, not throughput evidence. Ideal-source timing
verification does not establish actual device skew or settling.
[Schedule](evidence/rx-adc-schedule.json). Reproduce checks from repository root
with verification/check_bit6_complement.py and verification/audit_rx_adc_schedule.py
under projects/programmable_transceiver_platform (python3).

Next prioritize completing actual loaded receiver assessment over further bit6
sizing. Reference regulation still fails; autonomous clock quality remains open.
No layout or tapeout qualification.

## Pass 417 — catch inherited stimulus aliasing and insufficient code excursion

Pass416 rejected a physical candidate on measured tradeoffs. Confirmed live clock
3595903 beyond5.67us and loading3681439 beyond302ns. Independently audited the
running receiver fixture's measurement suitability using completed prior RF
conversion evidence and parsed actual ADC schedule.

Prior unloaded IF20.0003365MHz aliases to336.5Hz at20MS/s/channel. Prior I/Q
filter differential peaks3.60445/4.03776mV are0.461/0.517 nominal differential
8bit steps (ideal +/-1V range). Loaded amplitudes/frequency may differ, but the
inherited near-sample-rate tone and three-frame history cannot demonstrate useful
dynamic sampled-signal quality. This is a material measurement limitation, not
proof the architecture is impossible. Continue current run for bias/back-loading;
do not interpret steady/repeated ADC codes as successful RF conversion quality.

Added audit_rx_adc_measurement_budget.py and
[evidence](evidence/rx-adc-measurement-budget.json); updated ADC inventory with
this gap and explicit rejection of stronger bit6 complement. Next dynamic test
needs an in-band tone based on actual LO, adequate sample count and a measured
programmable gain/headroom budget. Do not simply raise RF input and assume
linearity to hide missing gain. Whole-chip objective remains unchanged; no layout.

## Pass 418 — finite filter gain prevents assuming resistor-ratio scaling

Pass417 identified aliasing and insufficient code excursion. Confirmed live
PLL3595903 and loading3681439. Reused stationary filter interface fixture for
four controlled AC/OP cases: RFB20k/40k at source CM0.9/1.177V,1kohm per leg,
unchanged actual FET filter,C20p,bias2.25V. feedback_gain_screen.py and
run_bb_feedback_gain.sh complete session52249; hashes and four-case coverage
verified by check_bb_feedback_gain.py. Both20k baselines match previous exported
AC trajectories exactly.

Source-to-output5MHz gain6.829→10.230 at0.9V CM and6.873→10.331 at1.177V CM.
At10MHz gain7.084→9.192 and7.145→9.291. At20MHz gain instead decreases
4.552→4.372 and4.580→4.395. Doubling RFB is therefore not doubling useful gain;
finite transistor gain, source loading and dynamics matter. Output DC CM remains
about1.071–1.087V in these cases. Modest adjustment does not establish adequate
RF-to-ADC dynamic range or a programmable gain implementation.

[Evidence](evidence/bb-feedback-gain.json). Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_feedback_gain.py`.
No candidate adopted: stationary AC excludes actual switched mixer/ADC interaction,
large-signal compression, noise and loop stability. Next use completed receiver
loading evidence to choose gain staging and an in-band dynamic stimulus rather
than an unbounded feedback-resistor sweep. Full-chip quality remains unqualified.

## Pass 419 — filter gain limitation is not nominal DC saturation loss

Pass418 completed four controlled AC cases. Confirmed PLL3595903 and loaded
receiver3681439 live; receiver beyond402ns at observation. Added
verification/audit_bb_feedback_headroom.py using exact saved OP artifacts.
For all24PMOS observations (six devices times four cases), model-reported positive
VDS matches physical source-minus-drain voltage within1e-10V. This verifies the
current operating-point convention rather than assuming NMOS sign conventions.

Worst reported VDS-minus-VDSAT margins:0.60027/0.59945V at source CM0.9V for
RFB20k/40k;0.54918/0.54623V atCM1.177V. No stationary saturation loss in these
cases explains the finite gain improvement. This is not dynamic headroom, output
swing, loop stability or process/noise evidence; a positive margin is insufficient
to qualify the filter. [Evidence](evidence/bb-feedback-headroom.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_bb_feedback_headroom.py`.
Next cheap gain-stage question is large-signal compression at both common modes,
keeping actual finite source impedance. Loaded RF/ADC and autonomous timing
remain leading live experiments. No resistor/device candidate adopted or layout.

## Pass 420 — filter DC swing quantifies higher-gain compression tradeoff

Pass419 verified stationary device headroom. Confirmed live clock3595903 and
receiver3681439. Added feedback_swing_screen.py/run_bb_feedback_swing.sh, retaining
four completed filter circuits and finite1kohm source legs. Replace only source
stimulus with common-mode plus signed differential sweep -0.4..+0.4V in5mV steps;
actual FET/filter/bias/RFB values unchanged. Exact circuit-body reversal and
source/parent/artifact hashes checked; all four cases complete161points.

check_bb_feedback_swing.py validates complete grids, headers, finite probes and
logs. Central source-to-output slopes about6.635/6.673 for20k and10.264/10.354
for40k at CM0.9/1.177V. At+50mV source differential,40k yields0.5112/0.5157V
output, gain ratios0.99614/0.99608 relative to central slope. At+100mV gives
1.0093/1.0179V but ratios0.98340/0.98307; at+200mV ratios0.89093/0.87992.
Both polarities, output CM and reported headroom retained in evidence. This
quantifies useful stationary swing and compression, not a dynamic qualification.

[Evidence](evidence/bb-feedback-swing.json). Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_feedback_swing.py`.
Central slope is a +/-5mV finite difference; no arbitrary compression threshold
or assumed whole-chain gain multiplication. No40k adoption: actual switched
loading, dynamic linearity, noise and programmable gain range remain unresolved.
Next prioritize completed connected receiver bias/loading evidence and in-band
stimulus/gain staging. Intrinsic autonomous timing, wired recovery and complete
RF quality remain system risks; no layout or scope reduction.

## Pass 421 — loaded receiver fails at coincident ADC source event

Pass420 completed DC evidence. Confirmed clock3595903 live beyond6.20us and
loading runner3681439 live. Loaded case now terminal return1 after983s wall:
478.5ns,239332rows, timestep too small at v.xadc.vrefl_res#branch. Sequential
isolated control has started and was confirmed beyond15.7ns; do not restart it.
Full checker correctly records loaded failure and isolated pending, not completion.

Added audit_rx_adc_failure.py: verifies failed artifacts and source consistency,
finite partial waveform, no backward times, reports repeated printed timestamps
and parsed actual source knots. Failure coincides with comparator-clock fall end
478.5ns and UPDATE fall start478.5ns (ends478.6ns). This is a concrete numerical
suspect, not proof of causality or physical impossibility.

Preceding450–469ns BN range~0.0397uV, reference RBN~0.0382uV; VH range0.937uV
and VL0.742uV. Earlier350–400ns VL drift~19.45uV reduces to~2.04uV over420–450ns.
These partial histories show bias charging is much reduced before conversion,
but do not establish full device headroom, noise or completed startup/conversion.
[Failure evidence](evidence/rx-adc-loading-failure.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_rx_adc_failure.py`.
Next observe isolated control to distinguish whether failure requires receiver
back-loading before choosing a source-event or reservoir-sensor discriminator.
Do not fix a numerical failure by adopting ideal isolation as hardware. In-band
stimulus work remains necessary after reliable integration; no layout.

## Pass 422 — stationary references are inaccurate before numerical failure

Pass421 preserved integration failure evidence. Revalidated live clock3595903
beyond6.30us and isolated-control runner3681439 beyond47ns; no restart. Extended
partial failure audit with actual driver/plate nodes, target-referenced rail errors,
reference span and478.3–478.5ns interval. No partial run promoted to completion.

At450–469ns reference variation is below1uV, yet VH is13.148mV above its2.15V
target and VL10.477mV below1.15V. Reference span1.023624–1.023626V is stationary
but wrong. At475.2–475.35ns high error reaches41.828mV and span falls to
0.949197–0.955828V. Thus insufficient reference accuracy exists before the
478.5ns numerical failure, independently of whether that failure is repaired.
Driver/plate/filter histories around the failure are retained; finite preceding
voltages are not proof of physical stability at the failed timepoint.

[Updated partial evidence](evidence/rx-adc-loading-failure.json), reproducible via
verification/audit_rx_adc_failure.py. Next retain two separate obligations:
complete connected simulation and improve actual reference regulation. Await
isolated-control comparison before attributing numerical failure to back-loading.
No source-event workaround, ideal reference substitution or driver-sizing candidate
adopted. Full RF/ADC quality and autonomous noise remain unresolved; no layout.

## Pass 423 — standalone reference pair reproduces static offsets

Completed pair_target_dc.py/run_reference_pair_target_dc.sh and checker;41points
per target across+/-20mV, actual tuned pair with diode-derived biases, no external
DC load. VH nominal2.16314761V (+13.14761mV), VL1.13952163V (-10.47837mV),
local target slopes0.991617/0.998606. Thus the connected stationary offsets also
exist without ADC switching. This does not isolate device cause or repair dynamics.
[Evidence](evidence/reference-pair-target-dc.json). Initial dependency scan failed
on quoted/indented PDK paths; corrected parser and used fresh ...-target-dc-v2
output directory. No target precompensation adopted.

## Pass 424 — extended clock run killed; prepare selective-save recovery

Pass423 produced new DC evidence. Receiver-isolated runner3681439 remains live,
confirmed beyond119ns. Clock3595903 missing; authoritative result is terminal
return-9, not timeout, unchanged deck/source hashes, no waveform. Last logged
progress6.48592us of8.001us. No lock or phase conclusion available from progress.

Docker OOM event at1789919439 occurs1.87s before result-file timestamp. Event
is retained in the failed output directory and evidence; supports memory pressure,
though removed-container mount identity was not retained. Original deck uses
save all over millions of timesteps. Prepared ...-latest-rf-loop-selective-prepared
with only48actually exported vectors saved; exact reversal recovers original
circuit/deck, unchanged horizon/solver/stimuli. Not launched yet.

[Failure/preparation evidence](evidence/latest-rf-loop-memory-failure.json).
Next short selected-save reproduction against completed original waveform before
another long run; preserve killed run and do not equate resource failure with
physical clock impossibility. This is an evidence-based new experiment after
terminal confirmation, not a restart caused by observation timeout. No layout.

## Pass 425 — selective-save preflight completes; matched horizon still needed

Pass424 identified terminal clock kill and prepared reduced waveform retention.
Isolated receiver3681439 remains live. Added selective_loop_screen.py and
run_latest_loop_selective.sh. Short session86457 completes10ns with5022rows,
all48exports and unchanged source/deck hashes. Actual circuit, clocks and solver
options retained; save list restricted to exported vectors.

check_latest_loop_selective_short.py verifies artifacts against completed3.201us
baseline, but exact timestamp-grid assertion fails. Diagnostic interpolation is
now explicitly reported without calling reproduction passed. Reducing horizon
is an additional numerical change; must compare10ns save-all and10ns selective
runs before attributing differences to observation selection. No long rerun yet.
[Evidence](evidence/latest-loop-selective-short.json). Original killed8.001us
artifacts remain intact. Next matched-horizon control, then memory-monitored
extension only if relevant reproduction holds. No intrinsic noise/lock claim.

## Pass 426 — exact matched-horizon save-mode reproduction; long retry launched

Pass425 completed selective short run but longer-baseline timestamps differed.
Added --all-control at the same10ns horizon, terminated session83593. Independent
check_latest_loop_save_control.py verifies both successful source/artifact sets,
exact single-save-directive difference, finite expected shapes, monotonic time,
full horizon and no warnings/errors. All5022rows and all exported values/timestamps
are bit-identical. [Evidence](evidence/latest-loop-save-control.json).

This distinguishes observation selection from the previous horizon change; it
does not guarantee later numerical agreement or memory bounds. Updated runner
manifest to distinguish exported vectors from save mode. Wrapper now retains
container.id for memory/OOM provenance. Launched full8.001us selective-save run
in fresh scratch/transceiver-latest-rf-loop-selective; old killed run untouched.
Observe returned session and cidfile, not restart on observation timeout.

Reproduce short comparison from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_latest_loop_save_control.py`.
Isolated receiver3681439 confirmed live. Next monitor memory and actual progress
of the existing long jobs; clock lock/noise and complete RF conversion remain
unqualified. No circuit/horizon/solver relaxation or layout.

## Pass 427 — reference device probes reproduce DC baseline exactly

Pass426 verified save-mode equivalence and launched selective PLL retry74725.
Confirmed its container live at31.99MiB/4GiB early in run; isolated receiver
3681439 live beyond243ns. No memory-bound claim from this early observation.
Added pair_device_dc.py/run_reference_pair_device_dc.sh as observation-only
version of paired target sweep, saving internal nodes and model device quantities.
Both41point sweeps complete. check_reference_pair_devices.py verifies source/
artifact/header/log checks and bit-identical reproduction of every original
exported value, including targets, outputs, biases and supply current.

At nominal targets high-buffer reported tail margin-0.15311V; input branch
currents317.27/339.13uA. Low-buffer tail margin+0.08587V, input currents
582.19/603.66uA. This corroborates earlier headroom concern and exposes unequal
branch currents; not proof that tail headroom alone causes either offset.
Physical terminal orientation and mirror drain-voltage effects must be checked
before deciding on a topology change. [Evidence](evidence/reference-pair-devices.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_reference_pair_devices.py`.
Next separate tail compliance from finite mirror output resistance while preserving
actual output loading and dynamic-regulation obligations. Live RF/ADC isolation
and autonomous-loop retry remain primary integration evidence; no layout.

## Pass 428 — longer reference mirrors reduce nominal DC offsets

Completed pair_long_mirror_dc.py/run_reference_pair_long_mirror_dc.sh. Four mirror
FETs only (XMP/XMN in high/low buffers) change8u/.5u to16u/1u, preserving W/L
and multiplicities. check_reference_long_mirror.py verifies exact expanded-deck
change, both41point grids and source/artifact provenance. High error13.1476→
8.1984mV; low error-10.4784→-8.1400mV. Mirror-current differences21.86→10.06uA
and21.44→8.97uA. High tail margin remains negative(~-0.152V). Changing length
has multiple device effects, so this is not unique output-resistance causality.
[Evidence](evidence/reference-long-mirror.json). Candidate not adopted; added
capacitance, area and dynamic regulation must be checked.

## Pass 429 — longer-mirror candidate connected to actual switching ADCs

Pass428 produced controlled DC evidence. Confirmed isolated receiver3681439
live beyond311ns. Added reference_long_mirror_screen.py and wrapper; baseline
is completed reltol probed dualADC, unchanged clocks, inputs, targets, reservoirs,
sensors and209.9ns history. Inline reference cells change only four declared
mirror geometries; exact reversal and source/deck hashes retained. Launched
session16154 in scratch/transceiver-reference-long-mirror-frames,1800s timeout.

Extended shared check_bit6_complement.py --reference-mirror with independently
reconstructed reference-cell substitution and separate evidence path. Measures
existing decision/held/code/current metrics plus reference supply energy; baseline
bit6 comparison still passes after extension. Current mirror result pending.
[Pending evidence](evidence/reference-long-mirror-frames.json).

Next compare worst reference error and motion, all48decision windows, held signals,
codes and reference energy. A DC improvement alone does not promote the candidate.
Selective PLL retry74725 and isolated receiver continue; no layout.

## Pass 430 — reconcile risk inventory with active integration discriminators

Pass429 launched physical mirror candidate. Confirmed receiver3681439 beyond349ns,
mirror3721723 beyond68ns, and selectivePLL container active beyond0.309us. Retained
Docker inspect/stats snapshot in selective output resource-observations.jsonl:
93.71MiB/4GiB, OOMKilled false at observation. This is current resource evidence,
not a bound or full-run guarantee. No jobs restarted.

Updated current risk entry from stale404 to430 and reconciled implementation map:
actual loaded RF/ADC failure, pending isolated control, rejected bit6 sizing,
DC-improved but unadopted mirror candidate, killed extended clock and active
selective retry. Explicit in-band/gain/sample-history measurement obligations
remain alongside intrinsic clock/RF noise, real bias, TX/wired/host/coexistence gaps.
Ran structural inventory audit:10obligations, schematic ready false, layout closed.
Next finish these discriminators before new local parameter sweeps.

## Pass 431 — reject longer mirrors after actual switched-load regression

Pass430 updated authoritative risk state. Verified existing mirror16154 and
isolated receiver jobs live, waited without restart; mirror now completes209.9ns,
45035rows. Shared checker exposed a report-path variable collision: loop variable
name overwrote experiment name, writing candidate.json while named pending file
remained stale. Fixed dedicated experiment_name, reran both completed comparisons
and inspected the correctly named mirror evidence. Preserve generic artifact as
historical; use named regenerated reports.

Longer mirrors worsen high worst decision error61.3485→67.4409mV and low
20.1733→25.4086mV. High motion3.1786→3.0071mV improves slightly, but low
motion6.8769→8.0894mV worsens. Ten of48target-error windows regress; captured
codes change and max held differential change0.2453mV. Reference mean power
44.4228→44.4181mW is effectively unchanged for this comparison. Reject candidate
as a connected regulation repair despite its DC offset benefit; retain original
mirror geometry. [Evidence](evidence/reference-long-mirror-frames.json).

Next focus on reference topology/loop response and completed receiver isolation
rather than further mirror-length sweeps. Isolated receiver and selective PLL
remain live; no layout or precision/noise qualification.

## Pass 432 — isolation also fails478.5ns; signed edge discriminator launched

Pass431 rejected mirrors on dynamic evidence. Loaded and isolated receiver cases
now both terminal return1 at478.5ns,239332rows; isolated failure names eisoqn#branch
rather than low-reference sensor. check_rx_adc_loading.py preserves both failures.
Thus receiver back-loading is not necessary for this observed numerical failure;
no inference of physical impossibility or unique solver cause.

prepare_rx_adc_event_offsets.py verifies both failed artifact sets and prepares
actual loaded circuit with only UPDATE fall478.5–478.6ns shifted by-10ps/+10ps.
All other events, loads,targets,610ns horizon and solver unchanged. Exact reversal
and independent token comparison verify exactly two time tokens per variant.
This changes pulse width slightly; completion would indicate timing sensitivity,
not a qualified hardware repair. [Preparation](evidence/rx-adc-event-offset-preparation.json).

Added rx_adc_event_offsets.py/run_rx_adc_event_offsets.sh; launched sequential
signed cases session88961 in scratch/transceiver-rx-adc-event-offsets,10800s timeout
per case with source/deck/artifact hashes. Original failed histories untouched.
SelectivePLL74725 remains active beyond0.68us at observation. Next inspect
signed completion and pre-failure reproduction, reference accuracy and real
conversion behavior; no ideal isolation adopted, no layout.

## Pass 433 — pre-failure filter loading is small in continuous tracking

Pass432 established same-time failure with/without back-loading and launched
signed timing tests. Verified event-offset runner3728638 live beyond17ns and
selectivePLL74725 beyond0.832us. Added analyze_rx_adc_partial_loading.py on both
hash-checked terminal partial histories. Fits native-time weighted DC/trend,
IF plus LO harmonics and RF leakage; actual LO measured from crossings.
Windows240–400ns and300–460ns precede first hold470ns, about3.2IF cycles each.

Loaded/isolated amplitude ratios I0.998999/0.998998 and Q0.998996/0.998996 across
two windows. Loaded I~3.6006mV, Q~4.0337mV in first window. Approximately0.10%
amplitude reduction is consistent across windows; not a completed-conversion,
noise or ADC ENOB claim. Tests only long initial tracking, not repeated10ns
acquisition or conversion kickback. Inherited20MHz IF still aliases at20MS/s.
[Evidence](evidence/rx-adc-partial-loading.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_rx_adc_partial_loading.py`.
Next retain continuous-tracking compatibility evidence while requiring signed
runs to complete real conversions. No ideal isolation adopted as hardware and
no further sizing sweep. Layout gate closed.

## Pass 434 — signed-event checker preserves failure and earlier behavior

Pass433 extracted scoped loading evidence. Confirmed signed-run3728638 live
beyond57ns and selectivePLL74725 beyond0.943us. Added check_rx_adc_event_offsets.py:
source/artifact/header/horizon checks, exact prepared-edge/output-path reversal,
explicit partial failures and repeated exported timestamps. Compares eight
filter/reference/plate nodes over350–478.3ns before either modified edge using
unshifted interpolation; no new numerical passing threshold or promotion.

Exercised failure parsing and comparison against the original failed loaded
artifact via --baseline-audit: terminal failure retained, exact zero self-errors.
This verifies the failed-data path, not the still-pending completed-case path.
Default checker records both variants pending. [Self-audit](evidence/rx-adc-event-checker-self-audit.json),
[pending variants](evidence/rx-adc-event-offsets.json). Run from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_rx_adc_event_offsets.py`.
Next use real signed results to distinguish event sensitivity; no new circuit
sweep while the existing discriminator runs. Layout gate remains closed.

## Pass 435 — tuned reference impedance confirms dynamic tradeoff

Pass434 prepared signed-run checks. Confirmed signed runner3728638 live. Reviewed
prior impedance fixture: it used older untuned buffers, so it did not establish
current pair response. Added pair_impedance_compare.py/run_reference_pair_impedance.sh
using exact completed baseline/long-mirror DC circuit bodies plus actual2048unit
PDK reservoir on each rail. Four zero-DC-load AC injections complete481points
1kHz–1GHz; source/artifact/header/grid checks pass. No return-ratio claim.

Baseline high peak39.728ohm at29.854MHz; longer mirrors41.580ohm at same grid
frequency. Baseline low peak13.607ohm at84.140MHz; candidate15.646ohm at102.920MHz.
Low-frequency impedance improves1.243→0.806ohm high and0.422→0.356ohm low, while
peaking worsens, consistent with rejected switched-load behavior. Retained high
impedance at20MHz is35.450ohm; improving DC offset alone misses relevant dynamics.
[Evidence](evidence/reference-pair-impedance.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_reference_pair_impedance.py`.
Next reference design question is actual loop/output-stage response with reservoir
loading; no further mirror-length tuning or reopening rejected candidate solely
on DC results. Signed receiver tests and selectivePLL continue; no layout.

## Pass 436 — small bipolar load pulses expose slow high-reference recovery

Pass435 established current pair AC impedance. Added pair_step_screen.py and
run_reference_pair_step.sh from exact retained impedance circuit bodies, actual
2048unit reservoirs and unchanged compensation/bias. Four+/-1mA pulses on one
rail at a time,20–30ns with100ps edges,100ns horizon5ps maxstep. Session30261
completes all cases; check_reference_pair_step.py verifies complete
100ns transient horizons, named exports, finite monotonic data, logs and hashes.

High positive/negative loading produces26.30/27.78mV deviations relative to its
pre-pulse output; max remaining deviation40–50ns is16.81/17.48mV,60–70ns about
0.54mV. Low rail peak10.52–11.04mV,40–50ns1.44–1.61mV and60–70ns below8uV.
These are selected recovery-window maxima, not a declared settling-time pass.
[Evidence](evidence/reference-pair-step.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_reference_pair_step.py`.
Static offsets remain excluded from these relative excursions. The high rail has
much slower recovery even under modest pulses, supporting a loop/output-stage
redesign priority; full periodic CDAC regulation remains mandatory. No sizing or
compensation candidate adopted. Signed receiver and selectivePLL remain active;
no layout or scope reduction.

## Pass 437 — avoid repeating a rejected high-reference polarity swap

Previous turn improved the reusable workflow (progress). Revalidated live signed
receiver PID3728638 and selective PLL PID3719500; neither restarted. Reviewed
existing buffer DC sweeps before proposing another circuit change. Added
`audit_reference_polarity_reuse.py`: verifies four selected historical case
artifact hashes, source hashes, full 81-point finite sweep grids, reported versus
raw measurements, and exact seven-FET equality between original/tuned cells.
The only internal element change is resistance in series with an ideal capacitor;
this preserves ideal DC equations, not transient behavior.

At 2.15V target and S=4 the existing NMOS-input/output buffer has -64.05mV
unloaded error and about -185.9mV at +3mA demand, versus complementary buffer
+13.15mV and +9.72mV. Both sweep directions support the comparison. Do not repeat
an unchanged polarity substitution just to improve input-tail headroom: output
regulation also matters. Historical post-run-only source provenance stays labeled;
no new simulation or model qualification is implied.
[Evidence](evidence/reference-polarity-reuse.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_reference_polarity_reuse.py`.
Updated whole-chip risk review with terminal isolation/mirror outcomes and live
clock/receiver discriminators. Next reference work needs a topology that addresses
input compliance and source/sink output behavior together. Existing baseline
retained, full product scope unchanged, layout gate closed.

## Pass 438 — account for selected connected analog supply costs

Pass437 changed the next design decision by auditing the rejected polarity swap.
Verified existing receiver PID3728638 and selectivePLL PID3719500 live; no restart.
Added `audit_rx_adc_partial_power.py` against the terminal failed loaded receiver
artifact, checking hashes, named headers, finite data and each measured source's
hierarchical 3.3V definition. Integrates with explicit window endpoints on strictly
increasing local timestamps, excluding repeated failure timestamps.

In450–469ns tracking, filters consume28.816mW, four sample drivers21.696mW and
reference supply42.448mW: selected supply total92.961mW. During470.2–478.3ns early
conversion these total95.834mW, with reference supply45.316mW. These are delivered
supply energies, not measured heat, full-chip power, repeated-conversion averages
or a guaranteed lower bound in a circuit containing other ideal energy sources.
LNA/LO/oscillator, ADC digital/comparator/CDAC drivers and bias/control realization
remain outside this sum. The reference stage is already a substantial measured
cost; a new output topology must report incremental energy alongside regulation.
[Evidence](evidence/rx-adc-partial-power.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_rx_adc_partial_power.py`.
No schematic candidate adopted. Continue the existing signed-event and autonomous
clock jobs, then evaluate a reference topology change against actual source/sink
loading and power. Full schematic, RF quality, clocks and coexistence remain
unqualified; layout gate remains closed.

## Pass 439 — wider high-reference input pair is insufficient

Pass438 produced scoped connected power evidence. Verified both existing long
runners live; signed receiver still pending at the checker. Tested one declared
schematic change: high-reference XIP/XIN widths8u→16u, retaining lengths,
multiplicities, all other transistors, compensation and ideal bias/targets.
`pair_wide_input_dc.py` reuses the paired-device DC fixture; two41-point target
sweeps complete. `check_reference_wide_input.py` checks artifact hashes, finite
named data, matched grids and exact reconstruction from the retained baseline
plus only these two width changes.

Nominal high tail reported VDS−VDSAT improves−153.110→−115.333mV, still negative;
high target error13.148→10.151mV. Mirror current difference worsens21.855→25.702uA.
Pair supply power42.451→43.130mW; low reference is unchanged to numerical precision.
The candidate does not resolve the stated headroom question and is retained for
diagnosis only, not adopted or sent to the expensive connected transient. This
model margin is a diagnostic, not proof of the sole cause of dynamic error.
[Evidence](evidence/reference-wide-input.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_reference_wide_input.py`.
Runner: `bash projects/programmable_transceiver_platform/verification/run_reference_pair_wide_input_dc.sh`
requires a fresh output directory; existing artifacts must be preserved.
Next investigate available bias/common-mode range and topology rather than assuming
more width guarantees recovery. Autonomous clock and signed receiver jobs continue.
No new layout, no full-chip qualification and no change to the required scope.

## Pass 440 — reduced high-stage tail current also leaves deficient headroom

Pass439 completed a controlled input-width experiment. Verified selectivePLL
PID3719500 and signed receiver PID3728638 live; early receiver advanced beyond
506ns, past original478.5ns failure, but remains incomplete and opposite sign
has not completed. This is progress past an event, not completed qualification.

Tested only high-reference XT multiplicity16*S→8*S, retaining input/output
geometry, compensation, ideal bias and targets. Two41-point DC target sweeps
complete; `check_reference_half_tail.py` verifies hashes, finite exports, exact
single declared transistor change and matched baseline grids. Nominal high tail
reported margin−153.110→−115.711mV; entire high-target sweep still negative
(−125.304..−105.771mV). Target error13.148→12.463mV; mirror current difference
21.855→16.841uA; pair power42.451→41.705mW. Low reference unchanged.
[Evidence](evidence/reference-half-tail.json).

This diagnostic separates a current-saving change from the prior width change,
but neither restores tail compliance. No adoption and no expensive transient
promotion. Avoid an indefinite multiplicity sweep: the next headroom investigation
should quantify attainable input common-mode/bias range or a changed architecture.
Autonomous clocks and complete RF conversion remain higher-level obligations.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_reference_half_tail.py`.
Runner `bash projects/programmable_transceiver_platform/verification/run_reference_pair_half_tail_dc.sh`
uses a fresh output directory; preserve completed artifacts. Layout remains closed.

## Pass 441 — first signed receiver variant fails at next frame's same boundary

Pass440 completed a controlled tail-current diagnostic. Existing early receiver
now terminal: return1,528.5ns,264545rows, no repeated timestamps. Checker validates
artifacts and exact declared edge change; eight pre-event analog nodes match
baseline exactly over350–478.3ns. This is a failed partial run, not610ns completion.
Late variant is confirmed live as ngspice PID3755129; selectivePLL runner3719500
also remained live this pass. Neither restarted.

Added `audit_rx_adc_repeated_failure.py`, independently parsing terminal logs and
actual source schedules. Original478.5ns and early528.5ns both coincide with VC
fall completion and VUPDATE fall start, exactly50ns apart, same reported low
reservoir source branch. Moving only the first offending UPDATE fall−10ps allows
advancement to the corresponding second-frame boundary. Supports source-event
sensitivity without establishing a unique numerical cause or physical repair.
[Signed comparison](evidence/rx-adc-event-offsets.json),
[repeated-boundary audit](evidence/rx-adc-repeated-failure.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_rx_adc_event_offsets.py`
and `python3 projects/programmable_transceiver_platform/verification/audit_rx_adc_repeated_failure.py`.
Prior planned common-mode sweep deferred to interpret this newly terminal result.
Next await the existing opposite-sign control; if consistent, evaluate explicit
schedule-wide separation and audit its real timing changes. Crossing first-frame
end is not evidence of accurate codes. No ideal isolation adopted, no layout,
no change to full programmable RF/wired companion scope.

## Pass 442 — common-mode sweep separates tail compliance from accuracy

Pass441 established repeated-boundary failure evidence. Verified late receiver
PID3755129 and selectivePLL3719500 live. Returned to deferred common-mode question:
retained actual paired references, high target1.5–2.4V in10mV steps both directions,
low target1.15V, unchanged biases and devices. Both91-point DC sweeps complete.
Checker verifies artifact hashes, exact baseline deck apart from sweep/output,
finite named data and grids; physical3.3V−tail voltage matches reported PMOS VDS.

Tail margin zero is bracketed by target1.83–1.84V in both directions. At1.8V,
margin+25.857mV yet static error15.286mV; at2.15V margin−153.110mV and error13.148mV.
Positive tail margin is therefore insufficient for precision regulation even in
this unloaded nominal case. Maximum forward/reverse output difference2.262uV.
Do not change ADC span to hide the issue or attribute every dynamic error solely
to tail headroom. [Evidence](evidence/reference-common-mode.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_reference_common_mode.py`.
Runner `bash projects/programmable_transceiver_platform/verification/run_reference_pair_common_mode_dc.sh`
requires fresh output directory. Updated whole-chip risk review after five passes;
next reference architecture needs static accuracy and dynamic bidirectional drive,
while existing clock/receiver discriminators continue. No candidate adoption,
no uncertain-fab bound inferred, no layout or tapeout-readiness claim.

## Pass 443 — inspect first actual receiver-driven conversion interval

Pass442 quantified reference common-mode behavior. Verified late receiver3755129
and selectivePLL3719500 live. Added `analyze_rx_adc_first_frame.py` for the early
variant's terminal failed528.5ns waveform: source/artifact checks, finite named
vectors and strict time order; explicit source/driver470–470.1ns, held470.2–470.35ns
and capture509–509.5ns windows. No overall run-completion promotion.

I source differential3.081–3.088mV, held2.79956–2.79970mV. Q source−2.305..−2.234mV,
held−2.76887..−2.76877mV. This gives held-minus-source envelopes about−0.289..−0.281mV
and−0.535..−0.464mV respectively. They include aperture/turnoff and possible CDAC
initial-state effects; not a uniquely attributed driver error. Threshold1.65V
late words are I127/Q128, with DONE3.2988–3.2996V; bit extrema retained. These are
observed words, not correct-code assertions. [Evidence](evidence/rx-adc-first-frame.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_rx_adc_first_frame.py`.
The actual-connected fixture now provides a scoped first-frame observation, but
signal is below one nominal ADC step and first acquisition is470ns rather than
repeated10ns. Do not infer ENOB, steady-rate acquisition or RF demodulation. Next
use the existing opposite-edge control and eventual complete repeated schedule;
in-band signal/gain/linearity remain necessary. No new circuit adopted or layout.

## Pass 444 — second acquisition observed, but nearly repeated input limits stress

Pass443 produced scoped first-frame observations. Verified late receiver3755129
and selectivePLL3719500 live. Extended the existing first-frame analyzer with
`--second-acquisition`, using520–520.1ns source/driver and520.2–520.35ns held windows
from the same terminal failed waveform; no second code completion claimed.
Re-ran default mode to preserve first-frame output behavior and separate evidence.

At the second hold, I source3.10581–3.11507mV, held2.80660–2.80675mV gives about
−0.308..−0.299mV discrepancy. Q source−2.28718..−2.26585mV, held−2.77951..−2.77940mV
gives−0.514..−0.492mV. Similar to startup, but near20MHz tone sampled20MS/s repeats
almost the same source value: mean I changes only29.1uV and Q about−8.2uV from first
turnoff. This is weak evidence for transition-dependent settling, even though
second track obeys the intended10ns interval. Do not infer full acquisition range.
[Evidence](evidence/rx-adc-second-acquisition.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_rx_adc_first_frame.py --second-acquisition`.
Next actual dynamic-quality fixture must use an in-band signal and meaningful
code excursion/transition history; the current fixture remains a loading/startup
and numerical-event discriminator. No circuit change, no quality promotion, no
layout. Existing signed control and autonomous clock simulations continue.

## Pass 445 — start an in-band actual receiver gain measurement

Pass444 quantified second acquisition and its nearly repeated input limitation.
Added `baseband/inband_screen.py` and `run_bb_inband.sh`, preserving the completed
actual LNA/seeded ring/quadrature/LO drivers/mixers/two filters/bypass baseline.
Only RF frequency2.51542263GHz→2.5004222935GHz and horizon401→1201ns change. Frequency
uses prior measured LO2.4954222935GHz for approximately5MHz IF; actual new LO must
be remeasured. Amplitude remains1mV; no gain or load change and no ADC in this test.
Matched tone/zero histories run sequentially, tone first. Exact reverse-deck check,
source/model before/after hashes, unique output and per-case progress are retained.

Launched session31431, output `scratch/transceiver-bb-inband`; verified simulator
progress. Existing signed receiver and selectivePLL are not replaced or restarted.
Predeclared400–800ns and800–1200ns fitting windows contain about two IF cycles each;
compare windows and matched zero history before interpreting conversion gain.
This is a gain/loading diagnostic, not enough samples or implementation for ENOB,
noise, autonomous phase quality or modulation claims. New stimulus can later drive
ADC acquisitions through substantially different phases without increasing RF
amplitude and assuming linearity.

Runner from repository root:
`bash projects/programmable_transceiver_platform/verification/run_bb_inband.sh`.
Do not repeat while live or overwrite artifacts. Next add/reuse frequency-aware
conversion checks for these histories; no result or circuit adoption yet. Whole
programmable RF/wired scope and schematic-before-layout requirement remain intact.

## Pass 446 — in-band completion and conversion checker prepared

Pass445 launched actual in-band receiver histories. Verified runner3759831,
late receiver3755129 and selectivePLL3719500 live; no restart. Added
`check_bb_inband.py` using existing `rf_tone_fit.fit`: a known5MHz quadrature
component with DC/drift/RF contamination is recovered within1e-12 in a400ns
window. Pending-path check runs successfully; both actual histories still pending.

For terminal cases checker verifies artifacts, source-hash consistency, exact
frequency/horizon-only reversal, named exports, finite strictly increasing data
and1201ns horizon. Measures actual oscillator crossings separately per window;
fits400–800/800–1200ns with4/8LOharmonics, IF and RF terms; retains matched zero
amplitudes and residuals. Full matrix completion requires both cases. Completed
real-waveform branch remains to be exercised when data arrives. No completed
receiver or quality claim from a synthetic fitter check.
[Pending evidence](evidence/bb-inband.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_inband.py`.
The windows contain about two IF cycles; window/order/no-tone comparisons remain
necessary and residuals are not noise. No ADC/PLL integration in this fixture.
Next apply checker to actual terminal histories and use gain results to allocate
meaningful ADC stimulus/gain without assuming increased RF amplitude is linear.
Full chip remains unqualified and layout remains deferred.

## Pass 447 — reconcile full-chip inventory with actual evidence

Pass446 added a validated synthetic measurement path and pending run checker.
Verified all three runners live: selectivePLL3719500 near2.876us, late receiver
3755129 near210ns and in-band3759831 near285ns. No job restarted. Reconciled
schematic-implementation.json: actual receiver/ADC connection is present but
failed; isolated control and long-mirror candidate are terminal; reltol-only
probe comparison is complete; signed early case and scoped acquisitions/power
are recorded. Removed stale pending/absent claims without promoting qualification.

Linked current evidence and source paths. Inventory audit checks all10 source
obligations' listed files and keeps whole_chip_schematic_ready=false. This audit
checks presence and declared gaps, not functional compliance. Updated periodic
risk review to retain real bias/startup, wired CDR, modulated TX, host timing,
programmability and whole-chip connectivity alongside active RF/clock work.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_schematic_inventory.py`.
Next interpret existing discriminators and choose a circuit change that removes
an actual missing capability; avoid endless reference diagnostics. Layout remains
closed and full programmable RF/wired scope unchanged.

## Pass 448 — begin signed charge-to-edge sensitivity experiment

Pass447 reconciled inventory. Reviewed installed noise evidence: stationary filter
noise/model coefficients do not establish oscillator phase noise. Prepared a
bounded deterministic experiment on the existing41ns loaded split-ring fixture:
matched zero/+100uA/-100uA differential current pulses,20–20.012ns with1ps ramps
and10ps plateau (signed charge1.1fC). This tests response to a specific output
perturbation, not intrinsic device noise or an oscillator-wide noise model.

Initial fixture incorrectly used floating top-level P/N. Connectivity inspection
caught it before interpretation; deliberately killed identified container53095885b705.
Preserved `scratch/transceiver-vco-charge-kick/invalid-fixture.json` and logs;
no results valid. Corrected to actual XRX.CP/XRX.CN, added baseline divider-port
assertion, and launched separate `scratch/transceiver-vco-charge-kick-v2`,
session77489. Runner `analog/pll/vco_charge_kick.py` captures recursive model/source
hashes and exact pulse/output-path reversal. Other live jobs untouched.

Reproduce runner from repository root (only with fresh output directory):
`bash projects/programmable_transceiver_platform/verification/run_vco_charge_kick.sh`.
Next compare ordinal CML crossings before/after pulses, signs and timestep
sensitivity before any phase-response claim. The pulse acts at the isolating output
buffer, not every internal noise source; it cannot establish full phase noise.
No autonomous timing qualification, candidate adoption or layout readiness.

## Pass 449 — corrected signed charge experiment completes

Pass448 launched corrected hierarchical output injection and retained invalid
artifacts. Added `check_vco_charge_kick.py`: exact original-deck reversal, actual
CML observation expression, hashes, logs, finite strict-time exports,41ns completion,
source before/after equality and ordinal crossing comparison. Initial checker
string expected spaces absent in actual let expression; corrected exact string
then exercised all three real terminal histories successfully.

Quiet and +/-1.1fC cases complete with equal rising-edge counts;10–19ns pre-pulse
edge differences exactly zero. Over20–21ns maximum absolute shifts0.83635ps positive
and0.84477ps negative with opposite signed mean shifts. Over30–40ns mean differences
are−0.00995ps/+0.01006ps. Do not interpret these very small tails physically without
numerical refinement. [Evidence](evidence/vco-charge-kick.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_vco_charge_kick.py`.
This is a single-phase output-buffer perturbation in a historical loaded open-loop
fixture. Next matched finer timestep is necessary before trusting magnitude;
amplitude and phase dependence and internal noise-source attribution remain
missing. No intrinsic jitter, phase-noise or autonomous PLL claim, no hardware
candidate adoption, no layout. Other long-running experiments remain independent.

## Pass 450 — matched0.5ps charge-response refinement launched

Pass449 completed signed deterministic output perturbations. Verified existing
PLL3719500, late receiver3755129 and in-band3759831 live. Added `--fine` to the
same charge-kick runner: only transient print/maxstep2ps→0.5ps, retaining41ns,
three zero/signed1.1fC pulses, circuit, sources and initial state. Separate
`transceiver-vco-charge-kick-fine`, session9361 launched; original artifacts stay.
Runner before/after hashes include its own changed version. Removed inherited
irrelevant requirement that fine mode needed supply ripple.

Shared checker `--fine` handles pending progress, verifies declared timestep and
exact reversal. Pending fine evidence emitted; original three terminal cases
rechecked successfully with identical reported measurements. Fine terminal path
will be exercised on actual results. No numerical-convergence or noise conclusion
from a started refinement.

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_vco_charge_kick.py --fine`
and runner `bash projects/programmable_transceiver_platform/verification/run_vco_charge_kick_fine.sh`.
Do not relaunch existing job. Next compare each signed-minus-quiet ordinal response
at both resolutions, not raw independently shifted oscillator phases. This is a
limited output-perturbation diagnostic, not phase-noise qualification. Layout closed.

## Pass 451 — in-band tone completes; useful ADC excursion still needs gain

Pass450 launched numerical refinement. Confirmed fine pulse runner3770494 live
at16.9ns; no restart. Added `compare_vco_charge_timestep.py` with exact
step-only deck reversal, artifact checks and ordinal signed-minus-quiet response
comparison; pending branch exercised, real fine results still required.

Meanwhile in-band tone completed1201ns and `check_bb_inband.py` exercised its real
terminal path successfully. Actual late LO2.495422295GHz implies IF4.9999985MHz.
800–1200ns/order8 filter peaks I7.182310mV,Q8.066296mV; Q/I1.123078,phase−93.01561deg.
400–800ns gives I7.182201mV,Q8.066314mV; order4/8 agreement close. These correspond
to roughly0.92/1.03 nominal7.8125mV differential ADC steps, so the stimulus is now
in-band but still insufficient for useful multi-code conversion. Additional gain
must be implemented and verified; do not simply increase RF amplitude assuming
linearity. No-tone history remains pending and full matrix completed=false.
[Evidence](evidence/bb-inband.json), [pending timestep comparison](evidence/vco-charge-timestep.json).

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_inband.py`
and `python3 projects/programmable_transceiver_platform/verification/compare_vco_charge_timestep.py`.
Next finish matched zero control and fine response runs, then allocate a practical
programmable gain stage with noise/headroom/settling evidence. Deterministic fit
residuals are not intrinsic noise. No ADC/PLL or whole-chip qualification; no layout.

## Pass 452 — allocate gain with noise, not code excursion alone

Pass451 produced real in-band tone evidence. Verified PLL3719500, late receiver
3755129, in-band3759831 and fine-pulse3770494 live. Fine quiet completes; signed
fine cases pending. Added `audit_inband_gain_budget.py` tying measured late tone
amplitude to explicitly conditional gain/noise arithmetic. At illustrative0.25V
peak ADC input, gain requirements34.81I/30.99Q. Targets0.125/0.25/0.5V are scenarios,
not adopted requirements or verified headroom.

Separate stationary filter-noise scenarios269/344uVrms would imply only23.4–26.5dB
signal/noise ratios for these measured tone amplitudes if applicable. They are
NOT measured receiver SNR: source/common-mode, noise band and switched loading
differ. Ideal quantization RMS2.255mV becomes comparable to those assumed noises
at post-filter gain6.56–8.38; more post-filter gain cannot recover upstream analog
SNR. The architectural next question is gain placement and input-referred noise,
not simply maximizing ADC code span. [Conditional budget](evidence/inband-gain-budget.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_inband_gain_budget.py`.
Complete matched zero control and quantify applicable filter/mixer noise before
choosing an amplifier chain; added noise, bandwidth, compression and power remain
mandatory. No ENOB, sensitivity, model-noise bounds or layout readiness claimed.

## Pass 453 — receiver-interface common mode does not remove filter-noise concern

Pass452 established a conditional gain/noise budget. Verified four existing
runners live; opposite-edge receiver and signed fine-pulse cases still pending.
Tested stationary filter at1.177V input common mode (existing interface-gain
scenario) versus original0.9V, retaining circuitry,1kohm noisy source legs and
both fnoicor settings. Resistor calibration and both201-point noise sweeps
complete. Checker verifies artifacts, exact common-mode-only deck reversal,
headers/finite frequency grids, integrated PSD and thermal calibration.

Integrated1kHz–20MHz output noise becomes271.30/347.83uVrms, versus269.26/343.62uV
at0.9V. Input-referred values41.223/52.537uVrms. Thus this particular common-mode
change does not remove the concern identified in the conditional gain budget.
Still a DC-linearized source-driven filter, not actual time-varying mixer noise,
sampled aliasing, whole receiver SNR or silicon bounds.
[Evidence](evidence/bb-noise-receiver-cm.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_noise_receiver_cm.py`.
Runner `bash projects/programmable_transceiver_platform/verification/run_bb_noise_receiver_cm.sh`
requires fresh output directory. Next gain placement must include measured source
loading and noise; more post-filter amplitude alone is insufficient. No circuit
adoption, receiver qualification or layout progression.

## Pass 454 — positive charge response changes about1.1% with finer timestep

Pass453 completed a controlled stationary-noise scenario. Verified PLL3719500,
late receiver3755129, in-band3759831 and fine pulse3770494 live. Fine quiet and
positive cases now terminal and validated; negative remains pending. Extended
`compare_vco_charge_timestep.py` to report explicitly partial matched cases while
keeping completed=false until both full matrices finish. Exact step-only deck
reversal and artifact/ordinal-count checks are exercised on real completed data.

Positive pulse peak displacement20–21ns changes0.836347→0.845716ps, maximum
matched response difference0.009370ps (~1.12% of coarse peak). Pre-pulse difference
zero. Maximum response difference21–25ns0.000169ps and30–40ns0.000141ps. These
are sensitivity observations at two resolutions, not a universal convergence
bound or noise floor. Both comparisons subtract each resolution's own quiet
trajectory; no nearest-edge rematching. [Partial comparison](evidence/vco-charge-timestep.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_vco_charge_kick.py --fine`
then `python3 projects/programmable_transceiver_platform/verification/compare_vco_charge_timestep.py`.
Next incorporate negative result when terminal; no new pulse sweep before that.
Single-phase external-output perturbation remains insufficient for internal-source
phase noise or autonomous timing quality. Receiver signed control and no-tone
history also continue. Full chip and layout gate unchanged.

## Pass 455 — complete both signs of charge-response timestep comparison

Pass454 validated positive partial comparison. Verified fine runner3770494 live,
observed its existing session and waited20s; no restart. Fine negative then
completed41ns. Shared checker and comparison now exercise full terminal matrices:
all artifacts, exact step-only reversal, source before/after equality, finite
waveforms, equal ordinal counts and matched quiet controls verified.

Positive peak0.836347→0.845716ps; negative0.844772→0.853208ps (2ps→0.5ps).
Maximum response differences0.009370ps/0.008436ps around pulse, approximately
1.12%/1.00% of coarse peaks. Both pre-pulse differences exactly zero; later-window
response differences below0.000247ps. This closes the declared numerical
comparison, not a universal convergence bound or silicon phase-noise estimate.
[Completed comparison](evidence/vco-charge-timestep.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_vco_charge_kick.py --fine`
then `python3 projects/programmable_transceiver_platform/verification/compare_vco_charge_timestep.py`.
No further timestep sweep justified solely by these observations. Clock-noise work
still needs internal-source/phase dependence and a justified noise model; this
external isolating-buffer pulse is not that model. Longer autonomousPLL, late
receiver and in-band zero histories continue. Full scope and layout gate unchanged.

## Pass 456 — both signed cases justify schedule-wide separation test

Pass455 completed pulse numerical comparison. Late receiver is now terminal:
both+/-10ps single-edge variants fail528.5ns, same low-reservoir branch, and
both eight-node pre-event comparisons are exactly zero. This strengthens event
sensitivity evidence without proving physical repair or conversion accuracy.

Prepared actual-loaded610ns fixture moving all24 UPDATE falling ramps coincident
with comparator-clock fall completion by+10ps. Rise times and100ps fall widths
unchanged; high plateaus lengthen10ps and following low plateaus shorten10ps.
Sample/acquisition/reset/clock circuits unchanged. Verified signed parent artifacts,
24 changes, unchanged voltages, strict knot ordering and exact deck reversal.
Preparation initially selected a different hierarchical VC source; fixed to
explicit VC CLK PWL line. Premature first launch failed before simulation because
manifest absent; preserved empty original output/preparation directories. Correct
prepared-v2 and output-v2 avoid overwriting them.

Launched session31251 via existing `rx_adc_event_offsets.py` runner with new
variant manifest; output `scratch/transceiver-rx-adc-separated-events-v2/separated`.
[Preparation](evidence/rx-adc-separated-event-preparation.json).
Runner from repository root:
`bash projects/programmable_transceiver_platform/verification/run_rx_adc_separated_events.sh`.
Do not relaunch while live. Next add corresponding full-horizon/analog/timing
checks and evaluate actual completion. This is an explicit timing change, not a
zero-impact numerical fix or physical clock implementation. PLL and in-band zero
histories continue; no layout readiness or scope reduction.

## Pass 457 — checker accounts for earlier first schedule-wide perturbation

Pass456 established both signed failures and launched separated schedule. Current
separated log advances beyond15ns; PLL3719500 and in-band3759831 confirmed live.
In-band zero still pending; completed tone remains validated. Extended shared
`check_rx_adc_event_offsets.py --separated` for prepared-v2/output-v2 and distinct
report. Its comparison now ends473.3ns, before the earliest changed473.5ns edge,
rather than reusing478.3ns from the single-edge experiment. Otherwise that check
would inadvertently compare behavior after a deliberate timing change.

Pending case path works. Exercised failed-baseline self-comparison at the new
window with zero differences; original self-audit rerun and separately preserved.
This checks failure parsing/comparison, not the still-pending successful610ns
path. [Pending run](evidence/rx-adc-separated-events.json),
[self-audit](evidence/rx-adc-separated-checker-self-audit.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_rx_adc_event_offsets.py --separated`.
Terminal checks retain source/artifact/horizon/header/finite/time-order and exact
whole-line reversal. Future analog quality still requires separate frame/reference
measurements, not this completion gate. No timing-generator implementation,
noise-quality claim, circuit adoption or layout readiness.

## Pass 458 — matched in-band no-tone control completes

Pass457 prepared separated-event checker. Both in-band receiver histories now
terminal1201ns/600516rows and pass full checker. AutonomousPLL3719500 remains
live; no restart. Added `compare_bb_inband_control.py` with completed-matrix and
artifact checks, matching window/order keys and no artificial waveform alignment.

Late order8 zero fitted peaks I0.06302uV,Q0.04066uV versus tone7.182310/8.066296mV;
ratios8.775e-6/5.041e-6. Driven amplitude change between400–800/800–1200ns windows
is+15.05ppm I/−2.22ppm Q. This supports a stimulus-dependent IF component and
selected-window/order robustness. Deterministic no-tone fits are NOT a noise floor,
noise figure or sensitivity result. Scalar magnitudes compared without subtracting
phasors at slightly different per-case measured LO frequencies.
[Completed control](evidence/bb-inband-control.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_inband.py`
then `python3 projects/programmable_transceiver_platform/verification/compare_bb_inband_control.py`.
Current5MHz gain evidence can inform next gain-stage allocation; amplitude still
about one nominal ADC step and real programmable gain/noise budget remains missing.
No compression/broadband/modulation/ADC/PLL qualification, no layout progression.

## Pass 459 — targeted first-stage gate-area noise candidate

Pass458 completed matched in-band control. Reviewed prior verified noise
attribution and resistor-change experiment before choosing a change: resistor
noise dominates fnoicor0, first-stage MOS largest group for fnoicor1. Tested only
first-stage PMOS input pair W/L4u/.28u→8u/.56u, same nominal ratio/multiplicity,
second stage and tail unchanged, atCM1.177V. Inline distinct first-stage cell;
retained production filter files unchanged. Three stationary noise cases complete.

Checker validates exact expanded baseline plus the declared pair geometry,
artifacts, named finite grids, PSD integration and resistor calibration. Input-
referred1kHz–20MHz noise41.223→40.467uV f0 and52.537→43.562uV f1. Output noise
271.300→279.641uV f0 (worse) and347.828→300.926uV f1. Changed transfer response
must therefore be checked rather than selecting by output noise alone. No
candidate adoption, connected receiver noise or guaranteed model bounds.
[Evidence](evidence/bb-noise-large-input.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_noise_large_input.py`.
Runner `bash projects/programmable_transceiver_platform/verification/run_bb_noise_large_input.sh`
requires fresh output directory. Next DC/AC headroom, source loading and bandwidth
comparison before any expensive connected transient; larger area changes more
than flicker coefficient. Programmable gain/real bias and full-chip scope remain
unimplemented or unqualified; layout stays closed.

## Pass 460 — larger input pair retains nominal headroom but changes peaking

Pass459 completed targeted noise candidate. Added `large_input_gain.py` using
retained actual filter DC/AC fixture atCM1.177V,1kohm source legs,RFB20k/C20p.
Only first-stage pair geometry matches noise candidate. One OP/100-point1–100MHz
run completes. Checker verifies source/artifacts/logs, finite named OP, equal
frequency grids and exact expanded original deck with only declared geometry.

Source gain5MHz6.87340→7.15461;10MHz peak7.14538→7.64167;20MHz4.58031→4.61628.
Differential input transfer5MHz0.67396→0.66770, so loading changes too. Minimum
reported VDS−VDSAT remains positive but falls0.54918→0.40629V (first-stage tail).
Output CM1.08723→1.09062V; nominal supply14.2086→14.0573mW. No negative headroom
screen result, but increased peaking requires dynamic/stability review. Not an
implemented programmable gain stage and insufficient gain increase by itself.
[Evidence](evidence/bb-large-input-gain.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_large_input_gain.py`.
Runner `bash projects/programmable_transceiver_platform/verification/run_bb_large_input_gain.sh`
requires fresh directory. Next bounded step/swing/source-load check before connected
RF adoption; AC gain alone is not stability margin. Full-chip and layout gates closed.

## Pass 461 — larger input pair preserves selected static swing approximately

Pass460 established candidate DC/AC tradeoff. Reused existing feedback_swing_screen
with actual completed candidate AC circuit, preserving1kohm source legs and
CM1.177V. Added shared checker --large-input and separate wrapper/output.161-point
−0.4..+0.4V differential sweep completes; hashes, parent provenance, named finite
data and exact sweep grid verified. Candidate remains monotonic in this sweep.

Central finite-difference gain6.67282→6.86731. At+50mV input normalized secant gain
0.998841→0.998834; at+100mV0.995175→0.995128; at+200mV0.977322→0.976483. Candidate
output+0.343/+0.683/+1.341V respectively. Minimum reported device margin at+200mV
falls0.15332→0.10508V. Both polarities retained in evidence; this is not transient
THD, settling, hysteresis or process qualification.
[Evidence](evidence/bb-large-input-swing.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_feedback_swing.py --large-input`.
Runner `bash projects/programmable_transceiver_platform/verification/run_bb_large_input_swing.sh`
uses fresh output. No static compression breakthrough or drastic regression at
these points; dynamic source loading/peaking and real gain programmability remain
unresolved. Candidate unadopted; no connected-receiver gain multiplication or layout.

## Pass 462 — larger input pair worsens selected dynamic recovery

Pass461 completed static swing screen. Verified autonomousPLL3719500 live and
separated receiver advancing~190ns. Added matched `large_input_step.py` derived
from verified baseline/candidate DC decks:0→+50mV→−50mV→0 at20/120/220ns,
100ps ramps,1kohm source legs,300ns horizon20ps maxstep, OP initialization.
Both complete. Checker validates exact stimulus/analysis-only reversal, artifacts,
finite strict time and horizon; measures against each circuit's own DC target.

Positive step overshoot baseline0.37115V versus0.33325V target; candidate0.39079V
versus0.34297V target. On+50→−50mV transition,40–50ns error73.42→94.72mV and
60–70ns error7.43→13.68mV. Return-to-zero60–70ns error3.94→6.25mV. Larger pair
worsens each selected recovery window and peaking, consistent with AC evidence.
Do not adopt as a direct replacement solely for stationary-noise improvement.
[Evidence](evidence/bb-large-input-step.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_large_input_step.py`.
This filter is a continuous-time stage; its step recovery is not automatically the
ADC10ns acquisition requirement. No allocated stability/settling pass threshold;
actual mixer/ADC loading, gain programmability and complete noise remain missing.
Next circuit choice must jointly trade noise, gain and damping, or change gain
placement. Avoid further geometry sweeps without that hypothesis. Layout closed.

## Pass 463 — implement actual transistor-selected filter feedback

Pass462 demonstrated a noise/dynamics tradeoff without closing programmability.
Added bb_filter_programmable.spice: each feedback leg has two20k sections with
actual complementary GF1801u/2u,L.28 transmission gate shunting one section.
SEL/SELB are explicit external ports, not claimed register/control implementation.
Retained amplifier geometry; two nominal20k/40k modes atCM1.177V,1kohm source legs.
Both DC/AC fixtures complete; checker verifies exact fixture changes, artifacts,
finite named grids and comparison to retained ideal-resistor settings.

At5MHz nominal20k switched gain7.35208 versus ideal6.87340 (+6.964%); nominal40k
10.33275 versus10.33111 (+0.0159%). The on switch is not an ideal short: actual
terminal voltage/current and effective resistance need measurement before sizing
or gain calibration. Off-state small-signal agreement alone does not establish
leakage over voltage/temperature or switching transients.
[Evidence](evidence/bb-programmed-feedback.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_programmed_feedback.py`.
Runner `bash projects/programmable_transceiver_platform/verification/run_bb_programmed_feedback.sh`
requires fresh output. This is a concrete two-setting transistor building block,
not enough extra gain or completed controls. Mode transitions/skew/noise, physical
passives and connected loading remain required. No receiver adoption or layout.

## Pass 464 — actual feedback switch has substantial on-state voltage drop

Pass463 implemented two-setting feedback. Added observation-only mid-resistor
node probes to both OP/AC cases; original OP values and entire AC export reproduce
bit-for-bit. Checker validates exact added-probe-only deck, hashes and named finite
OP data. Derives switch current by KCL from the two known20k resistor drops.

Enabled leg drop−7.798mV, switch current−3.351uA, DC secant resistance2.327kohm.
Parallel20k section therefore leaves effective full feedback22.0847kohm, consistent
with nominal20k mode's gain discrepancy. Disabled full path39999.99ohm; inferred
~1pA residual is not a validated leakage floor. Both symmetric legs recorded.
[Evidence](evidence/bb-feedback-switch-drop.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bb_feedback_switch_drop.py`.
This V/I is one bias-point secant, not incremental or RF resistance. Next controlled
switch-size change must measure off-state loading and transition injection as well
as on-drop; do not silently treat programmable modes as exact resistor values.
No register/level-shifter, physical passives, full gain range or receiver adoption.
Layout gate remains closed.

## Pass 465 — reassess priorities after programmable feedback implementation

Pass464 measured finite switch resistance. Verified existing PLL3719500 near4.792us
and separated receiver3779187 near316ns live; no restart. Reconciled inventory:
in-band controls complete, signed single-edge cases terminal, schedule-wide test
live, two-setting FET feedback present with explicit missing controls/transitions.
Linked actual evidence and checked all listed source/evidence paths; source
inventory audit still whole_chip_schematic_ready=false across10 obligations.

Updated risk priorities around autonomous phase quality, reference/ADC accuracy
and full RF gain/noise/linearity rather than more convenient switch-size tuning.
Larger input-pair noise benefit has dynamic cost; output-pulse phase response is
not intrinsic noise. Real bias/startup, wired recovery, modulated TX, host timing,
pads/shared supplies and complete connected schematic remain missing/unqualified.
No selected model/noise/package scenario is treated as a guaranteed fab bound.

Reproduce inventory from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_schematic_inventory.py`.
Next inspect existing high-risk runs when terminal and choose the next circuit
change from those results; no scope reduction, quality promotion or layout.

## Pass 466 — hybrid high-reference stage clears nominal headroom screen

Pass465 refocused whole-chip risks. Tested a distinct architecture rather than
repeating rejected whole-polarity swap: high stage's five input/tail/mirror devices
use existing NMOS-input topology, while PMOS output/NMOS load remain retained.
Low reference, biases, dimensions, compensation and targets unchanged. Exact
expanded-deck comparison verifies declared five-device substitution. Both41-point
DC target sweeps complete with hashes, finite matched grids and no log errors.

High error+13.148→−5.228mV; nominal tail margin−153.110→+865.253mV. All seven high
stage reported margins remain positive across±20mV sweep, minimum0.4463V. Pair
power42.451→44.558mW (+2.107mW); low rail unchanged. This addresses input compliance
while preserving stronger source output topology; static millivolt error remains.
[Evidence](evidence/reference-hybrid.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_reference_hybrid.py`.
Runner `bash projects/programmable_transceiver_platform/verification/run_reference_pair_hybrid_dc.sh`
requires fresh directory. Candidate diagnostic only; next actual reservoir AC/
bipolar step tests before costly CDAC integration. DC improvement does not prove
stability, dynamic regulation, mismatch or real bias startup. Layout remains closed.

## Pass 467 — hybrid reference improves selected reservoir-loaded impedance

Pass466 established positive nominal hybrid headroom and reduced DC error. Reused
paired-reference impedance method with exact baseline/hybrid DC bodies and actual
2048unit PDK MIM reservoir per rail. Four481-point1kHz–1GHz AC injections complete;
source/artifact/header/grid checks pass. Baseline numbers reproduce retained test.

High20MHz impedance35.450→15.232ohm, peak39.728ohm@29.854MHz→25.665ohm@50.119MHz;
low-frequency1.243→1.015ohm. At100MHz18.244→18.815ohm and200MHz9.242→9.894ohm,
so not uniformly better. Low-rail peak changes13.607→13.482ohm despite unchanged
low-stage devices; shared bias loading remains part of the paired circuit and
both rails must continue to be measured. No unique coupling-mechanism proof.
[Evidence](evidence/reference-hybrid-impedance.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_reference_hybrid_impedance.py`.
Runner `bash projects/programmable_transceiver_platform/verification/run_reference_hybrid_impedance.sh`
requires fresh directory. Next bipolar current pulses then actual CDAC decision
windows if useful; small-signal zero-DC-load output impedance is not loop phase
margin or nonlinear regulation. Candidate unadopted, layout gate closed.

## Pass 468 — hybrid reference improves small signed load-pulse recovery

Pass467 measured reservoir-loaded impedance. Ran same retained +/-1mA20–30ns
pulse100ps edges,100ns5ps transient on hybrid pair with2048unit reservoir per rail.
Session24672 completes all four cases. Checker verifies parent hashes, exact
pulse-only circuit-body reversal, artifacts/logs, finite named strict-time data
and full horizon. Existing PLL3719500 and separated receiver3779187 confirmed live.

High excursions now17.377/17.986mV versus retained26.301/27.779mV.40–50ns recovery
error4.966/5.720mV versus16.807/17.476mV;60–70ns33.05/38.50uV versus roughly
544/528uV. Low excursions10.459/10.968mV remain close to retained10.52/11.04mV;
low40–50ns1.465/1.632mV is slightly different, not universal improvement.
[Evidence](evidence/reference-hybrid-step.json).

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_reference_hybrid_step.py`.
These excursions exclude static target error and use a much smaller isolated
pulse than CDAC demand. Candidate remains unadopted, but now merits actual CDAC
frames with unchanged schedule/reservoirs and all decision-window/power checks.
No phase-margin, PVT/noise, real bias or layout qualification.

## Pass 469 — hybrid reference enters actual dualADC switching fixture

Pass468 completed improved signed pulse recovery. Prepared and launched actual
209.9ns dualADC reference-current/branch-probe fixture from retained reltol-only
baseline. Only five high-reference input/tail/mirror devices change to NMOS-input
topology; output stage, low reference, reservoirs, clocks, inputs, branch probes
and numerical tolerances retained. Source before/after and exact expanded-deck
reversal required by runner. Session4404 live beyond30ns; no result yet.

Extended shared `check_bit6_complement.py --reference-hybrid` to independently
reconstruct declared topology and preserve pending status. Existing mirror branch
rechecked successfully, retaining its rejection evidence. When terminal, checker
will compare48 rail/decision windows, held samples, captured codes, demand charge/
peaks and driver/reference energy; no promotion solely on isolated pulse results.
[Pending evidence](evidence/reference-hybrid-frames.json).

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bit6_complement.py --reference-hybrid`
and runner `bash projects/programmable_transceiver_platform/verification/run_reference_hybrid_frames.sh`.
Output `scratch/transceiver-reference-hybrid-frames`; do not relaunch live job.
Real RF sources, complete bias generation, noise/PVT and layout remain outside
this fixture. Full product scope unchanged; candidate still unadopted.

## Pass 470 — score simultaneous reference span as well as individual rails

Pass469 launched actual hybrid ADC fixture. Verified PLL3719500 and separated
receiver3779187 live; hybrid advances beyond63ns. Reviewed shared checker before
terminal candidate interpretation. Added explicit interpolation-window coverage
assertions and24 simultaneous VH−VL decision windows with min/max/motion and
nominal1V-span error; retain original48 individual rail windows and all other metrics.

Exercised completed baseline/rejected-long-mirror path: retained reltol/probed
baseline span0.922457..1.058053V, worst window motion10.0555mV. Rejected mirrors
0.907150..1.051762V, worst motion11.0964mV. These describe this exact fixture,
not historical variants; rejection unchanged. Hybrid check remains pending.
This prevents a single-rail improvement from hiding adverse simultaneous span.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bit6_complement.py --reference-hybrid`.
Completed-path exercise uses --reference-mirror and its existing terminal artifacts.
Next interpret actual hybrid result with simultaneous span, rail motion, acquisition,
codes and energy together. No converter accuracy threshold invented, no schematic
promotion or layout; whole-chip timing/bias/noise obligations remain open.

## Pass 471 — move charge perturbation inside the oscillator loop

Pass470 strengthened simultaneous reference-span checks. Verified PLL3719500
and separated receiver3779187 live; hybrid ADC advances beyond122ns. Began a
bounded internal-phase-response test now that both output-pulse timestep cases
complete. Same41ns loaded-ring fixture,0.5ps maxstep and zero/+/-1.1fC20ns pulse;
only injection nodes move from isolating output to actualXRX.XVCO.N0P/N0N.
Observe ordinal output crossings with matched quiet history, not nearest pairing.

Initial connectivity guard wrongly required a second .ic prefix on a shared line;
failed before simulation, original empty directory preserved. Corrected assertion
for both existing node assignments; separatev2 output launched session18302.
`check_vco_internal_kick.py --fine` verifies manifest nodes and exact pulse removal;
pending branch exercised. This is one internal injection phase/amplitude, not
an intrinsic noise source or a full periodic phase-sensitivity function.

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_vco_internal_kick.py --fine`
and `bash projects/programmable_transceiver_platform/verification/run_vco_internal_kick.sh`.
Output `scratch/transceiver-vco-internal-kick-v2`; do not relaunch while live.
Next compare persistence and sign of internal response, then decide whether
additional phase/source coverage is informative. No phase-noise or layout claim.

## Pass 472 — hybrid actual-CDAC result fails direct-replacement test

Pass471 launched internal clock perturbations. Added gated three-frame receiver
analyzer for eventual separated-event completion; pending path exercised. During
this work hybrid209.9ns ADC run completed and shared checker passed full artifact,
source/deck-reversal, finite/grid/horizon and digital-level checks.

Worst high-reference target error61.349→70.646mV and high motion3.179→4.946mV.
Low worst error20.173→20.314mV; low motion6.877→6.614mV. Span0.922457..1.058053V
becomes0.913715..1.033795V; worst span motion10.055→11.560mV. Reference mean power
44.423→46.477mW. Codes177/78/178→179/76/179 on both channels; maximum held change
1.042mV. Many individual target-error windows improve, but worst error/motion and
power worsen, so no direct replacement or regulation pass. Static offset also
changes sign; this result does not prove every dynamic property became worse.
[Completed hybrid evidence](evidence/reference-hybrid-frames.json).

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bit6_complement.py --reference-hybrid`.
New receiver analysis:
`python3 projects/programmable_transceiver_platform/verification/analyze_rx_adc_separated_frames.py`
requires completed upstream checker; it remains pending, with no unexecuted-path
qualification. Next separate static offset from load-induced deviation to explain
the hybrid tradeoff, preserving target-error failures. No layout or baseline change.

## Pass 473 — complete connected receiver diagnostic and internal phase response

Previous workflow pass changed authoritative documentation. This pass checks actual
terminal artifacts: schedule-wide separated receiver completes610ns/305712 rows,
no errors/repeated times, and eight pre-event analog vectors match baseline exactly.
Three frames hold470/520/570ns; I/Q captured codes127/128 have valid rail levels.
Held-minus-source envelopes span approximately-0.331..-0.281mV(I) and
-0.535..-0.464mV(Q). Across decision windows reference span0.949150..1.049324V.
These are diagnostic measurements, not correct-code or ENOB qualification: input
is sub-LSB and nearly repeats, and10ps edge separation is an ideal stimulus change.

Internal-ring pulse matrix also completes with matched edge counts and zero
pre-pulse differences. For+/-1.1fC at20ns, late30–40ns mean ordinal displacement
is+1.032512/-1.048230ps. Unlike the output-node pulse, internal perturbation leaves
persistent phase displacement. Scope remains one phase, node pair and charge in
a historical seeded fixed-control oscillator; no intrinsic noise/PLL jitter claim.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_rx_adc_event_offsets.py --separated`
`python3 projects/programmable_transceiver_platform/verification/analyze_rx_adc_separated_frames.py`
`python3 projects/programmable_transceiver_platform/verification/check_vco_internal_kick.py --fine`
`python3 projects/programmable_transceiver_platform/verification/audit_schematic_inventory.py`

Updated inventory and whole-chip risk priorities; all10 obligations remain
incomplete and layout gate closed. Autonomous PLL3719500 verified live near6.064us;
do not restart. Next discriminators: non-repeating in-band connected acquisition,
static-versus-dynamic hybrid reference error, and completed autonomous phase tail.
Preserve regulation, noise, real bias/startup, modulated TX, wired recovery, host
and coexistence obligations; no baseline circuit promoted this pass.

## Pass 474 — separate hybrid DC offset from loaded reference excursion

Previous pass completed receiver and clock evidence and updated whole-chip risks.
New analyzer validates completed parent evidence/artifact hashes, finite strict
native timelines and every original target-error window before subtracting the
independently measured nominal unloaded DC point. No circuit or threshold changed.

High-rail worst deviation about own DC point improves74.496->65.418mV (~12.2%),
while required-target error worsens61.349->70.646mV. Static offset changes
+13.148->-5.228mV. Low-rail deviation26.673->26.117mV changes only slightly;
target error20.173->20.314mV. Thus the hybrid is not uniformly dynamically worse,
but still cannot replace the baseline: absolute error, window motion and power
remain adverse. Subtracted DC comes from an independent unloaded fixture, not a
causally isolated switching response or an implemented calibration mechanism.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_reference_hybrid_offsets.py`.
Evidence: `evidence/reference-hybrid-offsets.json`. No additional SPICE run required.
Autonomous PLL runner3719500 and ngspice3719539 verified live near6.182us; retain.
Next reference experiment must address the still-large load-induced excursion
and static accuracy together; simply trimming away the offset is insufficient.
The completed receiver fixture also enables a non-repeating in-band input test.

## Pass 475 — actual receiver/ADC with changing in-band input launched

Previous pass established hybrid static/dynamic tradeoff. Reused completed610ns
separated-event receiver; change only RF frequency2.51542263->2.5004222935GHz,
keeping1mV input, seeded oscillator, all actual circuits and clocks. Expected IF
approximately5MHz gives90degree advance per50ns sample instead of near-repetition.
This tests changing acquisition history, not full-scale conversion or ENOB.
Preparation checks parent artifact hashes and exact reversal of the sole change.

Launched session87281, runner3817752, output`scratch/transceiver-rx-adc-inband/inband`;
verified live beyond15.8ns. Expected runtime order20minutes from prior connected
fixture; timeout10800s, horizon610ns. Do not restart while running. New checker
preserves terminal errors and checks exact frequency-only deck change, source
hashes, named vectors, finite strictly increasing data and requested horizon.
Pending branch exercised; completed branch not yet qualified. Shared three-frame
analyzer now accepts`--inband`; pending evidence remains explicitly incomplete.

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_rx_adc_inband.py`
`python3 projects/programmable_transceiver_platform/verification/analyze_rx_adc_separated_frames.py --inband`
Launch wrapper is`verification/run_rx_adc_inband.sh` under this project; preparation
is`verification/prepare_rx_adc_inband.py`. Both preserve earlier output directories.
Next inspect actual changing source/held envelopes, rail span and captured codes;
no correct-code inference without analog accuracy and transfer evidence. Autonomous
PLL3719500/3719539 also verified live this pass. No layout or baseline promotion.

## Pass 476 — move oscillator sensitivity to actual transistor terminals

Previous pass launched non-repeating in-band receiver acquisition. Verified both
that runner3817752 and autonomous PLL3719500/3719539 live; PLL near6.419us,
receiver near34ns. Preserve jobs. No new terminal waveform result this pass.

Inspected retained ring: stageX0 input transistorXMP connects OUTP/INP/TAIL/VSS;
its drain/source are XRX.XVCO.N0P and XRX.XVCO.X0.TAIL. Previous differential
N0P/N0N pulse is not an individual channel-terminal perturbation. New controlled
matrix moves only injection endpoints to these physical nodes, preserving zero/
+/-100uA pulse,1.1fC magnitude,20ns phase,41ns horizon and0.5ps maximum step.
Runner asserts the actual stage mapping and XMP terminal ordering and hashes
recursive model dependencies. Existing ring and output-pulse artifacts remain.

Launched session54878, output`scratch/transceiver-vco-channel-kick`; expected
order several minutes, per-case timeout600s. Checker verifies exact pulse removal,
artifact provenance, completion and ordinal crossing comparisons; pending branch
exercised. No model noise density, cyclostationary weighting, small-charge linearity
or full phase coverage follows from this deterministic injection. It is a step
toward a relevant source sensitivity, not an intrinsic phase-noise result.

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_vco_channel_kick.py --fine`
Launch:`bash projects/programmable_transceiver_platform/verification/run_vco_channel_kick.sh`.
Do not relaunch while live. Next compare persistent phase displacement and sign
against differential injection, then establish phase/amplitude coverage before
combining any response with an independently validated source-noise model.

## Pass 477 — executable physical noise-port inventory

Previous pass launched channel-terminal pulse matrix. Verified live PLL3719500/
3719539 near6.524us, receiver3817752 and channel pulse3818989. No restart.
Audited actual ring hierarchy rather than treating output differential injection
as every device's noise response. New parser expands four actual delay cells:
30 device instances inside the loop and10 in the isolating buffer, comprising
32 MOS instances (including8 MOS capacitors) and8 load resistors. It maps physical
terminal nodes and verifies the launched X0.XMP drain/source pulse against the
manifest and source hash. Initial eight-port/count assertion was incorrectly nine;
corrected against the actual declaration before successful execution.

This is topology coverage, not PDK internal source coverage or noise PSD. A useful
noise estimate still requires phase-dependent sensitivities, charge linearity,
model source strengths/correlations and appropriate treatment of MOS capacitors,
loads, bias, supply and feedback. Current pulse matrix samples one device at one
phase. Avoid multiplying it by a generic transistor noise figure or extrapolating
it to the complete oscillator. Existing stationary filter noise-model excerpts
cannot establish periodically operating ring noise.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/audit_ring_noise_ports.py`.
Evidence:`evidence/ring-noise-ports.json`; channel pulse checker remains pending.
Next use completed channel response to select a bounded phase/amplitude test;
model-source audit remains explicitly unfinished. No phase-noise, silicon-bound,
whole-chip or layout qualification claim.

## Pass 478 — inspect actual PDK noise-source structure

Previous pass produced physical terminal inventory. New model audit reads files
from the live pinned simulator container and checks exact resolved-file hashes
against channel-pulse dependency manifest. Preserves NFET and ppolyf_u wrappers,
line-numbered noise-parameter excerpts and simulator image identity.

nfet_03v3 wrapper instantiates m0 with geometry/multiplicity and optional mismatch;
ppolyf_u expands to terminal/body/terminal resistors rt1/rb/rt2 plus two substrate
capacitors. Its resistor models explicitly contain noise=1,af=1.79,kf=2.4e-23.
Eight schematic loads therefore contain24 internal resistive elements. This does
not imply24 equal or independently calibrated noise contributions. External load
injection alone cannot assume internal transfer equivalence with capacitive taps.
FET noise coefficients and fnoicor choices are preserved, not treated as silicon
bounds; active-bin effective contributions and periodic weighting remain unverified.

Reproduce while the recorded container remains live:
`python3 projects/programmable_transceiver_platform/verification/audit_ring_noise_models.py`.
Evidence:`evidence/ring-noise-models.json`. Later reproduction must use the same
pinned image and model hashes if the container exits; do not restart a simulation
just to inspect model files. Verified PLL3719500, receiver3817752 and channel
pulse3818989 live this pass. No additional circuit run launched. Next await channel
response and establish noise contributions with a calibrated measurement before
attempting a phase-noise sum. Full schematic and layout gates remain unchanged.

## Pass 479 — matched quiet controls verified; whole-chip risks revisited

Previous pass verified installed model structure. Channel quiet41ns case now
complete; positive/negative remain running. New comparison verifies artifact
hashes, exact deck equality after removing each zero-current source, finite strict
time and horizon. Output/internal/channel zero-pulse placements reproduce all
saved vectors and timestamps bit-for-bit. This establishes a common control
across physical injection locations, not small-charge linearity or noise accuracy.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/compare_vco_quiet_controls.py`.
Evidence:`evidence/vco-quiet-controls.json`. Verified PLL3719500/3719539 live near
6.713us, receiver3817752 near139ns, channel3818989 live. Updated whole-chip risk
review: await current discriminator results before expanding pulse diagnostics;
then prioritize a circuit change against measured regulation/acquisition/timing
failures. Real bias, modulated TX, wired recovery, programmability, host and shared
physical integration remain required. No circuit promoted or layout gate opened.

## Pass 480 — signed reference-load range exposes asymmetric limits

Previous pass verified quiet pulse controls and reassessed risks. Cheap next
reference discriminator keeps baseline/hybrid output stages and bias unchanged:
high-rail DC current-10..+10mA in0.25mA steps, both sweep directions. Four cases
complete; checker verifies parent bodies, source/artifact hashes, log/header/grid
and finite81point sweeps. Positive current withdraws from high rail.

At+10mA baseline2.152675V vs hybrid2.035700V; at+5mA2.157653 vs2.138747V.
At-5mA baseline2.172076V vs hybrid2.151313V, but-10mA drives both3.893686V,
outside supply range. Such points are failed ideal-source stresses, not usable
operation or safe device voltages. This adds static source/sink range evidence
to the hybrid's dynamic tradeoff; improved input headroom alone is insufficient.
Sweep-direction maximum differences2.812/2.682mV remain unresolved; do not infer
hysteresis or a precise compliance boundary without numerical/device checks.

Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_reference_hybrid_load_dc.sh`
`python3 projects/programmable_transceiver_platform/verification/check_reference_hybrid_load_dc.py`.
Output`scratch/transceiver-reference-hybrid-load-dc`, evidence
`evidence/reference-hybrid-load-dc.json`. No candidate promotion. Next diagnose
high-load output gate/first-stage compliance and sweep sensitivity before sizing.
PLL3719500, receiver3817752, channel3818989 verified live. Channel positive pulse
completed with late+0.5844ps displacement; negative pending, no matrix conclusion.

## Pass 481 — load-limit conclusions survive tighter DC tolerances

Previous pass completed signed-load matrix and exposed direction sensitivity.
Repeated exactly those four81point sweeps with only reltol1e-5/vntol1e-8/
abstol1e-14. Completed all; shared checker --tight verifies exact deck reversal,
source/artifact provenance, grid and logs. Maximum direction differences collapse
from2.812/2.682mV to29.4794uV for both variants, supporting numerical sensitivity
rather than a demonstrated physical hysteresis claim. This is one convergence
comparison, not proof of exact solutions or identification of one tolerance.

Key limits persist: hybrid high rail2.035700V at+10mA withdrawal vs baseline
2.152675V; both3.893686V at-10mA injection. No candidate promotion. Next examine
output-control and device operating points at high withdrawal before changing
geometry; sustained reverse-load failure remains a separate output-stage issue.

Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_reference_hybrid_load_tolerance.sh`
`python3 projects/programmable_transceiver_platform/verification/check_reference_hybrid_load_dc.py --tight`.
Evidence:`evidence/reference-hybrid-load-tolerance.json`; original default report
preserved. Also completed channel pulse matrix check: late30–40ns ordinal shifts
+0.584402/-0.586433ps for+/-1.1fC, same edge counts and zero pre-pulse differences.
No intrinsic noise/phase-noise inference. PLL3719500 verified live near6.978us;
receiver3817752 still live. Channel3818989 terminal; no restart needed.

## Pass 482 — high-load hybrid first-stage compliance diagnosed

Previous pass tightened load sweeps; now add internal A/T voltages and all seven
high-stage devices'vds/vdsat/id/gm/gds. Four replay sweeps complete. Checker verifies
exact observation-only deck changes, source/artifact hashes and every original
saved value/time bit-identical to tight-tolerance parent.

Hybrid+10mA: output2.035700V, output gate0.995460V, input-stageXIN reported
|VDS|-|VDSAT|=-0.193755V versus+0.494133V unloaded and+0.118252V at5mA.
OutputPMOS margin remains+0.055211V at10mA. This supports first-stage loss of
compliance as a candidate mechanism, not a unique causal proof. Baseline differs:
input tail margin remains negative, outputPMOS margin-0.077406V at10mA despite
smaller target error. A single saturation flag is not a performance verdict.

Next controlled candidate: increase only hybrid outputPMOS strength, seeking
required current at less gate swing and restoredXIN compliance. Increased gate
capacitance and power/dynamic costs must be checked; DC improvement alone cannot
promote it. Reverse-load sink capacity remains separate and is not fixed by this.

Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_reference_hybrid_load_probe.sh`
`python3 projects/programmable_transceiver_platform/verification/check_reference_hybrid_load_probe.py`.
Evidence:`evidence/reference-hybrid-load-probe.json`. PLL3719500 and in-band
receiver3817752 verified live. No baseline promotion or layout work.

## Pass 483 — larger hybrid output PMOS restores selected static compliance

Previous pass located hybridXIN compliance loss under high withdrawal. Controlled
candidate changes only high outputPMOS multiplicity8*S->16*S; same hybrid first
stage, low rail, bias, compensation, targets and tight bidirectional load sweep.
Both81point cases complete. Checker proves exact one-line deck reversal to probed
parent, source/artifact integrity, finite matched grid and named vectors.

At+10mA high output2.035700->2.142532V, gate0.995460->1.515503V, XIN reported
margin-0.193755->+0.320576V. At+5mA output2.138747->2.145098V. Unloaded target
error-5.228->-2.158mV and pair power44.557850->44.562280mW. At10mA power rises
77.395885->77.554596mW. Direction high-rail discrepancy33.136uV. At-10mA output
still3.875065V above supply: reverse-load compliance not repaired. Static gain
benefit supports the proposed mechanism but cannot prove dynamic performance.

Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_reference_hybrid_output2_dc.sh`
`python3 projects/programmable_transceiver_platform/verification/check_reference_hybrid_output2.py`.
Evidence:`evidence/reference-hybrid-output2-dc.json`. Candidate remains fixture-only;
retained reference cell unchanged. Next exact output-strength candidate faces
reservoir-loaded impedance and signed steps, then actual switched ADC if justified.
Added gate capacitance and existing reverse-current limit remain costs/risks.
PLL3719500 and receiver3817752 verified live this pass; do not restart.

## Pass 484 — stronger hybrid output passes useful AC screen; pulse test live

Previous pass restored selected DC compliance with doubled high outputPMOS.
Reservoir-loaded AC now completes481points1k..1GHz for both rails. Exact one-line
reversal to previous hybrid AC decks verified; actual2048MIM reservoirs retained.
High low-frequency impedance1.015407->0.614286ohm; peak25.665121->19.271581ohm,
frequency50.119->74.989MHz. At20MHz15.232194->10.078059ohm; at200MHz9.894093->
11.173046ohm worsens. Low peak essentially unchanged13.48245ohm. Not loop-gain,
phase-margin or switched-load qualification; no adoption.

Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_reference_output2_impedance.sh`
`python3 projects/programmable_transceiver_platform/verification/check_reference_output2_impedance.py`.
Evidence:`evidence/reference-output2-impedance.json`.

Launched signed1mA100ns steps using unchanged pair_hybrid_step runner and new
AC parents, session70244. Initial output-name collision with historical2mA tests
was rejected by mkdir before simulation; those artifacts remain intact. Current
output is`scratch/transceiver-reference-hybrid-output2-step`, checker
`verification/check_reference_output2_step.py`, evidence
`evidence/reference-hybrid-output2-step.json` pending. Launch wrapper
`verification/run_reference_output2_step.sh`; do not restart while live.
PLL3719500 near7.236us and receiver3817752 verified live. Next use pulse recovery
and actual switched demand before promoting the promising static/AC candidate.

## Pass 485 — stronger hybrid signed steps complete; prepare long-loop analysis

Previous pass completed AC and launched steps. All four signed1mA100ns cases now
complete; existing checker validates exact parent bodies, source/artifact hashes,
finite strict waveforms and full horizon. High positive-load droop17.377->14.149mV,
negative-load rise17.986->14.641mV. High40–50ns residual4.966/5.720->1.914/2.197mV;
60–70ns33.05/38.50->5.75/6.33uV. Low results essentially unchanged. These support
actual dualADC trial, not adoption; prior hybrid isolated improvements regressed
under actual switching, and sustained reverse-load capacity remains inadequate.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_reference_output2_step.py`.
Evidence:`evidence/reference-hybrid-output2-step.json`. Step runner terminal;
do not restart. Next controlled actualADC experiment should change only hybrid
high outputPMOS multiplicity8*S->16*S and preserve all clocks/probes/loads.

PLL3719500 and receiver3817752 verified live. Added --selective to shared
check_latest_rf_loop.py and analyze_latest_loop_phase_tail.py for eventual8.001us
artifacts, accommodating its manifest fields while retaining deck/hash checks.
Pending selective path executed; original completed3.201us phase-tail path rerun
and reproduces old diagnostics. Full selective completion path remains unexecuted,
not qualified. Commands use`--selective` for both check and phase-tail analysis;
run the latter only after upstream completion. No new lock/noise claim.

## Pass 486 — stronger hybrid actual dualADC trial running

Previous pass verified signed-step benefit. Built actual209.9ns dualADC candidate
from retained probed/reltol baseline with five hybrid input-stage substitutions
and high outputPMOS multiplicity8*S->16*S. Independent direct comparison against
completed prior hybrid frames proves only that output multiplicity differs; clocks,
loads, probes, stimuli, compensation and reservoirs are unchanged. Production
reference cell is unchanged. Shared checker --reference-output2 reconstructs the
six substitutions and retains baseline target/span/motion/code/power comparisons.
Pending branch exercised; no performance result asserted.

Launched session78671, runner3835976 verified live beyond9ns. Output
`scratch/transceiver-reference-hybrid-output2-frames`;209.9ns horizon,1800s timeout,
expected several minutes from previous fixture. Do not restart while running.
Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_reference_hybrid_output2_frames.sh`
`python3 projects/programmable_transceiver_platform/verification/check_bit6_complement.py --reference-output2`.
Evidence:`evidence/reference-hybrid-output2-frames.json` pending. Compare both the
retained baseline and prior hybrid, preserving absolute error and power regressions.
PLL3719500 and receiver3817752 remain live. No adoption, converter qualification
or layout claim; dynamic full-load evidence is the next decision gate.

## Pass 487 — signed actual reference demand and whole-chip risk review

Previous pass launched stronger-hybrid dualADC. Verified all three existing long
runs live (PLL3719500~7.536us, receiver3817752~430ns, ADC3835976~40ns).
New native-waveform audit checks parent completion/hashes and finite strict time;
forms actual CDAC demand=delivery-reservoir current. Splits integration at zero
crossings and verifies withdrawn-minus-returned charge equals signed net charge.

Baseline60–209.9ns high withdrawn100.905pC/returned9.154pC, low withdrawn6.482pC/
returned98.050pC. High peaks+94.281/-23.194mA; low+18.007/-66.235mA. Prior hybrid
has similar sign structure. These are complete-window peaks, not earlier selected
worst-decision-window peaks. Do not equate them to DC capacity requirements:
reservoir storage and transient driver action remain essential.

Reproduce:`python3 projects/programmable_transceiver_platform/verification/analyze_reference_return_charge.py`
from repository root. Evidence:`evidence/reference-return-charge.json`.
Updated inventory/risk review; all10 obligations incomplete, layout gate closed.
Next inspect pending candidate decision windows and receiver changing acquisition;
retain real bias, phase noise, modulated TX, wired recovery and coexistence gaps.

## Pass 488 — actualADC three-way comparison gated on completed evidence

Previous pass measured signed charge and updated risks. Verified existing live
PLL3719500 near7.655us, receiver3817752 near472ns and ADC3835976 near122ns; no
restart or new simulation. Added shared three-way summary with artifact checks,
complete48 rail/24 span-window coverage, reference/driver power, held samples and
captured codes. It requires identical retained-baseline reports before declaring
the three-way comparison complete. Original retained/hybrid completed paths run;
stronger candidate remains explicitly pending. No extrapolated candidate metrics.

Reproduce from repository root after refreshing candidate upstream checker:
`python3 projects/programmable_transceiver_platform/verification/check_bit6_complement.py --reference-output2`
`python3 projects/programmable_transceiver_platform/verification/compare_reference_hybrid_candidates.py`.
Evidence:`evidence/reference-hybrid-candidates.json`. Retained/high worst61.349mV,
hybrid70.646mV and reference44.423/46.477mW reproduce original results. The new
candidate must beat relevant competing metrics; summary adds no passing threshold
or adoption policy. Next inspect terminal candidate and autonomous phase evidence.

## Pass 489 — stronger hybrid actualADC improves error but worsens motion

Previous pass prepared three-way comparison. Verified running processes twice;
waited for existing ADC run to finish without restart. Full209.9ns candidate now
terminal; checker validates all artifacts, exact declared circuit changes, source
hashes, finite strict waveforms, digital levels and decision-window coverage.
Three-way baseline reports match exactly; completed comparison now available.

Retained / hybrid / stronger hybrid high worst target error61.349/70.646/56.366mV;
high window motion3.179/4.946/7.138mV. Low worst error20.173/20.314/21.056mV,
low motion6.877/6.614/6.529mV. Stronger span0.927269..1.035212V improves range
versus retained0.922457..1.058053V but worst window span motion10.055->13.666mV.
Reference mean power44.423->46.493mW. Codes177/78/178->179/76/179, matching prior
hybrid; not a correct-code criterion. Stronger held values-401.218,+401.105,
-401.131mV (bothchannels) improve nominal input accuracy relative to retained
selected histories, but do not qualify conversion with moving references.

Candidate remains unadopted: static/isolated improvements transfer only partly.
Next localize high-motion events and compare signed load/gate response before
choosing compensation or output architecture; do not blindly strengthen again.
Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_bit6_complement.py --reference-output2`
`python3 projects/programmable_transceiver_platform/verification/compare_reference_hybrid_candidates.py`.
Evidence:`evidence/reference-hybrid-output2-frames.json` and
`evidence/reference-hybrid-candidates.json`. PLL and receiver remained live at
last verified poll; refresh their actual status next pass. No layout qualification.

## Pass 490 — larger decision-window motion is active recovery

Previous pass completed stronger-hybrid actualADC tradeoff. New signed event audit
checks parent hashes, finite strict native waveform and endpoint-interpolated
windows122.5–125.35ns and125.2–125.35ns. Actual high-load event charge11.311->
11.609pC changes modestly; driver delivery3.906->7.302pC increases, reservoir net
discharge7.405->4.307pC decreases. Peak load72.574->70.972mA decreases slightly.

During decision window, both high rails rise monotonically toward target:
retained2.088651->2.091830V, candidate2.093634->2.100772V. High reservoir recharge
0.289->0.648pC; delivery0.412->0.846pC. Thus increased motion reflects stronger
recovery that is still late, not simply a larger load pulse. Observation does not
uniquely identify compensation/loop mechanisms. Do not slow recovery merely to
minimize motion: next change should improve earlier recovery or reduce disturbance,
with absolute error, window motion and power scored together. No timing-budget
extension or candidate adoption is justified by this diagnostic.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_reference_output2_event.py`.
Evidence:`evidence/reference-output2-event.json`. PLL3719500 verified live near
7.899us, receiver3817752 near552.7ns. Refresh terminal status before further work;
no restart. No layout or conversion qualification.

## Pass 491 — changing-input receiver completes; report-path bug corrected

Previous pass diagnosed reference recovery. Existing receiver610ns run now terminal;
upstream checker verifies exact frequency-only change, source/artifact integrity,
finite strict full waveform. Three-frame analysis executes completed in-band path.
Found analyzer's --inband output still targeted separated report; corrected path
selection and regenerated both reports from preserved checked artifacts. No raw
evidence lost; pending earlier report claim did not qualify the completed path.

At holds470/520/570ns source/held mean I(mV):5.2148/5.4433,-4.9119/-4.7407,
-5.2192/-5.4576; Q:5.1939/4.9927,6.1374/6.3878,-5.1911/-4.9752. Captured I
127/128/128 and Q127/127/128 show changing history, not correctness or ENOB.
Decision span across frames0.914493..1.068451V still demonstrates inadequate
reference regulation. Small signal excursion and ideal seeded clocks/bias remain;
this does not establish full-scale acquisition or useful modem performance.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_rx_adc_inband.py`
`python3 projects/programmable_transceiver_platform/verification/analyze_rx_adc_separated_frames.py --inband`
`python3 projects/programmable_transceiver_platform/verification/analyze_rx_adc_separated_frames.py`.
Distinct evidence:`rx-adc-inband-frames.json` and `rx-adc-separated-frames.json`.
PLL reached8.001us/4002382rows and wrote~4.3GB waveform; ngspice exited, runner
3719500 still live finalizing at last poll. Result.json absent then: do not restart
or assert final provenance/phase quality. Inspect finalization/resource behavior
next before loading full waveform. No layout or circuit promotion.

## Pass 492 — PLL finalization recovered without restarting simulation

Previous pass completed changing-input receiver. Found completed PLL ngspice had
exited after4002382rows, but runner3719500 was hashing4.3GB via read_bytes inside
4GB memory limit, using~4GB RSS and paging. Inspected live Docker state (not OOM
killed), preserved before/after resource records, raised existing memory limit to
8GiB with total memory+swap unchanged8GiB. Runner finalized result.json and exited;
no circuit/source/simulation changes or rerun. Resource records live beside artifacts.

Changed future selective runner and current checker/phase-tail hashing to bounded
1MiB chunks after runner terminal; original source hashes remain historical run
provenance. Added workflow lesson budgeting postprocessing memory separately.
Launched existing full selective checker session38859; processes4million rows,
still running at last observation. Do not duplicate it. Phase/lock conclusions
remain pending; raw complete export alone is insufficient qualification.

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_latest_rf_loop.py --selective`
then after completion:
`python3 projects/programmable_transceiver_platform/verification/analyze_latest_loop_phase_tail.py --selective`.
Next inspect completed phase/control/power results and determine whether actual
feedback settles; preserve seed/ideal-bias/zero-RF/no-ADC limitations.

Pass492 completion update: checker session38859 finished successfully, followed by
selective phase-tail analysis session45552. Complete8.001us seeded diagnostic.
Last21 paired edges(6960.978–7984.978ns) drift0.10809ps across1024ns, detrended
range0.57584ps; these are deterministic diagnostics, not intrinsic jitter or lock
qualification. Last5/10 windows drift0.21156/0.47264ps. Late7900–8000ns VCO
frequency2.500004861GHz; CTRL1.081369..1.090937V. Zero-RF I-filter differential
range-14.210..57.005mV is substantial and needs spectral/time diagnosis before
claiming useful autonomous RF conversion. Next prioritize that feedthrough/ripple
question alongside missing noise/startup, rather than treating frequency agreement
as full timing or signal-quality success. All checker processes terminal.

## Pass 493 — autonomous zero-input disturbance is reference-periodic

Previous pass completed8.001us PLL and identified71mVpp zero-RF I output. New
streaming analyzer hashes the complete waveform while retaining only late nodes;
checks named columns, finite strict time and completed parent evidence. Projects
onto first8 harmonics of19.53125MHz over three consecutive512ns windows using
10ps interpolation. Known7mV sinusoid plusDC/phase offset independently reproduces
amplitude within1e-12V with the same projection calculation.

I fundamental31.716/31.564/31.568mV peak across6400–6912/6912–7424/7424–7936ns;
late I71.237mVpp, Q6.183mVpp with2.820mV fundamental. Late one-reference-period
shift RMS differences I4.275uV,Q0.800uV establish near-periodic late behavior,
not decaying startup alone. CTRL9.560mVpp, fundamental0.172mV; no unique coupling
mechanism follows. Earlier desired5MHz response~7.182mV for1mV RF was a different
fixed-control fixture, not a measured SNR ratio to this autonomous spur.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_latest_loop_baseband.py`.
Evidence:`evidence/latest-loop-baseband.json`. Next distinguish control modulation,
reference/divider switching and RF/LO conversion paths with matched controlled
fixtures; preserve actual-feedback baseline. Frequency settling does not qualify
useful RF signal quality. No intrinsic noise or jitter result.

## Pass 494 — reference spur traced upstream through saved path nodes

Previous pass established persistent reference-periodic baseband disturbance.
Extended same streaming/hash-verified analyzer with --path-nodes, preserving
original report and projection windows. Adds ring, LO and mixer differential/
common-mode signals plus RF drain. Existing baseband projections reproduce exactly.

Late7424–7936ns fundamental peaks: I-LO CM72.798mV vs Q0.249mV; I-LO differential
7.599mV vs Q0.051mV; I-mixer differential7.147mV vs Q0.626mV; I-mixerCM81.728mV
vs Q12.174mV. Ring differential0.0638mV, ringCM0.412mV, RF drain40.870mV.
Thus observed spur is present before the baseband filter; I-LO/mixer asymmetry
is a useful next discriminator. Amplitude ordering is not proof of causation or
an additive transfer budget in this nonlinear coupled circuit.10ps interpolation
is not full edge-accuracy qualification, especially for small RF residual terms.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_latest_loop_baseband.py --path-nodes`.
Evidence:`evidence/latest-loop-path-nodes.json`. Next inspect I/Q LO edge/duty and
bias behavior, validate sensitive measurements against native or finer sampling,
then choose a controlled path perturbation with unchanged actual-loop baseline.
No new simulation, topology adoption, noise or layout qualification this pass.

## Pass 495 — I-channel LO has missing threshold crossings

Previous pass located reference spur upstream of filters. Native-grid analyzer
hashes complete saved waveform, retains late LO nodes, checks finite strict time,
and linearly interpolates1.65V crossings without uniform resampling. Over7424–
7936ns complete rising-to-rising intervals: OIP1169, OIN1159 versus OQP/OQN1279.
I maximum periods3239.6/3215.4ps and duty ranges3.18–98.31%/1.10–95.67%; Q
period ranges388.9–410.1ps/388.5–409.2ps and duty48.35–57.58%/48.17–57.32%.
Each retained interval has one falling crossing. Large gaps signify missing
1.65V crossings, not proof the oscillator stopped or a mixer conduction threshold.

This is a stronger local discriminator than the baseband spur alone: investigate
I-channel LO shaping/drive amplitude and bias under autonomous modulation before
further converter tuning. Q provides a simultaneous comparison, not identical
loading/topology. Need ring/differential crossing control and upstream BIP/BIN
amplitude/bias observations before selecting a circuit repair. No noise or layout
qualification; full autonomous signal-quality failure remains open.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_latest_lo_edges.py`.
Evidence:`evidence/latest-lo-edges.json` includes all cycle records. No new circuit
simulation; previously completed baseline preserved.

## Pass 496 — ring continues during I-LO missing-crossing gaps

Previous pass found long I-output crossing intervals. Added --upstream to same
hash-verified native-grid analysis, preserving earlier report. Ring differential
zero-crossing control gives1279 complete periods397.411–400.139ps over7424–7936ns.
There are38 I-output intervals longer than two nominal periods; no Q intervals
meet this diagnostic definition. A3.239ns OIP interval contains8 ring rises.

In that interval BIP spans1.43663..1.62921V (time mean1.53234V), quadratureIP
2.13927..2.34594V, while output OIP is mostly low. Another interval contains5 ring
rises with OIP mostly high. Thus lack of carrier cycles at the ring is not the
explanation for the output gaps. AC-coupled inverter bias/switching margin and
loaded drive require direct tests. BIP's range alone does not establish the
inverter threshold or unique mechanism; saved PRE/MID signals are missing.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_latest_lo_edges.py --upstream`.
Evidence:`evidence/latest-lo-upstream.json` includes per-gap voltages and ring
edge counts. Next controlled fixture should preserve actual coupling, self-bias,
buffer/final driver and mixer loading while observing MID/PRE. Validate its replay
against the autonomous parent before tuning; do not claim open-loop replay fixes
autonomous operation. No new simulation or adopted hardware this pass.

## Pass 497 — I input brackets actual LO chain static switching region

Previous pass localized missing edges downstream of oscillating ring. Cheap
actual three-inverter DC chain (pt_lo_buffer plus final stage,3.3V/27C/50fF)
swept1.3..1.8V both directions501points. Source/include hashes, finite grid and
logs checked. Initial dependency scan did not strip quoted paths; stopped before
simulation, corrected and preserved original directory; valid artifacts in
`scratch/transceiver-lo-transfer-dc-v2`.

MID/PRE/OUT1.65V crossing input1.523961/1.530659/1.530427V. Direction voltage
maximum difference6.131uV. Observed gap BIP extrema1.436633..1.629207V give static
output approximately3.3V/0V, so input does bracket static switching region.
This rejects a simplistic static out-of-range explanation, not dynamic bias,
loading or inadequate dwell time. Ideal DC input removes coupling/self-bias and
mixer load; those must return in replay before tuning. No buffer qualification.

Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_lo_transfer_dc.sh`
`python3 projects/programmable_transceiver_platform/verification/check_lo_transfer_dc.py`.
Evidence:`evidence/lo-transfer-dc.json`. Next actual waveform replay must retain
coupling, self-bias and loaded drive, with MID/PRE observations and reproduction
checks. No production topology change or layout work.

## Pass 498 — loaded receiver replay prepared and launched

Previous pass established static switching-region overlap. Prepared1024ns actual
receiver driven by saved autonomousP/N history6912–7936ns (512242native points,
interpolated endpoints). Streaming full waveform hash matches completed parent.
Retains quadrature, coupling reservoirs, self-bias100k resistors, all buffer/final
stages, actual mixers/LNA/filters and loads. Removes ring instance/its seed and
PLL/divider feedback; ideal P/N voltage sources replace boundary. Seeds available
saved receiver voltages only. Adds MID/PRE observations. This is a reduced fixture,
not an autonomous-loop implementation or exact internal-state reconstruction.

Preparation manifest records exact removed lines, parent hashes, source provenance,
PWL/deck/reference-sample hashes and limits. Runner recursively hashes dependencies,
checks overlapping parent sources, streams output hashing, preserves errors/timeouts.
Launched session43227, output`scratch/transceiver-lo-receiver-replay`, horizon1024ns,
2ps maximumstep/UIC,7200s timeout. Do not restart while live. Dense PWL breakpoints
may limit speed; no promise this reduced circuit is faster than parent.

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/prepare_lo_receiver_replay.py`
`bash projects/programmable_transceiver_platform/verification/run_lo_receiver_replay.sh`.
Next implement/check reproduction over512–1024ns against saved parent_samples.npy,
including I/Q LO edges, self-bias and filter waveforms. No acceptance threshold
invented after inspection; report discrepancies before approving diagnostic use.
No candidate tuning or claims from unverified replay. No layout work.

## Pass 499 — replay comparison checker and risk refocus

Previous pass launched loaded receiver replay; process3862793 verified live near
24ns this pass. Added checker with preparation/artifact/source checks, strict
finite time, horizon/errors, unshifted512–1024ns voltage differences and native
ordinal LO crossings. Unequal crossing counts prevent displacement comparison.
Explicit diagnostic_use_approved stays false: no post-hoc tolerance or automatic
approval. Parent-versus-itself exercises comparison numerics and reproduces zero
differences; pending real-result branch exercised. Completed real path untested.

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_lo_receiver_replay.py --self-check`
`python3 projects/programmable_transceiver_platform/verification/check_lo_receiver_replay.py`.
Evidence:`lo-receiver-replay-self-check.json` and pending`lo-receiver-replay.json`.
Updated whole-chip risk priorities around demonstrated autonomous I-LO failure;
keep actual-feedback requirement, converter failures and all missing blocks open.
Next inspect completed replay fidelity before modifying self-bias or driver.

## Pass 500 — self-bias is active feedback, not a passive RC assumption

Previous pass added replay comparison. Verified replay3862793 live near71ns;
retain existing run. Inspection finds unsaved filter first-stageMP/MN capacitors
and bufferMID/PRE states, so seeded external voltages do not reconstruct full
initial state. Late comparison remains essential; no initial-state equivalence.

Cheap isolated self-bias test adds actual4MIM coupling and100k MID feedback to
actual buffer/final stage with50fF load. OP and281point1k..10GHz AC complete,
source/artifact/header/grid checks pass. Static IN/MID/PRE/OUT near1.530318V.
Input transfer magnitude0.00627@1MHz,0.13273@19.53125MHz,1.05368@100MHz,
0.92068@2.5GHz; stationary inverter feedback alters simple RC behavior. Do not
translate static transfer into envelope rejection or periodic stability. Actual
mixer/source loading and RF switching are absent, explicitly limiting this test.

Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_lo_selfbias_ac.sh`
`python3 projects/programmable_transceiver_platform/verification/check_lo_selfbias_ac.py`.
Evidence:`evidence/lo-selfbias-ac.json`. This rules out choosing RFB from passive
RC intuition alone; next inspect validated replayMID/PRE behavior before changing
feedback bandwidth. No production circuit or layout change.

## Pass 501 — inventory reconciled with terminal system evidence

Previous pass measured isolated self-bias. Verified loaded replay3862793 live near
115.8ns; no restart or tuning. Inventory had stale pending/failed-only descriptions
for long PLL and receiver despite later appended notes. Rewrote RF receive,
synthesizer and ADC gaps from checked completed evidence; replaced pending retry
integration_gap and linked final phase/spur/edge/converter reports. Synthesizer
status now explicitly partial connected seeded loop with signal-quality failure.

Separates autonomous zero-RF loop from changing-input ADC fixture; neither proves
simultaneous autonomous conversion. Reaffirms missing real bias/startup, intrinsic
noise, RF accuracy/gain, wired recovery, modulated TX, host and coexistence work.
Audit still reports all10 block obligations incomplete and layout gate closed.
This is authoritative-state correction, not new circuit performance evidence.
Next inspect validated loaded replay before selecting LO repair.

## Pass 502 — input dwell alone does not separate failed I cycles

Previous pass reconciled inventory. Replay3862793 verified live near159ns; no
restart. New audit uses hash-checked cached autonomous parent samples and measured
static OUT switching threshold1.530427V. Integrates piecewise-linear time above
threshold within native ring cycles, groups actual output rising-edge counts.
I-positive109 zero-edge bins still spend48.36–50.35% above static threshold;
normal1170 bins46.63–50.36%. I-negative119 zero-edge bins46.32–50.24%, normal
1160 bins47.54–50.25%. Voltage ranges overlap strongly. Simple threshold-dwell
starvation therefore does not distinguish observed failed bins; dynamic internal
state, speed/loading and periodic interactions remain plausible.

Q-positive has10 empty plus10 double-edge bins from phase crossing bin boundaries;
Q-negative1279 single-edge bins. This confirms per-ring-bin missing counts alone
are weaker than directly observed multi-period output crossing gaps. Do not
relabel Q as losing pulses from this statistic. Static threshold is diagnostic,
not an RF switching or mixer conduction criterion.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_lo_input_dwell.py`.
Evidence:`evidence/lo-input-dwell.json`. Next prioritize MID/PRE evidence from
validated loaded replay, rather than tuning bias solely from input threshold
coverage. No new simulation or production circuit changes.

## Pass 503 — bare LO chain carries observed-scale ideal sinusoidal inputs

Previous pass found input dwell overlap. Replay3862793 verified live near190ns.
Independent bounded screen reuses actual three-inverter chain with50fF output,
ideal1.530318V-biased2.5GHz sine of95/108mV peak,100ns1ps maximumstep. Both cases
complete; hashes/logs/named vectors/finite strict time checked. Late50–100ns each
has125 input, PRE and final output rising edges; final periods~400ps. Smaller
output range0.355..2.987V versus larger0.301..3.053V; no loaded rail margin claim.

Smaller MID range1.421..1.643V never crosses1.65V yet downstream switches every
cycle: emphasizes that a universal half-supply marker is not each inverter's
switch threshold. Bare-chain unconditional inability to carry2.5GHz at these
ideal inputs is not supported. Actual coupling/self-bias, waveform distortion,
source impedance and mixer loading remain untested by this small screen.

Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_lo_sine_speed.sh`
`python3 projects/programmable_transceiver_platform/verification/check_lo_sine_speed.py`.
Evidence:`evidence/lo-sine-speed.json`. Preserve actual loaded replay for diagnosis;
no baseline circuit change, autonomous qualification or layout work.

## Pass 504 — coupling/self-bias alone retains ideal-sine output crossings

Previous pass bare-chain ideal sine produced all output edges. Added actual4MIM
coupling and100k MID feedback, replacing ideal input bias with ideal2.24V-CM source;
same95/108mV source amplitudes, stages,50fF load and100ns1ps transient. Both cases
complete. Checker independently reverses declared boundary changes to prior decks,
checks hashes/logs/grid and counts late edges. Input swing attenuates as expected:
small1.443..1.618V, large1.431..1.630V. Each final output has125 rising edges over
50–100ns at~400ps intervals; ranges0.303..2.861V and0.249..2.931V respectively.

Actual coupling/self-bias under a clean stationary sine does not by itself recreate
missing output crossings. Output excursions are not full-rail qualification and
mixer/source loading and measured waveform modulation remain absent. This narrows
one hypothesis without clearing self-bias behavior in the full receiver. Full
replay3862793 verified live this pass; do not restart or tune unvalidated replay.

Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_lo_selfbias_sine.sh`
`python3 projects/programmable_transceiver_platform/verification/check_lo_selfbias_sine.py`.
Evidence:`evidence/lo-selfbias-sine.json`. Next retain actual mixer loading or inspect
completed full replay before selecting repair; no production change or layout.

## Pass 505 — actual mixer reduces clean-sine drive swing without losing edges

Previous pass coupling/self-bias ideal-sine screen retained edges. Added a second
complementary real LO chain and actual16FET mixer,1k/1p IF loads, ideal1.5V RF via
300ohm. Both100ns cases complete. Checker verifies original driven chain unchanged
by removing only added complement/mixer block; opposite source amplitudes checked,
plus source/artifact hashes and finite strict timing. Original full replay3862793
verified live near244ns this pass; preserve run.

Small95mV source final output0.409..2.713V; large108mV0.336..2.805V, lower swing
than unloaded self-biased screen. Both125 late rising edges at400ps spacing.
Thus this particular clean-sine/mixer termination does not recreate missing
crossings. Reduced rail excursion is a margin warning, not measured receiver
conduction/linearity qualification. Actual LNA/filter interaction, source waveform
modulation and quadrature network remain absent from this simplified test.

Commands from repository root:
`bash projects/programmable_transceiver_platform/verification/run_lo_mixer_sine.sh`
`python3 projects/programmable_transceiver_platform/verification/check_lo_mixer_sine.py`.
Evidence:`evidence/lo-mixer-sine.json`. Stop extending clean-sine substitutions
until actual replay comparison resolves whether it reproduces missing edges.
No topology promoted; autonomous RF, noise, references and full-chip gaps remain.

## Pass 506 — recorded waveform shape does not yield simple distortion explanation

Previous pass actual-mixer clean-sine test preserved edges. Full replay3862793
verified live near293ns; no restart. New cached-parent analysis uses native ring
cycles and complex harmonic projection grouped by output-edge bins. Hashes tie
samples and grouping to prior evidence. I-positive fundamental means94.015mV
(empty bins) vs94.153mV(single edge), with overlapping ranges. Q-positive mean
fundamental~110.309mV, second5.087mV and third3.523mV exceed I's~2.165/1.014mV
higher components despite more regular Q output. Harmonic magnitudes alone do
not identify failure. I cycle means span~1.5232..1.5333V versus Q~1.5291..1.5313V;
dynamic bias sensitivity remains a candidate, not a demonstrated root cause.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_lo_input_shape.py`.
Evidence:`evidence/lo-input-shape.json`. No adopted topology or new simulation.
Avoid further scalar-input diagnostics without a discriminating hypothesis;
next decisive evidence remains validated replayMID/PRE and loaded output behavior.

## Pass 507 — internal replay diagnosis prepared with threshold caveat

Two preceding goal turns were verified waits; replay3862793 remains live and
advanced to379ns this pass. Added completion-gated MID/PRE/output stage analysis
for512–1024ns. Compares rising crossings at1.530318V and1.65V, native interpolated
intervals, ranges and intervals longer than800ps. A known triangle validates
crossing interpolation/direction. Pending path executed after correcting a syntax
error; full waveform path not yet exercised or qualified. No fabricated results.

Marker distinction follows completed small-sine evidence: MID can remain below
1.65V while the chain delivers every output edge. Replay-only report keeps parent
reproduction approval separate; missing saved parentMID/PRE states cannot be
silently reconstructed or called reproduced. Next run only after upstream checker
reports actual completion, then inspect reproduction before using stage results.

Command from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_lo_replay_stages.py`.
Evidence:`evidence/lo-replay-stages.json` pending. No new simulation or topology
change; retain session43227 rather than restarting existing run.

## Pass 508 — receiver replay completes and reproduces output edge counts

Previous turn was a verified wait. Original replay process3862793 finished without
restart:1024ns,2561138rows,return0,no reported warnings/errors. Reproduction checker
initially rejected a2.118e-22s floating-point endpoint difference between shifted
parent time and replay time. Restricted voltage comparison to the exact shared
window rather than extrapolating or altering samples; rerun and parent self-check
complete. Original simulation artifacts remain unchanged.

Late512–1024ns rising counts match parent for all outputs:OIP1170,OIN1160,
OQP1280,OQN1280. Maximum ordinal displacement0.367/0.626/0.044/0.044ps;
maximum output voltage difference2.201mV. Buffer input differences at most33.51uV.
LNA gate differs up to1.431mV, so this is not an identical all-node reproduction.
Evidence supports further scoped investigation of missing LO edges, but the report
keeps diagnostic_use_approved=false until that scope is explicitly audited.
Ideal replay boundaries, omitted feedback and unsaved initial internal states
remain limitations. No circuit promoted and no autonomous qualification inferred.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_lo_receiver_replay.py`
and the same command with `--self-check`. Evidence:`evidence/lo-receiver-replay.json`.
Next inspect saved MID/PRE histories and establish a scoped reproduction decision
before tuning; retain the original autonomous loop for subsequent validation.

## Pass 509 — replay localizes crossing loss within the loaded LO chain

Previous pass completed replay and reproduction measurements. Executed the prepared
stage analyzer against hashed terminal artifacts; native-grid and known-triangle
crossing checks pass. In512–1024ns, both I inputs and first-inverter MID nodes
retain1280 rising crossings at1.530318V, with maximum intervals at MID below408ps.
PRE has1250 crossings at that marker and1240 at1.65V; intervals reach1.665ns and
2.032ns respectively. Final outputs have1170/1160 crossings at1.65V with gaps
up to3.239ns. All Q stages retain1280 crossings at the lower marker and downstream
Q stages also retain1280 at1.65V. MID at1.65V gives only40 I crossings: this
illustrates why half-supply crossing counts alone cannot diagnose the first stage.

This localizes the observed timing-marker loss to PRE and further deterioration
at the final output in the replay; it does not prove which transistor, bias
interaction or loading mechanism causes it. Parent MID/PRE were not saved, so
these internal histories have no direct autonomous comparison. Prior output-count
agreement supports this investigation but is not an all-node reproduction pass;
formal scoped-use approval remains false. No circuit change adopted.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/analyze_lo_replay_stages.py`.
Evidence:`evidence/lo-replay-stages.json`. Next quantify per-cycle MID/PRE swing,
mean and pulse width during reproduced output gaps, then choose one controlled
loaded-chain change. Any improvement must return to autonomous feedback before
claiming the RF timing failure repaired. Full-chip schematic gate remains closed.

## Pass 510 — gap-conditioned internal-state measurement implemented

Previous pass completed stage-crossing evidence. Added analyze_lo_gap_state.py to
measure complete ring cycles inside output rising-edge gaps longer than800ps and
outside all such gaps; boundary-overlap cycles are excluded. Reports min/max/mean
voltage and piecewise-linear duration above the marker for input, MID, PRE, output.
Triangle calibration passes before reading the real waveform; source waveform
hash and native finite/strict time-grid checks gate analysis. Diagnostic markers
remain1.530318V internally and1.65V at output, not conduction criteria.

Started from repository root with
`python3 projects/programmable_transceiver_platform/verification/analyze_lo_gap_state.py`.
Analysis session71035 is still running at handoff; no results claimed. Preserve
that analysis job and inspect evidence/lo-gap-state.json after completion. This
is postprocessing of completed replay, not a new simulation or adopted circuit.
Next use the measured swing/bias/duty association to choose a controlled circuit
hypothesis; correlation alone does not prove cause or approve replay scope.

## Pass 511 — gap state measured; equivalent faster analysis verified

Original analysis3950899/session71035 completed normally. Added optional
`--contiguous` storage mode with a separate report while preserving the original
job. Both completed reports compare exactly equal as parsed JSON. Strided waveform
columns passed repeatedly into interpolation caused avoidable copying; contiguous
column storage completed in seconds versus over seven minutes for original.
No waveform samples, grouping, markers or measurement formulas changed.

There are108 complete IP and117 IN ring cycles wholly inside long output gaps;
1132/1123 outside, with boundary cycles excluded. Q has no long-gap cycles.
IP mean per-cycle input swing is189.914mV inside vs190.290mV outside; MID swing
223.607 vs213.796mV, PRE812.516 vs791.465mV. Thus average swing collapse upstream
is not demonstrated. Final-output mean per-cycle swing falls1.107V vs2.413V.
IP PRE cycle means span1.018..1.824V inside gaps; IN0.968..1.855V. Output duties
include0 and1. Large internal bias excursions and final-stage attenuation warrant
investigation, but distributions overlap and ring-bin phase effects remain;
this grouping does not isolate a causal mechanism or establish replay approval.

Commands from repository root: `python3 projects/programmable_transceiver_platform/verification/analyze_lo_gap_state.py`
and append `--contiguous` for faster equivalent storage. Evidence:
`evidence/lo-gap-state.json`, `evidence/lo-gap-state-contiguous.json`.
Next choose a controlled later-stage bias/loading experiment, retaining the
actual receiver load and requiring subsequent autonomous validation. No adopted
circuit change; whole-chip schematic and tapeout qualification remain incomplete.

## Pass 512 — scoped replay decision and whole-chip risk reassessment

Previous pass completed gap metrics and equivalent faster postprocessing. Added
and executed audit_lo_replay_scope.py. Its explicitly post-observation guardrails
permit screening the severe multi-cycle output symptom: all four rising counts
match, maximum ordinal disagreement0.626ps, output disagreement2.201mV and buffer
input disagreement33.51uV. These diagnostic limits are not design specifications
or retroactive performance passes. Original all-purpose reproduction approval
remains false; separate evidence/lo-replay-scope.json narrowly approves mechanism
investigation and candidate screening. Unsaved parentMID/PRE, LNA gate discrepancy,
ideal replay boundaries, noise/startup and autonomous qualification are excluded.

Updated receiver implementation inventory and current whole-chip risk priorities.
The observed LO failure remains the next circuit target, while intrinsic timing
noise, reference/ADC accuracy, RX gain/noise/linearity, modulatedTX, wired recovery,
host timing and whole-chip physical integration remain open. No topology promoted.
Reproduce: `python3 projects/programmable_transceiver_platform/verification/audit_lo_replay_scope.py`.
Next run one controlled loaded-chain change and compare against the retained
replay before spending another long autonomous history on it.

## Pass 513 — doubled second LO inverter launched under actual receiver load

Previous pass approved narrowly scoped replay diagnosis and reassessed whole-chip
risk. Prepared one candidate: XP2/XN2 multiplicity4*S to8*S in all four LO buffers.
First stage, final driver, feedback resistors, quadrature, stimulus, initialization,
mixer and baseband loads are unchanged. Exact reversal reconstructs original deck;
prepared inputs hash-checked. Candidate cell is inlined only in the experimental
deck; production lo_buffer.spice remains unchanged. Reuses the pinned runner and
its before/after dependency hashes, completion and artifact capture.

Hypothesis: stronger second-stage drive may reduce PRE/final-stage pulse loss,
with doubled first-stage capacitive loading a competing cost. This is a diagnostic
intervention, not a presumed fix. Evaluate512–1024ns against original replay:
all four output crossing counts, intervals over800ps, swing/duty, internal bias
and baseband disturbance. Fewer missing crossings alone cannot qualify performance;
Q regressions and distorted output swing matter. Current saved vectors do not
establish power cost; any promising candidate needs power and autonomous checks.

Commands from repository root:
`python3 projects/programmable_transceiver_platform/verification/prepare_lo_second_stage.py`
then `bash projects/programmable_transceiver_platform/verification/run_lo_second_stage.sh`.
Prepared directory:`scratch/transceiver-lo-second-stage-prepared`; output:
`scratch/transceiver-lo-second-stage`; live session24593.1024ns target; results
pending. Do not restart solely because polling expires. Next add/run candidate
completion and matched output comparison; no topology adopted or layout begun.

## Pass 514 — matched second-stage checker prepared

Previous pass launched one controlled transistor-sizing candidate. Process3966322
revalidated live; preserve session24593. Added check_lo_second_stage.py and executed
its pending path. It validates prepared hashes and exact reversal to the retained
baseline, then on terminal output checks dependency/artifact hashes, unchanged
deck, log failures, finite strict time grid and1024ns horizon before comparing.
Known triangle validates crossing interpolation. Full candidate-data path remains
unexecuted while simulation runs; pending evidence is not a performance result.

Matched512–1024ns metrics cover all four legs' input/MID/PRE/output ranges, mean,
rising counts, maximum intervals and intervals over800ps; baseband differential
mean, peak-to-peak and19.53125MHz fundamental use identical code for both runs.
No automatic topology promotion. Global ranges do not prove per-cycle swing or
duty; promising results require cycle-local analysis and power measurements,
followed by autonomous feedback validation. Source currents were not saved.
Command from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_lo_second_stage.py`.
Evidence:`evidence/lo-second-stage.json` pending. No production circuit changed.

## Pass 515 — candidate measurement path checked against completed baseline

Previous pass prepared the pending checker. Candidate3966322 revalidated live;
no restart. Added --baseline-check to check_lo_second_stage.py and executed it
on hashed completed replay. All16 measured node ranges, marker rising counts,
long-interval counts and maximum intervals exactly match the existing stage
analyzer report. This exercises waveform loading, completion/grid checks and
measurement code before candidate completion. Separate baseline-check report
prevents representing it as a finished candidate result.

Reproduce from repository root:
`python3 projects/programmable_transceiver_platform/verification/check_lo_second_stage.py --baseline-check`.
Evidence:`evidence/lo-second-stage-baseline-check.json`. This cross-check does not
prove physical marker validity, noise accuracy or candidate improvement. Full
candidate terminal branch remains pending. Preserve session24593 and use default
checker on completion; no topology adopted.

## Pass 516 — OpenSERDES circuit review narrows reuse claims

Previous user-requested survey added reference candidates. Revalidated running
LO candidate3966322 near479ns; no restart. Inspected actual OpenSERDES files at
pinned commit a0b98f0d9fd9937a479d5e39a194a26b8d6dfb14. Feedback is two
series diode-connected PMOS, requiring signed-I–V and dynamic validation before
GF180 reuse. CDR PDF shows50kHz example, blank maximum-frequency field and lists
PLL-based MM-CDR as future work. Thus the paper's2Gb/s link simulation does not
establish this uploaded CDR's high-speed autonomous recovery. SPICE/CDL also has
primitive placeholders/tool-specific dependencies. No circuit imported or adopted.
Details and source links:spec/openserdes-circuit-review.md; survey amended.

## Pass 517 — reconcile ADC and reference inventory with terminal evidence

Previous turn was a verified wait; LO candidate PID3966322 remains live near589ns.
Corrected stale pending inventory fields using completed separated-event, in-band
and hybrid-output2 reports. Ideal separated controls complete610ns but do not
implement physical phase generation. Stronger reference output improves target
error while worsening decision-window motion and power; no adoption. Added the
missing terminal evidence links. Source inventory audit still reports all ten
block obligations incomplete; layout gate remains closed. No circuit changed.

## Pass 518 — stronger second LO inverter worsens loaded replay

Previous turn was a verified wait. Original PID3966322 completed normally with
2561138 rows; no restart. Executed check_lo_second_stage.py successfully:
artifact/dependency hashes, full1024ns completion, finite strict time grid and
matched baseline/candidate metrics pass. Evidence:evidence/lo-second-stage.json.
Over512–1024ns, doubling second inverter multiplicity reduces I positive/negative
rising counts1170/1160 to1090/1080. Maximum gaps worsen3.239/3.215ns to5.628/5.610ns;
19 intervals over800ps remain on each I output. Q retains1280 rises per leg but
its global voltage swing shrinks. I baseband reference fundamental increases
31.613 to33.925mV peak and peak-to-peak disturbance71.319 to79.904mV.
Reject this candidate as a repair; production cell remains unchanged. Stronger
second-stage drive also increases first-stage loading, so this does not isolate
which mechanism dominates. Next diagnosis should compare internal MID/PRE bias
and per-cycle swing to separate extra input loading from output-drive effects
before another topology change. Autonomous loop, noise, power and physical
variation remain unqualified; replay is scoped diagnostic evidence only.

## Pass 519 — localize stronger-stage degradation upstream

Previous pass completed and rejected stronger second inverter. Extended existing
cycle-local analyzer with --second-stage, retaining baseline behavior and using a
separate report. Candidate result hash and waveform hash verified; triangle
integration self-check passes. Executed analysis successfully; evidence:
evidence/lo-second-stage-gap-state.json. Q provides a useful matched-cycle control:
1279 complete cycles in both histories, input mean cycle swing214.682->215.161mV
(QP), while MID falls255.611->114.623mV, PRE927.126->648.995mV and
output2610.443->2145.764mV. QN behaves similarly. Thus degradation is already
visible at the first inverter output, consistent with increased second-stage
input loading; stronger output drive did not compensate. This is not unique
causal proof: nonlinear feedback/Miller effects also change. I inside-gap cycles
increase108/117 to188/197; these group memberships differ across runs and must
not be treated as identical-cycle comparisons. Next controlled candidate should
reduce second-stage fanout burden, with all four legs matched, rather than assume
more drive fixes the path. No topology adopted or production source changed.

## Pass 520 — launch lower second-stage loading experiment

Previous pass localized stronger-stage swing degradation at MID. Prepared the
opposite controlled sizing change: second inverter XP2/XN2 m=4*S to2*S on all
four legs, first inverter and receiver unchanged. Extended existing preparer,
runner and checker with an explicit half variant; original doubled artifacts
remain separate. Preparation source hashes and exact deck reversal pass; checker
reports pending, and shell syntax check passes. Launched full1024ns replay using
unchanged input history,2ps maxstep, solver settings and initial conditions.
Session56764; scratch/transceiver-lo-second-stage-half. Evaluate using
`python3 projects/programmable_transceiver_platform/verification/check_lo_second_stage.py --half`.
Compare all four output counts/gaps, internal swing and baseband spur; weaker
output drive may offset reduced input loading. No adoption from isolated swing
improvement. Replay lacks autonomous feedback and saved supply currents; any
promising candidate requires those follow-ups. Production cell unchanged.

## Pass 521 — reassess whole-chip priorities after LO sizing evidence

Reviewed completed stronger-stage failure and cycle-local localization, corrected
ADC/reference inventory, and current risk register. Updated spec/risk-priorities.md
to keep loaded LO repair immediate while preserving autonomous noise/startup, RF
conversion, wired recovery and whole-chip coexistence obligations. Reduced-stage
runner4039372 verified live near16ns; session56764 preserved. No circuit adopted.

## Pass 522 — first fast architecture-screen increment

User approved faster hierarchical modeling while retaining transistor-before-layout
requirements. Added system_model/fast_screen.py: executable complex-envelope RX
chain with bandwidth, LO phase sensitivity, reference scaling, quantization and
clipping.72 hypothetical scenarios execute in0.134s. Filter and quantizer controls
pass. Evidence: evidence/fast-system-screen.json, explicitly unqualified with
missing whole-chip blocks listed. This is the first increment, not the completed
whole-chip behavioral model. Updated model README with integration sequence and
parameter-provenance requirements. Existing reduced-stage transistor run preserved.

## Pass 523 — connect signed reference charge to fast conversion model

Added linear reservoir/recovery dynamics and decision delay to fast_screen.py.
24 signed-charge/capacitance/resistance/delay cases supplement72 original RX
cases; all96 execute in0.203s. Closed-form impulse, zero-load and signed-symmetry
controls pass. Report distinguishes hypothetical charge proxy from actual SAR
switching; differential equivalent omits separate rails and amplifier dynamics.
No feasibility gate changed. Existing transistor runner4039372 verified live
near780ns; preserved. Next expand missing whole-chip paths and calibrate models
against circuit evidence rather than treating hypothetical sweeps as qualification.

## Pass 524 — fast TX/DAC-to-RX envelope loopback

Added54 loopback sensitivity cases to fast_screen.py, connecting DAC,
reconstruction filter, I/Q imbalance and radial compression through an explicit
external attenuator to RX filter and ADC.150 total cases run in0.373s. Analytic
identity/bound/phase controls pass alongside earlier filter/quantizer/reference
checks. EVM removes only one complex gain; converter clipping is separately
reported. Hypothetical envelope behavior is not transistor calibration, modem
compliance, PA impedance or spectral-image evidence. Missing whole-chip paths
remain explicit. Reduced-stage transistor runner4039372 live near797ns; preserved.

## Pass 525 — event-sampled wired channel sensitivity

Added system_model/wired_screen.py: exact one-pole NRZ channel evolution at
sample events,108 scenarios covering1.25/2.5Gb/s, bandwidth, signed timing offset,
signed100ppm period error and jitter. Executed in0.283s;84 scenarios show errors.
Closed-form step and polarity checks plus ideal-timing control pass. CDR absent
and explicitly labeled; finite zero-error records do not establish BER. This
expands the fast architecture toolkit without claiming a connected whole-chip
model. Reduced-stage transistor runner4039372 remains live near815ns, unchanged.

## Pass 526 — reconcile fast RF assumptions with host contract

Added and executed system_model/host_screen.py using hashed contract.json:
16 per-direction/mode cases, aggregate and integer-slot arithmetic, zero-load
and overload controls. Existing declared modes fit arithmetic only. Found a
whole-chip incompatibility in naive combination of separate screens:40MS/s8-bit
I/Q plus2.5Gb/s raw wire exceeds ddr156 payload capacity by259.74Mb/s under
planning ppm assumptions. Declared PCIe/RF mode uses20MS/s; fast40MS/s scenario
must not imply simultaneous support. No scope reduction or electrical/CDC/FIFO
qualification. Next integrate rate-aware paths and queues rather than independent
passing block results. Evidence:evidence/fast-host-screen.json.

## Pass 527 — one mode contract for RF and host screening

Parameterized loopback sample rate and converter width; preserved default150-case
screen and reran controls successfully. Added mode_screen.py, deriving40MS/s12-bit
components and20MS/s8-bit components from contract I/Q payload rates. Executed six
amplitude/mode cases with matching per-direction host arithmetic. Hashes identify
all model inputs. Declared12-bit words are targets, not existing converter ENOB.
Important modeling limitation: sampled envelope filters need oversampled
convergence before comparing spectral performance across sample rates. Thus these
EVM values are diagnostic only; lower20MS/s EVM is not improved analog bandwidth.
No queues, CDR or shared-rail integration yet. Reduced-stage runner4039372 live
near849ns, preserved. Evidence:evidence/fast-mode-screen.json.

## Pass 528 — resolve DAC holds and test envelope time refinement

Loopback now holds DAC codes on an oversampled grid, propagates TX/RX filters
and compression there, and samples ADC at the same end-of-hold phase for every
refinement. Default16x; explicit1/8/16/32/64x study executed across both mode
rates and three amplitudes. Earlier coarse EVM was materially optimistic:40MS/s
0.4V case0.06574 at1x versus0.11403 at64x. Largest32->64 absolute EVM difference
0.00156 in tested cases. No numerical acceptance threshold asserted; quantized
low-amplitude cases change discontinuously.150-case controls and six mode cases
rerun successfully. Evidence:fast-envelope-convergence.json plus regenerated
fast-system-screen.json and fast-mode-screen.json. Whole-chip model still lacks
CDR, queues and coupled supplies. Transistor runner4039372 live near868ns, retained.

## Pass 529 — independent continuous-time filter control

Added check_filter_analytic.py: exact two-state held-input recurrence for a linear
RC cascade, including equal-pole limit. Checked against closed-form continuous
step responses at20/40MS/s with12/20MHz and12/12MHz poles. All four controls pass.
Independent complex random held sequences show monotonically decreasing RMS
error for existing oversampled cascade at1/8/16/32/64x. Evidence:
evidence/fast-filter-analytic.json. This strengthens numerical validation without
claiming nonlinear-loopback convergence, physical transfer accuracy or default16x
sufficiency. Reduced-stage transistor runner4039372 live near885ns, preserved.

## Pass 530 — finite ingress queues under actual v2 service slots

Inspected existing transport model: its receiver uses legacy frame-end CRC
release, so did not reuse that behavior as v2 evidence. Reused stream_codec.slots
for actual v2 plans and added queue_screen.py: burst arrivals, frame snapshots,
two staging banks and assigned-slot service. Sixteen cases (two modes, two
sources, four phases),512 frames, fast-source/slow-host planning offsets complete
in~0.4s. Every event satisfies bit conservation; independent simple-burst control
passes. Maximum ingress520bits, below planning1024bits, not verified RTL depth.
Receiver egress, payload sequence, CDC, stalls and electrical timing remain absent.
Evidence:evidence/fast-queue-screen.json. This is finite ingress evidence only.
Existing transistor runner4039372 verified live near900ns; unchanged.

## Pass 531 — first shared-supply-to-RX coupling sensitivity

Added12 fast supply cases: explicit activity current through a first-order shared
impedance changes oscillator phase, RX gain and ADC span simultaneously. Sweep
zero/1/5ohm and both signs of VCO/gain sensitivity. Zero-impedance controls exactly
match uncoupled RX EVM.162 cases execute in2.746s. All coupling coefficients and
activity are hypothetical; oscillator phase integrates voltage-induced frequency
error without PLL correction, including DC offset. This is a deliberately exposed
missing feedback dependency, not autonomous PLL performance or physical PDN fit.
Host queue/wired/TX activity is not yet the current source. Whole-chip reassessment:
fast models now reveal transport and numerical constraints but do not retire
noise/startup, RF conversion, CDR or package risks. Next couple actual modeled
activity and feedback rather than infer feasibility from separate sweeps.
Transistor runner4039372 verified live near917ns, retained.

## Pass 532 — explicit linear phase feedback sensitivity

Added exact held-disturbance update for type-I phase feedback in fast_screen.py.
Eight signed VCO-sensitivity/correction-bandwidth cases supplement free-running
supply tests;170 cases execute in2.789s. Constant-frequency disturbance solution,
signed symmetry and free-running integration controls pass. This hypothetical
linear loop has finite static phase offset; it is not our type-II charge-pump PLL,
acquisition, saturation, divider dynamics or calibrated noise. Use it to expose
feedback-bandwidth sensitivity, not to claim autonomous timing qualification.
Existing transistor runner4039372 verified live near935ns; preserved unchanged.

## Pass 533 — host slot activity coupled into RF supply sensitivity

Added host_supply_screen.py using actual v2 slots and contract word/sample rates.
Binned weighted transfer events preserve total assumed charge; current excites
supply impedance, oscillator phase feedback, RX gain and reference span together.
Twelve cases execute; charge conservation and zero-load equivalence controls pass.
This is full-slot activity, not queue/payload-transition-derived current, and
sub-sample edges are discarded. Per-transfer charge and coefficients hypothetical.
RF converter remains8-bit sensitivity even for12-bit transport mode, explicitly
flagged; not a mode-compliance model. Evidence:fast-host-supply.json. Existing
transistor runner4039372 verified live near949ns, preserved unchanged.

## Pass 534 — remove converter-width mismatch in coupled modes

Parameterized RX fast-screen converter width and report sample rate/width per
case. host_supply_screen.py now derives both from contract I/Q words, asserting
rate consistency. Reran12 coupled cases with charge conservation and exact
zero-load comparisons at matching resolution;170 default cases and controls also
pass.12-bit is still a design target, not converter hardware or ENOB evidence.
Reports regenerated; prior pass5338-bit mismatch is superseded. Transistor
runner4039372 verified live near965ns, retained unchanged.

## Pass 535 — reassess fast-model coverage and physical risks

Updated risk-priorities.md after fast-model increments. Architecture screens
expose host-rate and numerical-resolution mistakes but do not retire autonomous
clock/CDR, conversion quality or coexistence risks. Prioritize completed LO
result, observable-signal behavioral recovery and transistor calibration rather
than more uncalibrated sweeps. Runner4039372 verified live near975ns, retained.

## Pass 536 — reduced second-stage loading helps modestly, does not repair LO

Original runner4039372 completed1024ns/2561138rows normally. Executed
check_lo_second_stage.py --half; artifact/dependency hashes, time-grid and full
completion checks pass. Evidence:lo-second-stage-half.json. I rises improve
1170/1160->1190/1180, still below1280; both I legs retain19 gaps over800ps.
Maximum gaps3.239/3.215ns->2.415/2.839ns. Q retains1280 rises per leg. I reference
fundamental31.613->30.100mV peak; I disturbance71.319->66.680mVpp. This is modest
improvement, not useful autonomous RX qualification. No topology promoted.
Together with doubled-stage degradation, results support loading sensitivity
but do not solve periodic bias/swing disturbance. Avoid an unbounded sizing sweep:
next compare this failure mechanism to a fast characterized LO model or short
validated replay, then consider gain distribution/bias topology. Full-loop noise,
startup, power and conversion requirements remain open. Production cell unchanged.

## Pass 537 — measured LO disturbance enters fast conversion sensitivity

Added measured_lo_screen.py using hashed completed baseline/doubled/halved
zero-RF replay summaries. Forty-eight ideal-signal/ADC scenarios sweep20/40MS/s,
8/250mV complex signal peak and unknown relative spur phase. Fundamental19.53125MHz
aliases to468.75kHz at20MS/s (explicit check). Even halved-stage disturbance yields
DC-removed error ratio~2.67 for8mV signal,~0.0855 for250mV signal under assumed
linear addition and ideal8-bit ADC. These are sensitivity ratios, not measured
receiver EVM: RF-driven superposition, harmonics and sampling physics unverified.
This scoped import shows modest pulse improvements leave a substantial conversion
problem. Evidence:fast-measured-lo.json. No topology adopted or long run launched.

## Pass 538 — receiver-observable transition clock tracking

Added clock_tracking_screen.py: received one-pole-channel zero crossings drive
PI period/phase correction with hypothetical+/-0.5% period bounds. Transmitted
bits/timing labels are not passed into feedback.72 cases cover three patterns,
three bandwidths, signed initial phase/frequency error and16/32 samples/UI.
Crossing and ideal-tracking controls pass; execution~1.3s. This is transition
tracking, not complete CDR: data decisions/BER, real detector limits, noise and
oscillator dynamics remain absent. Nearest-cycle association can slip/false-lock;
small detector residual does not prove correct recovery. Evidence:
evidence/fast-clock-tracking.json. No schematic or layout qualification.

## Pass 539 — independent cycle-count scoring for timing tracker

Tracker now returns its own cumulative cycle index. Separate evaluation-only
scorer attributes observed crossings to launched transitions and compares cycle
increments; no transmitted data enters feedback. Known artificial skipped-cycle
control is detected; ideal tracker count control passes. All72 current cases
rerun with zero cycle-count disagreements. This finite result does not establish
acquisition or BER; constant initial cycle offset is intentionally excluded,
and attribution assumes monotone one-pole step behavior. Missing/repeated
transition count is reported separately. Next recovered-data sampling must use
causal clock predictions, not post-corrected edges or transmitted sample timing.
Evidence: regenerated fast-clock-tracking.json. No transistor qualification.

## Pass 540 — causal recovered-data sampling reveals missed failures

Tracker emits predicted midpoint samples before each received transition updates
its state. Past samples cannot be relocated; corrections scheduling past samples
are omitted and counted via clock indices. Ideal midpoint and future-input-prefix
invariance controls pass. All72 cases rerun:24 have late data errors despite zero
cycle-increment disagreements in pass539; worst7168 late errors. Scoring uses
fixed source indices without posthoc alignment, so constant latency/phase ambiguity
can contribute and must be separated from distortion before claiming BER.
Finite data scoring, not lock/protocol qualification. This demonstrates why low
detector residual and correct cycle increments alone were insufficient. Evidence:
regenerated fast-clock-tracking.json. No transistor change or long simulation.

## Pass 541 — frozen training alignment and repeatable fast suite

Clock scoring now selects a fixed shift using indices 256–1023 and freezes it
for held-out samples at indices >=1024. Transmitted bits remain outside the
tracker. All 72 cases have zero held-out errors after this alignment; 24 cases
have ambiguous training shifts (periodic patterns). The previous fixed-index
errors therefore do not establish recovery failure in these cases. A synthetic
shift control and a single deliberately flipped held-out bit verify scoring.
This does not establish BER, hardware acquisition, noise tolerance or word framing.

Added system_model/run_fast_suite.py to rerun ten inexpensive model/control
scripts and record execution status, timing and source hashes in
 evidence/fast-suite.json. Execution success is distinct from meeting requirements.
The suite combines partially connected RF, clock, transport and supply screens;
it is not yet a fully connected or transistor-calibrated whole-chip simulator.

## Pass 542 — preserve measured LO distortion beyond the fundamental

Extracted the baseline zero-RF replay's differential I/Q waveform after verifying
its full source hash against completed replay evidence. The compact finite trace
covers 513–1023ns at 50ps spacing, retaining relative I/Q phase and waveform
structure. Independent 100ps/50ps sampling gives AC RMS 23.67288/23.67271mV and
residual RMS 7.624194/7.624191mV after least-squares DC/fundamental removal.
A synthetic complex coefficient recovery control passes. The remaining distortion
is substantial; a single sinusoidal-spur model omits relevant conversion stress.
This fit is a diagnostic, not proposed cancellation. No periodic extension is
justified by this finite record. Interpolation is not an anti-alias filter, and
neither resolution establishes physical ADC aperture behavior. The trace remains
specific to seeded zero-input replay; RF-driven superposition is unverified.
Evidence: evidence/lo-disturbance-trace.json and lo-disturbance-trace.npz.
Next use bounded-time trace sampling to test conversion sensitivity, then target
the actual LO bias/gain-distribution failure. No cell change or gate completion.

## Pass 543 — sample the finite LO trace through ideal conversion

Added trace_conversion_screen.py with trace hash verification, interpolation
control and rejection of out-of-range sampling. 128 scenarios compare the full
recorded I/Q disturbance against a DC/fundamental least-squares representation
with measured phase, and against quantization alone. Both 20/40MS/s rates sweep
32 sampling phases and hypothetical 8/250mV signals through an ideal 8-bit ADC.
At 20MS/s and 250mV, DC-removed error ratio spans 0.01075–0.09724 for the full
trace versus 0.01081–0.05314 for the fundamental approximation. At 40MS/s it spans
0.06948–0.11319 versus 0.04970–0.11778. The simpler model is not consistently
conservative: waveform structure and sampling phase change the result.

Only 10/20 samples exist per phase. These are sensitivity ratios, not receiver
EVM, statistical bounds or acceptable operating points. No trace tiling, physical
ADC aperture or demonstrated RF-driven superposition. Added the screen to the
repeatable suite. Evidence: evidence/fast-trace-conversion.json. This reinforces
prioritizing actual LO bias/swing repair over accepting the small sizing benefit;
no production cell changed and all physical qualification gates remain open.

## Pass 544 — loading improvement does not remove cycle-average movement

Extended the native-grid gap analyzer to the completed halved-second-stage
experiment; source waveform and completed-result hashes are verified. Its existing
analytic duration control passes. I+ cycles wholly inside gaps decrease from108
to88 (I−117 to97); group membership differs, so this is not a matched-cycle test.
I+ MID mean cycle swing inside gaps rises223.61→441.70mV, while PRE cycle means
still span0.9514–1.8547V. Q+ has no gaps and its MID swing rises255.61→511.77mV.
The observed improvement does not repair bias/swing disturbance. Reusable
compare_lo_gap_variants.py records all three variants, with cohort limitations.
Evidence: lo-second-stage-half-gap-state.json and lo-gap-variant-comparison.json.

Next targeted circuit experiment: vary the existing100k self-bias feedback
resistance in both directions with original transistor sizes. Stronger feedback
may reduce slow bias movement but attenuate useful RF; weaker feedback may do
the opposite. Measure both effects, output edge retention and baseband disturbance
before accepting any change. This is a hypothesis test, not proof of instability
or an adopted repair. Existing replay omits autonomous feedback and supply-current
measurement; promising results still require full-loop verification.

## Pass 545 — matched self-bias feedback experiments prepared and started

Prepared30k and300k feedback variants against the original100k loaded replay.
Exact reversal checks verify that only the four self-bias resistor values change;
transistor sizes, receiver loads, recorded ring input and simulation duration remain
identical. Baseline artifacts and diagnostic-scope approval are hash checked.
Started the30k candidate using the pinned simulator and existing recursive source
provenance checks;300k is prepared but not launched. No result or improvement is
claimed while the first run is live. Runner refuses to overwrite an output directory.

Hypothesis: stronger feedback may reduce slow bias displacement while worsening
signal loading; weaker feedback may reverse that tradeoff. Compare stage swing,
cycle-average motion, edge retention and baseband disturbance together. The same
512–1024ns observation window does not establish steady state for changed RC
constants. Replay still excludes autonomous backloading, cold startup and supply
current. No production cell changed. Artifacts: scratch/transceiver-lo-feedback-
{30k,300k}-prepared; live output scratch/transceiver-lo-feedback-30k.

## Pass 546 — completion checks ready for both feedback directions

Extended the existing loaded-replay checker and cycle-local gap analyzer with
--feedback30k/300k selection (CLI uses `--feedback 30k` or `--feedback 300k`).
The checker independently reverses all four resistor edits against the baseline,
checks prepared hashes, and requires complete artifact coverage for successful
terminal runs. It retains source immutability, log-error, final-time and native-grid
checks before recording completed metrics. Pending reports explicitly retain
completed=false and adopted=false; neither run preparation nor execution qualifies
a circuit.30k is confirmed live;300k remains prepared and unlaunched. Existing
baseline metric cross-check is rerun to guard the shared analysis path. The gap
analyzer cannot operate until the corresponding completion report succeeds.

## Pass 547 — both feedback directions live; whole-chip priorities refreshed

Confirmed30k remains live and advancing. With111GiB available memory and79GiB
free disk, started the prepared300k variant under its separate2CPU/4GiB limit;
both simulator containers are confirmed running. No restart or candidate adoption.
Updated the current risk decision point to reflect completed reduced-stage and
fast-model evidence, replacing stale claims that these tasks were still pending.
Keep this LO hypothesis test bounded: evaluate both results before another circuit
edit. Independent next calibration priority is actual two-rail ADC reference
motion at converter decision times. Physical noise/startup, wired CDR, host timing
and package/coexistence remain unresolved; no schematic obligation is closed.

## Pass 548 — measured two-rail trajectories at ADC decision windows

Added system_model/reference_window_trace.py. It verifies both completed nominal
reservoir waveforms against existing evidence hashes and extracts48 native-grid
pre-evaluation windows with interpolated exact boundaries. Retains VH and VL
trajectories, time-weighted means, extrema, span and common-mode motion. A clipped
nonuniform-grid analytic ramp control verifies the integration. Maximum motion
across these windows: VH1.994mV, VL4.110mV, span5.332mV, common mode1.517mV.
These maxima need not occur in the same window. Existing decision-window scripts
use sample-count means; this new artifact explicitly avoids that adaptive-grid
weighting bias without rewriting historical evidence.

Evidence: reference-window-traces.json/.npz. This is the nominal single-converter
reservoir fixture, not simultaneous I/Q or a calibrated predictive reference
model. Pre-evaluation windows are bench timing, not measured comparator aperture.
Next connect actual code/load history before inferring conversion error or reuse
under changed inputs. Both LO feedback containers are confirmed live; no results
are claimed or runs restarted. No schematic gate advanced.

## Pass 549 — code-history-aware reference sensitivity checked against residues

Added cdac_charge_screen.py using the actual complementary binary CDAC topology:
ideal weighted differential bottom voltage is(2*code-255)*(VH-VL)/256 including
the shared-low dummy. Ideal MSB-step and common-mode translation controls pass.
Both waveform hashes are verified; recorded SD commands are rail-valid at the
sampled decision-window midpoints. Each frame uses its first measured residue as
an explicit anchor;42 later decisions are scored without a fitted gain.
Measured-rail prediction error is2.129mV RMS/2.690mV maximum, versus nominal-span
12.542mV RMS/30.268mV maximum. Reference history explains much of the discrepancy,
but remaining error is not qualified or uniquely attributed.

SD commands precede physical drivers; settled bottom plates, matched capacitors
and charge conservation are ideal assumptions. Switch injection, parasitics,
leakage, comparator kickback and actual common-mode sensitivity remain absent.
Measured code-dependent rails cannot be reused to predict arbitrary SAR inputs.
Evidence: fast-cdac-charge.json. Next examine residual versus physical switching
settling before treating this as a calibrated converter. Both LO runs confirmed
live; no production transistor or layout changed.

## Pass 550 — measured MSB settling does not explain all residue error

Extended the charge-conservation diagnostic to replace the ideal128/256 MSB
contribution with the recorded differential MSB bottom-plate voltage, retaining
the same measured first-decision anchor. Zero-error and known half-weight controls
pass. Original nominal-span and measured-rail metrics reproduce exactly. Maximum
MSB rail-following discrepancy is1.608mV across48 samples; this correction reduces
42-comparison residue RMS2.129→1.980mV and maximum2.690→2.522mV. Thus MSB following
alone is insufficient to explain the residual. Do not infer a specific physical
cause from this partial correction or compensate the ADC around it.

Lower-branch bottom plates are not saved in these waveforms. Future predictive
calibration needs their settling or a validated reduced switch/capacitor model,
plus charge injection/parasitics and comparator loading. Existing branch-probe
reproduction restrictions still apply; adding probes cannot silently change the
circuit and then be treated as baseline evidence. Updated fast-cdac-charge.json
retains provenance and explicit observation limits. Both LO runs remain live.

## Pass 551 — reduced residue RMS still predicts wrong decisions

Extended cdac_charge_screen.py with explicit0.2/0.275/0.35ns offsets within the
existing pre-evaluation window and independent residue-sign comparisons. At all
three offsets, each model (nominal span, measured rails, measured rails plus MSB
bottom plates) disagrees with measured residue sign in3 of42 unanchored samples.
The MSB-corrected RMS remains1.974–1.985mV, exceeding the smallest measured
residue magnitude0.716–0.859mV. A low aggregate RMS therefore does not qualify
this approximation to stand in for transistor-level SAR decisions.

No actual converter error count or BER follows: comparison is model versus
sampled analog residue, not a physical comparator simulation. Recorded rail/code
histories remain externally supplied and code-dependent. Do not feed this model
into a supposedly autonomous whole-chip ADC until decision-sensitive residuals
are explained. Evidence: fast-cdac-charge.json and its0.2ns/0.35ns companions.
Both LO experiments are confirmed live, with30k beyond177ns of1024ns; preserved
without restart. No circuit change or physical gate completion.

## Pass 552 — held-out charge-gain diagnostic identifies a plausible omission

Added cdac_gain_diagnostic.py: fit a single through-origin charge-transfer gain
using14 nonanchor comparisons from70ns frames; score28 comparisons from120/170ns
frames. Per-frame measured anchors are still required. Known synthetic gain
recovery control passes. At0.275ns offset, fitted gain0.995185 reduces held-out
RMS1.987→0.1975mV, maximum0.4719mV, with zero held-out residue-sign disagreements.
Endpoint offsets0.2/0.35ns give gain0.995188/0.995184 and RMS0.1976/0.1993mV.

If attenuation arose solely from extra top capacitance, it would correspond to
~0.484% of array capacitance. This is an equivalent parameter, not an identified
physical capacitance or proof of cause. All records were previously inspected;
frame-separated fitting is not blind validation and repeats the same nominal
input levels. Do not declare the ADC model calibrated across input/process/load.
Next seek independent capacitance/loading evidence or a new stimulus before adding
fit parameters. Retain code/rail/MSB observation requirements and transistor
verification. Evidence: fast-cdac-gain-diagnostic.json. Both LO runs confirmed live.

## Pass 553 — independent PDK loading supports charge attenuation

Added analog/adc/top_load.spice: actual reset comparator plus open sampling gate,
symmetric1.65V held/input bias and differential AC excitation, alongside one PDK
MIM unit. Pinned ngspice46 completes10 points1MHz–1GHz without warnings. Symmetric
currents agree; positive capacitance and waveform/deck checks pass. Measured
per-side loading58.512fF at1MHz and unit44.334fF imply gain0.994871 for256 units,
close to empirical0.995185. This is independent static-circuit support for omitted
loading, not a fit to converter residues. adc-top-load.json scores that physical
estimate against all42 nonanchor recorded residues and retains provenance.

This fixture does not reproduce evolving comparator internal voltages, switch
injection, code-dependent CDAC parasitics or extracted loading. No dynamic ADC
qualification follows. Preserve the physical loading term in subsequent reduced
models, then verify new inputs and actual timing rather than adding arbitrary fit
coefficients. Both LO simulations remain live; no production cell changed.

## Pass 554 — physical loading estimate checked across input bias

Added reproducible run_adc_top_load_bias.py: nine reset-comparator/open-sampler
PDK AC cases across common modes1.45/1.65/1.85V and differential inputs−0.4/0/+0.4V.
All complete without warnings. The symmetric center reproduces prior gain within
1e−10 and reversing differential bias swaps port loading within1ppm. At1MHz,
per-port effective capacitance spans58.200–58.817fF, a modest variation around the
58.512fF center. This supports using a bias-sensitive loading term rather than
relying solely on the fitted charge gain, within this static fixture.

Differential-drive port susceptance includes coupling and is not a complete
capacitance matrix. Neither these selected voltages nor reset-state AC results
bound dynamic comparator behavior, process variation or extracted parasitics.
Evidence: adc-top-load-bias.json with per-case deck/log/data hashes and pinned
simulator image. Both LO feedback simulations confirmed running; no restart,
production circuit change or qualification gate advancement.

## Pass 555 — independent input drives resolve top-plate coupling

Added run_adc_top_load_matrix.py with separate positive/negative AC excitations
of the actual reset-comparator/open-sampler fixture. Both simulations complete
without warnings; local circuit sources are hash checked before/after. The full
complex two-port admittance reconstructs the previous differential-drive currents
within the declared1ppm/0.1pA numerical tolerance (maximum absolute discrepancy
0.2pA across the frequency sweep). At1MHz the effective capacitance matrix is
[[58.0330,-0.4787],[-0.4787,58.0330]]fF, explaining differential per-side58.5117fF.

Evidence: adc-top-load-matrix.json retains frequency-dependent conductance and
susceptance-derived capacitance, source/artifact hashes and pinned image. Do not
replace this active network with a frequency-independent passive matrix without
checking its intended domain. Reset-state cross-coupling is small but nonzero;
evaluation-state and clock-driven charge, process and layout remain absent.
This improves the reduced-model boundary without qualifying conversion. Both
LO feedback containers remain live and unchanged.

## Pass 556 — comparator clock charge requires transient accounting

Eight short PDK transients compare clocked versus reset-only actual comparator
and open sampler at differential±1mV/±400mV, with ideal clamped input voltages.
All complete without warnings. Artifact hashes, correct resolved output polarity,
signed-charge symmetry and independent integral checks pass. Near±1mV, net
differential source charge is only±0.00922fC, but maximum cumulative excursion
is1.786fC. At±400mV these become±3.742fC and6.563fC. Thus final net charge hides
transient cancellation; retain time history in any reduced clock-coupling model.

Evidence: adc-kickback-clamped.json and adc-kickback-excursion.json. Ideal voltage
clamps alter feedback relative to floating CDAC plates, so these charges are not
observed ADC voltage errors. Static loading alone cannot establish clock-driven
behavior. Clock slew, timestep refinement, bias/process spread and actual floating
input response remain to be checked. No production circuit or qualification gate
changed. Scripts: run_adc_kickback.py and check_adc_kickback.py.

## Pass 557 — timestep refinement checks clock-charge cancellation

Repeated all eight clamped-input clock/reset tests at1ps maximum step versus2ps,
using separate immutable output directories. Both runs pass artifact, polarity
and charge-integral checks. compare_adc_kickback_resolution.py records absolute
changes in signed net charge and maximum cumulative excursion. This is a
numerical diagnostic across two resolutions, not a full convergence proof or ADC
acceptance. Very small net charge is compared in absolute units rather than using
large relative percentages to obscure its scale. Evidence: adc-kickback-fine-
clamped.json, adc-kickback-fine-excursion.json and adc-kickback-resolution.json.
Both longer LO containers remain confirmed live; no restart or topology adoption.

## Pass 558 — clock charge tested on floating PDK capacitor loads

Four1ps-step transistor transients use the actual sampler and comparator with
256 grounded PDK MIM units per held side, releasing sampling before evaluation.
Paired clocked/reset-only cases at±1mV complete without warnings and preserve
correct comparator polarity. Held differential before evaluation is±1.00589mV.
Clock-induced peak differential disturbance is0.15645mV and common-mode motion
2.31978mV relative to the reset-only trace, symmetric under input reversal.
This directly observes floating-node voltage response instead of equating net
clamped-source charge with ADC error. Evidence: adc-floating-kickback.json.

The load is an explicit capacitor approximation, not the switched CDAC: reference
impedance, bottom-plate transitions, branch parasitics and full SAR history remain
absent. Two nominal inputs and a single slew do not qualify noise, offset, timing
or conversion. Next compare the charge-driven reduced prediction against these
waveforms before extending it to the actual loaded array. No production cell or
schematic gate changed; run_adc_floating_kickback.py retains artifacts/provenance.

## Pass 559 — independent charge model reproduces floating-load clock motion

Added system_model/kickback_prediction.py. Hash-verified1ps clamped-source
currents, with reset-only subtraction, are integrated on a merged native time
grid and applied through the independently measured two-port capacitance matrix
plus256 PDK MIM units per side. No voltage gain or time shift is fitted. Compare
against the independent floating sampler/comparator clocked-minus-reset tests.
For both±1mV inputs, predicted differential peak156.586uV versus156.450uV observed;
maximum waveform error0.5223uV and time-weighted RMS0.1038uV. Common-mode peak
2.319661 versus2.319780mV; maximum error1.1596uV, RMS0.2744uV.

Evidence: fast-kickback-prediction.json. This scoped result supports charge-history
plus loading-matrix reduction at this boundary, not a complete converter model.
Constant reset-state loading, omitted leakage, slightly different initial bias,
grounded capacitor loads and selected nominal stimuli limit transfer. Next
integration must retain actual switched CDAC/reference dynamics; do not stitch
this result to code-dependent reference traces and claim autonomous ADC behavior.
Both LO feedback containers confirmed live; no production circuit changed.

## Pass 560 — actual CDAC topology changes comparator clock disturbance

Extended run_adc_floating_kickback.py with --cdac. Four matched1ps-step runs
replace the grounded capacitor bank with the actual GF180 switched MIM CDAC at
fixed code127, keeping ideal2.15/1.15V rails and the same sampler/comparator.
All complete without warnings, circuit sources remain unchanged, and both±1mV
inputs resolve correctly. Differential peak rises0.15645→0.22171mV and common-mode
peak2.31978→2.67338mV versus grounded capacitors. Thus equal nominal capacitance
is insufficient to reproduce dynamic loading through the CDAC switch topology.

Evidence: adc-cdac-kickback.json with per-case artifacts and source provenance.
Do not transfer the excellent pass559 reduced-model agreement to this different
boundary. Next model/check actual bottom-plate settling and selected-rail paths;
physical reference impedance and changing SAR code remain absent. The experiment
isolates topology but does not uniquely attribute the increase. LO feedback runs
confirmed live beyond430ns/402ns respectively; no restart or circuit adoption.

## Pass 561 — fixed-code CDAC impedance exposes switch-path dynamics

Measured independent-drive reset input admittance with actual fixed-code127 CDAC,
then subtracted the prior sampler/comparator-only matrix. Both AC runs complete
without warnings and retain before/after local-source hashes. Low-frequency array
response is approximately11.3494pF in series with5.0465ohm; effective susceptance
capacitance falls to9.8457pF at1GHz. This supports including finite switch-path
impedance rather than transferring a grounded-capacitor model unchanged.

The one-RC approximation uses only the first frequency; remaining-frequency
errors are explicit in fast-cdac-impedance.json. Actual topology evidence is in
adc-cdac-load-matrix.json. No switched-transient prediction is claimed: ideal
rails, fixed code, static bias and linearization omit actual SAR/reference
history. Test that reduced impedance against the clock transient before reusing
it for full converter timing or accuracy. No production circuit change.

## Pass 562 — low-frequency one-RC model fails the full clock waveform

Added cdac_kickback_prediction.py: measured clamped comparator currents drive a
four-state network comprising the reset input-capacitance matrix plus per-side
series RC inferred independently from fixed-code AC data. No transient amplitude
or timing fit. Differential predicted peak215.81uV versus221.71uV observed looks
close, but maximum differential waveform error34.47uV remains. Common-mode peak
3.0501 versus2.6734mV and maximum waveform error1.4962mV reject treating this as
an accurate clock-transient model. Peak agreement alone would hide the failure.

Initial0.1/0.2ps numerical refinement differed by76.7uV at ports. Refining to
0.01/0.02ps reduces maximum port difference to0.114uV while the physical-model
mismatch remains. This tests numerical sensitivity, not SPICE timestep convergence
for the new topology. Evidence: fast-cdac-kickback-prediction.json. Reset-state
linearization and low-frequency RC do not cover the clock edge's full bandwidth
or nonlinear charge response. Next characterize higher-frequency impedance and
actual bottom-plate response before adopting this reduced model. No gate advanced.

## Pass 563 — characterize the frequency range actually excited by clocking

Extended fixed-code CDAC matrix AC sampling to51 points1MHz–100GHz as a model
scope diagnostic. Simulation completes without warnings and source checks pass.
Constant reset-capacitance plus low-frequency one-RC matrix error is2.76% at1GHz,
57.0% at10GHz and96.3% at100GHz. Finite2–4.9ns clock-current spectral accounting
passes Parseval: differential/common-mode energy above1GHz is96.0%/84.3%, and
above10GHz0.655%/16.47%. Thus the earlier1GHz sweep omitted substantial excited
bandwidth, particularly for common-mode motion.

Evidence: adc-cdac-load-matrix-wide.json and fast-cdac-bandwidth.json. This does
not demonstrate actual100GHz device accuracy or assign voltage error directly
from current energy. Finite rectangular records have spectral leakage and~345MHz
resolution. Next evaluate the measured frequency-dependent impedance or a stable
broadband reduced network, with explicit numerical controls, against the existing
transient before changing the actual ADC topology. Both LO runs confirmed live.

## Pass 564 — passive broadband model passes withheld AC points

Added fit_cdac_admittance.py. Transform the measured fixed-code input admittance
into common/differential modes and fit nonnegative shunt G/C plus fixed-pole
series-RC branches using alternating frequency points only. Three active branches
per mode suffice: withheld relative admittance error is at most0.014983% across
1MHz–100GHz. Remaining modal cross-coupling is explicitly omitted and bounded in
these measurements at0.024404% of matrix norm. A series-RC impedance identity and
positive-real conductance control pass. No transient waveform enters fitting.

Positive element values produce a passive stable network, not proof of unique
physical decomposition or validity outside this bias/code/band. Frequency holdout
uses one nominal fixture, not independent silicon conditions. Next compare this
network with the existing fixed-code clock transient before adoption. Evidence:
fast-cdac-passive-fit.json retains coefficients, selected indices, errors and
provenance. Both LO runs confirmed live;30k beyond534ns. No physical gate advanced.

## Pass 565 — passive broadband model predicts the fixed-code clock transient

Added cdac_broadband_prediction.py using only the AC-fit passive modal networks
and independent clamped clock-current records. State-space impedance is checked
against each network's direct admittance at1MHz/1GHz/100GHz; zero-input control
passes. No transient gain/time adjustment. Maximum differential waveform error
falls from34.47 to1.518uV, common-mode error from1.496mV to14.34uV. Differential
RMS is0.228–0.256uV; common-mode RMS1.049–1.054uV. Refining0.02→0.01ps changes
port voltages by less than0.835nV, below these discrepancies.

Evidence: fast-cdac-broadband-prediction.json. This supports a scoped dynamic
reduction of the fixed-code127, ideal-rail CDAC boundary, not a full ADC model.
Unmodeled modal coupling, evolving comparator state and differing held initial
bias remain. Repeated nominal stimuli do not establish code/process/load transfer.
Next integrate changing code and physical reference response rather than further
optimizing this already-close fixed-code fit. No production circuit or physical
qualification gate changed; original full-chip scope remains open.

## Pass 566 — frozen broadband model fails code-independent transfer

Extended the actual-CDAC AC runner to explicit codes0/128/255, each with two
independent port drives and51 frequency points. All complete without warnings
and source checks pass. Evaluate the existing code127 passive network without
refitting: maximum full-matrix relative error is0.03579% for128 but8.7668% for
both0 and255, worst near6.31GHz. This includes code-dependent modal coupling;
the prior diagonal-modal fit is not code-independent despite its excellent
single-code AC/transient agreement.

Evidence: adc-cdac-load-matrix-wide-code{0,128,255}.json and
fast-cdac-code-transfer.json. Selected fixed codes do not qualify transitions,
reference regulation or complete SAR operation. Next retain code-dependent
loading/rail connectivity rather than applying one global clock-disturbance model.
Do not refit each case and call it predictive without verifying how model state
and charge transfer across code changes are preserved. No production cell changed.

## Pass 567 — binary rail weighting predicts intermediate fixed-code loads

Tested the topology-based hypothesis that binary groups scale switch multiplicity
and capacitor count together, making fixed-code admittance a rail-weighted sum.
Using only measured endpoint0/255 matrices, predict Y(code)=Y0+(code/255)*(Y255-Y0).
Independent code85/170 AC runs complete without warnings; compare also existing
127/128 data. Maximum matrix relative discrepancy across all four codes and51
frequencies is4.42e−9 (near stored numerical precision), with endpoint algebra
controls passing. This explains why one fixed matrix failed while permitting a
compact code-dependent static representation. No intermediate-code fitting.

Evidence: adc-cdac-load-matrix-wide-code85/170.json and
fast-cdac-rail-weighting.json. Parallel addition relies on ideal fixed rails and
bias; finite shared references introduce additional coupling. Crucially, switching
between endpoint matrices is not a transient model: charge/state continuity and
nonlinear switch injection still need explicit treatment. Next derive that state
transition from the actual capacitor connections. Both LO runs remain confirmed
live beyond594ns/568ns. No production cell or qualification gate changed.

## Pass 568 — explicit capacitor state survives code transitions

Added cdac_state_model.py, a finite-resistance ideal-capacitor scaffold retaining
all eight bottom voltages and floating-top charge across selected-rail changes.
Eliminate the floating top using charge conservation before matrix-exponential
integration; a full-state implementation showed numerical drift of the conserved
mode and was replaced. Eight forward/return transitions (including127↔128 and
0↔255) pass analytic settled-voltage, zero-time continuity, charge conservation,
return-state and split-time controls. Evidence: fast-cdac-state.json.

This is a mathematical building block, not transistor-calibrated switching:
unit resistance is a declared1300ohm assumption; ideal abrupt selection omits
injection, overlap/deadtime and rail dynamics. Constant top loading and fixed
ideal references do not replace the verified broadband clock boundary. Next
compare actual transition waveforms and add physical rail/switch behavior while
preserving state, rather than swapping unrelated fixed-code models. No gate open.

## Pass 569 — actual carry transition exposes large common-mode movement

Four matched transistor runs compare fixed code versus127→128→127 and
128→127→128, with the actual CDAC/sampler and reset comparator. Ideal rails and
simultaneous100ps complementary gate commands isolate the transition. All runs
complete without warnings and source/artifact checks pass. Settled differential
step is±7.77243mV, peak magnitude8.02527mV; final differential return error is zero
at saved precision. Peak common-mode motion is66.7635mV in either direction.

The ideal charge-state scaffold's endpoint agreement cannot represent this large
common-mode transient; do not equate charge conservation with accurate switching.
Gate overlap/deadtime, switching injection and selected-rail dynamics need explicit
characterization before the reduced model can predict actual conversion margins.
These are ideal commands, not physical drivers, and no reference regulation or
active comparator evaluation is included. Evidence: cdac-carry-transition.json.
No circuit adopted or full ADC claim; next examine timing sensitivity and real
reference response around this concrete transition rather than fitting endpoints.

## Pass 570 — carry settling separates differential and common-mode risk

Hash-verified matched carry waveforms now produce finite residual envelopes from
selected delays after the100ps gate ramp through4.9ns. For127→128, at200ps after
the ramp, differential residual relative to the4.9ns endpoint is at most1.605uV,
while common-mode motion reaches6.591mV. At500ps these fall to0.350uV/0.11248mV;
at1ns to~0.010uV/0.000095mV (near saved waveform precision). Reverse direction
is separately recorded in cdac-carry-settling.json. The66.8mV peak alone was
insufficient to infer decision failure; early common-mode recovery remains the
more relevant concern in this fixture.

These are finite-window diagnostics, not acceptance limits or infinite-time
settling proof. Comparator is reset; physical code-driver timing and actual
reference rails remain absent. Next check an evaluating comparator at relevant
delays with both polarities before assuming common-mode tolerance. Both LO runs
confirmed live;30k beyond679ns. No restart, production edit or gate advancement.

## Pass 571 — evaluate comparator during carry common-mode recovery

Six actual-CDAC/comparator transients test127→128 and128→127 with input offsets
chosen to leave approximately±1mV residue, enabling evaluation100/200/500ps after
the ideal gate ramp ends. All complete without warnings and circuit hashes remain
unchanged. Pre-clock residues are±1.02306/±0.96154/±0.96029mV respectively; all
resolve intended polarity. Output differential first exceeds the2.97V diagnostic
threshold about727.5ps after evaluation ramp starts. This is output crossing,
not a qualified latch deadline or metastability bound.

Evidence: cdac-carry-decision.json. The prior66.8mV common-mode spike does not
cause an immediate nominal decision failure in these selected cases. Do not infer
noise/offset tolerance: ideal references, ideal100ps gate/clock ramps, no mismatch,
no physical drivers and no closed SAR remain. Preserve both this favorable result
and the known reference regulation failures when choosing next integration work.
LO runs confirmed live beyond710ns/685ns. No production cell or gate changed.

## Pass 572 — real reference loading changes the carry decision margin

Two matched earliest-delay carry cases replace only ideal VH/VL sources with
the existing transistor reference pair,20uA ideal bias generators and2048-unit
PDK reservoirs per rail. Additional rail/current outputs are diagnostic. Both
8ns runs complete without warnings and source hashes remain unchanged; DC
operating-point initialization explicitly excludes cold startup. Decisions retain
correct polarity, but pre-clock residue falls from±1.02306mV with ideal rails to
±0.41177mV. Span over2–4.4ns ranges0.774701–1.034115V in both directions.

Evidence: cdac-carry-real-reference.json. Successful bits do not qualify reference
accuracy or margin. This is a concrete reference/CDAC/comparator integration
problem, consistent with earlier regulation failures; ideal-reference behavioral
models cannot predict it. Preserve the unchanged transistor reference baseline,
then localize timing and signed rail charge before selecting a topology change.
Targets, supply and control edges remain ideal, and this is one CDAC rather than
simultaneous I/Q. Both longer LO runs confirmed live; no qualification gate opened.

## Pass 573 — reference collapse begins before comparator evaluation

Hash-verified event timing separates precarry offset and switching excursion.
Both directions have VH2.163362V/VL1.139712V before the carry (span1.023650V).
Span reaches0.976343V at gate-ramp end2.1ns and0.808279V by comparator clock start
2.2ns; minimum0.774701V occurs2.3615ns. Thus most collapse is already present
before evaluation. Comparator clocking cannot be the sole cause. Initial target
offset and dynamic span loss remain distinct. Evidence: cdac-carry-reference-timing.json.

Next isolate CDAC switching/rail demand with comparator held reset and then
characterize signed load without assuming timing correlation identifies current
paths. Existing supply-current probe does not measure individual rail demand;
additional probes require reproduction checks. No topology remedy is selected
from timing alone. Both LO runs confirmed live;30k beyond765ns. No gate advanced.

## Pass 574 — reset comparator does not remove reference collapse

Two matched real-reference carry transients hold CLK low throughout. Both finish
without warnings; comparison verifies that clock source removal is the sole deck
change and checks every recorded artifact hash. Minimum reference span is0.774691V
with reset versus0.774701V with evaluation. Maximum span difference over2–4.4ns
is0.116885mV; pre-clock waveform difference1.485uV reflects finite numerical/grid
reproduction and is retained explicitly. The~249mV drop from precarry span persists
without comparator evaluation, localizing the dominant issue to carry/reference
interaction rather than comparator clocking.

Evidence: cdac-carry-real-reference-reset.json and cdac-carry-reset-comparison.json.
Raw reset-run target-polarity=false is inapplicable, not a failed decision: no
comparison is requested from a held-reset latch. Redistribution, gate overlap,
injection and driver response are still not separated; signed rail load requires
further evidence. LO runs remain live;30k beyond792ns. No production change.

## Pass 575 — verified probes expose bidirectional carry demand

Inserted zero-volt probes only between each reference rail and the CDAC, with
exact netlist reversal checked. Both runs complete without warnings. Over the
predeclared2–4.4ns scope, held-node differences versus unprobed baseline are at
most0.42uV and rail differences0.05uV, passing the predeclared10uV diagnostic
threshold. Only then analyze current. The carry draws13.1595pC net from VH and
returns13.5285pC to VL. Peaks are157.6mA drawn from VH and141.3mA returned to VL;
both rails also briefly see opposite-direction current. Directions reproduce
under carry reversal.

Evidence: carry-rail-probes.json and carry-rail-charge.json. These currents flow
between reservoirs/drivers and CDAC, not directly equal supply current or average
power. Finite2–4.4ns integration excludes complete recovery and does not identify
redistribution versus overlap/injection paths. Any reduced rail model must support
signed demand and both rail dynamics. No circuit remedy or qualification claimed.

## Pass 576 — reservoir charge explains the early rail excursion

Added analyze_carry_charge_balance.py using verified CDAC current probes and
independently measured44.334fF MIM units (2048units≈90.80pF per rail). Signed
load charge plus CΔV gives aggregate non-reservoir contribution. At2.3615ns,
near minimum span, VH delivers11.996pC to CDAC and loses11.404pC from reservoir,
leaving0.591pC inferred from other circuitry. VL receives12.044pC and stores
11.200pC in its reservoir, with0.843pC removed elsewhere. By4.4ns the inferred
contributions are+5.330/−11.826pC respectively, showing slower, asymmetric recovery.

Evidence: carry-charge-balance.json. This is an approximate diagnostic, not direct
output-device current or independent KCL proof: compensation/parasitic storage
and any MIM voltage dependence remain in the residual. The early excursion is
consistent with reservoir-dominated fast demand. Do not simply enlarge reservoirs
without area/settling review; compare reducing switching demand versus faster
bidirectional drive, preserving actual rail/headroom behavior. LO runs confirmed
live beyond834ns/810ns. No production circuit changed.

## Pass 577 — reassess whether the carry stress represents SAR operation

Audited hash-verified SD commands at48 decision-window points across six nominal
frames. No sampled127→128 transition occurs. The largest observed decision-to-
decision toggle is128→64 (weight192); the artificial127→128 stress toggles255
units for a net+1 code. Other sampled changes toggle96/64/etc units. Therefore
carry-induced~249mV span loss must not be called ordinary SAR error. It remains
an integration stress revealing bidirectional rail demand, not a measured normal
conversion trajectory. Existing complete-SAR regulation failures remain valid.

Evidence: sar-switching-activity-audit.json. Sampling does not capture every
trial/capture/track/reset edge; SD is upstream of physical drivers. Toggle weight
alone cannot scale charge/error linearly. Next use observed128→64 timing/history
and physical drivers before selecting a reference remedy. This corrects scope
without dismissing reference risk or redefining the chip goal. Both LO runs live.

## Pass 578 — observed128→64 demand persists with transistor code drivers

Two8ns cases use the observed command pair128→64→128, actual CDAC and reference
network, comparing ideal complementary gates with existing weight-scaled GF180
code drivers. Both complete without warnings and source checks pass. Minimum
span is0.857772V ideal versus0.855706V physical; net VH draw8.649 versus8.703pC,
VL return8.893 versus8.929pC in2–4.4ns. Thus the artificial carry overstated the
load, but substantial regulation loss remains with a more representative command
and actual driver paths. No simplistic scaling from toggle weight was used.

Evidence: observed-sar-transition.json. This uses a synthetic isolated command
history and prebiased reference, not a complete SAR replay; comparator stays
reset. Prior probe reproduction was a different transition and cannot establish
identical unprobed behavior here. Gate/rail trajectories are saved for timing
analysis. Next relate rail settling to actual trial/decision spacing, not just
minimum span. LO runs confirmed live beyond889ns/867ns. No circuit adopted.

## Pass 579 — actual controller skew and decision delay change fixture scope

Audited three saved128→64 transitions with hash verification and analytic
crossing control. SD7 falls327.573–327.612ps before SD6 rises; the next comparator
clock midpoint is2097.646–2097.941ps after SD6. Actual B7 fall is also recorded,
but B6 is not saved. Complete-SAR reference spans at these clock midpoints are
0.954078–0.962336V. This is stronger timing context than an isolated transient
minimum, and still shows regulation error at the decision.

Evidence: sar-transition-timing-audit.json. The pass578 simultaneous command
fixture is a stress case, not exact controller replay, despite physical drivers.
Do not equate its0.856V minimum with the actual SAR decision span. Future focused
replay must preserve measured command shapes/skew and prior rail/capacitor state.
50% crossings are markers rather than switch conduction or comparator aperture.
No regulation failure is retired and no production circuit changed.

## Pass 580 — prepare measured-command replay with original initialization

Prepared an80ns shortened original SAR baseline and a matched replay that feeds
all eight physical code drivers with native-grid recorded SD waveforms.16086
points per command preserve observed skew/shape through the first two decision
windows. Original circuit, sampler/input/clock history and DC initialization are
retained; exact reversal verifies only command boundary/output naming changes
between the shortened pair. Donor deck/waveform hashes are checked.

Artifacts: scratch/transceiver-sar-command-replay-prepared/{baseline.spice,
replay.spice,commands.spice,manifest.json}. No simulation or reproduction claim
is made yet. Qualification must first reproduce the donor with the shortened
baseline, then compare replay rails/held nodes/B7/decisions. Recorded commands
remove driver-input backloading and alter interpolation breakpoints; the first
saved value is extended to time zero. These are explicit possible mismatch
sources, not evidence of equivalent internal state. LO runs remain live,30k
beyond925ns. No production cell or gate changed.

## Pass 581 — matched shortened SAR baseline started

Started the prepared80ns original-command baseline using pinned ngspice46 with
recursive include/model hashes captured before and after execution, immutable
prepared artifact checks and fresh output directory. Simulation is confirmed live;
measured-command replay has not been launched. Runner preserves terminal errors,
timeouts and artifact hashes rather than assuming completion from a log file.

Artifacts: scratch/transceiver-sar-command-baseline; runner
verification/run_sar_command_replay.sh baseline. Before using replay, compare
shortened baseline against the209.9ns donor through the first two decisions.
Predeclare diagnostic reproduction goals: held-node/reference waveforms within
10uV over70–79ns, B7 crossing times within10ps, matching first two resolved
comparator polarities. Failure must remain explicit rather than loosening limits
after observation. These are fixture-reproduction limits, not ADC specifications.
Both LO feedback runs remain active; no restart or production circuit change.

## Pass 582 — implement predeclared SAR reproduction gate

Added check_sar_command_replay.py. It requires terminal success, unchanged
recursive sources, prepared-deck equality, artifact hashes, finite monotone
waveforms through80ns, then compares70–79ns held/reference waveforms, both B7
edge directions and resolved polarities at72.4/77.4ns against the donor. Limits
remain10uV/10ps as declared before completion. Replay mode additionally requires
the shortened baseline's successful reproduction report and matching waveform
hash. No pending/incomplete run can pass. Current baseline report is explicitly
pending, completed=false, reproduction_pass=false; container remains confirmed
live beyond22ns. Both LO containers also remain live. No restart or gate opening.

## Pass 583 — baseline reproduces donor; measured-command replay started

Shortened80ns SAR baseline terminates successfully with unchanged recursive
sources and artifact checks. Over70–79ns all four held/reference voltage errors
are zero at saved precision, B7 falling-edge displacement is zero, and both
resolved comparator polarities match. It passes the predeclared10uV/10ps gate;
this is fixture reproduction, not ADC accuracy. Started the prepared measured-
command replay in its own fresh directory with the same pinned runner. No replay
result is claimed while active. Evidence: sar-command-baseline.json.

Refreshed current whole-chip risk priorities: scoped fast-model success has not
retired autonomous timing/noise, full RF conversion, wired CDR, host electrical
or physical coexistence obligations. Reference replay must enable a concrete
circuit remedy rather than an endless sequence of increasingly fitted models.
Both LO experiments remain live near their full observation endpoint.

## Pass 584 — stronger LO self-bias feedback helps but does not repair gaps

The30k feedback replay terminates normally through1024ns. Prepared artifacts,
sole resistor substitution, recursive before/after sources, completed output
hashes and waveform checks pass. Matched512–1024ns I+ edges improve1170→1220,
I−1160→1210 versus1280 expected; both still have19 intervals above800ps. Longest
I+ gap3.239→1.643ns; I−3.215→2.026ns. Q outputs retain1280 edges and no long gaps.
I reference fundamental falls31.613→16.336mV peak and total p-p71.319→35.905mV;
Q fundamental2.824→2.383mV. This is meaningful feedback sensitivity, not a repair.

Evidence: lo-feedback-30k.json. Candidate remains unadopted: missing pulses and
substantial zero-input baseband disturbance persist. Complete the opposite300k
comparison before another LO edit, then examine cycle-local bias/swing and restore
autonomous feedback for any promising topology. Supply current and full-loop
noise/startup remain absent.300k and measured-command SAR replay remain live.

## Pass 585 — opposite feedback direction worsens LO failure

The300k replay completes1024ns and passes immutable-source/artifact/sole-change
checks. I+ edges fall1170→1130 and I−1160→1090; both retain19 long intervals.
Longest I+ gap4.022ns, I−5.212ns. I reference spur rises31.613→82.101mV peak,
p-p71.319→177.623mV; Q retains1280 edges but spur rises2.824→5.116mV. Reject300k.
30k remains unadopted despite improvement: cycle-local analysis shows I+ PRE
means still range1.149–1.781V inside gaps, versus1.018–1.824V baseline. Complete
ring cycles inside gaps decrease108→58 (cohort membership differs), not to zero.

Evidence: lo-feedback-300k.json and lo-feedback-30k-gap-state.json. Feedback
strength materially affects the disturbance, but does not identify a unique
cause or qualify a fix. Both requested directions are now complete. Next target
interstage bias/gain distribution or a circuit-level disturbance-rejection remedy,
with loading and autonomous-loop effects retained; do not broaden resistor sweeps
without a new mechanism. Supply current remains absent from replay. SAR command
replay continues; no production cell or qualification gate changed.

## Pass 586 — local interstage isolation targets low-frequency amplification

After both resistor variants failed to repair LO gaps, tested a concrete topology:
four-unit PDK MIM coupling from MID to a new second-stage input, with10k feedback
from PRE to that input. Original transistor sizes and first-stage100k self-bias
remain. Local baseline/candidate AC fixtures reach1.530318V DC equilibria without
warnings.19.53125MHz source-to-final gain falls753.85→8.12V/V; interpolated2.5GHz
gain17.60→18.03V/V. This suggests rejection of slow bias movement while retaining
carrier gain in the local linearized fixture, warranting loaded transient testing.

Evidence: lo-interstage-ac.json and scratch/transceiver-lo-interstage-ac. Final
load is only50fF, not the actual mixer, and high small-signal gain cannot establish
large-signal amplitude or missing-pulse repair. No production cell changed.
SAR command replay confirmed live beyond65ns; no reproduction result yet.

## Pass 587 — reject inaccurate SAR replay; test loaded LO isolation

The terminal SAR command replay failed its predeclared reproduction gate despite
matching decisions and B7 timing: held-node differences reach1.49mV and rail
differences44uV. The intact80ns full-SAR baseline reproduces exactly within the
checked window and remains the preferred reference-remedy fixture. A faster model
that preserves bits but misses analog state cannot qualify a reference change.

Prepared and launched lo-interstage: four-unit PDK MIM coupling and10k local
second-stage feedback in all four original-size LO buffers, with the original
loaded1024ns replay and extra LOCAL/supply-current diagnostics. The checker
verifies exact reversal to the baseline circuit and records pending status until
terminal results exist. Local AC success is not loaded transient success; no
production cell has changed and autonomous backloading still needs verification.

## Pass 588 — intact SAR reservoir sensitivity

Launched one-factor80ns intact SAR candidate with4096 rather than2048 MIM units
on each reference rail. Exact deck reversal and successful baseline reproduction
are required by the checker. This retains actual controller/driver loading and
history; no command replay is used. Evaluate target/span errors and both saved
comparator decisions before considering any benefit. Added ideal plate area is
0.1024mm² (4096 additional5×5um units), excluding all physical overhead. This is
not a complete conversion, real-bias/startup test or production adoption.

Both this run and the loaded LO interstage run were confirmed live. Terminal
results are pending. The next circuit decision depends on measured improvement,
not the expectation that larger capacitors necessarily help settling.

## Pass 589 — larger reservoirs help but do not qualify the reference

The intact80ns double-reservoir experiment completed cleanly with immutable source
and artifact checks. Over70–79ns maximum span error decreased90.721→37.408mV;
maximum high/low target errors44.768/46.024→18.257/19.746mV. At the second clock
onset75.5ns, span improved0.952230→0.976286V against1V nominal. That remaining
23.714mV is6.07 nominal8-bit reference LSBs (a scale comparison, not measured code
error). Both late comparator decisions match baseline with full output swing.
Reference supply mean power in this short window44.647→43.895mW does not establish
steady-state energy savings: capacitor initial/final stored energy differs.

Evidence: evidence/sar-reservoir-double.json. Added plate area0.1024mm² excludes
physical overhead; production remains unchanged. Before adopting this candidate,
require complete-frame residue/code and reference settling across both input
polarities and repeated frames. Continued reference-driver regulation work is
still necessary; do not infer converter accuracy from two easy decisions. The
loaded LO interstage run remains live; no restart or autonomous qualification.

## Pass 590 — reference motion continues during comparator regeneration

Analyzed source-hash-verified intact baseline and doubled-reservoir waveforms
from each comparator clock start to its first90% differential output crossing.
For the second comparison, baseline resolves after458.13ps while reference span
moves17.108mV and held differential residue moves8.371mV. Doubling reservoirs
resolves after449.43ps with5.931mV span motion and2.860mV residue motion. Neither
residue crosses zero; initial magnitudes78.24/90.09mV make these easy decisions.
Matching bits here cannot establish small-residue decision fidelity.

Evidence: evidence/sar-reference-aperture.json, reproducible with
verification/analyze_sar_reference_aperture.py. The90% crossing is not the
comparator's effective aperture. Residue movement includes kickback and other
coupled effects; this measurement does not attribute all movement to reference
rails. Next evaluate full-conversion late/small-residue decisions before adopting
capacitance or timing changes. A longer clock delay trades conversion throughput
and must not silently weaken the RF sample-rate requirement.

## Pass 591 — full existing sequence for the reservoir candidate

Prepared and launched the doubled-reservoir circuit through the original209.9ns
three-frame sequence (+0.4,−0.4,+0.4V), retaining all transistor controller/driver
loading, original initialization and5ps maximum step. Exact reversal to the
hash-verified donor deck changes only the two added2048-unit reservoirs and output
filename. This extends the test to24 comparisons rather than assuming the first
two easy decisions represent a conversion.

Predeclared checks in scratch/transceiver-sar-reservoir-full-prepared/manifest.json:
source/artifact integrity, first80ns reproduction against the short candidate
within10uV, all decision margins, final codes and reference movement. The existing
50ns hold spacing is a diagnostic20MS/s sequence, not proof of the40MS/s mode or
autonomous ADC control. No clock slowing or sample-rate relaxation was introduced.
The named transceiver-sar-reservoir-full container was confirmed live; the LO
interstage simulation also remains live. Neither pending experiment is qualified.

## Pass 592 — full-conversion checker before terminal results

Added check_sar_reservoir_full.py: terminal source/artifact verification, short-run
reproduction over70–79ns with10uV limit, all24 pre-clock residues/reference spans,
comparator output swing/polarity, and three final captured codes. Checked its
hold+39ns code sampling convention against the original donor's existing frame
audit; all three codes match. Comparator polarity agreement is explicitly only a
diagnostic, not transfer accuracy or noise margin qualification. The first80ns
reproduction plan is scoped to the same predeclared70–79ns comparison window as
the original short-run check, not a claim of identity at every earlier time.

Full candidate and LO simulations remain live. Checker records pending without
interpreting partial waveforms as completed conversions. No production changes.

## Pass 593 — matched baseline for late-decision evaluation

Refactored full-reservoir evaluation to use the same measurements on the original
hash-verified donor and candidate. Added exact donor-deck reversal verification,
per-frame maximum span errors and explicit candidate-minus-baseline code changes.
The donor's bit1 pre-clock residues are−0.9346,−1.0729,−1.0422mV at holds70/120/170ns;
all resolve with matching polarity. These are the sensitive comparisons omitted
from the80ns test. They are measured at clock onset, so should not be silently
combined with earlier residue audits using different time offsets.

The full candidate remains pending, with its baseline measurements now recorded
in evidence/sar-reservoir-full.json. Valid final bits or matching signs alone still
do not establish converter accuracy, mismatch tolerance or40MS/s operation.

## Pass 594 — separate comparator speed from total conversion time

Measured all48 comparator regeneration intervals in both original three-frame
baseline streams with hash-verified waveforms. Worst first90% differential output
crossing is766.71ps (other stream752.13ps), within the existing1.9ns observation
window. Minimum absolute pre-clock residues are0.9346/1.1235mV. Evidence:
evidence/sar-full-aperture-baseline.json, analyze_sar_full_aperture.py.

This does not justify shortening the5ns bit period: reference recovery, controller
capture and physical code-driver settling share the budget. Existing10ns track
plus40ns conversion only demonstrates a20MS/s fixture.40MS/s remains an explicit
unverified requirement; do not conflate fast isolated comparator regeneration
with complete ADC throughput. No noise, mismatch, parasitic or unknown-package
margin is established by these nominal results. Full reservoir and LO candidates
remain pending, with no production change.

## Pass 595 — full reservoir result exposes a code change

The209.9ns candidate completed cleanly; source/artifact checks pass and70–79ns
HP/HN/VH/VL reproduce the short candidate exactly at exported precision. Across
holds70/120/170ns, final codes baseline78/178/78 become78/177/78. All24 candidate
comparator outputs exceed90% differential swing and agree with pre-clock residue
polarity; final bits are valid. Peak span errors over each hold-to-final window
fall122.878→64.143mV,62.667→45.064mV,123.622→63.316mV. These windows cover full
conversions and must not be compared directly with the earlier70–79ns maximum.

The negative-input bit1 pre-clock residue changes sign (−1.073→+5.996mV), changing
the subsequent trajectory and final code. Cleaner rails alone do not establish
better transfer accuracy. Keep the candidate unadopted. Next compare complete
charge/decision trajectories against an explicitly defined ideal transfer with
acquisition and actual CDAC topology accounted for; do not select the candidate
merely because its minimum decision margin grew. Evidence:
evidence/sar-reservoir-full.json. LO interstage run remains live and incomplete.

## Pass 596 — explicit ideal transfer rejects capacitor-only adoption

Derived ideal binary search from the actual code128 track mask, complementary
255 weighted units plus one dummy per side, and QN keep polarity. With constant
1V rail span, differential residue changes by2/256V per code. Ideal final code is
clipped floor(128−128*input), with exact-zero ties explicitly kept. Exhaustive
quarter/three-quarter-bin and exact-boundary arithmetic controls pass for256 bins.
This7.8125mV differential-input LSB differs from a1V/256 reference-span scale;
earlier “reference LSB” comparisons must not be read as ADC input code errors.

For source inputs+.4/−.4/+.4V ideal codes are76/179/76. Baseline78/178/78 has
errors+2/−1/+2 codes; doubled reservoirs78/177/78 give+2/−2/+2. Anchoring instead
to each measured first pre-clock residue yields ideal77/178/77 for both variants,
so the negative-input regression remains (0→−1 code relative to that anchor).
The capacitor-only candidate is not adopted. Evidence: sar-ideal-transfer.json;
system_model/sar_ideal_transfer.py. Anchoring removes acquisition/initial-state
errors, and this ideal model omits real input parasitics, injection and noise;
it is neither fitted correction nor INL/DNL qualification. Next circuit work
must target regulation/switching dynamics rather than larger reservoirs alone.

## Pass 597 — first divergent decision localizes late reference overshoot

Compared actual saved SD trial codes and comparator decisions with the ideal
held-anchor search. First disagreement occurs at bit1, with identical actual and
ideal trial code (78 for positive input,178 for the doubled-cap negative frame).
Baseline negative frame has no ideal-held decision divergence. Late reference
span at bit1 is1.0200/1.0190/1.0218V baseline versus1.03585/1.03606/1.03599V with
doubled reservoirs: reduced peak droop came with worse overshoot at this decision.

Ideal charge using measured rails predicts the observed wrong-sign residues in
all divergent cases. Approximately2.04–2.09mV residual remains, so rail history is
not the entire error and the formula does not prove a unique current mechanism.
Next reference experiment should target late overshoot/loop settling with original
reservoir size, while retaining intact controller timing. Do not optimize only
maximum rail error. Evidence: sar-decision-divergence.json and
system_model/sar_decision_divergence.py. After a divergent bit, later ideal/actual
trial sequences are not matched conditions. No production adoption; LO run live.

## Pass 598 — isolate compensation resistance before redesigning regulation

Reviewed prior compensation evidence: the earlier CC=2p/RZ=2000 combination
reduced errors but also changed the negative-input code. Avoid repeating it.
Launched an intact209.9ns three-frame intervention with CC=1p and original2048-unit
reservoirs retained, changing only RZ100→2000 (actual resistor25→500ohm at S=4)
in both reference amplifiers. Existing AC sweep worst peak impedance across six
rail/load cases is86.08→53.08ohm for these settings; this motivates the test but
does not guarantee transient improvement or loop stability.

Preparation verifies reversal to the hash-checked donor. Tunable cell definitions
were independently reversed to the original cells with exact text equivalence
apart from subcircuit/parameter names and resistor expression. No transistor sizes,
clock timing or targets changed. Evaluate bit1 overshoot, complete code errors and
regulation before adoption. Named transceiver-sar-reference-rzero container is
live; LO interstage still pending. Production reference cell remains unchanged.

## Pass 599 — matched compensation checker and risk reassessment

Extended the full-SAR checker with --rzero, using identical baseline/candidate
measurements while verifying exact resistor-only deck/cell reversal. It does not
apply the unrelated doubled-cap short-run reproduction gate. Rechecked the
completed reservoir experiment and regenerated its dependent ideal/divergence
reports; findings unchanged. New compensation report remains pending.

Risk review keeps loaded LO repair/autonomous timing and complete RF conversion
at the top. ADC capacitor-only change is rejected; resistor-only intervention is
bounded and in progress. Neither deterministic nominal decisions nor narrowed
supply/temperature bounds establish noise/mismatch/package tolerance. The ten
schematic obligations remain incomplete; no new layout gate opens. Updated
spec/risk-priorities.md to prevent the recent local ADC work being interpreted as
whole-chip feasibility or as proof of40MS/s conversion.

## Pass 600 — saved reference control nodes do not hit supply rails

Audited hash-verified baseline and doubled-reservoir X control nodes over all three
hold-to-final windows. High-driver gate ranges1.570–1.741V baseline and1.591–1.731V
candidate; low-driver gate ranges1.044–1.193V baseline and1.010–1.183V candidate.
Neither spends sampled time within100mV of0/3.3V. Evidence:
evidence/reference-control-nodes.json and verification/analyze_reference_control_nodes.py.

This excludes only observed rail contact at those two saved nodes. It does not
prove transistor saturation/compliance or exclude internal tail/mirror limitations,
slew or insufficient loop gain. Do not label the overshoot a proven compensation
problem. Await the resistor-only intervention; if inadequate, add targeted internal
node/device-current probes and qualify their reproduction before redesigning the
reference driver. Both active simulation handles were verified live this pass.

## Pass 601 — reuse transfer scoring and existing probe method

Added --rzero to sar_ideal_transfer.py so the compensation candidate is scored
against the same source-level and measured-held-anchor ideal transfer as the
capacitor experiment. It refuses pending results; unchanged capacitor-case output
and256-bin arithmetic controls were rerun successfully.

Reviewed existing connected_headroom_screen.py and reference-connected-headroom.json:
there already is a device-current/VDS/VDSAT probe method for an older compensated
ADC fixture, plus device_probe_contract.py validating reporting conventions.
Reuse that machinery if new probes are needed; do not imply internal-device work
has never been done. Those prior fixture-specific results do not qualify the
present baseline/doubled-cap/resistor-only headroom. Current resistor-only and LO
simulation handles were confirmed live; no restarts or circuit adoption.

## Pass 602 — resistor-only compensation also fails transfer comparison

The resistor-only209.9ns candidate completed cleanly with immutable source,
artifact and exact-circuit-change checks. Codes78/177/78 versus baseline78/178/78
again worsen the negative-input error relative to source-level ideal179 and
held-anchor ideal178. All24 comparisons resolve with matching pre-clock polarity.
Bit1 spans increase1.020017→1.025252V,1.018968→1.023043V,1.021768→1.025412V.
Peak per-frame span errors are117.889/64.932/117.918mV versus baseline
122.878/62.667/123.622mV: a mixed result, not uniform regulation improvement.

Evidence: sar-reference-rzero.json and sar-rzero-ideal-transfer.json. Reject this
candidate for adoption. Small-signal impedance improvement did not predict useful
large-signal conversion accuracy. Stop blind compensation-value sweeps; use
existing device-probe methodology to inspect actual internal currents/headroom
through the failing transition and late bit1 decision before changing topology.
LO interstage remains live around500ns of1024ns; no partial-result acceptance.

## Pass 603 — unchanged baseline with complete reference device observations

Prepared and launched the original209.9ns SAR stream with62 added saved vectors:
A/T/Z nodes and ID/VDS/VGS/VDSAT for all seven devices in each reference amplifier.
No circuit element, initialization or timing changed; removing save directives
and output probes exactly restores the hash-verified donor deck. Uses the existing
PDK device-name convention and documented multiplicity/sign probe contract.

Predeclared reproduction gate: HP/HN/VH/VL within10uV of donor over70–209ns, all24
comparator decisions matching, and actual time-varying device vectors. Only then
interpret tail/mirror/output headroom and current during switching and late bit1.
Selected DC ID conventions do not guarantee all transient/reverse-current regions,
and ID omits some terminal displacement current. The new named device-observation
container is live; source circuit remains unchanged. LO interstage run also remains
active; no candidate adopted and layout gate remains closed.

## Pass 604 — device-observation reproduction gate

Implemented check_sar_reference_devices.py with exact observation-only deck
reversal, unchanged simulation sources, artifact hashes, finite monotonic full
waveform and exact appended-probe column checks. It compares HP/HN/VH/VL over the
union of native grids plus70/209ns boundaries against the donor at10uV tolerance,
checks all24 resolved comparator polarities, and requires each added vector to
vary in time. Pending output explicitly has reproduction_pass=false.

Time variation rejects scalar final-OP repetition but alone cannot validate every
model quantity's physical meaning. Existing device-probe sign/multiplicity limits
still apply. No device-region interpretation or automatic production adoption is
performed. Device-observation and loaded LO simulations were both confirmed live;
no partial waveform is treated as terminal evidence.

## Pass 605 — device analysis covers switching as well as decisions

Added analyze_sar_reference_devices.py, gated on successful observation reproduction.
For all24 bit intervals it measures every reference transistor's minimum modeled
VDS−VDSAT and its time, current range, clock-onset current/margin, negative-margin
time fraction and minimum reported VDS. Windows include between-decision changes,
not only late comparator values. Exact window endpoints are interpolated and
included to avoid dropping boundary extrema from a native-grid mask.

Pending simulation yields only awaiting_qualified_observation; no device numbers
are fabricated or read from partial export. Negative modeled margin is an
operating-region diagnostic, not proof of inadequate closed-loop performance.
Negative reported VDS flags possible departure from the verified forward-device
sign convention; ID is not full terminal displacement current. Device and LO
simulation handles were confirmed live; no circuit changes this pass.

## Pass 606 — threshold-duration measurement correction

Replaced trapezoidal integration of Boolean negative-headroom samples with exact
below-zero duration for piecewise-linear interpolation of saved margin samples.
The former could assign half a timestep to a crossing regardless of where it
actually occurred. Controls cover rising/falling asymmetric crossings, exact-zero
endpoints, all-zero and all-negative sequences. They pass. This improves narrow
excursion reporting without changing the10uV reproduction gate or simulation.

Device-observation run remains live beyond155ns; LO interstage remains live beyond
586ns. Neither has terminal evidence, so the device analysis continues to report
awaiting_qualified_observation. No topology or headroom conclusion is inferred.

## Pass 607 — exact reproduction exposes persistent high-reference tail limitation

The209.9ns device-observation run completed cleanly. HP/HN/VH/VL errors against
donor are zero at exported precision over70–209ns; all24 decisions match and all62
added vectors vary. Qualified device analysis finds high-reference XT modeled
VDS−VDSAT negative throughout every analyzed bit interval, minimum−158.225mV.
At bit1 clock starts the margin is approximately−152.5mV across all three frames.
Reported VDS remains positive (minimum75.46mV), within the forward-polarity
convention. Tail ID ranges0.609–0.738mA across frames. Other13 observed devices
retain positive modeled margins; low-reference XT minimum is+75.76mV.

Evidence: sar-reference-devices.json and sar-reference-device-windows.json. This
is persistent limited tail compliance, not merely an early output-gate rail hit.
It does not alone prove the cause of ADC code error or establish adequate loop
gain elsewhere. Next compare this exact-fixture observation with prior tail/input
headroom interventions before selecting a repair; avoid repeating earlier failed
variants. LO interstage remains running; no transistor circuit adopted this pass.

## Pass 608 — common-mode attenuation candidate; avoid rediscovering old headroom

Reconciled the exact-baseline tail observation with historical passes265/266 and
related connected diagnostics. Tail noncompliance was already known in related
fixtures; wider inputs, dedicated bias changes and hybrid first-stage variants
were explored without a qualified full-chain repair. The new exact reproduction
narrows scope; it is not discovery of a previously unknown device mechanism.

Launched a different intact209.9ns candidate:10k/30k dividers on both high-reference
amplifier inputs, attenuating target and feedback nominally0.75 (2.15→1.6125V at
the input pair). Equal ratios preserve the ideal output setpoint while lowering
PMOS input common mode. Existing devices, compensation, reservoirs, clocks and low
reference are unchanged; exact donor reversal is checked. Each divider draws
about53.75uA at2.15V; real target loading, resistor noise/mismatch, reduced feedback
gain, poles and input offset amplification are material unqualified costs.

This is a topology experiment, not a repair claim. Evaluate full conversion and
static regulation first; actual transient tail headroom still needs observation
if it is promising. Candidate uses ideal resistors for schematic exploration,
not a resistor-layout qualification. Production reference remains unchanged.

## Pass 609 — divider comparison gate and explicit ratio uncertainty

Added --divider to the matched full-SAR checker and ideal-transfer scorer. Deck
reversal permits only the declared paired dividers and output filename; no
unrelated short-run reproduction gate applies. Existing resistor-only ideal
results were rerun unchanged. Divider simulation remains pending.

Enumerated all16 independent resistor-bound sign combinations per hypothetical
0.1/0.5/1/5% tolerance for the ideal divider feedback relation. High-reference
worst error is2.151/10.777/21.609/110.398mV respectively, even with an ideal
amplifier and target. Common resistor scaling cancels exactly. Evidence:
reference-divider-sensitivity.json and system_model/reference_divider_sensitivity.py.
These are sensitivity scenarios, not documented process bounds or mismatch
predictions. Nominal success must not bypass resistor matching/noise, target
loading and calibration questions; no calibration exists to assume away errors.

## Pass 610 — divider target loading is an explicit integration dependency

Extended the ideal divider sensitivity calculation with a finite DC Thevenin
resistance on the target source. Equal nominal divider ratios give
Vout=2.15*40000/(40000+Rs);100ohm creates5.362mV output error. Keeping this
contribution below an illustrative1mV requires Rs≤18.613ohm. This is a derived
screening condition, not an adopted converter error allocation or proof that the
real target generator meets it. The input divider draws53.75uA at ideal2.15V.

A successful candidate under ideal VHR would therefore still need an actual
low-impedance target source or a revised ratio/buffering design. Larger divider
resistance reduces DC loading but increases impedance/noise/pole sensitivity;
no free improvement is assumed. Evidence: reference-divider-sensitivity.json.
Both full-divider and LO simulations remain live; no additional variant launched
before evaluating the existing candidate.

## Pass 611 — divider topology fails the connected conversion test

The matched-divider209.9ns run completed cleanly, with verified source/artifact
integrity and exact circuit-change scope. Final codes78/177/78 again worsen the
negative-input error against ideal-source179 and held-anchor178. All24 comparisons
resolve and agree with pre-clock residue polarity. Per-frame peak span errors
132.892/97.261/126.463mV are worse than baseline122.878/62.667/123.622mV. Bit1 spans
are1.027296/1.053832/1.031582V, also worse than the corresponding baseline.

Reject this candidate for adoption. No added transient device probes were saved,
so do not claim measured tail-headroom repair; the common-mode mechanism alone
was insufficient for useful connected behavior. Its resistor matching/target
loading costs remain additional disadvantages. Evidence: sar-reference-divider.json
and sar-divider-ideal-transfer.json. Retain the original reference cell. Larger
reservoirs, RZ-only compensation and input dividers have now all failed transfer
comparison; avoid another isolated metric optimization without a new mechanism.
LO interstage run remains live and will determine the next clock-path action.

## Pass 612 — ideal-rail diagnostic separates remaining conversion mechanisms

Launched original209.9ns intact SAR with ideal voltage clamps VH=2.15/VL=1.15.
Physical reference drivers and reservoirs remain connected; clamps supply or sink
whatever current is required. No controller, sampling, CDAC, comparator, initial
condition directive or clock change. Removing the two clamp sources exactly
restores the hash-verified donor deck (apart from output filename).

This is an unrealizable diagnostic intervention, never a candidate for production.
It removes rail error while retaining transistor acquisition and switching, to
measure remaining conversion error before another reference redesign. Initial
analog state and subsequent SAR trajectories may change; it is not a unique
causal decomposition or rigorous best-case ADC accuracy bound. Evaluate all24
comparisons and ideal-transfer errors on completion. The named clamped run is
confirmed live. LO interstage remains live; no production circuit changed.

## Pass 613 — verify ideal boundary before interpreting clamp diagnostic

Added --clamped to the shared full-conversion checker and ideal-transfer scorer.
Exact deck reversal allows only the two ideal voltage clamps. Terminal validation
requires both saved rail voltages within1nV of their specified values across the
entire exported waveform, not just sample instants. This is a numerical check of
ideal sources, not a physical voltage-accuracy requirement. All original24
comparison, final-code and source-integrity checks remain active.

The ideal-transfer scorer requires clamps_verified before reporting diagnostic
code errors. Rechecked completed divider results and its ideal scoring unchanged.
Clamped report remains pending; the current simulation is live. No performance
claim follows merely from ideal-rail construction, and no physical reference is
replaced in production. LO interstage also remains live.

## Pass 614 — common decision-divergence analysis for all interventions

Generalized sar_decision_divergence.py with --variant double/rzero/divider/clamped,
using each completed candidate's waveform and ideal-transfer provenance. Ran it
on the completed divider case: first held-ideal disagreement is bit1 at matching
trial codes in all three frames. Measured-rail charge predicts the wrong-sign
residues; remaining errors are+2.031/−2.108/+2.077mV. This resembles prior residuals
but is not proof of a common physical mechanism.

The clamped case will use the same analysis only after its complete waveform and
ideal-rail checks pass. Later trajectory differences after the first divergent bit
remain explicitly unmatched conditions. Clamped and LO simulation handles are
live; no partial waveform or new circuit candidate was accepted this pass.

## Pass 615 — ideal rails remove observed held-anchor decision divergence

The clamped209.9ns diagnostic completed with source/artifact integrity and ideal
rail verification. Codes are77/178/77 versus physical-reference78/178/78. Every
clamped decision follows the ideal search anchored to its measured first residue;
there is no first divergent bit. Codes match held-anchor ideal77/178/77, while
source-level ideal remains76/179/76. Perfect references therefore remove one
observed positive-input code error but do not eliminate source-to-code error.

Evidence: sar-reference-clamped.json, sar-clamped-ideal-transfer.json and
sar-clamped-decision-divergence.json. This supports continued reference-regulation
work while retaining acquisition/parasitic/switching error as a separate problem.
It does not prove ideal-reference ENOB or that every residual is acquisition:
only two input levels and three frames are covered, and anchoring hides initial
state errors. Ideal clamps are never a realizable repair or production adoption.
LO interstage remains active; no simulation was restarted during the verified wait.

## Pass 616 — initial conversion error predates the sampling edge

Audited saved driver IP/IN and held HP/HN around all three track-to-hold transitions,
with verified baseline/clamped waveforms. Under ideal rails at the command edge,
driver differential magnitude is short by3.842–3.856mV; held magnitude is a further
2.679–2.686mV short. Total error is6.528–6.535mV before the opening ramp. By the
first comparator clock0.5ns later, held magnitude recovers about2.153mV. Results
are nearly symmetric across the selected opposite-polarity frames.

Evidence: sar-acquisition-boundary.json and analyze_sar_acquisition_boundary.py.
This localizes substantial error upstream of the sampling edge rather than proving
switch injection is the main problem. Driver settling/static tracking and sampler
tracking now need separation. Sampling and command-mask ramps coincide; the edge
change cannot uniquely be assigned to one mechanism. Do not infer bandwidth from
these two constant input levels. Physical-reference regulation remains unresolved;
LO interstage is still running beyond819ns of1024ns.

## Pass 617 — longer acquisition as a diagnostic, not a rate concession

Prepared and launched ideal-rail SAR with first hold delayed70→90ns while input
transition remains60ns. Control events at/after70ns shift together by20ns; relative
bit timing, analog circuit and5ps maximum step stay unchanged. Stop100ns covers
the first two comparisons before any later input change. Exact control-line and
stop-time reversal restores the hash-verified clamped donor; seven control-source
lines change, while VIP/VIN do not.

Compare pre-edge driver/held errors at90ns with donor70ns to distinguish finite
acquisition from persistent gain/offset/loading error. This artificial extra20ns
track time is explicitly not a production sample-rate reduction, nor proof of
40MS/s capability. Ideal rails isolate acquisition from reference recovery; no
full-conversion claim is possible from this short diagnostic. Named container is
live; LO interstage continues beyond835ns. No production circuit changed.

## Pass 618 — extended acquisition separates tracking lag from driver error

The100ns extended-track diagnostic completed cleanly with exact control-only
reversal, unchanged sources, artifact verification and ideal-rail checks. At hold,
tracking difference improves−2.679021mV→−0.103450uV with20ns additional acquisition;
driver error improves−3.855720→−2.460515mV. Held error improves−6.534741→−2.460618mV.
Evidence: sar-long-acquisition.json and check_sar_long_acquisition.py.

This establishes strong time dependence of the selected sampler-tracking error,
while substantial driver error remains after the longer wait. It does not prove
that the driver is fully settled or uniquely attribute the residual to DC gain.
Next acquisition repair must improve settling within the original time budget;
the diagnostic delay is not an acceptable sample-rate reduction. Two decisions
and one constant level do not qualify dynamic converter accuracy. LO interstage
continues running; no production change has been adopted.

## Pass 619 — double sampler fingers at original timing

Launched the full209.9ns clamped-rail fixture with one additional identical sampler
bank in parallel. Exact removal of XS_EXTRA restores the qualified clamped donor;
original input/control schedules, driver, CDAC and comparator remain unchanged.
This targets the measured acquisition lag without accepting extra tracking time.

Evaluate pre-edge tracking together with post-edge held error, all24 comparisons
and final codes. Doubling switch width also increases parasitic capacitance and
injection. Ideal SC/SCB sources hide added clock-driver energy and edge degradation;
nominal success would still require physical controls and restored reference
regulation. The switch cell itself is not edited. Named simulation handle is live,
and LO interstage continues beyond883ns. No production adoption or speed claim.

## Pass 620 — sampler candidate scored against matched ideal-rail baseline

Added --sampler-double to the full-SAR checker and ideal-transfer scorer. Exact
instance-removal reversal and hashed clamped baseline are required. Comparison
baseline is explicitly the one-bank ideal-rail circuit, not the physical-reference
case, avoiding a confounded sampler conclusion. Both rails must meet the ideal
clamp numerical check. Frame measurements now include driver and held errors at
hold+0/+0.1/+0.5ns alongside all24 decisions and final codes.

Pending candidate yields no performance score; source/terminal gates are unchanged.
Extra clock loading remains hidden by ideal controls and will require a physical
clock-drive test if this candidate helps. The doubled sampler and loaded LO
containers were confirmed live. No production change or accuracy claim this pass.

## Pass 621 — doubled sampler improves selected codes with larger disturbance

Full209.9ns double-bank run completes with exact-change/source/artifact and ideal
rail checks. Codes76/179/76 match source-level and held-anchor ideal at the selected
two levels, versus one-bank77/178/77. At first comparison held errors become
+1.9997/−1.3824/+1.3952mV versus−4.3810/+4.3763/−4.3751mV baseline.

This is not a clean settling win. At hold+0.1ns driver error magnitudes rise from
about29mV to83–85mV. Pre-edge driver-minus-held gaps also grow (roughly7.0–7.3mV
versus2.68mV), while held error alone improves. The larger edge disturbance and
changed driver trajectory may compensate errors at these specific levels. Keep
candidate unadopted pending robustness, physical clocks and actual references;
three exact codes do not establish INL/DNL or ENOB. Evidence:
sar-sampler-double.json and sar-sampler-double-ideal-transfer.json. Next distinguish
repeatable improvement from input/history-dependent cancellation before adopting
width changes. LO interstage remains live near956ns of1024ns.

## Pass 622 — matched amplitude check for possible sampler error cancellation

Launched one-bank and two-bank full209.9ns runs at differential+.1/−.1/+.1V,
retaining1.65V input common mode, original clocks and ideal reference rails.
Only the two VIP/VIN amplitude lines change relative to each verified preparation;
exact text reversal is checked. Nominal ideal-source codes115/140/115 are declared
before results. This complements the completed±.4V cases without assuming that
three exact codes imply an accurate transfer curve.

Compare final codes and pre/post-edge held errors between matched bank counts.
Ideal sampling sources still hide physical clock-drive cost; real references,
noise/mismatch and dynamic input tests remain required before adoption. Both named
amplitude containers are confirmed live. LO interstage continues near970ns/1024ns;
no unfinished simulation was restarted or treated as accepted.

## Pass 623 — matched smaller-amplitude checker

Added check_sar_sampler_amplitude.py. It verifies each amplitude-only deck reversal
and that the two prepared circuits differ solely by XS_EXTRA, then checks terminal
source/artifact integrity, full waveform and ideal rails. Reports final codes
against predeclared115/140/115, valid captured levels, all24 comparison margins
and pre/post-edge driver/held errors. Neither incomplete run is scored.

Both smaller-amplitude containers and the LO interstage container were verified
live. LO was beyond984ns/1024ns but remains unqualified until terminal provenance
and full-window metrics are available. No simulation restart or production change.

## Pass 624 — interstage LO isolation repairs missing crossings in loaded replay

The1024ns LO interstage replay completed; terminal source/artifact and exact-change
checks pass. Over512–1024ns, all four outputs have1280 rising crossings at1.65V,
with zero intervals>800ps. Baseline I+/I− had1170/1160 crossings and19 long gaps
each. Candidate maximum intervals are424.06/419.13/403.31/405.36ps for I+/I−/Q+/Q−.
I baseband reference fundamental falls31.613→0.79248mV peak, p-p71.319→2.289mV;
Q fundamental2.82388→0.04705mV, p-p6.1899→0.16461mV. DC residuals remain
−6.554mV I/+3.524mV Q. Saved VBUF source current averages−17.677mA; baseline
current was not saved, so no comparative power claim.

Evidence: lo-interstage.json. This is the first tested candidate here to eliminate
observed missing output crossings in the loaded replay. It is not autonomous
clock/noise qualification: source replay removes oscillator back-loading, initial
state is prescribed, and crossing counts do not prove duty/amplitude every cycle.
Next perform cycle-local pulse checks and restore the full autonomous circuit
before production adoption. Both amplitude ADC runs remain separate experiments.

## Pass 625 — LO cycle quality and sampler amplitude reversal

Cycle-local LO replay analysis verifies exactly one falling1.65V crossing in each
of1279 complete rising-to-rising cycles per leg over512–1024ns. I+/I− duty ranges
44.00–62.09%/43.60–60.74%; periods381.44–424.06ps/377.84–419.13ps. Worst I per-cycle
peak is2.633V and highest trough0.747V. Q duty is50.13–52.66%, with much narrower
period and amplitude ranges. Evidence: lo-interstage-cycles.json. Missing crossings
are repaired, but I drive is still modulated; no invented mixer-drive acceptance
threshold or noise/jitter qualification follows. Full autonomous loading remains
next for this promising topology.

Both matched±.1V sampler runs completed with exact deck/source/artifact checks.
One bank gives ideal115/140/115; doubled bank gives114/142/113 (−1/+2/−2 code
errors). Reject doubled sampler as a general acquisition fix: its success at±.4V
was not robust to amplitude/history. Evidence: sar-sampler-amplitude.json. Retain
original sampler and pursue driver/settling mechanisms rather than adopting a
width change based on three favorable codes. No production cell changed.

## Pass 626 — restore autonomous loading for the promising LO topology

Prepared and launched the complete8.001us selective-save autonomous receiver with
the replay-tested4-unit MIM/10k interstage isolation in all four LO buffers.
Inlining the candidate and reversing it restores the hash-verified original deck
exactly. Ring oscillator, divider, PFD/pump/filter, receiver load, prescribed initial
conditions,2ps maximum step and saved vectors remain unchanged. No replay sources
are used. Production lo_buffer.spice is not edited.

Named container transceiver-lo-interstage-autonomous is live, with8GB memory to
accommodate prior full-loop waveform/postprocessing needs and streamed hashes.
Compare late lock, output pulse quality, deterministic receiver disturbance and
supply currents against the original autonomous run. This fixture still has ideal
supplies/reference and prescribed UIC; it does not establish cold startup, intrinsic
phase noise, ADC coexistence or full-chip readiness. Source/artifact terminal
checks remain mandatory; no conclusion until completion.

## Pass 627 — autonomous terminal gate and matched late-window extraction

Added check_lo_interstage_autonomous.py: exact donor reversal, immutable simulation
sources and artifact hashes, clean terminal log, full8.001us time coverage and
monotonic exported times. Streaming extraction retains7424–7936ns plus adjacent
boundary samples for P/N, all four LO outputs, I/Q baseband, CTRL/FB/REF and buffer/
PLL supply currents. This matches the loaded replay's512–1024ns parent interval.
It avoids loading the entire multi-GB waveform just to inspect its late window.

Finite checks cover all extracted vectors; only times/column counts are checked
outside that window. A terminal extraction is explicitly metrics_pending, never
a lock/phase-noise or RF qualification. Current report is pending; named autonomous
container is live. Next compare cycle-local pulses, deterministic loop timing,
baseband disturbance and supply current with the original autonomous evidence.

## Pass 628 — matched autonomous baseline including current

Streamed and hash-verified the original full waveform, extracting7424–7936ns plus
boundary neighbors (256122rows). New shared autonomous-window metrics reproduce
I+/I−1170/1160 crossings with19 long gaps each, Q1280 each, I reference fundamental
31.568mV peak and Q2.820mV. Original autonomous VBUF/PLLVDD source mean currents
are−17.616/−8.454mA in this window. CTRL spans1.081369–1.090937V; FB/REF each have
ten rising crossings. These are deterministic observations, not lock/noise proof.

Evidence: lo-autonomous-baseline-window.json and its -metrics.json. Shared
analyze_lo_autonomous_window.py supports the candidate extraction once terminal
validation completes. Baseline autonomously measured supply current enables a
matched comparison later; do not directly interpret its difference from replay
current as candidate autonomous power. The8.001us candidate remains live.

## Pass 629 — do not optimize driver DC error independently of sampling

Hash-verified long-acquisition waveform shows a near-flat88–90ns plateau: driver
p-p0.2034uV and held p-p0.06268uV, yet held error remains−2.460618mV. At the first
clock90.5ns error is only−0.154178mV: the intervening edge-associated change adds
+2.306440mV. Thus a substantial apparent DC tracking error is mostly compensated
by subsequent sampling behavior in this particular diagnostic.

Evidence: acquisition-cancellation.json. Do not optimize driver offset alone or
claim the remaining−2.46mV necessarily dominates conversion error. Future driver
changes must be scored over acquisition, release and actual comparisons together.
This cancellation is not designed/calibrated and one long-track positive level
cannot establish robustness. Earlier driver damping/long-mirror work exists;
review it before selecting a fresh intervention. Autonomous LO handle remains
live beyond244ns/8001ns; no production change this pass.

## Pass 630 — driver damping hypothesis tested at both amplitudes

Reviewed prior loaded-driver1k/2k resistor tests and AC evidence.2k showed lower
overshoot than1k in the reduced fixture, but that fixture used different input
common mode and omitted comparator/controller loading. Launched intact209.9ns
SAR tests at±.4 and±.1V with only the sample-driver compensation resistor100→2000ohm
changed. Original0.5p compensation, transistor sizes, single sampler bank,1.65V
common mode and clock schedules remain; references stay ideally clamped.

Both prepared decks exactly reverse to their matching original-amplitude donors.
Evaluate complete acquisition/release and final codes at both amplitudes, not an
isolated near10ns voltage or DC output error. This is sample-driver compensation,
not a repeat of the rejected reference-driver RZ intervention. Both named
containers are live alongside the autonomous LO run. Production cells unchanged.

## Pass 631 — driver damping scored separately at both amplitudes

Added check_sar_driver_damping.py with exact resistor-only reversal, source/artifact
integrity, full waveform and ideal-rail checks. It retains separate±.4V and±.1V
case reports, all decision margins, before/after-edge errors and source-ideal code
errors. Original matching-amplitude waveform hashes are checked independently;
baseline final codes and first-clock held errors are recorded for comparison.

Both candidate reports are pending. An average improvement across amplitudes
cannot conceal a regression at one level; no automatic adoption is performed.
Original-driver baselines remain77/178/77 at±.4V and115/140/115 at±.1V. Both damping
containers and the autonomous LO container were confirmed live. No production
circuit changed and no incomplete result was scored.

## Pass 632 — driver damping survives both selected amplitudes

Both209.9ns driver2k runs completed cleanly with exact-change/source/artifact and
ideal-rail checks. At±.4V codes improve77/178/77→76/179/76 (source ideal), while
±.1V retains ideal115/140/115. First-comparison held errors at±.4V become
+1.901/−2.319/+2.317mV versus baseline−4.381/+4.376/−4.375mV. At±.1V they improve
−248.2/+250.7/−250.9uV→−20.48/+2.56/−3.29uV. Evidence: sar-driver-damping.json.

This survives the amplitude check that rejected sampler doubling, but is still
selected nominal ideal-reference behavior. Do not claim a transfer curve or
adopt the driver yet. Next restore the actual reference circuit with original
sampler and clocks, then assess whether acquisition improvement survives coupled
rail dynamics. Physical sampling-clock drive, noise/mismatch and40MS/s timing
remain open. Autonomous LO simulation continues independently.

## Pass 633 — restore physical regulation with matched small-input control

Prepared and launched driver2k candidates at±.4/±.1V with the ideal reference
clamps removed. Added the missing original-driver±.1V physical-reference control;
original±.4V physical baseline already exists. Removing only VCLH/VCLL exactly
reverses to each qualified clamped preparation. Actual reference drivers/reservoirs,
original sampler,209.9ns controller schedule and all other circuit elements remain.

Three named runs are live. This separates driver improvement from changed rail
history at each amplitude; do not compare a physical-reference candidate solely
against an ideal-rail baseline. Reference target/bias generators and phase controls
are still ideal, so this is another integration step rather than complete ADC
qualification. Autonomous LO run continues separately. No production cell changed.

## Pass 634 — physical-reference driver comparison gate

Added check_sar_driver_physical.py for both damped amplitudes and the small-input
control. Each deck restores its clamped preparation exactly when the two diagnostic
sources are reinserted. Further reversal of only the driver resistor change matches
the same-amplitude physical-reference control (existing donor at±.4V, newly prepared
control at±.1V). This catches unintended mixed circuit/timing changes.

Terminal evaluation records valid final levels, source-ideal code errors, all24
comparison margins and reference span at clock onset, full-frame peak span error,
and before/after-edge acquisition errors. It does not impose ideal-rail checks on
physical-reference waveforms or automatically adopt correct selected codes. All
three reports remain pending; their runtime handles and the autonomous LO handle
were confirmed live. No production circuit changed.

## Pass 635 — fast feasibility workflow and physical ADC result

The eleven-script fast suite passes its execution/mathematical controls in about
nine seconds. The aggregate now records coverage and links to
[the fast feasibility workflow](spec/fast-feasibility-workflow.md), which defines
architecture screening, short transistor calibration, and connected verification.
Models remain partially connected and mostly hypothetical; no feasibility or
layout gate opens.

All three physical-reference ADC runs completed and passed artifact/exact-change
checks. At ±0.4 V the 2k sample-driver candidate gives77/179/76 versus the original
physical baseline78/178/78 and source ideal76/179/76. At ±0.1 V both candidate and
matched original-driver control give115/140/115. Improvement survives partly,
but the first large-input code is still wrong. Evidence: sar-driver-physical.json.
No production cell is adopted. Reference dynamics and acquisition remain coupled
risks; selected codes do not establish transfer linearity, noise or40MS/s.

The autonomous LO interstage experiment remains running and is not scored here.

## Pass 636 — localize remaining physical-reference driver error

Added system_model/sar_physical_divergence.py. It checks completed waveform
hashes, reconstructs all actual trial codes and comparator decisions, verifies
final-code reconstruction and compares against an ideal binary search anchored
to the measured first held residue. Independent all-bin/boundary controls pass.
Evidence: evidence/sar-physical-divergence.json.

The driver2k ±0.4 V first frame diverges only at bit0, trial77: ideal held-input
residue is+3.380mV but measured residue is−1.592mV. The equal-capacitor measured-rail
formula predicts−3.668mV, leaving+2.075mV unexplained. Reference motion is therefore
consistent with the wrong sign, but this does not uniquely assign causality or
explain all charge dynamics. Acquisition alone would still give ideal code76.
The later same-positive-input frame and all other measured frames have no
held-ideal decision divergence. Reference history/settling remains a priority;
correct selected codes do not remove large reference excursions. No production
circuit changed. The autonomous LO container was verified live at15 minutes;
its incomplete output is not scored. Previous turn classification: progress.

## Pass 637 — reservoir/driver interaction test at both amplitudes

Previous turn classification: progress (localized the remaining wrong decision).
Prepared and launched sar-driver-reservoir-400 and sar-driver-reservoir-100 using
the completed physical-reference, 2k sample-driver fixtures. The sole additional
change is a second2048-unit MIM bank per reference rail. Exact removal restores
the matching baseline deck; baseline preparation/result hashes are checked.
Original sampler, controller timing, targets and bias remain. Both209.9ns runs
were confirmed live, alongside the autonomous LO experiment.

Hypothesis: reduced rail motion may now help after improved acquisition, despite
reservoir doubling having failed with the original driver. Assess each amplitude
and frame separately; the first large-input bit0 error and the later104uV
correct-sign margin must not be concealed by aggregate code counts. The previous
reservoir failure means success cannot be inferred from smaller peak droop alone.
Added capacitor plate area is approximately0.1024mm² before layout overhead.
No production adoption; simulation results pending.

## Pass 638 — matched reservoir comparison checks

Previous turn classification: progress (prepared and launched two exact-change
experiments). Extended check_sar_driver_physical.py with --reservoir-double,
sharing the existing full-waveform and24-decision evaluation. It verifies
baseline evidence/preparation hashes and exact removal of only the extra MIM
banks. Per-frame comparisons retain code error, minimum absolute decision residue
and peak reference-span error separately; no automatic adoption uses these values.
Absolute residue is not a correct-decision guarantee and is not a noise bound.

Both candidate reports are pending in evidence/sar-driver-reservoir.json. The
unchanged original mode was rerun successfully and regenerated its original
physical-reference report byte-for-byte, preserving downstream provenance.
Both new containers were verified live around one minute; the400mV run was at
approximately66ns of209.9ns. Autonomous LO run remains live. No incomplete
waveform is scored, and no production circuit changed.

## Pass 639 — signed residue sensitivity, without invented noise bounds

Previous turn classification: progress (comparison gate and regression check).
Added system_model/sar_decision_margin.py, with optional --reservoir-double.
It requires completed evidence, full comparator swings and polarity agreement,
then calculates the open signed additive-residue interval preserving each
recorded decision path. Sweeps cover both signs at0.1/0.25/0.5/1/2/5mV and stop
at the first at-risk bit; no changed-path final code is invented. Analytic
boundary and sign-reversal controls pass. Evidence: sar-driver-physical-margin.json.

For driver2k at±0.4V, the first already-wrong frame preserves its path over
(−5.361,+1.592)mV; preserving this path is not accuracy. The negative frame has
(−16.116,+0.919)mV and the later positive frame(−0.104,+12.161)mV. At±0.1V the
tighter side ranges1.781–3.146mV with driver2k and1.811–3.459mV with the original.
Thus correct codes alone do not demonstrate uniformly improved decision margin.

These are static residue sensitivities, not comparator offset/noise qualification.
Small margins are intrinsically expected near quantizer thresholds; final
acceptance requires transfer/noise metrics over input rather than imposing a
nonzero minimum margin at every input. Both reservoir simulations and autonomous
LO were confirmed live; no candidate result is claimed yet.

## Pass 640 — reject reservoir doubling with the damped driver

Previous turn classification: progress (signed sensitivity evidence). Both209.9ns
reservoir/driver runs completed cleanly, with exact-change, source/artifact and
full waveform checks. Large-input codes worsen77/179/76→78/178/77 against ideal
76/179/76, despite peak span errors falling121.435/62.720/122.163mV→
64.190/45.111/62.625mV. Small-input codes remain115/140/115. Selected nominal
accuracy therefore regresses at every large-input frame; reject this combination.
No production cell changed. Evidence: sar-driver-reservoir.json and
sar-driver-reservoir-margin.json.

The schematic obligation registry now links recent replay and ADC evidence while
retaining incomplete statuses and the closed layout gate. Autonomous LO remains
a separate unfinished experiment. Next reference work should diagnose decision-
time recovery and feedback dynamics, rather than increase reservoir capacitance
again on the strength of peak droop. Static residue sensitivities do not establish
noise or mismatch bounds.

## Pass 641 — enlarged reservoir fails during reference recovery

Previous turn classification: progress (completed and rejected reservoir/driver
combination). Extended sar_physical_divergence.py with --reservoir-double and
separate high/low rail errors plus backward0.2ns span slopes. Reconstructed all
trial codes and final decisions from hash-verified completed waveforms; ideal
all-bin/boundary controls pass. Reports: sar-reservoir-driver-divergence.json and
refreshed sar-physical-divergence.json.

At±0.4V, all three enlarged-reservoir frames still have correct source-ideal codes
when only their measured initial held residue is used. Errors therefore require
later conversion dynamics, rather than initial acquisition alone. First divergent
bits are1/0/0. Their spans are1.035941/1.023984/1.017336V and are declining at
approximately4.206/3.224/1.396mV/ns over the preceding0.2ns. High rails are above
nominal by24.626/14.873/12.857mV while low rails are below by11.315/9.111/4.479mV.
Both rails contribute to expanded span. Equal-capacitor measured-rail prediction
has the observed wrong sign in all three cases, with approximately±2.1mV residual
unexplained. This supports a recovery problem but does not isolate amplifier,
switching or capacitor parasitics as its unique cause. Do not extrapolate the
local slopes into a settling-time guarantee or reduce sample rate to hide it.

Next circuit investigation should examine both reference drivers' recovery under
actual signed charge loads. Autonomous LO container remains live at23 minutes.
No production circuit changed; layout gate remains closed.

## Pass 642 — reference output conduction is not always restorative

Previous turn classification: progress (localized reservoir decision failures).
Added analyze_reference_restoring_current.py using the existing hash-verified,
observation-reproduced original-driver device waveform. Forward-conduction VDS
checks pass for both output/load devices at all48 rail/decision points. The
previously checked PMOS/NMOS current conventions determine net output conduction
into each rail; no claim of complete terminal current balance is made.

Each rail has restorative output conduction at only14 of24 clock observations.
At the three final-bit clocks the high rail remains6.79–7.25mV too high while
net output conduction injects another63–89uA, and its preceding0.2ns slope is
positive. Low-rail late-decision conduction also alternates between restorative
and error-increasing directions. Evidence: reference-restoring-current.json.
This is the original100ohm sample-driver fixture, not the2kohm candidate.

The result does not establish loop instability or exclude inadequate drive.
It does show that a pure shortage-of-current explanation is insufficient for
these observations. Next work should characterize feedback response under the
actual switching load, keeping compensation/displacement currents distinct
from the reported device ID. Autonomous LO container verified live at24 minutes.
No production circuit changed.

## Pass 643 — bracket reference compensation under connected SAR loading

Previous turn classification: progress (restoring-current diagnostic). Prepared
and launched sar-reference-cc-half and sar-reference-cc-double. Both use the
qualified physical-reference ±0.4V deck with the2k sample driver, original2048-unit
reservoirs, sampler and209.9ns timing. Only both reference CC parameters change:
0.5p or2p instead of1p, giving actual2pF or8pF atS=4 versus baseline4pF. Exact
reversal of the overrides and pair-cell inlining restores the baseline deck.

This brackets compensation response without assuming that faster or slower
feedback is the correct direction. Require clean terminal completion, all24
comparisons, acquisition, decision-time rail motion and final codes before
ranking. Any promising direction needs the matched±0.1V experiment before
adoption. These remain ideal compensation passives, targets/bias and external
phases; no physical capacitor, noise or stability qualification is claimed.
Both named containers and autonomous LO were confirmed live. Production cells
unchanged; pending experiments are not scored.

## Pass 644 — compensation comparison gate with regression verification

Previous turn classification: progress (launched exact-change compensation tests).
Extended check_sar_driver_physical.py with mutually exclusive --compensation.
It verifies pair-cell source hashes, both explicit CC overrides, exact baseline
reversal, immutable artifacts and completed full-waveform checks before scoring.
The existing24-decision, acquisition and per-frame comparison logic is shared;
no conclusion depends only on peak reference error. Pending report:
evidence/sar-reference-compensation.json.

Reran both original and doubled-reservoir checker modes; each regenerated its
previous report byte-for-byte, preserving downstream evidence provenance. Both
compensation containers remain live around one minute, with the half-CC run at
approximately55ns of209.9ns. Autonomous LO remains live at26 minutes. No partial
result is scored and no production circuit changes are made.

## Pass 645 — first payload-bearing connected architectural model

Previous goal turn changed the architectural priority; the subsequent wait was
verified. Added system_model/connected/chip_model.py with one timeline, both host
directions, both contract modes and real framed payloads. Complex-envelope tone
samples are quantized, packed across10-bit transport words, multiplexed alongside
wired test data and reconstructed by the actual Receiver model. Bit-exact payload,
bit conservation, independent packing example and per-component quantization
bounds pass in approximately0.6s. Evidence: connected-platform.json.

Finite256-frame tests peak at320/272bits wire/IQ ingress for Ethernet mode and
520/88bits for PCIe mode. One final IQ sample remains split across incomplete
transport/sample words in each case and is accounted for explicitly. This is not
a complete burst-delivery or infinite queue-stability claim. Paced DAC/serializer
consumption, autonomous clocks, independent domains, analog dynamics and mode
control remain missing. complete_architecture=false is explicit. Prioritize these
connections before further isolated transistor tuning.

Both reference-compensation SPICE runs finished and passed artifact/exact-change
checks: half CC yields77/180/77; double CC78/178/78, versus baseline77/179/76 and
ideal76/179/76. Neither candidate improves selected large-input accuracy; neither
is adopted. Autonomous LO container remains live at30 minutes.

## Pass 646 — transmit consumption deadlines and underflow controls

Previous turn classification: progress (first connected payload model). Added
paced_sink to the connected platform, merging actual reconstructed-word arrivals
with rational independently scheduled consumption deadlines. No late sample can
retroactively meet a deadline; boundary-order controls pass. DAC sample and
serializer-word sinks run with a fixed two-frame prefill. Zero-prefill controls
produce underflows in all four stream/mode combinations, confirming timing errors
are visible rather than hidden by eventual payload equality.

The625-frame aligned tests consume every transmitted sample, preserve values and
exact deadlines, and drain with zero underflows. Ethernet wire/IQ output FIFO
peaks34/11samples (340/264bits); PCIe53/5samples (530/80bits). Prefill delays are
512ns and409.6ns. These are ideal matched-clock finite tests with immediate frame
preparation, not required hardware FIFO depths or RTL timing qualification.
Arbitrary finite burst flushing remains unresolved;625frames deliberately ends
on sample/transport boundaries. DAC reconstruction, serial-bit channel response,
autonomous clocks, mode transitions and analog dynamics remain open.

## Pass 647 — connected RF filtering and DAC hold response

Previous turn classification: progress (paced transmit sinks). Added rf_blocks.py
with exact continuous-time one-pole tone startup and piecewise-held DAC response.
RX filter output now feeds actual quantization/packing/host delivery. TX delivered
samples drive reconstruction at verified consumption deadlines. Both filters use
explicit hypothetical10MHz bandwidth; no transistor calibration is claimed.

Analytic DC response and hold-subdivision invariance controls pass, as do the
existing full payload, conservation, deadline and underflow tests in both modes.
Quantization error remains within the componentwise converter bound; stable
reconstruction cannot amplify its complex maximum beyond the corresponding bound.
RX is currently an analytic single-tone fixture, not arbitrary waveform support.
The complete mathematical architecture remains unfinished: recovered clocks,
serial channel, mode/calibration state, independent rates and nonideal sweeps
still need to be connected. No production transistor cell changed.

## Pass 648 — wired payloads traverse a serial channel

Previous turn classification: progress (RF envelope dynamics). Added stateful
LSB-first serialization and exact one-pole channel evolution with mid-bit
sampling. Connected actual wired payloads in both directions, at both contract
rates. RX waits a full word interval before releasing data to the host queue;
TX receives words at previously verified consumption deadlines. Whole-path
payload checks and paced sinks pass. Analytic constant-input/sign controls pass;
a low-bandwidth alternating-pattern control produces errors as expected.

Channel bandwidth equals bit rate as an explicit hypothetical scenario. Sampling
phase remains prescribed, so no recovered-clock or acquisition claim is made.
Word alignment, jitter, independent rates, mode control and calibrated analog
behavior remain open. complete_architecture remains false.

## Pass 649 — exact-crossing recovered-clock adapter

Previous turn classification: progress (wired channel connection). Added
connected/recovered_clock.py around the existing transition-only PI tracker,
using analytic channel zero-crossing times. Independent two-symbol crossing and
sign-reversal controls pass. Eight cases cover both rates, ±0.3UI initial phase
and ±100ppm initial period. Each has7168 tested bits after a1024-bit acquisition
interval, zero errors and zero omitted clock indices. Evidence:
connected-clock-adapter.json. Runtime approximately0.22s.

This is preparation for integration, not completed autonomous architecture.
The adapter is not yet driving host queues; no lock detector or word-framing
preamble exists. Ideal timestamp detection and near-nominal oscillator remain
assumptions. Next connect final recovered-bit times to word availability and
replace implicit word alignment with an explicit training/framing mechanism.

## Pass 650 — recovered RX words drive the host queue

Previous turn classification: progress (clock adapter verification). Connected
transition-driven RX recovery to host transport.1024 training bits plus a64-bit
marker establish framing by scanning observed decisions; true clock indices do
not select payload boundaries. Host availability follows the actual last recovered
bit of each word. Active-time origin follows the training interval, explicitly
separate from tracker feedback. All20000/32000 wired words reach the host correctly
in Ethernet/PCIe-rate modes with+0.3UI and+100ppm initial oscillator error, alongside
RF traffic. Existing RF, pacing and underflow controls still pass.

This marker is a test-link convention, not protocol-specific alignment. Missing
marker currently fails the test rather than driving a retraining state machine.
Shared startup, lock loss, independent host/RF clocks, configuration and calibrated
nonidealities remain incomplete. No silicon feasibility or BER claim is made.

## Pass 651 — explicit framing state and reset

Previous turn classification: progress (recovered RX integration). Replaced
whole-array marker slicing with a sequential SEARCH/PAYLOAD/FAULT receiver in
connected/framing.py. The actual connected path emits a word only upon its tenth
received payload bit.2048 observed bits without a marker latch fault; explicit
reset clears partial data and requires reacquisition. Missing marker, corrupted
marker, fault latching and reset midway through a word controls pass. Both modes'
complete payload/timing tests remain passing.

Observed-bit timeout does not detect absent clocks, and marker acquisition does
not prove clock lock. Loss-of-lock during payload, automatic retraining and shared
configuration/reset state remain open. The long autonomous LO container was
verified live at37 minutes. No production circuit changed.

## Pass 652 — signed clock offsets through the full connected path

Previous turn classification: progress (framing/reset states). Parameterized the
connected platform's initial wired phase and period and added --clock-sweep.
All eight mode/phase/period combinations pass: two contract modes, ±0.3UI and
±100ppm. Each case exercises received-marker acquisition, final-bit-timed word
release, host multiplexing with RF payloads, sample reconstruction and paced
transmit sinks. Evidence: connected-platform-clock-sweep.json. Complete finite
payload identity and no-underflow assertions pass, rather than only standalone
clock detector metrics.

This tests initial oscillator error corrected by feedback. It does not solve
persistent independent source/sink clock mismatch; finite FIFOs alone cannot
absorb that indefinitely. Host/DAC clock ownership and rate control remain a
high-level architectural obligation. The autonomous LO simulation was verified
live at38 minutes and approximately3.154us of8.001us. No production cell changed.

## Pass 653 — architectural latency correction exposes underflow

Previous turn classification: progress (connected clock-offset sweep). Reviewing
clock ownership exposed a concrete transport-model inconsistency: implemented
snapshot staging sends captured data in the following frame, while the new model
sent it immediately. Corrected the connected frame pipeline to transmit the prior
snapshot. The previous two-frame prefill then fails the actual underflow assertion.

Three-frame prefill passes both nominal connected modes; retained two-frame IQ
underflow and zero-prefill negative controls ensure this regression stays visible.
Evidence: refreshed connected-platform.json. This corrects a high-level optimistic
assumption; it does not establish the remaining RTL/CDC delays or long-term clock
rate matching. Prior clock-sweep report belongs to the earlier source hashes and
must not be used as verification of this revision. Persistent host/DAC source-rate
ownership remains an unresolved architectural obligation.

## Pass 654 — correct RF sample encoding and document clock obligations

Previous turn classification: progress (snapshot staging correction). Source
review found a second architectural mismatch: the transport specifies signed
two's-complement I/Q, while the connected model used offset binary at both ends.
Corrected8/12-bit encoding and decoding, with explicit independent vectors for
negative values, zero and clipping. Tightened unclipped tone quantization bounds
to half a signed converter step. All eight complete clock-offset cases pass with
both corrected staging and signed encoding; clock-sweep evidence is refreshed.

Added spec/clock-rate-ownership.md distinguishing GPIO capture clocks from
payload production/consumption clocks. Current v2 has no consumer credits or
timestamps, and raw data cannot silently be inserted/dropped to absorb drift.
Sustained TX rate control remains open; source-matched consumption in the current
model is an assumption, not proof. No hidden pins or free control slots are added.

## Pass 655 — host RF-mute commands affect the connected signal path

Previous turn classification: progress (signed encoding and clock obligations).
Connected existing opcode2 RF mute/unmute through snapshot staging, validated
Receiver metadata and next-DAC-sample application. Tests send mute/unmute at
frames100/200, verify acceptance at frames101/201 word4, and verify zero DAC
samples throughout the mute interval while queue consumption and deadlines
continue. The reconstruction filter retains state. Both modes retain complete
payload and no-underflow results. Corrupted mute metadata faults before emitting
any command. Evidence: refreshed connected-platform.json.

This candidate behavioral rule does not qualify hardware command CDC/latency.
Shared mode transitions, calibration and sustainable host-to-TX rate control
remain unresolved; the new control path does not excuse those obligations.

## Pass 656 — finite transmit FIFOs expose signed rate mismatch

Previous turn classification: progress (RF command path). Added explicit egress
capacity and overflow accounting to the paced sink used by both actual connected
TX paths. Capacity derives from the existing provisional2048-bit budget, rounded
to whole source items. Both nominal modes pass with no underflow/overflow and
complete payload conservation. No dropped data is silently counted as delivered.

Signed stress controls on the same sink use10000 arrivals, eight slots and four
periods prefill:−1000ppm consumer gives six overflows; +1000ppm gives six underflows
before the input ends; matched rate has neither. This is a model fault-detection
control, not a chip tolerance or full connected drift qualification. Sustained
rate matching still needs an explicit architectural mechanism. The autonomous
LO process was verified live at44 minutes. No production circuit changed.

## Pass 657 — explicit host payload pacing from forwarded clock

Previous turn classification: progress (finite FIFO and signed drift controls).
Implemented FPGA-side RationalPacer in the connected path. Host TX generation
now uses integer phase accumulators driven by chip-forwarded word ticks, rather
than querying each analog consumer's exact next deadline. Explicit ratios are
wire/IQ1/2 and4/25 in Ethernet mode,4/5 and8/125 in PCIe mode. They are checked
against contract rates.10000-tick controls prove bounded sub-sample count error
and exact counts at every denominator-length interval. Both nominal connected
modes pass all payload, FIFO, timing and RF-command checks.

Selected this as the baseline mathematical clock-ownership candidate in
spec/clock-rate-ownership.md. It requires local chip TX/sample clocks to maintain
the same average reference ratios, and does not require extra pins or credits.
Independent H2D service timing/CDC, actual synthesis, reference loss and modes
with intentionally independent/spread clocks remain unverified. It is a concrete
rate-matching mechanism under explicit assumptions, not physical clock closure.

## Pass 658 — independent H2D service clock with reference-paced payloads

Previous turn classification: progress (rational host pacing). Replaced the single
host-loop tick with merged rational D2H/H2D event streams. Frame position/sequence
advance on each direction's own service clock; H2D source generation advances on
D2H-reference edges, and sample consumption remains on the local reference ratio.
RF mute observations follow actual H2D header times. Added --host-clock-sweep.

All eight complete cases pass: both modes, H2D±100ppm and phase0/1⁄3 reference tick.
Payload conservation, bounded configured egress, no underflow/overflow, command
application and RF quantization checks remain enabled. Evidence:
connected-platform-host-clock-sweep.json. This separates service-clock drift from
payload-rate drift without extra control bandwidth. It is still ideal CDC queue
visibility with no synchronizer delay or metastability model; fixed finite tests
do not bound all stalls or drift. Analog clock-ratio accuracy remains assumed.

## Pass 659 — delayed cross-domain payload visibility

Previous turn classification: progress (separate host service clocks). Added
configurable reference-to-H2D visibility delay, expressed in destination-clock
periods with rational-time floor handling. Default is two periods; before the
first eligible reference edge, no payload enable is visible. Reference pacing
itself remains unchanged. All eight host frequency/phase cases pass with finite
queues, no underflows/overflows, complete payloads and RF commands. Evidence:
refreshed connected-platform-host-clock-sweep.json.

This selected delay is a sensitivity assumption, not a metastability bound or
verified synchronizer. Variable CDC delay and return paths remain open. Updated
report limitations to distinguish the now-tested independent H2D service clock
from still-unverified physical clock ratios/CDC. Autonomous LO container remains
live at49 minutes, approximately4.053us of8.001us; no partial result is scored.

## Pass 660 — host-driven RF conversion-chain loopback

Previous turn classification: progress (delayed cross-domain visibility). Added
exact equal-pole continuous cascade propagation from actual delivered/paced/muted
DAC samples through external attenuation and RX filtering to ADC codes. Both
modes pass at amplitude transmission0.25 and1; combined quantization error stays
within the derived stable-filter bound. Independent DC cascade formula, zero
attenuation and hold-subdivision controls pass. Existing transport/timing tests
remain passing. Evidence: refreshed connected-platform.json.

This connects the conversion chain through the ADC, but the resulting ADC samples
are not yet routed back through D2H. They are an additional observation alongside
the independent RX tone fixture. Both poles are hypothetical10MHz; equal LO,
aligned ADC sampling and noiseless linear behavior remain assumptions. No
whole-chip completion or silicon feasibility is claimed.

## Pass 661 — close the RF host-to-host feed-forward path

Previous turn classification: progress (RF conversion-chain observation). Added
loopback_return.py and connected actual ADC loopback samples to staged D2H
transport with simultaneous recovered wired data. Queue entry uses actual ADC
hold-end times. Both modes and attenuation0.25/1 return all6400/2560I/Q pairs and
20000/32000wired words exactly, with no residual packed bits or undrained queues.
Per-source ingress limits1024bits pass; peaks are320/272bits for Ethernet wire/IQ
and520/88bits for PCIe. Maximum ADC-to-host transport delays are140.75/154.125
reference-word ticks, respectively. Existing TX pacing and RF bounds still pass.

This closes the feed-forward mathematical host→RF→host graph under the stated
ideal-clock, linear/noiseless envelope assumptions. Absolute-time replay does
not establish shared-supply coexistence, arbitrary-waveform RF performance or
feedback-loop stability. Complete architecture remains false pending startup,
mode/calibration control, loss/retraining and remaining nonidealities.

## Pass 662 — signed LO mismatch inside the RF conversion chain

Previous turn classification: progress (RF host-to-host loop closure). Added exact
continuous LO rotation between TX reconstruction and RX filtering, with zero and
±100kHz offsets at each external attenuation. All12 mode/attenuation/offset paths
return samples through D2H alongside wired traffic. Combined converter-error
bounds are checked against the corresponding mismatched analog reference;
uncorrected error versus the zero-offset target is separately reported.

At unity external amplitude transmission, zero-offset RMS errors are0.000209/
0.004111 for the two converter-width modes, while ±100kHz produces approximately
0.775–0.780 normalized RMS deviation from the intended tuned waveform. Thus a
successful transport/quantization check is not an RF frequency-recovery success.
Controls include signed conjugacy, hold-subdivision invariance and independent
numerical quadrature of a continuous DC-step/mixer response. All pass.

Offsets are sensitivity scenarios, not measured oscillator bounds. Carrier
estimation/correction remains external FPGA modem work and is not modeled yet;
intrinsic phase noise, I/Q imbalance, nonlinearities and shared supply remain open.

## Pass 663 — external pilot correction from actual returned samples

Previous turn classification: progress (signed LO mismatch). Added pilot.py as
explicit external FPGA/host functionality. It receives the actual D2H-decoded
loopback ADC words, estimates tone offset and complex gain from samples64–255,
then freezes both estimates while scoring the last1024 samples. Simulated LO
offset is not supplied to the estimator. Pure-tone signed-offset controls pass;
a frequency change confined to the test tail deliberately produces large error.

Across both modes, attenuation0.25/1 and offsets0/±100kHz, estimated frequency
error is at most24.84Hz. Held-out normalized RMS error spans approximately
0.000394–0.025826. These are finite noiseless single-tone calibration results,
not Wi-Fi EVM, general modem recovery or phase-noise qualification. Pilot and
held-out intervals deliberately avoid startup and commanded mute; arbitrary
burst detection/acquisition is not implemented. All connected transport and
converter checks remain passing. Evidence: refreshed connected-platform.json.

## Pass 664 — explicit lifecycle prerequisites and independent engine faults

Previous turn classification: progress (external pilot estimation). Added a
candidate Session lifecycle to the connected run: configuration/host readiness
precede arm; mode remains frozen while armed; RF and wired readiness/fault states
are independent. The wired readiness input uses the observed marker acquisition
time before payload epoch, not future payload success. RF-lock and host-training
readiness are explicitly ideal external inputs, not implemented acquisition.

Controls reject unconfigured arm/live mode changes, preserve RF operation during
wired fault, require engine reset and new readiness after fault, and clear global
readiness on reset before selecting the other mode. Both complete nominal signal
paths remain passing. These are lifecycle-rule controls, not a midstream fault
recovery test of every FIFO/filter. Flush sequencing, physical lock detection,
shared startup and dynamic retraining remain incomplete. No layout gate changes.

## Pass 665 — transition starvation exposes recovered-data corruption

Previous turn classification: progress (lifecycle prerequisites). Added a received-
clock holdover screen with constant-zero runs followed by a changing256-word tail.
Both initial phase/period sign cases preserve payload after640 and40960 constant
bits, but both corrupt the tail after327680 constant bits. Received word count
still matches33024, so count-only verification would miss the failure. Evidence:
connected-clock-holdover.json. No physical holdover bound is inferred from these
noiseless, fixed-model cases, and no missing-transition lock detector is adopted.

The test exposed platform.py shadowing Python's standard library and causing a
circular NumPy import. Renamed the main entry point to connected/chip_model.py
and updated command documentation. Full nominal connected tests pass under the
new name and refreshed source hashes. Historical reports retain their earlier
source identities and must not be mistaken for current-revision verification.

Recovered raw-data operation requires explicit transition-density/encoding or
alternate timing assumptions. Long-run failures are not fixed by preserving
word counts or merely extending a timeout. General retraining/word realignment
remains incomplete; no protocol compliance or unlimited raw holdover is claimed.

## Pass 666 — bounded-transition controls survive the same long duration

Previous turn classification: progress (transition-starvation failure and import
fix). Added transition_density.py with327680-bit randomized payloads bounded to
runs of5 or64bits, independently checking the actual maximum run. The unchanged
channel/CDR/framing adapter recovers all32768 words in all eight cases: two rates,
two run bounds and paired±0.3UI/±100ppm initial offsets. Evidence:
connected-transition-density.json. This contrasts with same-length constant-run
failure without changing the recovery algorithm or hiding the failed payload.

Recorded external transition-density obligations in clock-rate-ownership.md;
no on-chip coding or extra bandwidth is assumed. These are artificial run-limited
patterns, not protocol-valid Ethernet/PCIe data or a physical holdover guarantee.
Noise/jitter/wander and full host/RF integration are outside this adapter screen.
Autonomous LO container was verified live at58 minutes. No production cell changed.

## Pass 667 — reassess architectural coverage and evidence age

Previous turn classification: progress (bounded-transition comparison). Added
check_connected_evidence_freshness.py. Current nominal, holdover and bounded-run
reports match their recorded source identities; previous complete-path wired/
host clock sweeps are stale after subsequent code revisions. The audit separates
source currency from functional coverage and does not mark any gate complete.

Updated risk-priorities.md around the connected mathematical architecture.
Highest next gap is a real midstream reset/retraining episode propagated through
actual queues/partial words/staging/filter state, with unaffected-engine traffic
and explicit discard accounting. Lifecycle predicates alone do not establish it.
Variable CDC/clock faults and calibrated RF noise/nonlinearity/coexistence follow.
Physical LO/ADC/CDR risks remain unresolved; no new local analog tuning is launched.
Autonomous LO container was verified live at59 minutes. Layout remains gated.

## Pass 668 — partial-word reset and reacquisition with RF continuity

Previous turn classification: progress (coverage/evidence reassessment). Added
reset_retraining.py using actual recovered decisions, Framer state and staged
shared D2H transport. A testbench reset after37 received payload bits discards
exactly seven partial bits; three completed words are retained. A second training
sequence/marker reacquires framing, and all256 subsequent words are recovered.
In both modes, all259 accepted wired words and250 uninterrupted RF sample pairs
reach the host with correct order, finite ingress occupancy and drained queues.
Evidence: connected-reset-retraining.json.

Scope is intentionally precise: CDR and channel state continue, so this is a
framer reset rather than complete wired-engine reset. Complete queued words drain;
no shared transport reset or RF reset is invoked. Triggering is an external test
request, not fault detection. Analog RF continuity, complete tracker reset and
host-visible epoch/discard status remain missing. This is one executed stage of
the reset/retraining obligation, not closure of the full lifecycle gate.

## Pass 669 — restart timing recovery while preserving channel state

Previous turn classification: progress (partial-framer reset). Added receive_reset
and reset_retraining.py --reset-clock. The reset time follows the37th observed
payload bit; the timing tracker restarts with opposite initial phase/period offsets
while the continuous channel response is retained. Exact prefix checks prove
that scheduling the later reset does not change earlier sample times or decisions.

Both modes reacquire the second marker and recover all256 later words, retain
three completed pre-reset words, discard exactly seven partial bits and preserve
250 RF sample pairs through shared transport. Original framing-only tests also
still pass. Evidence: connected-clock-reset-retraining.json and refreshed
connected-reset-retraining.json. This resets mathematical tracker/framer state,
not an analog oscillator/pad; automatic fault detection and host epoch/discard
status are still absent. Full startup/reset closure remains unproven.

## Pass 670 — coherent reset-boundary snapshot for the host model

Previous turn classification: progress (tracker/framer restart). Existing SPI
status lacks a reset epoch/boundary record. Added an abstract sideband ResetStatus
with32-bit saturating epoch,64-bit saturating accepted-word count, last reset
boundary, partial-bit discard count and overflow flag. Atomic snapshot stays
unchanged across later events; overflow latches instead of silently wrapping.
This is a candidate API, not an allocated register/RTL/CDC implementation.

Clock-reset tests in both modes now let the host locate the discontinuity using
snapshot epoch1/boundary3/discard7: the first three accepted words precede reset,
all256 later words follow reacquisition, and250 RF samples remain intact. No
stream bits, pins or payload slots are added. Complete accepted words must drain
without loss for this counting convention to hold. A host missing multiple resets
cannot reconstruct every boundary from the last-event snapshot alone; an epoch
jump must invalidate the ambiguous interval or require an event log/re-arm policy.
Evidence: refreshed connected-clock-reset-retraining.json.

## Pass 671 — reject ambiguous reset histories at the host

Previous turn classification: progress (coherent reset snapshot). Added
HostResetObserver, starting from a jointly established stream/count origin.
Consecutive epochs expose one reset boundary; repeated identical snapshots are
idempotent. Skipped epochs, rollback, inconsistent repeated-epoch snapshots or
counter overflow latch a host fault requiring explicit re-arm. The latest snapshot
is not treated as a complete event log. Controls cover duplicate observations,
missed resets, rollback and overflow.

Both full tracker/framer-reset transport tests now consume status through this
host observer and still recover the correct boundary3/discard7 while preserving
all accepted wire/RF data. Snapshot SPI transport and a coordinated re-arm protocol
remain unimplemented; host fault latching does not itself perform recovery.
Autonomous LO container was revalidated live at about one hour. No new analog
experiment or production change was made.

## Pass 672 — modulated RF payload through the complete host loopback

Previous turn classification: progress (host reset observer). Added --modulated
with a512-sample pilot followed by deterministic QPSK symbols held for eight
samples. Host source values traverse H2D, paced DAC, mute control, TX filter,
external attenuation, signed LO offset, RX filter, ADC and staged D2H return.
The external estimator freezes carrier/gain estimates from the early pilot;
last1024 samples contain128 scored QPSK symbols. All12 mode/attenuation/offset
cases recover every scored symbol. Evidence: connected-platform-modulated.json.

A synthetic clean-QPSK control passes and flipping the final sampled symbol
produces exactly one detected symbol error. Symbol timing is supplied by the
fixture; no blind timing recovery, coding, packet detection, Wi-Fi compliance,
noise or nonlinear-channel guarantee is claimed. QPSK operates at5/2.5Msymbol/s
in the40/20MS/s modes, not at full20MHz Wi-Fi occupied bandwidth. The autonomous
LO container remains live, last observed at approximately5.440us of8.001us.

## Pass 673 — receiver I/Q mismatch through the complete modulated path

Previous turn classification: progress (QPSK loopback). Added constant receiver
I/Q mismatch before ADC conversion: I gain1+g, Q gain1−g plus phase-induced I/Q
cross-coupling. Tested paired(g,phase)=(−0.1,−5deg) and(+0.1,+5deg) through both
modes, two attenuations and three LO offsets. Matrix identity, explicit gain and
90-degree phase controls pass. Converter-error bounds include the mismatch
matrix norm; transport payload checks remain enabled.

Each sign case returns1536 scored symbols across12 scenarios with zero symbol
errors, but pilot frequency estimate bias grows to approximately377/352Hz versus
at most25Hz in the balanced model. This exposes sensitivity despite correct
quadrant decisions. Evidence: connected-platform-iq-error--1.json and
connected-platform-iq-error-1.json. These are selected paired signs, not all
independent gain/phase combinations, physical mismatch bounds, EVM qualification
or full-bandwidth modem tests. No I/Q calibration is silently fitted.


### Pass 674 — independent I/Q errors and held-out waveform fidelity

Decoupled receiver gain/phase mismatch signs and measured normalized RMS/peak
QPSK error after frozen pilot correction. Both opposite-sign cases and balanced
model pass transport checks in both modes (12 RF scenarios/run, 1,536 symbols,
zero decision errors/run). Worst normalized RMS error is 12.34% balanced,
29.92% (-10%,+5 degrees), and 33.62% (+10%,-5 degrees). Thus decision success
conceals meaningful distortion; this is not modem/EVM qualification. Added
analytic zero-error and single-inverted-symbol controls. Reports are
connected-platform-modulated.json and connected-platform-iq-gain-*-phase-*.json.
Updated connected-model documentation to distinguish current coverage from old
chronological notes. Entire architecture remains incomplete; next priorities
are RF fidelity/calibration, timing noise and full-state lifecycle behavior.
The autonomous LO SPICE container remains running; no result claimed or restart.


### Pass 675 — attribute RF waveform error without hiding it

Previous turn: progress. Added an orthogonal projection of corrected QPSK
onto known held-out symbols, separating constant complex-gain bias from
nonscalar distortion. This is a truth-assisted diagnostic only; operational
pilot fitting and symbol decisions are unchanged. Error energy must equal
scalar-bias energy plus residual energy. Analytic uniform-gain and inverted-
symbol controls pass. Both modes pass balanced and (+10%,-5 degree) mismatch
runs, 12 scenarios/run, zero symbol errors; the full waveform error remains.

connected-platform-modulated.json: scalar-bias RMS 0.037785–0.123379; nonscalar residual RMS 0.000008–0.021825.

connected-platform-iq-gain-1-phase--1.json: scalar-bias RMS 0.000740–0.316997; nonscalar residual RMS 0.108061–0.116980.

The balanced case is dominated by scalar bias: a 1MHz tone's fitted gain/phase
is not automatically appropriate for settled QPSK symbol centers after the
filters. Mismatch also leaves a larger nonscalar residual. Next test a separate
known-symbol calibration preamble with frozen parameters and disjoint scoring,
then widen waveform bandwidth and add clock/noise stresses. The diagnostic
projection itself must never be reported as achieved receiver performance.
Autonomous LO container verified live; no restart or physical result claimed.
Architecture completion and layout gates remain open/incomplete.


### Pass 676 — frozen known-symbol calibration

Previous turn: progress. Reserved 64 known QPSK symbols before the 128 scored
symbols for an external-host scalar calibration. Frequency correction remains
frozen from the initial tone. Training and scoring indices are disjoint; both
modes' training starts after mute release, verified against reported events.
Analytic controls establish correction of a persistent scalar change and that
changes confined to scored data cannot change the calibration coefficients.
The latter change remains visible as error rather than being silently refitted.

connected-platform-modulated.json: calibrated held-out RMS 0.000021–0.022551; symbol errors 0.

connected-platform-iq-gain-1-phase--1.json: calibrated held-out RMS 0.111717–0.118636; symbol errors 0.

Both end-to-end modes pass transport and conversion checks in the balanced and
(+10%,-5 degree) scenarios. This calibration uses received sample words and
known preamble symbols; it assumes supplied symbol timing and reserves training
airtime. It is external FPGA functionality, not added chip modem logic. It
does not estimate the I/Q matrix, correct frequency-estimate drift, or establish
full-bandwidth Wi-Fi performance. Next priority: model frequency/image imbalance
separately before assuming a static post-derotation correction can fix it.
Autonomous LO container remains live. No tapeout or layout gate advanced.


### Pass 677 — verify I/Q correction ordering

Previous turn: progress. Added connected/iq_rotation_screen.py and evidence/
connected-iq-rotation.json. Independently derived y=a*z+b*conjugate(z) matches
the real I/Q matrix for both independent gain and phase signs. After frequency
correction, the image becomes b*conjugate(x)*exp(-j*2*omega*t), so its effective
coefficient rotates at twice the offset. A fixed post-derotation matrix is the
wrong general correction model. Disjoint training/scoring on synthetic QPSK
confirms approximately 11% residual at either signed 100kHz offset, despite
exact recovery at zero offset. An exact known-parameter inverse before frequency
correction recovers all 12 cases to floating-point precision (maximum RMS
3.34e-16). This is an ordering/control result, not a fitted receiver result.

Next estimator should identify direct and image terms before derotation, or
include the rotating conjugate basis. Keep estimates frozen for held-out tests
and test wrong/no calibration controls. Do not claim this synthetic memoryless
result solves filter mismatch, nonlinearities, clock noise, or RF bandwidth.
The connected waveform remains a narrow QPSK fixture; expand bandwidth after
this structural calibration issue, rather than indefinitely polishing it.
Autonomous LO container verified live. No circuit/layout gate advanced.


### Pass 678 — estimate rotating image from reserved symbols

Previous turn: progress. External-host estimator now fits direct and rotating
conjugate coefficients using only the same 64 reserved symbols. Frequency stays
frozen from the initial pilot; held-out samples never update coefficients.
Invertibility and training rank are checked. Original uncorrected/scalar scores
remain visible alongside image-corrected scores. Analytic controls verify exact
zero-offset image recovery and invariance of fitted coefficients to changes
confined to held-out data; those changes still produce visible scoring errors.

connected-platform-modulated.json: image-calibrated RMS 0.000021–0.022883; errors 0; maximum basis condition 1.0111.

connected-platform-iq-gain-1-phase--1.json: image-calibrated RMS 0.014944–0.032330; errors 0; maximum basis condition 1.0108.

Both modes pass connected transport/conversion checks. This tests balanced and
(+10%,-5 degree) mismatch only, both attenuation levels and all three LO offsets.
It does not establish robustness to unseen mismatch, ill-conditioned training,
noise or time-varying impairments. Residual frequency bias from the tone fit can
still accumulate after calibration. Next broaden signal bandwidth and impairment
stresses rather than assuming narrow, settled QPSK symbols qualify the radio.
Autonomous LO container verified live; no restart. No physical gate advanced.


### Pass 679 — increase connected waveform bandwidth

Previous turn: progress. Added --symbol-samples 2/4/8 to the connected stimulus
and external-host estimator. Training and scoring keep fixed sample durations;
training symbol count therefore increases with rate. Both modes run through
unchanged DAC holds, two 10MHz poles, ADC and transport. Stress runs report
symbol errors rather than requiring favorable outcomes; payload/conversion
invariants remain asserted. Existing analytic calibration controls still pass.

ethernet_rf40_12, 10 Msymbol/s: maximum calibrated RMS 0.013719, symbol errors 0.

pcie_rf20_8, 5 Msymbol/s: maximum calibrated RMS 0.022408, symbol errors 0.

ethernet_rf40_12, 20 Msymbol/s: maximum calibrated RMS 0.204119, symbol errors 0.

pcie_rf20_8, 10 Msymbol/s: maximum calibrated RMS 0.027329, symbol errors 0.

Evidence: connected-platform-symbols-{2,4}-gain-0-phase-None.json. This is
unshaped QPSK with supplied timing, not a spectrally compliant Wi-Fi waveform.
Increasing symbol rate alone does not establish occupied-channel bandwidth,
OFDM performance or noise tolerance. No equalizer is fitted beyond the existing
memoryless direct/image coefficients. Next isolate pulse-memory distortion
from frequency bias and test a bandwidth-defined waveform before claiming the
RF budget supports 20MHz operation. Autonomous LO container remains live.
Architecture completion and physical gates remain unproven.


### Pass 680 — bandwidth-defined multicarrier RF screen

Previous turn: progress. Added connected/multicarrier_screen.py using the same
DAC/ADC codec and exact two-pole held-input RF response. Fifty-two active
subcarriers extend to +/-8.125MHz at 312.5kHz spacing; cyclic prefix is 0.8us.
A fixed 0.18 complex RMS scale avoids per-record peak normalization. Peaks stay
below clipping. Eight training blocks fit per-bin response; 30 disjoint blocks
(1,560 QPSK symbols/case) are scored without refitting. FFT identity and a
held-out-only sign-inversion control pass; all four cases have zero errors.

At 20MS/s, 8 bits/component: normalized RMS error is 8.55% at attenuation
0.25 and 2.64% at unity. At 40MS/s, 12 bits/component: 0.398% and 0.120%.
Minimum measured normalized response is approximately 0.67–0.69 and 0.58,
respectively. These are sampled hold/filter responses, not continuous-time
filter measurements. Rates and resolutions change together, so this comparison
does not isolate the effect of either parameter. Evidence is
evidence/connected-multicarrier-screen.json.

This is a supporting RF screen, not yet integrated host/wired concurrency.
Finite rectangular multicarrier blocks have sidelobes; active-bin span does not
prove a spectral mask. No Wi-Fi compliance, noise, LO error, I/Q imbalance or
achieved converter resolution is implied. Next connect this waveform through
the host path and stress noise/headroom; do not substitute favorable 12-bit
math results for the unresolved physical ADC. Autonomous LO verified live.
No physical/layout gate advanced.


### Pass 681 — multicarrier ADC return with concurrent wired recovery

Previous turn: progress. The multicarrier screen now obtains its mode rates from
the contract and routes actual quantized ADC words through return_samples, with
continuous bit packing, finite ingress limits, previous-frame staging and host
decoding. Concurrent wired payload passes transition-driven clock recovery and
marker framing before entering the same return transport. External equalization
and scoring consume decoded host words. All ADC pairs and wired words arrive
in order, queues drain and no payload differs.

pcie_rf20_8, attenuation 0.25: 3200 ADC pairs, 40000 wired words, peak ingress {'wire': 520, 'iq': 88} bits, maximum ADC delay 154.000 host ticks, RMS 0.085518.

pcie_rf20_8, attenuation 1: 3200 ADC pairs, 40000 wired words, peak ingress {'wire': 520, 'iq': 88} bits, maximum ADC delay 154.000 host ticks, RMS 0.026437.

ethernet_rf40_12, attenuation 0.25: 6400 ADC pairs, 20000 wired words, peak ingress {'wire': 320, 'iq': 272} bits, maximum ADC delay 140.750 host ticks, RMS 0.003980.

ethernet_rf40_12, attenuation 1: 6400 ADC pairs, 20000 wired words, peak ingress {'wire': 320, 'iq': 272} bits, maximum ADC delay 140.750 host ticks, RMS 0.001203.

RMS scores match the prior direct-ADC screen, as expected for bit-exact return.
The evidence source inventory includes transport, contract and timing dependencies.
This completes only the return-path integration: multicarrier H2D generation
still bypasses framing/pacing, and LO/noise/mismatch remain ideal in this screen.
Next add the actual host-to-DAC path without silently changing sample deadlines
or claiming full-chip closure. Autonomous LO container verified live. No
physical/layout gate advanced.


### Pass 682 — framed multicarrier host-to-DAC path

Previous turn: progress. Added optional decoded sample timestamps to the common
framing replay helper. The multicarrier host source now traverses framing,
previous-frame staging and chip decoding alongside wired TX words. Decoded RF
pairs feed finite DAC queues at exact rational consumption deadlines, with
three-frame prefill. The analog response uses actual DAC consumption times and
ADC return timestamps include this absolute delay. Both modes preserve every
TX sample with zero underflow/overflow; zero-prefill controls fail as expected.

pcie_rf20_8, attenuation 0.25: DAC FIFO peak 5 pairs; zero-prefill underflows 10; scored RMS 0.085518.

pcie_rf20_8, attenuation 1: DAC FIFO peak 5 pairs; zero-prefill underflows 10; scored RMS 0.026437.

ethernet_rf40_12, attenuation 0.25: DAC FIFO peak 11 pairs; zero-prefill underflows 23; scored RMS 0.003980.

ethernet_rf40_12, attenuation 1: DAC FIFO peak 11 pairs; zero-prefill underflows 23; scored RMS 0.001203.

Both RF directions are now framed in this multicarrier test. The reused helper
retains D2H-oriented field names internally; H2D data is explicitly reported as
host_transmit. This is shared-reference feed-forward replay, not independent
CDC or feedback simulation. Wired TX words are framed/decoded but not passed
through a serializer here; wired RX still uses transition-based recovery.
No mute/reset/acquisition/noise/LO-error qualification is claimed. Next add
noise/headroom stress to the bandwidth-defined chain and reassess priorities
against physical timing/converter evidence. Autonomous LO container verified
live; no physical/layout gate advanced.


### Pass 683 — ADC-input noise through the framed multicarrier chain

Previous turn: progress. Added --noise-rms with reproducible IID complex Gaussian
noise before ADC quantization. Its absolute RMS does not scale with external
attenuation; training and scoring both see noise, and coefficients stay frozen
for scoring. The same realization is reused for attenuation comparisons. Noise
power, clipping counts, symbol errors and full transport checks remain visible.
Re-ran zero noise plus 0.01 and 0.03 complex RMS in normalized component full-
scale units; these are sensitivity cases, not measured physical noise limits.

noise 0, pcie_rf20_8, attenuation 0.25: RMS 0.085518, symbol errors 0/1560, clipped pairs 0.

noise 0, pcie_rf20_8, attenuation 1: RMS 0.026437, symbol errors 0/1560, clipped pairs 0.

noise 0, ethernet_rf40_12, attenuation 0.25: RMS 0.003980, symbol errors 0/1560, clipped pairs 0.

noise 0, ethernet_rf40_12, attenuation 1: RMS 0.001203, symbol errors 0/1560, clipped pairs 0.

noise 0.01, pcie_rf20_8, attenuation 0.25: RMS 0.267394, symbol errors 0/1560, clipped pairs 0.

noise 0.01, pcie_rf20_8, attenuation 1: RMS 0.068151, symbol errors 0/1560, clipped pairs 0.

noise 0.01, ethernet_rf40_12, attenuation 0.25: RMS 0.187071, symbol errors 0/1560, clipped pairs 0.

noise 0.01, ethernet_rf40_12, attenuation 1: RMS 0.046505, symbol errors 0/1560, clipped pairs 0.

noise 0.03, pcie_rf20_8, attenuation 0.25: RMS 0.777569, symbol errors 274/1560, clipped pairs 0.

noise 0.03, pcie_rf20_8, attenuation 1: RMS 0.190203, symbol errors 0/1560, clipped pairs 0.

noise 0.03, ethernet_rf40_12, attenuation 0.25: RMS 0.571311, symbol errors 101/1560, clipped pairs 0.

noise 0.03, ethernet_rf40_12, attenuation 1: RMS 0.139992, symbol errors 0/1560, clipped pairs 0.

All transport/deadline checks pass. Noise degrades scored signal quality without
being hidden by payload identity. These single-seed finite records do not
establish BER, receiver sensitivity, noise figure or tolerance bounds. Identical
sample noise RMS at different sample rates is not identical analog noise PSD.
Next establish a physically interpretable input noise/bandwidth budget and
headroom stress, with uncertainty in either direction. Autonomous LO container
verified live at pass start; no restart or physical result claimed.


### Pass 684 — compare a consistent noise density across rates

Previous turn: progress. Added mutually exclusive --noise-density, specifying
the square root of complex two-sided PSD, flat over [-fs/2,fs/2]. Thus total
complex sample variance is density squared times fs, and each real component
has half that variance. With FFT spacing fixed, predicted demodulated noise
per active bin is unchanged between rates; this equality was verified to
1e-14. This removes an unfair sample-noise advantage in cross-rate comparisons.

pcie_rf20_8, attenuation 0.25: sample noise RMS 0.01000000, predicted FFT-bin noise RMS 0.05007710, calibrated RMS 0.267394, symbol errors 0.

pcie_rf20_8, attenuation 1: sample noise RMS 0.01000000, predicted FFT-bin noise RMS 0.05007710, calibrated RMS 0.068151, symbol errors 0.

ethernet_rf40_12, attenuation 0.25: sample noise RMS 0.01414214, predicted FFT-bin noise RMS 0.05007710, calibrated RMS 0.265477, symbol errors 0.

ethernet_rf40_12, attenuation 1: sample noise RMS 0.01414214, predicted FFT-bin noise RMS 0.05007710, calibrated RMS 0.065782, symbol errors 0.

Density was 2.2360679775e-6 normalized units/sqrt(Hz). If one normalized
component unit corresponds to V_FS volts, multiply this density by V_FS
for the square root of total complex voltage PSD; each component's two-sided
PSD is half the complex PSD. Do not equate normalized noise with dBm, noise
figure, or RF sensitivity without impedance, gain and voltage conventions.
The flat Nyquist model is an effective sampled noise assumption; physical
antialias filtering and aliased out-of-band noise still need representation.
Evidence: connected-multicarrier-density-2.23607e-06.json. Independent noise
realizations and changed resolution prevent isolating sample-rate effects
from these four records alone. Autonomous LO verified live. No gate advanced.


### Pass 685 — low-amplitude quantization and high-amplitude clipping

Previous turn: progress. Added --drive-rms with fixed waveform scaling, preserving
clipping instead of normalizing peaks away. DAC/ADC clipping counts use rounded
code bounds matching the signed codec, including the asymmetric positive rail.
Training sees the same impaired path; held-out correction stays frozen. Tested
0.03 and 0.6 complex RMS at both attenuations and modes without added noise.

drive 0.03, pcie_rf20_8, attenuation 0.25: DAC clipped 0 pairs, ADC clipped 0, RMS 0.495527, symbol errors 59/1560.

drive 0.03, pcie_rf20_8, attenuation 1: DAC clipped 0 pairs, ADC clipped 0, RMS 0.156679, symbol errors 0/1560.

drive 0.03, ethernet_rf40_12, attenuation 0.25: DAC clipped 0 pairs, ADC clipped 0, RMS 0.022751, symbol errors 0/1560.

drive 0.03, ethernet_rf40_12, attenuation 1: DAC clipped 0 pairs, ADC clipped 0, RMS 0.007109, symbol errors 0/1560.

drive 0.6, pcie_rf20_8, attenuation 0.25: DAC clipped 116 pairs, ADC clipped 0, RMS 0.061657, symbol errors 0/1560.

drive 0.6, pcie_rf20_8, attenuation 1: DAC clipped 116 pairs, ADC clipped 0, RMS 0.056752, symbol errors 0/1560.

drive 0.6, ethernet_rf40_12, attenuation 0.25: DAC clipped 234 pairs, ADC clipped 0, RMS 0.050212, symbol errors 0/1560.

drive 0.6, ethernet_rf40_12, attenuation 1: DAC clipped 234 pairs, ADC clipped 0, RMS 0.050206, symbol errors 0/1560.

Transport and DAC deadlines still pass. These two drive points bracket separate
quantization/headroom failures; they do not select an optimum operating level
or bound random waveform peaks. Actual analog compression can occur before
code saturation and remains unmodeled. The 12-bit mode remains an architectural
target, not established physical converter performance. Reassess next against
physical ADC/reference limits rather than optimizing ideal digital resolution.
Autonomous LO container verified live at approximately 7.113us of 8.001us;
its advancing log is evidence of progress, not a completed timing result.
No tapeout/layout gate advanced.


### Pass 686 — converter precision independent of transport width

Previous turn: progress. Added --adc-bits 4/6/8 as an ideal quantizer stress.
Quantized values are exactly re-encoded in the existing transport width;
sample rates, wire traffic, queue limits and DAC resolution remain unchanged.
Round-trip equality is asserted, and clipping counts use actual quantizer rails.
Both 6-bit and 8-bit tests pass all payload/deadline invariants.

ADC 6 bits, pcie_rf20_8, attenuation 0.25: RMS 0.319305, symbol errors 3/1560.

ADC 6 bits, pcie_rf20_8, attenuation 1: RMS 0.085518, symbol errors 0/1560.

ADC 6 bits, ethernet_rf40_12, attenuation 0.25: RMS 0.248104, symbol errors 1/1560.

ADC 6 bits, ethernet_rf40_12, attenuation 1: RMS 0.060341, symbol errors 0/1560.

ADC 8 bits, pcie_rf20_8, attenuation 0.25: RMS 0.085518, symbol errors 0/1560.

ADC 8 bits, pcie_rf20_8, attenuation 1: RMS 0.026437, symbol errors 0/1560.

ADC 8 bits, ethernet_rf40_12, attenuation 0.25: RMS 0.060341, symbol errors 0/1560.

ADC 8 bits, ethernet_rf40_12, attenuation 1: RMS 0.015222, symbol errors 0/1560.

These are resolution sensitivities, not models of physical SAR decision errors,
DNL/INL, reference memory or measured ENOB. Even 8-bit ideal conversion is not
qualified in hardware. The useful operating amplitude is intertwined with
converter accuracy and gain control; a 12-bit transport field proves neither.
Reassessed priority: finish the autonomous LO result when terminal; meanwhile
bring converter/reference uncertainty into the RF model before more favorable
modem demonstrations. Noise, clock phase noise and package coupling remain
unbounded physically. Autonomous LO container verified live. No gate advanced.


### Pass 687 — causal reference-memory sensitivity

Previous turn: progress. Added a hypothetical ADC reference-span state driven
by prior input transition magnitude, clipped at unit activity and relaxed with
a 50ns time constant. Current conversion uses the earlier state, then updates
it; no future input is consulted. Signed strength is bounded below unity in
magnitude, ensuring positive span. Zero-strength identity, analytic step/decay,
span bounds and future-input invariance controls pass. Both +/-0.1 strengths
run through quantization, framing, calibration and scoring.

strength -0.1, pcie_rf20_8, attenuation 0.25: span 0.990183–1.000000, RMS 0.085205, errors 0.

strength -0.1, pcie_rf20_8, attenuation 1: span 0.960732–1.000000, RMS 0.027135, errors 0.

strength -0.1, ethernet_rf40_12, attenuation 0.25: span 0.994598–1.000000, RMS 0.004008, errors 0.

strength -0.1, ethernet_rf40_12, attenuation 1: span 0.978392–1.000000, RMS 0.002845, errors 0.

strength 0.1, pcie_rf20_8, attenuation 0.25: span 1.000000–1.009817, RMS 0.084874, errors 0.

strength 0.1, pcie_rf20_8, attenuation 1: span 1.000000–1.039268, RMS 0.027058, errors 0.

strength 0.1, ethernet_rf40_12, attenuation 0.25: span 1.000000–1.005402, RMS 0.003900, errors 0.

strength 0.1, ethernet_rf40_12, attenuation 1: span 1.000000–1.021608, RMS 0.002847, errors 0.

The strength is an activity coupling, not a constant 10% reference error.
Observed spans above are the actual exercised excursions. This is not fitted
to transistor SAR data and does not model within-conversion bit decisions,
reference common mode, capacitor mismatch or signed charge injection. It is
a causal sensitivity axis; favorable results cannot validate the real ADC.
All payload/deadline checks pass. Next connect measured reference trajectories
to converter decision behavior rather than accumulating arbitrary stress knobs.
Autonomous LO verified live at 7.285us of 8.001us. No physical gate advanced.


### Pass 688 — reject reference-span-only physical prediction

Previous turn: progress. Re-ran sar_physical_divergence.py against hash-verified
completed transistor waveforms, then added sar_reference_model_audit.py. The
audit evaluates residue signs at observed trial codes, avoiding unsupported
counterfactual replay of rail traces with changed switching histories.

Across nine recorded conversions, measured rail span explains the first large-
signal wrong final-bit decision, but predicts the wrong final-bit sign in two
later conversions that actually resolve correctly. Omitted residue reaches
approximately 2.73mV; the smallest actual decision margin is only 104uV. Thus
rail-span correction alone is not an adequate predictive physical SAR model.
The synthetic reference_memory block remains a sensitivity model only.

Evidence: evidence/sar-reference-model-audit.json. No generalized error bound
or noise distribution follows from nine conversions. Next isolate the omitted
residue using actual bottom-plate/held-node charge behavior; retain reference
and sampling histories. Do not feed observed rail traces into arbitrary RF
samples and label the result physical prediction. Autonomous LO container
verified live; no restart, completion claim, or layout gate advancement.


### Pass 689 — quantify saved MSB bottom-plate lag

Previous turn: progress. Added sar_bottom_plate_audit.py against hash-verified
physical waveforms. Only the MSB bottom plates were saved. The explicit CDAC
has 128 MSB units among 256 units per side, including the dummy capacitor.
Using that half weight and subtracting the anchor's initial lag, the saved
MSB explains up to 0.61–0.78mV of residue across the nine conversions.
Remaining unexplained residue still reaches 1.92–2.56mV per conversion.

Thus MSB settling is material but does not close the charge accounting. The
calculation assumes linear equal capacitors and neglects held-node parasitics;
it cannot establish a unique mechanism. Evidence: sar-bottom-plate-audit.json.
Next physical diagnostic must save all 16 bottom plates and relevant held-node
loading for charge reconstruction. Existing waveforms cannot provide that
missing information, and replaying only the saved MSB would be insufficient.
Autonomous LO container verified live (two hours elapsed); no restart or
physical/layout qualification claimed.


### Pass 690 — launch observation-only full bottom-plate repeat

Previous turn: progress. Prepared sar-bottom-plates from the hash-verified
sar-driver-physical-400 deck by adding only the fourteen previously missing
bottom-plate vectors to wrdata. Exact reversal restores the original deck;
all circuit, stimulus and solver settings remain identical. Existing recursive
source-provenance runner records simulator completion and artifact hashes.

Launched container transceiver-sar-bottom-plates (003fc9e4465f), exec session
10810, pinned ARM64 image bd7a702bef0b85f5ebf67efca449f270fbeb185380ead204559fcd2457959305,
1 CPU/4GB, runner timeout7200s. Verified live with advancing transient at
3.48ns of209.9ns. Output scratch/transceiver-sar-bottom-plates. Do not restart
while live. Before charge reconstruction, require terminal success and compare
original saved vectors/codes against the source experiment. No result yet.
Autonomous LO log has advanced to7.622us of8.001us; no result claimed.
No circuit adoption or physical/layout gate advanced.


### Pass 691 — prepare strict observation-repeat acceptance

Previous turn: progress. Added check_sar_bottom_plates.py: requires terminal
success, complete finite209.9ns data, matching recursive simulator/circuit
sources, artifact hashes and exact reversal of the observation-only deck edit.
It requires every originally saved vector and time point to reproduce exactly
before using the new nodes. A mismatch stops analysis; it is not silently
accepted as an observation effect.

Prepared weighted differential bottom-plate reconstruction, anchored before
the first decision, using128+64+...+1 units over256 including the common dummy.
Residual is reported at each of24 decisions. This equal-linear-capacitance
identity excludes top-node parasitics and nonlinear capacitor charge; no
predictive-model claim follows from closure alone.

Checker currently reports no terminal result and performs no scoring. Verified
SAR container live near26.7ns/209.9ns and LO live near7.655us/8.001us. Existing
runs remain active; no restarts. No physical qualification gate advanced.


### Pass 692 — verify the actual MIM charge law

Previous turn: progress. Inspected the pinned simulator's included MIM model
and matched its hash to the resolved path in the running SAR manifest. The
active cap_mim_1f5_m4m5_noshield model is linear in voltage. Voltage-coefficient
parameters exist, but their capacitor expression is commented out. Active
capacitance uses area/perimeter, corner/mismatch and temperature; nominal25C
5um square unit capacitance is44.33fF before corner/mismatch.

Evidence: sar-mim-charge-law.json records active lines and source hash.
Voltage-dependent MIM charge cannot explain residual in this simulation; the
prior caveat was too broad for this model. Real silicon voltage dependence
remains unqualified. Top-node MOS charge, sampling-switch effects and comparator
kickback remain candidates after all-bottom reconstruction. No unique cause
yet. SAR verified live near86.5ns/209.9ns; LO near7.743us/8.001us. No restart
or physical gate advancement.


### Pass 693 — independently check charge reconstruction

Previous turn: progress. Extracted the weighted-bottom calculation into a
shared checker function and tested it against independent per-side charge
conservation with255 switched units plus a common dummy. Random bottom-plate
changes and common dummy motion reproduce differential held voltage to1e-14;
a known2mV differential charge injection remains visible in the residual.
Individual binary weights are also checked. Updated the limitation to reflect
the verified voltage-linear active MIM model while retaining omitted MOS
charge/leakage. Controls pass before the terminal-result guard; no incomplete
physical waveform is scored.

SAR verified live near145.8ns/209.9ns and original exec session10810 remains
running. LO verified live near7.838us/8.001us. Await completion of these same
runs; no restart or physical qualification claim.


### Pass 694 — full bottom-plate result and held-out loading hypothesis

Previous turn: progress. SAR observation repeat completed cleanly; provenance
and exact reversal pass. Every original saved vector reproduces exactly.
All-bottom reconstruction still leaves approximately2.1mV near final decisions
and up to2.66mV earlier. Thus missing bottom plates alone do not close charge.

Fitting one multiplicative charge-transfer factor using only the70ns conversion
gives beta0.9948834, equivalent to an extra0.5143% capacitance if interpreted
as a constant top-node load. Frozen beta reduces maximum residual in the two
held-out120/170ns conversions to34.36/35.75uV (training24.14uV). This strongly
supports testing omitted loading, but does not identify a unique device cause.
The model still consumes measured bottom trajectories and held anchors; it is
not a predictive SAR or evidence of converter noise/linearity qualification.

Evidence: sar-all-bottom-plates.json and sar-top-load-fit.json. Next verify
loading from device charge or a controlled load perturbation and then extend
to independent amplitudes. LO remains unqualified; last verified live near
7.917us/8.001us. No circuit adoption or layout gate advanced.


### Pass 695 — launch controlled loading test; LO transient terminal

Previous turn: progress. Prepared sar-known-load from the all-bottom-plates
deck with only two60fF shunt capacitors, one per held node. Exact reversal
restores baseline. With the active nominal MIM capacitance at27C, the prior
beta predicts0.9896781284 before seeing the perturbed result. Manifest stores
the prediction and source hash. This is a mechanism test, not a proposed fix.
Container transceiver-sar-known-load and exec session57204 verified live;
output scratch/transceiver-sar-known-load. Preserve this run until terminal.

Autonomous LO completed the8.001us transient (4,002,382 rows); result.json
exists and container has exited. Provenance/late-window extraction checker
is running in exec session58558; no clock-quality result claimed before it
finishes. Next run analyze_lo_autonomous_window.py --candidate after acceptance.
No circuit adoption or layout gate advanced.


Pass695 terminal LO assessment: provenance/extraction passed, but the candidate
FAILS autonomous operation in the late window7424–7936ns. All four LO outputs
have zero1.65V rising crossings and sit near1.5V. Feedback has32 rises versus
10 reference rises; control is approximately-0.00049 to0.0741V. The tiny
baseband reference component is therefore not a successful spur improvement:
usable LO switching has disappeared. Zero long intervals with zero edges is
not timing success. The previously successful recorded-waveform replay did
not survive reconnection to the autonomous oscillator/loop.

Evidence: lo-interstage-autonomous.json and its -metrics.json. Reject this
candidate for production adoption; original lo_buffer.spice remains unchanged.
Highest next timing task is locate collapse/startup trajectory and distinguish
loading-induced oscillator/loop failure from buffer bias failure. Do not repeat
the same long run without a discriminating change or diagnostic. SAR known-
load experiment remains active.


### Pass 696 — locate autonomous-clock failure trajectory

Previous turn: progress. Added trace_lo_interstage_failure.py, streaming the
completed4-million-row trace into100ns bins for oscillator P/N, four buffered
outputs, control, feedback and reference. It hashes the full waveform while
reading and requires the terminal artifact hash before reporting. Analysis
is active in exec session55905; retain this handle, do not duplicate the scan.

Immediate inspection of the already hash-verified late-window extraction
shows oscillator P at3.1012–3.1051V and N at3.0953–3.1102V. Thus the late
failure includes collapse of large-signal oscillator swing, not merely missing
output-buffer crossings. Causality among loop control, feedback and amplitude
awaits the full trace; don't infer it from final values alone.
SAR known-load container verified live near108ns/209.9ns. No restart, circuit
adoption or qualification gate advancement.


### Pass 697 — feedback activity persists while useful LO swing decays

Previous turn: progress. Full-run hash-verified trace completed. In100ns bins,
CTRL declines from about1.08V toward zero; usable buffer crossings largely
vanish around700ns. Feedback nevertheless continues6–7 rises/bin while REF
has about2. Oscillator common-mode rises above1.65V early, so absolute-threshold
P/N counts are not valid early frequency measurements.

Added early differential trace (lo-differential-startup.json). First bins show
P-N rises251,211,190,178,165,160 while Q7P-Q7N has3/bin and FB5–6/bin. Later
small-signal differential crossings rise sharply as swing decays; these may
be chatter, not usable clocks. This exposes a suspicious mismatch between
differential divider output and converted feedback. Next examine Q7-to-FB
conversion and intermediate divider states before blaming the loop filter.
Event counts alone do not identify causality or prove divider self-oscillation.
SAR known-load verified live near160ns/209.9ns; no restart. No adoption.

Critical scope finding at end of pass697: the candidate replaces the shared
pt_lo_buffer subcircuit globally. The deck has FIVE instances: four RF legs
AND XFB in the PLL feedback conversion path. Thus the autonomous experiment
also modified feedback conditioning, unlike the intended four-leg isolation
interpretation. Its failure cannot be attributed solely to RF backloading.
Next create a separately named RF-only candidate subcircuit, leave XFB on the
original buffer, and test a short startup window before any full-length run.
The failed experiment remains valid evidence for the globally modified design;
it does not reject an RF-only version. This supersedes that broader rejection.


### Pass 698 — correct RF-only experiment scope and launch short startup

Previous turn: progress. Prepared a distinct pt_lo_rf_isolated subcircuit and
changed exactly XBIP/XBIN/XBQP/XBQN. XFB retains the original pt_lo_buffer
include and unchanged instance line. Assertions enumerate all five call sites
and exact reversal restores the original autonomous baseline deck. Only other
change is transient stop8001ns to1001ns; timestep/UIC/stimuli remain unchanged.
This covers the prior failure interval without another full-length run.

Launched transceiver-lo-rf-only, exec session90675, same pinned image and
recursive source runner. Verified live near9.58ns/1001ns. Output
scratch/transceiver-lo-rf-only; do not restart while live. A successful short
run will only justify longer validation, not establish clock quality.

SAR known-load finished209.9ns with42,618 rows; result.json exists and its
container exited. Terminal provenance, unchanged-deck scope and frozen-beta
assessment remain to be performed before interpreting the load experiment.
No production circuit was changed or adopted; no gate advanced.


### Pass 699 — controlled loading validates frozen charge-transfer prediction

Previous turn: progress. check_sar_known_load.py verifies terminal success,
recursive source identity, artifact hashes, exact two-capacitor-only change and
the pre-recorded prediction source. No perturbed-run fitting is performed.
With60fF added per held node, frozen beta0.9896781284 predicts the three
conversions' observed residue within24.17/34.65/35.12uV maximum. Keeping the
old beta instead leaves2.48/2.70/2.53mV errors. This is strong controlled
evidence for the effective top-load model in these simulated trajectories.

Evidence: sar-known-load.json. The prior inferred load is about58.4fF per
side under the nominal256-unit linear-capacitor interpretation. Its exact
device origin remains unproven. This still uses measured anchors and bottom
trajectories; it does not close reference dynamics, acquisition, ADC noise or
independent input generalization. Next carry the loading term into a causal
bit-decision model and test independent amplitude/history, rather than treating
this attribution as a completed converter.
RF-only LO container remains active; no restart or production adoption.


### Pass 700 — independent amplitude observation repeat

Previous turn: progress. Generalized the observation-only preparation/checker
with --small, selecting the existing0.1V physical-reference damping experiment.
Only fourteen missing bottom-plate vectors are appended; exact deck reversal
and source hashes remain mandatory. Launched transceiver-sar-bottom-plates-small,
exec session74286, verified live. Output scratch/transceiver-sar-bottom-plates-small.
Checker correctly refuses waveform scoring before terminal result.

Recorded sar-small-amplitude-prediction.json: use original large-amplitude
beta0.9948834141 unchanged, with its source hash, and report all decision errors.
No new fit is allowed for this generalization check. A pass still conditions
on measured bottom trajectories/anchors; it cannot establish autonomous SAR
behavior. RF-only autonomous clock run remains active near128ns/1001ns.
No circuit adoption or physical qualification gate advanced.


### Pass 701 — RF-only startup acceptance checks

Previous turn: progress. Added check_lo_rf_only.py. It enumerates exactly four
RF-only call sites, checks XFB remains original, and reverses the separate
subcircuit/instance/stop-time edits to the baseline deck exactly. Terminal
checks require unchanged recursive sources, artifact hashes, complete finite
1001ns waveform and monotonic time. Prepared800–1000ns metrics combine1.65V
crossings with low-to-high0.66/2.64V excursions, oscillator differential swing
and CTRL range. Synthetic rail pulses count correctly; midrail chatter does
not count as a rail excursion. These thresholds are diagnostics, not qualified
mixer/PLL acceptance limits.

Scope/synthetic controls pass; checker declines physical scoring while run is
active. RF-only verified live near200ns/1001ns, SAR small-amplitude near40ns/
209.9ns. No restarts. Full loop/noise qualification remains incomplete.


### Pass 702 — frozen independent-amplitude assessor

Previous turn: progress. Added sar_small_amplitude_assessment.py. It verifies
the frozen prediction/fit hashes, accepted waveform hash and current checker
hash, then applies beta without regression. Reports every predicted/actual
residue, maximum error and sign-disagreement bit. A synthetic wrong-sign case
is retained as a negative control; it cannot disappear through refitting.
Controls pass; independent waveform acceptance remains pending, so no physical
assessment report is emitted yet.

Both original run handles remain live: SAR small-amplitude near110ns/209.9ns
and RF-only LO near300ns/1001ns. Next run observation acceptance then frozen
assessment after terminal success. No restart or qualification claim.


### Pass 703 — causal charge-domain SAR skeleton

Previous turn: progress. Added connected/sar_charge.py: each trial follows prior
comparator decisions, updates weighted differential bottom potential and held
voltage using the explicit extra-capacitance ratio, then makes the next ideal
comparison. A rail callback receives current bit/trial and previous trial so
a later reference model can respond to actual code history; no recorded future
trajectory is built into the block.

Verified768 mid-bin transfer cases across three load ratios, saturation and
callback ordering. The physical sign convention is explicit: increasing held
differential voltage reduces the offset-binary code, unlike the transport's
signed rounded codec. Evidence: sar-causal-charge-controls.json. This remains
a skeleton: sampled anchor is supplied, settling/reference dynamics and
comparator noise are absent, and it is not yet substituted into RF conversion.
Next use independent physical assessment to constrain these missing terms.
SAR small-amplitude verified live near161ns/209.9ns and RF-only LO near378ns/
1001ns. No restart or physical qualification gate advancement.


### Pass 704 — independent-amplitude result and remaining causal-model gap

Previous turn: progress. Small-amplitude observation repeat completed with
exact reproduction of every original saved vector. Provenance and full-run
checks pass. Frozen large-amplitude beta predicts three independent-amplitude
conversions within27.06/46.69/38.36uV maximum, with no preclock sign
disagreements across24 decisions. No refitting. Evidence:
sar-all-bottom-plates-small.json and sar-small-amplitude-assessment.json.

Then tested the causal SAR skeleton with that loading term, supplied held
anchors and nominal1V reference span. It predicts5/6 physical final codes,
but predicts76 instead of77 for the first large-amplitude conversion. This
retains a real failure instead of substituting measured bottom trajectories
and calling the converter predictive. Evidence: sar-nominal-reference-gap.json.
Next causal model needs code/history-dependent reference and settling behavior;
acquisition/noise/linearity remain separate unqualified requirements.
RF-only LO verified live near460ns/1001ns. No restart, adoption or gate advance.


### Pass 705 — separate decision-time settling from reference dynamics

Previous turn: progress. Added sar_settling_budget.py joining hash-linked
physical residue, measured rail-span and all-bottom-plate records at identical
trial histories. With frozen loading correction, replacing actual bottom
movement by instantaneous rail following changes predicted residue by at most
0.064–0.240mV across six conversions. Loaded rail-only residue errors reach
0.069–0.264mV; all48 preclock signs agree with the physical observations.

This is far better than the earlier rail-only model without top loading, but
is still conditioned on measured rails. It does not prove predictive reference
dynamics or establish a settling bound across arbitrary histories. Opposite
plate errors may cancel and larger inter-decision transients are unscored.
Evidence: sar-settling-budget.json. Prioritize causal reference-rail dynamics
next, retaining a separate settling uncertainty instead of blaming all missing
error on ideal instantaneous switching. No physical gate is advanced by sign
agreement alone. RF-only LO verified live near543ns/1001ns; no restart.


### Pass 706 — reject an inadequate single-state reference recurrence

Previous turn: progress. Added sar_reference_recurrence_screen.py to test a
simple span-error recurrence driven by switched binary weight, signed code
step and a constant. Fit only the three large-amplitude histories, then froze
coefficients. Both training and independent small-amplitude rollouts use only
the initial measured span plus observed code history, never intermediate
measured spans. The fitted pole magnitude is below1, but this does not imply
adequate prediction: peak span error is29–30mV on training and20–35mV on
held-out frames. Reject this model for physical ADC prediction.

Evidence: sar-reference-recurrence-screen.json. Stability and least-squares
fit are not qualification. One scalar state omits separate high/low rail and
amplifier memory; known reference-loop behavior makes those important next
states to inspect. Avoid raising polynomial order merely to fit six frames.
A physically structured reference model or targeted state observations are
needed before closing the causal converter. RF-only LO verified live near
636ns/1001ns; no restart or production adoption.


### Pass 707 — identify reference-amplifier memory beyond rail voltage

Previous turn: progress. Inspected actual complementary/high and NMOS/low
reference amplifiers. Each contains internal X plus series RC/compensation Z
(25ohm and4pF atS=4), in addition to rail reservoir and MOS charge states.
Added reference_state_observability.py against hash-verified saved waveforms.
At matched bit/trial and output within0.71mV, high-rail internal X differs by
15.22mV and output slope by1.74mV/ns. Another pair has only90uV output
difference but7mV internal difference. This supports retaining internal state
rather than predicting dynamics from instantaneous span alone.

Evidence: reference-state-observability.json. These pairs do not match full
load history and therefore do not uniquely identify amplifier dynamics.
Compensation Z and MOS charge/current trajectories are missing for charge
closure. Next retain separate high/low output and internal compensation states
with explicit switched-capacitor load, rather than arbitrary polynomial fits.
RF-only LO verified live near713ns/1001ns. No physical gate advanced.


### Pass 708 — reconstruct physical compensation branch state

Previous turn: progress. Added reference_compensation_reconstruction.py using
the actual25ohm/4pF branch. State w=V(Z)-V(OUT) obeys
dw/dt=(V(X)-V(OUT)-w)/(R*C). Exact updates for linearly interpolated forcing
pass independent constant/ramp analytic controls. Initial state uses DC
equilibrium, consistent with this non-UIC SAR deck. Hash-verified saved X/OUT
trajectories reconstruct decision-time compensation currents spanning roughly
-104 to+148uA across both rails. These are material compared with previously
observed output-device currents, so conduction-only restoring-current analysis
must not be mistaken for complete output-node KCL.

Evidence: reference-compensation-reconstruction.json. Direct saved Z validation
is still absent, and waveform interpolation introduces approximation. This
reconstructs one known branch conditional on measured nodes; it does not
predict autonomous reference dynamics or unique failure causes. Next directly
save Z and output-device currents together to close/check KCL before fitting
a reduced reference model. RF-only LO verified live near844ns/1001ns.
No circuit adoption or physical qualification gate advanced.


### Pass 709 — validate reconstructed compensation state against saved Z

Previous turn: progress. Located existing qualified sar-reference-devices
waveforms with directly saved Z; no new simulation required. Extended the
reconstruction with --device-probe and verified source waveform hash plus
reproduction qualification. Maximum Z discrepancy over70–209ns is0.864uV
high/0.625uV low. At24 decision instants, maxima are14.2nV/66.6nV, corresponding
to compensation-current discrepancies0.567nA/2.662nA. Thus saved-node linear
interpolation accurately reconstructs this RC branch for the tested history.

Evidence: reference-compensation-validation.json. This run has the original
sampling driver, so it validates the branch equation rather than reproducing
the damped-driver fixture. Output-device ID still excludes displacement and
full reference-node KCL remains incomplete. Next combine validated compensation
current with reservoir charge and switched-CDAC load to expose omitted current.
RF-only LO verified live near992ns/1001ns; no restart or gate advancement.


Pass709 LO terminal update: corrected RF-only1001ns run completed and passed
scope/provenance/full-waveform checks. In800–1000ns all four outputs have509
midrail rises; FB and REF each have4. Differential oscillator swing is0.820V
peak-to-peak and CTRL1.058–1.141V. The catastrophic collapse seen with globally
modified feedback is absent in this short window. However I-leg rail excursions
are only424/427 versus509 midrail rises, while Q legs have509/508. Thus pulse
amplitude remains a real concern; counts alone do not demonstrate usable cycles.
Evidence: lo-rf-only.json (qualified=false). Next quantify cycle-local I-leg
peaks/troughs and matched baseline behavior before extending or adopting.
No long-term lock, cold-start or phase-noise qualification is implied.


### Pass 710 — matched cycle-local LO comparison

Previous turn: progress. Added compare_lo_rf_cycles.py with full waveform hash
checks, interpolated crossings and complete-cycle extrema/duty metrics. Same
800–1000ns interval: baseline I legs have only9/5 complete midrail cycles and
7/3 gaps over800ps; corrected RF-only candidate has508 cycles per leg with
no such gaps. Q candidate cycles all meet diagnostic0.66/2.64V excursions,
with roughly50.4–52.4% duty.

I candidate cycles still fail amplitude/duty quality: minimum peaks1.90–1.91V,
maximum troughs1.58V, about20–90% duty,42–44 weak-peak cycles and40 elevated-
trough cycles per508. Thus recovered crossing count is a real improvement but
not usable-clock qualification. Independent square-wave control verifies basic
cycle metrics. Evidence: lo-rf-cycle-comparison.json.

Next locate the phase/amplitude disturbance upstream of the I buffer and
relate weak cycles to reference/CTRL timing; do not launch a longer run merely
because all four outputs have nominal edge counts. Need actual mixer response
and longer lock validation after resolving the pulse-quality issue. No adoption
or physical/layout gate advanced.


### Pass 711 — reference-phase localization of weak I cycles

Previous turn: progress. Added trace_lo_weak_cycles.py on the hash-verified
RF-only waveform. Of508 complete cycles, I+ has84 and I-82 failing at least
one diagnostic amplitude threshold. Weak cycles cluster near reference phase
0–6.4ns and44.8–51.2ns; none occur from6.4–38.4ns. Q legs have no weak
cycles under the same thresholds. Weak I cycles show about30mV higher mean
upstream IP/IN, while carrier swing falls only about6mV on average.

Circuit inspection: I uses low-pass resistor/capacitor branches, passing
oscillator common-mode variation; Q is capacitively coupled and resistively
returned to ideal CM. Q means stay near1.5V. This supports testing low-frequency
common-mode disturbance rejection, but correlation does not establish a unique
coupling path. Buffer MID/LOCAL/PRE nodes were not saved.

Evidence: lo-weak-cycle-trace.json. Next diagnostic should preserve the actual
differential oscillator waveform while removing only common-mode variation
in a clearly labeled replay, then compare cycle quality. Any replay success
must subsequently survive reconnection to the autonomous loop. No adoption
or physical qualification gate advanced.


### Pass 712 — launch matched common-mode replays

Previous turn: progress. Prepared recorded and fixed-common-mode receiver
replays from the completed RF-only autonomous waveform. P-N is preserved to
1e-14; fixed CM equals the time-weighted800–1000ns recorded mean. Both include
identical RF-only buffer circuitry and receiver loads. Oscillator and PLL
feedback are excluded, and saved node initial values seed the receiver.
Additional MID/LOCAL/PRE vectors expose internal pulse distortion.

Launched named containers transceiver-lo-common-mode-recorded/session89700
and transceiver-lo-common-mode-fixed/session70931. Both verified live; no
restart while active. Recorded replay must reproduce the source's cycle
behavior before interpreting the fixed-CM comparison. Fixed CM removes all
common-mode variation, including high-frequency content, so a positive result
would not uniquely validate a low-frequency filter. It also removes autonomous
backloading; eventual transistor changes must be tested in the full loop.
No adoption or physical qualification gate advanced.


### Pass 713 — integrate partial reference-node charge balance

Previous turn: progress. Added analyze_reference_partial_balance.py using
qualified device-probe data and verified ID sign convention. Over each0.2ns
predecision window, integrate output conduction and directly observed RC
compensation current, then subtract the nominal2048-unit reservoir charge
change. Integration avoids interpreting a noisy point derivative as KCL.
Results retain all48 signed unaccounted charges and equivalent currents.

Evidence: reference-partial-balance.json. Residual is explicitly not labeled
CDAC current: it includes switched load, output-device displacement and other
omitted node terms. These quiet windows also exclude the largest switching
transients. Next identify actual switched-load terminal currents before using
this balance to fit reference dynamics. Matched LO replay containers verified
live; no restart or physical gate advanced.


### Pass 714 — measure CDAC reference-load currents

Previous turn: progress. Prepared sar-reference-balance from the verified
all-bottom-plate deck. Added zero-volt sense sources between VH/VL and only
the CDAC reference pins, plus compensation Z and output/load device ID/VDS
probes. Positive sense current is defined from reference rail into CDAC.
Exact reversal removes only sense wiring and observations to restore baseline.

Reproduction criteria recorded before run: full209.9ns clean transient,
HP/HN/VH/VL differences at most10uV over70–209ns and all24 comparator
decisions matching. Zero-volt insertion can affect numerical behavior; no
current attribution is allowed before these checks. Launched named container
transceiver-sar-reference-balance, exec session19483, verified live. Output
scratch/transceiver-sar-reference-balance. No restart while active.
Matched LO replay containers remain active. No production changes or physical
qualification gate advancement.


### Pass 715 — reference-current instrumentation acceptance gate

Previous turn: progress. Added check_sar_reference_balance.py. Exact reverse
edits restore the baseline deck. Terminal checks require clean return, full
finite209.9ns waveform, matching recursive sources and artifact hashes.
Analog reproduction is evaluated on the union of both time grids over70–209ns;
all24 comparator signs must match with full differential output swing.
Sense-source endpoints must match within1pV. Report separates completion from
reproduction_pass so a completed but perturbed run cannot authorize attribution.

Scope checks pass; no physical scoring occurs before result.json exists.
Verified named SAR run live near26ns and both LO replays near97ns. All three
existing processes remain active; no restart. No current-balance or physical
qualification claim is made yet.


### Pass 716 — matched LO replay reproduction gate

Previous turn: progress. Added check_lo_common_mode_replay.py. Recorded/fixed
receiver decks must be byte-identical; only separately hashed PWL sources
differ. Before comparison require terminal success, unchanged recursive
sources, artifact hashes and complete finite1001ns waveforms. Recorded replay
is compared to autonomous source over800–1000ns on a shared interpolation
grid. Predeclared diagnostic limits:20mV RMS,100mV peak output error,5ps
nearest-edge error and at most1 edge count difference per output. Both edge
directions of nearest matching are checked. These are reproduction tolerances,
not physical mixer specifications.

Preparation checks pass; no results are interpreted while runs are active.
All three named simulations verified live; LO replays near136ns, SAR current
instrumentation still active. Do not relax reproduction limits after seeing a
failure; investigate replay state/timing if they fail. No gate advanced.


### Pass 717 — instrumented SAR fails predeclared reproduction limit

Previous turn: progress. Added analyze_reference_loaded_balance.py to subtract
measured CDAC charge from conduction+compensation-reservoir charge, guarded
by accepted waveform status. Instrumented run completed; provenance and exact
scope pass, all24 decisions match, and sense-source voltage difference is
2.22e-15V. However HP/HN maximum reproduction errors are13.99/14.13uV,
above predeclared10uV. VH/VL errors are1.35/1.24uV. reproduction_pass=false;
the current-attribution analyzer correctly refuses to run.

Evidence: sar-reference-balance.json. Do not raise the tolerance after this
result or label the residual a measured physical mechanism. Next localize the
error and perform a timestep/tolerance control on matched original/instrumented
decks. Both LO replays remain active; no restart or gate advancement.


### Pass 718 — matched timestep refinement of failed reproduction

Previous turn: progress. Prepared original and instrumented SAR copies changing
only maximum transient step5ps to2.5ps (same output-step request,209.9ns stop,
circuit and stimulus). Exact reversal restores each prepared parent; source
hashes and previous manifests are linked. Keep the10uV/24-decision reproduction
limits unchanged. Comparison must include both refined-versus-parent results
as well as the refined pair; cross-deck agreement alone is not convergence.

Launched transceiver-sar-balance-refined-baseline/session17557 and
transceiver-sar-balance-refined-instrumented/session73799, each1CPU/4GB, same
pinned simulator. Both verified live with advancing logs. Existing LO recorded
and fixed-CM replay runs also remain live. No restarts or new interpretations
of the failed coarse instrumented run. No adoption or qualification gate advance.


### Pass 719 — separate reproduction and numerical refinement evidence

Previous turn: progress. Added check_sar_balance_refinement.py. It reverses
each timestep-only edit, verifies linked prepared parents and terminal hashes,
and will compare four pairs: coarse original/instrumented, refined pair, and
each circuit versus its coarse version. Reports analog errors and all24
decision matches independently for every comparison, retaining10uV limits.
No automatic physical qualification follows from a passing refined pair;
coarse failed probes cannot be retroactively accepted by refined agreement.

Prepared scope checks pass. Both SAR refinement containers verified live
near15ns/209.9ns; matched LO replays verified live near236ns/1001ns. Keep
existing runs; no restart or completed-result claim.


### Pass 720 — reassess whole-platform priorities

Previous turn: progress. Updated the leading risk-priorities decision point
from historical pass667 to current evidence: timing pulse quality and causal
reference fidelity remain immediate physical risks; connected lifecycle/CDR
and die/package coexistence remain explicit unfinished whole-platform work.
Named active experiments, prerequisite acceptance checks, evidence links and
stop rules prevent favorable isolated diagnostics from implying chip closure.
All four simulations verified live. No gate advanced and no scope reduced.

### Pass 721 — RF consumer state and framed reset/drain contract

Added rf_tx_state.py: finite DAC queue, acceptance/conservation accounting,
explicit underflow behavior and analytically evolving filter charge. Local
controls verify that reset discards queued samples without erasing analog
state, rejects traffic while disabled and preserves independent wired readiness.

Added rf_reset_transport.py: 128 reset cases cover every host-frame word
position in both modes during DAC prefill. Actual encoded old RF frames drain
while acceptance is disabled; wired payload is preserved exactly. A candidate
coherent host stop/drain acknowledgement precedes rearm. No old RF sample is
accepted after reset; each case accepts all50 new samples. Premature-rearm
negative controls admit35 and44 stale samples in the two modes. This demonstrates
why clearing the DAC queue alone cannot implement reset. Active-filter reset
is covered by the separate consumer control, not by the framed prefill cases.
Management-register/CDC realization and arbitrary-length bursts remain open.
Evidence: rf-tx-reset-state.json and connected-rf-reset-transport.json.

### Pass 722 — reproducible connected mathematical regression

Added system_model/connected/run_architecture.py and ARCHITECTURE.md with the
connected signal paths, executable scenarios, abstraction boundaries and closure
requirements. All six fresh scenarios pass: modulated RF with wired traffic,
wired acquisition sweep, host service-clock sweep, framed multicarrier RF,
wired clock reset/retraining, and RF reset/drain. Suite checks unchanged source
hashes and fresh result artifacts and retains per-scenario logs/hashes.
Evidence: connected-architecture-suite.json. Runtime was approximately82seconds
in this environment. Successful scenario execution does not set complete_architecture:
whole-chip lifecycle/mode transitions, arbitrary burst flush, noisy clock startup,
causal converter/reference fidelity and shared analog coupling remain unfinished.
No transistor, layout, package or compliance qualification gate advances.

### Pass 723 — active RF reset and SAR convergence reassessment

Previous turn: progress. Extended framed RF reset tests from prefill to active
playback:256 total cases,128 active, covering every frame position in both modes.
Active cases consume5–21 samples before reset and retain nonzero filter charge.
Decay through the actual frame-drain interval matches the analytic response
within1.23e-15; no stale RF samples are accepted with the barrier. All four
premature-rearm negative controls expose stale samples. Wired words remain exact.
Only the affected reset test was rerun; pass722 suite remains historical with
its recorded hashes, not a fresh aggregate claim after this edit.

Matched SAR refinement completed with terminal/provenance checks. Refined
original/instrumented maximum difference4.19uV passes the unchanged10uV probe
reproduction criterion and all24 decisions match. However coarse-to-fine
HP/HN errors reach2.49/2.07mV, with rail errors up to89.94uV. Matching decisions
and refined cross-deck reproduction do not establish timestep convergence.
Next localize differences at switching edges versus decision apertures before
using current attribution or selecting further numerical refinement. Both LO
common-mode replays verified live; no restart. No physical gate advanced.

### Pass 724 — localize SAR timestep sensitivity against decisions

Previous turn: progress. Added verification/localize_sar_refinement.py, reusing
fresh terminal/provenance checks for all four waveforms. Signed differential
interpolation/peak controls pass. Report records full-window peaks, nearest
external PWL boundaries,24 preclock residues per circuit and200ps preclock
window errors; no thresholds are relaxed.

The largest differential change is4.433mV at173.43875ns,38.75ps after the
external CLK falling ramp starts. In contrast maximum change at comparator
rising-ramp start is4.823uV (baseline) and4.813uV (instrumented). All preclock
signs match; minimum refined preclock margin is108.8uV. This localizes the
largest observed error away from those sampled decisions, but does not make
it harmless: reset/kickback and integrated reference current can still depend
on that transient. Numerical convergence remains unestablished.

Evidence: sar-refinement-localization.json. Next test finer-step stability of
preclock margins and switching-region integrated charge separately, before
using a current-balance fit. Both LO replay containers verified live; no restart
or physical qualification.

### Pass 725 — second matched SAR timestep refinement launched

Previous turn: progress. Added prepare_sar_balance_second_refinement.py and
paired run scripts. Verified completed2.5ps parent artifacts and recursive
source consistency before preparing1.25ps maximum-step copies. Exact reversal
restores each parent deck; circuit, output-step request, stop time, tolerances,
stimulus and non-UIC startup are unchanged. Manifests retain parent hashes and
the original10uV cross-deck reproduction criterion with24 full-swing decisions.

Predeclared analysis separates preclock residues, switching peaks and integrated
CDAC current over fixed hold+5*j+[2.5,3.6]ns windows. No charge tolerance is
invented: a sensitivity measurement alone cannot establish an acceptable
physical error budget. Compare these results to2.5ps and earlier5ps changes;
do not call cross-deck agreement numerical convergence.

Launched transceiver-sar-balance-second-baseline/session43975 and
transceiver-sar-balance-second-instrumented/session38172, each1CPU/4GB, same
pinned simulator. Both verified live with advancing logs near1ns of209.9ns.
Both original LO common-mode replays also verified live. Retain these runs;
no restart, completed-result interpretation or physical gate advancement.

### Pass 726 — pre-result refinement and charge comparison checker

Previous turn: progress. Added check_sar_balance_second_refinement.py. Freshly
revalidates the first refinement, second prepared decks and linked parent hashes.
Terminal analysis requires complete finite209.9ns waveforms, artifact hashes,
unchanged recursive sources and exact prepared/run deck equality. Reports
cross-deck reproduction separately from each circuit's timestep sensitivity.

Preclock residues/signs/margins are compared at all24 fixed decision times over
5ps,2.5ps and1.25ps records. CDAC sense currents integrate on the predefined
UPDATE/CLK switching windows using piecewise-linear endpoint interpolation.
Constant-current, linear-ramp, sign reversal and out-of-record rejection controls
pass. Charge changes are reported in coulombs without inventing a physical
acceptance budget. Failed5ps current reproduction remains labeled, signed-charge
cancellation remains a limitation, and no asymptotic convergence is inferred.

Preparation and integration controls pass; terminal results still pending, so
result-dependent analysis has not yet been exercised on the second refinement.
Both second SAR runs and both LO common-mode replays verified live at turn start.
No restart or physical qualification gate advancement.

### Pass 727 — expose cancellation in SAR switching-charge comparisons

Previous turn: progress. Extended second-refinement checker with net, absolute,
positive and negative charge measures. Absolute-current integration explicitly
splits linearly interpolated segments at zero crossings; simply trapezoiding
absolute endpoint values would overestimate sign-changing segments. Analytic
zero-net/nonzero-activity, subdivision-invariance and zero-current controls pass,
alongside previous ramp, polarity and endpoint controls. Preparation checks pass;
second-refinement terminal data remains pending.

Applied the metric diagnostically to48 predefined rail/window combinations in
the existing5ps/2.5ps records. At2.5ps the smallest abs(net)/absolute-charge ratio
is0.14026: net charge alone conceals about86% of total absolute charge movement
in that window. Evidence: sar-charge-cancellation-screen.json. This is numerical
sensitivity evidence, not authorized physical current attribution:5ps probe
reproduction failed and2.5ps timestep convergence remains unresolved. Future
comparison now reports absolute-charge change as well as net-charge change.
Both second SAR simulations and both LO replays verified live. No restart or
qualification gate advanced.

### Pass 728 — LO reproduction must preserve falling edges too

Previous turn: progress. Both LO common-mode replay containers remain live and
terminal results pending. Inspected their reproduction checker and strengthened
it before seeing results: both rising and falling edges must satisfy the same
5ps bidirectional nearest-edge and count-difference limits, in addition to the
unchanged20mV RMS/100mV peak waveform limits. This explicitly tests pulse width;
rising-edge agreement alone cannot qualify clock reproduction.

Added synthetic controls: unchanged rise with shortened pulse passes rise matching
but fails fall matching; missing edges fail; asymmetric missing-edge matching
fails;1ps shifts pass. Empty edge sets now produce JSON null plus an explicit
failure rather than nonstandard Infinity. Strict JSON serialization rejects
nonfinite metrics. Controls and prepared waveform/deck hashes pass; no terminal
reproduction result or fixed-common-mode interpretation is claimed. SAR second
refinement containers also verified live. No restart or physical gate advance.

### Pass 729 — explicit finite-burst length and padding contract

Previous turn: progress. Added connected/burst_codec.py as a candidate streaming
block: a coherent predeclared sample count determines the exact transport-word
count; only the final word is zero-padded. Decoder checks final padding before
releasing samples from that word and never interprets padding as another sample.
Incomplete and excess payload are rejected; empty bursts are explicitly valid.
An independent16-bit bit-order vector checks packing without relying solely on
a round trip. Padding corruption and incomplete/extra-word controls pass.

260 finite-burst cases (lengths0–129 in both modes) traverse the real frame codec
with independent wired payload; sample identity/count and wired preservation
pass. Evidence: connected-burst-codec.json. This establishes a block contract,
not integration: descriptor transport/CDC, timed burst scheduling, back-to-back
boundaries and connected reset interactions remain open. The main analog model
still uses continuous packing; do not claim arbitrary-length RF delivery there.
Four targeted physical simulations verified live; no restart or gate advance.

### Pass 730 — finite bursts in timed RF return transport

Previous turn: progress. Added optional explicit burst_count to return_samples.
The burst encoder releases its padded final word only after the declared final
sample actually arrives; decoder consumes exactly the declared samples. Existing
continuous packing stays the default. Empty RF/wired streams now have defined
behavior and null rather than fabricated latency extrema. Ingress accounting
includes partial encoder state and padded final-word storage.

Added burst_return_screen.py:20 timed cases exercise both widths, partial final
words, staging and concurrent wired traffic; empty streams also pass. Aligned
bursts match continuous-path payload and exact delivery ticks. Delaying the only
sample from17 to1000 host ticks delays its release correspondingly. Evidence:
connected-burst-return.json. Reran modulated connected platform and framed
multicarrier screens after the helper edit; both pass. This establishes one
predeclared timed burst, not descriptor transport, back-to-back epochs, reset
interaction or arbitrary-rate queue sufficiency. No architecture/physical gate
is declared complete. Four physical simulations verified live at turn start.

### Pass 731 — replace invalid fixed burst drain horizon

Previous turn: progress. Reproduced a system-model bug:63 simultaneous16-bit
samples (1008 payload bits,1010 padded transport bits) fit the1024-bit ingress
but fail the old512-tick drain tail because mode1 services only7 IQ words/frame.
This was a simulation-horizon failure, not inadequate physical queue capacity.

Replaced the magic tail with a service-derived bound: ceiling of capacity in
transport words divided by the minimum applicable per-source frame quota,
plus three frames for partial-frame/staging/final-boundary allowance. Capacity
checks remain unchanged. Added near-capacity cases for both modes and explicit
over-capacity rejection controls. All20 ordinary timed burst cases, both
near-capacity cases and empty/late-arrival/aligned-timing controls pass. Evidence:
connected-burst-return.json. The bound assumes static service without host stalls;
no arbitrary clock-stall or backpressure capability is implied.
Modulated connected platform regression also passes after the timing-bound edit.
All four targeted physical simulations verified live; no restart or gate advance.

### Pass 732 — burst phase sweep exposes payload-ordering bug

Previous turn: progress. Added256 cases spanning64 frame positions, integer and
quarter-tick arrival phases, both modes, and both ingress queues near capacity
(1010 IQ and1020 wire transport bits). The first test failed: tuple sorting of
same-time arrivals used payload values as a tie-breaker, silently reordering
nonmonotonic source words. Changed arrival sorting to stable timestamp-only
order. Descending RF samples and wrapping wired values now retain source order.

All256 phase cases and20 existing timed cases pass. Maximum final RF delivery
delays389.75/1049.75 ticks remain below derived512/1152-tick drain bounds for
modes0/1. No queue capacity was increased and no payload reordered by the test
oracle. Evidence: connected-burst-return.json. This is a finite phase sweep,
not proof under stalls, arbitrary CDC behavior or unconstrained traffic.
All four physical simulations verified live; no restart or qualification gate.

### Pass 733 — reassess risk after transport fixes

Previous turn: progress. Refreshed risk-priorities against current evidence.
Timing quality and causal converter fidelity still outrank additional transport
convenience features. Recorded numerical SAR margin is not physical noise/offset
headroom; matched codes, net charge and ideal common-mode removal each require
explicit limits on interpretation. Mathematical lifecycle integration remains
unfinished despite passing separate burst/reset scenarios.

Second SAR checker preparation/integration controls pass; terminal results pending.
Verified same four containers live with advancing logs: SAR144/142ns of209.9ns,
LO744/745ns of1001ns. No restart. On terminal results use predefined comparison
gates before choosing further circuit changes. No feasibility claim advanced.

### Pass 734 — gated LO pulse-quality comparison prepared

Previous turn: progress/verified wait. Added analyze_lo_common_mode_cycles.py.
It executes the current reproduction checker first and refuses fixed-CM
interpretation unless recorded replay passes. Shared cycle metrics were made
importable without rerunning historical comparisons. The new analysis compares
all four output legs over the same800–1000ns window: complete-cycle count,
minimum peak, maximum trough, weak-cycle counts, period range and duty range.
No cycles or ambiguous crossings are explicitly reported as failed analysis,
not a successful low-noise clock.

Independent full-amplitude, weak-amplitude, narrow-duty and stopped-clock controls
pass. Exact replay preparation checks pass; terminal results pending, so no
physical comparison report is produced yet. Source/report hashes will link
completed analysis to its gate and waveforms. Ideal common-mode intervention
still requires a realizable circuit and autonomous validation afterward.
Both LO replays and both second SAR runs verified live. No restart or gate advance.

### Pass 735 — retain stalled-clock tails in LO diagnostics

Previous turn: progress. Found a coverage limitation in complete-cycle metrics:
a waveform stopping partway through the window can retain healthy earlier cycles
while its stalled tail is excluded. Added boundary-inclusive rising-edge gap,
leading/trailing gap and window edge-count reporting to the gated replay analysis.
A synthetic clock stopped at900ns retains complete earlier cycles but now exposes
its approximately100ns trailing gap. Fully stopped and continuous clocks also
pass their distinct diagnostic controls. Finite, monotonic, full-window inputs
are required. No physical threshold or gate was relaxed.

Current checker controls/preparation checks pass; terminal replay results remain
pending. Same four containers verified live with advancing logs: second SAR near
175/171ns of209.9ns; LO near783/784ns of1001ns. No restart or physical claim.

### Pass 736 — fresh eight-scenario architecture regression

Previous turn: progress. Added finite-burst codec/malformed-payload and timed
burst/near-capacity phase tests to run_architecture.py. Updated the closure
checklist to distinguish working codec/return behavior from unimplemented burst
management, back-to-back epochs and reset integration. Ran all eight scenarios
against one unchanged source snapshot: modulated RF/wired, wired acquisition,
host service-clock sweep, framed multicarrier, wired reset/retraining, burst
codec, timed burst phase sweep and active/prefill RF reset. All pass with fresh
hashed results and logs in connected-architecture-suite.json (about84seconds).

This supersedes the stale pass722 aggregate for the current source tree; it does
not turn separate lifecycle tests into whole-chip lifecycle closure. Physical
SAR and LO simulations remain live; second SAR preparation/provenance controls
pass with terminal results pending. No physical/tapeout gate advanced.

### Pass 737 — second SAR refinement completes with decreasing sensitivity

Previous turn: progress. Both second SAR sessions43975/38172 completed cleanly.
Executed check_sar_balance_second_refinement.py: full209.9ns finite waveforms,
artifact/preparation/parent hashes and recursive-source checks pass. Matched
1.25ps original/instrumented pair maximum error1.833uV passes unchanged10uV
reproduction criterion; all24 decisions match.

Compared with2.5ps, baseline preclock residue change falls to1.337uV from4.822uV
in the prior refinement; instrumented change1.260uV. Minimum sampled margin is
about110uV, with all signs unchanged. Full-window per-top-plate changes remain
0.653/0.556mV; rails change up to23.42uV. Decreasing sensitivity is evidence of
numerical improvement, not proof of asymptotic convergence or physical margin.

Maximum fixed-window net charge changes are0.115fC high/0.188fC low; absolute
charge changes0.543/0.654fC. Prior net changes were0.900/0.666fC. Retain both
metrics; no charge-error acceptance budget has been established. Evidence:
sar-balance-second-refinement.json. Next assess current balance on the finest
accepted probe with these numerical sensitivities visible; do not reuse failed
coarse probes or claim device accuracy from matching conversion codes. No gate
advances to physical qualification. LO replays remain separately in progress.

### Pass 738 — accepted fine-probe reference charge balance

Previous turn: progress. Added analyze_reference_refined_balance.py. Freshly
runs second-refinement checks, requires both2.5ps and1.25ps reproduction passes,
verifies waveform hashes and selected-DC probe convention contract, and checks
positive savedVDS in every analyzed window. Analytic charge/sign controls pass.
Accounts for output conduction, compensation branch, nominal reservoir charge
and measured CDAC load over24 preclock and24 switching windows on both rails.

Finest-run unaccounted equivalent current ranges: preclock high−4.10..17.57uA,
low−35.55..7.54uA; switching high−42.96..9.79uA, low−9.39..39.20uA.
Maximum residual timestep changes: preclock0.249/0.0463fC high/low;
switching0.0834/0.0428fC. These sensitivities are measured differences, not
rigorous numerical error bounds. Residuals persist after explicit CDAC loading;
a conduction+compensation+reservoir-only reference model is incomplete.

Evidence: reference-refined-balance.json. Output MOS displacement and omitted
node terms remain candidates, not uniquely identified causes. Next account for
terminal displacement or compare integrated terminal charge before fitting a
causal reference model. Current reconstruction still uses recorded trajectories;
no autonomous prediction or physical qualification is established.

### Pass 739 — audit missing reference-rail terminal paths

Previous turn: progress. Added audit_reference_rail_connections.py and verified
buffer sources against the completed finest-run recursive source hashes. Exact
OUT-node incidence is XIP gate, XOUT drain, XLOAD drain and CC negative terminal
for both complementary buffer versions. Existing accounting includes channel
conduction, CC/RC compensation, external reservoir and measured CDAC current.

The XIP feedback gate is an additional omitted dynamic terminal path. Therefore
attributing the remaining charge solely to output-drain displacement would be
premature. Evidence: reference-rail-connections.json. Next measurement must cover
full terminal currents at the feedback gate and both output drains, including
PDK wrapper parasitics, with another reproduction check. Incidence alone does
not establish which term dominates. Both LO replays verified live; no restart,
new physical qualification or fitted residual cancellation.

### Pass 740 — full reference terminal-current probe launched

Previous turn: progress. Added prepare_reference_terminal_probe.py and runner.
Diagnostic local copies insert zero-volt sources from each rail to the XIP gate,
XOUT drain and XLOAD drain at the PDK wrapper boundary. This measures whole
terminal current, including wrapper internals, without relying on a channel-ID
interpretation. Six positive probe directions point outward from the rails.
Production buffer sources are unchanged. Exact reversal restores both original
buffer definitions and the parent deck; hashes link the qualified1.25ps parent.

Circuit stimulus, stop209.9ns, maximum step1.25ps and tolerances remain unchanged.
Require10uV reproduction and24 full-swing decisions before any current attribution.
Full terminal currents already include conduction: next accounting must avoid
subtracting channel conduction a second time. Probe insertion can affect numerical
behavior, so ideal electrical equivalence alone does not waive reproduction.

Launched transceiver-reference-terminal-probe/session9462,1CPU/4GB,same pinned
simulator. Verified live with advancing transient log near1ns. Both LO replays
also verified live. No restart or physical qualification claim.

### Pass 741 — terminal-current probe reproduction gate

Previous turn: progress. Added check_reference_terminal_probe.py. Verifies
parent/preparation hashes and exactly reverses every diagnostic buffer terminal
edit, local include substitution and added saved vector. Terminal comparison
requires clean complete finite209.9ns waveforms, all six current vectors, exact
run/prepared deck equality and recursive source identity except the three
explicitly audited local copies. Uses the unchanged10uV/all24-full-swing-decision
reproduction criterion against the finest parent.

Exact scope and preparation checks pass. Terminal experiment remains live;
result-dependent checks are pending and no current attribution is authorized.
The eventual evidence report carries both checker and shared comparison source
hashes. Full terminal current includes conduction; accounting must not combine
it with the old conduction term a second time. LO replays also verified live.
No restart or physical qualification gate advancement.

### Pass 742 — gated whole-terminal rail balance prepared

Previous turn: progress. Added analyze_reference_terminal_balance.py. Executes
fresh terminal-probe reproduction checks before charge attribution. Compensation
is positive into the rail; gate/drain/load probes and CDAC current are positive
outward. The residual is compensation minus reservoir change minus all outward
terminal/CDAC charge. Channel conduction is not added again. Reports each terminal
separately over the same preclock and switching windows as the prior analysis.

Synthetic balanced KCL, polarity reversal, omitted feedback gate and explicit
channel double-count controls pass, as do integrator controls. No accepted
terminal waveform yet, so no physical charge report is produced. Even eventual
KCL closure will reconstruct recorded traces, not establish a causal predictive
reference model. All three running containers verified live. No restart or gate
advancement.

### Pass 743 — common-mode replay reproduces and removes weak LO pulses

Previous turn: progress. Preserved and polled both original live LO sessions
through completion; no restart. Both1001ns artifacts passed terminal/source/hash
checks. Recorded replay passes unchanged reproduction limits including rising
and falling edges: output RMS error<=2.147mV, peak<=6.955mV, nearest-edge error
<=0.686ps. Gated complete-cycle analysis then ran on both records.

Over800–1000ns all outputs have508 complete cycles. Recorded I legs retain
44/42 weak peaks and40/40 high troughs, minimum peaks1.911/1.905V and maximum
troughs1.583/1.589V. Fixed common mode has no weak peaks/high troughs on any leg;
I minimum peaks2.796/2.796V, maximum troughs0.336/0.337V, duty0.5012–0.5087.
Q remains healthy by the same diagnostic thresholds. No missing/stalled interval
appears in the boundary-inclusive edge diagnostics.

Evidence: lo-common-mode-reproduction.json and lo-common-mode-cycle-comparison.json.
This supports common-mode sensitivity as a causal mechanism in the replayed
receiver. The intervention preserves recorded differential input but removes
ALL common-mode variation, not only slow drift; it is not a realizable circuit
or autonomous clock. Next evaluate a physical common-mode rejection/bias remedy
without modifying PLL feedback, then rerun autonomous startup/timing. Phase noise,
mixer signal quality, extracted loading and yield remain unqualified.

### Pass 744 — finite RF bias-restoration remedy launched

Previous turn: progress. Inspected actual quadrature/buffer network: RF inputs
already AC-couple through MIM capacitors and restore first-inverter bias via100k
feedback resistors. Prepared a two-resistor experiment reducing only I-leg
RFBIP/RFBIN to10k. Exact reversal restores the qualified recorded-replay deck;
recorded P/N waveform, Q branches, initial conditions, loads, timing and all
other circuitry remain unchanged. No ideal common-mode subtraction is added.

Hypothesis is stronger rejection of slow bias movement; increased feedback and
loading may instead degrade carrier response. This is a lumped-resistor circuit
candidate, not a qualified physical resistor implementation. Replay cannot show
reverse oscillator loading; any favorable result needs a separately prepared
autonomous test with PLL feedback unchanged. Compare all four legs, amplitude,
duty and boundary gaps using existing diagnostics before deciding.

Added prepare_lo_bias_restoration.py and runner. Launched
transceiver-lo-bias-restoration/session34575,2CPU/4GB,same pinned simulator,
1001ns replay. Verified live and advancing; terminal-reference probe also live.
No restart, adopted circuit change or physical qualification.

### Pass 745 — bias-restoration comparison gate prepared

Previous turn: progress. Terminal-current experiment remains live near134ns of
209.9ns; its signed-balance controls and preparation checks pass, with no result
interpretation yet. Added check_lo_bias_restoration.py while the RF candidate runs.
It requires exact reversal of the two I-leg resistor changes, identical recorded
input, qualified parent reproduction hash, clean full1001ns data and recursive
source equality. It will compare all four legs' amplitude, duty and boundary-gap
metrics against the actual recorded parent; no ideal-CM reference is substituted.

Scope/input checks and pulse controls pass. Result-dependent comparison awaits
terminal artifacts. The candidate cannot qualify autonomous timing because its
source waveform is prescribed despite changed receiver loading. Both active
containers verified live; no restart or physical gate advance.

### Pass 746 — latch malformed burst epochs instead of silent retry

Previous turn: progress. Audited burst error behavior: decoder previously rejected
bad padding but allowed a corrected word to resume the same epoch. Changed
BurstDecoder to latch faults on malformed words, excess payload or incomplete
finish. Feed/finish after fault cannot alter state or produce samples; recovery
requires a new decoder descriptor/epoch. No implicit resynchronization is claimed.

Added retry-after-padding-fault, incomplete-finish latch, explicit new-epoch
recovery and late-fault-after-one-streamed-sample controls. The latter preserves
the already delivered count: streaming payload is not quarantined and cannot be
retracted after a later error. Host notification/epoch control still needs
integration. All260 framed codec cases,20 timed bursts and256 near-capacity phase
cases pass. Both physical simulations verified live. No physical gate advanced.

### Pass 747 — connect late burst corruption to RF session fault

Previous turn: progress. Added burst_fault_screen.py with a BurstIngress adapter.
A final padding error propagates through the real frame decoder to RF queue
flush, zero DAC hold and latched RF session fault. Wired payload continues through
the same frames unchanged. First-fault snapshot preserves time and decoded/
consumed sample counts; retries cannot overwrite it or resume RF acceptance.
Analog filter charge persists and decays analytically after mute.

Both modes pass: one sample is intentionally consumed before the late error,
no remaining RF payload is accepted, all three nonmonotonic wired words retain
order, and asserting readiness alone cannot clear the fault. Initial test code
used an invalid complex comparison; corrected to magnitude and reran successfully.
Evidence: connected-burst-fault.json. This adapter assumes instantaneous mute;
physical CDC, serialized status, epoch recovery and drain remain open. Both
physical runs verified live; no restart or qualification gate advance.

### Pass 748 — terminal-current probes reproduce and close recorded rail balance

Previous turn: progress. Preserved live terminal-current run through completion.
Fresh checker verifies clean209.9ns data, exact local-copy scope, artifact/source
hashes and all six probes. Reproduction maximum voltage error3.404uV passes the
unchanged10uV limit; all24 full-swing decisions match. Full terminal-current
analysis is therefore authorized for this diagnostic waveform.

Including feedback-gate and both output-drain wrapper currents leaves maximum
absolute charge residual0.01243fC across the tested windows. Preclock equivalent
residual maxima61.47/53.88nA high/low; switching11.30/8.80nA, versus prior omitted-
terminal residuals reaching tens of uA. Gate charge alone spans−8.896..1.311fC
high and−1.624..6.272fC low during switching, so its omission was not negligible.

Evidence: reference-terminal-probe.json and reference-terminal-balance.json.
This closes observed partial-window charge accounting to a small numerical
residual, not a physical noise/error budget or autonomous reference model.
Next separate terminal dynamic charge from channel conduction on the same
accepted trace, then choose state variables and an independent validation input.
Do not fit recorded rail trajectories and call them predictive. RF bias-restoration
experiment remains a separate live run; no physical qualification gate advances.

### Pass 749 — decompose accepted terminal charge beyond channel conduction

Previous turn: progress. Extended reference-terminal balance analysis on the same
accepted waveform with signed channel-current subtraction. Reuses selected-DC
probe convention evidence and requires positive savedVDS throughout each window.
Checks algebraic reconstruction: channel-only residual equals feedback-gate plus
both beyond-channel drain terms plus the full-terminal residual, within1e-24C.
Reproduction and charge-balance checks rerun successfully; residual unchanged.

Maximum absolute switching beyond-channel charges high/low: load drain39.848/
38.347fC, output drain2.851/7.682fC, feedback gate8.896/6.272fC. Preclock load
drain maxima2.906/5.180fC also exceed the other individual terms. Maxima need not
occur in the same window; these are not additive worst-case bounds. Differences
include displacement/junction/wrapper terms, not uniquely identified capacitances.

Evidence: updated reference-terminal-balance.json. Next test a load-drain charge
law with frozen parameters and held-out windows, then different stimuli, before
using it in a causal reference model. Full reference internal-state dynamics
remain necessary. RF bias-restoration run verified live; no physical gate advance.

### Pass 750 — frozen constant load-drain charge hypothesis

Previous turn: progress. Added reference_load_charge_fit.py with waveform/report
hash checks and independent constant-capacitance/polarity controls. Fits Q=C*dV
without intercept using only the16 preclock/switching windows of the first70ns
conversion per rail. Parameters freeze before scoring32 windows in the other
two conversions. No measured charge from held-out windows enters the fit.

Fitted high/low capacitances0.9419/0.9370pF are positive. Maximum high-rail errors
train1.478fC, held-out2.018/1.530fC; low errors0.234fC, held-out0.277/0.219fC.
Thus a fixed capacitance captures much of the38–40fC switching charge, but its
remaining error is far larger than the approximately0.0125fC full-terminal KCL
residual. Do not equate that residual or fitting accuracy with physical accuracy.

Evidence: reference-load-charge-fit.json. This is a constitutive-law screen using
measured rail endpoints, not an autonomous rail prediction. Next examine voltage
dependence/other terminal motion, especially high rail, with frozen validation
and eventually a different stimulus. No charge-error budget or physical gate
is claimed passed. RF bias-restoration run verified live.

### Pass 751 — inspect shared load-gate bias before nonlinear fitting

Previous turn: progress. Topology inspection found RBN/RBP are20uA current-fed
diode bias nodes, not ideal voltage sources. Each drives a load device on one
rail and tail device on the other. Gate motion and cross-rail coupling could
therefore contribute to apparent load-drain charge error. Existing accepted
wrdata omitted these nodes, so rail-only voltage dependence is not identifiable
from the current endpoint fit alone.

Added prepare_reference_bias_observation.py and runner. Copies accepted terminal-
probe preparation and appends only v(RBN)/v(RBP) to wrdata; save all already
includes them. Exact reversal restores the parent deck; circuits, saved-state
set, timestep, stimulus and local probe copies are unchanged. Predeclared gate
requires exact original-vector/time-grid equality, not merely10uV reproduction.

Launched transceiver-reference-bias-observation/session98705,1CPU/4GB,same pinned
simulator. Verified live with advancing log; RF bias-restoration also live.
Next measure bias motion before increasing charge-law fit complexity. No measured
bias result, independent-stimulus validation or physical qualification yet.

### Pass 752 — exact shared-bias observation gate prepared

Previous turn: progress. Added check_reference_bias_observation.py. Verifies
parent acceptance, local copy/source hashes and exact wrdata-only reversal.
Terminal processing requires full clean finite209.9ns data, unchanged recursive
sources and byte-hashed artifacts. Every original waveform vector and time point
must match exactly; interpolation/tolerance cannot turn an output-only mismatch
into acceptance. Bias statistics are withheld on reproduction failure.

On acceptance, reports70–209ns bias ranges plus fixed preclock/switching endpoint
changes for all24 decisions, enabling the next frozen charge-law comparison.
Preparation/scope checks pass; terminal data pending. Bias motion alone will not
prove cross-rail causality or constitute an autonomous bias model. Both running
containers verified live; no restart or physical gate advancement.

### Pass 753 — risk reassessment after rail accounting and LO mechanism evidence

Previous turn: progress. Updated leading risk-priorities to distinguish completed
common-mode intervention/full-terminal accounting from the two active physical
experiments. Kept autonomous timing and causal converter fidelity as leading
risks. The0.0125fC recorded balance residual is not model predictive error; frozen
load-charge fit errors remain larger, and shared bias motion is unresolved.

Set explicit next validation order: observe bias, freeze a physically motivated
charge law, test another amplitude/code history, then predict rail/code behavior
without measured reference trajectories. LO remedy needs autonomous reverse-load
and startup validation after its replay result. Existing lifecycle screens remain
separate; neither their passing status nor narrow operating-point permission
closes physical timing, noise, package or process uncertainty. Verified existing
RF run near320ns and bias observation near25ns, both advancing; no restart or
new physical sweep. No qualification gate advanced.

### Pass 754 — predeclare two-terminal load-charge screen

Previous turn: progress. Added reference_two_terminal_charge.py, gated on fresh
exact bias-observation reproduction and linked terminal/constant-fit hashes.
Candidate law Q=Cds*dVdrain+Cdg*(dVdrain−dVgate) uses the correct RBN/RBP load
bias mapping. Fits only first-conversion windows, freezes coefficients, then
reports held-out errors beside the previous constant model. Normalized design
conditioning and coefficient signs remain visible; rank-deficient/unexcited
fits are rejected and negative capacitance coefficients are not clipped.

Independent known-coefficient recovery and unidentifiable-design controls pass.
Bias data remains pending, so no physical coefficients or improvement report is
produced. This remains a measured-endpoint constitutive screen, not rail-state
prediction or independent-stimulus validation. Both targeted simulations verified
live; no restart or physical gate advance.

### Pass 755 — distinguish accurate charge fits from identifiable parameters

Previous turn: progress. Extended two-terminal charge screen with coefficient
sensitivity to independently bounded charge perturbations: row sums of the
scaled pseudoinverse report F/C gain. This is not an assumed numerical/physical
error bound; it excludes voltage-input error and model inadequacy. It complements
rank and normalized-condition reporting rather than treating positive fitted
capacitances as proof of unique device identification.

Synthetic signed perturbations attain the predicted per-coefficient worst-case
linear sensitivity. Nearly parallel drain/gate-motion columns amplify parameter
sensitivity by over1000x while remaining full rank. Controls pass; actual bias
observations are still pending, so no physical parameter sensitivity is claimed.
Both targeted containers verified live. No restart or physical qualification.

### Pass 756 — independent-amplitude validation fixture prepared

Previous turn: progress. Added prepare_reference_charge_validation.py. Changes
only VIP/VIN amplitudes from1.45/1.85V to1.60/1.70V: differential magnitude400mV
to100mV, same1.65V common mode and transition/decision schedule. Exact reversal
restores the prepared bias-observation parent. All devices, six terminal probes,
output vectors, timestep and initial conditions remain unchanged. Manifest pins
the existing frozen constant-capacitance fit and coefficients.

Prepared only, not launched: the current bias-observation result must first pass
exact reproduction and any selected two-terminal law must freeze before this
new fixture executes. Then score all windows without refitting and verify KCL;
a changed stimulus cannot inherit old-vector identity. Independent amplitude
validation remains distinct from autonomous rail/code prediction. Both current
simulations verified live; no restart or extra broad sweep.

### Pass 757 — enforce fit/validation separation at launch

Previous turn: progress. Added freeze_reference_charge_validation.py and guarded
runner. Verifies exact amplitude-only deck changes, unchanged probe copies and
pinned constant-fit hash. Requires freshly accepted exact bias observation and
current two-terminal fit dependencies before writing immutable coefficient/hash
snapshot. Existing differing snapshots cannot be overwritten, and initial freeze
is rejected if the validation output directory already exists. Runner calls this
gate before creating its output directory or launching simulation.

Validation scores all predefined windows without refitting; unidentifiable or
negative-coefficient status remains explicit instead of silently clipping or
selecting parameters using new data. Check-only scope/provenance checks pass;
accepted bias data pending, so no snapshot or validation simulation launched.
Both current physical containers verified live. No physical gate advanced.

### Pass 758 — independent frozen-charge scorer prepared

Previous turn: progress. Added score_reference_charge_validation.py with no
fitting operation. Requires frozen model/source hashes, prepared/run deck equality,
clean finite terminal data and source identity with the bias-observation parent.
Will report all96 rail/window combinations, constant and two-terminal prediction
errors, coefficient admissibility and full-terminal KCL residual. Negative or
unidentifiable fits remain labeled; no validation window is used for retraining.

Known-coefficient prediction, zero-motion, polarity and integration controls pass.
Frozen model and terminal validation data are absent, so scorer correctly emits
pending status without a physical report. Result-dependent code remains to be
exercised. Charge-law validation does not predict rail trajectories, converter
codes or physical performance; explicit code-history verification remains part
of the validation follow-up. Both current simulations verified live, no restart.

### Pass 759 — check validation SAR trial and decision history

Previous turn: progress. Added sar_conversion_history.py and connected it to the
independent charge scorer. Samples all eight trial codes before evaluation,
requires complementary full-swing comparator decisions, reconstructs retained
bits from QN, and compares the final code plus DONE. Records preclock residues
without claiming comparator correctness from the sequence alone. Every conversion
gets an explicit consistency status in the future validation report.

Independent85/all-zero/all-one sequence controls pass. Applied to the accepted
terminal waveform, all three histories are consistent with final codes77,179,76.
Inverting the savedD0 trace is detected. Scorer controls/pending behavior pass;
independent validation remains unlaunched until accepted bias data and frozen
coefficients. Logical sequencing is separate from analog accuracy or correct
nominal quantization. No physical qualification gate advanced.

### Pass 760 — nine-scenario regression after fault-latch integration

Previous turn: verified wait. Added late burst corruption/RF fault propagation to
run_architecture.py and documented the behavioral adapter's streaming exposure,
instantaneous mute assumption and missing recovery/CDC pieces. All nine scenarios
pass freshly against one unchanged source snapshot, including current latched
burst decoder, timed phase sweeps, RF reset, wired reset, clock offsets and
modulated/multicarrier paths. Evidence: connected-architecture-suite.json.

This validates existing scenario composition, not a complete unified lifecycle
or transistor implementation. Rechecked bias-model analysis gate: observation
still pending; no coefficients or validation launch inferred from partial data.
Both current physical simulations verified live at pass start; no restart.

### Pass 761 — bias motion measured; passive two-terminal fit rejected

Previous turn: progress. Bias-observation run completed cleanly and all original
vectors/time points reproduce exactly. Over70–209ns RBN varies7.316mV peak-to-peak,
RBP5.118mV. Frozen two-terminal fits from first-conversion windows are full rank
but poorly conditioned (normalized condition449/357 high/low). High coefficients
Cds−1.854pF,Cdg2.966pF; low−0.454pF,1.452pF. Both fail passive coefficient signs.
High held-out error does not improve (maximum2.070fC versus constant2.018fC).
Low fit error drops to0.0177fC, but a favorable fit cannot justify negative
passive capacitance or unique parameter identification. No such model is adopted.

Evidence: reference-bias-observation.json and reference-two-terminal-charge.json.
Kept the positive constant-capacitance candidate and the rejected-sign fit as
explicitly labeled diagnostic comparators. Guarded runner froze all coefficients,
statuses and source hashes before launching independent100mV amplitude validation.
Snapshot: reference-charge-validation-frozen.json. Launched
transceiver-reference-charge-validation/session44665,1CPU/4GB,same pinned simulator;
verified live. No refit may use its new data. RF bias-restoration remains live.
No autonomous reference prediction or physical qualification is established.

### Pass 762 — independent AC columns correct passive-fit interpretation

Previous turn: progress. Added load_terminal_ac.py and runner: actual GF180 load
wrappers w8u/l0.5u/m256, independent drain and gate AC excitations at nominal
bias,27C,1MHz–1GHz. First attempt failed because generated nodeGnd is a SPICE
ground alias; preserved failed artifacts, renamed nodes and ran a newv2 output.
Clean rerun has finite data, stable PDK/script hashes and verified artifact hashes.

Effective drain-charge derivatives dQd/dVd and dQd/dVg: NMOS1.066/−1.004pF,
PMOS0.995/−1.379pF at selected biases. Mapping to the previous law gives
Cds=dQd/dVd+dQd/dVg: positive0.061pF NMOS but negative−0.384pF PMOS. Thus the
PDK itself need not map to two positive reciprocal lumped capacitors. Correct
interpretation: rejecting the passive-capacitor realization is justified;
negative fittedCds alone does NOT invalidate a general MOS terminal-charge law.
The PMOS fit sign may reflect real nonreciprocal charge partition rather than
mere fitting failure. High-rail fit conditioning/error remains a separate concern.

Evidence: load-terminal-ac.json. Selected-bias linearization is not a large-signal
charge law, shared-bias dynamics, physical measurement or qualification. Frozen
independent-amplitude models remain unchanged; judge prediction separately from
passive realizability. Next compare independent AC derivatives with transient
charge-law validation before choosing model structure.

### Pass 763 — independent AC derivatives predict low-rail transient charge

Previous turn: progress. Added reference_ac_charge_prediction.py. Uses1MHz
independently excited drain/gate AC derivatives directly, with no transient fit.
Checks AC artifact/PDK hashes and exact-bias-observation linkage to the accepted
terminal waveform. Predicts all existing charge windows from measured endpoint
changes; frozen independent-amplitude comparator files remain unchanged.

Maximum high-rail error1.947fC (constant fit2.018fC); low0.02617fC (constant fit
0.2770fC), RMS0.00880fC low. Low-rail improvement supports the terminal-derivative
interpretation despite its negative two-capacitor Cds mapping. Persistent high-rail
error is not resolved by independent linear capacitances. Evidence:
reference-ac-charge-prediction.json. Next check steady terminal-minus-channel DC
current at the actual high-rail bias before assuming all beyond-channel charge
is displacement or increasing nonlinear fit complexity. Measured voltages are
still inputs; neither rail dynamics nor silicon accuracy is predicted. Both
current physical simulations verified live; no gate advancement.

### Pass 764 — high-rail beyond-channel term contains steady body current

Previous turn: progress. Added load_terminal_dc.py with independent drain, gate,
body and source voltage-source current measurements at three drain biases per
load type, actual256x geometry and nominal gate biases. Clean run, finite outputs,
PDK/script hashes and artifacts verified. Terminal KCL closes below1.3e-17A;
drain-minus-signed-reported-channel current matches opposite body current within
1e-14A at all six points. No displacement exists in this DC experiment.

NMOS excess drain current0.738/1.348/2.334uA at2.05/2.15/2.25V drain; PMOS
−9.04/−3.96/−1.77nA at1.05/1.15/1.25V drain. Nominal high-rail DC excess alone
integrates to1.483fC over1.1ns, comparable to the unexplained high-rail charge-law
error. Therefore terminal-minus-channel integrated current cannot be treated
entirely as displacement charge. This corrects the intended reduced-model target:
retain a steady bias-dependent current term plus terminal-charge derivatives.

Evidence: load-terminal-dc.json. Selected DC points do not identify the detailed
body-current mechanism or its full bias dependence. Independent-amplitude frozen
comparators remain untouched; new DC evidence must be evaluated separately.
No autonomous reference model or physical qualification claimed.

### Pass 765 — persistent mathematical chip lifecycle

Added system_model/connected/whole_chip_lifecycle.py, joining real host framing,
finite RF burst decoding, the retained-state DAC/filter consumer, wired channel
and session controls. Four scenarios cover both mode-change directions and
reference loss in flight, early/wrong-epoch drain acknowledgement rejection,
stale-event rejection, reacquisition, successive partial/empty burst descriptors,
and host-watchdog mute. The controller quiesces both engines for mode changes;
restored reference alone cannot clear the fault or bypass the drain barrier.

This is a candidate executable control contract, with assumed acquisition time,
instant loss detection and coherent management delivery. Epoch IDs are simulator
bookkeeping rather than added transport fields. This screen consumes samples
on arrival; independent paced bidirectional paths remain separately exercised.
The new scenario is part of run_architecture.py. Architecture closure and
physical qualification remain false; this pass does not change frozen analog
charge comparators or interrupt existing transistor simulations.

### Pass 766 — separate steady current from displacement charge

Previous turn: progress. Both long-running transistor jobs were revalidated live;
independent100mV reference validation then completed cleanly and was scored with
its frozen comparator hashes intact. All96 windows scored, and three SAR trial,
decision and DONE histories are internally consistent. Frozen constant-model
maximum errors high/low2.952/0.418fC; two-terminal3.039/0.0810fC. This is not a
physical acceptance threshold or ADC transfer qualification.

Added reference_ac_dc_charge_prediction.py, checking independent DC artifacts,
source consistency and common AC/DC PDK hashes. No transient fit: nominal excess
DC current times the window duration is added to independent AC charge prediction.
On the original96 windows high-rail max/RMS error falls1.947/1.192→0.464/0.170fC.
Low-rail max rises0.0262→0.0305fC while RMS falls0.00880→0.00784fC. This supports
the omitted steady-current explanation without claiming all errors are resolved.

Evidence: reference-ac-dc-charge-prediction.json and reference-charge-validation.json.
Next evaluate the independent AC+DC model on the new100mV stimulus, separately
from the already frozen comparators, before increasing model complexity.
The RF bias-restoration replay remains live beyond874ns/1001ns; preserve it.
Risk priorities refreshed; autonomous timing and causal converter dynamics still
lead. No physical gate advanced and tapeout readiness remains unproven.

### Pass 767 — independent-amplitude AC plus DC charge prediction

Previous turn: progress. Added reference_ac_dc_validation.py. It runs fresh
parent provenance gates, retains the frozen comparator files, checks all96
unique windows and applies exactly the independent nominal AC derivatives/DC
currents without fitting the new waveform. Because this diagnostic was chosen
after waveform completion, it is explicitly not a pre-registered comparator.

New100mV stimulus high-rail maximum/RMS error falls2.040/1.188fC AC-only to
0.558/0.183fC AC+DC. Frozen constant/two-terminal maxima are2.952/3.039fC.
Low-rail AC-only maximum0.1047fC rises slightly to0.1091fC with nominal DC;
frozen two-terminal maximum0.0810fC is better here. Full terminal KCL residual
maxima0.01624/0.01041fC are smaller than these remaining errors, supporting a
remaining model discrepancy rather than complete charge-accounting failure.

Evidence: reference-ac-dc-validation.json. Measured voltage endpoints remain
inputs; there is no autonomous converter/reference prediction or physical
acceptance budget. Next probe independent drain/gate bias dependence over the
actually observed envelope, keeping both residual signs rather than assuming
nominal correction is universally beneficial. Do not refit the validation data.
The RF replay was verified live past909ns/1001ns; exact two-resistor scope and
unchanged recorded input checks still pass, with terminal result pending.

### Pass 768 — independently probe the observed load-bias envelope

Previous turn: progress. Added reference_load_bias_envelope.py, verifying both
completed transient artifacts/source sets and measuring drain/gate extrema with
explicit70–209ns endpoints. Startup extrema are separately retained. Padded
rectangles: NMOS drain2.05–2.23V/gate0.790–0.802V; PMOS drain1.07–1.25V/
gate2.268–2.278V. These cover observed samples, not all histories or uncertainty.

Added load_terminal_dc_grid.py and runner: independent5x5 DC grid per device,
unchanged256x8u/.5u geometry, typical27C. check_load_terminal_dc_grid.py verifies
all50 unique intended combinations, source/artifact hashes, terminal KCL and
opposite body-current equality below1e-14A. Published load-terminal-dc-grid.json.
NMOS excess current spans0.703–2.207uA; PMOS−7.686 to−1.766nA. Over1.1ns,
NMOS contributes0.773–2.428fC versus nominal1.483fC. This supports testing bias-
dependent DC correction before interpreting remaining error as charge nonlinearity.

Next integrate independent-grid current along recorded drain/gate trajectories,
reject extrapolation, and keep AC endpoint charge separate. Grid interpolation
must itself be checked; the sampled extrema are not rigorous continuous bounds.
No transistor fitting to validation data or physical qualification. RF replay
remains live past949ns/1001ns and must not be restarted.

### Pass 769 — trajectory-dependent reference current diagnostic

Completed reference_bias_current_prediction.py before the user requested a
refocus on full simplified architecture closure. All192 windows use independent
DC-grid interpolation without extrapolation and nominal AC derivatives. Fixed
PDK path comparison to compare actual loaded filenames/content across canonical
and alias prefixes; required design/model names prevent an empty comparison.
Bilinear analytic controls and outside-grid rejection pass. Midpoint subdivision
changes integrated charge by at most1.42e-22C; this does not validate interpolation.
Original/new high-rail maximum errors0.0718/0.1688fC versus nominal-DC0.464/0.558fC.
Low maxima0.0307/0.1092fC are essentially unchanged. Recorded trajectories remain
inputs, and off-grid transistor validation remains open.

The already-running RF replay also completed. Exact scope/provenance checker
passes.10k I-leg bias restoration improves minima to2.542/2.546V and removes
high-trough cycles, but leaves12 weak-peak cycles per I leg of508. It is not a
qualified remedy or autonomous oscillator result. Evidence retained in
lo-bias-restoration.json. No new physical simulation launched during refocus.

### Pass 770 — independently timed DAC in the persistent mathematical controller

Previous pass: progress. Following user clarification, priority is completing
the simplified whole-chip model before deeper physical refinement. Added
connected/timed_lifecycle.py extending the existing framed lifecycle controller.
Eight cases across both modes,±100ppm and two host phases use real burst/frame
transport and independently scheduled40/20MS/s DAC deadlines. Reference loss
occurs after one consumed sample with two queued: both are discarded, held DAC
mutes, analog state decays continuously, and recovery changes mode. A scheduled
sample with no data latches underflow. Simultaneous DAC edge precedes delivery.

New targeted eight-case screen and existing four-case lifecycle screen pass.
Added the new screen to the suite runner; the earlier10-scenario aggregate is
historical and was not rerun this pass. Small prefilled bursts do not prove
full-rate behavior. Wired consumption and RF receive/return still need events
in this persistent controller, followed by explicit mathematical PLL/reference/
supply assumptions. Complete architecture and physical qualification remain false.

### Pass 771 — shared independently paced wired and DAC transmit paths

Previous pass: progress. Preserve revised user ordering: complete the full-chip
mathematical model, then transistor schematic, then begin layout. Added a wired
acceptance hook to the existing lifecycle controller and a finite128-word queue
in TimedChip. The chronological event loop services independent DAC and wired
word clocks, with deterministic coincident-edge ordering before host arrival.
Quiesce cancels both schedules, discards pending data with conservation checks,
and preserves analog RF filter state. Missing wired payload latches a fault.

Eight simultaneous cases across two modes and independent±100ppm errors verify
prefill, no premature output, actual word order, both occupied queues at reference
loss, mode-change recovery, new wired period and underflow. All16 timed cases and
the existing four lifecycle cases pass. The aggregate architecture suite remains
historical; this pass ran the affected screens. Wired events process ten-bit
words rather than continuous partial-word state. RX/host return remains to be
integrated; no full-chip completion or physical qualification claim.

### Pass 772 — persistent RF sampling and framed host return

Previous pass: progress. Added connected/rf_return_lifecycle.py, extending the
shared independently timed TX controller with ADC sample events, ideal coherent
RF gain, quantization, bounded return queue, frame staging and independent host
word-clock delivery. Real finite-burst packing, frame encoding, host decoding
and final-padding validation carry actual captured values. Wired TX runs during
the RF round trip. The first ADC sample is checked against the analytic TX
filter step response, not solely a copied expected payload.

Eight cases (two modes,opposite100ppm offsets,normal/interrupted return) pass.
Reference loss cancels capture/return, discards staged data and permits no later
host samples; host abort/reset is presently a coherent management assumption.
The new scenario is registered in the aggregate runner; only its targeted screen
was run this pass. RX filtering/noise, wired RX, realistic recovery and continuous
throughput remain open. No whole-chip or physical completion claim.

### Pass 773 — all four paths share the persistent controller

Previous pass: progress. Added wired_return_lifecycle.py using existing channel,
transition-only recovery and marker framing. Recovered words enter a finite
128-word RX return queue at causal completion times, share timed host frames
with ADC payload, and pass through the external host decoder. Independent wired
and DAC TX continue in the same controller. Sixteen cases cover two modes,
±0.3UI phase,±100ppm and interruption: normal cases preserve80 wired words and
three ADC samples; fault cases preserve only a prefix and emit no later stale
words. RF-return and timed-lifecycle regressions also pass (8 and16 cases).

Added aggregate scenario; full suite not rerun this pass. Four-direction finite
traffic is now connected, not mathematically complete. Recovery is batch-computed
from a fixed external waveform; shared disturbance feedback, partial-frame
retraining, realistic clocks/analog assumptions and sustained traffic remain
open. Next close lifecycle/clock assumptions without resuming transistor detail.

### Pass 774 — bounded sampling jitter reaches the connected signal path

Previous pass: progress. Added SampleClock with absolute nominal phase and
bounded four-edge timing modulation; quarter-period-or-larger amplitude and
nonfinite clock parameters are rejected. ADC/DAC events now use this clock,
with zero jitter as the default. Clock controls check1000 periods for bounded
error, monotonicity and no accumulated phase drift.

Ten new four-path cases pass with opposite ADC/DAC jitter±100ps/±2ns across
both modes and reference-loss interruption. Larger modulation changes ADC codes,
proving it affects the modeled signal, while host transport preserves captured
samples. Existing timed16,RF-return8 and four-path16 cases also pass. Registered
the new aggregate scenario; complete aggregate rerun remains outstanding.

This is periodic timing sensitivity, not realistic random phase noise or PLL
acquisition. Common-mode timing noise, RX filtering/impairments, sustained traffic
and explicit partial-frame recovery remain to be integrated before mathematical
completion. Transistor and layout stages remain downstream of that completion.

### Pass 775 — continuous RX filtering in the persistent RF path

Previous pass: progress. Replaced unity RX bandwidth with RfCascadeState, an
analytic equal-pole TX/RX cascade driven by actual DAC holds. The RX filter sees
the continuous TX waveform rather than held endpoints. Every event propagates
both states; reset mutes the DAC but preserves TX and RX charge. Controls verify
the closed-form step,100-way event subdivision and post-reset tail to1e-14.
The integrated first-sample oracle now uses the two-pole response.

All34 affected RF-return,four-path and jitter scenarios pass. Full architecture
regression launched to refresh evidence after the accumulated connected-model
changes. Equal10MHz poles and coherent gain remain explicit assumptions; arbitrary
RX bandwidth, noise/compression, PLL dynamics and recovery remain open. Completion
of the mathematical model is still unproven; no schematic/layout progression.

Pass775 regression outcome: all14 aggregate scenarios passed with unchanged
source hashes across the run. Fresh manifest: connected-architecture-suite.json.
This verifies the declared scenarios only; complete_architecture remains false.

### Pass 776 — explicit host abort before epoch recovery

Previous pass: progress. Removed automatic host-abort assumption from ReturnChip:
quiesce retains external decoder state until acknowledge_host_abort names the
stopped epoch. Drain acknowledgement is forbidden before that barrier, so mode
reconfiguration cannot silently reset a partial host frame. Delivered samples
remain visible; only incomplete decoder state is discarded.

All128 frame-position/both-mode recovery cases pass on persistent DuplexChip
objects, with137ns management delay, early/wrong/stale acknowledgement controls,
mode change and fresh three-sample/three-wired-word delivery. Existing four-path16
and jitter10 cases pass. New scenario added to aggregate runner; full aggregate
not rerun this pass. Serialized management/CDC and pin-level frame acquisition
remain assumptions; zero-input RF recovery is not modulated-throughput proof.
Full mathematical model remains incomplete; schematic/layout stages stay pending.

### Pass 777 — finite sustained four-path traffic with causal production

Previous pass: progress. Added sustained_lifecycle.py:128 production intervals,
129 actual host frames,three-frame playback prefill and all four paths at both
contract modes/±100ppm.4096/6553 wired words and1310/524 complex samples per
direction arrive intact with empty payload queues and no underflow. Observed
wired TX peaks65/103 words and RF TX20/8 samples; peak sampling is not a bound.

The first fixture incorrectly made one future frame of production visible;
its mode1 overflow therefore did not prove real FIFO insufficiency. Corrected
production availability and reran successfully. Separately replaced hardcoded
TX capacities with the existing2048-bit planning budget:204 wired words and
85/128 RF samples. This budget is not actual RTL-depth evidence. Recovery128
and timed16 regression cases pass after the capacity change.

Added aggregate scenario; targeted checks only this pass. Finite26–33us traffic
is not long-term drift proof. Source phase/CDC sweeps, predictive PLL/reference
states, RF impairments and shared supply effects remain mathematical gaps.

### Pass 778 — dynamic near-lock state gates the persistent controller

Previous pass: progress. Added clock_lock_lifecycle.py: analytic critically damped
phase/frequency evolution,40MHz observation and eight-consecutive-sample lock
qualification in a DuplexChip variant. Fixed-delay lock is disabled in that
variant, including reference restoration during acquisition. Both modes and both
phase/frequency error signs pass eight startup/receive/disturbance cases. Analytic
subdivision, sign symmetry and lock-count/reset controls also pass. An injected
phase error quiesces both paths after the next reference observation.

This is explicitly near-lock linear dynamics with assumed thresholds/bandwidth.
It gates readiness but does not yet set payload edge phase; integrate that next
before claiming an autonomous mathematical timing model. Nonlinear capture,
saturation and reference noise remain open. Registered in aggregate runner;
only targeted new tests ran this pass. Whole-model completion remains false.

### Pass 779 — loop phase determines ADC/DAC event times

Previous pass: progress. Added LoopEdges and PhasedChip. Sample deadlines solve
an implicit phase equation using the same analytical near-lock trajectory as
readiness qualification. Reference-cycle phase maps to absolute timing error;
nominal ppm and bounded jitter remain explicit inputs. Controls verify zero-error
agreement, sign reversal, residual, monotonicity and state-subdivision invariance.
Four connected both-mode/sign cases preserve payloads while first DAC deadlines
shift by±0.073ps/±0.180ps after prefill. No claim that these assumed values predict
physical clock quality.

Trajectory is snapshotted for undisturbed propagation. Disturbances during live
playback still require invalidating future deadlines; serializer and host clocks
remain prescribed. Added aggregate scenario; targeted tests pass, aggregate not
rerun. Full mathematical closure remains open before transistor/layout work.

### Pass 780 — active loop disturbances reschedule pending sample edges

Previous pass: progress. Added explicit PhasedChip disturbance API: evolve old
state to intervention, change phase/frequency, preserve sample indices, solve
new ADC/DAC deadlines and commit both together. Bracketing now clips to the
state timestamp; roots at/before intervention or outside the bracket fault
rather than silently replaying/skipping edges. Both schedules and queued data
are cancelled through existing shared quiesce.

Four active signed disturbance cases preserve all four paths and three ADC/DAC
samples. Two near-edge positive phase steps trigger explicit discontinuity faults,
discard both queued DAC samples and deliver no later host samples. Previous four
undisturbed loop-edge cases and analytic controls still pass. New aggregate
scenario registered; targeted checks only. Discrete linear interventions are
not stochastic noise, nonlinear cycle-slip recovery or serializer/host clock
coupling. Full mathematical completion remains open before schematic/layout.

### Pass 781 — sustained traffic on the loop-driven controller

Previous pass: progress. Reassessed isolation between simplified sustained tests
and short dynamic-clock tests. Parameterized the same sustained workload with a
controller factory; dynamic variants wait for qualified lock rather than a fixed
startup timestamp. Added clocked_sustained_lifecycle.py using PhasedChip across
both modes,±100ppm and both intervention signs. Each run changes phase/frequency
at frames40/80 and verifies signed shifts of both pending sample deadlines.

Eight sustained cases and16 interventions pass, preserving all wired/RF payloads,
empty final queues and no underflow. Original four sustained cases rerun and pass.
This joins the most detailed current lifecycle/clock/filter/transport blocks for
finite sustained traffic rather than inferring integration from separate tests.
Registered aggregate scenario; no full rerun this pass. Next larger gaps are RF
impairments, causal reference/converter behavior and shared-supply effects, with
serializer/host clock physics still prescribed. Mathematical completion remains
false; transistor schematic and layout stages remain downstream.

### Pass 782 — causal reference state in sustained full-path traffic

Previous pass: progress. Added ReferenceChip with finite capacitance, resistance
recovery and an explicit aggregate charge impulse after actual ADC conversions.
Previous conversions causally alter later reference span and quantization; no
recorded transistor waveform is used. State is retained across lifecycle resets
and recovered according to elapsed time. Invalid/nonfinite component parameters
and nonphysical load-capacitance ratios are rejected.

Six sustained loop-driven cases pass, including signed clock interventions,
exact zero-load equivalence and analytic charge/recovery/subdivision controls.
Weak-drive minima0.9125/0.9496V versus ideal1V alter ADC words in both modes while
all captured payloads arrive intact. Strong-drive minima0.9964/0.9972V. Values
are hypothetical sensitivity settings, not actual GF180 capabilities or worst
case bounds. Intra-conversion SAR switching and DAC/shared-supply loading remain
open. Added aggregate scenario; targeted new tests only this pass.

### Pass 783 — sampled receiver mismatch, compression and noise

Previous pass: progress. Added receiver_impairments.py extending the causal-
reference, loop-driven four-path controller. I/Q gain and basis phase errors,
radial compression and seeded per-conversion Gaussian noise alter actual ADC
input and subsequent reference loading. Noise draws depend only on conversion
count, not incidental host event subdivision. Added full ADC-stream SHA256 to
sustained evidence instead of relying solely on a weighted checksum.

Six sustained cases pass across both modes and both mismatch signs, including
zero-impairment stream-digest equivalence and changed impaired outputs. Analytic
basis/identity/compression controls and seeded noise repeatability/power checks
pass. This is ADC/baseband frontend distortion, not RF LNA/mixer blocker behavior;
noise is sample-integrated, not a continuous PSD. Reported distortion is not modem
EVM or GF180 feasibility. New aggregate scenario registered, targeted checks only.

### Pass 784 — host switching reaches analog data through a shared RC rail

Previous pass: progress. Added CoupledChip atop the impairment/reference/loop-
driven controller. Actual H2D word transitions plus clock edges draw assumed
charge from an RC rail; analytic elapsed-time recovery precedes conversion.
Signed receiver-gain sensitivity links that rail to ADC data and reference load.
Six sustained cases pass with zero and±2/V sensitivities in both modes. Zero
coupling reproduces baseline stream digest. RC impulse, exact recovery and
subdivision controls pass. Both sensitivity signs change captured words while
all four transport paths preserve the resulting payloads.

Assumed100ohm/100pF and100fC per transition produce26.39/29.53mV peak droop and
sampled gain changes up to±4.85/±5.11%. These are intentionally explicit model
inputs, not GF180/package estimates or worst-case bounds. PLL/DAC and digital
threshold coupling, return-bus/internal switching and inductance remain open.
New aggregate scenario registered; targeted checks only. Full mathematical model
completion and subsequent transistor/layout stages remain pending.

### Pass 785 — return-bus activity closes a causal supply feedback path

Previous pass: progress. Added emitted-word hook to timed host return and optional
D2H switching charge in CoupledChip. Actual clock/data transitions, including
headers/idle slots, draw from the same RC rail as H2D activity. Coincident ADC
sampling precedes return switching, so earlier ADC data can influence later
samples through output activity without a same-sample algebraic loop.

Six sustained signed/zero-sensitivity comparisons pass, along with a direct
24-transition control and total input/output charge conservation. Zero coupling
preserves ADC digest; nonzero output coupling changes it while all captured data
still returns intact. Existing eight RF-return cases pass. New scenario registered;
full aggregate remains outstanding. This is an assumed shared RC, not a domain/
package extraction. PLL/DAC and internal switching coupling remain open.

### Pass 786 — shared supply changes DAC amplitude at actual updates

Previous pass: progress. Added sampled DAC gain hook to RfTxState before hold;
CoupledChip derives it from the same rail affected by both host buses. Zero gain
sensitivity preserves baseline behavior. Held amplitude stays fixed until the
next update, preserving analytic TX/RX evolution. Invalid gain is rejected before
removing a queued sample. DAC update count remains tied to actual consumption.

Six sustained both-mode/zero/signed-sensitivity cases pass. Analytic hold and
queue accounting controls pass, as do16 timed and8 RF-return regression cases.
Nonzero DAC coupling changes actual returned ADC words, zero coupling matches
baseline stream digest. New aggregate scenario registered; targeted checks only.
This sampled-gain assumption omits continuous supply feedthrough and DAC load
current; PLL supply response/domain/package dynamics remain open. Full-chip
mathematical completion remains unproven before schematic/layout stages.

### Pass 787 — audit full mathematical scope against intended architecture

Previous pass: progress. Compared persistent model against block-diagram.md,
clock-rate-ownership.md and contract.json. Added mathematical-closure.json with
10 explicit requirement groups, source evidence and remaining obligations.
Notable missing scope: intended routing/capture/diagnostic functions and a
sustainable common-reference rate mechanism in the persistent model. Finite
signed-ppm runs cannot establish indefinite FIFO safety. Combined uncertainty
cases and correct RF blocker/LO ordering remain partial.

Separated mathematical assumptions from later transistor calibration: assumed
parameters are permissible, missing functions/causality/rate ownership are not.
Updated stale risk priorities and replaced obsolete suite gap text with the
source-linked inventory. The runner now hashes closure/basis documents too.
Full current aggregate regression launched; completion remains false regardless
of individual scenario passes. Next prioritize persistent rate ownership and
resource/control coverage over further isolated analog refinements.

Pass787 outcome: all25 current aggregate scenarios passed with unchanged source
snapshot, including closure/basis files. Manifest refreshed. This verifies the
implemented scenarios; the10-group closure inventory remains incomplete.

### Pass 788 — shared reference owns production and consumption rates

Previous pass: progress. Extended sustained workload with common-reference mode:
RationalPacer enables run from forwarded edges, independent H2D service rate
controls frame arrival, and local DAC/serializer use matching average ratios.
Exact integer reference/service ppm arithmetic controls source availability;
D2H service follows the chip reference. No credits, resampling or new pins added.

Eight signed reference/service cases pass on CoupledChip with dynamic lock and
active retiming; previous four unmatched finite-run regressions also pass.
Exact rational checks compare producer ratios to contract source rates/sample
widths and show service margin at opposite±100ppm endpoints. This removes
persistent rate slope only under the declared common-reference ownership.
Finite queues still need phase/service-gap/CDC and lock-transient bounds.
Updated closure inventory but left requirement partial; full model incomplete.

### Pass 789 — matched rates tolerate declared fixed visibility delays

Previous pass: progress. Added fixed source-to-service visibility lag and
fractional phase to the common-reference workload using exact rational arithmetic.
No future payload is exposed; final drain continues until all delayed samples
are generated and sent. Unavailable payload still faults the running consumer.

Sixteen cases across both modes,opposite±100ppm,2/32-word delay and±0.49-word phase
pass with unchanged buffers and three-frame prefill. Two256-word controls produce
explicit wired/DAC underflow, not synthesized payload or stopped clocks. Added
TrafficFault to distinguish that expected negative result from unrelated errors.
Registered the scenario and left rate-ownership closure partial: variable CDC,
service stalls, full phase coverage and long-duration bounds remain open.

### Pass 790 — host service pauses do not stop payload clocks

Previous pass: progress. Added frame-boundary H2D pauses to matched-reference
workloads. Service timestamps and rational source visibility use cumulative
elapsed time, so producers continue while host service stops. The persistent
controller advances DAC,serializer,ADC and D2H throughout each pause. No queue
capacity or prefill increase was made.

Eight cases with repeated16-word/single64-word pauses,2-word source visibility
and signed independent clock error pass. Per-pause counters confirm all three
payload consumers/capture engines keep running. Two256-word pauses produce
explicit underflow faults. Added scenario to suite and retained partial rate
closure: in-frame pauses,variable CDC and universal bounds remain open.

### Pass 791 — capture resource enters the persistent mathematical model

Previous pass: progress. Inspected pt_memory.sv before selecting bank behavior:
separate32x24 capture/playback arrays,one-shot capture,zero reads until done.
Added CaptureBank/CaptureChip; actual ADC words fill the bank,hold when full,
and are compared to the first32 returned ADC samples. Disarmed configuration and
quiesce disabling incomplete capture are explicit candidate control semantics.

Two sustained both-mode cases pass with simultaneous frontend/reference effects,
signed RX/DAC supply sensitivities,bidirectional switching and clock interventions.
Controls check partial reads,stop-on-full,rearm,new data and rejected live changes.
The report pins the inspected RTL hash but claims no equivalence/CDC/SPI proof.
Resource closure moves missing→partial; playback,routing/diagnostics and capture-
only MCU mode remain. Full mathematical model is still incomplete.

### Pass 792 — playback memory supplies actual DAC requests

Previous pass: progress. Added PlaybackChip with separate32x24 bank and a DAC
request hook in the persistent timed controller. Bank pointer advances only on
DAC edges; host IQ descriptors are forbidden while memory owns the source.
Disarmed writes,complete initialization,active format width and one-shot rearm
are checked before scheduling. Engine quiesce stops playback and retains data.

Four complete/interrupted both-mode cases pass, verifying32 ordered DAC samples,
ADC capture/host return,wired RX and interruption after eight samples. Controls
reject31-word initialization,live writes/source changes and exhausted replay.
Existing16 timed cases pass. Final amplitude is held after completion; global
memory reset and explicit mute/rearm policy remain to define/test. Resource
closure stays partial pending MCU-only mode,routing and diagnostics.

### Pass 793 — finite MCU-style operation without streaming host clocks

Previous pass: progress. Added ManagementChip with explicit local capture routing,
no H2D stream acceptance/no D2H clock generation and no streaming-host watchdog.
Actual DAC/ADC clocks continue under dynamic lock; completed capture is read by
coherent timed management transactions. Reference faults still quiesce/mute,
and interrupted capture remains unreadable.

Four both-mode complete/interrupted cases pass:32 playback/capture samples,
32 reads over320us without streaming traffic,and explicit fault behavior.
Existing128 return-epoch and8 RF-return cases pass. New scenario registered.
Timed reads are not SPI/CDC implementation,session readiness is an abstract
management-ready signal,and external RF input remains the loopback fixture.
Routing/diagnostics and repeated/global-reset memory behavior remain open.

### Pass 794 — repeated memory epochs and validity reset

Previous pass: progress. Tested three local capture/playback epochs on each of
two persistent chip objects with alternating modes and distinct waveforms. Found
and fixed an evidence bug: capture checks used the first-ever32 ADC samples;
rearm now records the current ADC-history offset. Captures match each epoch's
actual samples and differ from the prior epoch.

Added disarmed reset_memory: invalidates playback initialization/capture status,
rejects live reset and incomplete reload, and retains analog TX/RX filter state.
This represents memory validity reset, not asynchronous chip-reset implementation
or assumed SRAM bit clearing. Six epoch cases pass plus four management-only and
two integrated capture regressions. Routing/filter controls and diagnostics remain
missing resource functions; full mathematical completion stays false.

### Pass 795 — independent TX/RX filter programmability

Previous pass: progress. Generalized continuous cascade propagation to unequal
poles using stable expm1 near the equal-pole limit. Added disarmed independent
2/5/10/20MHz selections preserving analog state. A strict preservation test found
zero-duration arithmetic could perturb state by roundoff; zero duration is now
an exact no-op. Unsupported selections/live changes are rejected.

Six integrated playback/capture cases produce distinct ADC streams for tested
settings in both formats. Analytic step,near-equal-pole,100-way subdivision and
state-preservation controls pass, plus8 RF-return and10 jitter cases. Registered
aggregate scenario. Values are candidate settings, not physical tuning capability;
routing/gain resources,diagnostics and full reset remain open mathematical scope.

### Pass 796 — bounded receive routing and atomic gain/filter selection

Previous pass: progress. Added RoutedChip with exclusive external-tone,existing
external-loopback and mute selections,plus0.5/1/2 baseband gain and existing
filter choices. Validate the full configuration before state changes; reject
armed reconfiguration. Independent source enters before the RX filter and is
propagated analytically,not substituted after sampling. DAC playback continues
in all test cases to expose source-selection independence.

Twelve both-mode route/gain cases pass; analytic filtered-tone,subdivision and
configuration-atomicity controls pass,plus six programmable-filter regressions.
This introduces bounded resource selection,not the full intended resource graph.
External source is an ideal downconverted envelope and loopback is a fixture,
not a promised silicon RF route. Diagnostics/trim,full reset and RF LO/blocker
ordering remain unfinished mathematical scope.

### Pass 797 — explicit diagnostics and existing register-field readback

Previous pass: progress. Inspected pt_spi.sv and added DiagnosticChip snapshots
plus a limited register adapter using existing field addresses. Both modes pass
lock/active/partial-capture/completion/fault snapshots,all32 split capture reads,
stored trim readback and rejected armed trim writes. Snapshot copies stay stable
after later state changes. Report pins the inspected RTL source hash.

No packed status ABI was invented: address3 is explicitly unsupported pending
mapping; named snapshots have no claimed hardware register allocation. Trim
storage is marked unapplied rather than falsely treated as analog calibration.
Full SPI/CDC behavior,trim mappings and resource capability graph remain open.
New targeted scenario registered; full mathematical completion remains false.

### Pass 798 — independent RF oscillator conversion before filtering

The previous status-only turn made no implementation progress. Added independent
TX/RX LO frequency offsets and phases to the connected RF envelope cascade, with
exact continuous mixing before the receive pole. External sources depend on the
RX LO alone; common TX/RX LO shifts cancel only in the loopback fixture. Settings
are disarmed-only and validated atomically. Zero-offset defaults retain the old
cascade calculation. No GHz carrier time stepping is needed.

Ten integrated both-mode playback/capture cases pass, with exact host return and
32 consumed DAC samples each. Independent midpoint integration and subdivision
check the complex convolution, including nearly equal poles and signed offsets.
A 2MHz RX pole passes 89.4% amplitude at 1MHz mismatch but only 7.97% at 25MHz:
signal quality can fail while digital transport remains operational. The first
20,000-step quadrature missed the 2e-9 tolerance by about 5%; doubling numerical
resolution reduced the discrepancy without loosening tolerance. Routing and
filter regressions pass. Constant ideal oscillators are not autonomous physical
LOs; phase noise, acquisition, supply response, blockers/images and modem carrier
recovery remain outside this increment. Mathematical closure remains incomplete.

Fresh aggregate verification: all36 scenarios passed with unchanged source hashes,
including the new LO checks and all prior registered scenarios. This verifies the
declared scenarios; the full-chip closure inventory still contains open requirements.

### Pass 799 — RF blockers and weak nonlinearity before conversion/filtering

Previous pass made verified progress. Added a bounded complex RF-envelope cubic
x + alpha*x*|x|^2 before the ideal mixer and RX filter, with up to four independent
blocker tones. Exact exponential expansion integrates the cubic products through
the continuous receive pole without GHz carrier stepping or a numerical timestep.
The loopback expansion includes evolving TX filter state and independent LO offsets.
Mute disconnects the input, retaining filter decay. Configuration is atomic and
disarmed-only. A conservative input-envelope bound and |alpha|*limit^2 <=0.25
prevent extrapolation into an unmodeled overload regime; range violations stop the
simulation explicitly rather than pretending to model hardware fault detection.

Independent quadrature, event subdivision, signed third-order intermodulation,
range rejection and moving-TX/LO controls pass. Two blockers at10/20MHz produce
a DC product alpha*A^2*conj(B), demonstrating why filtering after a nonlinear
stage cannot remove all blocker damage. Six finite playback/capture cases and
four sustained simultaneous wired/RF cases pass in both modes, with positive and
negative coefficients, matched-reference source rates, host clock mismatch and
sample-clock disturbances. All sustained transport/accounting assertions hold.
The ten existing LO cases and twelve routing cases also pass.

This is weak RF-envelope nonlinearity, not a calibrated LNA/mixer or overload model.
Absolute RF gain/bandwidth, images, leakage, noise and comprehensive alias/blocker
limits remain open. The scenario is registered in the aggregate runner; this pass
ran targeted checks, so the previous36-scenario aggregate report is historical.
Full mathematical closure is still incomplete; schematic and layout work remain
behind that milestone.

### Pass 800 — causal RF phase trajectories and combined four-path impairments

Previous pass made verified progress. Added PhaseChip with scheduled TX/RX LO
phase and frequency changes. Advance the complete chip to each intervention under
the old oscillator settings, then change future mixing while retaining analog
filter charge. Frequency changes preserve instantaneous phase unless an explicit
phase step is requested. Coincident ADC/DAC events execute before intervention.
Invalid or reordered schedules are rejected atomically. Events represent external
physical disturbances, not software register writes or an RF lock detector.

Analytic phase/frequency response and event-subdivision controls pass. Four
sustained cases plus two baselines show bit-identical cancellation of common LO
phase in ideal zero-delay loopback, and changed ADC data for independent phase.
The fixture uses151 seeded bounded phase samples on a100ns grid from5us to20us,
with absolute phase bounded to +/-0.15rad. It is not a calibrated phase-noise PSD.

Four additional cases combine both signs of RF cubic coefficient, RX/DAC supply
sensitivity and frontend gain/phase error with blockers, reference charge loading,
frontend saturation/noise, independent RF phase trajectories, sample-loop
interventions, host-rate mismatch and a16-word-period service pause. Both
throughput modes pass sustained wired/RF transport, queue accounting and exact
ADC-code return. This closes the isolated-knob-only testing gap for those modeled
resources; it does not establish modem signal quality or hardware feasibility.
Ten LO regressions also pass. New scenario registered; aggregate report from
pass798 remains historical until rerun. Autonomous RF lock, supply-to-LO coupling,
noise spectrum, missing wired/control contracts and converter envelopes remain.

### Pass 801 — signal-quality classification beyond lossless transport

Previous pass made verified progress. Added held-out waveform comparison against
an ideal envelope/quantizer run with identical source, filter and sampling times.
Fit one complex gain on the first quarter, freeze it, and score remaining samples;
report raw error, corrected error and gain separately. No time alignment, adaptive
carrier correction or post-hoc equalizer hides LO errors. A0.5–2 gain range rejects
a nearly dead channel even if scalar normalization would conceal its loss.
A provisional10% incremental waveform RMS budget is explicit, not protocol EVM.

Sixteen impaired/ablation runs and four matched ideal runs complete with sustained
wired/RF accounting and exact ADC-code transport. Combined cases fail the screen
at25.5–26.9%; severe phase cases fail at89.6–100.8%. Removing blockers reduces
error to9.8–11.7%; removing RF phase variation alone leaves23.6–24.5%. These are
signal-quality failures in assumed conditions, not failed test infrastructure.
Synthetic controls verify identity, static gain/phase correction, rejection of
zero/low gain and a phase reversal confined to held-out samples. Updated risk
priorities to address selectivity and admissible blocker/phase budgets next.

The reference includes intended filtering and quantization, so this measures
incremental error, not total transmitter-to-demodulator EVM. A single-tone fixture
cannot establish Wi-Fi performance. No physical feasibility claim follows.
Registered scenario; targeted verification only, full aggregate remains historical.

### Pass 802 — preserve RF channel bandwidth while specifying selectivity

Previous pass made verified progress. Rechecked the intended roughly20MHz RF
channel against the10/20MHz blocker fixture:10MHz is at its nominal edge, not a
clean stopband point. Preserve that boundary stress test and add20/30MHz blockers
with identical amplitudes and all other combined assumptions unchanged. Both
modes still fail the provisional10% waveform budget:18.3/19.7% for20/30MHz versus
25.7/26.5% for10/20MHz (positive-sign cases). Six sustained runs including matched
ideal references pass transport and sampling-time checks. Nominal alias locations
are reported separately; they are not a complete phase-noise sideband spectrum.

A proposed8MHz/1dB passband and20MHz/30dB stopband yields minimum Butterworth
order5, cutoff9.1574MHz, and33.93dB loss at20MHz. The existing10MHz single pole
loses2.15dB at8MHz and only6.99dB at20MHz. The next-lower order fails the proposed
stopband requirement at a cutoff preserving the passband. These are explicit
candidate screening targets, not inferred Wi-Fi limits. No multipole filter is
yet implemented; the transfer-function calculation alone is not system or silicon
qualification. Next: integrate the candidate and a modulated wanted signal without
silently reducing channel bandwidth or changing the10% screen. Registered targeted
scenario; full aggregate remains historical. Mathematical closure remains open.

### Pass 803 — continuous fifth-order receive filter in connected traffic

Previous pass made verified progress. Implemented optional Butterworth RX filtering
as exact continuous modal states, driven by the same RF nonlinear/mixer exponential
terms as the original receive pole. Refactored forcing-term generation so blockers,
TX filter transients and LO disturbances all enter before the selected RX filter.
Topology selection is initial/unenergized-only; incompatible single-pole tuning
is rejected rather than silently changing an unused parameter. Input disconnect
preserves modal charge and homogeneous decay. Default single-pole behavior remains.

Independent normalized SciPy step response agrees within1.04e-15; analytic magnitude,
event subdivision and retained-state decay checks pass. Four sustained runs use
matched multipole ideal references and combined positive-sign20/30MHz blockers.
Corrected waveform error is9.963% in mode0 and11.507% in mode1, versus18.30% and
19.66% with the one-pole receiver. Mode0 barely passes the provisional10% screen;
mode1 fails. These results support selectivity improvement, not overall RF closure.
Ten existing LO cases and four sustained plus six finite blocker regressions pass.

The existing waveform is two-frequency I/Q (different I and Q frequencies); prior
shorthand calling it single-tone was imprecise. It remains far short of a full-band
modulated wanted signal. Modal states are a mathematical realization, not a claim
of physical complex-pole circuitry. Component variation, internal noise/overload,
finite gain-bandwidth and topology-switch state mapping remain future requirements.
Registered new targeted scenario; aggregate remains historical. Mathematical
closure is incomplete and no schematic/layout milestone is claimed.

### Pass 804 — full-band custom multicarrier stimulus in sustained four-path model

Previous pass made verified progress. Added validated source-waveform injection to
the sustained driver and a seeded50-carrier QPSK fixture with312.5kHz spacing,
edge carriers at+/-7.8125MHz,3.2us useful symbol and0.8us cyclic prefix. FFT sizes
128/64 preserve physical symbol timing at40/20MS/s. Independent FFT reconstruction
checks occupied bins; the40MS/s signal decimates exactly to the20MS/s fixture.
Fixed amplitude is never rescaled to hide overload; generated records peak at0.7103
and RMS is approximately0.246. Source length, finite values and converter input
range are checked before encoding. Ideal and impaired runs use identical source
hashes and sample times. Existing default waveform remains unchanged.

Four sustained runs with fifth-order RX filtering include two matched ideal
references and two positive-sign combined20/30MHz blocker cases. All wired/RF
transport and accounting checks pass. Incremental corrected waveform error is
9.154% in mode0 and12.264% in mode1; the latter still fails the provisional10%
screen. The older narrow-waveform failure is therefore not merely an artifact of
that stimulus. Four default sustained regressions also pass.

This custom modulated waveform is not an802.11 packet. The test measures waveform
error relative to a matched filter/quantizer reference, not total constellation EVM
or decoded packet performance. Symbol boundaries/DAC images are not bandlimited;
all carrier occupancy claims refer to useful-symbol bins. One seed and one sign
are not a worst-case envelope. Independent external-source reception, signed
uncertainty, admissible phase/noise budgets and remaining architectural contracts
still need work. Registered scenario; aggregate remains historical.

### Pass 805 — mode1 phase sensitivity including ADC quantization

Previous pass made verified progress. Instrumented ideal pre-ADC analog samples
and allowed an explicitly unquantized matched reference. Existing callers retain
quantized references by default. Ideal code errors are checked against half an
LSB per component; no clipping is hidden in these baseline records. The new
reference still includes intended filtering and DAC codes, so it is not total
transmit-to-modem EVM. Added explicit nonnegative finite phase-scale control;
all other combined impairment settings and the10% budget stay fixed.

Eight sustained mode1 runs cover two signed ideal references and six signed
combined cases with full-band custom QPSK. Against the unquantized reference,
ADC-only corrected RMS error is3.04%. Combined error is4.80/4.96% with the seeded
RF phase trajectory disabled,7.09/7.77% at half phase amplitude, and11.20/11.90%
at original amplitude. Original-amplitude cases are required negative controls
and remain recorded as screen failures. All transport assertions pass.

A conditional model requirement is therefore plausible: under these other
assumptions, the tested independent LO phase sequences bounded to+/-0.075rad
per LO on the100ns grid pass, while+/-0.15rad fails. This does not establish a
universal bound, PSD, jitter specification or physical RF PLL capability. One
waveform/phase seed and finite tests do not close stochastic uncertainty or
independent external-source reception. Continue architecture completion using
explicit assumptions without pretending this is transistor qualification.
Registered scenario; aggregate remains historical. Full mathematical closure
and subsequent schematic/layout milestones remain open.

### Pass 806 — ADC sample-to-valid pipeline and stale-conversion cancellation

Previous pass made verified progress. Added configurable fixed ADC latency and
finite pipeline capacity to the base connected return engine. Sampling/aggregate
reference loading occurs at acquisition; codes reach capture RAM, the valid-word
record and burst encoder only on completion. Last-word padding waits for the last
valid conversion. Existing due conversions complete before coincident sampling;
zero-latency completion precedes coincident host service. The default remains zero
latency for compatibility, while new cases exercise actual nonzero latency.

Capture memory now subscribes to conversion-valid events rather than the sampling
callback. Stop/reset discards pending conversions and accounts for them explicitly;
epoch mismatch at completion is an assertion failure. Capacity exhaustion triggers
quiescence instead of silently delaying sampling. Starting another capture with
pending conversions is rejected. Invalid latency/capacity arguments are rejected.

Six both-mode finite cases verify2.25-period latency, no premature RAM/transport
visibility, full completion, cancellation across an epoch barrier, and overflow
with insufficient capacity. Two sustained simultaneous four-path cases pass with
two-period ADC latency, source/host clock mismatch and bounded queues. Eight RF
return, four management-only, two capture-memory, six memory-rearm, six clock
intervention and return-epoch recovery regressions pass.

Fixed pipeline delay is an architectural contract, not SAR timing closure.
Conversion-dependent delay/metastability, DAC update latency, clipping diagnostics
and both converter reference-load envelopes remain open. Registered targeted
scenario; no fresh aggregate claim. Mathematical closure remains incomplete.

### Pass 807 — DAC consumption versus analog update with simultaneous ADC latency

Previous pass made verified progress. Added finite DAC sample-to-update latency
and bounded pending-update capacity to the shared timed controller. Digital sample
consumption is distinct from the analog held-output transition. The latter evaluates
supply sensitivity at actual update time and propagates continuous filter state
before changing the drive. Played records now identify analog updates; default
zero latency preserves prior timing. Due updates precede coincident consumption
and ADC sampling. Stop/reset cancels pending updates, mutes the held drive and
retains filter decay. Starting playback with pending updates is rejected; capacity
exhaustion faults rather than dropping samples or stretching the clock. Failed gain
validation retains the pending entry rather than losing its accounting record.

Six finite both-mode cases verify2.25-period delay, no premature drive, analytic
filter step response, gain evaluation at completion, overflow and epoch cancellation.
Two sustained simultaneous wired/RF cases pass with1.5-period DAC latency and
two-period ADC latency together. Consumed/updated/cancelled/pending accounting
closes. Six DAC-supply, four playback-memory, existing timed lifecycle and eight
ADC pipeline scenarios pass as regressions. The final DAC pipeline rerun passes.

Fixed update latency is not transistor settling, glitch or code-dependent delay
qualification. Filters continue settling after the update. DAC reference charge,
clipping diagnostics and full converter envelopes remain open. Registered targeted
scenario; aggregate remains historical. Mathematical closure remains incomplete.

### Pass 808 — DAC reference load and shared converter reference coupling

Previous pass made verified progress. Added DAC reference transfer at actual analog
update time. Held output uses the pre-impulse reference span, then an aggregate
clock-plus-code-transition charge impulse loads the selected RC reference. A
coincident ADC sample sees that impulse under the existing DAC-before-ADC ordering.
ADC and DAC can share the modeled reference or use separate identical networks.
DAC reference effects are opt-in so previous default evidence is not silently
reinterpreted. Metrics distinguish DAC update count/charge and shared topology.

Eight sustained both-mode cases cover disabled/zero-load equivalence and shared
versus separate loaded references with both converter latencies active. Shared and
separate cases produce different ADC streams while maintaining exact transport and
converter accounting. Analytic load/recovery, coincident conversion ordering and
subdivision controls pass. An intentionally weak reference collapses below0.1V;
both ADC and DAC then reject conversions as outside the modeled range. Six existing
causal-reference scenarios and eight DAC-pipeline scenarios pass as regressions.

This is an aggregate reference interaction model, not SAR bit loading, DAC glitch
shape, source/sink asymmetry or physical driver qualification. The0.1V limit is an
explicit simulation envelope, not hardware undervoltage telemetry. Complete clipping,
validity and operating-envelope diagnostics remain open. Registered targeted
scenario; aggregate remains historical. Mathematical closure stays incomplete.

### Pass 809 — fresh aggregate verification after RF and converter integration

Previous pass made verified progress. Ran the complete registered architecture
suite to completion: all46 scenarios pass in263.5s, with source hashes
unchanged throughout. Rechecked every recorded source hash and all46 evidence/log
hashes afterward. This replaces the historical pass798 aggregate with a consistent
current snapshot, including multipole/wideband RF, signed phase budgets, converter
latency and shared ADC/DAC reference loading. No regressions were found.

Scenario acceptance includes intentional RF-quality failures and explicit invalid
state controls; it does not assert that every operating condition meets the10%
screen. The mathematical closure inventory still lists incomplete requirements.
Next implementation priority is live wired recovery and disturbances: current RX
recovery is precomputed from a fixed external waveform. Electrical idle/detection,
partial-word reset, wired TX clock ownership, timed control visibility and converter
operating-envelope diagnostics also remain. Do not treat this regression result
as permission to skip mathematical functional closure before schematic/layout.

### Pass 810 — live wired channel, transition recovery and partial-word cancellation

Previous pass made verified progress with a fresh full regression. Added LiveReceiver
and LiveWireChip atop the current RF model. External bit launches, exact one-pole
channel propagation, observed crossings, PI timing updates and analog-sign samples
are processed as events while the chip advances. Only observed crossings enter
feedback; scheduled source bits are external stimulus, not timing oracle inputs.
Recovered words enter bounded return queues only on their actual completion event.
The earlier batch adapter remains as comparator/default for older scenarios.

Eight both-rate/signed-phase/signed-ppm controls match batch recovery and original
words. Two sustained four-path cases pass matched-rate transport and accounting.
A0.03UI/50ppm intervention after20 completed words preserves all data; a1.1UI step
corrupts subsequent data while leaving the completed prefix unchanged. This is an
explicit failure case, not a claim of automatic slip detection. Two integrated
reset cases stop after three payload bits, discard partial framing and verify no
stale words cross the epoch barrier. Search-limit failure now quiesces the chip.

Zero-crossing detection remains ideal and the PI oscillator remains a hypothetical
near-nominal model. Test-marker framing is not Ethernet/PCIe PCS. Whole-chip stop
currently cancels the external stimulus fixture; analog wired channel persistence
across reset/retraining, electrical idle/detection, swing/EQ and supply-to-CDR
coupling are still missing. Registered targeted scenario; pass809 aggregate is
historical after these changes. Full mathematical closure remains incomplete.

### Pass 811 — wired channel continuity through digital reset and retraining

Previous pass made verified progress. Decoupled live receiver enable from channel
launch/crossing events. Reset clears tracking/framing while retaining held input,
analog state and scheduled external transitions. Disabled recovery produces no
samples; the channel continues evolving. An exhausted external fixture explicitly
holds its final symbol. Subsequent incoming training inherits the retained channel
state instead of starting from zero. Intermediate advances propagate analog state
to current chip time even when no discrete channel event occurs.

Four both-rate/signed-initial-phase tests stop after three payload bits, verify
exact state continuity and analytic between-event decay, and confirm external
launches continue with zero disabled samples. Final analog state agrees with an
independent uninterrupted channel. After the epoch barrier, reacquisition and a
new training/marker, only the new words reach host framing. Zero-length RF capture
starts D2H service without RF payload. Existing live/batch comparisons, small/large
clock interventions, partial reset and two sustained four-path regressions pass.

This models digital receiver reset, not analog termination/bias switching. A held
last symbol is not electrical idle. The peer explicitly supplies new test training;
mid-payload restart and protocol-specific training are not implied. Idle/detection,
swing/EQ, supply-to-CDR coupling and slip reporting remain architectural gaps.
Registered targeted scenario; pass809 full-suite record remains historical after
these changes. Full mathematical closure remains open.

### Pass 812 — received-energy electrical-idle detection

Previous pass made verified progress. Added exact integrated squared channel
voltage between events and externally controlled differential source amplitude.
Driving zero retains channel charge and lets it decay; it does not zero the
receiver state. IdleWireChip observes RMS over8UI windows, requires two consecutive
windows below0.15 for idle or above0.25 for presence, and holds state in between.
Thresholds are normalized mathematical assumptions, not protocol voltage limits.
No lack-of-transition heuristic substitutes for received signal energy.

Four both-rate cases distinguish long constant-one payloads from driven idle,
verify retained charge, stop new delivery after idle declaration and keep the
session stopped when signal returns. Two sustained live four-path scenarios pass.
Analytic energy integration and hysteresis controls pass. Four persistent-reset
cases and existing live/batch, disturbance, partial-word and sustained regressions
also pass. Idle detection is integrated with the existing full-session quiescence
policy; independent RF continuity across wired idle remains an explicit gap rather
than a claimed final architectural restriction.

The detector is behavioral, not a physical RMS implementation. Receiver-detect
impedance, common mode, independent engine idle recovery, on-chip programmable
swing/EQ and supply-to-CDR coupling remain open. External source-amplitude control
is not an on-chip transmitter feature. Registered targeted scenario; pass809
aggregate remains historical. Full mathematical closure remains incomplete.

### Pass 813 — wired receive idle no longer stops independent engines

Previous pass made verified progress but exposed full-session idle coupling. Changed
the default idle response to disable only wired RX recovery/framing. RF conversion,
wired TX, pending converter updates and the shared host-frame scheduler continue.
Already completed wired words remain queued in order; only partial reception is
reset. A named idle event records receive generation, partial bits and completed
word count. Signal return only clears the detector indication: explicit host idle
acknowledgement plus new peer training is required to resume reception. This is a
receive-generation change, not a global epoch reset. The legacy full-session policy
remains selectable and its tests request it explicitly.

Both-mode tests interrupt wired RX during RF playback/capture with nonzero ADC/DAC
latencies. Remaining RF samples finish, exact ADC codes reach the host, and wired
TX scheduled after the idle event still sends both requested words. New RX words
arrive only after signal return, explicit acknowledgement and training. Global epoch
and active RF state remain unchanged. Four legacy idle/constant-signal cases and
two sustained live four-path cases pass as regressions.

Named generation/idle events are coherent management abstractions, not allocated
hardware bits or proven CDC timing. Detector latency can admit completed words
before idle declaration; external protocol logic still validates those words.
Receiver-detect impedance, TX swing/EQ, supply effects and slip reporting remain
open. Registered targeted scenario; aggregate remains historical. Full mathematical
closure is incomplete, without restricting the goal to globally coupled engines.

### Pass 814 — host switching impulses affect live wired recovery

Previous pass made verified progress. Added shared-supply impulse callbacks and a
wired phase sensitivity adapter. Both H2D and D2H switching-charge rail steps can
perturb the pending recovered sample. Merged D2H switching deadlines into the live
wire event scheduler so a nested host event cannot retime a wired sample behind
already advanced chip time. Zero sensitivity preserves the baseline RF digest.

The first positive small-coupling run exposed one silently omitted sample: an
external phase jump moved its pending threshold just before current time. Corrected
this to emit at the intervention time; subsequent thresholds remain causal. Added
an explicit tiny-threshold-crossing control with one sample and no omission rather
than reducing coupling to hide the defect. This does not alter the documented
hypothetical phase-jump nature of the model.

Six signed/zero cases plus two baselines pass in both modes with small sensitivities
+/-0.02UI/V and both bus loads active. Four deliberately extreme+/-100UI/V cases
fail framing/transport and are recorded as expected failures, not acceptable
performance. Independent wired idle/retraining and existing live/batch comparisons,
small/large interventions, reset and sustained regressions pass.

Impulse sensitivity is not a continuous supply-to-frequency transfer function,
physical jitter PSD or GF180 tolerance boundary. Automatic reporting of every data
slip, RF LO and wired TX supply coupling and a full package/domain network remain
open. Registered targeted scenario; the full aggregate is historical. Mathematical
closure remains incomplete.

### Pass 815 — declared combined programmable PHY mathematical candidate

Previous pass made verified progress. Added a machine-readable mathematical top
profile and PlatformChip test assembly using the latest live wired and RF blocks.
It combines live transition recovery, received-energy idle policy, both host-bus
switching impulses, independent sample-loop disturbances, seeded RF phase events,
fifth-order RX filtering,20/30MHz blockers, nonlinear/gain/phase/noise impairments,
ADC/DAC pipelines, shared reference loading, matched-rate traffic and a host pause.
A50-carrier QPSK stimulus spans+/-7.8125MHz. The profile records assumed parameters
and explicit missing functions; it does not replace the intended architecture.
The aggregate runner now includes this case and hashes the profile as a source.

Eight runs provide four matched unquantized analog references and four signed
combined cases. All have identical paired source hashes and sample times, exact
wired/RF transport, closed converter accounting, live receiver completion and no
false idle indication. Corrected held-out waveform error is5.785/5.801% in mode0
and6.747/7.464% in mode1. All four meet the unchanged provisional10% screen.
The halved RF phase fixture is the conditional assumption established in pass805,
not a claimed improvement in physical oscillator performance. Earlier failing
conditions remain documented and registered as negative evidence.

This is an integrated executable candidate, not full mathematical closure:
independent external RF reception, autonomous RF/wired-TX clock ownership, TX
swing/EQ and receiver detect, timed hardware control mapping, complete converter
operating diagnostics and broader uncertainty remain open. No schematic/layout
milestone is claimed. Targeted combined verification passed; full aggregate after
recent wired changes remains outstanding.

### Pass 816 — independent external modulated RF reception

Previous pass made verified progress. Added an independent scheduled complex-envelope
source ahead of RX filtering and mixing, continuing while chip control is reset.
It uses a separate seeded50-carrier QPSK sequence at40MS/s and250kHz carrier offset;
local DAC playback is unrelated. Source transitions advance all chip events first,
then change the future drive, retaining filter state. Invalid source timing and
nonfinite waveform values are rejected. Source amplitude holds after exhaustion.

Six both-mode sustained four-path cases use live wired reception, fifth-order RX
filtering and nonzero ADC/DAC latency. Changing local TX data from+0.2 to-0.6 and
its oscillator by3MHz/0.7rad leaves the RX ADC digest identical with coupling disabled.
Changing RX LO phase by0.15rad changes the captured stream. All transport/accounting
checks pass. A reset-state control confirms source events and analog RX evolution
continue without producing ADC words. This exposes the intended directionality
instead of relying only on local external-loopback fixtures.

The held source envelope has sample edges/images; it is not a calibrated RF source
or a modem. Uncoupled TX invariance is a mathematical isolation control, not physical
isolation. Full combined-profile uncertainty, signal quality and source/sample
frequency mismatch still need testing for independent-source reception. The prior
combined profile remains an unchanged loopback candidate with its own open-function
list and evidence hash. Registered new scenario; full aggregate remains outstanding.
Mathematical functional closure and later schematic/layout milestones remain open.

### Pass 817 — explicit ADC clipping and diagnostic lifetime

Previous pass made verified progress. Centralized connected ADC quantization after
frontend/reference effects and added conversion, clipped-sample and signed I/Q
rail counts. Rounding remains nearest with ties to even, matching existing encoded
samples. Clipping means the rounded integer exceeds a code rail; it is distinct
from merely touching a representable rail. Nonfinite values are rejected, including
before frontend/reference mutations. Diagnostic snapshots copy counters; counters
persist across epoch recovery and clear only through a disarmed explicit action.

Four both-format/signed overload cases preserve sample validity and80ns conversion
latency, capture RAM completion and exact host delivery of32 saturated samples.
Random quantizer equivalence and explicit positive/negative half-LSB rail controls
pass. A review caught that an early negative-range shortcut would incorrectly flag
values within half an LSB below-1; restricting the shortcut to far out-of-range
values and adding exact count assertions corrected it. Existing diagnostics, ADC
pipeline and shared-reference regression scenarios pass.

Clipping telemetry is a named model API, not allocated hardware status or per-sample
transport flags. Physical overload recovery, comparator common-mode limits, DAC
analog limits and full reference operating envelopes remain open. No transistor
qualification is implied. Registered targeted scenario; fresh aggregate is still
outstanding after recent additions. Full mathematical closure remains incomplete.

### Pass 818 — bounded management transactions and stale-command fencing

Previous pass made verified progress. Added ManagedChip above the integrated live
wired/external-RF model. Requests occupy four32-bit serial words, cross on the next
control edge plus two cycles, then return four32-bit response words. Queue capacity
includes requests until response visibility. Candidate fields are16-bit header,
16-bit token,32-bit global epoch,32-bit RX generation and32-bit payload. Trace
metadata is not free wire payload. Status uses flags and a saturating16-bit clipping
count. Token exhaustion rejects rather than silently wrapping. This is an explicit
candidate multiword contract, not a claim that existing single-word SPI RTL supports it.

Four both-mode/control-phase cases verify no action before control visibility,
no early reply, immutable status copies, accepted current idle acknowledgement,
and rejection of a queued acknowledgement after RX generation changes. RF capture
and exact32-sample return continue during management transactions. Capacity overflow
and global-epoch stale-write controls pass. Named operations currently cover status,
wired-idle acknowledgement and ADC diagnostic clear. Rejected actions still return
a response so transport does not silently disappear across a chip epoch change.

Full resource/configuration/reset command coverage and four-word hardware assembly,
register allocation and CDC implementation remain open. Delay is a bounded model
assumption, not a metastability probability or physical SPI qualification. Registered
targeted scenario; aggregate remains outstanding. Mathematical closure is incomplete.

### Pass 819 — timed resource configuration and execution-time permissions

Previous pass made verified progress. Extended the128-bit candidate transaction
with a validated32-bit payload for RX route/gain/filter selection, playback writes
and selection, capture enable/read, mode configuration and stop/abort/drain. RX
settings occupy8 bits; playback uses24 sample bits plus5 index bits. Reserved bits
are rejected before resource mutation. RX route selection preserves independent
external-source fixture settings. All operations retain request serialization,
control visibility delay, response delay and epoch/generation fences.

Both-mode tests configure RX settings, load all32 playback words, enable capture,
configure mode, run RF playback/capture, reject armed writes, read captured data
and perform ordered stop/host-abort/drain. Early drain is rejected. An explicit
race queues playback selection while disarmed, arms before delivery, and confirms
the write is rejected without changing selection. Existing timed management,
capacity and stale-epoch scenarios pass as regressions.

The timed adapter now covers concrete resource behavior but remains a candidate
model encoding, not an implemented SPI RTL ABI. Sample scheduling is still an
explicit engine request from the testbench; autonomous continuous/run-length
controls, LO/trim/calibration and complete topology coverage remain open. Registered
targeted scenario; aggregate remains outstanding. Full mathematical closure is
incomplete and schematic/layout stages have not been advanced prematurely.

### Pass 820 — command-driven local converter run with atomic rejection

Previous pass made verified progress. Added start_local to the timed management
adapter. Its32-bit payload declares TX/RX directions,16-bit sample count and14-bit
control-clock start delay. The current local memory path requires32 samples; RX
starts10ns after TX. Direction prerequisites and scheduler/codec construction are
staged before committing either engine, so failure of RX after TX preflight does
not accidentally start playback. Status now includes capture completion.

Both-mode tests program memory and mode via timed transactions and use only the
new command to start local playback/capture. No converter activity precedes command
execution; scheduled starts are in the future. Both32-word streams complete with
nonzero ADC/DAC latency, exact host delivery and closed pipeline accounting. Repeat
start without rearming is rejected. Two busy-RX controls verify that a rejected
combined request leaves pending RX timing and idle TX state unchanged. Existing
timed resource configuration and permission-race regressions pass.

This establishes finite local operation, not arbitrary continuous run/stop or
trigger topology. Those remain explicit gaps alongside LO/trim/calibration and
multiword hardware SPI implementation. Staging depends on existing scheduler methods
assigning fresh objects without advancing shared analog state; the rejection tests
protect this assumption. Registered targeted scenario; full aggregate remains
outstanding and mathematical closure remains incomplete.

### Pass 821 — programmable wired TX swing and postcursor shaping

Previous pass made verified progress. Added normalized TX swing and one-postcursor
shaping to the actual held drive used by the wired channel. Drive is
swing*(current-tap*previous)/(1+tap), retaining previous-symbol history across words.
The fixed-peak normalization bounds output by the selected swing. Candidate settings
are0.25/0.5/1 swing and0/0.25/0.5 tap, applied only while disarmed. Timed management
uses four payload bits; reserved/invalid encodings and armed writes are rejected.
Configuration persists into mode setup instead of merely storing unused fields.

Nine driver controls verify constant-level response, alternating data and peak
bounds. Six both-mode sustained cases preserve exact four-path data with different
swing/shaping choices. Timed configuration permission controls, command-driven local
RF runs and existing timed lifecycle regressions pass. The default1/0 setting
preserves the earlier mathematical driver behavior.

This adds actual TX waveform programmability, not physical voltage/current or pad
qualification. The TX channel still evaluates complete words at word events; bit
clock ownership and mid-word cancellation remain open. A single TX postcursor does
not replace RX equalization/adaptation or receiver-detect modeling. Registered
targeted scenario; full aggregate remains outstanding. Mathematical closure is
still incomplete and no schematic/layout milestone is claimed.

### Pass 822 — actual wired bit events and partial-word cancellation

Previous pass made verified progress. Replaced immediate whole-word TX evaluation
in the shared timed controller with serializer launch and peer-sample events. A
word is fetched at its start but reaches wired_output only at its tenth midpoint
sample. The held shaped drive propagates through persistent channel state between
all events. Reset cancels an unfinished word, mutes drive and preserves analog tail.
New accounting proves fetched words equal completed words plus cancelled partial
words plus the currently serialized word, separately from the queue invariant.

Six rate/shaping controls match the previous word model after actual completion.
Six both-rate cuts at0.25/3.25/9.25UI verify no prematurely completed word, exact
partial cancellation and analytic decay without later sample events. Existing
simultaneous tests incorrectly expected entire words at their first bit; updated
them to require delayed completion and exclude the half-transmitted second word
following reset. A test-only floating endpoint mismatch was removed by using the
same integer-indexed boundary for adjacent words. Timed lifecycle, both converter
pipeline cases and independent wired-idle/RF continuation regressions pass.

The bit oscillator still has a prescribed period rather than autonomous wired TX
PLL dynamics. Midpoint peer sampling is not a recovered receiver. Mode setup still
creates a fresh TX external-channel model; cross-mode analog persistence remains
open. RX equalization, receiver detect and slip reporting also remain gaps.
Registered targeted scenario; a fresh aggregate is required after this core timing
change. Mathematical closure remains incomplete.

### Pass 823 — preserve external TX channel across line-rate reconfiguration

Previous pass made verified progress. Mode setup now carries the external wired TX
channel voltage and absolute pole from the stopped serializer into the new-rate
serializer. It no longer clears channel charge or scales external bandwidth with
the new bit rate. Digital previous-symbol history remains reset; muted drive decays
through reacquisition. Initial configuration retains the original default fixture.

Six cuts across both rate-transition directions preserve exact voltage/pole at the
mode boundary, match analytic muted decay and deliver only new complete words.
Global accounting retains one cancelled partial word plus two later completed words.
The analytic test initially used an ideal decimal timestep instead of the actual
representable timestamp difference; using the actual elapsed interval fixed that
reference mismatch without relaxing tolerance. Existing bit/shaping/partial-reset,
timed lifecycle and command-driven local RF run regressions pass.

This preserves a single-pole external fixture, not physical termination/common-mode
switching or package reflections. Autonomous TX clock acquisition/jitter, RX EQ,
receiver detect and slip reporting remain open. Registered targeted scenario;
fresh aggregate remains outstanding after recent core timing changes. Mathematical
closure remains incomplete and no schematic/layout milestone is claimed.

### Pass 824 — causal receive equalization in live clock/data recovery

Previous pass made verified progress. Added an optional one-zero/one-pole RX
equalizer: output=(1+boost)*channel-boost*lowpass(channel), with DC gain1 and
high-frequency gain1+boost. Exact continuous state propagation feeds both recovered
clock zero crossings and data decisions. Crossing roots use two-exponential segments
split at their analytic extremum, permitting multiple crossings within a held bit.
Default zero boost preserves the raw-channel path. Receiver construction is now an
explicit hook so equalization can be selected without replacing transport behavior.

Twenty channel/boost screens include severe failure controls; the narrowest channel
fails at every tested boost, and0.1-rate bandwidth still does not recover all words.
No universal equalization benefit is claimed. Four both-mode sustained cases with
zero/unit boost pass exact wired/RF transport. Independent midpoint integration and
subdivision validate analog equalizer state. Existing live/batch, disturbance,
partial-word, persistent-reset and sustained receiver regressions pass.

This ideal CTLE model has no internal noise/overload, adaptation or hardware trim
mapping. Equalizer state currently needs explicit preservation when a new receiver
object replaces the old one; channel state alone already persists. Autonomous TX
clock, receiver-detect impedance and slip reporting remain gaps. Registered targeted
scenario; fresh aggregate remains outstanding. Mathematical closure is incomplete.

### Pass 825 — equalizer charge retention and timed programming

Previous pass made verified progress. Added explicit compatible analog-state
inheritance when replacing a live receiver. Raw channel voltage/held input and
absolute bandwidth persist; equalized receivers additionally retain their lowpass
state and pole. Digital framing/timing begins a new acquisition without inventing
a discharged analog path. Shared equalizer controls now serve both test adapters
and ManagedChip; configure_wire_rx selects0/0.5/1/2 boost through the timed payload.
Armed writes and unsupported encodings are rejected. Disarmed gain changes retain
stored state and recompute future crossings when an external frame is still active.

Four both-mode/boost cases prove exact channel/equalizer output continuity at the
new frame boundary and ordered old/new host words. Two timed cases program boost,
reject armed changes, stop/drain, change boost and change mode while retaining
absolute channel/equalizer poles. Timed management, persistent wired reset and
command-driven local-run regressions pass.

Compatible topology retention does not model arbitrary switched-capacitor charge
redistribution, bias switching or physical equalizer noise/overload. Automatic
adaptation/calibration, autonomous TX clock, receiver-detect impedance and slip
reporting remain open. Registered targeted scenario; fresh aggregate is outstanding.
Full mathematical closure and later schematic/layout milestones remain incomplete.

### Pass 826 — verified aggregate and bounded autonomous clock foundation

The previous goal turn made a verified wait on the live aggregate process. Its
existing handle was resumed, not restarted. The baseline completed all62 scenarios
in920.2s with614 unchanged source hashes; every evidence/log hash was independently
checked before subsequent edits. The archived report is
`evidence/connected-architecture-pass826-baseline.json`. It proves that version's
registered regression coverage, not complete architecture or silicon feasibility.

Added an averaged unwrapped phase/frequency detector, PI loop with continuous
anti-windup, and bounded VCO. It represents independent31.25/60/62.5 average divider
ratios for1.25GHz/2.4GHz/2.5GHz. Reference loss holds tuning voltage while oscillator
phase continues; supplied frequency disturbances change phase evolution. The
model rejects impossible positive-frequency envelopes and predicts future phase
crossings without mutating state. Caller must invalidate predictions after a new
disturbance. Fractional divider patterns and stochastic phase noise are absent.

Independent analytic small-signal comparison has maximum phase error1.1e-15
reference cycles. Adaptive RK integration resolves tuning saturation; timestep
refinement over its transition agrees within1e-8 in state coordinates. Six signed
capture cases recover from three reference cycles and4% frequency offset; three
outside-range controls remain unlocked. Holdover drift, reacquisition and192
half-cycle predictions pass. A PLLSerializer adapter drives the existing bit/channel
serializer from actual oscillator phase. Four rate/sign cases deliver18 words each
with mid-word frequency changes and correctly retimed pending sample events.

New scenarios pass and are registered; the full64-scenario aggregate is not rerun.
The full-chip scheduler still uses its previous ideal wired word clock; RF mixers
still use supplied LO events. Next connect independent PLL state, lock gates and
phase-driven word/bit scheduling, then RF mixing and supply response. All tuning,
bandwidth and oscillator-gain numbers remain declared mathematical assumptions.
The full mathematical, transistor-schematic and layout goal remains incomplete.

### Pass 827 — autonomous wired timing in the connected chip

Previous turn made verified implementation progress. AutonomousWireChip now extends
the complete managed RF/wired controller. Its independent bounded PLL gates initial
activation and owns both queued-word consumption and serializer launch/sample phase
crossings. The first word begins on an oscillator edge at or after the requested
start; all subsequent words advance by ten oscillator cycles. Payload pacing must
match the configured common-reference offset. Four sustained cases cover both modes,
reference+/-100ppm and oppositely signed host-service offsets, preserving exact
wired and RF transport through existing finite queues.

Small signed frequency changes retime pending word and bit deadlines causally.
A phase jump past a pending edge faults rather than replaying it; a larger frequency
change loses qualification at a reference observation and quiesces transport.
Outside-range VCO settings never arm the chip. Four simultaneous burst cases,
two fault/reset cases, two timed-management/reference-loss cases and four sustained
cases pass. Mode changes preserve oscillator phase and integral/filter state while
assuming an explicit instantaneous VCO-band/divider reconfiguration. Reference loss
holds tuning voltage while oscillator phase continues after digital shutdown.

Timed configuration commands are merged with PLL reference events so commands which
create a reference schedule cannot leave unseen deadlines in the past. Added narrow
clock-readiness, serializer-factory and word-deadline hooks to the existing lifecycle.
Existing whole-chip, timed DAC/wired, dynamic-lock, sample-edge, local-run and timed
management regressions pass, as do standalone PLL and serializer controls.

Registered65th scenario; no fresh65-scenario aggregate is claimed. The new full-chip
variant does not yet replace the declared combined profile's controller. RF mixers
still need autonomous LO state, and supply-to-frequency coupling, fractional divider
edge patterns and stochastic noise remain open. Parameters remain mathematical
assumptions; schematic and layout milestones remain downstream of full math closure.

### Pass 828 — shared autonomous RF LO in the full-chip model

Previous turn made verified implementation progress. AutonomousRFChip extends the
managed autonomous-wired controller with one independent RF synthesizer feeding
both mixers, matching the block diagram rather than assuming another RF PLL for
TX/RX isolation. Its bounded loop state gates startup, qualifies lock, retains
phase/filter state across wired mode reset and continues in holdover after
reference loss. Existing scheduled ideal LO trajectories are rejected in this
variant; disturbances act on the oscillator state instead.

The envelope mixer/filter model uses continuous linear phase segments with endpoints
predicted from the nonlinear loop. Constant-frequency propagation inside each
segment retains the existing exact analog-filter update. An independent unsaturated
analytic-loop solution and midpoint convolution give waveform errors6.85e-6,
1.90e-6 and4.86e-7 for maximum25/12.5/6.25ns segments. Boundary phase continuity
error remains below1.5e-14rad in those tests. This is numerical convergence, not
an RF noise or silicon-performance measurement.

Four both-mode/sign independent-tone tests expose oscillator disturbances in ADC
samples while shared-LO loopback cancels them. Two outside-tuning-range, timed
configuration and reference-loss/recovery cases pass. Two sustained four-path
cases pass with autonomous RF/wired clocks. Four further cases receive an external
50-carrier waveform through a fifth-order filter and delayed ADC while local DAC
and both wired paths run. Changing uncoupled local TX data/branch phase leaves RX
codes identical, as required by the chosen ideal-isolation control.

The candidate carrier is2.4GHz with an ideal average divider. Supply-to-frequency
coupling, stochastic phase noise, fractional spurs, LO branch mismatch and carrier
programming/calibration remain open. Independent modulated transport is not yet a
complete signal-quality envelope. Registered66th scenario; no fresh66-scenario
aggregate or physical qualification is claimed. Full mathematical closure remains
incomplete, with transistor schematic and layout still downstream.

### Pass 829 — continuous shared-rail oscillator pulling

Previous turn made verified implementation progress. AutonomousPLL now integrates
an exponential frequency-forcing term at every RK stage. A rail update advances
under the previous forcing before installing the new post-switching voltage and
RC decay; copying/predicting oscillator state does not read a mutable future rail.
This preserves phase through impulses and includes recovery between them. Phase
crossing searches use a conservative positive-frequency bound over the entire
future decay, including either sign of sensitivity.

OscillatorSupplyChip connects both host-bus switching callbacks to RF and wired
VCO frequency. Both states are staged before commit; pending wired word/bit edges
are retimed. RF phase propagation splits at return-word events, so a newly arriving
supply impulse cannot retroactively change an earlier mixer segment. Mode changes
retain the current rail tail in the replacement wired oscillator. The rail remains
driven by return-clock transitions after payload completion; it is not forced to
zero merely because a finite data burst has ended.

Six divider/sign analytic holdover cases verify repeated-impulse phase integrals
and edge predictions, with maximum integral error4.4e-11 output cycles. Four signed
both-mode four-path cases pass under assumed1MHz/V sensitivities. Four independent
RF cases show pre-ADC differences around0.002–0.0032 normalized units; one8-bit case
has no changed codes because quantization conceals the analog response. Four extreme
1GHz/V controls correctly fault on wired clock loss. Two lifecycle controls prove
zero-sensitivity equivalence and rail-tail continuity across a mode reset.

PLL, serializer, autonomous wired and autonomous RF regressions all pass after the
forcing/scheduler changes. Registered67th scenario; a fresh67-scenario aggregate
has not run. Lumped RC values and Hz/V sensitivities remain explicit assumptions,
not extracted power distribution or GF180 measurements. Internal switching currents,
separate domains/package coupling, stochastic noise/fractional spurs and combined
wideband RF quality remain open. Mathematical closure and schematic/layout work
remain incomplete.

### Pass 830 — reproducible VCO noise inside autonomous feedback loops

Previous turn made verified implementation progress. Added immutable frequency-noise
spectra with random phases drawn once at construction. Subsequent RK stages, edge
predictions and caller subdivisions query the same absolute-time waveform and consume
no randomness. Noise acts at the VCO before loop suppression, alongside continuous
rail forcing. Positivity guards include the spectrum's worst-case amplitude, and
integration steps resolve its highest line to prevent RK-stage aliasing.

The declared candidate uses eight lines at250kHz intervals through2MHz. Its20kHz
RF frequency RMS is independently recovered over a complete waveform period.
Clock subdivision error is below6e-18 reference cycles, and a single-tone analytic
closed-loop response agrees within2.7e-16 cycles. A high-offset holdover integral
checks numerical alias rejection; that stress frequency is not a physical RF claim.

NoisyOscillatorChip retains separate RF/wired realizations across digital reset.
Both modes pass independent reception with exact repeated-seed ADC results; different
seeds change the pre-ADC waveform and29/16 codes respectively. Both modes pass
four-path traffic with20kHz/10kHz RF/wired frequency RMS plus signed supply sensitivity.
A20MHz RF-noise RMS negative control blocks startup in both tested modes. Standalone
PLL and full supply-clock regression suites pass after the shared-core changes.

This is a finite periodic random-phase spectrum, not infinite-band Gaussian noise
or measured GF180 phase noise. Shared reference/divider/charge-pump noise and actual
fractional-divider edge patterns remain absent. Combined wideband RF quality,
carrier/band controls and other functional gaps still prevent mathematical closure.
Registered68th scenario; the full68-scenario aggregate has not run. Physical noise
calibration belongs to the later schematic/extraction work rather than being
silently inferred from these assumed spectra.

### Pass 831 — counted divider timing and integer-reference candidate

Previous turn made verified implementation progress. Added an exact rational
accumulator divider. At nominal wired rates,40MHz comparison gives31/31/31/32 and
62/63 count sequences with raw feedback-edge peak-to-peak error600ps/200ps. These
are pre-loop count quantization errors, not predictions of transmitted jitter.
Direct oscillator-phase crossings verify every counted edge and exact mean ratio.

IntegerClockChip instead divides the shared reference by four for wired comparison:
10MHz with integer125/250 feedback counts, while the RF PLL remains40MHz/60. Output
rates and external payload pacing remain unchanged. Initial phase and lock
thresholds scale with reference division to retain the same physical time and
relative-frequency tolerances. Eight slower observations deliberately extend lock
qualification. Reference-divider reset counts four future40MHz input edges,
including configuration between input edges; no off-grid reference edge is invented.

Four both-mode/reference-offset cases pass four-path traffic with VCO noise and
supply pulling. Counted noisy feedback edges follow the VCO's actual phase. Two
recovery cases preserve the noise realization across mode reset and test timed
configuration and off-grid reference alignment. Default autonomous wired, PLL and
spectral-noise regressions also pass. Registered69th scenario; the full69-scenario
aggregate has not run.

This is a new connected candidate, not a rewrite of historical combined-profile
results. The loop still uses an averaged detector/filter; finite comparison-rate,
charge-pump and reference-divider propagation effects require further modeling.
The integer choice removes intentional fractional count modulation, not physical
reference noise, VCO noise or implementation jitter. GF180 high-speed divider
feasibility and later schematic/layout work remain unproven; mathematical closure
is still incomplete.

### Pass 832 — finite reference comparison cadence

Previous turn made verified implementation progress. SampledPLL updates detector
error only at reference edges, holds it into the continuous PI filter and preserves
VCO phase between updates. Edge predictions copy detector state and include future
reference updates without consuming real events. SampledClockChip applies this to
both the10MHz wired and40MHz RF comparison clocks, retaining the integer-divider,
spectral-noise and supply-pulling models. Wired detector scheduling is initialized
before reference-state handling, including a new oscillator after mode reset.

An independent exact unsaturated discrete recurrence matches simulated state within
6.3e-14 across tested gains. At1MHz natural frequency its spectral radius is0.556
for10MHz comparison and0.889 for40MHz; both are locally stable. Increasing natural
frequency to3MHz at10MHz comparison gives radius2.395 and fails qualification in
the nonlinear bounded model. This negative control distinguishes finite-rate
stability from the continuously observed baseline.

Both modes pass noisy/supply-coupled four-path traffic, off-grid configuration,
holdover with frozen detector updates and mode recovery. Independent RF captures
respond to a100kHz oscillator disturbance, changing27/9 ADC codes while filter
state and mixer phase remain continuous. Predictive crossings preserve real state
and agree across caller subdivisions. The earlier integer-clock scenario passes
again after the shared factory/scheduling edits. Registered70th scenario; the
full70-scenario aggregate has not run.

Held detector error is still an approximation to physical charge-pump pulses.
Pulse ripple, compliance, extra filter poles, dead zones and shared-reference
noise remain material clock risks, alongside combined wideband RF quality and
frequency-programming controls. These results establish selected mathematical
stability, not GF180 clock feasibility or complete mathematical closure.

### Pass 833 — explicit passive charge-pump filter and pulse limits

Previous turn made verified implementation progress. Added an exact two-capacitor
filter: pump/VCO node shunt capacitance plus a resistor to the slow storage
capacitor. Constant-current intervals propagate both capacitor voltages, charge,
voltage-time integral, source work and resistor loss. Interior voltage extrema
are checked analytically, so compliance peaks cannot disappear between samples.
No capacitor voltage is clipped to fabricate a passing result.

Independent midpoint KCL integration agrees within2e-9V; subdivision error is
below5e-15V and energy residual below3e-28J in the control. Component mapping
preserves the requested low-frequency PI coefficients but introduces an explicit
additional pole. Sixteen both-rate/capacitance/pulse-width cases screen balanced
UP/DOWN pulses under assumed100uA current and a+/-1V control/compliance envelope.
Some choices exceed this envelope even with250ps pulses. At2.5GHz, the50% fast-cap
case stays just within it for5ns pulses, peaking at0.991V, but its extra pole is
about707kHz. That small margin is not a robust design qualification.

Exact local discrete maps expose a material modeling difference: for50% fast
capacitance, held-current error has spectral radius1.076 while impulsive charge
has radius0.901. Finite small pulses converge to the latter linearization. Actual
PFD edge timing must therefore determine stability; the previous ideal-PI result
cannot qualify this physical filter network by itself.

This remains an imposed-pulse filter screen, not a closed-loop PFD implementation.
Out-of-envelope results invalidate constant-current operation, not necessarily the
chip: compliance-limited startup behavior has not been modeled. Next connect actual
reference/feedback-edge pump logic, then evaluate bounded current/compliance and
noise. Registered71st scenario; the full71-scenario aggregate has not run. Full
mathematical closure, transistor schematic and layout remain incomplete.

### Pass 834 — edge-driven PFD around the passive filter

Previous turn made verified implementation progress. EdgePumpPLL now derives actual
integer-feedback edge times from integrated VCO phase. Reference and feedback edges
set UP/DOWN states and reset them when both are present; pump current therefore has
causally determined pulse widths. The passive filter propagates exactly between
transitions. The solver checks the earliest compliance boundary, without clipping
capacitor voltage or extrapolating current beyond its declared range. Coincident
edges are processed together, including cancellation at a boundary.

Controls verify zero pumping for coincident matched clocks, correct first UP/DOWN
pulse timing, global phase/voltage-integral consistency, capacitor charge and energy,
and caller-subdivision invariance. Subdivision differences are below4.3e-13V and
2.2e-11 oscillator cycles. The passive-filter regression also passes.

Forty-four startup cases cover1.25/2.5GHz wired and2.4GHz RF, signed4% free-running
frequency error, two loop-gain settings, capacitor choices and phase offsets.
Outcomes are34 qualified,6 compliance boundaries and4 not qualified by30us. The
500kHz natural-frequency/50% shunt-cap candidate qualifies all12 +/-1ns phase cases
by12.5us with at least74.9mV compliance margin. Expanding to+/-5ns exposes two2.5GHz
compliance-boundary cases. They remain recorded; the passing subset is not a claim
of a complete acquisition basin. Larger capacitance also produces slow-settling
cases, so ripple reduction alone is not a sufficient selection criterion.

This standalone loop assumes zero reset delay, matched ideal pump currents and
noiseless clocks. Boundary termination means its constant-current assumption stops
being valid; it does not prove that a physical compliance-limited pump cannot
recover. Next add that current law, mismatch/reset delay and noise, then connect
the pulse loop to the full-chip controller with the observed longer acquisition
latency. Registered72nd scenario; no fresh72-scenario aggregate or physical
qualification is claimed. Mathematical closure and schematic/layout milestones
remain incomplete.


## Pass 835–836: compliance recovery and programmable converter timing

The standalone pump model now reduces current continuously near the control rails
instead of terminating at the ideal-current compliance boundary. Charge, energy
and integrated phase are accounted for without clipping voltage. All six former
boundary cases recover under this assumed law;40 of44 startup cases qualify and
four miss the30us deadline. This is conditional evidence, not a measured GF180
pump characteristic or full-chip integration.

The connected management path now exposes `configure_local_timing`: a signed16-bit
RX offset in control-clock periods, with upper16 bits reserved. It is writable
only while disarmed and applies to `start_local`; the legacy default remains10ns.
Six both-mode runs exercise negative, zero and positive offsets. Past-start
requests reject atomically without starting TX; reserved encodings and writes
while armed also reject. Prior management, resource and local-run regressions pass.
Control-period timing remains an abstract scheduling contract pending clock-phase
implementation. External triggers and continuous-run control remain unfinished.

There are now74 registered scenarios. No fresh aggregate, complete mathematical
architecture, transistor schematic or layout closure is claimed. Functional
architecture gaps should be closed with explicit assumptions before further
unbounded refinement of individual clock models.


## Pass 837: managed RF carrier tuning

`configure_rf_carrier` now retargets the shared RF synthesizer through the timed
management path, only in reset/disarmed state. The command accepts integer-Hz
values from2.3 to2.5GHz; its divider is referenced to nominal40MHz, so reference
frequency error remains visible. The fixed2.4GHz RF envelope coordinate is retained.
Retuning preserves oscillator phase, filter state, free-running frequency and
noise sources, clears lock qualification and requires reacquisition. Both mixers
continue to follow the same oscillator. The loop gains remain unchanged.

Six both-mode cases at2.32,2.412 and2.48GHz pass command timing, phase/frequency
continuity, reacquisition,32-sample capture transport and armed-write rejection.
Invalid targets reject without mutation. This found and corrected a command-time
ordering issue: the oscillator must advance to execution time under its old
configuration before retargeting. The timed-management regression also passes.

Fractional divider ratios remain ideal averages; these tests do not establish
fractional-N spur/noise quality, a physical VCO range, or independent tuned RF
signal quality.75 scenarios are registered; no fresh aggregate or mathematical
closure is claimed.


## Pass 838: independent tuned RF response

The shared tunable LO is now checked against an independent tone at2.32,2.412
and2.48GHz in both modes. A250kHz baseband offset passes the analytical
single-pole response without fitting received samples. Absolute LO phase is
read once before observation; subsequent amplitude and phase evolution are
predicted independently. Two20MHz-detuned controls exhibit the expected
attenuation, and every case returns64 converter samples without transport loss.
Maximum relative analog error across these cases is2.35e-12.

This is a noiseless single-tone check, not wideband modulation/EVM or concurrent
wired/RF quality. Fractional divider edge timing remains an ideal-average
assumption.76 scenarios are registered; full mathematical closure remains open.


## Pass 839: independent wideband quality with autonomous clocks

`wideband_clock_quality.py` connects an independent50-carrier source to the
sampled-clock full-chip model while all four transport paths operate for32 frames.
A matched comparison uses identical sampling times, external source and fifth-order
filter. The impaired case adds20kHz RF /10kHz wired RMS spectral frequency noise,
1MHz/V oscillator supply pulling,3% I/Q gain/phase settings and0.001 frontend noise.
Both buses switch the shared rail (100fC H2D /50fC D2H per transition); ADC/DAC
latencies remain30ns/20ns. Actual coupling events and external updates are checked.

Held-out incremental waveform errors are3.947% and4.857%, below the provisional10%
screen in both modes. Source metadata, traffic results and supply metrics are saved
in `evidence/connected-wideband-clock-quality.json`. This is not modem EVM: an
initial quarter-record fits one complex gain, and the matched reference includes
the same analog filter. The finite source, short32-frame window and one signed
coupling point do not establish an uncertainty envelope. Blockers, fractional
edge timing and the charge-pump pulse-loop model remain outside this combined test.
77 scenarios are registered; the aggregate and full mathematical closure remain open.


## Pass 840: signed blockers and sampled overload memory

The independent wideband/noisy-clock screen now includes20/30MHz normalized0.1
blockers, signed0.05 cubic RF response and both signs of coupling/IQ errors. All
four cases retain four-path transport and pass the provisional waveform screen:
3.965%,3.939%,4.988%,4.728%. These are finite candidate points, not a complete
uncertainty envelope or physical blocker-power claim.

An optional ADC overload-memory model is connected after reference normalization.
At each sampled decision its bounded signed residual decays exponentially; excess
input beyond+/-1 deposits an input-referred residual for subsequent conversions.
Zero memory remains the default. A200ns decay,0.1 coupling and0.25 residual bound
are sensitivity assumptions. Both signs and modes clip then recover without
reset, preserving all256 returned samples. An analytic exponential check and
invalid-input nonmutation check pass. Actual device recovery and overload between
sampling instants remain unmodeled.

The reference-loaded converter formerly called quantization directly. It now
passes through the common conversion method so recovery cannot be bypassed.
Original clipping tests pass.79 scenarios are registered; full mathematical,
transistor and physical closure remain incomplete.


## Pass 841: discoverable current resource ownership

`resource_count` and `resource_status` now expose six existing engines through
serialized management: paired ADC, paired DAC, capture bank, playback bank, wired
RX and wired TX. Status packs owner in bits7:0, pending/in-flight busy in bit8,
armed/frozen in bit9 and assigned in bit10. Model IDs remain provisional. The
inventory also records named dependencies for review, without implying they are
all physically implemented or dynamically allocatable.

Both modes pass scheduled busy-state snapshots, completion clearing, invalid-ID
rejection and existing playback-versus-host DAC conflict checks. Idle return
frames are deliberately excluded from ADC busy: they continue after conversion
and do not own the converter. Unconfigured discovery handles an absent serializer.
The original timed-management regression passes. Full analog tile/calibration and
diagnostic allocation remain open; this is partial resource discovery, not full
resource-graph closure.80 scenarios are registered; no fresh aggregate is claimed.


## Pass 842: resource shutdown semantics

Both-mode resource tests now stop with ADC/DAC conversions in flight and a live
wired receiver. Each converter cancels one pending conversion, receiver tracking
is disabled, and no stopped engine remains busy. Configuration remains frozen
until host-abort/drain acknowledgement, then becomes writable in reset.

Fixed discovery to exclude a disabled live receiver and to include queued wired
TX words. These are ownership/visibility corrections, not new analog capability.
The80-scenario aggregate is being rerun; its result must be checked before any
new aggregate pass claim. Mathematical closure remains incomplete.


## Pass 843: verified aggregate and receiver-detector promotion

All80 pass842 connected scenarios passed.649 source hashes and all80 evidence/log
hash pairs were independently verified before further model edits; the baseline
is archived as `evidence/connected-architecture-pass842-baseline.json`.

The detector experiments and adapters are now repository modules with reproducible
imports. `receiver_detect_lifecycle.ReceiverDetectChip` exposes the connected
variant; `receiver_detection_screen.py` reruns nine circuit/control screens.
These pass load decisions and counterexamples, persistent-state release/energy,
two-leg sequencing, interrupted rearm, concurrent RF/wired RX, TX exclusion,
reference-loss abort, timed commands, stale fencing and resource interlocks.
The81st aggregate entry registers that composite screen. This is targeted evidence
after the historical80-scenario baseline, not a new81-scenario aggregate.

The detector still does not load the chip supply. The driver-current relationship
must be declared and coupled before claiming electrical coexistence. Small coupling
capacitors remain a known false-absence failure outside the candidate envelope.
Full uncertainty, calibration/resource topology and clock-model integration still
prevent mathematical closure; transistor/layout milestones remain downstream.


## Pass 844: receiver-detect load reaches shared supply and oscillators

`SupplyDetectChip` extends the connected managed detector with an explicit driver
load law: enabled100uA bias plus positive output current, plus10% of sink current.
These are assumptions, not GF180 measurements. Sinking never credits supply charge.
The load feeds the same RC rail and oscillator pulling used for host activity.
A left-endpoint charge approximation is tested at0.5ns and0.25ns intervals.

Both modes retain64 captured RF samples and the present-load detection result.
The refined nominal case draws267pC, peaks at0.951mA supply current, and reaches
84.2mV rail droop/84.2kHz RF pulling for the selected100ohm/100pF rail and1MHz/V
sensitivity. RF analog values change by0.00434; timestep refinement changes them
by about5e-6. Zero-load controls remain undisturbed. These finite cases establish
one-way shared-rail coupling, not full electrical qualification.

The0.2V probe stimulus and detector sensor remain ideal despite rail droop. Their
supply dependence is the next required feedback path.82 scenarios are registered;
the archived80-scenario baseline remains the latest complete aggregate.


## Pass 845: two-way probe stimulus and sensor rail sensitivity

The probe now supports held stimulus gain1+k*rail_delta and a sensed-voltage
additive offset proportional to rail delta. The actual driver current uses the
same held stimulus as the RC circuit, closing the load/rail/stimulus loop rather
than changing only a displayed result. Zero sensitivities retain the prior model.
Tests use signed1/V gain and0.02V/V sensor offset, both modes and the nominal
present load. Finite RF capture, detection, changed source charge and measurement
response pass. Halving the load step and splitting the caller interval137 ways
keep changes below5% of the induced measurement effect. Original one-way supply
and standalone sequence regressions pass.

An important remaining realizability gap was identified: baseline/release checks
currently inspect both simulated capacitor voltages, including the remote node.
That node is not directly observable on the chip. Replace the simulation-state
criterion with a bounded timed/local-sensing policy, then verify its uncertain-load
limits. The current tests are conditional model evidence, not a realizable final
release controller.83 scenarios are registered; the80-scenario archived aggregate
is still the latest full regression.


## Pass 846: realizable local-only release policy

Removed remote capacitor voltage from controller baseline/release decisions.
Release now requires at least4us of active restoration and a chip-side measured
voltage within5mV. The remote state remains available only to circuit validation.
Sixteen candidate load histories pass repeated detection and rearm with safe
observed hidden-state values. A stuck-high local monitor reaches the existing
1ms fault timeout, disables stimulus and publishes no result.

All nine detector circuit/control regressions and both supply/feedback screens
pass with the longer release. This is a realizable observation/timer policy,
not a full settling guarantee: arbitrary initially charged remote networks,
monitor noise/offset, clock tolerance and driver compliance remain unqualified.
84 scenarios are registered; the archived80-scenario run is the latest aggregate.
